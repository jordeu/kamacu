# S02: Agent management UI and per-project selection — UAT

**Milestone:** M001
**Written:** 2026-07-06T17:20:20.047Z

## Human-Verify Gate (T05) — PASSED

User: "both look right, approved"

### Checks run (6-step loop against `./bin/kamacu`, real data):
1. **Settings → Agents section → New agent** — added a custom agent (name + command template) ✓
2. **Set as default** (star control) — exactly-one default, Claude seed lost its star ✓
3. **Project Settings → Agent dropdown** — selected the custom agent, saved ✓
4. **Task → Agent tab → Start** — custom command ran in the worktree terminal, status showed running then exited ✓
5. **Switch project back to Claude** — working/waiting/idle + resume + QuotaIndicator all returned with zero regression ✓
6. **Gate-surfaced fixes re-verified:**
   - Custom agents appear in the Active Sessions bar (running count + row) ✓
   - Claude agent edit dialog carries Extra parameters; standalone global section removed ✓

### Evidence:
- Live binary run (`./bin/kamacu`, `--db ~/.kamacu/kamacu.db`)
- Fake-claude regression suite (`TestAgentLifecycle`) green across every spawn-path change
- Full Go suite + tsc + vite build green throughout

### Verdict: PASS — milestone ready to complete.
