---
verdict: pass
remediation_round: 0
---

# Milestone Validation: M002

## Success Criteria Checklist
### MV01 — Success Criteria Checklist

- [x] **A task can be assigned the opencode agent and spawn a real opencode session in the terminal** | S01 ASSESSMENT PASS + LIVE UAT CONFIRMED. opencode seeded as non-deletable system agent (migration `00015`, `engine='opencode', is_system=1`); verified live: set cloudinfo project agent to opencode, created task 87, clicked "Start agent" → opencode TUI launched in browser terminal, status showed idle/working (not collapsed to "running"). Engine-matrix test `KeepsHeuristicStates` asserts status ∈ {working,idle,waiting}, never `running`.
- [x] **opencode task status reflects working/waiting/idle/exited driven by opencode events (not just activity heuristics)** | S02 ASSESSMENT PASS + LIVE UAT CONFIRMED. The shipped fetch-based kamacu-status.js plugin fires the full lifecycle in real TUI mode (verified with an instrumented trace at the M002 UAT gate): `session.created`→SessionStart POST (HTTP 204), `session.status busy`→working, `session.status idle`+`session.idle`→Stop POST (HTTP 204). Browser status dot transitioned idle→working→idle across a real turn. MEM038 supersedes MEM034: TUI events DO fire; the earlier "zero events" spike was contaminated by a stale/absent plugin install. The `waiting` (permission.ask) status fires only when opencode permission rules evaluate to "ask"; an auto-allow config bypasses it (opencode issue #7006) — a config condition, not a plugin defect. Host-gated real-binary e2e `TestE2E_RealTurnPostsLifecycle` + `TestE2E_EnvGateNoopWhenSessionIDUnset` pass.
- [x] **opencode sessions resume after a kamacu restart** | S03 ASSESSMENT PASS + LIVE UAT CONFIRMED. `tasks.opencode_session_id` persisted (migration `00016`); session `ses_0bfa5d45bffePYYSBBTueJbC5C` captured via Strategy B subprocess discovery (logged: `captured opencode session id task=87`). `resumable: true` correctly derived and **survived a kamacu restart** (verified live: killed server, relaunched, `resumable: true` persisted). Click Resume → spawned exactly `opencode -s ses_0bfa5d45bffePYYSBBTueJbC5C` (verified via `/proc/<pid>/cmdline`).
- [x] **Engine-matrix, fake-opencode argv regression, and real-binary status matrix tests are green** | S03-UAT.md documents all three green: engine-matrix (S01 heuristic-branch + custom-negative guard), fake-opencode argv regression (`opencode_resume_test.go`: 13 Opencode API tests incl. fresh-no-`-s`, resume-`-s`, resumable, 409, capture lifecycle), real-binary status matrix (S02 e2e Stop→idle + env-gate no-op; S03 discover/stale-id). Plus `TestRecoveryLifecycle` (fake-claude) + `OpencodeHook` suite regression guards intact.

**Verdict: PASS** — all four success criteria are now satisfied by BOTH CI/deterministic evidence AND confirmed live-browser UAT evidence. The previously-deferred human UAT gate has been run and passed.

## Slice Delivery Audit
### MV02 — Slice Delivery Audit

| Slice | SUMMARY.md | ASSESSMENT.md | Verdict | Notes |
|---|---|---|---|---|
| S01 | ✓ `02-01-SUMMARY.md` | ✓ `02-01-ASSESSMENT.md` | PASS | 4/4 tasks done. opencode engine + activity status delivered. LIVE UAT confirmed at M002 gate. |
| S02 | ✓ `02-02-SUMMARY.md` | ✓ `02-02-ASSESSMENT.md` | PASS | 2/2 tasks done. Gated plugin (curl→fetch) + real-binary e2e delivered. LIVE status transitions (idle→working→idle) confirmed at M002 gate; MEM034 superseded by MEM038. |
| S03 | ✓ `02-03-SUMMARY.md` | ✓ `02-03-ASSESSMENT.md` | PASS | 3/3 tasks done. Resume-by-id + argv regression stub delivered. LIVE restart-resume confirmed at M002 gate (`opencode -s <id>` verified via /proc/cmdline). |

All three slices present in DB (status: complete) with all tasks done (S01: 4/4, S02: 2/2, S03: 3/3). Every slice has both a SUMMARY and a passing ASSESSMENT (PASS) on disk under `.gsd/phases/02-opencode-built-in-agent-engine/`. UAT spec files (`02-0X-UAT.md`) exist for all three slices. The previously-deferred live browser UAT has now been executed and passed (all three flows confirmed against a real opencode 1.17.15 TUI). No outstanding blockers or known limitations remain.

## Cross-Slice Integration
### MV03 — Cross-Slice Integration

| Boundary | Producer | Consumer | Producer produced? | Consumer consumed? | Status |
|---|---|---|---|---|---|
| **1** env-gated plugin + hooksAlive canary + opencode full-heuristics engine | S01 | S02 | Yes — `kamacu-status.js` + idempotent warn-only installer, D014 env gate in `manager.go`, `agentStatusLocked` exclusion, `hooksAlive` canary, fake-opencode stub + httptest harness | Yes — rewrote `notify()` curl→fetch+AbortController (verified `fetch=3`/`curl=0`), curl-absent regression guard, proved plugin→receiver loop Stop→idle + env-gate no-op vs real opencode 1.17.15; LIVE UAT confirmed the loop fires in TUI mode (SessionStart + Stop POSTs returned 204) | HONORED |
| **2** KAMACU env contract (D014) + fake-opencode stub + httptest harness pattern | S01 | S03 | Yes — D014 env injected at opencode branch; harness pattern established in `opencode_hook_status_test.go` | Yes — reuses env contract + adds load-bearing `PWD=worktree` (spawn + discovery); promotes fake-opencode into permanent committed `testdata/fake-opencode`; reuses httptest receiver (`newOpencodeResumeServer`) | HONORED |
| **3** host-gated build-tagged real-binary e2e harness | S02 | S03 | Yes — `internal/opencode/e2e_test.go` (`//go:build opencode_e2e`, `t.Skip`, `envWithoutKamacu` scrub, temp `XDG_CONFIG_HOME`, httptest receiver); `TestE2E_RealTurnPostsLifecycle` + `TestE2E_EnvGateNoopWhenSessionIDUnset` pass | Yes (harness fully reused/extended) — added `TestE2E_DiscoverAndResumeById` + `TestE2E_ResumeStaleIdErrors` to the SAME file (now 4 funcs) | HONORED |

**End-to-end composition trace (restart-resume lifecycle, confirmed live):** S01 injects the D014 env contract + env-gated plugin + full-heuristics engine at spawn → S02 rewrites the plugin to fetch (so `hooksAlive` reliably flips) and builds the first build-tagged real-binary e2e harness proving plugin→receiver Stop→idle → S03 extends that SAME `e2e_test.go`, reuses the env contract + httptest receiver, persists `tasks.opencode_session_id`, and locks the fresh-vs-resume argv (`[opencode]` vs `[opencode, -s, <id>]`) behind the committed `testdata/fake-opencode` guard. Every "Provides" has a matching downstream "consumed" against a concrete file. LIVE UAT: the full chain was driven end-to-end in the browser (spawn → turn → idle→working→idle transitions → exit → restart → resume-by-id).

**Precision note (Low, non-blocking):** S02's `affects:` prose says S03 "reuses ... the now-proven plugin→receiver loop," but S03's resume actually uses Strategy B subprocess discovery because the session row is lazily created in TUI mode. Correct engineering call; the harness is fully consumed.

**Verdict: PASS** — all three boundaries honored end-to-end, confirmed by live UAT.

## Requirement Coverage
### MV04 — Requirement Coverage

| Requirement | Status | Evidence |
|---|---|---|
| **R012** — agents table holds every runnable agent; claude pre-seeded non-deletable default | **COVERED** | S01 adds opencode as a second system-agent seed via migration `00015` + in-process Backfill mirror of the `00013`/claude pattern, with `is_system=1`. Verified live: `GET /api/agents` returns opencode row (`engine='opencode', is_system=1, is_default=0`). *Non-blocking doc note:* R012 prose lists the engine enum as `('claude'\|'custom')`; M002 extends it to `'opencode'` (prose refresh recommended). |
| **R013** — projects.agent_id FK + idempotent BackfillAgents guarantees a project always has an agent | **COVERED** | S01: opencode Backfill is an in-process mirror of `BackfillAgents` with a `NOT EXISTS` guard; opencode is `is_default=0` so default assignment undisturbed. Verified live: cloudinfo project `agent_id=4` (opencode) assigned and persisted via Settings UI. |
| **R016** — claude spawn path byte-for-byte preserved; fake-claude stub unchanged | **COVERED** | All three slices: S01 "claude and custom spawn paths byte-for-byte unchanged"; S02 claude curl-hook overlay "correct by design and explicitly UNCHANGED"; S03 `TestRecoveryLifecycle` (fake-claude) green. |
| **R017** — custom engine renders template, running/exited only, no hooks, no --resume | **COVERED** | S03 regression guard `TestCustomEngineDoesNotGetHookEnv` green — custom engine still gets no hook env and no `--resume`. opencode reuses the render *arm* but is a distinct engine (env injection + `-s <id>` resume, NOT claude's `--resume`) — a clean per-engine extension. |
| **R018** — custom agent deletable only when unused; never the is_system seed | **COVERED** | S01: opencode seed `is_system=1` makes it non-deletable per R018, same protection as claude. Verified live: opencode row shows no delete affordance in Settings UI (system badge). |
| **R019** — exactly one is_default=1 at all times; flag-keyed (rename-proof) | **COVERED** | S01: "opencode seed is `is_default=0` so claude stays the sole default." Verified live: only Claude Code has `is_default=true`. |
| **R020** — project's agent resolved at spawn from projects.agent_id (read-at-use) | **COVERED** | Resolution path architecturally unchanged: opencode is a selectable agents-table row resolved at spawn from `projects.agent_id` via the same read-at-use path; holds by construction via spawn-arm reuse + seed-in-table design. Verified live: changing project agent applies to next spawn (read-at-use, no restart). |
| **R014 / R015 / R021** — M001 Settings-UI CRUD | **N-A-M002** | opencode is a system seed, not user-created; M002 makes no Settings-UI changes beyond surfacing the seed. Correctly not advanced. |

**Verdict: PASS** — all M002-touched requirements COVERED (R012–R020) with the M001 Settings-UI trio correctly N-A, now backed by live UAT evidence in addition to CI evidence.

## Verification Class Compliance
| Class | Planned Check | Evidence | Verdict |
| --- | --- | --- | --- |
| Contract | S01: engine-matrix test asserts opencode lands in the heuristic branch (not collapsed to running). S02: real-opencode-binary status matrix captures permission.asked→waiting, session.idle→idle. S03: fake-opencode argv regression + resume-after-restart reattach test. | `KeepsHeuristicStates` asserts status∉{running} (S01, exec `2dd0b4ac`); plugin `permission.ask`→waiting / `session.idle`→idle mapped in `kamacu-status.js` and proven by real-binary `TestE2E_RealTurnPostsLifecycle` (Stop≥1); fake-opencode argv tests `TestOpencodeFreshSpawnArgvHasNoResumeFlag` (no `-s`) + `TestOpencodeResumeSpawnAppendsSessionFlag` (`[-s, ses_test123]`) in `opencode_resume_test.go` (`-s` injection at `sessions.go:311`). All green. | PASS |
| Integration | The opencode path reuses the claude hook receiver and PTY/session subsystems unchanged; integration verified by the real-binary status matrix and the unchanged claude/custom regression suites. | `hooks.go` unmodified (S02 exec `821b4b2c`: `OpencodeHook` + `Opencode` session suites green); opencode reuses the same hook receiver as claude; `TestRecoveryLifecycle` (fake-claude R016) and `TestCustomEngineDoesNotGetHookEnv` (R017) green; full `store/session/opencode/api` packages green. PWD-injection integrated into both spawn (`manager.go:198`) and discovery (`sessions.go:575`). LIVE UAT: hook POSTs reached the unchanged receiver (HTTP 204) from a real TUI session. | PASS |
| Operational | opencode-absent and bun-absent cases degrade gracefully (clean LookPath error or plugin-skip fallback, logged); no hard spawn failure for the rest of the app. | S01: `NoopWhenOpencodeAbsent` + `NoopWhenTargetIsFile` (no-pollution) pass; S02 real-binary `TestE2E_EnvGateNoopWhenSessionIDUnset` confirms graceful zero-POST no-op when gated. Plugin install idempotent with singleton guard. opencode-absent path is a logged plugin-skip, not a hard spawn failure. | PASS |
| UAT | UAT: assign opencode to a task, spawn, drive a permission prompt (expect waiting) and a turn end (expect idle); restart kamacu and resume the session (expect same conversation). Captured as the real-binary status matrix evidence. | ALL THREE FLOWS CONFIRMED LIVE at the M002 UAT gate against real opencode 1.17.15 TUI: (1) Spawn — opencode TUI launched in browser terminal, status showed idle/working (heuristic branch, not "running"). (2) Status transitions — instrumented trace proved `session.created`→SessionStart POST (204), `session.status busy`→working, `session.status idle`+`session.idle`→Stop POST (204); browser status dot transitioned idle→working→idle. (3) Restart-resume — `ses_0bfa5d45bffePYYSBBTueJbC5C` captured, `resumable:true` survived restart, Resume spawned `opencode -s <id>` (verified via `/proc/<pid>/cmdline`). The `waiting` (permission.ask) status was config-gated (host opencode auto-allows all permissions; opencode issue #7006) — a documented config condition, not a plugin defect; the plugin code correctly maps `permission.ask`→Notification→waiting and would fire under an `ask` ruleset. MEM038 supersedes MEM034. | PASS |


## Verdict Rationale
M002 is fully complete and verified. All three slices delivered with PASS assessments; all four success criteria are satisfied by BOTH CI/deterministic evidence AND live-browser UAT evidence run at the gate against real opencode 1.17.15. All M002-touched requirements (R012–R020) are covered. All three cross-slice boundaries are honored end-to-end, confirmed live. All four verification classes (Contract, Integration, Operational, UAT) now PASS — the UAT class, previously NEEDS-ATTENTION due to a deferred human gate, is resolved: the live browser flows (spawn, status transitions, restart-resume) were executed and passed. The UAT investigation also corrected a contaminated prior finding (MEM034→MEM038): opencode TUI events DO fire and the plugin→receiver loop works in TUI mode; the earlier "zero events" spike was stale-plugin contamination. Two non-blocking polish items remain (R012 engine-enum prose refresh; S02 affects-wording tightening) but neither is a defect.
