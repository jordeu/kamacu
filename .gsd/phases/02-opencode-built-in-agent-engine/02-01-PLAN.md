# S01: opencode engine spawn and activity-based status

**Goal:** Establish the opencode engine seam: a task assigned the opencode agent spawns a real opencode session in a PTY and reports working/idle/exited via the existing activity heuristics. No waiting yet (that needs the plugin in S02).
**Demo:** After this: create a task with the opencode agent, watch it launch in the terminal, status toggles working/idle as it runs.

## Must-Haves

- Migration 00015 seeds a non-deletable (is_system=1) opencode agent row (engine='opencode', command='opencode', is_default=0); BackfillOpenCodeAgent guarantees it idempotently at startup; claude remains the sole is_default=1 row (R019 invariant intact).
- manager.Spawn injects KAMACU_SESSION_ID=<id>, KAMACU_HOOK_TOKEN=<token>, KAMACU_HOOK_BASE=<baseURL> into the opencode process env (gated to engine=="opencode"); the claude and custom env postures are byte-for-byte unchanged.
- agentStatusLocked() treats engine=='opencode' as a full-heuristics engine (working/waiting/idle), NOT the custom 'running' collapse; the claude and "" back-compat paths are unchanged.
- hooks.go is unchanged (engine-agnostic); the opencode plugin emits only SessionStart/Stop/Notification.
- internal/opencode leaf package embeds kamacu-status.js and idempotently installs it to ${XDG_CONFIG_HOME:-~/.config}/opencode/plugin/ ONLY when the opencode config dir already exists (no pollution for non-opencode users); the plugin no-ops unless KAMACU_SESSION_ID is set.
- Tests pass: opencode-engine session reports a heuristic status (working/idle/waiting), never 'running'; opencode spawn env carries the three KAMACU_* vars (verified via a stub that records env); plugin install is idempotent and content-correct (gating + event names); POSTing Notification/Stop/SessionStart to an opencode session drives waiting/idle/hooksAlive transitions.
- The fake-claude regression suite (internal/session/agent_test.go) and the custom-engine tests (agent_engine_test.go) pass UNCHANGED — no claude/custom regression (R016/R017).

## Proof Level

- This slice proves: integration

## Integration Closure

Upstream surfaces consumed: the agents table + projects.agent_id FK (M001 R012/R013); session.Manager.Spawn engine switch (manager.go ~L156-215); the hook receiver internal/api/hooks.go (POST /api/hooks/sessions/{id}, engine-agnostic); session.go status machine + agentStatusLocked; AgentConfig (BaseURL/Token).
New wiring introduced: migration 00015 + BackfillOpenCodeAgent() called in cmd/kamacu/main.go right after BackfillAgentExtraParams; internal/opencode.InstallPlugin() wired in main.go at startup (writes the plugin only if ~/.config/opencode exists); manager.Spawn opencode env injection (gated on engine=="opencode"); agentStatusLocked gate widened to exclude "opencode".
What remains before the milestone is truly usable end-to-end: a LIVE opencode UAT (real opencode binary in a worktree → plugin loads → curls hook → status dot transitions) which needs opencode installed and is NOT automatable in CI without opencode; deferred to the milestone's UAT gate. The opencode seed appears in the existing agent dropdowns automatically (M001 R021/R022 UI needs no change for SELECTION); a dedicated 'opencode' engine badge label is cosmetic and may be polished in a later slice.

## Verification

- Runtime signals: an opencode-engine session's Info().AgentStatus now reflects hook-driven working/waiting/idle (previously collapsed to 'running' under the custom gate). The hooksAlive canary (flipped by the SessionStart hook POST) is the diagnostic for "the plugin successfully reached the hook receiver" — if it never flips, the plugin/env path is broken.
- Inspection surfaces: GET /api/sessions .status for an opencode task; `SELECT name,engine,command,is_system FROM agents WHERE engine='opencode'`; the managed plugin file at ~/.config/opencode/plugin/kamacu-status.js (carries a kamacu-managed header).
- Failure visibility: a missing/unreachable plugin or unset KAMACU_SESSION_ID leaves the opencode session stuck on 'working' (lastActivity from PTY output) with hooksAlive=false (BEL fallback stays armed) — a future agent localizes this by checking whether status ever reaches 'idle' and whether hooksAlive flipped after spawn.
- Redaction constraints: KAMACU_HOOK_TOKEN is injected via env and posted only to loopback; never logged (slog). Same trust boundary as claude's --settings overlay (which also exposes the token to the spawned process).

<tasks>
- [ ] **T01**: opencode system agent seed (migration + backfill) _(1h)_
  WHY: opencode must be a first-class selectable built-in agent. M001's `agents` table is the single source of truth (R012); a `is_system=1` seed row makes opencode non-deletable (R018) and available in the existing Settings + Project Settings dropdowns with NO UI change. This mirrors the Claude Code seed (migration 00013_agents.sql + BackfillAgents in internal/api/agents_backfill.go).
  - Files: `internal/store/migrations/00015_opencode_agent_seed.sql`, `internal/api/agents_backfill.go`, `internal/api/agents_backfill_test.go`
  - Verify: go test ./internal/api/ -run 'BackfillAgent' && go test ./internal/store/ -run 'Migrate'
- [ ] **T02**: opencode heuristic status branch + engine-matrix test _(2h)_
  WHY: opencode runs in a PTY like claude (D013) but its status hooks come from an on-disk plugin, not a --settings overlay. That plugin needs the Kamacu session id + hook token + base URL via env (D014) to curl back. And agentStatusLocked must treat opencode as a full-heuristics engine (working/waiting/idle), not collapse it to 'running' like custom. These are the ONLY two Go runtime changes (D013: "only agentStatusLocked() gains opencode").
  - Files: `internal/session/session.go`, `internal/session/agent_engine_test.go`
  - Verify: go test ./internal/session/ -run 'Opencode|Engine'
- [ ] **T03**: opencode spawn branch + AgentConfig.OpencodeBin + fake-opencode stub _(2h)_
  WHY: opencode loads status plugins from static files in ~/.config/opencode/plugin/ (verified empirically: the dir is SINGULAR `plugin/`, NOT `plugins/` as D013's prose said — the existing reference plugin slayzone-notify.js lives there). Unlike claude (hook via --settings argv), opencode can't receive a per-instance hook command via argv — the plugin must be a FILE on disk. So Kamacu ships an env-gated plugin that no-ops for non-Kamacu opencode runs and curls the hook receiver with claude-compatible event names (D013/D014). This on-disk counterpart is what makes "activity-based status" actually work.
  - Files: `internal/session/agent.go`, `internal/session/manager.go`, `internal/api/testdata/fake-opencode`, `internal/session/agent_test.go`
  - Verify: go test ./internal/session/ -run 'Opencode' && go build ./...
- [ ] **T04**: sessions.go engine wiring (exempt opencode from custom-render) + integration _(1h)_
  WHY: This is the cross-cutting integration proof. hooks.go is UNCHANGED and engine-agnostic, but we must prove that an opencode-engine session (made full-heuristics by T02) actually transitions working/waiting/idle when the UNCHANGED hook receiver fires — exactly the loop the plugin (T03) drives in production. This closes the Go-side status contract without needing the real opencode binary.
  - Files: `internal/api/sessions.go`, `internal/api/agent_integration_test.go`
  - Verify: go test ./internal/api/ -run 'Opencode' && go test ./internal/api/ -run 'AgentIntegration' && go test ./internal/session/
</tasks>

## Files Likely Touched

- internal/store/migrations/00015_opencode_agent_seed.sql
- internal/api/agents_backfill.go
- internal/api/agents_backfill_test.go
- internal/session/session.go
- internal/session/agent_engine_test.go
- internal/session/agent.go
- internal/session/manager.go
- internal/api/testdata/fake-opencode
- internal/session/agent_test.go
- internal/api/sessions.go
- internal/api/agent_integration_test.go
