# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | v1.0 (Phase 1) | architecture | Single-binary deployment: Go backend embeds the React SPA | One Go process serves the API + embedded static frontend via stdlib net/http ServeMux (Go 1.22+ method/path-wildcard routing) + embed.FS, with an SPA fallback (unknown paths → index.html). No third-party router or framework. | Matches the local-only single-user constraint; embed keeps deployment to one binary; stdlib ServeMux is sufficient for ~15 REST endpoints + 1 WS route. Held across all 10 milestones with zero new router/framework deps. | No | human |
| D002 | v1.0 (Phase 3) | architecture | Git worktree management mechanism | Shell out to system git (git worktree add/remove/list --porcelain) via os/exec. Never use go-git. | git is guaranteed present (the app's premise is local repo checkouts). go-git v5 cannot create linked worktrees; v6 support is alpha-only with commondir bugs. --porcelain output is stable and machine-readable. Every comparable tool (vibe-kanban) shells out. | No | collaborative |
| D003 | v1.0 (Phase 2) | architecture | Terminal transport: PTY + WebSocket framing | creack/pty v1 (import the v1 path — v2 is orphaned) for PTY allocation; coder/websocket for the WebSocket (concurrent-write safe); BINARY frames only (never text — PTY output is arbitrary bytes and UTF-8 validation on text frames corrupts split multi-byte sequences). | coder/websocket avoids gorilla's concurrent-write panic footgun in multi-client fan-out. Binary frames preserve byte fidelity. This was the riskiest subsystem and was proven against plain bash before anything depended on it (v1.0 Phase 2). | No | collaborative |
| D004 | v1.0 (Phase 1) | data | SQLite driver choice | modernc.org/sqlite (pure Go, CGO-free) with goose migrations embedded via embed.FS. Run goose.Up at startup. | Pure Go keeps the single-binary build trivial (CGO_ENABLED=0, easy cross-compile, no gcc in CI). Performance gap vs mattn is irrelevant at single-user localhost load. Do not independently bump modernc.org/libc — let go mod tidy resolve it (mismatches break the driver). | No | collaborative |
| D005 | v1.3 (Phase 10) | pattern | External-tool integration resilience (gh, claude, git) | Degrade-don't-break: every external-tool shell degrades to a safe default and never crashes when the tool is absent or errors. GitHub features are gh-gated (OFF cascade when gh missing); gh shells return safe defaults. Always-200 status endpoints. | A missing/older binary must never break the core local workflow. Held since v1.3 GitHub Foundations; carried through repo-first clone (v1.4), quota proxy (v1.2). Degrade is structural, not a fallback bolted on later. | Yes | collaborative |
| D006 | v1.0 (Phase 3), reinforced v1.3 (Phase 13) and v1.9 | pattern | Destructive operation default posture | Destructive actions default to leave-it-for-the-human: conservative multi-gate auto-cleanup skips on ANY of dirty / unpushed / stash / running-session, runs force=false, and NEVER deletes the git branch. Workspace delete is block-until-empty (never cascade). | Never bulldoze a project (and its worktrees/sessions) or lose uncommitted work as a side effect. Auto-cleanup only touches pristine + idle merged/closed PR worktrees. A destructive action must never be irreversible by accident. | No | collaborative |
| D007 | v1.0 onward, formalized v1.1 | quality | Dependency discipline gate | Zero-new-deps discipline, proven by diffing go.mod and package.json against the previous tag at each milestone gate. Sanctioned deps only (xterm set, dnd-kit, radix primitives). | Held across all 10 milestones — every milestone verified zero new Go modules and zero new npm deps (except sanctioned UI primitives). Keeps the binary lean, the build reproducible, and the attack surface small. The manifest-diff-vs-tag check makes it mechanical proof, not aspiration. | Yes | collaborative |
| D008 | v1.4 (Phases 14–15) | process | Standard delivery shape: backend-capability phase then UI-driver phase | Split a user-facing feature into two phases: (1) prove the engine/backend with tests and a typed wire contract, (2) a thin UI layer over it. Verify the seam with an integration audit rather than assuming it. | Proven across v1.4–v1.9. Backend-first de-risks the integration seam; the UI becomes low-risk; the audit confirms the contract matches rather than hoping. When plans touch the same file (e.g. projects.go), serialize across waves instead of parallelizing. | Yes | collaborative |
| D009 | v1.2 (internal/tmux), v1.3 (internal/github) | architecture | Leaf-package-per-external-tool, extended not scattered | Every verb for an external tool lives in one leaf package (internal/tmux, internal/github, internal/migrate) and is extended there, not scattered as raw shell calls across handlers. Shared services are constructed once in main.go and injected via narrow interface seams (e.g. PRStateGetter) to avoid import cycles. | One cache, one auth context, no divergent instances. The compiler enforces the seam. Adding a sibling capability extends the leaf rather than widening a shared helper (separate-function-over-widen keeps blast radius small). | Yes | collaborative |
| D010 | v1.7 (Phase 18) | pattern | Small fixed lookup sets shared between Go and TS | For a small, fixed, shared lookup (e.g. the project color palette), keep the source of truth in Go and mirror it byte-for-byte as a TS const with a provenance comment — no endpoint round-trip. | Cheaper than an API call and both sides can never disagree (white-text contrast curated in once). Used for the 9-swatch project palette (internal/api/icons.go ↔ web/src/lib/palette.ts). Caveat: needs a drift guard (test/codegen) since nothing currently fails the build on divergence. | Yes | collaborative |
| D011 | v1.1 (Phase 6) | pattern | Settings storage: KV with absent-row-as-default, managers DB-free | Settings live in a SQLite KV store with NO seeded rows — defaults live in code, so an absent row = the default. Managers (session, worktree) take values via SpawnOpts/locals and never import the store. | New settings need no migration; Reset to default is a row delete. Because managers are DB-free, applies-at-next-spawn is structural (not a restart). Read-at-use in handlers keeps the seam clean. | Yes | collaborative |
| D012 | v1.5, strongly re-confirmed v1.7 | process | Human-verify gate authority over design | A human-verify gate is allowed to CHANGE a design, not just bless it. When the gate flips a visual decision, revise the design contract (CONTEXT/UI-SPEC) alongside the code and re-verify against what shipped. For net-new visual primitives, produce a cheap visual mock during ui-phase before building. | Token/spacing review passed layouts the eye rejected in v1.5 (dot-next-to-glyph clash → left-rail redesign) and v1.7 (square→circle, ring→filled-row, palette re-mute that reached back into a shipped phase requiring a 2nd migration). Two straight milestones where the eye overruled the spec made pre-build gestalt mocking a standing rule. | Yes | collaborative |
