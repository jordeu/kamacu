# Kangent

## What This Is

A local-only web app for organizing agent sessions around projects and tasks. A left sidebar lists projects — grouped into switchable named workspaces — each pointing at a local git repo checkout and assigned a configurable agent (Claude Code, opencode, or a custom CLI); the main area is a kanban board of tasks. Clicking a task expands a view with the main agent session (the configured agent CLI running in a PTY, rendered in a browser terminal) plus optional tabs with bash sessions. Single user, runs locally, accessed from a browser at localhost.

## Core Value

One place to see and drive all agent work: every task gets its own isolated worktree and a persistent agent session you can open, leave, and reattach to from the browser.

## Current Milestone: v1.13 Global Task

**Goal:** Give the user a single global scratchpad — a task-like view with agent + bash tabs running directly in a configured repo/folder — for small checks unrelated to any project, without creating a task for it.

**Target features:**
- Single global task: a task-like view with ONLY the agent tab + bash tabs (no description, no diff, no kanban) — not linked to any workspace or project
- Sessions run directly in the configured repo/folder (no worktree, no branch — a true scratchpad)
- Global Settings section to configure the global root (a local folder path, or a GitHub repo cloned like a managed project per the v1.4 pattern) and its default agent (v1.10 agents)
- Reachable from the Active Sessions bar when a session is live; from Settings when idle
- Same session semantics as tasks: persistent server-side PTYs, restart reconciliation/resume, live agent status

## Milestone Status

**Current: v1.13 Global Task — in planning (started 2026-08-25).**

**Shipped: v1.12 Activity & Statistics (2026-08-25).** 4 phases (10–12 + 12.1-inserted), 9 plans, 20 tasks; 21/21 requirements incl. the audit's TD-ACT-01..03 hardening closed by Phase 12.1 (UAT 3/3). Milestone audit `tech_debt` status resolved — the three WR items were the debt. Archived to `milestones/v1.12-*`; tag v1.12.

**Previously shipped:** v1.11 Kamacu MCP Server shipped 2026-07-29 (23/23 requirements; 4 phases, 11 plans; archived to `milestones/v1.11-*`). AGNT-01 fully delivered.

---

_v1.11 Kamacu MCP Server shipped 2026-07-29 (23/23 requirements, 4 phases, 11 plans; archived to `milestones/v1.11-*`). Prior shipped milestones are in the collapsible blocks below._

<details>
<summary>Shipped milestone targets — v1.11 Kamacu MCP Server (2026-07-29)</summary>

**Goal:** Expose Kamacu's task / session / project / workspace / review surface as MCP tools so an agent running inside a Kamacu task PTY can drive the entire app as the user's delegate. Read + subscribe only on terminals — no PTY keystroke injection (write stays browser-only).

**Delivered features:**
- `kamacu mcp serve` stdio MCP subcommand (built on `github.com/modelcontextprotocol/go-sdk` v1.6.1) speaking JSON-RPC over stdio, bridging tool calls to the running Kamacu HTTP API at `127.0.0.1:7333` via an env-driven `*bridge` HTTP client with the `X-Kamacu-Token` header (reusing the existing `KAMACU_HOOK_TOKEN` / `KAMACU_HOOK_BASE` envelope injected in v1.10). `kamacu` is now a `google/subcommands` dispatcher; `kamacu serve` runs the server verbatim.
- 16 CRUD MCP tools (6 task + 5 project + 5 workspace) mirroring every SPA action, each a thin build-path-and-delegate to a shared `bridge.call` helper. Includes the v1.4 managed-checkout atomic create + all-or-nothing gated delete, the v1.9 guarded workspace delete, and cross-workspace project transfer — all gates apply unchanged through the bridge.
- 4 session tools with type-level read-only terminal access: `list_sessions` / `get_session` / `get_session_output` (snapshot, default 4 KB / 512 KiB clamp) / `subscribe_session_output` (bounded live tail, default 30s / hard cap 300s) with proven cancellation-and-no-leak semantics (ctx cancel returns the partial stream + detaches within 2s; goroutine count stable across N cycles). An agent observing a sibling session can never inject PTY bytes — the read-only contract is enforced at the type level (scoped grep gates: zero `WriteInput`/`FrameData` refs, no `internal/session` import in the bridge).
- 3 PR-review tools (`list_pending_reviews` / `list_recently_reviewed` / `open_review`) reusing Kamacu's existing v1.3/v1.5 `internal/github` surface — zero new endpoints, zero new `gh` calls, zero bypass of the existing two-gate ladder.
- A coordinated `X-Kangent-Token` → `X-Kamacu-Token` rename across the source (byte-for-byte security posture preserved) and three small Kamacu endpoint additions (`GET /api/tasks`, `?workspace_id=` on `GET /api/projects`, `GET /api/projects/{id}`) the bridge tools need.

**Settled decisions (milestone-time):** actor = agents inside Kamacu (the AGNT-01 framing expanded from self-report to full delegate); transport = stdio MCP subcommand spawned by the agent CLI (one binary serves both modes, reuses `KAMACU_HOOK_TOKEN` envelope); session I/O = read + subscribe only (write would conflict with the browser-attached user — the read-only contract enforced at the type level, not just by convention); per-resource file split (`internal/mcp/{tasks,projects,workspaces,sessions,reviews}.go`) tracks the resource acted on (D-07); `bridge.call` widened from HTTP 200-only to the full 2xx range so POST→201 and DELETE→204 surface as results. Post-milestone quick tasks (agent delegate surface) reversed two v1.11 Out-of-Scope rules: `start_task_agent` + `send_session_message` (PTY input via MCP, bracketed-paste-wrapped) and `post_pr_review` (GitHub writes via MCP) — these are agent-delegate write primitives only; the browser stays the authoritative interactive surface.

</details>

_v1.10 Configurable Agents shipped 2026-07-11 (18/18 requirements; archived to `milestones/v1.10-*`). Prior shipped milestones are in the collapsible blocks below._

<details>
<summary>Shipped milestone targets — v1.10 Configurable Agents (2026-07-11)</summary>

**Goal:** Make agents first-class configurable data — users define custom agent CLIs in Settings and assign them per-project, with opencode joining Claude Code as a second built-in engine with its own activity-based status and restart-resume.

**Delivered features:**
- `agents` table (migration 00013) + `projects.agent_id` FK + `BackfillAgents` — Claude Code pre-seeded as a non-deletable (`is_system=1`) default; every existing project lands on Claude with zero migration action (mirrors the v1.9 workspaces pattern end-to-end).
- `/api/agents` CRUD + set-default — create/edit/delete custom agents (block-until-unassigned + `is_system` non-deletable), with the claude seed's engine locked (hook/resume plumbing internal) but its name + command editable.
- Spawn engine forked on `agent.engine` — the claude path is byte-for-byte unchanged (fake-claude regression suite green); the custom path renders the command template in the worktree PTY (tokenize-first-then-substitute, `{{worktree}}`/`{{session_id}}` placeholders) with running/exited status only.
- Agent management UI — a Settings Agents section (ordered list + add/edit/delete/set-default) reusing the v1.9 `WorkspaceNameDialog`/`ManageWorkspacesDialog` shape; a per-project agent selector; the Active Sessions bar widened to show custom-agent running sessions; and extra Claude params (`--dangerously-skip-permissions`) consolidated onto the Claude agent's own row (migration 00014 + `BackfillAgentExtraParams`).
- opencode as a second built-in engine — seeded as a non-deletable system agent (migration 00015 + `BackfillOpenCodeAgent`), with its own spawn branch (exempt from the custom-render tokenize path), the `KAMACU_*` hook env injected for activity-based status, and a fetch-based status plugin (`fetch()`+`AbortController(3s)`, replacing a silent-no-op curl shell-out) so waiting/idle unlock reliably. A build-tagged real-opencode e2e harness proves the shipped plugin loads and its POSTs reach a receiver.
- opencode restart-resume — `tasks.opencode_session_id` (nullable, migration 00016) persists the opaque `ses_…` id (opencode has no transcript files); `captureOpencodeSessionAsync` discovers it via an async bounded poll launched at spawn (best-effort, warn-only); and the `--resume` argv closes the restart-resume loop.

**Settled decisions (milestone-time):** the `engine` column encodes a capability tier (`claude`|`custom`|`opencode`) — spawn branches on it, never name-string matching; custom agents report only running/exited (D-M001-2 — custom TUIs can't be reliably introspected), while opencode gets its own engine branch with real heuristics via the plugin; extra Claude params moved from an orphaned global setting onto the agent row itself (configured wholly in one place); opencode session identity is an opaque foreign-minted id discovered via async poll (unlike claude's kamacu-minted id present at spawn).

</details>

_v1.9 Workspaces shipped 2026-07-06 (12/12 requirements, final phase human-verified; archived to `milestones/v1.9-*`). Prior shipped milestones are in the collapsible blocks below._

<details>
<summary>Shipped milestone targets — v1.9 Workspaces (2026-07-06)</summary>

**Goal:** Group projects into named workspaces (e.g. "Personal", "Professional"), switchable from the top of the expanded projects sidebar, while the global Active Sessions bar keeps showing agent work across every workspace.

**Delivered features:**
- `workspaces` table + `projects.workspace_id` `NOT NULL … REFERENCES … ON DELETE RESTRICT` FK (migration 00012), reassigning every existing project to a protected default **Personal** workspace (id=1) in-SQL, with an idempotent `BackfillWorkspaces` startup hook (mirroring `BackfillProjectIcons`) guaranteeing a default always exists — a project can never be workspace-less.
- `/api/workspaces` CRUD — list (name-sorted), create-by-name (case-insensitive dup → 409), rename (Personal included), and a guarded delete keyed off `is_default` + a non-empty `COUNT` guard (ON DELETE RESTRICT backstop).
- Expanded-only sidebar **workspace switcher** (hidden in the collapsed icon rail) with a ✓-marked dropdown + New/Manage entries; a mode-switched create/rename dialog + a Manage-workspaces hub with disabled-delete guards.
- A single `.filter` scopes BOTH sidebar surfaces (expanded rows + collapsed icon rail) to the active workspace; the active workspace persists in `localStorage` (`kamacu.workspace`, `is_default` fallback for a stale id) via one shared `useActiveWorkspace` context mounted in `AppLayout`.
- Workspace-aware routing — the index redirect lands on the active workspace's first project (or a workspace-scoped empty state); a deep-linked project flips the active workspace to its own (URL wins) via a `lastSyncedProjectId`-guarded `BoardWorkspaceSync`.
- **Transfer a project** to another workspace from its existing `⋯` menu ("Move to" submenu, follow-the-project); a newly created project lands in the currently active workspace.
- The global **Active Sessions bar stays cross-workspace** (WSBAR-01 non-regression guardrail — zero workspace refs).

**Settled decisions (milestone-time + UAT):** delete is block-until-empty (never cascade) and keyed off the `is_default` flag (rename-proof), never the literal name "Personal"; project transfer reuses the existing `PATCH /api/projects/{id}` with an optional validated `workspace_id` (no dedicated route, D-17); active workspace is a single shared React context (not a per-component localStorage hook) so the switcher and sidebar filter never desync; workspace stays OUT of the URL (localStorage-only). UAT surfaced one real bug — switching to an EMPTY workspace did nothing because React-Router-v7 wraps `navigate()` in `startTransition`, so the still-mounted old route's `BoardWorkspaceSync` reverted the just-set workspace; fixed by gating reconciliation behind a `lastSyncedProjectId` ref (fires only on a URL-project change, deep-link URL-wins preserved). Workspaces are name-only (no icons/colors) for v1.9 — parked as WSFUT-01.

</details>

_v1.8 Kamacu Rebrand & UX Polish shipped 2026-07-04 (audited, archived to `milestones/v1.8-*`). Prior shipped milestones are in the collapsible blocks below._

<details>
<summary>Shipped milestone targets — v1.8 Kamacu Rebrand & UX Polish (2026-07-04)</summary>

**Goal:** Rebrand kangent → Kamacu (full rename incl. a one-time, gated on-disk migration of `~/.kangent` → `~/.kamacu`) and sharpen the daily-driver UX — GitHub-style diff review, forceable worktree cleanup, tab renaming, and session-bar / board refinements.

**Delivered features:**
- Full rename kangent → kamacu across UI/brand, binary, Go module, and packages (Phase 20), plus a gated startup migration of the `~/.kangent` → `~/.kamacu` data dir — git worktree repair, DB worktree-path rewrite, tmux socket `-L kangent` → `-L kamacu` + `kangent-*` → `kamacu-*` session prefix, and `kangent.*` → `kamacu.*` localStorage keys — so live worktrees/sessions/DB survive (Phase 21)
- New Kamacu logo + favicon + shareable repo-root README.md (Phase 20)
- GitHub "Files changed"-style diff view: left file tree, collapsible per-file diffs, and a persistent per-file "Viewed" checkbox that resets + re-expands only when a file changes (Phase 22)
- Settings worktree-cleanup panel: a cleanup-candidate list (orphan/stale/finished; active work hidden) with per-item force-remove (D-01 blocked-dir degrade) + a never-force bulk "clean eligible" action (Phase 23)
- Task-view three-dots menu (Stop + Insert review prompt + Insert description) replacing the Stop button, with the PR review prompt no longer auto-inserted (Phase 24)
- Rename bash/tmux tabs (custom label persists across reopen and server restart; auto-names Bash 1/2… kept as defaults) (Phase 24)
- Session-bar / board polish: remove the total-sessions count, auto-collapse the bottom bar on outside click, remove the To Do "+ New task" shortcut (Phase 24)

**Settled decisions (milestone-time + UAT):** the rebrand kept runtime data paths untouched in Phase 20 (a `kamacu` binary reading `~/.kangent`) with the on-disk flip deferred to Phase 21's idempotent, recoverable gated migration; the diff view adopted GitHub-style stacking sticky headers + a non-sticky flow anchor for tree-jump + a viewport-pinned shell (UAT-directed); WTREE-01 was refined post-approval from "list every worktree" to a cleanup queue (orphan/stale/finished only); and a renamed tmux tab's label survives a server restart (reattach carries the stored label and reapplies it before the back-fill).

</details>

_v1.7 Project Icons in Collapsed Sidebar shipped 2026-06-19 (archived to `milestones/v1.7-*`). See the Validated requirements and Current State below for what shipped._

<details>
<summary>Shipped milestone targets — v1.7 Project Icons in Collapsed Sidebar (2026-06-19)</summary>

**Goal:** Give every project a colored monogram avatar (background color + two uppercase letters) so projects are identifiable and switchable directly from the collapsed sidebar — no expand needed.

**Delivered features:**
- Collapsed sidebar became a thin 3rem rail of clickable circular project avatars (color + 2 letters); clicking switches project (previously the offcanvas sidebar slid fully off-screen, leaving no project reference). The floating re-expand trigger was retired.
- The same monogram avatar appears inline beside the project name in the expanded sidebar (consistent identity in both states), with the existing waiting-count chip preserved.
- Default letters derive from the name (word initials, else first two — "My Cool App" → MC, "kangent" → KA); editable in Project settings via a normalized ≤2-uppercase "Initials" input.
- Default color is assigned at random from a curated dark-theme-friendly palette; edited by picking a preset swatch (9-swatch grid, current color ring+check-marked) with a live avatar preview.
- Collapsed icons keep the amber waiting indicator (overlay badge) and show the full project name on hover (tooltip); the active project's row is highlighted.
- New `projects.icon_letters` + `projects.icon_color` columns (migration 00009) with an idempotent startup backfill; existing projects backfilled with derived letters + an assigned palette color.

**Settled decisions (milestone-time + UAT):**
- One curated palette is the single source of truth, defined in Go (`internal/api/icons.go projectPalette`) and mirrored byte-for-byte in TS (`web/src/lib/palette.ts`); used by both the random-default assignment and the settings swatch picker. **UAT re-muted it** — the bright Tailwind-600 hues were replaced by 9 muted desaturated hues (still legible white text), changing both sources plus migration `00010_muted_palette.sql` to remap existing rows by palette position.
- One shared `<ProjectAvatar>` primitive renders in BOTH the collapsed rail and inline when expanded (rail|inline sizes).
- **UAT reversed two planned visuals:** avatars are circles, not `rounded-md` squares (reversed D-04); the active project is shown by a filled rail-row highlight (same treatment as expanded selection), not a ring on the avatar (reversed D-06).
- Mostly a frontend milestone: the only backend work was the two columns + migration/backfill and extending the partial-PATCH path to validate + persist both fields. Add-project (+) and Settings (gear) were NOT added to the collapsed rail (deferred, ICON-FUT-05).

</details>

_v1.6 Global Active Sessions Bar shipped 2026-06-18 (audited passed, archived to `milestones/v1.6-*`). See the Validated requirements and Current State below for what shipped._

<details>
<summary>Shipped milestone targets — v1.6 Global Active Sessions Bar (2026-06-18)</summary>

**Goal:** A persistent bottom status bar that shows every active Claude agent session across all projects at a glance — collapsed for global stats, expanded to browse and jump straight into any session.

**Delivered features:**
- Persistent **bottom status bar** (`ActiveSessionsBar`, mounted in `AppLayout` outside `<Outlet/>`) present app-wide on every route.
- Tracks **agent sessions only** (working / waiting / idle; PR-review agent sessions included), driven by the existing 5s `/api/agents/status` poll widened with task title + project name (JOIN on both passes, no new endpoint, no migration).
- **Collapsed (default):** per-state colored counts (reused `dotMeta()`) + total, with the waiting count amber + pulsing only when > 0; quiet "No active sessions" zero state.
- **Expanded:** a flat attention-first list (waiting→working→idle), each row `project · task/PR title · state` (PR rows get a `#n` badge), current-task row highlighted; the panel floats UP over content as an overlay so the xterm terminals never reflow; clicking a row jumps into that task's full-page agent view (cross-project).
- **In-bar alerting only** (pulsing-amber + count) — no browser/OS notifications, no tab-title/favicon changes.
- Additive: the per-project sidebar "N waiting" chips stay; collapse/expand persists (localStorage), default collapsed; live-only (exited drops off).

**Settled decisions (milestone-time):** placement = bottom bar; agent sessions only; in-bar alerting only (browser notifications deferred to SBAR-FUT-03); expand overlays-up (not push, chosen to protect terminal layout — D-03); flat attention-first list (grouping deferred); the sole backend change is a JOIN onto the existing query (no migration). Audit passed (10/10 requirements, 6/6 integration seams, 1/1 E2E flow).

</details>

_v1.5 Sharper Review Column shipped 2026-06-17 (audited, archived to `milestones/v1.5-*`). Shipped-milestone history is in the collapsible blocks below._

<details>
<summary>Shipped milestone targets — v1.5 Sharper Review Column (2026-06-17)</summary>

**Goal:** Make the Review column convey two independent signals at a glance — your agent's state and the PR's CI state — and stop losing sight of PRs after you review them.

**Delivered features:**
- **Agent state on PR cards** — a colored **left rail** (green working / pulsing-amber waiting / blue idle / gray exited) marks any PR with an open review session; redesigned from a `StatusDot` dot to a rail at the human-verify gate so it never clashes with the CI mark.
- **CI status as icons** — bare glyphs: green check (passing) / red cross (failing) / static amber circle (running); no-checks (`none`) renders nothing.
- **"Recently reviewed" section** — open PRs you've reviewed (`reviewed-by:@me`, approve OR request-changes) stay visible until merged/closed; the top section remains "awaiting your review"; a PR appears in only one section (top wins; server-side deduped); quietly omitted when empty.

**Settled decisions (milestone-time):**
- Two searches drive the two sections (`user-review-requested:@me draft:false` + `reviewed-by:@me`) from ONE cached `gh` cycle, server-side top-precedence deduped; `--state open` bounds the reviewed list (drops on merge/close) — no separate time window.
- Agent state is a **left rail, not a dot** (user-directed redesign at the human-verify gate): the two signals split onto separate visual channels (left edge = your agent, right glyph = the PR's CI).
- All-internal milestone: reuses `internal/github` query/cache, the `source='github_pr'` task↔PR link + `/api/agents/status`, and the reaper's merge/close detection. No new external surface beyond the second `gh pr list` search.

</details>

**v1.4 Repo-First Projects — shipped 2026-06-15.** See the Validated requirements and Current State below for what v1.4 delivered.

<details>
<summary>Shipped milestone targets — v1.4 Repo-First Projects (2026-06-15)</summary>

**Goal:** When GitHub integration is on, add a project by naming a GitHub repo — Kangent clones and manages the checkout under `~/.kangent` on the default branch — with the folder path as the optional fallback.

**Delivered features:**
- Repo-first "Add project" when GitHub is on: enter `owner/name` → Kangent `gh repo clone`s it into `~/.kangent/repos/<owner>/<name>` on the repo's auto-detected default branch (main/master).
- Project name auto-derived from the repo (prefilled, editable); GitHub link auto-set + description auto-captured.
- Folder path becomes the optional alternative when GitHub is on; folder-only when GitHub is off — existing folder-based projects untouched (backward compatible).
- Task (and PR-review) worktrees branch off the managed checkout; best-effort fetch of the latest default branch before creating each new task worktree.
- Deleting a managed-checkout project does an all-or-nothing gated removal of the clone (dirty / unpushed / stash / running-session gates, like worktree cleanup); folder-based project dirs are never touched.
- Degrade-don't-break: clone failures (auth/network/bad repo) surface inline without leaving a half-created project; re-adding an existing managed dir reattaches instead of failing.

**Settled decisions (milestone-time):**
- Repo cloning uses `gh repo clone` (host auth, so private/org repos work) into an `owner/name`-namespaced dir under `~/.kangent/repos/` — collision-safe.
- The managed clone IS the repo root that task/PR-review worktrees branch off (the user just never picks a folder); the `managed` schema marker (migration 00008) distinguishes Kangent-managed checkouts from user-pointed folders so delete knows what it owns.
- GitHub-only for the repo path (consistent with v1.3); arbitrary git URLs / other forges stay out of scope — the folder is the non-GitHub escape hatch.
- Built on v1.3's `internal/github` (gh auth, `ParseRepoRef`/`ValidateRepo`, repo link) and the existing worktree/cleanup gating.

</details>

<details>
<summary>Shipped milestone targets — v1.3 GitHub PR Review (2026-06-14)</summary>

**Goal:** Surface the GitHub pull requests that need your review on a linked project's board, and open each as a full task-like review workspace — a worktree checked out on the PR's branch, with the same agent session, bash tabs, and diff as a normal task.

**Delivered features:**
- Global GitHub integration toggle in settings (gh-gated; default on when `gh` present, OFF + un-enableable when absent); when off, all GitHub UI disappears
- Per-project config section: optional short description + a linked GitHub repository
- Collapsible "Review" column on the right of a linked project's board, listing open PRs where review is requested from you, fetched via `gh`, auto-polled (paused when tab hidden) + manual refresh
- PR cards rendered like task cards; the list syncs live from GitHub (cards appear/disappear as review state changes)
- Clicking a PR opens a task-like view backed by a worktree on the PR's real head branch (fetch `refs/pull/<n>/head`, named branch with `pr/<n>` collision fallback — **not** `gh pr checkout`, which the research ruled out as not worktree-aware)
- A review's worktree is auto-removed when its PR merges/closes (gated on dirty/unpushed/stash/running-session; branch kept)

**Settled decisions (milestone-time):**
- GitHub access is via the `gh` CLI (already authenticated on host) — no tokens stored; mirrors how Kangent shells out to git/claude. `gh` is a soft dependency: the integration is best-effort (like the quota indicator) and degrades gracefully when `gh` is absent or unauthenticated.
- PR reviews are a GitHub-synced list, not kanban tasks — they never enter To Do/In Progress/Done.
- No in-app GitHub write actions (approve/request-changes/comment/merge) — done in the terminal, preserving the manual-git philosophy.
- Worktrees CAN be auto-removed on PR merge/close — a deliberate, gated exception to the prior "worktrees never auto-removed" rule.

</details>

## Requirements

### Validated

- ✓ Projects sidebar — create/list/rename/delete projects pointing at local git repos, with path validation — Phase 1
- ✓ Kanban board per project with fixed columns (To Do / In Progress / In Review / Done), drag-and-drop with persisted manual order — Phase 1
- ✓ Tasks with title, markdown description, status; full CRUD and full-page task view with extensible tab strip — Phase 1
- ✓ All projects/tasks stored in local SQLite; single Go binary serves embedded SPA at localhost — Phase 1
- ✓ Real PTY-backed terminal in the browser: full interactive TUI, resize/scrollback/copy-paste, server-owned sessions that survive tab closes with replay on reattach, full-process-tree stop, Origin/Host-validated WebSocket with loopback-only binding — Phase 2
- ✓ Worktree-per-task isolation: task creation auto-creates `task/<slug>-<id>` branch + worktree under `~/.kangent/worktrees/`; bash session tabs run inside the worktree; confirmed cleanup with dirty-tree and running-session gates, branch always kept — Phase 3
- ✓ Claude Code agent sessions: explicit Start button spawns real `claude` in the worktree PTY (full TUI, inherits user settings, invisible additive status hooks, deterministic session IDs); live status dots (working/waiting/idle/exited) on cards, tab, and sidebar waiting chips; dimmed exited state with Reset session — Phase 4
- ✓ Server-side session persistence: sessions keep running when the browser tab closes and reattach with replay (Phase 2); after a server restart, silent reconciliation leaves no ghosts and the agent offers Resume session via `claude --resume <uuid>` from the persisted session ID — Phase 5
- ✓ Read-only diff tab: review everything a task changed vs the base branch merge-base (committed + uncommitted + untracked), collapsible unified diffs — Phase 5
- ✓ Global settings page (sidebar gear → full-page route, SQLite-backed, served over the API) with per-field commit, reset-to-default, and inline validation — Phase 6
- ✓ Configurable claude extra-params, pre-filled with `--dangerously-skip-permissions` (removable; applies at next spawn, running sessions unaffected) — Phase 6
- ✓ Configurable worktree base location (new worktrees only; existing stay put) — Phase 6
- ✓ Configurable bash-tab shell (dropdown, bash-only for now; bash de-hardcoded) — Phase 6
- ✓ Configurable branch-name token template (default `task/{slug}-{id}`, validated to a legal ref at save and create time) — Phase 6
- ✓ Task-view header layout spans full page width (title row + three-dots actions menu) — Phase 6
- ✓ Claude quota indicator: compact "Claude 5h" trigger + threshold-colored usage bar (zinc/amber/red, D-69) in the top-right of board and task views — Phase 7
- ✓ Quota hover popup with one row per server-reported window (5h, 7d, per-model): equal-width bars, rounded %, compact ticking reset countdowns, "Updated Xs ago" footer with cache-bypassing manual refresh — Phase 7
- ✓ Background quota auto-poll (60s, paused when tab hidden) backed by a cached, backoff-protected, token-keyed server proxy of the OAuth usage endpoint that never writes credentials and degrades without breaking — Phase 7
- ✓ "tmux" offered in the global shell setting dropdown only when `tmux` resolves on PATH (call-time LookPath, one truth shared by validation + options) — Phase 8
- ✓ Invisible tmux-backed bash tabs: spawn attach-or-create `kangent-<task>-<n>` sessions on a dedicated `-L kangent` socket (status off, mouse on), indistinguishable from plain bash tabs; × kills (kill-on-close parity), sessions survive leaving the task view and reattach on reopen; exit-vs-detach discriminated via `tmux has-session`; plain-bash and agent stop paths untouched (regression-guarded) — Phase 8
- ✓ tmux sessions survive a Kangent server restart and reattach automatically and invisibly when the task is reopened — no Resume button (DB-derived ghost entry gated by `has-session` → `new-session -A`); dead-on-restart sessions vanish quietly with lazy row GC — Phase 9
- ✓ Cleanup/delete account for tmux: live sessions fold into the single "N sessions running" count and are killed before worktree removal on both cleanup and task-delete; a once-at-startup orphan sweep kills any `kangent-*` session with no DB row (reconciles offline deletes) — Phase 9
- ✓ Done-TTL reaper: a background ticker kills sessions (bash, tmux, AND agent) of tasks in Done past a configurable TTL (`done_session_ttl`, default 24h from entering Done, `0`/`never` disables); the agent stays resumable (PTY killed, transcript kept); worktrees never auto-removed; new per-status timestamps (`todo_at`/`in_progress_at`/`in_review_at`/`done_at`) bank future cycle-time stats — Phase 9
- ✓ Global GitHub integration toggle (`github_integration` settings KV) with a full OFF cascade hiding all GitHub UI app-wide; **gh-gated**: when `gh` is absent the toggle defaults OFF and refuses to enable, surfacing "Install the GitHub CLI (gh) before enabling GitHub integration." (`GET /api/github/status` → `gh_available`); when `gh` is present it defaults on — Phase 10 (GHSET-01/02/03)
- ✓ Per-project config: optional short description (280 cap) + linked GitHub repo via Project settings dialog (⋯ menu), origin-prefilled and soft-validated through the degrade-don't-break `internal/github` leaf package (`gh` canonicalization with syntactic fallback) — Phase 10 (GHPRJ-01/02/03)
- ✓ Schema foundation for the whole milestone: migration 00007 adds `projects.description`/`github_repo` and `tasks.source`/`pr_number`/`pr_base_ref` (so Phases 11–13 need no further migration) — Phase 10
- ✓ Collapsible per-project "Review" column listing `user-review-requested:@me draft:false` open PRs via one cached `gh pr list` call (server-side `statusCheckRollup` → pass/fail/pending/none), 60s visibility-paused auto-poll + manual refresh, inline loading/empty/degraded states, default-collapsed, never blocking the board — Phase 11 (GHCOL-01..06)
- ✓ Open-a-Review: clicking a PR card opens a task-like review workspace (`source='github_pr'` task reusing TaskPage/agent/bash/diff) backed by a worktree on the PR's real head branch (fetch `refs/pull/<n>/head`, `pr/<n>` collision fallback, primary checkout HEAD provably unchanged); open-or-reattach (no duplicates); diff vs the PR's own base merge-base matching GitHub; PR reviews never leak onto the kanban board — Phase 12 (GHREV-01..05)
- ✓ PR worktree auto-cleanup: the reaper reconciles PR state (`gh pr view --json state`) and gated-removes a merged/closed worktree only when pristine + idle (dirty/unpushed/stash/session all skip), always keeping the branch; manual cleanup from the review view via the same gated `CleanupWorktreeGated` flow + a merged/closed banner — Phase 13 (GHCLN-01/02/03)
- ✓ Managed-checkout backend: `github.Clone` (`gh repo clone`, exit-0-only) provisions a gh-validated repo into `~/.kangent/repos/<owner>/<name>` recorded as the project repo root with a `managed` marker (migration 00008, existing folder rows backfill to never-touch); atomic clone-then-create (failed clone → no row, no dir); reattach-on-origin-match / error-on-mismatch — Phase 14 (CKOUT-01, RPROJ-05, CKOUT-05)
- ✓ Managed-checkout worktree freshness + gated delete: managed task/PR-review worktrees best-effort `fetch origin <default>` before `ResolveBase` (folder projects keep the D-24 no-network path); deleting a managed project is all-or-nothing gated (dirty/unpushed via `origin/<default>..HEAD`/stash/session across every worktree + the clone root → 409 reason list, removes nothing) then unlinks worktrees → `os.RemoveAll` clone → deletes row; folder-project delete byte-for-byte unchanged — Phase 14 (CKOUT-02, CKOUT-03)
- ✓ Repo-first Add-project flow: when GitHub is on, the "Add project" dialog shows a segmented "GitHub repo | Local folder" toggle (repo default, reusing `ui/tabs.tsx`); the `owner/name` input prefills the editable project name, submit drives the Phase-14 atomic create with a blocking "Cloning…" spinner, clone failures surface inline (dialog stays open, values preserved, no half-created project), and the GitHub repo description is auto-captured into the project. When GitHub is off, the dialog is byte-for-byte the pre-v1.4 folder-only form — Phase 15 (RPROJ-01/02/03/04, CKOUT-04)

- ✓ Sharper Review column — agent session state and CI state as two independent at-a-glance signals per PR: agent state is an always-on colored **left rail** on the card (green working / pulsing-amber waiting / blue idle / gray exited — redesigned from a `StatusDot` to a rail at the human-verify gate so it never clashes with the CI mark), CI state is a bare glyph (green `Check` / red `X` / static amber `Circle`; no-checks renders nothing). Two separate channels — left edge = your agent, right glyph = the PR's CI — Phase 16 (SIGNL-01/02/03, CHECK-01/02)
- ✓ "Recently reviewed" section in the Review column — open PRs you've reviewed (`reviewed-by:@me draft:false`, approve OR request-changes) stay visible until merged/closed, riding one cached `gh` cycle, server-side deduped against the awaiting-review list (top precedence), quietly omitted when empty — Phase 16 (REVWD-01/02/03/04)

- ✓ Global Active Sessions Bar — a persistent bottom bar (`ActiveSessionsBar`, mounted in `AppLayout` outside `<Outlet/>`, every route) showing every LIVE agent session across all projects. Collapsed (default) shows per-state colored counts (working/waiting/idle via reused `dotMeta()`) + total with the waiting count amber + pulsing; expanded floats a panel UP over content (overlay — never reflows the xterm terminals) listing sessions attention-first (waiting→working→idle), each row `project · task/PR title` (PR rows get a `#n` badge), the current task's row highlighted, click → that task's agent view (cross-project). Live-only (exited drops off); quiet "No active sessions" zero state; collapse persists in `localStorage` (default collapsed). Driven by the existing 5s `/api/agents/status` poll, widened with `taskTitle`+`projectName` via a JOIN on both passes (no new endpoint, no migration); per-project sidebar waiting chips kept (additive) — Phase 17 (SBAR-01..SBAR-10)

- ✓ Project monogram avatars + collapsed-sidebar rail — every project has a colored two-letter avatar (`projects.icon_letters` + `icon_color`, migration 00009, idempotent startup backfill so pre-existing projects are never blank; letters auto-derived from the name, color a random curated-palette pick). One shared `<ProjectAvatar>` renders both as a clickable collapsed-rail icon (`collapsible="icon"` 3rem rail, filled-row active highlight, name tooltip, amber waiting badge — previously the offcanvas sidebar slid fully off-screen) and inline beside the name when expanded (waiting-count chip preserved). Editable in Project settings: a normalized ≤2-uppercase "Initials" input + a 9-swatch palette grid with live preview, wired into the existing conditional PATCH (sent only when changed). Palette is one Go source of truth (`internal/api/icons.go`) mirrored byte-for-byte in TS (`web/src/lib/palette.ts`). **UAT-revised contract:** circular avatars (reversed D-04), rail-row active highlight not a ring (reversed D-06), and a muted desaturated palette replacing the bright Tailwind-600 hues (migration 00010 remapped existing rows) — Phases 18–19 (ICON-01..ICON-12)

- ✓ Kamacu rebrand (code identity + brand + README) — Go module/binary renamed `kangent` → `kamacu` (module path, all imports, `cmd/kamacu/`, `Makefile` → `bin/kamacu`), the UI presents as Kamacu (tab title, ember-spark `KamacuMark` + favicon, sidebar lockup, all user-facing copy), and a shareable repo-root `README.md` with a branded screenshot. Runtime data paths (`~/.kangent`), the `-L kangent` tmux socket/prefix, `X-Kangent-Token`, and `kangent.*` localStorage keys are deliberately left unchanged for Phase 21's gated migration — Phase 20 (REBRAND-01/02/03, BRAND-01/02/03)

- ✓ GitHub "Files changed"-style diff review — a two-pane Diff tab: a fixed left file tree of all changed files (nested, path-compressed, folders-expanded) whose row-click scroll-jumps to that file's diff (targeting a non-sticky per-file `[data-diff-anchor]` at the flow top so both up- and down-jumps work — a sticky header's `offsetTop` reflects its pinned position) with an IntersectionObserver scroll-spy highlighting the file at the top of the pane; GitHub-style **stacking** sticky per-file headers (Collapsible root `display:contents`, always exactly one pinned under a fixed totals bar) with per-file collapse/expand independent of Viewed; and a per-file "Viewed" checkbox that persists across reopen AND server restart (keep-history `diff_viewed` table, migration 00011, composite PK `(task_id, file_path, diff_hash)`) and auto-resets to un-viewed + re-expanded only when that file changes again (new content hash → no matching row; reverting restores the checkmark), unchanged viewed files staying collapsed — Phase 22 (DIFF-01/02/03/04)

- ✓ Settings worktree-cleanup panel — a Settings section that lists **cleanup-candidate** worktrees across all projects grouped by project (orphaned / stale-pointer / referenced-but-finished — active in-progress tasks and open PRs are intentionally hidden; a post-approval refinement of WTREE-01 into a cleanup queue rather than a full inventory), each classified with dirty/unpushed(network-free)/stash/session flags and task/PR association; per-item confirmed force-remove that always keeps the branch and degrades a foreign-uid/permission-blocked removal (the `sched` uid-70 `./.db` case) to a copyable-but-never-executed `sudo rm -rf` banner (D-01) instead of a 500; a bulk "clean eligible" that server-recomputes the safe set (done/merged-pristine + orphans, all-four-gates) and **never forces**; and orphan/stale reconciliation of the accumulation the reaper skips. Backend: `worktree.List` porcelain parser, the extended shared `CleanupWorktreeGated` (orphan mode + blocked outcome), and `GET /api/worktrees` + `POST /api/worktrees/{remove,clean-eligible,clear-pointer}` — Phase 23 (WTREE-01/02/03/04)
- ✓ Session-bar, board & tab polish — renameable bash/tmux tabs (double-click inline editor, `PATCH /api/sessions/{id}` → mutex-guarded `Session.SetLabel`, custom label persisted to `tmux_sessions.label` and restored on reattach after a server restart, empty-commit → `Bash N` reset, gated to bash/tmux tabs only); the agent view's Stop button folded into a ⋯ menu (Stop + Insert review prompt + Insert description) with the PR-review seed no longer auto-pasted on connect (manual menu item only); and active-sessions-bar/board cleanups (dropped the collapsed total-sessions count, outside-click auto-collapse, retired the To Do inline `+ New task`/`QuickAdd.tsx`) — Phase 24 (TABS-01/02, REVMENU-01/02, POLISH-01/02/03)
- ✓ Gated `~/.kangent` → `~/.kamacu` data migration — a one-time, idempotent, recoverable startup migration (`internal/migrate`): preflight + atomic dir rename + WAL-safe DB rename, per-path `git worktree repair` (log-and-skip so a stale/unregistered worktree can't abort it), DB managed/worktree-path rewrite (boundary-checked LIKE-gated), tmux `-L kangent` → `-L kamacu` socket + `kangent-*` → `kamacu-*` session-prefix switch with pre-existing live sessions reattached-or-retired, and client `kangent.*` → `kamacu.*` localStorage keys; a fresh or already-migrated install skips safely and a mid-migration failure leaves the original `~/.kangent` untouched with a clear error instead of a half-migrated state — Phase 21 (MIGRATE-01/02/03/04/05)
- ✓ Workspace data foundation — migration 00012 creates a `workspaces` table (protected default **Personal** seeded id=1, `is_default` flag, `name COLLATE NOCASE` unique index) and adds `projects.workspace_id` as a DB-enforced `NOT NULL DEFAULT 1 REFERENCES workspaces(id) ON DELETE RESTRICT` FK that reassigns every existing project to Personal in one migration (so a project can never be workspace-less from 00012 onward); `workspace_id` is threaded onto the projects read wire (`GET/POST/PATCH /api/projects`) with both create paths defaulting to Personal, and an idempotent `BackfillWorkspaces` startup hook (mirroring `BackfillProjectIcons`) guarantees a default workspace always exists on every boot — Phase 25 (WSDATA-01, WSDATA-02)
- ✓ Workspace switcher, management & assignment — the full user-facing workspaces feature on the Phase 25 data layer. Backend `/api/workspaces` CRUD (list/create-by-name/rename incl. Personal/guarded delete keyed off `is_default` + a non-empty `COUNT` guard with `ON DELETE RESTRICT` backstop) plus project transfer (optional `workspace_id` on `PATCH /api/projects/{id}`, target-validated) and create-into-active-workspace (optional `workspace_id` on `POST`, Personal fallback). Frontend: a single shared `useActiveWorkspace` context (localStorage `kamacu.workspace`, `is_default` fallback for a stale id) mounted in `AppLayout`; an expanded-only `WorkspaceSwitcher` (hidden in the collapsed rail) with a ✓-marked dropdown + New/Manage entries; create/rename (`WorkspaceNameDialog`) and a `ManageWorkspacesDialog` hub with disabled-delete guards; a single `.filter` scoping BOTH sidebar surfaces (expanded rows + collapsed icon rail) to the active workspace; workspace-aware routing (index redirect → active workspace's first project or a workspace-scoped empty state, URL-wins deep-link reconciliation via a `lastSyncedProjectId`-guarded `BoardWorkspaceSync`); a "Move to" project ⋯ submenu with follow-the-project behavior; and the WSBAR-01 non-regression guardrail keeping the global Active Sessions bar cross-workspace (zero workspace refs). — Phase 26 (WSMGMT-01/02/03/04, WSNAV-01/02/03, WSPROJ-01/02, WSBAR-01)

- ✓ Configurable agents data foundation — `agents` table (migration 00013, mirroring 00012 exactly) seeds Claude Code as a non-deletable (`is_system=1`) default (`is_default=1`, `engine='claude'`); `projects.agent_id` NOT NULL REFERENCES … ON DELETE RESTRICT backfills every existing project to Claude in-SQL; idempotent `BackfillAgents` startup hook — Phase 01 (AGDATA-01/02, R012/R013)
- ✓ Agent management CRUD — `/api/agents` create/edit/delete/set-default reusing the workspaces.go handler shape; `is_system` non-deletable, in-use block-until-unassigned (ON DELETE RESTRICT backstop), exactly-one-default transactional invariant (flag-keyed, rename-proof); claude seed engine locked but name+command editable — Phase 01 (AGMGMT-01..04, R014/R015/R018/R019)
- ✓ Spawn engine fork — claude path byte-for-byte unchanged (fake-claude `TestAgentLifecycle` regression green); custom path renders the command template (tokenize-first-then-substitute, cwd=worktree, `{{worktree}}`/`{{session_id}}` placeholders) with running/exited status only (D-M001-2); agent resolves at spawn from `projects.agent_id` (read-at-use, no restart) — Phases 01 (AGSPAWN-01/02/03, R016/R017/R020)
- ✓ Agent management UI — Settings Agents section (ordered list + add/edit/delete/set-default, reusing v1.9 dialog shapes); per-project agent selector; Active Sessions bar LIVE filter widened to include `running` so custom-agent sessions appear; extra Claude params consolidated onto the Claude agent row (migration 00014 + `BackfillAgentExtraParams`, edited in the Claude agent's dialog) — Phase 02 (AGUI-01/02/03, R021)
- ✓ opencode built-in engine — seeded as a second non-deletable system agent (migration 00015 + `BackfillOpenCodeAgent`, `engine='opencode'`); its own spawn branch (exempt from custom-render, `KAMACU_*` hook env injected); fetch-based status plugin (`fetch()`+`AbortController(3s)` replacing a silent-no-op curl shell-out so waiting/idle unlock reliably); build-tagged real-opencode e2e harness — Phases 03–04 (OCENG-01/02/03)
- ✓ opencode restart-resume — `tasks.opencode_session_id` (nullable, migration 00016) persists the opaque `ses_…` id; `captureOpencodeSessionAsync` discovers it via async bounded poll (best-effort, warn-only, Done-channel-gated); `--resume` argv closes the restart-resume loop — Phase 05 (OCRESUME-01/02/03)

- ✓ MCP subcommand foundation — `kamacu` is a `google/subcommands` dispatcher (bare `kamacu` prints help + exits non-zero; `kamacu serve` runs the server verbatim); a new `kamacu mcp serve` stdio subcommand speaks JSON-RPC over stdin/stdout via `github.com/modelcontextprotocol/go-sdk v1.6.1`, dispatching `mcp serve` through Pattern 2 nested Commander; `internal/mcp/` package is the bridge-pattern foundation every subsequent phase mechanically repeats (env-driven `*bridge` HTTP client with `X-Kamacu-Token` header → loopback Kamacu HTTP API → raw TextContent passthrough; `127.0.0.1:7333` default + `KAMACU_HOOK_BASE` override) — Phase 06 (MCPPROC-01/02/03)
- ✓ MCP `list_projects` tool + SC2 regression — `list_projects` is the one production tool registered (proves the bridge architecture end-to-end); the SC2 malformed-tool regression test (in-memory transport, end-to-end via real `mcp.Client`) proves handler-returned errors emit clean JSON-RPC error responses on stdout — never crashes, never stream-desync; `echo -n '' | kamacu mcp serve; echo $?` exits 0 (Pitfall 5 nil-return mapping); `internal/mcp/` is free of stdout pollution (no `fmt.Print*`/`log.Print*`/`os.Stdout.Write`) — Phase 06 (MCPPROC-01/02/03)
- ✓ `X-Kamacu-Token` coordinated rename — single source-only sweep renaming `X-Kangent-Token` → `X-Kamacu-Token` across 10 source occurrences in 7 files (3 production + 4 test); byte-for-byte security posture preserved (32-byte `crypto/rand` token, `crypto/subtle.ConstantTimeCompare` receiver gate, fresh-per-start re-injection); historical references under `.planning/` and `.gsd/` intentionally preserved as time-stamped records — Phase 06 (MCPPROC-02)

- ✓ MCP task tools (6) — `list_tasks(project_id?)` (new `GET /api/tasks` manual-only, status/position-ordered), `get_task`, `create_task` (worktree+branch auto-provisioned, agent NOT auto-started), `update_task` partial-PATCH via `*string` pointer fields (nil=omit, ""=clear), `move_task` (hardcodes `after_id:null` so Kamacu's MIN(position)-1.0 top-of-column path is the only one taken), `delete_task` (gated cleanup runs); every handler a thin build-path-and-delegate to the shared `bridge.call` — Phase 07 (MCPTASK-01..06)
- ✓ MCP project tools (5) — `list_projects(workspace_id?)` (the Phase-06 tool's InputSchema corrected to the real `workspace_id?` arg, stale `project_id` no-op removed; `?workspace_id=N` query param on `GET /api/projects`), `get_project` (new `GET /api/projects/{id}` endpoint, 404 on missing), `create_project` (two-arg fork: folder path or `owner/name` triggers the v1.4 managed-checkout atomic create), `update_project` partial-PATCH excluding `workspace_id`+`agent_id` (transfer has its own tool; agent reassign is Out-of-Scope), `delete_project` (v1.4 all-or-nothing gated delete; folder projects byte-for-byte unchanged) — Phase 07 (MCPPROJ-01..05)
- ✓ MCP workspace tools (5) — `list_workspaces`/`create_workspace`/`update_workspace` (D-03 single-field `*string` rename)/`delete_workspace` (v1.9 guarded delete keyed off `is_default` + non-empty `COUNT`, both 409 messages) bridging to `/api/workspaces`, plus `move_project_to_workspace` (cross-route transfer PATCHing `/api/projects/{id}` with a `workspace_id`-only body, owned by the workspaces registrar per the D-07 resource-acted-on split) — Phase 07 (MCPPROJ-06/07)
- ✓ MCP bridge infrastructure — D-07 per-resource file split (`internal/mcp/{tasks,projects,workspaces}.go`, each owning its `register*Tools` + `*bridge` methods; `server.go` shrunk to a 3-line delegator) and the shared `bridge.call` helper extracted from Phase-06's inlined `do→ReadAll→non-200-wrap→TextContent` pattern, halving per-tool line count so all 16 Phase-07 tools share one shape; one Rule-1 auto-fix widened `bridge.call` from HTTP 200-only to the full 2xx range so POST→201 (create) and DELETE→204 surface as results instead of errors — Phase 07

- ✓ MCP PR review tools (3) — `list_pending_reviews(project_id)`/`list_recently_reviewed(project_id)` (both via a shared `listReviews` helper that GETs the cached `/api/projects/{id}/pull-requests` endpoint and projects the chosen queue — `prs` or `reviewed` — verbatim, riding the existing per-repo TTL cache so one `gh` cycle serves both queues, no N+1) + `open_review(project_id, pr_number)` (a verbatim `bridge.call` POST to `/api/projects/{id}/pull-requests/{n}/review` reusing the v1.3 Phase 12 find-or-create `source='github_pr'` path — zero gate logic in the bridge, board-leak guard + dedup live server-side); degrade branch preserves the raw upstream body verbatim (D-02) and surfaces `IsError:true` when GitHub integration is off / no linked repo / `gh` absent (never bypassing the v1.3 two-gate ladder). `project_id` is REQUIRED (locked D-04 refinement — keeps every tool a single HTTP call) — Phase 09 (MCPREV-01/02/03)

- ✓ MCP session tools (4) + read-only terminal access — `list_sessions(project_id?|task_id?)`/`get_session`/`get_session_output`/`subscribe_session_output` (the milestone's risk center and only genuinely new capability). Server half (internal/api/sessions.go): three read-only GET handlers — `GET /api/sessions/{id}` (live OR exited in-memory, 404 unknown), `GET /api/sessions/{id}/output?bytes=N` (base64 D-08 snapshot of the existing ring, default 4 KB, server-clamped to 512 KiB + `clamped` flag), `GET /api/sessions/{id}/subscribe` (plain-HTTP chunked bounded octet-stream, attach→drain-replay→stream→defer-Detach-on-every-return, default 30s/hard-cap 300s) — plus a `?project_id=N` filter (one task-set query + in-memory filter, never N round-trips) and a batch task→project→agent JOIN carrying `taskTitle`/`projectName`/`agentName`. MCP half (internal/mcp/sessions.go): the three non-streaming tools repeat the Phase 06/07 `bridge.call` shape; `subscribe_session_output` diverges — a dedicated no-timeout streaming `*http.Client` (NOT `bridge.call`/its 10s timeout), incremental accumulate under a 1 MiB most-recent cap (D-04), `ctx.Done()` partial-return (D-01/SC3), and follow-up GET for the exit marker (D-03). A deferred `withRecover` panic wrapper wraps every session handler (Phase 06 Open Q1, prerequisite for the streaming surface). **Read-only contract enforced at the type level (D-14):** scoped grep gates prove zero `WriteInput`/`FrameData` refs in both files and no `internal/session` import in the bridge — an agent observing a sibling session can never inject PTY bytes. **SC3 proven structurally** (go-sdk@v1.6.1 returns `(nil, context.Canceled)` to the client, so a handler-side recorder observes the partial envelope): `defer sess.Detach(connID)` on every return path + zero `NumGoroutine` delta across N subscribe/cancel cycles + the fake streaming server's `r.Context().Done()` fires within 2s. 4/4 ROADMAP success criteria verified; code review 0 blockers (4 warnings/2 info, advisory). — Phase 08 (MCPSESS-01/02/03/04)

### Active

**v1.13 Global Task** — a single global scratchpad for small checks unrelated to any project:
- A task-like view with ONLY the agent tab + bash tabs (no description, no diff, no kanban board) — not linked to any workspace or project
- Sessions run directly in the configured repo/folder (no worktree, no branch)
- Global Settings section configures the global root — a local folder path OR a GitHub repo cloned like a managed project (v1.4 pattern) — plus its default agent (v1.10 agents)
- Reachable from the Active Sessions bar when a session is live; from Settings when idle
- Same session semantics as tasks: persistent PTYs, restart reconciliation/resume, live agent status in the bar

### Recently Validated

**v1.12 Activity & Statistics** — a new top-level Activity page (route + sidebar entry, like Settings) with a Global / Workspace / Project scope selector and a segmented Week/Month toggle (last 7 / last 30 days rolling):
- ✓ **Tasks done list** — manual tasks (`source='manual'`) with `done_at` in the window, grouped by project — Phases 10–11
- ✓ **Reviews done list** — PRs from `reviewed-by:@me` that MERGED or CLOSED in the window (GitHub merge/close timestamp), grouped by project — Phases 10–11
- ✓ **Statistics** — counts (tasks done, reviews done) + min/max/median **cycle time** (In Progress→Done) and **per-column dwell** (time in In Progress, time in In Review), powered by the existing per-status timestamps (migration 00006) — Phases 10–11
- ✓ Time stats apply to **tasks only**; reviews surface counts only (GitHub's merge signal has no Kamacu in-progress/in-review timestamps) — Phases 10–11
- ✓ **Activity chart** — daily stacked-bar chart (tasks + reviews per day; Week = 7 bars, Month = 30 bars) with hover tooltips showing absolute date + counts, plus explanatory tooltips on the stats strip and absolute per-entry completion times (recharts via shadcn chart pattern; no backend change) — Phase 12 (CHART-01/STAT-01/ENTRY-01)
- ✓ **Activity hardening (audit tech debt closed)** — the `useActivity` fetch `enabled` gate wired so first-time users fire exactly one workspace-scoped fetch with no Global-label flash (TD-ACT-01); a stale/deleted persisted scope self-heals to effective Global in a `useMemo` demotion that never writes localStorage (TD-ACT-02); ReviewsList external links carry the PR title in their accessible name (TD-ACT-03) — Phase 12.1

### Out of Scope

- External task services (JIRA, Linear, etc.) — explicitly excluded; local database only
- Multi-user support, auth, remote deployment — single user at localhost only
- Desktop app packaging (Electron/Tauri) — this is a web app by design
- Multiple agent CLIs (Codex, Gemini, etc.) as first-class built-in engines with their own status heuristics — v1.10 made agents configurable (custom CLIs run in the worktree PTY) and added opencode as a second built-in engine, but additional first-class engine integrations (with working/waiting/idle heuristics + restart-resume) are deferred (AGFUT-03)
- Merge/PR *write* automation from the app (approve/request-changes/comment/merge) — done by the user in the terminal; v1.3 adds read-only PR surfacing + worktree checkout for review, but no GitHub writes
- Storing GitHub credentials/tokens — access is via the host's already-authenticated `gh` CLI only (v1.3)
- Non-GitHub forges (GitLab, Bitbucket, Gitea) — GitHub-only for v1.3
- Custom kanban columns, labels, priorities — fixed columns and lean task cards for v1
- Auto-starting agents on task creation — sessions start only via explicit Start button
- **MCP write to live PTYs (keystroke injection)** — v1.11 deliberately exposes read + subscribe only; an agent observing a sibling session must never inject bytes into its PTY (would conflict with the browser-attached user and the other agent's intent). PTY write stays browser-only
- **MCP server for external AI editors (Claude Desktop / Cursor / VS Code)** — v1.11 targets agents running inside Kamacu; an external-editor MCP surface (with its own auth + transport considerations) is a separate future milestone

## Current State

**Shipped: v1.0 MVP (2026-06-11)** — 5 phases, 28 plans, 74 tasks. ~10,400 LOC Go + ~5,800 LOC TS/React. Single `make build` binary serving the embedded SPA at `127.0.0.1:7333`.

Kangent v1 does the whole loop: create a project on a local git repo → add a task (worktree + `task/<slug>-<id>` branch auto-created under `~/.kangent/worktrees/`) → Start a real `claude` session in the worktree PTY → watch the board as a dispatcher (status dots, amber when an agent needs you) → leave and reattach across tab closes and server restarts (`claude --resume`) → review the diff vs merge-base → mark Done with gated worktree cleanup. Stack: Go stdlib mux + modernc SQLite + creack/pty + coder/websocket; React 19 + Vite + Tailwind 4 + shadcn + xterm.js 6 + dnd-kit + TanStack Query.

**v1.2 complete (2026-06-13)** — all 3 phases done. Phase 7 — Claude quota indicator: `internal/quota` server proxy (`GET /api/usage`) + QuotaIndicator in both headers. Phase 8 — invisible tmux shells: `internal/tmux` leaf package, migration 00005 `tmux_sessions`, call-time shell dropdown, killer-first `Stop()`, end-to-end spawn wiring with honest 409 errors. Phase 9 — restart durability + cleanup: `GET /api/sessions` reconciles surviving tmux rows as auto-reattaching ghost tabs (TMUX-05, the milestone's headline — sessions survive a server restart and reattach invisibly), cleanup/delete kill tmux before worktree removal + a startup orphan sweep (TMUX-08), and the codebase's first background goroutine — a Done-TTL reaper keyed on new per-status timestamps (migration 00006) that kills bash/tmux/agent sessions of long-Done tasks while keeping the agent resumable (REAP-01). All phases verified green (full Go suite + frontend build).

**v1.3 in progress — Phase 10 (GitHub Foundations) complete (2026-06-13)** — 5 plans (3 original + 2 gap-closure). Schema migration 00007 lands all five v1.3 columns + the `github_integration` toggle KV; `internal/github` is the degrade-don't-break leaf (`ParseRepoRef`/`ValidateRepo`/`Available`, `gh` a soft dependency); projects gained partial-PATCH description + repo link with origin auto-detect; the `/settings` GitHub Switch and Project settings dialog ship the OFF cascade. UAT-driven refinement (`gh`-gated enablement + copy) closed via `GET /api/github/status` and a gh-aware toggle. Verified passed (full Go suite + web build green; gh-absent degrade exercised live).

**Phase 11 (PR Review Column) complete (2026-06-14)** — 4 plans, strict sequential chain. `internal/github` gained `ListReviewRequested` (the verified `gh pr list --search "user-review-requested:@me draft:false"` arg array with `cmd.Dir` = repo path, server-side `statusCheckRollup` → pass/fail/pending/none reduction, and exit-code+stderr degraded classification) plus a per-repo TTL cache `Service` cloned from `quota.Service` — no N+1 `gh pr checks`. Always-200 `GET /api/projects/{id}/pull-requests[?refresh=1]`, two-gate ladder (toggle → link) short-circuiting before any `gh` spawn. Frontend: `usePullRequests` (60s visibility-paused poll + manual refresh), `formatAgo` lifted to `lib/time.ts`, `PRCard`, and a self-gating collapsible `ReviewColumn` pinned right of Done outside the dnd machinery. Per-project localStorage collapse persistence, now **default collapsed** (user override of D-05/D-06 during the human-verify checkpoint). 17/17 must-haves verified; human-verify checkpoint approved.

**Phase 12 (Open-a-Review) complete (2026-06-14)** — 7 plans (5 original + 2 gap-closure after the human-verify checkpoint). The milestone headline: clicking a PR card opens it as a task-like review workspace — a `tasks` row with `source='github_pr'` rendered through the same `TaskPage`/`TaskTabs` shell. `worktree.CheckoutPR` fetches `refs/pull/<n>/head` and `git worktree add -b <branch> <path> <headOID>` on the PR's **real head branch** with a `pr/<n>` collision fallback (the named-branch path *supersedes* the original detached D-01 after human-verify, and preserves GHREV-05 — an existing local ref is never reused/moved, main checkout HEAD provably unchanged). Open-or-reattach `POST .../pull-requests/{n}/review` (find by project+pr_number) + read-only `GET .../pull-requests/{n}` (live `gh pr view` re-hydration on F5). Diff branches on `pr_base_ref` (fetch + merge-base `origin/<base>`, renderer reused). Board-leak guard: `source='manual'` on all 5 board/position queries + `/move` 409. Frontend: read-only single-line clickable GitHub-style header (`#<n> @<author> wants to merge <N> commits into <base> from <head>`), read-only PR-body Description, Agent-tab-first, seed prefilled-once-per-session from the configurable `pr_review_seed` Settings field. 5/5 success criteria verified; human-verify approved end-to-end.

**Phase 13 (PR Worktree Auto-Cleanup) complete (2026-06-14) — v1.3 fully implemented** — 3 plans. The reaper (the Phase 9 Done-TTL goroutine) gains a second `reconcilePRsOnce` pass: per tick, each `source='github_pr'` task with a worktree is checked via `github.PRState` (`gh pr view --json state` → OPEN/CLOSED/MERGED); a MERGED/CLOSED PR's worktree is auto-removed **only when pristine and idle** (conservative gate: uncommitted / unpushed via `rev-list FETCH_HEAD..HEAD` re-fetched in the worktree / stash / running session — skip on any), and on success the task row is deleted (FK-ordered `tmux_sessions` before `tasks`). The gated-cleanup logic was extracted byte-equivalent into `CleanupWorktreeGated` (shared by the HTTP handler + reaper; reaper passes `force=false`, never stops sessions); the branch ref is never deleted. Manual cleanup (GHCLN-03) re-adds a PR `⋯` "Clean up worktree" item + a merged/closed banner in the review view. 18/18 must-haves verified; human-verify approved end-to-end. **v1.3 (GitHub PR Review) shipped 2026-06-14** — milestone audit passed (20/20 requirements, 6/6 integration seams, 4/4 E2E flows), archived to `milestones/v1.3-*`.

**v1.4 in progress — Phase 14 (Managed Checkout Foundations) complete (2026-06-14)** — 4 plans, the backend capability for Kangent-managed checkouts. Migration 00008 adds the `managed` marker (existing folder projects backfill to never-touch). `github.Clone` wraps `gh repo clone` (exit-0-only, test-seam) into `~/.kangent/repos/<owner>/<name>`; `createByRepo` does gh-validate → clone → INSERT atomically (failed clone leaves no row, no dir) with reattach-on-origin-match. Managed task/PR worktrees best-effort fetch the default branch before `ResolveBase` (folder projects keep the D-24 no-network path); managed-project delete is all-or-nothing gated (dirty/unpushed/stash/session across every worktree + clone root) then unlinks worktrees → removes the clone dir → deletes the row, while folder-project delete stays byte-for-byte unchanged. 5/5 requirements verified (CKOUT-01/02/03/05, RPROJ-05); `go build`/`vet`/`test ./...` all green.

**v1.4 fully implemented — Phase 15 (Repo-First Creation Flow) complete (2026-06-15)** — 3 plans. The repo-first Add-project UI on top of Phase 14's primitive: `AddProjectDialog` gains an integration-gated "GitHub repo | Local folder" segmented toggle (repo default, reusing `ui/tabs.tsx`); the `owner/name` input prefills the editable name, submit drives the atomic Phase-14 create with a blocking "Cloning…" spinner, and clone failures surface inline (dialog open, values preserved, no half-created project — Phase-14 atomicity). A dedicated best-effort `github.RepoDescription` (`gh repo view --json description`) captures the repo description into `projects.description` at create (visible/editable later in Project settings), leaving the shared `ValidateRepo` signature untouched. Integration-off renders the byte-for-byte pre-v1.4 folder-only form. 5/5 requirements verified (RPROJ-01..04, CKOUT-04); human-verify gate approved end-to-end; `go build`/`vet`/`test ./...` + `tsc` all green; embedded SPA rebuilt. **v1.4 (Repo-First Projects) shipped 2026-06-15** — milestone audit passed (10/10 requirements, 6/6 integration seams, 5/5 E2E flows), archived to `milestones/v1.4-*`.

**v1.5 Sharper Review Column — Phase 16 complete (2026-06-17), milestone fully implemented** — 2 plans, 2 waves (data → rendering). The Review column now carries two independent at-a-glance signals per PR. Backend (`internal/github` + `internal/api`): a second `reviewed-by:@me draft:false` search rides ONE cache cycle alongside the existing `user-review-requested:@me` call (`fetchLists`, degrade-together), server-side top-precedence `dedupeReviewed`, the `{prs, reviewed}` endpoint response, and `pr_number`+`source` joined onto `/api/agents/status` entries (no migration — columns pre-exist in 00007). Frontend (`PRCard`/`ReviewColumn`): CI status as a bare lucide glyph (green `Check` / red `X` / static amber `Circle`; `none` renders nothing), agent state as an always-on colored **left rail** (green working / pulsing-amber waiting / blue idle / gray exited) — **redesigned from a `StatusDot` to a rail at the human-verify gate** per user feedback (a dot beside the line-art glyph clashed), so the two signals split cleanly by channel (left edge = your agent, right glyph = the PR's CI); and a quietly-omitted "Recently reviewed" subsection holding reviewed-still-open PRs until merge/close. 10/10 must-haves verified; human-verify approved end-to-end (incl. the redesign); `go test ./...` (12 pkgs) + `tsc -b && vite build` green; embedded SPA rebuilt. **v1.5 (Sharper Review Column) shipped 2026-06-17** — milestone audit passed (9/9 requirements, 4/4 integration seams, 3/3 E2E flows), archived to `milestones/v1.5-*`.

**v1.6 Global Active Sessions Bar — Phase 17 complete (2026-06-18), milestone fully implemented** — 2 plans, 2 waves (data → UI). The app now has a persistent global view of all live agent work. Backend (`internal/api/agents.go`): the existing `/api/agents/status` query was widened with `tasks.title` + `projects.name` via a JOIN on BOTH passes (manager-derived + DB-derived/post-restart), the `agentStatusEntry` struct + the TS `AgentStatusEntry` gained `taskTitle`/`projectName` — no new endpoint, no migration (the columns pre-exist; mirrors v1.5's `prNumber`/`source` widening). Frontend: `ActiveSessionsBar` (new, mounted in `AppLayout` outside `<Outlet/>`, present on every route) reuses the 5s `useAgentStatuses()` poll as a 4th consumer and `dotMeta()` for status colors — collapsed per-state counts + total with amber-pulsing waiting emphasis, an overlay panel that floats UP over content (chosen so the xterm terminals never reflow, D-03) listing live sessions attention-first with cross-project click-through and current-row highlight, `localStorage` default-collapsed persistence, and live-only filtering (exited drops off). The per-project sidebar waiting chips were kept (additive). 11/11 must-haves verified; human-verify gate approved end-to-end (incl. the critical no-terminal-reflow check); `go test ./...` (11 pkgs) + `tsc -b` + `vite build` green; embedded SPA rebuilt. **v1.6 fully implemented 2026-06-18** — run `/gsd:complete-milestone` to audit and archive.

**v1.7 Project Icons in Collapsed Sidebar — Phase 18 (Project Icon Data Foundation) complete (2026-06-19)** — 3 plans, 2 waves (backend data layer ∥ frontend wire path, then model wiring). Every project now has two persisted properties: `icon_letters` (≤2 uppercase alphanumerics) + `icon_color` (a member of a curated 9-hex Tailwind-600 palette). New `internal/api/icons.go` is the single source of truth — `projectPalette` (9 hexes, fixed white text, D-01/D-02), `deriveLetters` (whitespace+`-_.`-split monogram covering the 7 locked cases incl. `nf-core`→`NC`, emoji-only→`?`, D-04/D-05), `pickColor` (pure-random palette member, D-03), `validateIconLetters`/`validateIconColor` (server-enforced; color must be a palette member, D-10/D-11), and an idempotent post-`Migrate` `BackfillProjectIcons` (D-08). Migration 00009 adds both columns (`NOT NULL DEFAULT ''`, reverse-order DROP Down, D-07); both create paths auto-assign at INSERT; the partial-PATCH `update` handler validates+persists both (D-09); `web/src/api/{types,mutations}.ts` carry them on the wire for Phase 19's editors (D-12). 4/4 must-haves verified (incl. end-to-end backfill + HTTP POST/PATCH behavioral spot-checks); code review clean after fixing WR-01 (PATCH letter-validation now alphanumeric-filtered to match `deriveLetters`/D-10, commit `aeff8e0`); `go test ./...` (11 pkgs) + `tsc -b && vite build` green.

**v1.7 Project Icons in Collapsed Sidebar — Phase 19 (Sidebar Avatars & Settings Editors) complete (2026-06-19), milestone fully implemented** — 3 plans, 2 waves (shared primitive → sidebar ∥ settings). The data foundation became a visible feature. Wave 1 added the `PROJECT_PALETTE` TS const (verbatim Go mirror, `web/src/lib/palette.ts`) and the shared `<ProjectAvatar>` primitive (`web/src/components/ui/ProjectAvatar.tsx`; rail|inline sizes, rail-only active state + static amber waiting dot). Wave 2 (parallel, no file overlap): the sidebar switched to `collapsible="icon"` rendering a 3rem rail of per-project avatars (click-to-switch, side=right name tooltip, amber waiting badge, filled-row active highlight) with the same avatar inline in expanded rows and the floating re-expand trigger retired; and Project settings gained the "Initials" input (client-normalized ≤2 uppercase alphanumerics, mirroring the server rule) + a 9-swatch color grid + live preview, wired into the existing conditional PATCH. **UAT reshaped the visual contract and was accepted as the new spec:** circular avatars (reversed D-04), rail-row filled-highlight active state instead of a ring (reversed D-06), and a muted desaturated palette replacing the bright Tailwind-600 hues — changing the Go source of truth, the TS mirror, and adding migration `00010_muted_palette.sql` to remap existing rows by palette position (a Phase 18 artifact edited during Phase 19 UAT). 8/8 must-haves verified (9/9 per the verifier); code review CR-01 + WR-01/03 fixed; `go test ./...` + `tsc -b && vite build` green; UAT-approved end-to-end. **v1.7 (Project Icons in Collapsed Sidebar) shipped 2026-06-19** — 2 phases (18–19), 6 plans, 12 tasks, 12/12 requirements (ICON-01..ICON-12); pre-close artifact audit surfaced 4 lingering-but-completed quick tasks (false positive, noted in MILESTONES.md); archived to `milestones/v1.7-*`.

**v1.8 Kamacu Rebrand & UX Polish — Phase 20 (Kamacu Rebrand & Brand) complete (2026-07-01)** — 4 plans, 3 waves (backend Go rename ∥ frontend brand assets → sidebar lockup + copy rename → README + human-verify). The product now presents as **Kamacu** everywhere in code and UI: `go.mod` is `module kamacu`, all 41 imports rewritten, `cmd/kamacu/` builds `bin/kamacu`; the browser tab, ember-spark `KamacuMark` component + `favicon.svg`, sidebar brand lockup, and every user-facing string say Kamacu (`grep Kangent web/src` = 0); and a concise shareable repo-root `README.md` (kamaq/kamacu naming story, prerequisites, `make build` → `bin/kamacu`, workflow walkthrough) embeds a post-rebrand `docs/kamacu-board.png`. The Phase-21 keep-out boundary held byte-for-byte — `~/.kangent` data paths, the `-L kangent` tmux socket + `kangent-<task>-<n>` prefix, `X-Kangent-Token`, and `kangent.*` localStorage keys are untouched (a kamacu binary reading `~/.kangent` is the accepted intermediate state until Phase 21's gated migration). UAT reshaped one detail (accepted as spec): the sidebar brand mark is now expanded-only — hidden in the collapsed icon rail where it misaligned with the toggle (revising D-08, commit `7b5c170`). 5/5 must-haves verified against the code (cache-cleared `go test ./...` (12 pkgs), `go build`/`vet`, `tsc -b && vite build`, smoke test all green); code review found one real regression (smoke.sh launched `bin/kangent`) — fixed + verified (`958fa47`); human-verify approved end-to-end.

**v1.8 Kamacu Rebrand & UX Polish — Phase 21 (Data Directory Migration) complete (2026-07-01)** — 8 plans (5 original + 3 gap-closure), the on-disk flip Phase 20 deliberately deferred. New `internal/migrate` runs a one-time, idempotent, recoverable startup migration behind a gate table: preflight → atomic `~/.kangent` → `~/.kamacu` dir rename → WAL-safe DB rename → tmux retire (Part 1), then managed/worktree-path rewrite in the DB (boundary-checked LIKE-gated), per-path `git worktree repair` with log-and-skip so a stale/unregistered worktree can't abort the migration, and tmux-row cleanup (Part 2), wired into `main.go` ahead of the runtime socket/path/prefix literal flip (`-L kamacu`, `kamacu-<task>-<n>`, `X-Kamacu-Token`). The client migrates `kangent.*` → `kamacu.*` localStorage keys via a prefix-scan on boot. A fresh or already-migrated install skips safely; a mid-migration failure leaves the original `~/.kangent` untouched and surfaces a clear error rather than a half-migrated state. Verified live end-to-end against a copied HOME (dir/DB/worktrees/sessions/localStorage all survive) plus a git-safe copied-HOME re-run smoke and a real-install isolation proof; code review CR-01 (broaden `repairWorktrees` to folder-pointed repos) + WR-01/02 (boundary-check the LIKE-gated path rewrites) fixed; `go build`/`vet`/`test ./...` green. 5/5 requirements verified (MIGRATE-01..05).

**v1.8 Kamacu Rebrand & UX Polish — Phase 22 (GitHub-Style Diff Review) complete (2026-07-02)** — 5 plans, 4 waves (per-file diff hash + Viewed persistence backend → client Viewed control → two-pane file tree/scroll-spy → human-verify). The Diff tab became a GitHub "Files changed" two-pane view. Backend: `internal/diff` computes a per-file content hash alongside the unified diff (committed + uncommitted + untracked vs the base merge-base); a keep-history `diff_viewed` table (migration 00011, composite PK `(task_id, file_path, diff_hash)`) drives sticky per-file Viewed state — `GET` read-merges the viewed flags, `PUT /api/tasks/{id}/diff/viewed` toggles them. Frontend: a fixed 288px left `FileTree` (nested, path-compressed, folders-expanded) whose row-click is scroll-only (jumps to a non-sticky `[data-diff-anchor]` at each file's flow top — a sticky header's `offsetTop` resolves to its *pinned* position, which broke upward jumps) and an IntersectionObserver scroll-spy (`use-scroll-spy`, rooted on the right pane) highlighting the topmost in-band file; GitHub-style **stacking** sticky headers (Collapsible root `display:contents` so each header shares the list's containing block — always exactly one pinned under a fixed totals bar) with per-file collapse independent of Viewed; and `DiffTab` remounting each section on `key={path:hash}` so a changed file auto-resets to un-viewed + re-expanded (DIFF-04). The app shell was pinned to the viewport (`h-svh overflow-hidden` on the shadcn `SidebarProvider` wrapper, which was only `min-h-svh`) so only the diff pane scrolls and the tree stays fixed. UAT reshaped the sticky/scroll behavior, accepted as spec: the totals bar moved OUT of the scroll pane into a fixed header, stacking headers were chosen over boxed cards, and tree-jump made bidirectional via the flow anchor. 5/5 must-haves verified; human-verify approved end-to-end. Code review then found a real **BLOCKER** — a filesystem-legal untracked filename beginning with `-` (`-p`, `-`, `--output=…`) was passed as a bare positional to `git diff --no-index`, parsed as an option (exit 129), aborting the *entire* diff — fixed by anchoring the path with `./` (CR-01, `2c14263`), plus per-file error tolerance in the untracked loop so one vanished file no longer kills the whole computation (WR-01, `7ca2b89`) and keying the scroll-spy on `path:hash` so it re-observes after a content-only refresh (WR-02, `72579f1`); `go test ./...` + `tsc -b && vite build` green, embedded SPA rebuilt.

**v1.8 Kamacu Rebrand & UX Polish — Phase 23 (Worktree Cleanup Panel) complete (2026-07-02)** — 5 plans, 4 waves (backend primitives ∥ frontend contract → panel backend → panel UI → human-verify). Settings gained a worktree-cleanup section. Backend: `worktree.List` — a faithful `git worktree list --porcelain -z` parser (`Entry`/`parseWorktreeList`) — plus `RefResolves`/`Prune`; the shared `CleanupWorktreeGated` extended with orphan mode (`taskID==0` skips the null-columns UPDATE) and the load-bearing **D-01 blocked outcome** (`*BlockedError`, detected via stderr fallback since `git worktree remove` surfaces a string error, and NEVER retries `--force`); and `GET /api/worktrees` (a cross-project union of `worktree.List` ∪ `tasks` rows, classified referenced/orphan/stale with dirty/unpushed(network-free base)/stash/session flags + lowercased PR-state) plus `POST /api/worktrees/{remove,clean-eligible,clear-pointer}` wired into `cmd/kamacu/main.go`. Frontend: a shadcn `Badge`, a fetch-on-mount (no-poll) `worktreeCleanup.ts` hook set, and the four `settings/*` components — `WorktreeCleanupSection` (per-project `ProjectAvatar` groups, spinning Refresh), `WorktreeRow` (classification + flag chips, D-01 blocked banner with a copy-only `sudo rm -rf` hint via `clipboard.writeText`), `ForceRemoveDialog` (names what's destroyed, type-the-dirname gate when dirty, "branch is kept", `{outcome:"blocked"}` degrades to the row banner), and `CleanEligibleDialog` (dry-run preview, non-destructive CTA). **Post-approval refinement:** after the human-verify approval the user reversed WTREE-01 from "list every worktree" to a **cleanup queue** — `GET /api/worktrees` now filters via `isCleanupCandidate` (orphan/stale always; referenced only when task Done or PR MERGED/CLOSED; gh-unconfirmed hidden), REQUIREMENTS.md/ROADMAP updated to match. 9/9 must-haves verified; human-verify approved against a real install incl. the live uid-70 `./.db` blocked case. Code review found 1 Blocker + 6 Warnings; the 3 correctness-on-the-delete-path findings were fixed TDD-first — CR-01 (validate the force-remove target against the enumerated set + derive the null-columns task from the match, not the request body), WR-01 (a failed git list no longer fabricates stale rows), WR-02 (PR state resolved once per row) — with the remaining warnings/info deferred to `/gsd:code-review 23 --fix`; `go build`/`vet`/`test ./...` (13 pkgs) + `tsc -b && vite build` green.

**v1.8 Kamacu Rebrand & UX Polish — Phase 24 (Session-Bar, Board & Tab Polish) complete (2026-07-04) — final phase, milestone v1.8 done** — 4 plans, 2 waves (rename backend ∥ bar/board polish ∥ agent ⋯ menu → rename frontend). Daily-driver surfaces sharpened. Backend: mutex-guarded `Session.SetLabel` + `PATCH /api/sessions/{id}` rename handler (trim → 200-rune cap → in-memory rename; tmux tabs also persist to `tmux_sessions.label`; empty body re-derives the `Bash N` default via `defaultTmuxLabel`), plus a D-04 hardening so the spawn INSERT persists the default label and reconcile re-derives `Bash N` (the `"Bash ?"` sentinel is gone). Frontend: `useRenameSession` optimistic mutation, a double-click inline tab editor reusing the TaskPage title contract (Enter commits, Esc cancels, blur commits), gated to running bash/tmux tabs (Agent/Description/Diff keep fixed labels); the agent header's Stop button replaced by a ⋯ menu (Stop + Insert review prompt + Insert description) built on `TerminalPane`'s new agent-only `headerMenu` prop, with the connect-time PR-seed auto-paste (`seededSessionIds`) deleted so the review prompt enters only via the menu; and bar/board cleanups (collapsed total-count span removed, document-mousedown outside-click `collapse()`, To Do `QuickAdd.tsx` retired). 5/5 must-haves verified; code review found 0 blockers (4 warnings/6 info, advisory). Human-verify surfaced one real gap — a renamed tmux tab reverted to `Bash N` after a server restart because the reattach path dropped the persisted label and the post-spawn back-fill then clobbered `tmux_sessions.label`; fixed TDD-first in `1b12b52` (carry the stored label through reattach + reapply via `SetLabel` before the back-fill; regression test `TestSessionTmuxReattachPreservesCustomLabel` covers rename → restart → reattach) and re-verified. `go test ./...` (13 pkgs) + `tsc -b && vite build` green. **Phase 24 was the last phase of v1.8. v1.8 (Kamacu Rebrand & UX Polish) shipped 2026-07-04** — milestone audit passed (27/27 requirements satisfied, cross-phase integration wired), 5 phases (20–24), 26 plans, 53 tasks; archived to `milestones/v1.8-*`. Two post-milestone quick fixes landed the same day (Review-column card overlap; board drag-and-drop cross-column overshoot) — see STATE.md.

**v1.9 Workspaces — Phase 25 (Workspace Data Foundation) complete (2026-07-05)** — 3 plans, 2 waves (migration → projects wire ∥ startup backfill). The data layer the switcher UI (Phase 26) builds on. Migration `00012` creates the `workspaces` table (Personal seeded id=1, `is_default` flag, `name COLLATE NOCASE` unique index) and adds `projects.workspace_id` as `NOT NULL DEFAULT 1 REFERENCES workspaces(id) ON DELETE RESTRICT` — every pre-existing project reassigned to Personal in-SQL, NOT NULL + RESTRICT DB-enforced, all under `-- +goose NO TRANSACTION` + `PRAGMA foreign_keys OFF/ON` discipline with a reversible Down. `workspace_id` is threaded through `projectColumns`/`scanProject` so `GET/POST/PATCH /api/projects` all carry it, and both create paths (`create()`/`createByRepo()`) resolve the default via `is_default=1` (500 rather than a workspace-less project). The idempotent `BackfillWorkspaces` startup hook (`internal/api/workspaces.go`, wired into `cmd/kamacu/main.go` after `BackfillProjectIcons`) re-creates Personal only when the default is missing. 13/13 must-haves verified — the verifier ran the goose upgrade-path test itself (existing install → every project under Personal, tasks intact, exactly one default, idempotent re-run). Code review found 0 blockers / 4 warnings / 1 info, all latent states unreachable through the Phase 25 surface (they become reachable once Phase 26 makes `is_default` mutable) — flagged as Phase 26 hardening candidates. `go build`/`vet`/`test ./...` (13 pkgs) green.

**v1.9 Workspaces — Phase 26 (Workspace Switcher, Management & Assignment) complete (2026-07-06) — final phase, milestone v1.9 fully implemented** — 6 plans, 4 waves (workspaces CRUD API ∥ project transfer/create → frontend data-layer contract → switcher/management UI ∥ workspace-aware routing → sidebar wiring + human-verify). The switcher UI landed on the Phase 25 foundation. Backend: `internal/api/workspaces.go` CRUD (rename ungated for Personal, delete keyed off `is_default` + a `COUNT(*)` non-empty guard, `?`-parameterized) plus `projects.go` transfer (`workspace_id` on partial-PATCH, validated before `SET`) and create-into-workspace (Personal fallback via `resolveCreateWorkspaceID`). Frontend: the shared `useActiveWorkspace` context (`kamacu.workspace` localStorage, `is_default` fallback) mounted in `AppLayout`; `WorkspaceSwitcher` (expanded-only), `WorkspaceNameDialog`, `ManageWorkspacesDialog` (guarded delete); a single `.filter` scoping both sidebar surfaces; workspace-aware `App.tsx` (redirect → active workspace's first project / workspace-scoped empty state, `BoardWorkspaceSync` URL-wins); a "Move to" ⋯ submenu with follow-the-project; and the WSBAR-01 guardrail (Active Sessions bar left cross-workspace). Human-verify surfaced one real bug — switching to an EMPTY workspace did nothing because React-Router-v7 wraps `navigate()` in `startTransition`, so the still-mounted old project route's `BoardWorkspaceSync` reverted the just-set active workspace; fixed by gating reconciliation behind a `lastSyncedProjectId` ref so it only fires on a URL-project change (deep-link URL-wins preserved), plus a "Move to workspace"→"Move to" label fix. 23/23 must-haves verified (5/5 ROADMAP criteria); code review found 0 blockers / 3 warnings (client-side error-handling robustness — WR-01/02/03) / 4 info, non-blocking; `go build`/`vet`/`test ./...` + `tsc -b && vite build` green. **Phase 26 was the last phase of v1.9. v1.9 (Workspaces) shipped 2026-07-06** — 2 phases (25–26), 9 plans, 22 tasks, 12/12 requirements (WSDATA·WSMGMT·WSNAV·WSPROJ·WSBAR); no formal milestone audit (final phase human-verified, 23/23 must-haves); archived to `milestones/v1.9-*`.

**v1.10 Configurable Agents — Phase 01 (Agent data foundation and spawn engine) complete (2026-07-06)** — 6 plans. Stood up the agents data layer + spawn engine fork. Migration 00013 (mirroring 00012 exactly) creates the `agents` table seeding Claude Code (`is_system=1`, `is_default=1`, `engine='claude'`) and adds `projects.agent_id` NOT NULL DEFAULT 1 REFERENCES agents(id) ON DELETE RESTRICT; `BackfillAgents` startup hook mirrors `BackfillWorkspaces`. `/api/agents` CRUD (list/create/update/delete/set-default) reuses the workspaces.go shape with `is_system` non-deletable + in-use block-until-unassigned guards. The spawn engine fork (the milestone's risk center) branches on `SpawnOpts.AgentEngine`: claude byte-for-byte unchanged (`TestAgentLifecycle` fake-claude regression passes unchanged), custom renders the command template (tokenize-first-then-substitute — a real bug where initial replace-then-tokenize broke `{{worktree}}` values with spaces, caught + fixed) with running/exited status only (D-M001-2). 9/9 requirements verified (R012–R020); full Go suite (13 pkgs) + `tsc -b && vite build` green.

**v1.10 Configurable Agents — Phase 02 (Agent management UI and per-project selection) complete (2026-07-06)** — 5 plans. The management UI on the Phase 01 data layer. Frontend agents API client (`Agent` type + `useAgents` + CRUD/default mutations mirroring the workspaces client shapes); a Settings Agents section (ordered list with engine badges, add/edit/delete/set-default reusing `AgentNameDialog` + the v1.9 dialog shapes, delete disabled with a tooltip when `is_system` or in-use); a per-project agent selector. The Active Sessions bar LIVE filter widened to include `running` so custom-agent sessions appear (ranked between working and idle). Extra Claude params (`--dangerously-skip-permissions`) consolidated from an orphaned global setting onto the Claude agent's own row (migration 00014 `agents.extra_params` + `BackfillAgentExtraParams` copying the effective setting value — no one loses their config), edited in the Claude agent's edit dialog; the spawn path reads `extra_params` from the joined agent row instead of `settings.Get`. Human-verify gate passed (6-step loop); fake-claude regression intact; full Go suite + frontend build green. (AGUI-01/02/03, R021)

**v1.10 Configurable Agents — Phase 03 (opencode engine spawn and activity-based status) complete (2026-07-07)** — 4 plans. opencode joins Claude as a second built-in engine. Migration 00015 + `BackfillOpenCodeAgent` seed opencode as a non-deletable system agent (`engine='opencode'`, `is_system=1`); the opencode engine gets its own spawn branch in `sessions.go` (exempt from the custom-render tokenize-substitute path) with the `KAMACU_*` hook env injected so activity-based status works; `tasks.opencode_session_id` (nullable, migration 00016) persists the opaque `ses_…` id for resume (opencode has no transcript files — sessions live in `opencode.db`). Three shared tests that assumed a single-system-agent baseline were regression-fixed. A build-tagged real-opencode e2e harness proves the shipped plugin loads in opencode 1.17.15. Full Go suite green. (OCENG-01/02)

**v1.10 Configurable Agents — Phase 04 (Gated status plugin unlocks waiting and idle) complete (2026-07-07)** — 2 plans. A load-bearing defect fix: the opencode status plugin's `notify()` was a curl shell-out that silently no-op'd when curl was absent, so activity signals never reached the receiver and waiting/idle never unlocked. Rewritten on `fetch()`+`AbortController(3s)` — removing the silent failure mode; the 3s timeout bounds the post so a stuck receiver can't hang the plugin. A build-tagged real-opencode e2e harness proves three things: the shipped fetch-based plugin loads in opencode 1.17.15, its POSTs reach a receiver (Stop observed → idle unlocks), and the env-gate no-ops against the real binary — without burdening the normal test suite. (OCENG-03)

**v1.10 Configurable Agents — Phase 05 (Session resume and argv regression hardening) complete (2026-07-08) — final phase, milestone v1.10 done** — 3 plans. Closed the opencode restart-resume loop. `captureOpencodeSessionAsync` — an ASYNC bounded poll launched at spawn — discovers opencode's opaque `ses_…` id (minted sometime after start, so kamacu must DISCOVER it, not read at spawn) and persists it to `tasks.opencode_session_id`; best-effort + warn-only (a capture failure costs only the ability to resume after restart, not the live session), gated on the session's Done channel so it stops when the session exits. The opencode `--resume` argv keys off the persisted id, closing the restart-resume loop. Build-tagged real-opencode e2e proves the capture + persist lifecycle. `go build`/`vet`/`test ./...` green. **Phase 05 was the last phase of v1.10. v1.10 (Configurable Agents) shipped 2026-07-11** — 5 phases (01–05), 20 plans, 18/18 requirements (AGDATA·AGMGMT·AGSPAWN·AGUI·OCENG·OCRESUME); archived to `milestones/v1.10-*`.

**v1.11 Kamacu MCP Server — Phase 06 (MCP Subcommand Foundation) complete (2026-07-21)** — 3 plans across 2 waves (CLI dispatcher refactor ∥ token header rename → MCP serve subcommand). The foundation every Phase 07+ tool mechanically repeats. Plan 01 slimmed `cmd/kamacu/main.go` to a 14-line `google/subcommands` dispatcher and moved today's entire server body verbatim into `cmd/kamacu/serve.go`'s `serveCmd` (D-04 break-clean: bare `kamacu` prints help + exits non-zero; `kamacu serve` preserves all 5 flags and the full mux + SPA fallback + middleware stack). Plan 03 did a coordinated source-only rename `X-Kangent-Token` → `X-Kamacu-Token` across 7 files (3 prod + 4 test), byte-for-byte preserving the 32-byte `crypto/rand` token + `subtle.ConstantTimeCompare` gate + fresh-per-start re-injection (historical references under `.planning/`/`.gsd/` intentionally preserved). Plan 02 built the `internal/mcp/` package on `github.com/modelcontextprotocol/go-sdk v1.6.1`: `kamacu mcp serve` speaks JSON-RPC over stdio via Pattern 2 nested Commander (`mcpCmd` owns an inner `subcommands.Commander`); the per-process `*bridge` HTTP client sends `X-Kamacu-Token` on every request, defaults to `http://127.0.0.1:7333` with `KAMACU_HOOK_BASE` override, and returns raw TextContent passthrough capped at 1 MiB; `list_projects` is the one production tool registered, proving the bridge architecture end-to-end; the SC2 regression test drives server+client over `mcp.NewInMemoryTransports()` end-to-end proving handler-returned errors emit clean JSON-RPC error responses on stdout (never crashes, never stream desync); `internal/mcp/` is free of stdout pollution (no `fmt.Print*`/`log.Print*`/`os.Stdout.Write`); Pitfall 5 nil-return mapping makes `echo -n '' | kamacu mcp serve; echo $?` exit 0. 24/24 plan-level deliverables auto-passed via automated tests; UAT 1/1 passed (single confirmation checkpoint — pure backend plumbing, no UI); SECURITY verified (13/13 threats closed at L1 grep-depth, threats_open: 0, 10 accepted risks logged — SDK panic-recovery deferred to Phase 08 when `subscribe_session_output` introduces more panic surface). 3/3 requirements verified (MCPPROC-01/02/03); `go vet`/`test ./...`/`build` all green. `go test ./...` clean except pre-existing PTY-timing flakes that pass in isolation.

**v1.11 Kamacu MCP Server — Phase 07 (Tasks, Projects & Workspaces Tools) complete (2026-07-22)** — 4 plans across 2 waves (endpoints+scaffold → tasks ∥ projects ∥ workspaces tools). The mechanical-bulk phase: 16 MCP tools (6 task + 5 project + 5 workspace) satisfying all 13 v1.11 requirement IDs (MCPTASK-01..06, MCPPROJ-01..07), each a thin build-path-and-delegate to the shared `bridge.call`. Plan 01 laid the foundation: three Kamacu endpoint additions the bridge tools need (`GET /api/tasks` D-01 manual-only/status-ordered, `?workspace_id=` D-02 backward-compatible filter on `GET /api/projects`, and `GET /api/projects/{id}` Gap-1 single-project fetch with 404-on-missing), the D-07 per-resource file split (`internal/mcp/{tasks,projects,workspaces}.go`, `server.go` shrunk to a 3-line delegator), the extracted `bridge.call` shared helper (halves per-tool line count), and the corrected `list_projects(workspace_id?)` InputSchema (stale `project_id` no-op removed per 07-RESEARCH Pitfall 6). Plans 02/03/04 each filled one resource's `register*Tools` shell on the identical shape. D-03 partial-PATCH pointer fields (`*string`: nil=omit, ""=explicit-clear) carry through `update_task`/`update_project`/`update_workspace`; D-06 hardcodes `move_task`'s `after_id:null`; D-04 `create_project` two-arg fork drives the v1.4 managed-checkout atomic create; the v1.9 guarded workspace delete surfaces both 409 guards; `move_project_to_workspace` is a cross-route transfer (PATCHes `/api/projects/{id}` with a workspace_id-only body) owned by the workspaces registrar per the D-07 resource-acted-on split. One Rule-1 deviation (documented): `bridge.call` widened from HTTP 200-only to the full 2xx range so POST→201 and DELETE→204 surface as results instead of errors — without it every successful create/delete would report an error to the agent. 4/4 ROADMAP success criteria verified against the code; 43 tests pass in `internal/mcp/` (10 task + 12 project + 10 workspace + 11 carried-over); `go test ./...` (16 pkgs) + `go build`/`vet` green; no human-verify items (pure backend, no UI/real-time/external-service components outside HTTP loopback exercised by httptest). No DB schema changes, no migrations, no new goroutines (v1.11 milestone rule preserved).

**v1.11 Kamacu MCP Server — Phase 08 (Sessions & Terminal Read Access) complete (2026-07-23)** — 3 plans across 2 waves (Kamacu read-only endpoints ∥ three non-streaming MCP tools → subscribe streaming tool + SC3 proof). The milestone's risk center and only genuinely new capability: an agent can enumerate sessions and read terminal output — snapshots immediately, live tails bounded — without ever injecting bytes into a PTY. Plan 01 (internal/api/sessions.go) added three read-only GET handlers on top of the existing `*session.Session` primitives (Snapshot/Attach/Detach/Done/Info — no migration, no new background goroutine, request-scoped subscribe reader): `GET /api/sessions/{id}` (D-12 live OR exited, 404 unknown), `GET /api/sessions/{id}/output?bytes=N` (base64 D-08 envelope, default 4 KB / clamp 512 KiB + `clamped` flag), `GET /api/sessions/{id}/subscribe` (plain-HTTP chunked octet-stream, default 30s/hard-cap 300s, attach→drain-replay→stream→defer-Detach), plus a `?project_id=N` filter (one task-set query, in-memory filter — never N round-trips) and a batch task→project→agent JOIN (`taskTitle`/`projectName`/`agentName`, the agents.go:99 shape). Plan 02 (internal/mcp/sessions.go + server.go) added the three non-streaming tools (`list_sessions`/`get_session`/`get_session_output`) repeating the Phase 06/07 bridge pattern, and established the deferred `withRecover` panic wrapper (Phase 06 Open Q1 — prerequisite for the streaming surface Plan 03 introduces) applied to every session handler. Plan 03 added `subscribe_session_output` — the riskiest tool — which diverges from `bridge.call` (10s timeout, body-buffered): a dedicated no-timeout streaming `*http.Client`, incremental accumulate under a 1 MiB most-recent cap (D-04), `ctx.Done()` partial-return (D-01/SC3), and a follow-up GET for the exit marker (D-03). **Read-only contract (D-14) proven at the type level:** scoped grep gates show zero `WriteInput`/`FrameData` refs in both files and no `internal/session` import in the bridge — an agent can never inject PTY bytes. **SC3 (the milestone's central regression gate) proven structurally:** go-sdk@v1.6.1 returns `(nil, context.Canceled)` to the client (its own `Example_cancellation`), so a handler-side recorder observes the partial-envelope return; `defer sess.Detach(connID)` runs on every return path, `NumGoroutine` delta is 0 across N=5 subscribe/cancel cycles, and the fake streaming server's `r.Context().Done()` fires within 2s (detach propagation confirmed). Two Rule-1 auto-fixes (comment wording to avoid false-tripping the literal `FrameData` grep gate; SC3 test reshaped to a handler-side recorder to match the SDK's actual cancellation surface). 4/4 ROADMAP success criteria verified against the code; 61 `internal/mcp` tests + `internal/api` tests green; code review 0 blockers (4 warnings/2 info, advisory — WR-04 subscribe-stall WriteTimeout is the only theoretical-production-impact one, SC3's cancel-via-ctx path unaffected); `go build`/`vet`/`test ./...` green (one pre-existing timing flake in `internal/session/TestSpawnPumpFillsRing` under parallel load, a package Phase 08 did not modify).

**v1.11 Kamacu MCP Server — Phase 09 (PR Review Tools) complete (2026-07-28) — final phase, milestone v1.11 fully implemented** — 1 plan. The mechanical closer: 3 thin MCP tools (`list_pending_reviews`, `list_recently_reviewed`, `open_review`) that bridge Kamacu's existing v1.3/v1.5 `internal/github` HTTP surface with zero new endpoints, zero new `gh` calls, and zero new gates. `internal/mcp/reviews.go` (NEW, 337 LOC) owns a shared `listReviews(project_id, queue)` helper that GETs the cached `/api/projects/{id}/pull-requests` endpoint and projects the chosen queue (`prs` ↔ `reviewed`) verbatim — both list tools share one HTTP call and the existing per-repo `repoEntry` TTL cache (the no-N+1 invariant is structural: `fetchLists` already fetches both queues in one `gh` cycle). `open_review` is a verbatim `bridge.call` POST to `/api/projects/{id}/pull-requests/{n}/review` — the v1.3 Phase 12 find-or-create `source='github_pr'` path with worktree on the PR's real head branch; the bridge adds zero logic (no gate checks, no settings refs, no DB access), so the board-leak guard and dedup live entirely server-side. Degrade branch (D-02) preserves the raw upstream body and surfaces `IsError:true` when GitHub integration is off / no linked repo / `gh` absent — never bypassing the v1.3 two-gate ladder (toggle → link). `project_id` is REQUIRED (locked D-04 refinement of the roadmap's `?` signature — keeps every tool a single HTTP call; documented in `09-CONTEXT.md`/`09-RESEARCH.md` Pitfall 4). 6 review tests (happy + state≠ok + 409 per tool); code review 0 blockers (1 warning WR-01: empty-queue success path emits `"null"` not `"[]"` — non-blocking, no SC fails); `go build`/`vet`/`test ./internal/mcp/...` green (full suite green modulo one pre-existing timing flake in `internal/session` unrelated to phase scope). 13/13 must-haves verified (3/3 SC). **Phase 09 was the last phase of v1.11. v1.11 (Kamacu MCP Server) fully implemented 2026-07-28** — 4 phases (06–09), 11 plans, all v1.11 requirement IDs satisfied (MCPPROC·MCPTASK·MCPPROJ·MCPSESS·MCPREV). Run `/gsd:complete-milestone` to audit and archive.

**v1.12 Activity & Statistics — Phase 10 (Activity Data & API) complete (2026-07-30)** — 3 plans across 2 waves (github data source ∥ pure helpers → combined activity endpoint). The backend that powers the v1.12 Activity page (Phase 11 builds the UI on top). Plan 10-01 added the merged/closed reviews data source to the shared `*github.Service`: `GetMergedClosed` with its own 5min-TTL per-repo cache (D-03/D-04, fully decoupled from the 60s review-column cache), backed by `listCompletedReviews` — a `reviewed-by:@me draft:false is:closed` gh search (`--limit 100`, the authoritative merged+closed filter per GitHub docs #5599; `--state closed` belt-and-suspenders only since cli/cli #8102), returning `ReviewDoneSummary` values; a shared pure `classifyGhListError` extracted so `listPRs` and `listCompletedReviews` never drift. Plan 10-03 extracted the PURE logic the handler consumes into a stdlib-only `internal/api/activity_helpers.go` (scope/window parsing, in-Go cycle/dwell/median stat compute, review-state rollup, a shared mismatched-precision ISO-time parser) — no DB, no gh — shipping as a wave-1 sibling. Plan 10-02 built `GET /api/activity`: ONE combined handler answering {tasks-done, reviews-completed, stats} for any Global/Workspace:N/Project:N scope and Week(7d)/Month(30d) window — runs the tasks-done SQL (D-12), computes cycle/dwell/median stats in Go (D-10/D-11), aggregates merged/closed reviews concurrently across in-scope linked repos via `GetMergedClosed` (errgroup SetLimit 5 + per-repo 12s timeout, D-07/D-08), enforces the GATE 1 toggle (D-02 — `github_integration != "on"` → `reviews.state="disabled"` with zero gh spawns), and ALWAYS returns HTTP 200 with degradation riding in `reviews.state` (REVIEWS-04). 7/7 requirements verified (TASKS-01, REVIEWS-01, REVIEWS-04, STATS-01..04); UAT 16/16 (15 automated + 1 live gh smoke); `go build`/`vet` + scoped tests green. Frontend Activity page is Phase 11.

**v1.12 Activity & Statistics shipped (2026-08-25)** — Phase 11 (Activity Page & Controls, 3 plans) delivered the `/activity` route + ScopeSelector/WindowToggle/StatsStrip/ActivityList/ReviewsList + sidebar entry, and closed the UAT-surfaced "black page on empty instance" blocker (non-nil-slice wire contract, byte-level regression-tested). Phase 12 (Activity Chart & Stat Tooltips, 2 plans) added the recharts stacked-bar chart via the shadcn chart primitive (sole new dep), zero-day baseline stubs, sparse Month x-axis, per-bar hover tooltips, explanatory stat-strip tooltips, and absolute formatDateTime entry timestamps. Phase 12.1 (INSERTED, 1 plan, UAT 3/3 on 2026-08-25) closed the milestone audit's three tech-debt warnings — WR-01 enabled-gate wiring, WR-02 stale-scope self-heal (useMemo demotion, never persisted), WR-03 reviews-link accessible names. Milestone audit: requirements 18/18 + 3 TD-ACT hardeners, integration 9/9, flows 4/4. v1.12 totals: 4 phases, 9 plans, 20 tasks, 21/21 requirements; archived to `milestones/v1.12-*`; tag v1.12.

## Next Milestone

**v1.13 Global Task scoped 2026-08-25 — see Current Milestone above.** Prior-milestone candidates remain parked below.

Banked forward investments worth a future milestone:
- **v1.11 MCP follow-ups** (scoped in `milestones/v1.11-REQUIREMENTS.md` "v1.12+ Requirements"): per-task auto-scoping convenience tools (`get_my_task`/`get_my_session`, ToolFilter — MCPAUTO-01..03); agent-CLI auto-registration at spawn (`.mcp.json` / `opencode.json` writers — MCPREG-01..03); additional tool categories — agents CRUD, settings, worktree-cleanup ops (MCPMORE-01..03); quality/hardening — typed JSON-RPC error taxonomy, stdout-pollution guards, real-binary e2e harness (MCPHARD-01..03).
- v1.10 configurable-agents follow-ups: additional built-in engines beyond claude + opencode with their own status heuristics + restart-resume (AGFUT-03 — e.g. codex, gemini CLI as first-class seeds); per-agent workspace-scoped overrides (AGFUT-01); agent template export/import sharing (AGFUT-02).
- v1.7 project-icon follow-ups, scoped in `milestones/v1.7-REQUIREMENTS.md` "Future Requirements": image/logo icons (ICON-FUT-01), emoji icons (ICON-FUT-02), free-form hex color beyond the curated swatches (ICON-FUT-03), drag-to-reorder projects in the rail (ICON-FUT-04), and Add-project (+) / Settings (gear) reachable as icons in the collapsed rail so it is fully functional without expanding (ICON-FUT-05).
- The v1.4 managed-checkout follow-ups, already scoped in `milestones/v1.4-REQUIREMENTS.md` "Future Requirements": on-demand checkout sync (CKMNT-01), non-default base branch at create (CKMNT-02), shallow/partial clone for large repos (CKMNT-03), and live clone-progress streaming + cancel (CKUX-01). The frontend `Project.managed` wire field is already in place to hang a managed-delete cleanup affordance on.
- Per-status task timestamps (`todo_at`/`in_progress_at`/`in_review_at`/`done_at`, migration 00006) ready to power board cycle-time / dwell-time stats.
- The background-goroutine reaper pattern (Done-TTL + PR reconcile passes) that future periodic maintenance (e.g. MAINT-01 stale-worktree purge) can model on.
- Deferred v1.3 GitHub follow-ups already scoped in the archived `milestones/v1.3-REQUIREMENTS.md` "Future Requirements": richer PR cards (diff size, fork pill, head→base line, review-decision/labels — GHCARD-01..04), filter options (team review requests, draft PRs — GHFILT-01/02), and broader surfaces (cross-project review inbox, author-side PRs — GHWIDE-01/02).

**Known tech debt (non-blocking):** ~29 pre-existing `react-hooks` eslint errors (primarily `set-state-in-effect` in TaskPage.tsx) — logged in `milestones/v1.12-phases/12-activity-chart-tooltips/deferred-items.md`; a dedicated lint-cleanup pass is the right home. The v1.12 audit's WR-01..03 tech debt is CLOSED (Phase 12.1).

## Deferred (post-v1.1)

Parked candidates: browser notifications on waiting/finished (NOTF-01), one-click "Move to In Review?" on the Stop hook (NOTF-02), stale-worktree purge list (MAINT-01), ~~MCP server for agent board access (AGNT-01)~~ ✓ delivered in v1.11, per-project settings overrides. One carried bug to confirm: whether plan-mode exit-plan approval triggers the amber waiting dot (research OQ1) — still unobserved as of Phase 6's gate (user approved without reporting it).

## Context

- Inspiration: layout and basic features of SlayZone (https://github.com/debuglebowski/SlayZone), but as a web app instead of a desktop app, in the spirit of vibe-kanban (https://github.com/BloopAI/vibe-kanban)
- Vibe-kanban-style worktree-per-task isolation: each task works on its own branch in its own worktree, so multiple agent sessions never collide in one checkout
- The agent is a real CLI (Claude Code, opencode, or a user-configured custom agent) in a pseudo-terminal — not an SDK-driven chat UI — so the full interactive experience (plan mode, slash commands, permission prompts) works as-is
- Server-side session persistence means the backend owns PTYs; the browser is just an attached view (xterm.js or similar). A server restart loses live processes; resuming via `claude --resume` is the recovery path

## Constraints

- **Tech stack**: Go backend, React frontend — user's choice
- **Deployment**: Single binary/process serving API + static frontend at localhost — local-only by design
- **Storage**: Local database (e.g., SQLite), no external services
- **Agent**: Claude Code CLI must be installed on the host; the app spawns it, never reimplements it

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Claude Code CLI in a PTY (not Agent SDK chat UI) | Full interactive CLI experience with zero reimplementation | ✓ Good — full TUI/plan-mode/hooks work unmodified (Phase 4) |
| Worktree + branch auto-created per task | Isolates concurrent agent sessions; vibe-kanban-proven model | ✓ Good (Phase 3) |
| Sessions persist server-side | Browser is a detachable view; work survives tab closes | ✓ Good — detach/reattach + restart resume (Phases 2, 5) |
| Explicit Start button for agent sessions | Most control, least surprise; no accidental agent runs | ✓ Good (Phase 4) |
| Go backend + React frontend | User preference | ✓ Good — single embedded binary |
| Manual git workflow, app only cleans up worktrees | Keeps v1 scope lean; user merges/PRs in terminal | ✓ Good (Phase 3) |
| Fixed columns: To Do / In Progress / In Review / Done | In Review holds agent-finished work awaiting human check | ✓ Good (Phase 1) |
| D-51 reversed: `--dangerously-skip-permissions` on by default (Phase 6, AGENT-02) | Dispatcher workflow favors unattended agents; flag is a removable settings default, so interactive prompts are one edit away | ✓ Intentional — documented side effect: amber waiting dot rarely fires while the flag is active |
| Quota via server-side proxy of the OAuth usage endpoint, never writing/refreshing the token (v1.2, Phase 7) | Browser never holds credentials; token rotation stays Claude Code's job; degrade-don't-break on every failure mode | ✓ Good — `internal/quota`, six-state matrix, token-keyed cache |
| Invisible tmux: × kills, leaving the task view detaches, sessions survive a server restart and auto-reattach with NO Resume button (v1.2, Phases 8–9, D-77/D-78/D-88) | Durability is the point; the user shouldn't have to know it's tmux. Reattach is cheap/lossless so it diverges from the agent's explicit Resume | ✓ Good — restart-survival verified end-to-end (the milestone's headline) |
| Done-TTL reaper kills idle bash/tmux/agent sessions of long-Done tasks; agent stays resumable; worktrees never auto-removed (v1.2, Phase 9, REAP-01/D-87/D-96) | Sessions shouldn't pile up on finished work; killing a PTY is reversible (transcript kept) but deleting a worktree is not | ✓ Good — codebase's first background goroutine, keyed on `done_at` |
| PR Review column reads `user-review-requested:@me draft:false` via the `gh` list call, with `statusCheckRollup` riding the same call (no N+1) and degraded states classified by exit-code+stderr (v1.3, Phase 11) | Pure read path stands up the whole `gh` integration at lowest risk; degrade-don't-break on every failure mode; rate-limit-bounded by one cached call | ✓ Good — always-200 endpoint, per-repo cache, never blocks the board |
| Review column **defaults to collapsed** per-project (v1.3, Phase 11, supersedes D-05/D-06) | User-directed change during the human-verify checkpoint — the review queue is secondary to the task board, so it stays out of the way until opened | ✓ Intentional — `localStorage` absent key reads as collapsed; explicit expand persists |
| PR review worktree checks out the PR's **real head branch** (named, `pr/<n>` fallback on collision), not detached (v1.3, Phase 12, supersedes D-01) | User-directed during the Phase 12 human-verify — a detached HEAD reads as "not checked out"; a named branch matches GHREV-01's wording. Collision fallback keeps GHREV-05 safety (never reuse/move an existing ref; main checkout HEAD unchanged) | ✓ Good — `worktree add -b`, collision-checked; fork same-name `master` lands on `pr/<n>` |
| Configurable PR-review seed prompt via a global Settings field (v1.3, Phase 12) | User-requested during human-verify — was deferred at discuss-time, pulled in once the feature was live | ✓ Good — `pr_review_seed` KV key (default template, `<n>`/`<title>` placeholders), prefilled once per agent session, never auto-sent |
| PR worktrees auto-removed on merge/close, but only when pristine+idle; branch always kept (v1.3, Phase 13, GHCLN-01/02) | Closes the loop without ever bulldozing work — the reaper detects merge/close server-side; any dirty/unpushed/stashed/busy worktree is left for manual cleanup | ✓ Good — second reaper pass + shared `CleanupWorktreeGated` (force=false); auto deletes the row, manual keeps it nulled |
| Agent state on PR cards is a colored **left rail**, not a `StatusDot` dot (v1.5, Phase 16, supersedes the planned SIGNL-01/02 dot) | User-directed at the human-verify gate: a filled dot beside the line-art CI glyph clashed (different styles, misaligned). The rail carries the full state by color and splits the two signals onto separate channels (left edge = your agent, right glyph = the PR's CI) | ✓ Good — `agentRail()` 3px span; green working / pulsing-amber waiting / blue idle / gray exited; no dot, zero layout shift |
| Two PR lists from one cached `gh` cycle: `user-review-requested:@me` (awaiting) + `reviewed-by:@me` (recently reviewed), server-side top-precedence deduped (v1.5, Phase 16, REVWD-01/03/04) | Keeps reviewed-but-unmerged PRs in view without a second poll or N+1; `--state open` bounds the list (drops on merge/close) so no retention bookkeeping; one classified state/stale degrades both together | ✓ Good — `fetchLists`/`dedupeReviewed` in the existing `Service`; both lists ride one `repoEntry` |
| Global sessions bar mounts in the app shell (`AppLayout`, bottom, outside `<Outlet/>`) and is a 4th consumer of the existing 5s `/api/agents/status` poll — the only backend change is a JOIN of `tasks.title`+`projects.name` onto both query passes (v1.6, Phase 17, SBAR-01/08/10) | Reusing the single status feed (no new endpoint, no new migration, no second poll) keeps the bar cheap and consistent with the card dots/sidebar chips; columns already existed (mirrors v1.5's `prNumber`/`source` widening) | ✓ Good — one query fans out to 6 consumers; tests cover both passes |
| Expanded sessions panel floats UP over content as a fixed overlay, not a docked "push content up" layout (v1.6, Phase 17, D-03) | A docked panel would resize the height-sensitive xterm terminals on every expand/collapse; an overlay never reflows `<main>`. User-confirmed at the human-verify gate (critical no-reflow check passed) | ✓ Good — `fixed inset-x-0 bottom-0`; terminal proven not to reflow |
| Sessions bar shows LIVE sessions only (working/waiting/idle); exited/resumable drop off immediately; in-bar alerting only (no browser/OS notifications) (v1.6, Phase 17, D-09/D-14) | Keeps "active sessions" honest (exited stays discoverable on the card); browser notifications (NOTF-01) deliberately deferred to keep the milestone tight | ✓ Good — client live-filter; SBAR-FUT-03 parks notifications |
| One shared `<ProjectAvatar>` + one curated palette as a single source of truth: Go (`internal/api/icons.go projectPalette`) mirrored byte-for-byte in TS (`web/src/lib/palette.ts`), used by random-default assignment AND the settings swatch picker (v1.7, Phases 18–19, ICON-03/05/10/12) | One component renders the rail icon and the inline-expanded avatar (no drift); one palette constant means the random default and the editable swatches can never disagree, and white-text contrast is guaranteed by curation | ✓ Good — avatar reused in both sidebar states; palette change touched exactly two files + one migration |
| Project icons are name-derived letters + a random curated-palette color, auto-assigned at create with an idempotent startup backfill for pre-existing rows; editing is a ≤2-uppercase input + preset swatches only (no free hex / image / emoji) (v1.7, Phase 18, ICON-01..04, ICON-FUT-01/02/03) | Zero-effort identity for every project (new and old) with no blank states; curated swatches keep the dark-theme palette consistent and legible; richer icon types deferred to keep the milestone a thin slice | ✓ Good — migration 00009 + `BackfillProjectIcons`; image/emoji/hex parked as ICON-FUT-01/02/03 |
| v1.7 UAT reversed two planned avatar visuals and re-muted the palette, accepted as the new contract (v1.7, Phase 19, supersedes D-04/D-06/D-01/D-13) | User-directed at the human-verify gate: `rounded-md` squares read worse than circles; a dim ring was invisible and a near-white ring was ugly, so active state reuses the expanded selection's filled-row highlight; the bright Tailwind-600 hues were too loud against the dark rail | ✓ Intentional — circular avatars, rail-row active highlight, 9 muted hues; migration 00010 remapped existing rows by palette position (a Phase 18 file edited during Phase 19 UAT) |
| Per-file diff "Viewed" persisted server-side in a keep-history `diff_viewed` table keyed on `(task_id, file_path, diff_hash)`; a changed file auto-resets to un-viewed + re-expanded (v1.8, Phase 22, DIFF-03/04) | Viewed must survive reopen AND server restart (not client-only) and must re-surface a file the instant its content changes — a content-hash PK means a changed file has no matching row (reads un-viewed) while reverting restores the old row (checkmark returns), with zero cleanup bookkeeping | ✓ Good — migration 00011; `DiffTab` remounts sections on `key={path:hash}` so the changed file remounts un-viewed + expanded |
| GitHub-style **stacking** sticky per-file diff headers + a non-sticky flow anchor for tree-jump + a viewport-pinned app shell (v1.8, Phase 22, DIFF-01/02, UAT-directed) | Boxed per-file cards trapped each sticky header (prior file's content showed above the next header); a sticky element's `offsetTop` reflects its *pinned* position so upward tree-jumps failed; `min-h-svh` on the shell let tall diffs scroll the whole window, dragging the tree away. Chosen at the human-verify gate | ✓ Good — `display:contents` Collapsible + fixed totals bar (exactly one header pinned), scroll to a non-sticky `[data-diff-anchor]` (both directions), `h-svh overflow-hidden` shell (only the diff pane scrolls) |
| Workspace delete is block-until-empty and keyed off the `is_default` flag, never the name "Personal" (v1.9, Phase 26, WSMGMT-03/04, D-05/D-06) | Never bulldoze a project (+ its worktrees/sessions) as a side effect of deleting a workspace; keying the guards off `is_default` (not the literal name) keeps the protection rename-proof, and a non-empty `COUNT` guard with an `ON DELETE RESTRICT` FK backstop guarantees at least one workspace always exists | ✓ Good — server-side 409 with a clear "transfer or remove its projects first" message; Personal renamable but never deletable |
| Project transfer reuses the existing `PATCH /api/projects/{id}` with an optional validated `workspace_id` — no dedicated transfer route (v1.9, Phase 26, WSPROJ-01, D-17) | The partial-PATCH path already validates + persists optional project fields; adding a target-validated `workspace_id` is the minimal surface, and create-into-workspace is the symmetric optional field on `POST` (Personal fallback) | ✓ Good — one field on the existing routes; "Move to" ⋯ submenu with follow-the-project |
| Active workspace is one shared React context (localStorage `kamacu.workspace`, `is_default` fallback), kept OUT of the URL; a `lastSyncedProjectId` ref gates the URL→workspace reconciliation (v1.9, Phase 26, WSNAV-01/02/03, D-14) | A single context (not per-component hooks) means the switcher and the sidebar `.filter` never desync; workspace stays localStorage-only per Out of Scope. The empty-workspace-switch bug (React-Router-v7 wraps `navigate()` in `startTransition`, so the URL lags the synchronous state set and the still-mounted old route reverted the switch) is fixed by re-anchoring only when the URL's `projectId` actually changes | ✓ Good — one `.filter` scopes both sidebar surfaces; deep-link URL-wins preserved; WSBAR-01 sessions bar left cross-workspace |
| The `engine` column encodes a capability tier (`claude` \| `custom` \| `opencode`) — spawn branches on it, never name-string matching (v1.10, Phase 01, R012, D-M001-1) | Name-string matching is brittle (renames break it) and unextensible; a typed engine column lets each engine fork cleanly (claude keeps hooks/resume, custom keeps it simple, opencode gets its own branch) and new engines are an enum addition, not a string patch | ✓ Good — claude path frozen + fake-claude regression green; custom + opencode branches additive |
| Custom agents report running/exited status only; no hook overlay, no BEL scanning, no `--resume` (v1.10, Phase 01, R017, D-M001-2) | Custom TUIs can't be reliably introspected for working/waiting/idle — guessing would be dishonest. Running/exited (PTY process liveness) is what we can reliably know. opencode gets real heuristics via its own engine branch + plugin, not the generic custom path | ✓ Good — `agentStatusLocked` engine branch; capability-tier-specific status without a separate pipeline |
| Extra Claude params (`--dangerously-skip-permissions`) consolidated onto the Claude agent's own row, not an orphaned global setting (v1.10, Phase 02, AGUI-03, migration 00014) | Agent configuration belongs on the agent row, not in a disconnected settings field; `BackfillAgentExtraParams` copies the effective setting value so no one loses their config or the default; the spawn path reads `extra_params` from the joined agent row instead of `settings.Get` | ✓ Good — one place to configure an agent; fake-claude regression intact |
| opencode status plugin uses `fetch()`+`AbortController(3s)`, not a curl shell-out (v1.10, Phase 04, OCENG-03) | curl was a silent no-op when absent, so activity signals never reached the receiver and waiting/idle never unlocked — a load-bearing defect. `fetch` is available in the opencode plugin runtime; the 3s timeout bounds the post so a stuck receiver can't hang the plugin. Status-signal delivery must fail loudly or time out, not silently no-op | ✓ Good — waiting/idle unlock reliably; build-tagged e2e proves POSTs reach a receiver |
| opencode session identity is an opaque foreign-minted id discovered via async bounded poll, not read at spawn (v1.10, Phase 05, OCRESUME-02) | opencode mints its `ses_…` id sometime after start, so kamacu must DISCOVER it. Best-effort + warn-only: a capture failure costs only restart-resume, not the live session. The poll is Done-channel-gated so it stops on exit. Never mixed with claude's kamacu-minted id (present at spawn) | ✓ Good — `captureOpencodeSessionAsync`; `--resume` keys off the persisted id; restart-resume loop closed |
| Any API field typed as a JSON array is initialized as a non-nil Go slice on EVERY return path (happy-empty, error, degrade), with byte-level regression tests asserting the wire shape (v1.12, Phase 11, ACT-01 gap-closure) | Go nil slices marshal to JSON `null`, but the frontend types arrays and iterates without null guards — on an empty instance `/activity` shipped `tasks:null`/`prs:null`, React threw on `for...of null`, and the page blanked to black. The canonical fix is at the source of truth (`make([]T,0)`/`[]T{}` on every path incl. error/degrade), with frontend `?? []` as defense-in-depth. A decoded `len==0` check can't catch a nil-slice regression (`json.Unmarshal` accepts null), so the regression test asserts on the RAW body bytes (`"tasks":[]`, never `"tasks":null`) | ✓ Good — `TestActivityHandler_EmptyInstanceNonNilSlices` locks the byte-level contract; three-layer frontend null defense; empty-instance black page fixed in-plan during UAT |
| `GET /api/activity` is ONE combined endpoint ({tasks, reviews, stats}) with a 5min-TTL merged/closed cache fully decoupled from the 60s review-column cache, and `is:closed` as the authoritative merged+closed gh search qualifier (v1.12, Phase 10, D-01/D-03/REVIEWS-04) | One endpoint = one TanStack query + one loading state; degradation rides in `reviews.state` with an always-200 body so the page never blanks. A separate activity cache keeps the 5s-poll review column from tripling gh load; relying on `--state closed` alone would break when gh fixes cli/cli #8102 | ✓ Good — always-200 + GATE 1 proven; reviews quietly empty when GitHub is off |
| Stale/deleted persisted Activity scope demotes to effective Global via a `useMemo` over `[savedScope, workspaces, projects]` that never calls a setter — the demotion is ephemeral by construction (v1.12, Phase 12.1, TD-ACT-02/D-06) | D-06 (never persist a demotion — the dead id may come back, e.g. an offline workspace) is satisfied structurally: the memo returns the literal global without touching localStorage, mirroring `useActiveWorkspace`'s fallback, and avoids the react-hooks/set-state-in-effect lint-debt class | ✓ Good — UAT-verified: label/query/selection agree on Global while Storage still holds the dead id |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd:transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd:complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-08-25 to start v1.13 Global Task. Milestone Status flipped to v1.13 planning. Current Milestone section added; Active section reset to the v1.13 scope (a single global scratchpad: task-like view with agent + bash tabs only, sessions running directly in a configured folder/managed-clone root — no worktree; configured in Global Settings with folder-or-GitHub-repo root + default agent; reachable from the Active Sessions bar when live, from Settings when idle). Phase numbering continues from the previous milestone (no reset — v1.12 ended at phase 12.1, v1.13 starts at phase 13). Prior evolution entry preserved below.*

*Last updated: 2026-08-25 after v1.12 Activity & Statistics milestone close. Full evolution review: Milestone Status flipped to shipped (4 phases 10–12.1, 9 plans, 20 tasks, 21/21 requirements); Phase 12.1 hardening entries added to Recently Validated (TD-ACT-01..03); consolidated v1.12 Current State entry added; two new Key Decisions logged (combined activity endpoint + decoupled 5min cache + is:closed qualifier; ephemeral useMemo scope demotion); Next Milestone reset to "not yet scoped"; lint tech-debt note refreshed (WR-01..03 closed, ~29 react-hooks errors remain parked in deferred-items). Archived to `milestones/v1.12-*`; tag v1.12.*

*Last updated: 2026-08-01 after Phase 12 (v1.12 Activity & Statistics milestone complete). Phase 12 added the activity chart (recharts stacked-bar, Week=7/Month=30 bars, hover tooltips), stat-strip explanatory tooltips, and absolute per-entry completion times (CHART-01/STAT-01/ENTRY-01); no backend change. UAT 10/10 passed, verification canonicalized to passed. Three v1.12 chart/tooltip requirements moved to Validated; Milestone Status flipped to "v1.12 complete". v1.12 now 3 phases (10–12), 6 plans — ready for `/gsd-complete-milestone` audit + archive.*

*Last updated: 2026-07-30 after Phase 10 (activity-data-api) completion — the first phase of v1.12. New Current State entry for Phase 10 (the `GET /api/activity` backend + merged/closed reviews data source + pure stat helpers). 7/7 Phase-10 requirement IDs (TASKS-01, REVIEWS-01, REVIEWS-04, STATS-01..04) verified complete in REQUIREMENTS.md.*

*Last updated: 2026-07-29 to start v1.12 Activity & Statistics. Milestone Status flipped to v1.12 planning. Active section reset to the v1.12 milestone scope (a new top-level Activity page: tasks-done + reviews-done lists over a rolling Week/Month window, scoped Global/Workspace/Project, plus a statistics section with counts and min/max/median cycle + per-column dwell times built on the banked v1.2 per-status timestamps). Built on the v1.3/v1.5 GitHub integration (reviewed-by:@me merge/close) and migration 00006. Phase numbering continues from the previous milestone (no reset).*

*Last updated: 2026-07-23 after Phase 08 (sessions-terminal-read-access) completion. Added one validated entry to Requirements (MCPSESS-01/02/03/04 — four MCP session tools + read-only terminal access, the milestone's risk center). New Current State entry for Phase 08. Active section unchanged (the v1.11 milestone scope already covered read + subscribe terminal access; Phase 08 delivered it). One new Key Decision implicit in the entry: the read-only D-14 contract is type-level-enforced (scoped grep gates prove zero PTY-write refs), and SC3 (<100ms detach-on-cancel) is structurally proven via a handler-side recorder because go-sdk@v1.6.1 returns `(nil, context.Canceled)` to the client.*

*Last updated: 2026-07-28 after Phase 09 (pr-review-tools) completion — the final phase of v1.11. Added three validated entries to Requirements (MCPREV-01/02/03 — the 3 PR review tools: `list_pending_reviews`, `list_recently_reviewed`, `open_review`, all thin bridges over the existing v1.3/v1.5 `internal/github` surface). New Current State entry for Phase 09. v1.11 Kamacu MCP Server is now fully implemented (4 phases, 11 plans) — ready for `/gsd:complete-milestone` to audit and archive.*

*Last updated: 2026-07-22 after Phase 07 (tasks-projects-workspaces-tools) completion. Added four validated entries to Requirements (MCPTASK-01..06 task tools, MCPPROJ-01..05 project tools, MCPPROJ-06/07 workspace tools, and the bridge infrastructure — D-07 per-resource split + `bridge.call` shared helper). New Current State entry for Phase 07. Active section unchanged (v1.11 milestone scope already covers MCPTASK/MCPPROJ).*

*Last updated: 2026-07-21 to start v1.11 Kamacu MCP Server. Active section reset to the v1.11 milestone scope (the promoted-and-expanded AGNT-01 — full-surface MCP server for agents running inside Kamacu). Three settled decisions logged in Active (actor = agents-inside, transport = stdio subcommand, session I/O = read + subscribe only). Two new Out-of-Scope items (MCP PTY writes; MCP for external editors). Prior evolution entry preserved below.*

*Last updated: 2026-07-11 after v1.10 Configurable Agents milestone. Full evolution review: "What This Is" now names configurable agents + multiple engines; the v1.10 milestone block added to shipped history; all 18 v1.10 requirements (AGDATA·AGMGMT·AGSPAWN·AGUI·OCENG·OCRESUME) moved to Validated (Phases 01–05); Active reset to "next milestone not yet scoped"; six v1.10 Key Decisions logged (typed engine column, custom running/exited-only status, extra-params consolidated on the agent row, fetch-based status plugin, async opencode session-id capture, opencode restart-resume). "Multiple agent CLIs" Out-of-Scope item revised (configurable agents + opencode shipped; additional first-class engines deferred as AGFUT-03). v1.10 shipped 2026-07-11 — 5 phases, 20 plans, 18 tasks; archived to `milestones/v1.10-*`.*

*Last updated: 2026-07-31 after v1.12 Activity & Statistics milestone. All 8 v1.12 requirements (ACT·TASKS·REVIEWS) validated across Phases 10–11; Active reset to "next milestone not yet scoped"; Milestone Status flipped to "just shipped v1.12". One new Key Decision logged (non-nil-slice wire contract as the canonical JSON-array contract + byte-level regression tests). v1.12 implemented 2026-07-31 — 2 phases (10–11), 6 plans, 8/8 requirements; ready for `/gsd:complete-milestone` audit + archive.*

*Last updated: 2026-07-29 after v1.11 Kamacu MCP Server milestone. Full evolution review: the v1.11 milestone block added to shipped history; all 23 v1.11 requirements (MCPPROC·MCPTASK·MCPSESS·MCPREV·MCPPROJ) already in Validated from per-phase transitions; Active reset to "next milestone not yet scoped"; Next Milestone updated (v1.11 follow-ups banked: MCPAUTO, MCPREG, MCPMORE, MCPHARD); AGNT-01 marked delivered in Deferred. Milestone Status flipped to "planning next milestone". v1.11 shipped 2026-07-29 — 4 phases (06–09), 11 plans, 23/23 requirements; archived to `milestones/v1.11-*`; tag v1.11.*

*Last updated: 2026-08-01 after Phase 12 (v1.12 Activity & Statistics milestone complete). Phase 12 added the activity chart (recharts stacked-bar, Week=7/Month=30 bars, hover tooltips), stat-strip explanatory tooltips, and absolute per-entry completion times (CHART-01/STAT-01/ENTRY-01); no backend change. UAT 10/10 passed, verification canonicalized to passed. Three v1.12 chart/tooltip requirements moved to Validated; Milestone Status flipped to "v1.12 complete". v1.12 now 3 phases (10–12), 6 plans — ready for `/gsd-complete-milestone` audit + archive.*
