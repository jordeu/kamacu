# Phase 12.1: Address Activity tech debt (WR-01..03) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-02
**Phase:** 12.1-address-activity-tech-debt-wr-01-03
**Areas discussed:** none (all deferred to recommended option)

---

## Process note

The four gray areas identified (WR-01 gate strategy, WR-02 stale-scope handling, WR-03 a11y fix shape, verification approach) were presented for selection. The user declined to discuss any of them and asked the agent to **choose the recommended option for each** ("none of them, choose recommended options"). The table below records the alternatives that were on offer and the recommended option locked in each case. Full rationale lives in `12.1-CONTEXT.md` decisions D-01..D-12.

## WR-01: gate strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Wire the `enabled` gate | Expose `scopeResolved` from `useActivityView`; defer fetch until (saved scope exists) OR (activeWorkspaceId resolved). Honors the documented Pitfall 6 mitigation; eliminates the first-open wrong-scope flash + wasted fetch. | ✓ |
| Delete the parameter | Accept the one wasted loopback fetch as fine for localhost; remove the dead `enabled` param + misleading comment (resolves IN-01 the other way). | |

**User's choice:** recommended (wire the gate).
**Notes:** The parameter was a deliberately-designed mitigation documented in 4 Phase 11 artifacts; the implementation simply omitted it. Wiring is a one-line caller change that fixes a real user-visible defect, not just a code smell.

## WR-02: stale-scope handling

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-demote to `global` as the value | Resolve stale id against live rows in `useActivityView`; effective scope becomes `global` (label + query + dropdown agree). Mirror `useActiveWorkspace`; don't persist the demotion. | ✓ |
| Honest "Deleted workspace" label | Keep the stale id + query; show a distinct label so the mislabel is at least visible. | |

**User's choice:** recommended (auto-demote to global as the value, centralized in `useActivityView`, don't persist).
**Notes:** The "silently changes a saved preference" objection is a non-issue — the saved preference is already dangling. Falling back loses nothing valid; overwriting localStorage would be the actually-destructive silent change, so the demotion is runtime-only (re-derived each load).

## WR-03: a11y fix shape

| Option | Description | Selected |
|--------|-------------|----------|
| Include title in aria-label | `aria-label={`Open PR #${number} ${title} on GitHub`}`. Minimal surgical fix; preserves the "on GitHub" cue in the accessible name. | ✓ |
| Drop aria-label + visually-hidden cue | Let visible text (`#42 Title <time>`) be the accessible name; add a visually-hidden "opens in new tab" span. More idiomatic a11y in the abstract, larger diff. | |

**User's choice:** recommended (title in aria-label).
**Notes:** Smaller diff, lower regression risk for a tech-debt phase. Timestamp excluded from the accessible name (secondary info; adds verbosity without aiding identification).

## Verification approach

| Option | Description | Selected |
|--------|-------------|----------|
| UAT + typecheck/build (codebase norm) | `tsc -b && vite build` gate + behavioral UAT for all three fixes. No new RTL/JSDOM harness. | ✓ |
| Add RTL unit tests | Stand up component tests for stale-scope resolution, the gate, and the aria-label. | |

**User's choice:** recommended (UAT + typecheck/build).
**Notes:** The `web/` dir has almost no component unit tests; a harness for three small surgical edits isn't warranted. Optional vitest unit test for WR-02 IF a pure `resolveEffectiveScope` helper is extracted (encouraged, not a gate).

## the agent's Discretion

All four areas — the user deferred entirely to the recommended option. Discretion items within the locked decisions (how `scopeResolved` is computed, where WR-02's liveness check lives structurally, opportunistic IN-03 dedupe, exact aria-label wording, scope of gate-vs-liveness interaction) are noted in `12.1-CONTEXT.md` for the planner/executor.

## Deferred Ideas

- Phase 12 pre-existing lint debt (`react-hooks/set-state-in-effect`, 27–29 errors) — separate dedicated task (deferred-items.md); explicitly OUT of Phase 12.1 scope despite appearing in the same milestone audit.
- IN-02 (`ReviewsList` href scheme validation) — separate security-hardening item.
- IN-03 (`ScopeSelector` double row queries) — out of scope; may fall out for free from WR-02.
- Tightening WR-01's gate to also wait on liveness — future option.
- Restructuring the reviews link to visible-text-as-name (rejected WR-03 alternative) — not worth the larger diff.
