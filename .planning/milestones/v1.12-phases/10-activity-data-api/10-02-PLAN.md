---
phase: 10-activity-data-api
plan: 02
type: execute
wave: 2
depends_on: ["10-01", "10-03"]
files_modified:
  - internal/api/activity.go
  - internal/api/activity_test.go
  - cmd/kamacu/serve.go
autonomous: true
requirements:
  - TASKS-01
  - REVIEWS-01
  - REVIEWS-04
  - STATS-01
  - STATS-02
  - STATS-03
  - STATS-04
user_setup: []

must_haves:
  truths:
    - "GET /api/activity?scope=global&window=week returns HTTP 200 with a JSON body {tasks, reviews, stats} — the whole response is ALWAYS 200, degradation rides in reviews.state (D-01/D-02)"
    - "tasks is an array of done manual tasks in-window, each carrying id, title, done_at, and project provenance (projectId, projectName), ordered done_at DESC, filtered status='done' AND source='manual' AND done_at IS NOT NULL AND done_at >= cutoff (D-12, TASKS-01)"
    - "?scope=workspace:N narrows tasks+reviews to projects.workspace_id=N; ?scope=project:N narrows to project_id=N; ?scope=global (and absent scope) is unscoped; any other scope shape OR a non-numeric N returns HTTP 400"
    - "?window=month uses a 30-day cutoff; ?window=week (and absent window) uses a 7-day cutoff; cutoff = now.UTC().Add(-window).Format(\"2006-01-02T15:04:05.000Z\") matching the reaper's done_at format; any other window value returns HTTP 400"
    - "reviews mirrors github.Result shape {state, stale, fetchedAt, prs} where prs are ReviewDoneSummary values with provenance (projectName/projectId/repo) annotated by the aggregation loop (REVIEWS-01, D-02/D-06)"
    - "reviews-done is WINDOW-FILTERED, not just scope-filtered: aggregateReviews receives the window cutoff (time.Time) and drops any ReviewDoneSummary whose parsed completedAt is BEFORE the cutoff — satisfying REVIEWS-01 ('MERGED or CLOSED within the selected window') and STATS-01 ('reviews done within the selected window'). The cutoff-vs-completedAt comparison parses BOTH sides via plan 10-03's parseActivityTime (cutoff derives from now−window; completedAt is gh's second-precision closedAt) and compares time.Time values — NEVER a string compare across mismatched precisions (ms cutoff vs s completedAt)"
    - "When settings github_integration != 'on' (GATE 1), reviews.state='disabled', NO gh spawns, and tasks+stats still populate normally (D-02/REVIEWS-04/ACT-04)"
    - "When gh is absent or ALL in-scope repos degrade, reviews.state is the worst degraded state (no_gh/error); when SOME repos succeed and SOME degrade, reviews.state='partial' and prs carries only the successful repos' PRs (D-08, REVIEWS-04)"
    - "stats has taskCount, reviewCount, cycle{n,min,max,median}, dwellInProgress{n,min,max,median}, dwellInReview{n,min,max,median}; every duration is an integer number of seconds (D-11, STATS-01/02/03)"
    - "cycle = done_at - in_progress_at; dwellInProgress = in_review_at - in_progress_at when in_review_at is set, else done_at - in_progress_at; dwellInReview = done_at - in_review_at (Open Q3 semantic, locked here); rows missing any *_at needed for a given stat are excluded from that stat's slice but still counted in taskCount (D-09)"
    - "reviewCount is a plain number and reviews NEVER carries cycle/dwell objects — GitHub's merge signal has no Kamacu in-progress/in-review timestamps (STATS-04)"
    - "Cross-repo aggregation is concurrent and bounded (errgroup with SetLimit, 5-wide) with a per-repo context timeout; a failing/slow repo degrades without blocking the others and never propagates an error that fails the whole endpoint (D-07, REVIEWS-04). aggregateReviews carries the cutoff param and applies the reviews window filter to every repo's PRs before flattening"
    - "?refresh=1 propagates force=true to every in-scope repo's GetMergedClosed, bypassing the 5min TTL (bound by attemptFloor) — Open Q4, locked here"
    - "ActivityRoutes(mux, db, ghSvc) is registered in cmd/kamacu/serve.go alongside PullRequestRoutes, receiving the SAME shared *github.Service instance (D-03 reuse)"
  artifacts:
    - "internal/api/activity.go: activityResponse / aggregateReviewsResult wire types (the pure activityTask/timeStat/statsBlock types + parseActivityTime/parseScope/parseWindow/computeTimeStat/buildDurationSlices/rollupReviewState helpers come from plan 10-03's activity_helpers.go — import, do NOT redeclare); aggregateReviews(ctx, ghSvc, repos, force, cutoff time.Time) aggregateReviewsResult with the per-PR completedAt window filter; the GET /api/activity handler closure (computes cutoffTime + cutoffStr, threads cutoffTime into aggregateReviews, formats cutoffStr for the tasks-done SQL); ActivityRoutes(mux, db, ghSvc) registrar"
    - "internal/api/activity_test.go: TestActivityHandler_Global / _Workspace / _Project scope variants; TestActivityHandler_WindowWeek/Month; TestActivityHandler_ReviewsWindowFilter (a PR whose completedAt is before the week cutoff is DROPPED under window=week and INCLUDED under window=month — asserts cutoff threading into aggregateReviews); TestActivityHandler_Gate1Disabled (no gh spawn, tasks+stats populate); TestActivityHandler_NoGh / _Partial / _AllDegraded rollup; TestActivityHandler_StatsOverSeededDB (cycle/dwell numbers); TestActivityHandler_RefreshPropagation (force fans out to every repo); TestActivityHandler_InvalidScope/Window (400)"
    - "cmd/kamacu/serve.go: api.ActivityRoutes(mux, db, ghSvc) call alongside api.PullRequestRoutes"
  key_links:
    - "ActivityRoutes consumes plan 01's (*github.Service).GetMergedClosed — one call per in-scope linked repo, aggregated by aggregateReviews"
    - "The handler consumes plan 10-03's pure helpers (parseScope/parseWindow from activity_helpers.go for validation; buildDurationSlices/computeTimeStat for stats; rollupReviewState for the degrade rollup; parseActivityTime for the reviews window filter) — import, do NOT redeclare"
    - "The GATE 1 settings.Get(db, settings.KeyGithubIntegration) check short-circuits to reviews.state='disabled' BEFORE any gh spawn (mirrors pullrequests.go:56-64)"
    - "The tasks-done SQL cutoff string (cutoffStr, ms-ISO) and the reviews-window cutoff time (cutoffTime, time.Time) BOTH derive from now.UTC().Add(-window); cutoffStr feeds the done_at >= ? lexical compare, cutoffTime feeds aggregateReviews' completedAt < cutoffTime time-compare (parse both sides via parseActivityTime — never string-compare across the ms/s precision mismatch)"
    - "?refresh=1 → force=true fans out to every repo's GetMergedClosed (Open Q4)"
    - "The scope param drives BOTH the tasks-done SQL WHERE clause AND the in-scope linked-projects query that feeds aggregateReviews — one parsed scope, two consumers"
  prohibitions:
    - statement: "Do NOT interpolate the raw scope param into SQL — parse+validate it (global|workspace:N|project:N) and branch structurally on the parsed shape; pass the numeric N as a parameterized ? placeholder"
      status: resolved
      verification: "The tasks-done query and the linked-projects query use ? placeholders for any numeric scope id; the scope kind selects which WHERE clause is appended, never string concatenation of raw input (region-scoped: the query-building section of the handler)"
    - statement: "Do NOT compute median in SQL — SQLite has no MEDIAN; compute cycle/dwell in Go over the fetched *_at timestamps (D-10)"
      status: resolved
      verification: "computeTimeStat is a pure-Go function operating on []time.Duration; the tasks-done SQL selects raw *_at strings, not aggregated durations"
    - statement: "Do NOT let one slow or failing repo block the aggregate — each repo's GetMergedClosed runs under a per-repo context timeout and a degrade is returned, never an errgroup error that cancels the group (Pitfall 4)"
      status: resolved
      verification: "aggregateReviews uses errgroup.Go closures that return nil on degrade (the degraded state rides in the per-repo result); a per-repo context.WithTimeout bounds each call"
    - statement: "Do NOT include source='github_pr' rows in tasks-done — they would double-count PR-review work already covered by the reviews-done gh list (D-12)"
      status: resolved
      verification: "The tasks-done WHERE clause contains source = 'manual' (grep -v '^//' activity.go | grep -c \"source = 'manual'\" >= 1)"
    - statement: "Do NOT string-compare the reviews-window cutoff against completedAt across mismatched precisions — the cutoff string is ms-ISO (2006-01-02T15:04:05.000Z) while gh's closedAt is second-ISO (2026-07-25T14:30:00Z); a lexical compare would mis-order them. Parse BOTH sides via parseActivityTime and compare time.Time values"
      status: resolved
      verification: "aggregateReviews parses each PR's CompletedAt via parseActivityTime and compares the resulting time.Time against the cutoff time.Time with Before (region-scoped: the window-filter step inside aggregateReviews); the cutoff is threaded in as a time.Time, not a string"
    - statement: "Do NOT redeclare parseScope/parseWindow/computeTimeStat/buildDurationSlices/rollupReviewState/parseActivityTime in activity.go — they live in plan 10-03's activity_helpers.go and are imported (same package); duplicating them drifts the contract"
      status: resolved
      verification: "grep -c 'func parseScope\\|func parseWindow\\|func computeTimeStat\\|func buildDurationSlices\\|func rollupReviewState\\|func parseActivityTime' activity.go == 0"

artifacts_this_phase_produces:
  - "Endpoint: GET /api/activity?scope=global|workspace:N|project:N&window=week|month&refresh=1"
  - "Struct: api.activityResponse (fields: Tasks, Reviews, Stats) with JSON tags tasks/reviews/stats"
  - "Struct: api.aggregateReviewsResult (fields: State, Stale, FetchedAt, PRs) — the reviews sub-object"
  - "(Pure helpers activityTask / timeStat / statsBlock / parseActivityTime / parseScope / parseWindow / computeTimeStat / buildDurationSlices / rollupReviewState are produced by plan 10-03's activity_helpers.go — imported, not redeclared here)"
  - "Func: api.aggregateReviews(ctx, ghSvc, repos, force, cutoff time.Time) aggregateReviewsResult — cutoff drives the reviews-done window filter"
  - "Func: api.ActivityRoutes(mux *http.ServeMux, db *sql.DB, ghSvc *github.Service)"
  - "Route registration: cmd/kamacu/serve.go adds api.ActivityRoutes(mux, db, ghSvc)"
---

<objective>
Build the `GET /api/activity` endpoint: one combined handler that answers "which tasks were done, which reviews were completed (merged/closed), and what are the cycle/dwell stats" for any Global / Workspace:N / Project:N scope and a Week (7d) / Month (30d) window. It parses+validates scope/window, runs the tasks-done SQL (D-12), computes cycle/dwell/median stats in Go (D-10/D-11), aggregates merged/closed reviews concurrently across in-scope linked repos via Plan 01's GetMergedClosed (D-07/D-08), enforces the GATE 1 toggle (D-02), and always returns HTTP 200 with degradation riding in reviews.state (REVIEWS-04). Registered alongside PullRequestRoutes on the shared ghSvc.

Purpose: Deliver the data + API layer Phase 11's Activity page renders — one endpoint, one TanStack query, one loading state.
Output: `internal/api/activity.go` (+ tests) and the `serve.go` route wiring.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-activity-data-api/10-CONTEXT.md
@.planning/phases/10-activity-data-api/10-RESEARCH.md
@.planning/phases/10-activity-data-api/10-01-SUMMARY.md
@.planning/phases/10-activity-data-api/10-03-SUMMARY.md

# The code this plan mirrors / consumes — read before editing:
@internal/api/pullrequests.go
@internal/api/respond.go
@internal/api/routes.go
@internal/api/tasks.go
@internal/reaper/reaper.go
@internal/github/service.go
@internal/github/prlist.go
@cmd/kamacu/serve.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: activity.go handler — wire types, aggregateReviews (with window cutoff), GATE 1, route registration (pure helpers imported from plan 10-03)</name>
  <files>internal/api/activity.go, cmd/kamacu/serve.go</files>
  <read_first>
    - internal/api/activity_helpers.go (plan 10-03 — the activityTask/timeStat/statsBlock types + parseActivityTime/parseScope/parseWindow/computeTimeStat/buildDurationSlices/rollupReviewState helpers; IMPORT these, do NOT redeclare)
    - internal/api/pullrequests.go (GATE 1 settings.Get pattern lines 56-64; the always-200 + State-field degrade template; PullRequestRoutes signature)
    - internal/api/respond.go (writeJSON / writeError signatures)
    - internal/api/tasks.go (taskColumns const line 61; the source = 'manual' board-leak filter at lines 194/233; statusAtCol map lines 486-491)
    - internal/reaper/reaper.go (the done_at cutoff format at line 161: now.UTC().Add(-ttl).Format("2006-01-02T15:04:05.000Z"); the status='done' AND done_at IS NOT NULL AND done_at < ? pattern)
    - internal/github/service.go (GetMergedClosed from Plan 01; Result shape State/Stale/FetchedAt)
    - internal/github/prlist.go (ReviewDoneSummary from Plan 01 — note its CompletedAt is gh's closedAt, SECOND-precision ISO)
    - cmd/kamacu/serve.go (the ghSvc := github.New(...) + api.PullRequestRoutes(mux, db, ghSvc, wtSvc) wiring at lines 268-269)
    - .planning/phases/10-activity-data-api/10-CONTEXT.md decisions D-01 through D-12
    - .planning/phases/10-activity-data-api/10-RESEARCH.md § "Code Examples" (window cutoff, in-Go stats, concurrent bounded aggregation), § "Pattern 1" (degrade-via-State), § Pitfalls 4/5/6/7
  </read_first>
  <behavior>
    - The handler imports parseScope/parseWindow/computeTimeStat/buildDurationSlices/rollupReviewState/parseActivityTime from plan 10-03's activity_helpers.go (same package) — they are NOT redeclared here. It returns 400 on an invalid scope or window; otherwise always 200.
    - The handler computes BOTH a cutoffStr (ms-ISO, for the tasks-done SQL lexical compare) AND a cutoffTime (time.Time, for the reviews window compare) from the SAME instant: now := time.Now().UTC(); cutoffTime := now.Add(-window); cutoffStr := cutoffTime.Format("2006-01-02T15:04:05.000Z").
    - GATE 1 (github_integration != "on") sets reviews.state="disabled" with empty prs and spawns ZERO gh processes; tasks + stats still populate from SQL.
    - aggregateReviews(ctx, ghSvc, repos, force, cutoff) returns reviews.state="ok" when all in-scope repos succeed, "partial" when some succeed + some degrade, and the worst degraded state (no_gh/auth_required/error) when all degrade; when there are zero in-scope linked repos, state="ok" with empty prs.
    - aggregateReviews applies the WINDOW FILTER to every repo's PRs: for each ReviewDoneSummary, parse its CompletedAt via parseActivityTime (handles gh's second-precision closedAt) and DROP it if the parsed time is Before(cutoff). A PR merged/closed outside the window is excluded; this satisfies REVIEWS-01 ("within the selected window") and STATS-01. The comparison is time.Time-vs-time.Time — NEVER a string compare across the ms/s precision mismatch.
    - ?refresh=1 sets force=true which fans out to every repo's GetMergedClosed.
  </behavior>
  <action>
    Create internal/api/activity.go. Do NOT redeclare the pure helpers or the activityTask/timeStat/statsBlock types — they live in plan 10-03's activity_helpers.go (same package `api`); reference them directly. Define ONLY the handler-specific wire types here: an aggregateReviewsResult mirroring github.Result's shape (State string `json:"state"`, Stale bool `json:"stale"`, FetchedAt *time.Time `json:"fetchedAt"`, PRs []github.ReviewDoneSummary `json:"prs"`); and activityResponse (Tasks []activityTask `json:"tasks"`, Reviews aggregateReviewsResult `json:"reviews"`, Stats statsBlock `json:"stats"`).

    Add a small repoRow struct (unexported) carrying github_repo, repo_path, and project provenance (project_id, project_name) — the row shape aggregateReviews consumes.

    Add aggregateReviews(ctx context.Context, ghSvc *github.Service, repos []repoRow, force bool, cutoff time.Time) aggregateReviewsResult. Use golang.org/x/sync/errgroup with SetLimit(5) (D-07); promote the indirect errgroup dep to direct by importing it and running go mod tidy (the WaitGroup+semaphore alternative is acceptable if the agent prefers zero new direct deps, but errgroup is recommended). Each g.Go closure: create a per-repo context.WithTimeout(ctx, 12*time.Second) (Pitfall 4 — bound each repo independently), defer cancel, call ghSvc.GetMergedClosed(rc, repo.github_repo, repo.repo_path, force); then for EACH returned ReviewDoneSummary: annotate ProjectName/ProjectID/Repo from the repoRow (D-06 provenance), parse its CompletedAt via parseActivityTime (plan 10-03 — handles gh's second-precision closedAt AND the ms layout), and DROP it when the parsed time is Before(cutoff) — this is the reviews WINDOW FILTER (REVIEWS-01 / STATS-01). Store the per-repo (window-filtered + annotated PRs, state, fetchedAt, stale) into a pre-sized results slice indexed by i (no map — preserve determinism). The closure returns nil on ANY outcome including degrade — a degrade is NEVER an errgroup error (Pitfall 4). When parseActivityTime returns false (unparseable completedAt), drop that PR defensively rather than panic. After g.Wait(), flatten the window-filtered annotated PRs (sort each repo's slice is already done by listCompletedReviews; concatenate in repo order), roll up the aggregate state via rollupReviewState (imported from plan 10-03), set FetchedAt to the latest non-nil per-repo fetchedAt, set Stale if any per-repo Stale is true. When repos is empty, return state="ok" with empty PRs.

    Add ActivityRoutes(mux *http.ServeMux, db *sql.DB, ghSvc *github.Service). Register `GET /api/activity`. The handler: (1) parse+validate scope + window via parseScope/parseWindow (imported from plan 10-03; scope default "global", window default "week"); on error writeError 400 and return. (2) Compute now := time.Now().UTC(); cutoffTime := now.Add(-window); cutoffStr := cutoffTime.Format("2006-01-02T15:04:05.000Z") — the SAME instant, two representations (cutoffStr for the lexical SQL compare, cutoffTime for the reviews time-compare). (3) Build + run the tasks-done query: SELECT t.id, t.title, t.done_at, t.in_progress_at, t.in_review_at, p.id, p.name FROM tasks t JOIN projects p ON p.id = t.project_id WHERE t.status = 'done' AND t.source = 'manual' AND t.done_at IS NOT NULL AND t.done_at >= ? + the scope clause (none for global; AND p.workspace_id = ? for workspace; AND t.project_id = ? for project), ORDER BY t.done_at DESC — pass cutoffStr first, then the scope id (if any) as parameterized ? placeholders. Scan into []activityTask (project_id → ProjectID, p.name → ProjectName; fill the InProgressAt/InReviewAt/DoneAt string fields from plan 10-03's activityTask so buildDurationSlices can parse them). (4) Compute stats: taskCount = len(tasks); cycle/dip/dir via buildDurationSlices(tasks) then computeTimeStat each (both imported from plan 10-03). (5) GATE 1: settings.Get(db, settings.KeyGithubIntegration); on error reviews.State="error"; if val != "on" reviews.State="disabled" and SKIP the gh aggregation entirely (no spawns). (6) Else (gate open): query the in-scope LINKED projects (SELECT id, name, github_repo, repo_path FROM projects WHERE github_repo IS NOT NULL AND TRIM(github_repo) != '' + the scope clause: none / AND workspace_id = ? / AND id = ?), then reviews = aggregateReviews(r.Context(), ghSvc, linkedRepos, force, cutoffTime) where force = (r.URL.Query().Get("refresh") == "1") AND cutoffTime is threaded in. (7) reviewCount = len(reviews.PRs) (already window-filtered). (8) writeJSON 200 with activityResponse{Tasks, Reviews, Stats}.

    Wire the route in cmd/kamacu/serve.go: add `api.ActivityRoutes(mux, db, ghSvc)` on the line immediately after the existing `api.PullRequestRoutes(mux, db, ghSvc, wtSvc)` call (it reuses the SAME ghSvc — no new github.New construction). It takes only (mux, db, ghSvc) — no wtSvc, activity is read-only.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/ ./cmd/kamacu/ && go test ./internal/api/ -run 'TestActivityHandler_ReviewsWindowFilter' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - activity.go declares `func ActivityRoutes(mux *http.ServeMux, db *sql.DB, ghSvc *github.Service)` and registers `GET /api/activity`.
    - activity.go declares aggregateReviews with signature parameter list `(ctx context.Context, ghSvc *github.Service, repos []repoRow, force bool, cutoff time.Time)` returning aggregateReviewsResult — the cutoff param threads the window into the reviews path.
    - activity.go does NOT redeclare parseScope/parseWindow/computeTimeStat/buildDurationSlices/rollupReviewState/parseActivityTime (grep -c 'func parseScope\|func parseWindow\|func computeTimeStat\|func buildDurationSlices\|func rollupReviewState\|func parseActivityTime' activity.go == 0) — they are imported from plan 10-03's activity_helpers.go.
    - The tasks-done query contains the literal filter `source = 'manual'` and `done_at >= ?` and `ORDER BY t.done_at DESC` (grep -v '^//' activity.go | grep -c "source = 'manual'" >= 1).
    - The scope id is passed as a parameterized ? placeholder, never string-concatenated — the WHERE clause is selected by branching on the parsed kind, not by formatting raw input (Pitfall 5).
    - The window cutoff uses the format string "2006-01-02T15:04:05.000Z" for cutoffStr (the reaper's done_at format) — grep confirms the layout literal — AND a parallel cutoffTime time.Time is threaded into aggregateReviews.
    - GATE 1: the handler calls settings.Get(db, settings.KeyGithubIntegration) and, when the value is not "on", sets reviews state to "disabled" WITHOUT calling aggregateReviews (grep confirms the disabled branch precedes the aggregateReviews call).
    - aggregateReviews uses errgroup (or an equivalent bounded-concurrency primitive) with a per-repo context.WithTimeout and closures that return nil on degrade — a degrade never cancels the group — AND applies the completedAt < cutoff window filter to each repo's PRs via parseActivityTime before flattening.
    - stats durations are int64 seconds (timeStat fields are int64); reviewCount is a plain int; no cycle/dwell object exists under reviews (STATS-04).
    - cmd/kamacu/serve.go contains `api.ActivityRoutes(mux, db, ghSvc)` on the line after `api.PullRequestRoutes(mux, db, ghSvc, wtSvc)` (grep confirms).
    - go build ./... exits 0; go vet ./internal/api/ ./cmd/kamacu/ exits 0.
  </acceptance_criteria>
  <done>The GET /api/activity handler imports the plan 10-03 pure helpers, runs the source='manual' tasks-done SQL with the lexical done_at cutoffStr, computes cycle/dwell/median in Go, aggregates merged/closed reviews concurrently across in-scope linked repos via GetMergedClosed with per-repo degrade AND a completedAt window filter (cutoffTime threaded into aggregateReviews), enforces GATE 1, and always returns 200; the route is wired in serve.go.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: activity handler integration tests — scope/window variants, GATE 1, degrade rollup, reviews window filter, stats over seeded DB, refresh propagation</name>
  <files>internal/api/activity_test.go</files>
  <read_first>
    - internal/api/activity.go (the handler + aggregateReviews from Task 1 — the contract under test)
    - internal/api/activity_helpers.go (plan 10-03 — the wire types + parseActivityTime used to build ReviewDoneSummary fixtures with second-precision completedAt for the window-filter test)
    - internal/api/pullrequests_test.go (the newPRTestDB / newPREnv / prFake patterns — how to build an isolated migrated DB, wire a fake-runner Service, and drive the mux via httptest; reuse newPRTestDB or add a sibling helper)
    - internal/github/service_test.go (fakeCompletedRunner shape from Plan 01 — how to inject a canned GetMergedClosed result)
    - internal/store (store.Open + store.Migrate for the test DB; the tasks/projects schema including the *_at columns from migration 00006)
    - .planning/phases/10-activity-data-api/10-CONTEXT.md decisions D-02, D-08, D-09, D-12
    - .planning/phases/10-activity-data-api/10-RESEARCH.md § "Code Examples / Concurrent bounded aggregation" and § Pitfall 7
  </read_first>
  <behavior>
    - GET /api/activity?scope=global&window=week over a DB with two done manual tasks (one in-window, one out-of-window) returns 200 with tasks containing ONLY the in-window task, ordered done_at DESC.
    - ?scope=workspace:N returns only tasks whose project is in workspace N; ?scope=project:N returns only that project's tasks.
    - ?window=month includes a task done 20 days ago that ?window=week excludes.
    - When github_integration is off (GATE 1), reviews.state="disabled" and the fake gh runner records ZERO calls; tasks + stats still populate.
    - When the fake GetMergedClosed returns ok for repo A and no_gh for repo B (two in-scope linked projects), reviews.state="partial" and prs carries only repo A's PRs (annotated with repo A's projectName/projectId/repo).
    - When ALL in-scope repos degrade, reviews.state is the degraded state (no_gh/error) and prs is empty; tasks + stats still populate.
    - reviewCount equals len(reviews.PRs); stats carries no time-stat object under reviews.
    - ?refresh=1 causes the fake GetMergedClosed to be invoked with force=true on every in-scope repo.
    - REVIEWS WINDOW FILTER: a ReviewDoneSummary whose completedAt (gh closedAt, second-precision ISO) falls BEFORE the window cutoff is DROPPED from reviews.prs; the same PR is INCLUDED when the window is widened. Concretely, a PR completed 10 days ago is EXCLUDED under window=week (7d cutoff) but INCLUDED under window=month (30d cutoff) — this is the REVIEWS-01/STATS-01 "within the selected window" gate that was missing before revision.
    - An invalid scope (?scope=bogus) or window (?window=decade) returns HTTP 400.
    - cycle/dwell numbers over a seeded DB (known *_at timestamps) match hand-computed values including the even-N median.
  </behavior>
  <action>
    Add an activityTestDB helper to activity_test.go (or reuse newPRTestDB from pullrequests_test.go if it is package-visible — it is, same package) that opens + migrates an isolated temp DB. Add a seedActivityDB helper that inserts projects (with workspace_id, github_repo, repo_path as needed) and tasks (with status='done', source='manual', and the *_at columns set to known millisecond ISO-8601 strings via UPDATE, since the move endpoint is not exercised here). Include at least: one in-window done manual task with full In Progress→In Review→Done timestamps; one in-window done manual task that skipped In Review (in_review_at NULL); one out-of-window done manual task; one done source='github_pr' row (to assert it is EXCLUDED from tasks-done — D-12); and for the reviews tests, two linked projects with distinct github_repo values.

    Add a fakeCompletedSvc helper that builds a *github.Service via github.New(github.Config{CompletedRunner: ...}) returning canned ([]ReviewDoneSummary, state) per repo (mirror the Plan 01 fakeCompletedRunner pattern, keyed by repo name so repo A and repo B can return different states). The Config.Runner (review-column) can be a no-op since the activity handler never calls Get.

    Add an newActivityEnv helper that wires a fresh mux with ActivityRoutes(mux, db, ghSvc) (mirroring newPREnv). Drive requests via httptest.NewRequest + httptest.NewRecorder and decode the body with encoding/json into an activityResponse-shaped map or struct.

    Add the integration tests: TestActivityHandler_GlobalScopeAndWindow (in-window included, out-of-window excluded, source='github_pr' excluded, done_at DESC order); TestActivityHandler_WorkspaceScope and TestActivityHandler_ProjectScope (narrowing correctness); TestActivityHandler_WindowMonth (a 20-day-old task included under month, excluded under week); TestActivityHandler_ReviewsWindowFilter (the reviews-side window gate: configure the fake CompletedRunner for a single in-scope repo to return TWO ReviewDoneSummary values — one whose CompletedAt is 10 days ago (second-precision ISO, e.g. a closedAt-shaped string) and one whose CompletedAt is 1 day ago; under window=week the 10-day PR is DROPPED and the 1-day PR appears in reviews.prs (assert len and the surviving number); under window=month BOTH appear. This proves cutoff is threaded into aggregateReviews and the completedAt < cutoff comparison parses the second-precision closedAt correctly via plan 10-03's parseActivityTime — no string-compare across the ms/s mismatch); TestActivityHandler_Gate1Disabled (set github_integration off via settings, assert reviews.state="disabled", assert the fake runner recorded 0 calls, assert tasks+stats populate); TestActivityHandler_NoGh (all repos degrade no_gh → reviews.state degrades, prs empty, tasks+stats ok); TestActivityHandler_Partial (repo A ok + repo B no_gh → reviews.state="partial", prs carries only A's PRs annotated with A's provenance); TestActivityHandler_StatsOverSeededDB (hand-compute the expected cycle/dwellInReview/dwellInProgress for the seeded timestamps incl. the even-N median path, assert stats.cycle etc. match — covers Pitfall 6 + the Open Q3 dwellInProgress branch); TestActivityHandler_RefreshPropagation (assert the fake runner observed force=true on every repo when ?refresh=1); TestActivityHandler_InvalidScope and TestActivityHandler_InvalidWindow (400). For the GATE 1 test, set the setting via the same settings store the handler reads (settings.Set or a direct DB INSERT into the settings table — match how pullrequests_test.go toggles it).

    Keep the tests hermetic: no real gh spawns (always inject the fake CompletedRunner); no real git operations; each test its own temp DB.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestActivityHandler' -count=1 && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - activity_test.go contains TestActivityHandler_GlobalScopeAndWindow asserting: in-window manual task present, out-of-window task absent, source='github_pr' row absent, order is done_at DESC.
    - activity_test.go contains TestActivityHandler_Gate1Disabled asserting reviews.state == "disabled" AND the injected fake CompletedRunner recorded 0 calls AND the tasks/stats arrays are non-empty.
    - activity_test.go contains TestActivityHandler_Partial asserting reviews.state == "partial" and reviews.prs carries only the successful repo's PRs with provenance (projectName/projectId/repo) set.
    - activity_test.go contains TestActivityHandler_StatsOverSeededDB asserting hand-computed cycle/dwellInProgress/dwellInReview second values including the even-N median (Pitfall 6) and the dwellInProgress branch (in_review_at set vs NULL — Open Q3).
    - activity_test.go contains TestActivityHandler_RefreshPropagation asserting force=true reached every in-scope repo's GetMergedClosed when ?refresh=1 is present.
    - activity_test.go contains TestActivityHandler_ReviewsWindowFilter asserting: under window=week a PR whose completedAt is 10 days ago is DROPPED (and a 1-day PR survives), and under window=month BOTH appear — proving cutoff is threaded into aggregateReviews and the second-precision closedAt is parsed (not string-compared) against the cutoff (REVIEWS-01 / STATS-01).
    - activity_test.go contains TestActivityHandler_InvalidScope and TestActivityHandler_InvalidWindow each asserting HTTP 400.
    - No test spawns a real gh process (all use the injected fake CompletedRunner).
    - go test ./internal/api/ -count=1 passes (all activity tests green); go vet ./internal/api/ exits 0.
  </acceptance_criteria>
  <done>The GET /api/activity endpoint is verified across scope variants (global/workspace/project), window variants (week/month), the GATE 1 disabled short-circuit, the ok/partial/all-degraded rollup, the REVIEWS WINDOW FILTER (a PR closed outside the window dropped under week, included under month — cutoff threaded into aggregateReviews), hand-computed stats (cycle/dwell incl. even-N median and the dwellInProgress in_review_at branch), ?refresh=1 force propagation, and 400 on invalid params — all hermetic with zero real gh spawns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/activity | The single entry point. Two query params (scope, window) cross here; both are parsed+validated server-side before any SQL or gh spawn. |
| handler → SQLite | Parameterized ? SQL only; the scope kind selects the WHERE clause structurally, the numeric N is a ? placeholder. |
| handler → gh (via GetMergedClosed) | One gh spawn per in-scope linked repo, bounded by errgroup + per-repo timeout; repo/repo_path come from the DB, never the request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | scope param → tasks-done / linked-projects SQL | high | mitigate | parseScope validates the shape (global \| workspace:N \| project:N); the handler branches structurally on the parsed kind to pick the WHERE clause and passes N as a parameterized ? — raw scope is never string-concatenated into SQL (Pitfall 5, ASVS V5). Invalid scope → 400. |
| T-10-06 | Tampering | window param | medium | mitigate | parseWindow accepts only the literals week/month (and empty=week); anything else → 400. The value drives a fixed time.Duration, never raw SQL. |
| T-10-07 | Denial of Service | aggregateReviews fan-out | medium | mitigate | errgroup with SetLimit(5) bounds concurrency; each repo runs under a per-repo context.WithTimeout (~12s); a slow/failing repo degrades without blocking the others or canceling the group (Pitfall 4). |
| T-10-08 | Information Disclosure | gh error surfacing | low | accept | GetMergedClosed (Plan 01) classifies gh failures into no_gh/auth_required/error without leaking stderr; the handler passes the state string through verbatim — no stderr body reaches the wire. |
| T-10-09 | Spoofing | github_integration GATE 1 | low | mitigate | The settings.Get(db, KeyGithubIntegration) check short-circuits to reviews.state="disabled" before any gh spawn — consistent with the established two-gate ladder (GHSET-02, pullrequests.go:56-64). |
| T-10-SC2 | Tampering | package install (errgroup) | low | accept | errgroup is `golang.org/x/sync/errgroup`, already present as an indirect dep in go.mod (verified v0.20.0, Plan 01 RESEARCH § Package Legitimacy Audit). Promoting indirect→direct via go mod tidy requires no registry verification; it is the Go team's official supplementary sync library. No new package is fetched from a registry. |
</threat_model>

<verification>
- `go test ./internal/api/ -count=1` passes (all activity tests + the rest of the api suite).
- `go test ./internal/github/ -count=1` still passes (Plan 01 unchanged).
- `go build ./...` and `go vet ./internal/api/ ./cmd/kamacu/` are clean.
- Manual smoke (operator, post-execute): with gh installed and a linked project that has reviewed-then-closed PRs, `curl -s 'http://localhost:<port>/api/activity?scope=global&window=month' | jq` returns 200 with non-empty tasks/reviews/stats and reviews.state in {ok,partial,no_gh,auth_required,error,disabled}.
- grep confirms the route is registered exactly once in serve.go and the source='manual' filter is present in activity.go.
</verification>

<success_criteria>
- GET /api/activity returns the combined {tasks, reviews, stats} for any global/workspace/project scope and week/month window, always HTTP 200 (D-01, TASKS-01, STATS-01).
- reviews-done lists merged/closed reviewed-by:@me PRs with provenance via the Plan 01 GetMergedClosed aggregation AND is WINDOW-FILTERED — only PRs whose completedAt falls within the selected week/month window appear (cutoff threaded into aggregateReviews; REVIEWS-01 "within the selected window", STATS-01).
- Degradation is graceful: GATE 1 off → reviews.state="disabled" no spawns; gh absent / repo fail → ok/partial/degraded rollup; tasks+stats always populate (REVIEWS-04, D-02).
- stats carries taskCount/reviewCount + cycle/dwellInProgress/dwellInReview {n,min,max,median} in seconds, computed in Go over the *_at timestamps; reviews contribute a count only (STATS-02/03/04, D-10/D-11).
- The dwellInProgress semantic is locked AND USER-CONFIRMED during revision: in_review_at - in_progress_at when in_review_at set, else done_at - in_progress_at (Open Q3).
- ?refresh=1 fans force=true out to every repo (Open Q4); invalid scope/window → 400 (Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/10-activity-data-api/10-02-SUMMARY.md` when done
</output>
