---
milestone: v1.8
name: Kamacu Rebrand & UX Polish
audited: 2026-07-04
status: tech_debt
scores:
  requirements: 27/27
  phases: 5/5
  integration: 5/5 seams wired
  flows: 3/3
gaps:
  requirements: []      # 27/27 satisfied — none unsatisfied, none orphaned
  integration: []       # all 5 seams wired, no cross-phase breaks
  flows: []             # all 3 E2E flows complete
tech_debt:
  - phase: milestone-wide
    items:
      - "Pre-existing frontend ESLint react-hooks/set-state-in-effect baseline: ~21 errors across ~16 files (hooks/use-mobile.ts, pages/TaskPage.tsx, board/Board.tsx, board/PRCard.tsx, board/ReviewColumn.tsx, terminal/TerminalPane.tsx, ui/*, etc.). Inherited from v1.3+, NOT introduced by v1.8; gating build (tsc -b && vite build) is green. Home: a dedicated react-hooks lint-cleanup pass."
      - "Security gate not run for any v1.8 phase (no SECURITY.md in phases 20-24). Phase 24 added a new PATCH /api/sessions/{id} endpoint; code review found no injection/auth risk but the formal /gsd:secure-phase gate was not executed."
  - phase: 20-kamacu-rebrand-brand
    items:
      - "Pre-existing gofmt drift in 8 Go files (gofmt-dirty at the phase-20 base commit, NOT caused by the kangent→kamacu rename): cmd/kamacu/main_test.go, internal/api/icons.go, internal/github/{prlist,service}.go, internal/reaper/reaper_test.go, internal/settings/settings_test.go, internal/ws/{handler_test,integration_test}.go. Home: a `gofmt -w ./...` pass."
      - "Code review: 1 warning + 3 info (advisory, non-blocking)."
  - phase: 21-data-directory-migration
    items:
      - "migrateStorage.ts (MIGRATE-04, LOW): localStorage prefix migration mutates the store (setItem) while iterating by numeric index (localStorage.length/key(i)); index shifts during iteration could theoretically skip a key on some browsers. Cosmetic (one UI preference not carried over, self-heals on next set), single-run guarded. Fix: snapshot keys before the loop or iterate in reverse. Flagged by the integration checker."
      - "Code review CR-01 (critical) RESOLVED by plan 21-08 (worktree repair for folder-pointed repos + prune-survival regression test); WR-03/IN-01/IN-02 deferred to Phase 23."
  - phase: 22-github-style-diff-review
    items:
      - "Code review CR-01 (critical, dash-prefixed untracked filename aborting git diff --no-index) RESOLVED (2c14263); WR-01/WR-02 fixed (7ca2b89 / 72579f1). Remaining info items deferred."
  - phase: 23-worktree-cleanup-panel
    items:
      - "Code review status partially_resolved: CR-01 (unvalidated force-remove path) + WR-01 + WR-02 fixed TDD-first (9134db5/1729cbe, a6de21d/63ec46c). Deferred: WR-03 (unpushed count can under-count when the base ref is ambiguous) and remaining warnings/info — run /gsd:code-review 23 --fix."
  - phase: 24-session-bar-board-tab-polish
    items:
      - "Code review: 0 critical, 4 warnings (empty-body rename 400 makes the no-op path unreachable; agent ⋯ Stop lacks an in-flight guard + onError; rename input stays editable after a session exits then silently drops the commit; empty-reset is a silent no-op on plain-bash vs Bash-N reset on tmux) + 6 info. Advisory — run /gsd:code-review 24 --fix."
      - "GAP-01 (tmux custom label lost on restart via reattach clobber) was found during human-verify and FIXED (1b12b52) with regression test TestSessionTmuxReattachPreservesCustomLabel; re-verified. Not open."
---

# Milestone v1.8 — Kamacu Rebrand & UX Polish — Audit

**Audited:** 2026-07-04
**Status:** `tech_debt` — all requirements met, all phases verified, cross-phase integration and E2E flows complete; accumulated (mostly inherited) tech debt is worth a review but does not block completion.

## Verdict

- **Requirements:** 27/27 satisfied (3-source cross-referenced: REQUIREMENTS.md traceability `[x]`/Complete + phase VERIFICATION.md `passed` + SUMMARY `requirements_completed` frontmatter). No unsatisfied, no orphaned.
- **Phases:** 5/5 verified `passed` (20: 5/5, 21: 5/5, 22: 5/5, 23: 9/9, 24: 5/5 must-haves).
- **Integration:** All 5 cross-phase seams WIRED, no orphaned exports, no missing connections.
- **E2E flows:** 3/3 complete.
- **No critical blockers:** every code-review critical was resolved in-phase.

## Requirements Coverage (27/27)

| Category | REQ-IDs | Phase | Status |
|----------|---------|-------|--------|
| Rebrand | REBRAND-01/02/03 | 20 | satisfied |
| Brand | BRAND-01/02/03 | 20 | satisfied |
| Migration | MIGRATE-01/02/03/04/05 | 21 | satisfied |
| Diff review | DIFF-01/02/03/04 | 22 | satisfied |
| Worktree cleanup | WTREE-01/02/03/04 | 23 | satisfied |
| Tab rename | TABS-01/02 | 24 | satisfied |
| Review menu | REVMENU-01/02 | 24 | satisfied |
| Board/bar polish | POLISH-01/02/03 | 24 | satisfied |

## Cross-Phase Integration (integration_ok: true)

| Seam | REQ-IDs | Result |
|------|---------|--------|
| Rebrand → Migration bridge (kamacu identity vs runtime paths) | REBRAND-*, MIGRATE-01/02/03 | WIRED — every runtime path resolves to `~/.kamacu` / `-L kamacu`; the only `kangent` literals are the deliberate migration source in `internal/migrate/*`. Boot ordering `Prepare → store.Migrate → Complete` is load-bearing and correct. |
| tmux prefix consistency | MIGRATE-03, TABS-01/02 | WIRED — spawn mints `kamacu-<task>-<n>`; `defaultTmuxLabel` is prefix-agnostic; migration retires legacy `kangent-*` rows. |
| Route wiring | TABS, WTREE, DIFF | WIRED — `PATCH /api/sessions/{id}`, `GET/POST /api/worktrees*`, `PUT /api/tasks/{id}/diff/viewed` all registered and consumed by the frontend; no path collisions. |
| Migrations + boot | all | WIRED — 00001–00011 apply in order; app boots to v11. |
| E2E flows | all | COMPLETE — (a) task→worktree→tab→rename→survives restart; (b) PR review→diff→per-file Viewed persistence; (c) mark Done→cleanup offer / Settings force-remove. |

**Build/test gates:** `go build`/`go vet`/`go test ./...` and `tsc -b && vite build` all pass. One transient `internal/api` failure under full-suite parallel load matched the documented flaky `TestSessionTmuxReattach` (passes in isolation) — a test-infra flake, not an integration regression.

## Tech Debt (see frontmatter for the itemized list)

1. **Pre-existing react-hooks lint baseline** (~21 errors / ~16 files) — inherited from v1.3+, build stays green. Dedicated lint-cleanup pass.
2. **Pre-existing gofmt drift** (8 Go files) — not caused by the rename. `gofmt -w ./...` pass.
3. **Deferred code-review warnings** — Phase 23 (WR-03 + remaining) via `/gsd:code-review 23 --fix`; Phase 24 (4 advisory warnings) via `/gsd:code-review 24 --fix`; Phase 22 remaining info.
4. **migrateStorage.ts index-iteration robustness** (MIGRATE-04, low) — snapshot keys before mutating.
5. **Security gate not run** for any v1.8 phase; Phase 24 added `PATCH /api/sessions/{id}` (no injection/auth risk found in review, but `/gsd:secure-phase` not executed).

None of these block completion; all code-review criticals were resolved in-phase and GAP-01 was fixed and re-verified.
