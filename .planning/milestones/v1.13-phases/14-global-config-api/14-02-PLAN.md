---
phase: 14-global-config-api
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - internal/api/global_test.go
autonomous: true
requirements: [GCONF-02, GCONF-04]
must_haves:
  truths:
    - "A PUT supplying any root field while a REAL live global tmux session exists returns 409 with a non-empty structured reasons list and leaves the singleton row and disk untouched — proven against a real detached tmux session, not just zero-session unit tests (GCONF-04, D-13/D-15)"
    - "An agent-only PUT succeeds (200) under the same live session — the gate scopes to root changes only (GCONF-03/D-24)"
    - "Config-history sequences (managed -> folder -> clear -> managed) never leave a stale github_repo marker and always bump updated_at (Pitfall 8)"
    - "A same-repo managed re-PUT reattaches without invoking the clone runner, and STILL clears the persisted resume ids — D-16's no-no-op-re-PUT exception ban proven on the reattach path (D-04/D-16)"
    - "The real production binary (cmd/kamacu serve) serves GET/PUT /api/global end-to-end: unconfigured GET shape, folder configure, agent set, clear, and the 400 grammar — the phase goal's curl-verifiable contract"
  artifacts:
    - internal/api/global_test.go (extended: the live-tmux gate test, config-history matrix, no-reclone reattach assertions)
  key_links:
    - "TestPutGlobalRootBlockedByLiveTmux -> real tmux server on a per-test socket + seeded tmux_sessions scope='global' row + newGlobalTestServerWithTmux harness"
    - "Curl smoke -> cmd/kamacu serve --addr/--db -> api.GlobalRoutes registration from plan 14-01"
---

<objective>
Close the verification halves of GCONF-02/GCONF-04 and the phase's curl-verifiable goal: trip the 409 gate against a REAL live global tmux session (the one test Pitfall 7 demands), prove config-history never leaks a stale github_repo marker nor skips the D-16 id clearing (including on the reattach re-PUT), and run the real binary through a curl round-trip covering the full PUT grammar end-to-end.

Purpose: Plan 14-01 shipped the production surface with unit/seam tests; the gate has only been exercised against zero sessions, and the lifecycle sequences (managed->folder->clear->managed) and the production serve.go wiring have no dedicated proof. Phase 15 builds on this endpoint immediately — the wire contract and gate semantics must be proven, not presumed.

Output: internal/api/global_test.go extended with TestPutGlobalRootBlockedByLiveTmux + TestPutGlobalConfigHistory; a recorded curl smoke transcript (in the SUMMARY) of the real binary; full-package regression green.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-global-config-api/14-CONTEXT.md
@.planning/phases/14-global-config-api/14-RESEARCH.md
@.planning/phases/14-global-config-api/14-PATTERNS.md
@.planning/phases/14-global-config-api/14-01-SUMMARY.md
@internal/api/global.go
@internal/api/worktrees_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-tmux 409 gate test + config-history staleness matrix + reattach no-reclone/no-no-op assertions</name>
  <files>internal/api/global_test.go</files>
  <read_first>
  - internal/api/global_test.go (current state after plan 14-01 — harness and seam tests already exist)
  - internal/api/global.go (the gate + PUT as shipped — the code under test)
  - internal/api/worktrees_test.go lines 397-440 (the verbatim detached-tmux recipe: LookPath skip-guard, per-test socket Client, KillServer cleanup registered BEFORE the server is built (LIFO), seeded tmux_sessions row, real detached new-session -d, awaitHasSession poll)
  - internal/api/sessions_test.go line 652 (awaitHasSession helper — reuse it, same package)
  - internal/api/projects_test.go lines 49-57 (failCloneNeverCalled — the must-not-be-called clone fake pattern)
  - 14-RESEARCH.md Pitfall 7 (why this test must exist), Pitfall 8 (the staleness matrix), Pattern 4 step 5 (reattach)
  </read_first>
  <action>
  Extend internal/api/global_test.go with two tests:

  1. TestPutGlobalRootBlockedByLiveTmux — the Pitfall-7 gate trip, adapting the worktrees_test.go recipe to the global scope: skip-guard on exec.LookPath("tmux") with t.Skip; build tmux.Client with Socket fmt.Sprintf("ktest-globalgate-%d", os.Getpid()) and ConfPath "/dev/null"; register t.Cleanup KillServer BEFORE constructing the server (LIFO so it runs after everything else); build the harness via newGlobalTestServerWithTmux(t, c) from plan 14-01; configure a folder root first via a normal PUT (gitRepo fixture) so "row untouched" is assertable against a non-empty prior value; INSERT INTO tmux_sessions (scope, n, name, label) VALUES ('global', 1, 'kamacu-global-1', 'Bash 1') — the 00018 columns: task_id stays NULL, scope 'global' satisfies the XOR CHECK; start a REAL detached session with exec.Command("tmux", append(c.BaseArgs(), "new-session", "-d", "-s", "kamacu-global-1", "-c", <t.TempDir()>)...) and awaitHasSession(t, c, "kamacu-global-1", true). Then assert: PUT {"root_path":""} -> 409 with decoded body["error"] non-empty and body["reasons"] a non-empty array whose first element is a map with kind "sessions" and target "kamacu-global-1" (the structured D-15 grammar); PUT with a non-empty folder root_path -> 409 as well (every root-field shape gates); the DB row still holds the prior root_path (query it) and a follow-up GET returns the prior config unchanged. Under the SAME live session: PUT {"agent_id": <existing id>} -> 200 (D-24 — agent changes never gate). Finally KillServer, awaitHasSession false, and PUT {"root_path":""} now succeeds — proving liveness, not row existence, is what blocks (D-13/D-14).

  2. TestPutGlobalConfigHistory — the Pitfall-8 lifecycle matrix over one HOME-sandboxed server, using the github seams (defer restore()): managed first (validate + clone fakes; the clone fake git-inits dest and adds origin matching the canonical ref so later steps can reattach) -> GET shows github_repo set and root_path under repos/global/; then PUT a folder root (gitRepo fixture) -> GET shows github_repo JSON null and the folder root_path — the stale-marker assertion; then PUT {"root_path":""} -> GET shows both empty; then PUT the same repo again -> 200 via REATTACH with a must-NOT-be-called clone fake (failCloneNeverCalled pattern) and GET shows the managed config restored — the no-reclone assertion (D-04). Weave the D-16 assertions into the same sequence: before each root PUT, hand-UPDATE global_task SET claude_session_id='c<N>', opencode_session_id='o<N>' and after each successful root PUT assert both are NULL in the DB — INCLUDING the final same-repo reattach re-PUT (identical values, no no-op exception — D-16's ban proven on the exact path most likely to skip it); after an agent-only interlude PUT, assert the ids survive. Assert updated_at strictly increases across each successful PUT (millisecond-precision strftime makes lexical comparison valid).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestPutGlobalRootBlockedByLiveTmux|TestPutGlobalConfigHistory' -count=1 -v</automated>
  </verify>
  <done>The 409 gate is proven against a real live global tmux session (and unblocks on stop), agent-only PUTs pass under a live session, and the managed->folder->clear->managed history never leaks a stale github_repo, never re-clones on reattach, and always clears resume ids on root changes — no exceptions.</done>
  <acceptance_criteria>
  - internal/api/global_test.go contains "func TestPutGlobalRootBlockedByLiveTmux" with the exec.LookPath skip-guard, a per-test tmux socket, the scope='global' row INSERT, and a real detached kamacu-global-1 session
  - The 409 assertions decode reasons as an array of maps with kind "sessions" and target "kamacu-global-1"; the DB row is verified untouched after each 409
  - The same test asserts agent-only PUT returns 200 under the live session, and that the root clear succeeds after KillServer
  - TestPutGlobalConfigHistory asserts github_repo null after the folder transition, no clone-runner invocation on the reattach PUT, resume ids NULL after every root PUT (reattach included) and still set after an agent-only PUT, and strictly increasing updated_at
  - Both tests exit 0 via the automated command (tmux 3.4 present on this host — verified in 14-RESEARCH Environment Availability; the skip-guard keeps CI-elsewhere honest)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Real-binary curl smoke round-trip + full-package regression</name>
  <files>.planning/phases/14-global-config-api/14-02-SUMMARY.md</files>
  <read_first>
  - cmd/kamacu/serve.go (flags: --addr, --db; the registry line api.GlobalRoutes from plan 14-01; loopback-only addr enforcement)
  - internal/api/global.go (the wire shapes and error copy the curls must match)
  - .planning/phases/14-global-config-api/14-RESEARCH.md "Curl smoke script" section (the SC1-SC4 shape this run follows)
  </read_first>
  <action>
  Verification-only task (no production code changes; record the transcript in the SUMMARY):

  1. Build the real binary: go build -o /tmp/opencode/kamacu-smoke ./cmd/kamacu (honor the repo's existing embed/dist guard if plain go build complains — check how CI/make builds it; if the embed directive requires web/dist, use the same make target the repo ships).

  2. Prepare an isolated environment: temp HOME, temp DB path, and a git init fixture dir for the folder variant; pick a free high port on 127.0.0.1.

  3. Start /tmp/opencode/kamacu-smoke serve --addr 127.0.0.1:<port> --db <tmp>/smoke.db with the temp HOME (so ~/.kamacu resolves into the sandbox), wait for GET /api/healthz to return 200, and keep it running for the sequence.

  4. Run the curl round-trip and capture status + body for each step: GET /api/global -> 200 with root_path "", github_repo null, live zeros, non-null agent summary (the GVIEW-04 honest unconfigured shape); PUT {"root_path":"<fixture>"} -> 200 with root_exists true and github_repo null (SC1); PUT {"agent_id":<id from the GET agent summary>} -> 200 with the agent summary reflecting it (SC3 settable half; the delete-guard half shipped in Phase 13); GET -> config reflects both; PUT {"root_path":""} -> 200 and GET shows the unconfigured shape again (SC4 clear half — the 409 half is Task 1's live-tmux proof, since nothing in Phase 14 can create a live global session on a real server); PUT {} -> 400 nothing to update; PUT {"repo":"o/w","root_path":"/x"} -> 400 both-supplied. The gh-dependent managed path is deliberately NOT curled (unit-covered via seams in 14-01; a live clone would be flaky) — note that in the transcript.

  5. Stop the server (SIGTERM), confirm clean exit.

  6. Full regression: go test ./internal/api/ -count=1 and go vet ./internal/api/ ./cmd/kamacu/ — the whole package green (the four known pre-existing failures logged in Phase 13's deferred-items are the only acceptable exceptions; if any global test fails, FIX before finishing).
  </action>
  <verify>
    <automated>go test ./internal/api/ -count=1 && go vet ./internal/api/ ./cmd/kamacu/ && test -f .planning/phases/14-global-config-api/14-02-SUMMARY.md && grep -q 'curl' .planning/phases/14-global-config-api/14-02-SUMMARY.md</automated>
  </verify>
  <done>The real binary serves the complete /api/global contract over curl (unconfigured GET, folder configure, agent set, clear, 400 grammar), the transcript is recorded in the SUMMARY, and the full internal/api package plus vet are green.</done>
  <acceptance_criteria>
  - The SUMMARY contains the curl transcript with observed status codes matching: 200 (GET unconfigured), 200 (folder PUT), 200 (agent PUT), 200 (clear PUT), 400 (empty body), 400 (repo+root_path both supplied)
  - The unconfigured GET body observed on the REAL binary contains the keys root_path, github_repo, agent_id, updated_at, root_exists, live, agent — and no resume-id keys
  - The managed/clone path is documented as seam-covered (14-01) rather than curled, with the reason
  - go test ./internal/api/ -count=1 exits 0 (or green except the four pre-existing logged failures, explicitly noted)
  - go vet on both packages exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real binary <-> localhost HTTP | The smoke run exercises the production loopback surface end-to-end |
| test <-> real tmux server | The gate test drives a real detached tmux session on a per-test socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | gate false-negative (live session missed) | medium | mitigate | Task 1 trips the gate against a REAL detached session and proves unblock-on-stop; probe contract asserted implicitly via awaitHasSession preconditions |
| T-14-08 | Tampering | stale config markers misleading Phase 15/16 | medium | mitigate | Task 1's config-history matrix proves github_repo never goes stale and D-16 clearing has no exceptions (reattach path included) |
| T-14-09 | Information Disclosure | resume ids in real-binary responses | low | mitigate | Task 2 asserts the observed GET body carries no resume-id keys on the production server |

No package installs occur (Go, zero new dependencies). No new production code ships in this plan — test and verification artifacts only.
</threat_model>

<verification>
- go test ./internal/api/ -run 'TestGlobal|TestGetGlobal|TestPutGlobal' -count=1 -v — every global test, old and new, green
- The curl transcript in the SUMMARY shows the real binary honoring the full wire contract
- git status clean of stray artifacts (temp files under /tmp/opencode only)
</verification>

<success_criteria>
- The 409 gate is proven against real liveness (not just unit fakes) and releases when the session stops
- Config-history invariants hold across every transition; D-16 has no no-op exception anywhere
- The phase goal "independently testable with curl before any frontend exists" is demonstrated against the real binary
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by THIS plan (14-02) — verification artifacts only, no new production symbols:
- internal/api/global_test.go (extended): TestPutGlobalRootBlockedByLiveTmux, TestPutGlobalConfigHistory
- .planning/phases/14-global-config-api/14-02-SUMMARY.md including the real-binary curl smoke transcript

Created by plan 14-01 (the phase's production surface — listed for the convergence pass): internal/api/global.go with GlobalRoutes, globalHandlers, globalConfig/globalLive/globalAgent wire types, get/put handlers, globalLiveBlockers, liveGlobalTmuxNames; internal/api/global_test.go harness (newGlobalTestServer, newGlobalTestServerWithTmux); cmd/kamacu/serve.go registration line; routes GET/PUT /api/global; on-disk namespace ~/.kamacu/repos/global/<owner>/<name>/.

Reused, NOT new (exclude from drift verification): awaitHasSession, failCloneNeverCalled pattern, doJSON/gitRepo, github test seams, tmux.Client, kamacu serve binary/flags, all plan-14-01 production symbols.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-global-config-api/14-02-SUMMARY.md` when done (including the curl smoke transcript)
</output>
