---
phase: 15-global-sessions-backend
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - internal/api/sessions.go
  - internal/api/agents.go
  - internal/api/agent_integration_test.go
  - internal/api/sessions_global_test.go
autonomous: true
requirements: [GSESS-02, GSESS-04, GVIEW-02, GINT-03]
must_haves:
  truths:
    - "POST /api/sessions {scope:\"global\", kind:\"agent\"} spawns the configured default agent with cwd = the global root, label \"Agent\", engine/args read-at-use from the singleton (D-24, GVIEW-02)"
    - "A second CONCURRENT global agent spawn returns 409 \"global agent already running\" (D-34); an exited agent never blocks — fresh spawn replaces"
    - "scope:\"global\" + resume:true with no persisted engine id returns 409 \"no global <engine> session to resume\" — never a silent fresh spawn (D-31); with a valid claude csid the spawn argv carries --resume <csid> (GSESS-02)"
    - "After a fake-claude global agent spawn, global_task.claude_session_id is persisted (warn-only on failure); opencode capture writes global_task.opencode_session_id — never the tasks table (re-target, not a forked poller)"
    - "A live global agent session appears in /api/agents/status with source \"global\", taskId 0, projectId 0, projectName \"Global\", taskTitle \"Scratchpad\", non-empty sessionId (manager pass, D-09/D-10, SC3)"
    - "Post-restart (fresh Manager, seeded global_task ids), /api/agents/status shows exactly ONE global entry with status \"exited\" and resumable true from the engine-branched id check — no duplication when the manager pass already covers it (GSESS-02, Pitfall 1)"
    - "Global sessions never appear in Activity stats or lists (GINT-03) and no reaping pass was added (GSESS-04) — verified functionally, both hold by construction"
    - "Every existing task/dev suite passes byte-for-byte untouched — whole repo green"
  artifacts:
    - internal/api/sessions_global_test.go (agent + status additions)
    - captureOpencodeSessionAsync persist-target parameter
  key_links:
    - "create() global agent branch: singleton resolution -> D-33 gate order (root gates then one-agent) -> Spawn -> csid UPDATE global_task -> (opencode) parametrized capture poll -> UPDATE global_task"
    - "mgr.List() global scan (outside the newest map) + global_task singleton read -> ONE synthesized agentStatusEntry per pass in /api/agents/status"
    - "agentStatusEntry.Source union extension \"global\" — the Phase-16 discriminator (Spike 1 resolution)"
  prohibitions:
    - statement: "No forked capture poller — one function, parametrized persist target"
      status: enforced
      verification: "exactly one captureOpencodeSessionAsync definition in internal/api"
    - statement: "The global resumable derivation never references worktree_path — root_path plays that role (Pitfall 10)"
      status: enforced
      verification: "no worktree_path token in the global branches of agents.go/sessions.go"
    - statement: "No rewrite of the status INNER JOINs — synthesized entries are appended, never LEFT-JOIN surgery (Pattern 2)"
      status: enforced
      verification: "agents.go JOIN query text unchanged; diff is append-only in both passes"
    - statement: "The global agent entry is collected OUTSIDE the newest[TaskID] map (Pitfall 3 — key 0 would leak onto dev scope)"
      status: enforced
      verification: "code review + at-most-one-global-entry test"
    - statement: "No auto-reap analog, no PR reconcile pass for global sessions (GSESS-04)"
      status: enforced
      verification: "reaper.go untouched in the diff; functional activity/reaper exclusion tests"
    - statement: "No frontend file changes — the TS source union widens in Phase 16 only"
      status: enforced
      verification: "git diff shows zero files under web/"
---

<objective>
Global AGENT spawn + the widened status feed (Wave 2 of 2 — depends on 15-01's Info.Global/ListGlobal()).

Complete the `scope:"global"` spawn path for agents: singleton root+agent resolution, the D-33 gate order, the D-34 one-agent 409, engine-branched resume (D-31), csid persist to `global_task`, and the opencode capture re-target (parametrized persist, one host-gated e2e). Then widen `/api/agents/status` — ONE synthesized global entry in BOTH passes (manager-derived live + DB-derived post-restart) with `source:"global"`, zero ids, and the locked D-09/D-10 labels — closing the co-phasing mandate (SC3: visible live AND post-restart).

Purpose: Without the feed widening the global session is invisible and the phase cannot be human-verified (roadmap co-phasing mandate). This is the milestone's risk center — every task path must diff clean.
Output: Agent branch + capture re-target in sessions.go, both status passes widened in agents.go, harness + fake-claude/stub/host-gated tests.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-global-sessions-backend/15-CONTEXT.md
@.planning/phases/15-global-sessions-backend/15-RESEARCH.md
@.planning/phases/15-global-sessions-backend/15-PATTERNS.md
@.planning/phases/15-global-sessions-backend/15-UI-SPEC.md
@.planning/phases/15-global-sessions-backend/15-01-SUMMARY.md

@internal/api/sessions.go
@internal/api/agents.go
@internal/api/agent_integration_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global agent spawn — one-agent gate, engine-branched resume, csid persist, capture re-target</name>
  <files>internal/api/sessions.go, internal/api/agent_integration_test.go, internal/api/sessions_global_test.go</files>
  <read_first>
  - internal/api/sessions.go (create agent path: task one-agent gate :305-315, engine-branched resume validation :316-339, custom-argv resume wiring :371-373, csid persist + capture launch :458-478, captureOpencodeSessionAsync :793-858 with the tasks UPDATE at :811, discoverOpenCodeSession :720-778)
  - internal/api/agent_integration_test.go (newAgentIntegrationServer :47-92, stage pattern :165-170, agentStatusForTask/waitAgentStatus poll helpers :110-139)
  - internal/api/opencode_resume_test.go (discoverOpenCodeSession stub injection + host-gated skip conventions, git fixture :161)
  - internal/api/resume.go (transcriptExists :16 — cwd-agnostic glob)
  - .planning/phases/15-global-sessions-backend/15-PATTERNS.md (§sessions.go csid/capture analogs, Shared Patterns 4/6/8; §test harness)
  </read_first>
  <behavior>
  - {scope:"global", kind:"agent"} unconfigured → 409 "global root not configured" (gate uniform across kinds, D-30)
  - Configured root → 201, session label "Agent", Info.Global true, engine/command/extra-args from the singleton's agent (read-at-use, D-24); fake-claude runs with cwd = the global root
  - Second concurrent spawn while RUNNING → 409 "global agent already running" (D-34); after the first exits → fresh spawn 201 (replaces)
  - resume:true with no claude id in the singleton → 409 "no global claude session to resume"; opencode engine with no ocsid → 409 "no global opencode session to resume" (D-31)
  - Seeded global_task.claude_session_id + fake transcript → resume spawn argv contains --resume <csid>
  - After a global fake-claude agent spawn, global_task.claude_session_id is non-empty (persist warn-only on failure)
  - Stubbed discoverOpenCodeSession → global opencode spawn → global_task.opencode_session_id written; the tasks table gains no opencode_session_id write
  - Host-gated (opencode on PATH): real opencode in a temp-git-repo global root → first turn → global_task.opencode_session_id captured
  </behavior>
  <action>
  First extend the harness: register GlobalRoutes in newAgentIntegrationServer (one line, mirroring serve.go :293 — additive wiring that catches main drift) and add a seedGlobalRoot helper doing PUT /api/global with a temp folder root (reuse 15-01's helper if exported there). In create's global branch (after the 15-01 root gates, D-33 order): one-agent gate — iterate mgr.ListGlobal(), any Kind==agent with Status RUNNING → 409 "global agent already running" (D-34 copy voice; only RUNNING blocks, exited never blocks). Agent engine/command/extra-params come from the 15-01 singleton JOIN (read-at-use, D-24 — a live session keeps the agent it was spawned with). Resume (D-31): if req.Resume — opencode engine: singleton ocsid invalid → 409 "no global opencode session to resume"; claude engine: csid invalid OR !transcriptExists(h.globRoot, csid) → 409 "no global claude session to resume" (transcriptExists is cwd-agnostic — works verbatim); valid ids wire exactly as the task path (claude: opts.ResumeSessionID; opencode: the -s flag append in the custom arm). csid persist: replace the tasks UPDATE with a scope-branched persist — global → `UPDATE global_task SET claude_session_id = ? WHERE id = 1`, slog.Warn non-fatal on failure (Pattern 5). Capture re-target: parametrize captureOpencodeSessionAsync's persist target — an owner parameter distinguishing task-id from global (enum or small persist closure; the single call site at :476 is the only caller) — global opencode non-resume spawn → `UPDATE global_task SET opencode_session_id = ? WHERE id = 1`. The PWD pin, cmd.Dir, directory filter, and most-recently-updated-wins are already cwd-parameterized — unchanged. Write tests per the behavior block using fake-claude (argv capture per harness conventions), the discoverOpenCodeSession stub (CI), and one host-gated e2e behind exec.LookPath("opencode") (temp git repo root via the gitRepoWithCommit pattern; assert global_task.opencode_session_id captured; the fuller restart E2E stays Phase 17). Do NOT fork the poller, touch the task csid/capture paths, or modify argv construction.
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestGlobalAgent|TestGlobalOpencode' -count=1 && go test ./internal/api -count=1</automated>
  </verify>
  <acceptance_criteria>
  - `grep -q 'global agent already running' internal/api/sessions.go` (D-34) and `grep -q 'no global ' internal/api/sessions.go` (D-31 family: both engine variants)
  - `grep -c 'func captureOpencodeSessionAsync' internal/api/sessions.go` returns exactly 1 (no forked poller) and `grep -q 'UPDATE global_task SET opencode_session_id' internal/api/sessions.go` (re-target)
  - `grep -q 'UPDATE global_task SET claude_session_id' internal/api/sessions.go` (csid persist)
  - agent_integration_test.go registers GlobalRoutes in the harness
  - Tests cover every behavior line; one-agent 409 proven concurrent-only (exited → fresh 201); resume argv carries --resume with the seeded csid
  - Host-gated e2e exists behind the opencode LookPath guard and passes on this host: `go test ./internal/api -run 'TestGlobalOpencodeCaptureHost' -count=1 -v` shows capture; skips cleanly where opencode is absent
  - `go test ./internal/api -count=1` exits 0 (task path untouched)
  </acceptance_criteria>
  <done>Global agent spawns, resumes engine-branched, persists ids to the singleton, and captures opencode ids into global_task — all behind the D-decided gates, task paths byte-for-byte clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Status feed widening — synthesized global entry in BOTH passes, source:"global"</name>
  <files>internal/api/agents.go, internal/api/sessions_global_test.go</files>
  <read_first>
  - internal/api/agents.go (agentStatusEntry wire contract :26-38, status :44-218 — manager-pass TaskID filter :50 + newest map :53-56, task resumable derivation :135-137, DB-derived pass :156-218, synthesized append precedent :201-213)
  - internal/api/agents_test.go (both-passes test conventions)
  - internal/api/activity.go (:134-135 task-keyed queries — verify-don't-touch), internal/reaper/reaper.go (:166, :230 — verify-don't-touch)
  - .planning/phases/15-global-sessions-backend/15-RESEARCH.md (Spike 1 resolution + consumer-safety table, Pitfalls 1/3/10)
  - .planning/phases/15-global-sessions-backend/15-UI-SPEC.md (Wire Contract table — locked field values)
  </read_first>
  <behavior>
  - Live: a RUNNING global fake-claude agent → GET /api/agents/status contains exactly ONE entry with source "global", taskId 0, projectId 0, projectName "Global", taskTitle "Scratchpad", non-empty sessionId, real status/exitCode
  - Post-restart: fresh Manager over a DB with seeded global_task claude_session_id (+ fake transcript) → exactly ONE global entry, sessionId "", status "exited", resumable true, source "global"
  - No duplication: when the manager pass emits the live entry, the DB-derived pass emits none (and vice versa); at most one global entry in the feed at any time
  - Unconfigured root → no DB-pass global entry (nothing resumable)
  - Task entries byte-identical: same count and same fields as before this phase for a seeded task+project fixture (task-path regression)
  - GINT-03/GSESS-04 by construction: a RUNNING global session is absent from GET /api/activity stats/lists while a task session appears; reaper surface unchanged
  </behavior>
  <action>
  Manager pass (Pass 1b): after the existing task loop, a separate scan of mgr.List() for Kind==agent AND Info.Global — collect the newest OUTSIDE the newest[TaskID] map (Pitfall 3 — the map is task-keyed; a zero key would collide with dev scope). Append ONE synthesized agentStatusEntry: TaskID 0, ProjectID 0 (zero values, never nullable — Spike 1: TS types stay non-nullable, zero never equals a real id), SessionID from the live info, real Status/ExitCode/StopRequested, Resumable engine-branched (read singleton csid/ocsid + engine: claude → csid valid AND transcriptExists; opencode → ocsid valid; root_path plays the worktree's existence role — Pitfall 10, never reference worktree_path), PRNumber nil, Source "global", TaskTitle "Scratchpad", ProjectName "Global" (D-09/D-10 locked strings). DB-derived pass (Pass 2b): after the task DB loop, when NO manager global entry was emitted, read the singleton (root_path, csid, ocsid, engine via the agents JOIN — one query, no fan-out); if root non-empty AND the engine-branched id check passes → append the entry with SessionID "", Status "exited", ExitCode nil, Resumable true, Source "global", same locked labels. Do NOT rewrite the task INNER JOINs or touch the task passes — append-only (Pattern 2). The interim ActiveSessionsBar dead-nav row is the accepted artifact (UI-SPEC: zero frontend changes; deferring the widening is forbidden by the co-phasing mandate). Tests per the behavior block: fake-claude live entry via the 15-02 Task-1 harness; restart-sim with a fresh Manager; duplication guard; activity exclusion (global RUNNING absent while a task session present); reaper untouched (existing suites). No TS/frontend changes.
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestGlobalStatus|TestGlobalAgent|TestActivity' -count=1 && go test ./internal/... -count=1</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c '"global"' internal/api/agents.go` ≥ 2 (Source value in both passes) and `grep -q 'Scratchpad' internal/api/agents.go` + `grep -q '"Global"' internal/api/agents.go` (locked labels)
  - The global scan does not feed the task-keyed newest map (code review: separate variable per Pitfall 3)
  - Tests assert the live entry (both field-by-field values above), the post-restart exited/resumable entry, at-most-one global entry, and task-entry byte-stability
  - Activity exclusion test: global RUNNING session absent from activity response; a task session present
  - `go test ./internal/... -count=1` exits 0 — whole repo green (task-path byte-for-byte gate)
  - `git diff --name-only` contains no files under web/ (backend-only phase)
  </acceptance_criteria>
  <done>The co-phasing mandate is closed: a global agent is visible live and post-restart with honest locked labels; task feed byte-identical; GSESS-04/GINT-03 hold by construction and are test-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API spawn body | resume flag + scope on POST /api/sessions (loopback + origin allowlist upstream); no new fields beyond 15-01's scope |
| API→agent CLI | argv for the global agent is built from the singleton's agent config (Phase-10 validated surface), never client input |
| API→DB (async) | capture poller writes session ids to global_task — server-derived values only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering | capture poller persist target | high | mitigate | parametrized owner (enum/closure) — a wrong-table write is structurally impossible to express as a task-id sentinel; CI stub test asserts global_task written and tasks untouched |
| T-15-07 | Spoofing | status feed global entry | medium | mitigate | entry synthesized only from manager state + singleton ids — no client-controllable fields; labels are locked server constants |
| T-15-08 | Repudiation | csid persist failure | low | accept | warn-only slog (Pattern 5) matches task parity — costs resume, never the live session; single-user local app |
| T-15-09 | DoS | one-agent gate bypass via race | low | accept | single-threaded-per-request posture identical to the task path; worst case two agent tabs in a single-user local app |
| T-15-SC | Tampering | package installs | n/a | accept | zero external packages this phase |
</threat_model>

<verification>
- `go test ./internal/... -count=1` — whole repo green
- Host-gated: `go test ./internal/api -run 'TestGlobalOpencodeCaptureHost' -count=1 -v` — real opencode capture into global_task (skips where opencode absent)
- Curl story: PUT root → spawn global agent → GET /api/agents/status shows the Global · Scratchpad entry within the poll; restart the server → entry returns exited+resumable; POST {scope:"global", kind:"agent", resume:true} → 201 resumed conversation
- Phase-wide requirement sweep: GSESS-01..04, GVIEW-02/03, GINT-02/03 each backed by at least one automated test across 15-01 + 15-02
</verification>

<success_criteria>
- All must_haves.truths hold and are test-proven; roadmap SC1–SC5 for Phase 15 verifiable via the curl story
- GSESS-02 (restart reconcile + engine-branched resume), GSESS-04 (no auto-reap), GVIEW-02 (agent in root cwd, 409 on second concurrent), GINT-03 (activity exclusion) delivered
- Co-phasing mandate closed (SC3 both passes); the interim bar artifact is the accepted UI-SPEC state; zero frontend changes
</success_criteria>

<output>
Create `.planning/phases/15-global-sessions-backend/15-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 15-02)

New symbols (exclude from drift verification; consumed by Phase 16):
- `agentStatusEntry.Source` union value `"global"` (wire discriminator — Phase 16 branches on it)
- Pass 1b (manager-pass global scan) + Pass 2b (DB-derived singleton read) synthesized entries in agents.go status — internal branches, no new exported symbol
- `captureOpencodeSessionAsync` persist-target owner parameter (enum or closure type — exact spelling at executor discretion) + the scope-branched call-site dispatch in create
- Global csid persist branch: `UPDATE global_task SET claude_session_id = ? WHERE id = 1`
- Global resume validation branches (D-31 409s, engine-branched)
- One-agent gate in create's global branch (D-34)
- `newAgentIntegrationServer` GlobalRoutes registration + seedGlobalRoot test helper (harness additions)
- Host-gated opencode capture e2e test (LookPath skip-guard)
- sessions_global_test.go agent/status/activity test additions
