---
phase: 15-global-sessions-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/manager.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/api/sessions.go
  - internal/api/global.go
  - internal/api/sessions_global_test.go
autonomous: true
requirements: [GSESS-01, GSESS-03, GVIEW-03, GINT-02]
must_haves:
  truths:
    - "POST /api/sessions {scope:\"global\"} with no root configured returns 409 \"global root not configured\" for bash AND tmux kinds — never a silent home/cwd fallback (D-28, D-30)"
    - "With a configured root whose directory was deleted since PUT, spawn returns 409 \"global root no longer exists on disk: <path>\" with the path verbatim (D-29)"
    - "Global plain-bash and tmux bash tabs spawn 201 with cwd = the configured global root and the same shell options as tasks (GVIEW-03); tmux names mint kamacu-global-<n> from a scope-scoped counter (D-11/D-02)"
    - "Cross-scope tmux reattach 404s in both directions — global scope cannot see task rows, task scope cannot see global rows (D-32 scoped lookup)"
    - "GET /api/sessions?scope=global returns exactly the global sessions with taskTitle \"Scratchpad\" and projectName \"Global\" (D-09/D-10); dev sessions (TaskID 0, non-global) keep EMPTY join fields — no 0-keyed context leak (Pitfall 3)"
    - "GET /api/global live.agent/live.bash count RUNNING global PTYs from the engine (D-19 seams become real); PUT /api/global root change while a global session runs returns 409 with reasons (GCONF-04 bite, Pitfall 4)"
    - "A surviving global tmux row surfaces after restart-sim as an orphaned entry with Global:true via the scoped reconcile; the sweep already protects it (GSESS-03)"
    - "Engine scope stop kills exactly the global RUNNING sessions; dev and task sessions survive; ListGlobal lists only global sessions (GSESS-01 scope half)"
    - "Every existing task/dev test passes byte-for-byte untouched — full suites green (task-path regression gate)"
  artifacts:
    - internal/api/sessions_global_test.go
    - session.SpawnOpts.Global field
    - session.Manager.ListGlobal() / StopAllForScope()
  key_links:
    - "create() global branch: singleton SELECT (global_task JOIN agents, id=1) -> D-28/D-29 root gates -> opts.Cwd + opts.Global -> Manager.Spawn"
    - "mgr.ListGlobal() -> list ?scope=global, deriveGlobalState Live.Agent/Live.Bash, globalLiveBlockers manager half"
    - "info.Global -> per-entry synthesized labels in the sessionDetail loop + getSession (GINT-02; MCP rides GET /api/sessions)"
  prohibitions:
    - statement: "No side-channel global spawn endpoint — POST /api/sessions gains a scope field only"
      status: enforced
      verification: "no new session-spawn route registered in routes wiring"
    - statement: "The global path never stops sessions by fanning out over task id with a zero literal (kills every dev terminal — GSESS-01 landmine)"
      status: enforced
      verification: "repo-wide grep for that zero-literal call returns zero matches; scope stop uses the new engine method"
    - statement: "No LEFT-JOIN surgery on the hot list/status queries — global entries are synthesized, existing JOINs untouched"
      status: enforced
      verification: "joinSessionContext query text unchanged; diff shows no JOIN rewrites"
    - statement: "No silent home/cwd fallback on an unconfigured or vanished root"
      status: enforced
      verification: "D-28/D-29 409 tests"
    - statement: "No 0-keyed entry inserted into the ctxByTask map (leaks Scratchpad labels onto dev sessions — Pitfall 3)"
      status: enforced
      verification: "dev-session empty-label regression test"
    - statement: "No raw tmux has-session execs — all probes via tmux.Client (exact-match =name)"
      status: enforced
      verification: "no new exec.Command tmux probes in the diff"
---

<objective>
Engine global scope + the global BASH/config surface (Wave 1 of 2).

Add the additive `Global` scope discriminator to the session engine (`SpawnOpts.Global`/`Info.Global` + `globalCounter` + `ListGlobal()` + `StopAllForScope()`), then widen `POST /api/sessions` with a `scope:"global"` branch covering the D-28..D-33 root gates, plain bash, tmux mint `kamacu-global-<n>`, and D-32 scoped reattach — plus the visibility surface: `?scope=global` list with scoped reconcile, honest synthesized labels (GINT-02), and `GET /api/global` counts/gate going real (D-19, GCONF-04 bite).

Purpose: This is the foundation Wave 2 (agent + status feed) builds on; it is independently curl-verifiable and carries the milestone's task-path-regression guarantee (byte-for-byte clean).
Output: Modified engine + API files, new `sessions_global_test.go` suite, real `GET /api/global` counts.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-global-sessions-backend/15-CONTEXT.md
@.planning/phases/15-global-sessions-backend/15-RESEARCH.md
@.planning/phases/15-global-sessions-backend/15-PATTERNS.md
@.planning/phases/14-global-config-api/14-CONTEXT.md

@internal/session/manager.go
@internal/session/session.go
@internal/api/sessions.go
@internal/api/global.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine global scope — flag, counter, ListGlobal, StopAllForScope</name>
  <files>internal/session/manager.go, internal/session/session.go, internal/session/session_test.go</files>
  <read_first>
  - internal/session/manager.go (Manager struct :35-52, SpawnOpts :54-80, Spawn label switch :331-345, session literal :347-365, List/ListByTask/listWhere :414-448, StopAllForTask :450-475)
  - internal/session/session.go (Info :44-68 with Orphaned/TmuxName omitempty precedents, Session struct :79-91, Info() :199-224)
  - internal/session/session_test.go (engine test conventions)
  - .planning/phases/15-global-sessions-backend/15-PATTERNS.md (§manager.go + §session.go patterns, Shared Pattern 1)
  </read_first>
  <behavior>
  - Global bash sessions get labels "Bash 1", "Bash 2", ... from a dedicated monotonic counter — never the dev-session label family
  - Dev sessions (no task, not global) keep their existing label family byte-identically; task sessions keep "Bash N" per-task counters
  - A global agent session labels "Agent" (kind arm stays first — one agent per scope, API enforces)
  - ListGlobal() returns exactly the sessions spawned with the global flag
  - The scope stop terminates only global RUNNING sessions; concurrent dev and task sessions stay alive
  - Info.Global serializes as "global" (omitempty — false omits for task/dev sessions)
  </behavior>
  <action>
  Add `Global bool` to SpawnOpts with a comment documenting the zero-value back-compat contract (rides the Kind/Shell/TmuxName precedent — false = current task/dev behavior). Add unexported `global bool` to the Session struct next to `taskID` (same "immutable after Spawn" contract) and set it in the Spawn session literal next to `taskID: opts.TaskID`. Add `Global bool` with json tag `global,omitempty` to Info and populate it in Info() alongside TaskID. Add `globalCounter int` to Manager next to `counter`/`taskCounters` (same never-reused monotonic contract). In the label switch, insert a `case opts.Global:` arm producing "Bash N" from globalCounter — positioned AFTER the KindAgent arm but BEFORE the TaskID-zero dev arm (Pitfall 2 ordering, or global bash tabs get dev labels). Add `ListGlobal()` as a listWhere call with the global predicate (mirror ListByTask :421). Add `StopAllForScope()` mirroring the StopAllForTask collect-under-lock → concurrent Stop → Wait shape with the predicate swapped to the global flag — scope-targeted stop only via this new method (the task-id fan-out is the dev-terminal landmine, never used here). Do NOT touch the claude/custom argv arms (:152-243), PTY/ring/attach machinery, or Spawn's cwd validation — the flag is opaque to everything except labels/listing/stopping. Add engine tests per the behavior block (label sequences for all three scopes, ListGlobal filter, scope-stop isolation with one dev + one task + one global session alive).
  </action>
  <verify>
    <automated>go test ./internal/session -count=1</automated>
  </verify>
  <acceptance_criteria>
  - manager.go declares `Global bool` on SpawnOpts; session.go declares `Global bool` with json tag `global,omitempty` on Info and populates it in Info()
  - `grep -c 'func (m \*Manager) ListGlobal\|func (m \*Manager) StopAllForScope' internal/session/manager.go` returns 2
  - The global label arm sits between the agent arm and the dev arm: `grep -n 'case opts.Global' internal/session/manager.go` line number is greater than the KindAgent label line and less than the TaskID-zero arm line
  - Engine tests assert: global labels "Bash 1"/"Bash 2"; dev label family unchanged; task "Bash N" unchanged; ListGlobal returns exactly global sessions; the scope stop leaves dev+task sessions RUNNING
  - `go test ./internal/session -count=1` exits 0
  </acceptance_criteria>
  <done>Engine carries the third scope additively; all three label families verified; scope-isolated stop proven; existing engine suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scope body field + global bash spawn — root gates, plain bash, tmux mint, scoped reattach</name>
  <files>internal/api/sessions.go, internal/api/sessions_global_test.go</files>
  <read_first>
  - internal/api/sessions.go (create :223-496 — request struct :224-234, kind validation :239-247, task worktree resolution :282-304, one-agent gate :305-315, resume validation :316-339, reattach lookup :375-394, settings shell read :396-401, tmux mint :403-436, spawn-failure release :437-457)
  - internal/api/global.go (loadGlobalConfig :198-215 — the read-at-use singleton SELECT shape to mirror)
  - internal/store/migrations/00017_global_task.sql (singleton + resume-id columns), internal/store/migrations/00018_tmux_scope.sql (scope, XOR CHECK, name UNIQUE)
  - internal/api/global_test.go (PUT /api/global folder seeding, tmux skip-guard + per-test socket conventions :558-577)
  - .planning/phases/15-global-sessions-backend/15-PATTERNS.md (§sessions.go — gate/mint/reattach verbatim analogs, Shared Patterns 2/3/7/8)
  </read_first>
  <behavior>
  - {scope:"global", kind:"bash"} with root_path='' → 409 "global root not configured"
  - Same request after PUT-ing a folder root then deleting the directory → 409 "global root no longer exists on disk: <path>" (path verbatim)
  - {scope:"global", kind:"bash", tmux:true} hits the same two gates (uniform gating, D-30)
  - {scope:"global", task_id:5} → 400 "supply either scope or task_id, not both"; {scope:"weird"} → 400 "invalid scope"
  - Configured root + plain bash → 201, session alive, shell cwd == global root (proven via POST /api/sessions/{id}/input writing pwd to a file)
  - Configured root + tmux bash → 201, tmux session named kamacu-global-1 exists (second mint → kamacu-global-2), tmux_sessions row has task_id NULL + scope 'global' + label "Bash 1"
  - Reattach by the minted name → 201 keeping label "Bash 1"; reattach of a task-scoped name from global scope → 404 "no session to reattach"; reattach of a global name from task scope → 404
  </behavior>
  <action>
  Add `Scope string` with json tag `scope` to create's request struct. Validate at the kind switch: scope must be "" or "global" else 400 "invalid scope" (the invalid-kind family voice); scope "global" together with a non-zero task_id → 400 "supply either scope or task_id, not both" (the global.go:288 mutual-exclusion family). Insert the global resolution block BEFORE kind dispatch, exactly where the task worktree query feeds every kind today: singleton read-at-use SELECT of root_path, agent_id, claude_session_id, opencode_session_id, engine/command/extra_params via JOIN agents (WHERE g.id = 1, mirroring loadGlobalConfig); ErrNoRows → 500 (corrupted invariant). Gate order per D-33: root_path empty → 409 "global root not configured" (D-28); os.Stat fails or not-a-dir → 409 "global root no longer exists on disk: " + path verbatim (D-29). One gate block covers plain bash, tmux, and (Wave 2) agent + resume uniformly (D-30). On pass: opts.Cwd = rootPath, opts.Global = true. Plain bash: read the settings shell exactly as the task path does (settings.Get with KeyShell). tmux: counter from `SELECT COALESCE(MAX(n),0)+1 FROM tmux_sessions WHERE scope = 'global'`; name fmt "kamacu-global-%d" (D-11/D-02); label fmt "Bash %d"; INSERT with (NULL task_id, 'global', n, name, label) — the 00018 XOR CHECK requires both to agree; the real mint race backstop is name UNIQUE (NULL task_ids are DISTINCT under the task-id composite — do not claim otherwise in comments). The spawn-failure row release and the tmux-not-found 409 path apply verbatim (name-keyed, scope-agnostic). Reattach: `SELECT label FROM tmux_sessions WHERE scope = 'global' AND name = ?` → ErrNoRows → 404 "no session to reattach" (D-32 — a foreign row simply does not exist in this scope; no scope-mismatch branch, no information leak). Write sessions_global_test.go per the behavior block: CI-safe gate/validation/bash tests using PUT /api/global to seed a temp folder root, plus tmux-guarded mint/reattach/cross-scope tests behind the exec.LookPath("tmux") skip-guard with per-test socket + KillServer cleanup. Do NOT touch the task branches, argv construction, or the agent branch (Wave 2).
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestGlobalSession' -count=1 && go test ./internal/api -count=1</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'Scope string' internal/api/sessions.go` ≥ 1 and the request struct json tag includes scope
  - `grep -q 'global root not configured' internal/api/sessions.go` and `grep -q 'no longer exists on disk' internal/api/sessions.go` (D-28/D-29 strings)
  - `grep -q "WHERE scope = 'global' AND name" internal/api/sessions.go` (D-32 scoped reattach) and `grep -q 'kamacu-global-%d' internal/api/sessions.go` (D-11/D-02 mint format)
  - sessions_global_test.go exists and covers every behavior line above; gates tested for bash AND tmux kinds (D-30)
  - `grep -rn 'StopAllForTask(0)' internal/` returns no matches (the dev-terminal landmine never appears)
  - `go test ./internal/api -run 'TestGlobalSession' -count=1` exits 0 and the full `go test ./internal/api -count=1` exits 0 (task path untouched)
  </acceptance_criteria>
  <done>scope:"global" bash + tmux spawn works end-to-end behind honest 409 gates; cross-scope reattach 404s both ways; existing suites green untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ?scope=global list + scoped reconcile + honest labels + real GET /api/global counts + GCONF-04 gate widening</name>
  <files>internal/api/sessions.go, internal/api/global.go, internal/api/sessions_global_test.go</files>
  <read_first>
  - internal/api/sessions.go (list :65-134 — project filter/default branches, task branch reconcile call :118-123, ctxByTask loop :127-132; reconcileTmux :144-214 — survivor/GC logic + orphaned Info synthesis :196-204; joinSessionContext :917-964; getSession context application :976-977)
  - internal/api/global.go (liveGlobalTmuxNames :88, globalLiveBlockers :123-129, loadGlobalConfig :198-215, deriveGlobalState :223-234 with the Agent/Bash zero-seams)
  - internal/mcp/sessions.go (:478-579 — list/get bridge riding GET /api/sessions; the GINT-02 consumer)
  - cmd/kamacu/serve.go (sweep :389 — already scope-aware, verify only)
  - .planning/phases/15-global-sessions-backend/15-RESEARCH.md (Pitfalls 3, 4, 6; Open Question 1)
  </read_first>
  <behavior>
  - GET /api/sessions?scope=global returns exactly the global sessions; each carries taskTitle "Scratchpad", projectName "Global", and the singleton's agentName; a surviving tmux row (no live in-memory session) surfaces as an orphaned entry with orphaned:true + global:true + label via defaultTmuxLabel
  - Unscoped GET /api/sessions (what MCP list_sessions rides) includes global sessions with the same honest synthesized labels
  - A dev session (no body create) in the same unscoped list keeps EMPTY taskTitle/projectName/agentName — the Scratchpad labels never leak onto TaskID-0 dev sessions (Pitfall 3 regression)
  - After spawning a global plain-bash session, GET /api/global returns live.bash ≥ 1 and live.agent 0; a global tmux tab counts only under live.tmux (fields disjoint by name — OQ1 resolution); a global agent (Wave 2) will count under live.agent
  - PUT /api/global root change while a global bash session is RUNNING → 409 with reasons including the live session; after stopping it → PUT succeeds (GCONF-04 bite)
  - Restart-sim: fresh Manager over the same DB, alive global tmux row → ?scope=global surfaces the orphaned entry (GSESS-03)
  </behavior>
  <action>
  In list(), add a `scope=global` query branch: infos = mgr.ListGlobal(), then a scoped reconcile variant — the reconcileTmux survivor/lazy-GC logic with the WHERE clause scoped to scope='global' instead of task_id; the synthesized orphaned session.Info gains Global:true and the empty-label fallback goes through defaultTmuxLabel exactly as tasks do (defaultTmuxLabel("kamacu-global-3") already yields "Bash 3" — verified). Leave the unscoped default branch as mgr.List() — global sessions flow through it already; MCP rides it. Honest labels (GINT-02, D-09/D-10): in the shared sessionDetail loop and in getSession, synthesize per-entry gated on info.Global — taskTitle "Scratchpad", projectName "Global", agentName from one singleton agent JOIN read per request — and NEVER insert a zero-keyed entry into the ctxByTask map (Pitfall 3: the map is keyed by the field global sessions don't have; key 0 would leak labels onto every dev session). In global.go deriveGlobalState: replace the two zero-seams — Live.Agent = count of RUNNING KindAgent sessions in mgr.ListGlobal(); Live.Bash = count of RUNNING plain-bash sessions (not agent, TmuxName empty — tmux stays its own field, disjoint by name per OQ1 resolution); Live.Tmux unchanged. Widen globalLiveBlockers: after the tmux loop, every RUNNING session in mgr.ListGlobal() appends a deleteBlocker with Kind "sessions" and a Target identifying it (label or id) — the GCONF-04 gate must bite now that global PTYs exist (Pitfall 4). Write tests per the behavior block; reuse the Task-2 seeding helpers. Verify-don't-touch: serve.go sweep, reaper, activity (already scope-safe/task-keyed).
  </action>
  <verify>
    <automated>go test ./internal/api -count=1 && go test ./internal/session -count=1</automated>
  </verify>
  <acceptance_criteria>
  - `grep -q 'Get("scope")' internal/api/sessions.go` (the list filter branch exists)
  - `grep -c 'ListGlobal' internal/api/global.go` ≥ 2 (deriveGlobalState + globalLiveBlockers both consume it)
  - The deriveGlobalState zero-seam comments/seams are gone: no `Live.Agent = 0` / `Live.Bash = 0` assignments remain in internal/api/global.go
  - Tests assert: global labels Scratchpad/Global on ?scope=global AND unscoped list; dev session keeps empty join fields (Pitfall 3); live.bash ≥ 1 after global bash spawn; root-change 409 with reasons while RUNNING and success after stop; restart-sim orphaned entry with global:true
  - `go test ./internal/api -count=1` exits 0 (full package, task path untouched)
  </acceptance_criteria>
  <done>Global bash/tmux sessions are visible, labeled, counted, and gate-protecting the root config; reconcile surfaces restart survivors; existing suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API spawn body | `scope` is a new untrusted body field on POST /api/sessions (loopback + origin allowlist already enforced upstream) |
| API→filesystem | cwd for global spawns resolves against the server-side singleton (PUT-validated in Phase 14) |
| API→tmux | reattach names are client-supplied; minted names are server-generated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | spawn body → cwd | high | mitigate | cwd comes ONLY from the server-side singleton (Phase-14 PUT validation, footgun blocks); the spawn body carries no path; D-28/D-29 gate every kind (D-30) |
| T-15-02 | Tampering | tmux reattach names | medium | mitigate | names looked up via parameterized scope-scoped SQL (D-32); all liveness probes via tmux.Client exact-match (=name) — no raw exec, no prefix-match trap |
| T-15-03 | Elevation | cross-scope reattach | medium | mitigate | scoped lookup: a foreign-scope row simply does not resolve → 404, no information leak, no scope-mismatch oracle |
| T-15-04 | Tampering | tmux mint race | low | accept | name UNIQUE is the backstop (NULL task_ids DISTINCT under the composite); single-user loopback window matches the task-path posture today |
| T-15-05 | Input validation | `scope` field | low | mitigate | closed-set validation → 400 "invalid scope"; mutual exclusion with task_id → 400 |
| T-15-SC | Tampering | package installs | n/a | accept | zero external packages this phase (zero-new-deps composition, verified against go.mod) |
</threat_model>

<verification>
- `go test ./internal/... -count=1` — whole repo green (task-path byte-for-byte gate)
- Curl story (run against a dev server): unconfigured spawn → 409; PUT a folder root; spawn {scope:"global", kind:"bash"} → 201 + listed under ?scope=global with Scratchpad/Global labels; GET /api/global shows live.bash 1; PUT root change → 409 with reasons
- tmux surface (host with tmux): mint kamacu-global-1, reattach keeps label, cross-scope 404 both directions
</verification>

<success_criteria>
- All must_haves.truths hold and are test-proven; every existing suite green without modification (test-file additive changes only)
- GSESS-01 (engine scope + scope-targeted stop), GSESS-03 (tmux durability + invisible reattach), GVIEW-03 (bash tabs, same shell options), GINT-02 (honest labels through the API/MCP surface) delivered for the bash surface
- Wave 2 (15-02) unblocked: Info.Global + ListGlobal() available to the agent branch and status passes
</success_criteria>

<output>
Create `.planning/phases/15-global-sessions-backend/15-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 15-01)

New symbols (exclude from drift verification; consumed by 15-02):
- `session.SpawnOpts.Global` (bool field, zero value = task/dev scope)
- `session.Session.global` (unexported field, immutable after Spawn)
- `session.Info.Global` (bool, json `global,omitempty`)
- `session.Manager.globalCounter` (unexported field)
- `session.Manager.ListGlobal() []Info`
- `session.Manager.StopAllForScope()`
- `POST /api/sessions` body field `scope` (string; "" | "global")
- `GET /api/sessions?scope=global` query filter
- Scoped global reconcile variant in sessions.go (reconcileTmux sibling; name at executor discretion)
- Per-entry global context synthesis in the sessionDetail loop + getSession (behavior, no new exported symbol)
- `globalHandlers.globalLiveBlockers` manager half + `deriveGlobalState` real Live.Agent/Live.Bash derivations
- `internal/api/sessions_global_test.go` (new test file + seeding helpers reused by 15-02)
