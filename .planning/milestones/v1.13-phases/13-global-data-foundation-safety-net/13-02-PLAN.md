---
phase: 13-global-data-foundation-safety-net
plan: 2
type: execute
wave: 2
depends_on:
- 13-01
files_modified:
- internal/api/global_backfill.go
- internal/api/global_backfill_test.go
- internal/api/agents_crud.go
- internal/api/agents_crud_test.go
- cmd/kamacu/serve.go
- cmd/kamacu/sweep_test.go
autonomous: true
requirements:
- GDATA-01
- GDATA-02
- GDATA-03
must_haves:
  truths:
  - "BackfillGlobalTask is idempotent — a no-op on healthy boots (singleton already present) and re-inserts id=1 seeded from agents WHERE is_default=1 after a hand DELETE (GDATA-01, ROADMAP SC2's boot half; TestBackfillGlobalTask green)"
  - "cmd/kamacu/serve.go wires BackfillGlobalTask AFTER BackfillAgents/BackfillOpenCodeAgent (the seed reads the default agent — ordering load-bearing); an error refuses boot with subcommands.ExitFailure"
  - "DELETE /api/agents/{id} for the agent referenced by global_task returns 409 with body containing 'reassign the Scratchpad agent first' (D-09 — the one user-facing string this phase ships, UI-SPEC Copywriting row 1); after moving global_task.agent_id away, the delete succeeds 204 (GDATA-01, SC2's FK half)"
  - "The startup sweep never kills a live global tab: on a per-test tmux socket with a seeded scope='global' row + a live kamacu-global-1 session, sweepOrphanTmux leaves kamacu-global-1 ALIVE, leaves task-backed kamacu-1-1 ALIVE, and still kills true task orphan kamacu-42-1 (GDATA-03, ROADMAP SC4; host-gated, skips when tmux absent)"
  - "The known-set query in sweepOrphanTmux is exactly the scope-aware form — global rows known WITHOUT a task JOIN — and everything else in the sweep (30s timeout, degrade-don't-break warn-only returns, kamacu- prefix filter, warn-only kill failures) is unchanged"
  - "A .backup copy of the real v1.12 install (goose v16, 10 custom-labeled tmux rows) booted once with the real binary upgrades to goose v18 with the tmux survival surface byte-for-byte identical, task count unchanged, and global_task seeded from the install's default agent (GDATA-02 SC1 rehearsal + SC2 boot proof on real data)"
  artifacts:
  - internal/api/global_backfill.go
  - internal/api/global_backfill_test.go
  - cmd/kamacu/sweep_test.go
  key_links:
  - "serve.go backfill wiring order: BackfillAgents (serve.go:171) → BackfillOpenCodeAgent (serve.go:186) → BackfillGlobalTask (new block after line 189) — Pitfall 6: wiring before BackfillAgents garbles boot on an agents-wiped install"
  - "sweepOrphanTmux's known-set query (serve.go:369) reads the scope column that only exists after migration 00018 — this plan depends on 13-01"
  - "agents_crud.go delete handler's COUNT guard reads global_task.agent_id ahead of the 00017 ON DELETE RESTRICT backstop — friendly 409 before the raw FK error (the projects.agent_id pattern, agents_crud.go:255-263)"
---

<objective>
Land the runtime safety net around Plan 13-01's schema: the idempotent boot backfill that re-arms the `global_task` singleton (GDATA-01), the scope-aware startup tmux orphan sweep that can never kill a live global tab (GDATA-03 — the roadmap's co-phasing mandate: the sweep fix MUST ship in the same phase as the migration, because the migration alone makes every live `kamacu-global-*` session look orphaned and kills it at next startup), the agents delete-guard extension emitting the phase's one user-facing string "reassign the Scratchpad agent first" (D-09, UI-SPEC row 1), and the real-install upgrade rehearsal that proves byte-for-byte survival on a copy of the actual v1.12 database (GDATA-02 SC1).

Purpose: The DB invariants from 13-01 are unconditional only once the backfill runs every boot; the sweep fix converts a known killer bug into a tested no-kill guarantee before any global session can ever exist (the spawn path is Phase 15).

Output: `BackfillGlobalTask` + serve.go wiring, the one-query sweep fix + host-gated kill/no-kill regression test, the delete-guard COUNT + 409 test, and the scripted real-install rehearsal. No new API routes, no frontend, no spawn-path code (CONTEXT phase boundary; 13-RESEARCH Pitfall 7: the Go diff is exactly backfill + wiring + delete-guard COUNT + sweep query).
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-global-data-foundation-safety-net/13-CONTEXT.md
@.planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md
@.planning/phases/13-global-data-foundation-safety-net/13-PATTERNS.md
@.planning/phases/13-global-data-foundation-safety-net/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BackfillGlobalTask + boot wiring + Scratchpad delete-guard (tests first)</name>
  <files>internal/api/global_backfill.go, internal/api/global_backfill_test.go, cmd/kamacu/serve.go, internal/api/agents_crud.go, internal/api/agents_crud_test.go</files>
  <read_first>
  - internal/api/agents_backfill.go (THE analog — BackfillAgents at lines 22-37: QueryRow no-op fast path → errors.Is(err, sql.ErrNoRows) discrimination → literal INSERT; mirror its doc-comment posture stating the invariant, idempotence, and ordering)
  - internal/api/agents_backfill_test.go (the backfill test analog — TestBackfillAgents: healthy-boot no-op → hand-DELETE → re-create → seed shape assertions → second-call idempotence)
  - cmd/kamacu/serve.go lines 150-189 (the backfill wiring block — the new call goes after BackfillOpenCodeAgent at line 186; copy the block shape: requirement-tagged comment → if err := api.BackfillX(db) → slog.Error → return subcommands.ExitFailure)
  - internal/api/agents_crud.go lines 226-275 (the delete handler being extended — insert the new COUNT after the projects guard at 255-263, before the DELETE exec)
  - internal/api/agents_crud_test.go TestAgentDeleteInUse at lines 203-231 (the in-use test analog — newTestServer + doJSON + direct db.Exec seeding)
  - .planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md ("Code Examples → The delete-guard extension" + Pitfall 4 and Pitfall 6)
  </read_first>
  <behavior>
  - Test 1 (TestBackfillGlobalTask): after store.Open + store.Migrate on a temp DB, the singleton exists (migration 00017 seeded it) → BackfillGlobalTask returns nil and the row count stays exactly 1 (healthy-boot no-op)
  - Test 2: after `DELETE FROM global_task` (hand-deleted row), BackfillGlobalTask re-inserts exactly one row with id=1 and agent_id = the agents row where is_default=1 (assert via SQL comparison, never a hardcoded agent id); a second call is again a no-op (still 1 row)
  - Test 3 (TestAgentDeleteInUseByGlobal): create a custom agent via POST /api/agents, point global_task.agent_id at it via direct db.Exec UPDATE → DELETE /api/agents/{id} returns 409 and the body contains "reassign the Scratchpad agent first"
  - Test 4: after UPDATE global_task SET agent_id back to the default agent's id, the same DELETE returns 204 (block-until-reassigned semantics)
  </behavior>
  <action>
  RED: write internal/api/global_backfill_test.go (TestBackfillGlobalTask, mirroring TestBackfillAgents' four-phase shape) and add TestAgentDeleteInUseByGlobal to internal/api/agents_crud_test.go (mirror TestAgentDeleteInUse's seeding: newTestServer, doJSON POST /api/agents with name "Scratch"/command "x", direct db.Exec for the global_task UPDATE). Run them — they fail to compile (BackfillGlobalTask undefined) / fail on the missing 409.

  GREEN: create internal/api/global_backfill.go (package api, imports database/sql + errors only, mirroring agents_backfill.go's leaf posture): `func BackfillGlobalTask(db *sql.DB) error` — fast path `db.QueryRow(`SELECT id FROM global_task WHERE id = 1`)` scan into int64; err == nil → return nil (healthy-boot no-op); !errors.Is(err, sql.ErrNoRows) → return err (real DB errors propagated, never swallowed); else `db.Exec(`INSERT INTO global_task (id, agent_id) SELECT 1, id FROM agents WHERE is_default = 1`)` and return its err — the same never-hardcode-1 rule as migration 00017 (Pitfall 4). Doc comment states: the GDATA-01 invariant (singleton exists on every boot), why migration-only seeding is insufficient (goose runs once; a hand-deleted row would stay missing forever), and that wiring order after BackfillAgents is load-bearing (the seed reads the default agent).

  Wire in cmd/kamacu/serve.go: insert a new block immediately AFTER the BackfillOpenCodeAgent block (after line 189), before the opencode.InstallPlugin block — requirement-tagged comment (GDATA-01) explaining the ordering constraint, then `if err := api.BackfillGlobalTask(db); err != nil { slog.Error("backfilling global task", "error", err); return subcommands.ExitFailure }`. Never place it before the BackfillAgents call (Pitfall 6).

  Extend internal/api/agents_crud.go's delete handler: after the existing projects COUNT guard and before the DELETE exec, add — `var g int`; `h.db.QueryRow(`SELECT COUNT(*) FROM global_task WHERE agent_id = ?`, id).Scan(&g)` with err → writeError 500 err.Error(); `if g > 0 { writeError(w, http.StatusConflict, "reassign the Scratchpad agent first"); return }`. The message is count-free because the singleton references exactly one agent, always (UI-SPEC Copywriting row 1 — exact string, D-09 user-facing naming; the DB-level RESTRICT from 00017 is the backstop, the guard runs first for the clean message).

  Do NOT add API routes, do NOT touch the session engine or reaper (13-RESEARCH Pitfall 7: no writer changes are needed — every existing tmux reader is safe by construction).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestBackfillGlobalTask|TestAgentDeleteInUseByGlobal' -v && go build ./... && grep -c "BackfillGlobalTask" cmd/kamacu/serve.go</automated>
  </verify>
  <acceptance_criteria>
  - internal/api/global_backfill.go contains `func BackfillGlobalTask(db *sql.DB) error` with the QueryRow fast path, the errors.Is(err, sql.ErrNoRows) discrimination, and the INSERT..SELECT seeded from `WHERE is_default = 1`
  - cmd/kamacu/serve.go contains exactly one BackfillGlobalTask call, positioned after the BackfillOpenCodeAgent error-check block, with the slog.Error + return subcommands.ExitFailure shape
  - The 409 body string in agents_crud.go is exactly `reassign the Scratchpad agent first` (count-free, D-09)
  - `go test ./internal/api/ -run 'TestBackfillGlobalTask|TestAgentDeleteInUseByGlobal' -v` exits 0 with both tests passing
  - `go build ./...` exits 0
  - TestAgentDeleteInUseByGlobal proves both halves: 409 while referenced, 204 after reassignment
  </acceptance_criteria>
  <done>Boot re-arms the singleton on every start (idempotent backfill, error refuses boot); deleting the Scratchpad's agent returns the clean D-09 409 ahead of the FK RESTRICT backstop; both behaviors test-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope-aware orphan sweep — failing regression first, then the one-query fix</name>
  <files>cmd/kamacu/sweep_test.go, cmd/kamacu/serve.go</files>
  <read_first>
  - cmd/kamacu/serve.go lines 343-403 (sweepOrphanTmux — ONLY the known-set query at line 369 and its adjacent comment change; the 30s timeout, ListSessions degrade-don't-break warn-only return, kamacu- prefix filter, and warn-only kill failures are all correct and stay untouched)
  - internal/tmux/tmux_test.go lines 13-36 (newTestClient + newDetachedSession — the host-gate + per-test-socket helpers to RE-CREATE in package main; they are unexported test-file funcs in package tmux, not importable)
  - internal/api/sessions_test.go lines 650-679 (the liveness poll-with-deadline helper shape and the t.Skip("tmux not on PATH") convention)
  - internal/tmux/tmux.go lines 88-114 (HasSession/KillSession exact-match `=`+name target semantics), lines 51-64 (BaseArgs)
  - .planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md (Pattern 4 — the test design; "Code Examples → The sweep fix" — the verified query; Pitfall 1 — the co-phasing mandate this task closes)
  </read_first>
  <behavior>
  - RED (run before the fix, expect FAILURE): with the current INNER-JOIN known-set query, seeding a scope='global' tmux_sessions row (task_id NULL) plus a live kamacu-global-1 session → sweepOrphanTmux KILLS kamacu-global-1 (the test's ALIVE assertion fails) — this is the empirically demonstrated killer bug the co-phasing mandate exists for; commit the failing test
  - GREEN (after the fix): kamacu-global-1 ALIVE (sweep-no-kill on a live global tab — SC4 first half), task-backed kamacu-1-1 ALIVE (unchanged behavior), true orphan kamacu-42-1 (no row, no task) DEAD (pre-existing orphan-killing preserved — SC4 second half)
  </behavior>
  <action>
  Create cmd/kamacu/sweep_test.go (package main — sweepOrphanTmux is package-private so the test MUST live here; confirmed no existing sweep test file). Re-create the two helpers locally (the analogs are test-file-unexported in internal/tmux): a host-gate + per-test-socket client builder using exec.LookPath("tmux") → t.Skip("tmux not on PATH"), a unique socket via fmt.Sprintf("ktest-%d-%s", os.Getpid(), t.Name()), Client{Socket: socket, ConfPath: "/dev/null"}, with t.Cleanup(KillServer) registered BEFORE any session is created; and a detached-session starter using BaseArgs + "new-session", "-d", "-s", name, "-c", t.TempDir() (tests have no tty — production's -A would fail).

  TestSweepOrphanTmuxScopeAware: store.Open(filepath.Join(t.TempDir(), "test.db")) + store.Migrate(db). Seed: one project (`INSERT INTO projects (name, repo_path, workspace_id) VALUES (...)` with unique repo_path), one task under it; tmux row kamacu-1-1 (task_id = the seeded task's id, n=1, label ''); tmux row kamacu-global-1 with task_id NULL and scope='global' (n=1) inserted DIRECTLY via SQL — no global spawn path exists until Phase 15, the row is the sweep's only input; NO row for kamacu-42-1 and no task 42 (a true task-orphan). Create all three live sessions on the per-test socket. Call sweepOrphanTmux(context.Background(), db, client). Assert via client.HasSession with the poll-deadline helper (kills and probes are async): kamacu-global-1 alive, kamacu-1-1 alive, kamacu-42-1 dead.

  Run the test against the CURRENT sweep → it fails at the kamacu-global-1 ALIVE assertion (the INNER JOIN at serve.go:369 drops NULL-task rows, so the live global session looks orphaned and dies — demonstrated on real data: 11 known rows → 10 under the INNER JOIN). Commit the failing regression.

  Then apply the one-query fix in cmd/kamacu/serve.go: replace ONLY the known-set query string at line 369 with `SELECT name FROM tmux_sessions WHERE scope = 'global' OR task_id IN (SELECT id FROM tasks)` and update its adjacent comment to the new known-set semantics (a row whose task STILL exists, OR any global-scoped row — task-less by design, GDATA-03). The LEFT-JOIN equivalent is NOT used (planner resolution of the discretion item — the subquery form is what the research verified against real data). Everything else in sweepOrphanTmux stays byte-identical. Run the test → green. Commit the fix.
  </action>
  <verify>
    <automated>go test ./cmd/kamacu/ -run TestSweepOrphanTmuxScopeAware -v && grep -c "WHERE scope = 'global' OR task_id IN (SELECT id FROM tasks)" cmd/kamacu/serve.go</automated>
  </verify>
  <acceptance_criteria>
  - cmd/kamacu/sweep_test.go exists in package main with the exec.LookPath host-gate skip, a unique per-test socket, and KillServer cleanup registered before session creation
  - The test seeds the global row via direct SQL with task_id NULL + scope='global', and asserts all three outcomes (global ALIVE, task-backed ALIVE, orphan DEAD)
  - The failing-test-first commit exists in the phase commit history (test commit preceding the fix commit)
  - cmd/kamacu/serve.go's known-set query is exactly the verified scope-aware form; the git diff on serve.go's sweep function shows ONLY the query string + adjacent comment changed
  - `go test ./cmd/kamacu/ -run TestSweepOrphanTmuxScopeAware -v` exits 0 PASSING on this host (tmux 3.4 present; skips cleanly elsewhere)
  - `go test ./cmd/kamacu/` full package green (TestEnsureLoopback unregressed)
  </acceptance_criteria>
  <done>The co-phasing mandate is closed: the migration and the sweep fix ship together, with a regression test that first PROVES the killer bug (live global tab dies under the old query) and then proves the fix (global tab survives, orphan killing unchanged).</done>
</task>

<task type="auto">
  <name>Task 3: Real-install rehearsal — .backup copy, boot the real binary, byte-for-byte diff</name>
  <files>(none — verification-only task; artifacts live under /tmp/opencode, nothing committed except the SUMMARY)</files>
  <read_first>
  - .planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md ("Code Examples → The verified upgrade rehearsal script" — executed 11/11 green by the researcher; "Pitfall 5" — NEVER cp a live WAL database; "Runtime State Inventory" — the real install's shape)
  - cmd/kamacu/serve.go SetFlags lines 60-62 (--db and --addr flags exist; default addr 127.0.0.1:7333)
  </read_first>
  <action>
  Run the roadmap-mandated rehearsal on a copy of the real v1.12 install (~/.kamacu/kamacu.db at goose v16: 147 tasks, 10 custom-labeled tmux rows, 4 agents, default=Claude id=1, live 4MB WAL):

  1. WAL-safe copy: `mkdir -p /tmp/opencode && sqlite3 ~/.kamacu/kamacu.db ".backup /tmp/opencode/rehearsal-v113.db"` — the online-backup API, safe while the real server stays up (a raw cp of the main file without the WAL is a torn, stale snapshot and the rehearsal proves nothing — Pitfall 5).
  2. Snapshot BEFORE: `sqlite3 /tmp/opencode/rehearsal-v113.db "SELECT id,task_id,n,name,label,created_at FROM tmux_sessions ORDER BY id" > /tmp/opencode/rehearsal-before.txt`; also record `SELECT COUNT(*) FROM tasks;` and the current max goose version.
  3. Build the real binary: `go build -o /tmp/opencode/kamacu-rehearsal ./cmd/kamacu`.
  4. Boot it once against the copy on a spare loopback port: run `/tmp/opencode/kamacu-rehearsal serve --db /tmp/opencode/rehearsal-v113.db --addr 127.0.0.1:7391` in the background with output to /tmp/opencode/rehearsal.log; wait for the listening log line; then terminate the process and wait for exit. This single boot exercises migrations 00017+00018 AND the BackfillGlobalTask wiring AND the fixed sweep against real data (SC2's boot half on a real install — the binary path the researcher called out as the stronger form). Safety notes: the copy's known-set covers all 10 live tmux rows so the boot's sweep kills nothing; the distinct port avoids the bind conflict with the real running server.
  5. Snapshot AFTER with the identical SELECT → /tmp/opencode/rehearsal-after.txt; `diff /tmp/opencode/rehearsal-before.txt /tmp/opencode/rehearsal-after.txt` — MUST be empty.
  6. Assert on the copy: max version_id in the goose version table == 18; `SELECT COUNT(*) FROM global_task` == 1; its agent_id equals the install's `SELECT id FROM agents WHERE is_default = 1`; task count unchanged from step 2; `PRAGMA foreign_keys` reads 1 on a fresh connection. Assertion form is load-bearing: the sqlite3 CLI exits 0 for ANY successfully-executed query regardless of rows returned — a bare `HAVING`/`WHERE`-filtered query yields zero rows and still exits 0, so the gate cannot fail. Always assert on captured output equality (`[ "$(sqlite3 ...)" = "expected" ]`), exactly as the automated verify below does; run the task-count and pragma checks in the same output-equality form when recording them for the SUMMARY.
  7. Record the before/after diff result, the assertions, and the boot log's migration/backfill lines in the SUMMARY; clean up /tmp/opencode artifacts.

  If ~/.kamacu/kamacu.db is absent on the executing host, fall back to a seeded staging DB (the Task-2-of-13-01 shape at volume) and say so explicitly in the SUMMARY — weaker but sufficient (research Environment Availability fallback).
  </action>
  <verify>
    <automated>diff /tmp/opencode/rehearsal-before.txt /tmp/opencode/rehearsal-after.txt && [ "$(sqlite3 /tmp/opencode/rehearsal-v113.db 'SELECT COUNT(*) FROM global_task')" = "1" ] && [ "$(sqlite3 /tmp/opencode/rehearsal-v113.db 'SELECT agent_id FROM global_task WHERE id = 1')" = "$(sqlite3 /tmp/opencode/rehearsal-v113.db 'SELECT id FROM agents WHERE is_default = 1')" ] && [ "$(sqlite3 /tmp/opencode/rehearsal-v113.db 'SELECT MAX(version_id) FROM goose_db_version')" = "18" ]</automated>
  </verify>
  <acceptance_criteria>
  - The before/after tmux survival diff is EMPTY (byte-for-byte — exact strings, not counts)
  - The goose version table reports 18 after the boot
  - global_task has exactly one row whose agent_id matches the install's is_default agent
  - Task count identical before/after; no migration or backfill errors in the rehearsal boot log
  - The copy was made via the .backup online-backup API, never a filesystem copy of the live DB
  </acceptance_criteria>
  <done>ROADMAP SC1 closed on real data: a seeded real-install copy upgrades cleanly through 00017+00018 with everything byte-for-byte identical, the singleton seeded from the real default agent, and the full boot pipeline (migrations + backfill + sweep) green on a real database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Startup sweep ← live tmux server | The sweep holds kill power over live user shells on the dedicated kamacu socket; a wrong known-set is a denial-of-service on user sessions |
| Real install DB ← rehearsal binary | The rehearsal boots the real binary against a COPY; the copy path and port choice must never touch the live install or its port |
| agents delete handler ← API clients | The new COUNT guard is the friendly gate ahead of the FK RESTRICT backstop for any caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Denial of Service | sweepOrphanTmux vs live global tabs | high | mitigate | Scope-aware known-set query (verified against real data: fixed-known 11 = total vs INNER JOIN's 10) + host-gated kill/no-kill regression test (Task 2, RED proves the bug first); degrade-don't-break posture preserved — query failure still never kills blindly |
| T-13-06 | Tampering/Destruction | rehearsal against the live install | high | mitigate | sqlite3 .backup online-backup API only (never cp on a 4MB WAL — Pitfall 5); all rehearsal I/O against the copy; spare loopback port avoids the real server's bind; the real DB is only ever read |
| T-13-07 | Tampering | agents delete path vs global_task reference | low | mitigate | Handler COUNT 409 with the D-09 message ahead of the 00017 ON DELETE RESTRICT backstop (the projects.agent_id pattern); TestAgentDeleteInUseByGlobal proves both the block and the unblock |
| T-13-08 | Tampering | singleton dropped between boots | medium | mitigate | BackfillGlobalTask re-arms id=1 every boot, seeded from is_default (never hardcoded); error refuses boot so a broken backfill is loud, not silent |
| T-13-SC | Tampering | package installs | low | accept | Zero installs this phase — locked decision; go.mod/web/package.json untouched (13-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./internal/api/ -run 'TestBackfillGlobalTask|TestAgentDeleteInUseByGlobal' -v` — both PASS
- `go test ./cmd/kamacu/ -run TestSweepOrphanTmuxScopeAware -v` — PASSING on this host (tmux 3.4 present), skip-clean elsewhere
- `go test ./...` — full repo suite green (no regression to sessions/reaper/agent surfaces)
- Real-install rehearsal gates (Task 3): empty before/after diff, goose version 18, singleton seeded from the default agent, task count unchanged
- Diff-scope guard: the combined phase diff touches exactly the files listed in this plan's and 13-01's files_modified — the session engine and reaper are untouched (13-RESEARCH Pitfall 7), and no existing migration file changed
</verification>

<success_criteria>
- GDATA-01 runtime halves closed: boot backfill re-arms the singleton (SC2 boot half), the agent-delete guard 409s with the locked D-09 string ahead of the FK backstop (SC2 FK half)
- GDATA-03 closed: the sweep fix ships in the same phase as the migration (co-phasing mandate), regression test proves no-kill on a live global tab AND unchanged orphan killing (SC4)
- GDATA-02 real-data half closed: the real-install rehearsal diffs byte-for-byte (SC1)
- The phase's Go diff is exactly backfill + wiring + delete-guard COUNT + sweep query — zero spawn-path, zero API-route, zero frontend changes
</success_criteria>

## Artifacts this phase produces (Plan 13-02)

New Go functions: `api.BackfillGlobalTask(db *sql.DB) error` (internal/api/global_backfill.go).
New Go test functions: `TestBackfillGlobalTask` (internal/api/global_backfill_test.go), `TestAgentDeleteInUseByGlobal` (internal/api/agents_crud_test.go), `TestSweepOrphanTmuxScopeAware` + its local unexported helpers (per-test-socket client builder, detached-session starter) in cmd/kamacu/sweep_test.go.
Modified code points: cmd/kamacu/serve.go (new BackfillGlobalTask wiring block after BackfillOpenCodeAgent; sweepOrphanTmux known-set query replaced with the scope-aware form), internal/api/agents_crud.go (global_task COUNT guard inside the delete handler).
New user-facing string (the phase's one shipped UI string per UI-SPEC Copywriting row 1): the 409 body `reassign the Scratchpad agent first`.
No new API routes, no new CLI flags, no frontend files, no new dependencies, no new DB objects (this plan consumes 13-01's schema).

<output>
Create `.planning/phases/13-global-data-foundation-safety-net/13-02-SUMMARY.md` when done
</output>
