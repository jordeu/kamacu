---
phase: 16-global-view-settings-bar-integration
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - web/src/components/settings/ScratchpadSection.tsx
  - web/src/pages/SettingsPage.tsx
autonomous: true
requirements: [GCONF-05]
must_haves:
  truths:
    - 'SettingsPage renders <ScratchpadSection /> inside the max-w-[640px] settings column (sibling of AgentsSection, placed directly after it) — the section heading reads Scratchpad in the standing uppercase-muted h2 idiom'
    - 'The summary card row 3 carries a primary (default-variant) Open Scratchpad button navigating to /global — the GCONF-05 idle-reachability affordance, the section single primary CTA'
    - 'Root row is server-truth-driven: folder root renders root_path mono/truncate/title-attributed; managed clone renders owner/name mono + a secondary Badge reading managed; unconfigured renders No root configured yet.'
    - 'Default-agent row: a Select listing useAgents() entries ({a.name} + " (default)" suffix for defaults), controlled by the cached agent_id from the shared ["global"] query, instant-saving PUT {agent_id} on change with help Applies at the next Start. (D-24/D-46) — on failure the control snaps back to the saved value and an inline Couldn'"'"'t save. Try again. shows'
    - 'The Change-root dialog mirrors AddProjectDialog verbatim (D-43): segmented GitHub repo | Local folder Tabs rendered only when github_integration is on (repo default; folder-only otherwise), mono inputs with exactly-one-of help/error, blocking Cloning {owner/name}… Loader2 spinner, reset-on-open via the adjust-during-render prevOpen tracker — NOT a setState-in-effect'
    - 'Every failure (clone failure, 400, 409, 5xx) leaves the dialog OPEN with field values and active mode preserved; only a 2xx closes (degrade-don'"'"'t-break; no half-configured state)'
    - 'A blocked change/clear (409) renders the body verbatim in the open dialog: sentenceCase(error) as the lead sentence plus the reasons targets as a mono-span list — NO client re-wording (D-45; the reasons ride ApiError.reasons from 16-01)'
    - 'A Clear root destructive button renders only when a root is configured; it sends PUT {root_path: ""} behind the same 409 gate with NO nested confirmation dialog (D-44)'
  artifacts:
    - 'web/src/components/settings/ScratchpadSection.tsx — NEW: section wrapper + summary card + agent selector + Open Scratchpad CTA + Change-root dialog'
  key_links:
    - 'Shared ["global"] query (16-01 useGlobal) drives the card rows AND the dialog; useSaveGlobal writes the PUT response back into the cache (D-17/D-23) — no local mirrors of server truth'
    - 'ApiError.reasons (16-01) feeds the dialog 409 list; integrationOn from useSettings github_integration gates the segmented toggle'
    - 'Open Scratchpad + the 16-02 /global route close the Settings↔view loop both directions (D-39 CTA)'
  prohibitions:
    - statement: 'No new packages and NO shadcn adds — the summary card is a composed rounded-lg border bg-card div precisely because no card primitive exists in web/src/components/ui'
      status: resolved
      verification: 'git diff on web/package.json + components.json is empty; npx shadcn add never runs'
    - statement: 'No client-side re-wording of any server message — 400/409/clone messages render verbatim (sentenceCase lead only); no client root validation beyond empty-field submit disable (the shipped backend owns validateRepoPath, D-07/D-27 footguns, and the 409 gate)'
      status: resolved
      verification: 'the catch block maps ApiError to its message/reasons fields verbatim; generic fallback only for non-ApiError'
    - statement: 'No hash/anchor routing to the section — the app has none; the /global degraded-state CTAs land on plain /settings (UI-SPEC Surface 2)'
      status: resolved
      verification: 'grep -ci "#" ScratchpadSection.tsx href patterns returns 0; navigation uses navigate("/global") only'
---

<objective>
The Settings Scratchpad section (GCONF-05, D-43..D-46): a summary card (root display + default-agent selector + Open Scratchpad affordance) plus the AddProjectDialog-derived Change-root dialog — folder/repo capture, blocking clone feedback, verbatim 409-reasons surfacing, and the gated Clear-root action.

Purpose: Make the global scratchpad configurable and reachable when idle — the Settings half of the D-39 CTA loop, consuming the Phase-14 PUT grammar through the 16-01 client (useGlobal/useSaveGlobal/ApiError.reasons). Zero new packages, zero shadcn adds.
Output: web/src/components/settings/ScratchpadSection.tsx (new), one-line mount in SettingsPage.tsx.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-global-view-settings-bar-integration/16-CONTEXT.md
@.planning/phases/16-global-view-settings-bar-integration/16-RESEARCH.md
@.planning/phases/16-global-view-settings-bar-integration/16-PATTERNS.md
@.planning/phases/16-global-view-settings-bar-integration/16-UI-SPEC.md
@.planning/phases/16-global-view-settings-bar-integration/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScratchpadSection summary card — root row, agent selector, Open Scratchpad CTA + SettingsPage mount</name>
  <files>web/src/components/settings/ScratchpadSection.tsx, web/src/pages/SettingsPage.tsx</files>
  <read_first>
  - web/src/pages/SettingsPage.tsx:128-218 (the column composition you join — section wrapper idiom, GithubSection instant-save posture :57-63, error/retry idiom)
  - web/src/components/sidebar/ProjectSettingsDialog.tsx:232-250 (the Select idiom D-46 copies)
  - web/src/api/queries.ts:28 (useAgents — the agent list for the selector)
  - web/src/api/global.ts (16-01: useGlobal/useSaveGlobal/GlobalConfig)
  - web/src/components/ui/select.tsx + badge.tsx (the primitives composed — no adds)
  - 16-UI-SPEC.md Surface 3 (summary card frame + rows) + Copywriting rows 2/3/27/28/29
  </read_first>
  <action>
  NEW web/src/components/settings/ScratchpadSection.tsx exporting ScratchpadSection:
  - Section wrapper: section flex flex-col gap-3 with h2 text-xs font-medium tracking-wide text-muted-foreground uppercase reading Scratchpad (standing idiom, 13-UI-SPEC row 4).
  - Summary card: a composed div rounded-lg border bg-card p-4 (NOT a shadcn add — no card primitive exists) hosting three rows in a flex flex-col gap-4:
    - Row 1 Root (label text-xs font-medium): driven by useGlobal data — folder root (github_repo null, root_path non-empty) renders root_path in a mono, truncating span with a title attribute carrying the full path; managed clone renders the owner/name ref in mono plus a secondary Badge reading managed (the non-null github_repo pointer IS the marker — never a boolean); unconfigured (root_path empty) renders the muted line No root configured yet.
    - Row 2 Default agent (D-46): label Default agent + the ProjectSettingsDialog Select idiom listing useAgents() entries as {a.name} plus " (default)" for defaults. CONTROLLED by the cached agent_id (value from useGlobal data) — NOT local state; onValueChange fires useSaveGlobal mutate with {agent_id: Number(value)} carrying an onError that sets an inline Couldn'"'"'t save. Try again. (text-xs text-destructive); the untouched cache snaps the control back on failure automatically (Pitfall 10). Help line under the Select: Applies at the next Start. (D-24 hint — agent changes are never 409-gated).
    - Row 3 Actions (flex items-center gap-2): primary default-variant Button Open Scratchpad navigating to /global via useNavigate — the GCONF-05 affordance and the section single primary CTA; outline Button opening the Change-root dialog — label Change root… when a root is configured, Configure root… when not (dialog state opened here; the dialog component lands in Task 2 — this task wires the open state + button).
  SettingsPage.tsx: import ScratchpadSection and mount it in the max-w-[640px] flex flex-col gap-6 column directly after AgentsSection (placement adjustable within the column — research A1).
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'Open Scratchpad' web/src/components/settings/ScratchpadSection.tsx && grep -n 'Applies at the next Start' web/src/components/settings/ScratchpadSection.tsx && grep -n 'No root configured yet' web/src/components/settings/ScratchpadSection.tsx && grep -n 'ScratchpadSection' web/src/pages/SettingsPage.tsx && cd web && npx eslint src/components/settings/ScratchpadSection.tsx</automated>
  </verify>
  <acceptance_criteria>
  - ScratchpadSection.tsx exists with the section heading Scratchpad, the three rows, and both action buttons (grep hits for Open Scratchpad, Applies at the next Start., No root configured yet.)
  - The agent Select value derives from the ["global"] query cache (agent_id), never a local state mirror; the mutate carries the onError inline error
  - The managed branch renders owner/name + Badge managed; the folder branch truncates with a title attribute
  - SettingsPage.tsx mounts ScratchpadSection (import + JSX, 2 grep hits); no other SettingsPage edits
  - npx eslint on the new file clean; build green
  </acceptance_criteria>
  <done>The Settings column hosts the Scratchpad section with server-truth-driven summary rows, instant-save agent selection, and the GCONF-05 reachability CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Change-root dialog — segmented capture, clone feedback, verbatim 409 reasons, gated Clear root</name>
  <files>web/src/components/settings/ScratchpadSection.tsx</files>
  <read_first>
  - web/src/components/sidebar/AddProjectDialog.tsx (whole file — THE pattern source: sentenceCase helper :27-29, integrationOff degradation :45/:67, prevOpen reset-on-open tracker :73-95, mono inputs + exactly-one-of help/error :181-206, catch posture :131-147, blocking Cloning spinner :250-259, Tabs :166-179)
  - web/src/components/settings/ScratchpadSection.tsx (your Task 1 output — the dialog mounts inside the section)
  - web/src/api/client.ts (16-01: ApiError.reasons — the structured list you render)
  - internal/api/global.go:292-320 (PUT pointer grammar — root_path "" is THE clear; repo is the managed dispatch field) and :338-346 (the 409 body: error lead sentence + reasons array of {kind, target})
  - 16-RESEARCH.md Pitfall 1 (reasons mechanics), Pitfall 8 (prevOpen tracker over setState-in-effect), Pitfall 10 (PUT response handling)
  </read_first>
  <action>
  Add the Change-root dialog inside ScratchpadSection, derived from AddProjectDialog with the locked deltas (D-43/D-44/D-45; UI-SPEC Surface 3 table):
  - Dialog frame: Dialog + DialogContent sm:max-w-[480px] aria-describedby={undefined}, DialogTitle Scratchpad root.
  - Segmented toggle: Tabs with TabsList aria-label Scratchpad root source (w-full), triggers GitHub repo | Local folder — rendered ONLY when the GitHub integration is on (settings github_integration value === "on", the AddProjectDialog gate); repo mode default when on; folder-only when off. Triggers disable while pending.
  - Fields: repo mode — Label Repository + mono Input placeholder owner/name; folder mode — Label Repository path + mono Input placeholder /home/you/code/my-repo. Exactly one of {help, error} renders below the active input (AddProjectDialog idiom: help muted text-xs; error in the rounded-md border-destructive/50 bg-destructive/10 box). NO Name field or prefill machinery — the scratchpad has no name.
  - Reset-on-open: the prevOpen adjust-during-render tracker (mode/fields/error reset each time the dialog opens) — NOT a setState-in-effect (repo lint-debt class; Pitfall 8).
  - Submit Save root (disabled while the active field is empty or pending): repo mode fires useSaveGlobal with {repo: trimmed owner/name}; folder mode with {root_path: trimmed path}. While the repo PUT is pending, render the blocking spinner line: Loader2 spin + Cloning {owner/name}… (mono ref, AddProjectDialog idiom). On 2xx the dialog closes (the mutation onSuccess already wrote the GET-shaped response into the ["global"] cache — D-23; no refetch).
  - Clear root (D-44): a destructive-styled button INSIDE the dialog (left of Cancel/Save; exact placement your call), rendered ONLY when a root is currently configured. Fires useSaveGlobal with {root_path: ""} — the one clear spelling — behind the same 409 gate. NO nested confirmation dialog: the explicit button is the intent, the gate is the guard.
  - Failure posture (D-43/D-45, degrade-don't-break): EVERY failure — clone failure, 400, 409, network/5xx — keeps the dialog OPEN with field values and active mode preserved; only a 2xx closes. In the catch: ApiError → set the error box to sentenceCase(err.message) (copy the sentenceCase helper) and, when err.reasons is present, append a list-disc ul of text-xs items rendering each reasons target in a mono span — wire strings verbatim, NO client re-wording; non-ApiError → Couldn'"'"'t save. Try again. The 400 ~/.kamacu footgun message surfaces through the same sentenceCase path verbatim (D-27).
  All wire-supplied strings render as JSX text/span children (React auto-escaping); never through a raw-HTML injection API.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'Clear root' web/src/components/settings/ScratchpadSection.tsx && grep -n 'reasons' web/src/components/settings/ScratchpadSection.tsx && grep -n 'prevOpen' web/src/components/settings/ScratchpadSection.tsx && grep -n 'Cloning' web/src/components/settings/ScratchpadSection.tsx && grep -n 'sentenceCase' web/src/components/settings/ScratchpadSection.tsx && test "$(grep -c 'dangerouslySetInnerHTML' web/src/components/settings/ScratchpadSection.tsx)" = "0" && cd web && npx eslint src/components/settings/ScratchpadSection.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Dialog title Scratchpad root, submit Save root, segmented triggers GitHub repo | Local folder gated on the github_integration setting; repo default when on
  - prevOpen tracker present (no useEffect-based reset); blocking Cloning {ref}… spinner during repo PUT
  - Clear root button present, rendered only when configured, sends the empty-string root_path PUT; no second/nested confirm dialog in the file
  - The catch block renders sentenceCase(err.message) and iterates err.reasons targets as mono list items — verbatim, no re-wording branch
  - The raw-HTML injection API literal appears 0 times; npx eslint clean; build green
  - web/package.json and web/components.json unchanged (zero adds)
  </acceptance_criteria>
  <done>Root and default agent are fully configurable from Settings with inline, verbatim, non-destructive error surfacing — GCONF-05 complete, closing the D-39 Settings↔/global loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localhost API | Existing boundary; the dialog PUTs through the shipped Phase-14 handler which owns validation, cloning, and gating |
| wire → DOM | Server messages (error, reasons[].target, paths, owner/name refs) rendered inside the dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | Server strings (error messages, reasons targets, root paths) rendered into DOM | medium | mitigate | JSX text nodes + mono spans (React auto-escaping); grep gate proves the raw-HTML injection API appears 0 times in the new file |
| T-16-02 | Tampering | Client-side root validation bypass (dialog is the only write surface this phase adds) | low | accept | Server owns every check (validateRepoPath, ParseRepoRef, D-07/D-27 footgun blocks, 409 gate — shipped Phase 14); the dialog disables submit only on empty input and renders server messages verbatim |
| T-16-03 | Tampering | Clone-failure half-configured state | low | mitigate | Shipped server atomicity (SC2, Phase 14) + the dialog failure posture: every failure keeps the dialog open with values preserved; only a 2xx closes |
| T-16-SC | Tampering | package installs | n/a | accept | Zero-install phase (16-RESEARCH audit: none; the composed card div exists precisely to avoid an add) |
</threat_model>

<verification>
- npm --prefix web run build green; npx eslint clean on ScratchpadSection.tsx in isolation
- All grep gates above pass (CTA copy, tracker, reasons rendering, Clear root, zero raw-HTML injection, zero package/registry drift)
- End-of-phase manual UAT (drives GCONF-05): Settings shows the Scratchpad section; Open Scratchpad reaches /global; agent selector instant-saves and shows the hint; Change root… with a folder path configures; a repo ref shows the blocking Cloning spinner then the managed badge; with a live global session (curl-spawned), Save root/Clear root surface the 409 lead sentence + reasons list with the dialog still open; Clear root with everything stopped clears to No root configured yet.
</verification>

<success_criteria>
- GCONF-05: the Open Scratchpad affordance makes /global reachable when idle; the section configures root + default agent per D-43..D-46
- D-45 verbatim 409 surfacing works end-to-end via ApiError.reasons (16-01 dependency exercised)
- Zero new packages, zero shadcn adds, no TaskPage/view-file overlap (parallel-safe with 16-02)
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / File | Kind |
|---------------|------|
| web/src/components/settings/ScratchpadSection.tsx | NEW file — export ScratchpadSection (summary card + Change-root dialog + selector + CTA) |
| sentenceCase (module-local helper) | private function, ScratchpadSection.tsx (copied from AddProjectDialog.tsx:27-29) |
| ScratchpadSection mount | the one SettingsPage.tsx edit (import + JSX sibling of AgentsSection) |

<output>
Create `.planning/phases/16-global-view-settings-bar-integration/16-03-SUMMARY.md` when done
</output>
