---
phase: 17-hardening-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/global_noleak_test.go
  - internal/api/global_interlock_test.go
  - internal/mcp/global_parity_test.go
autonomous: true
requirements: [GINT-02, GINT-03]
must_haves:
  truths:
    - "With a configured global root AND live global sessions (a fake-claude agent plus a tmux bash tab), GET /api/tasks and the board fetch GET /api/projects/{id}/tasks contain no global-titled row, the manual peer task still moves (200) with positions unaffected, and GET /api/projects returns the same count as before the global was configured (D-56 boards/listings surfaces)"
    - "DB structural proof: the tasks and projects tables gained ZERO rows after configuring + spawning the global (exclusion by construction, not filtering — the singleton design of research P2 / Phase-13 invariants)"
    - "An empty workspace DELETE returns 200 while global sessions are live, and a workspace holding a project is refused 409 with the 'move or remove its N project(s) first' message (the workspaces.go COUNT guard never counts the global)"
    - "GET /api/activity raw body contains the manual fixture row and zero occurrences of the strings 'Scratchpad' and 'Global' (GINT-03 / D-56, the TestGlobalActivityExclusion posture re-homed)"
    - "MCP leak direction (D-57): list_tasks through the bridge surfaces no global entity; MCP honest direction (GINT-02): list_sessions surfaces the global session row with taskTitle 'Scratchpad' / projectName 'Global' labels intact through the orphan-filter passthrough, and get_session of the global id returns 200, never 404"
    - "Interlock direction 1 (D-58): with a managed project clone at <HOME>/.kamacu/repos/octo/widgets AND a managed global root at <HOME>/.kamacu/repos/global/octo/widgets of the SAME repo ref (allowed by construction, 13-CTX D-03), DELETE /api/projects/{id} returns 204, removes ONLY the project clone directory, leaves the global root directory on disk, and GET /api/global still reports the root configured"
    - "Interlock direction 2 (D-58): PUT /api/global with root_path empty-string clears the global row while the project clone directory and the project row remain intact; the folder-root/folder-project direction is asserted cheaply with temp dirs (neither delete touches the other)"
    - "The completed audit table (surface → query/guard file:line → assertion, one row per enumerated surface, at least 8 rows) is recorded in the plan SUMMARY so the phase verifier carries it into 17-VERIFICATION.md (D-55)"
  artifacts:
    - internal/api/global_noleak_test.go
    - internal/api/global_interlock_test.go
    - internal/mcp/global_parity_test.go
  key_links:
    - "Every board/position query's source='manual' guard (tasks.go:194/:233/:288/:432/:538/:554) ⇄ zero global rows on boards — the tests prove each guard stays manual-keyed"
    - "workspaces.go:215 SELECT COUNT(*) FROM projects WHERE workspace_id=? ⇄ the global singleton is not a project row, so it can never block a workspace delete"
    - "activity.go:135 t.source='manual' filter ⇄ global sessions never appear in Activity tasks[]/stats (GINT-03)"
    - "Managed namespace split: project clones at <HOME>/.kamacu/repos/<owner>/<name> vs the global root at <HOME>/.kamacu/repos/global/<owner>/<name> (13-CTX D-01/D-02) ⇄ the interlock's two compared directories"
    - "mcp bridge list_tasks → GET /api/tasks passthrough (mcp/tasks.go:182-198) and list_sessions/get_session → /api/sessions[/{id}] passthrough (mcp/sessions.go:496/:571) ⇄ the D-57 both-direction assertions"
---

<objective>
Prove the sentinel-leak invariant across EVERY enumeration surface (D-55/D-56 / GINT-03 / Phase-13 architecture), the managed-root ↔ project-delete interlock in both directions (D-58 / SC3), and the MCP both-direction parity spot-check (D-57 / GINT-02) — as permanent, deterministic, in-package regression tests plus the documented audit table.

Purpose: the global entity's exclusion is by construction (a singleton row, never a task/project row) — this plan makes each surface's exclusion observable and re-provable, and gives future surfaces a pattern to copy (one audit-table row + one subtest).

Output: three new test files (consolidated TestGlobalNoLeak* family, the interlock test with filesystem assertions, the MCP bridge parity tests) and the surface→guard→assertion audit table recorded in the SUMMARY.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hardening-e2e/17-CONTEXT.md
@.planning/phases/17-hardening-e2e/17-RESEARCH.md
@.planning/phases/17-hardening-e2e/17-PATTERNS.md
@.planning/phases/13-global-data-foundation-safety-net/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated TestGlobalNoLeak* family across every enumeration surface (D-55/D-56)</name>
  <files>internal/api/global_noleak_test.go</files>
  <read_first>
  - .planning/phases/17-hardening-e2e/17-PATTERNS.md (noleak section — the enumerated-surfaces guard table with verified file:line values, the harness shapes, the leak-assertion posture)
  - internal/api/sessions_global_test.go (TestGlobalActivityExclusion near lines 1444-1486 — the posture being generalized; the global harness with fake-claude + tmux near lines 26-106 and 700-836)
  - internal/api/global_test.go (newGlobalTestServer / newGlobalSessionServerWithTmux harness shapes near lines 27-66 — Routes + SessionRoutes + GlobalRoutes over one DB+Manager)
  - internal/api/tasks.go (the source='manual' guards at lines 194, 233, 288, 432, 538, 554)
  - internal/api/activity.go (the manual filter near line 135)
  - internal/api/workspaces.go (the non-empty COUNT guard near line 215)
  </read_first>
  <action>
  Create internal/api/global_noleak_test.go (package api) — ONE consolidated TestGlobalNoLeak* family walking every surface with a configured + LIVE global session, so SC4's enumeration is legible in one place and trivially extensible (D-56).

  Setup: reuse the existing in-package harness (the newGlobalSessionServerWithTmux shape — Routes + SessionRoutes + GlobalRoutes over one DB + Manager, HOME sandboxed via t.Setenv). Configure a folder root (temp git repo), spawn a LIVE global agent (fake-claude via the existing SetAgentConfig convention) AND a global bash tmux tab, so the leak probes run against real live state. Seed a manual peer fixture (one project + one manual task) so each surface has its one legitimate row to positively assert against.

  Subtests — one per surface, each asserting the surface's own legitimate row IS present and zero global presence:
  - Unscoped task list: GET /api/tasks contains the manual task and no global-titled row; the count equals the pre-global-configure count.
  - Board fetch: GET /api/projects/{id}/tasks for the peer project — same assertions.
  - Move/position arithmetic: move the manual task (the existing move endpoint) → 200; positions unaffected by global presence (the 5 guarded queries at tasks.go:194/:233/:288/:432/:538/:554 stay manual-keyed).
  - Project list: GET /api/projects — no sentinel project, count unchanged after configuring + spawning global.
  - Workspace delete guard: DELETE an EMPTY workspace → 200 while the global is live (the COUNT guard never counts it); negative control — DELETE a workspace holding the peer project → 409 with the 'move or remove its' message so the guard is proven actually working.
  - Activity stats/lists: GET /api/activity?window=week raw body contains the peer fixture's title and zero occurrences of the strings 'Scratchpad' and 'Global' (assert on RAW bytes, the Phase-11 lesson: decoded len==0 checks cannot catch shape regressions).
  - DB structural proof: SELECT COUNT(*) FROM tasks and FROM projects directly against the harness DB — both unchanged by configure+spawn (exclusion by CONSTRUCTION, mirroring the existing 'tasks rows = 0' global-spawn assertion).
  - MCP surfaces live in plan 17-02 Task 3's file, not here.

  Audit table (D-55): while writing each subtest, verify the guard's current file:line (the PATTERNS table was verified 2026-08-28 — record actual values if anything drifted) and collect one row per surface: surface → query/guard file:line → assertion. Record the completed table (at least 8 rows) in the plan SUMMARY so the phase verifier carries it into 17-VERIFICATION.md.
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestGlobalNoLeak' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
  - internal/api/global_noleak_test.go exists and go test ./internal/api -run 'TestGlobalNoLeak' -count=1 exits 0
  - The family runs with a LIVE global agent AND a live global tmux tab configured (not just a configured row) — grep finds both a kind agent and a kind bash global spawn in the setup
  - The Activity assertion operates on raw response bytes and negative-asserts the two leak strings 'Scratchpad' and 'Global'
  - The workspace subtest asserts BOTH the empty-workspace 200 (with global live) and the holding-project 409
  - A direct SQL COUNT subtest proves tasks/projects tables gained zero rows
  - The plan SUMMARY contains the audit table with at least 8 rows in the surface → guard → assertion shape
  </acceptance_criteria>
  <done>
  Every enumerated surface (boards/positions, task+project listings, workspace guard, Activity) has a green leak assertion against live global state, plus the structural DB proof; the audit table is recorded for 17-VERIFICATION.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Managed-root ↔ project-delete interlock, both directions (D-58)</name>
  <files>internal/api/global_interlock_test.go</files>
  <read_first>
  - .planning/phases/17-hardening-e2e/17-PATTERNS.md (interlock section — the seams block, fakeGitClone, the namespace helper, the direction-by-direction skeleton)
  - internal/api/global_test.go (the github.Set*ForTest seams + fakeGitClone + globalManagedDest near lines 353-425; TestPutGlobalClear semantics near lines 242-265)
  - internal/api/projects.go (removeManaged — the gated delete reaching os.RemoveAll on the project clone only, near lines 888-917; DELETE returns 204)
  - .planning/phases/17-hardening-e2e/17-CONTEXT.md (carry-forward: namespaces separated by construction, same repo allowed on both sides, no new guards — the interlock PROVES the design, 13-CTX D-01..D-03)
  </read_first>
  <action>
  Create internal/api/global_interlock_test.go (package api). Deterministic, offline, in-package — no network, no real gh (the seams), no UAT dependency (D-58).

  Setup with the Phase-14 seams: github.SetCloneRunnerForTest wrapping fakeGitClone (real directories with git init + origin remote), github.SetValidateRunnerForTest returning the canonical ref as valid, github.SetAvailableForTest(true) — all with deferred restores. HOME sandboxed via t.Setenv so the .kamacu/repos namespace lands in a temp dir.

  The SAME-ref fixture (namespace separation is the point, not repo identity — 13-CTX D-03):
  - Project side: POST /api/projects {"repo":"Octo/Widgets"} → clone lands at <HOME>/.kamacu/repos/octo/widgets.
  - Global side: PUT /api/global {"repo":"Octo/Widgets"} → clone lands at <HOME>/.kamacu/repos/global/octo/widgets (same canonical ref, both succeed).

  Direction 1 (project delete never touches the global root): DELETE /api/projects/{id} → 204 (no tasks → no worktrees → gates clear). Assert via os.Stat: the project clone directory is GONE, the global root directory STILL EXISTS, and GET /api/global still reports the root configured.

  Direction 2 (global clear never touches project clones): PUT /api/global {"root_path":""} → 200 (the D-21 clear). Assert: the global row is cleared (GET /api/global shows no root), the project clone directory STILL EXISTS, and the project row is intact (GET /api/projects still lists it).

  Folder-direction cheap assert: a temp-dir folder global root + an unrelated folder project; delete the project; os.Stat both — neither touches the other.
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestGlobalInterlock' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
  - internal/api/global_interlock_test.go exists and go test ./internal/api -run 'TestGlobalInterlock' -count=1 exits 0
  - The test installs all three github.Set*ForTest seams with deferred restores (grep finds SetCloneRunnerForTest, SetValidateRunnerForTest, SetAvailableForTest)
  - The SAME canonical ref (e.g. Octo/Widgets) is used for both the project and the global root
  - Direction 1 asserts 204 on project delete + os.Stat: project clone gone, global root present, global still configured
  - Direction 2 asserts 200 on the clear + os.Stat: project clone present, project row listed, global row cleared
  - The folder/folder direction is asserted with temp dirs and os.Stat
  </acceptance_criteria>
  <done>
  The namespace split is proven in both directions with deterministic filesystem assertions: no cross-entity delete is possible because no cross-entity path exists — exactly the interlock SC3 describes, without adding any new guard.
  </done>
</task>

<task type="auto">
  <name>Task 3: MCP both-direction parity through the real bridge (D-57 / GINT-02)</name>
  <files>internal/mcp/global_parity_test.go</files>
  <read_first>
  - .planning/phases/17-hardening-e2e/17-PATTERNS.md (mcp parity section — the bridge construction pattern and the result-unwrapping idiom)
  - internal/mcp/sessions_test.go (the house in-memory-transport pattern near lines 23-52: httptest backend + bridge{base, token, client}; the CallToolResult unwrapping near lines 128-141 including the orphaned-row filter precedent)
  - internal/mcp/tasks.go (listTasks → b.call GET /api/tasks passthrough, lines 182-198)
  - internal/mcp/sessions.go (listSessions near line 496 with the orphan-filter passthrough; getSession near line 571)
  - internal/api/sessions_global_test.go (the exact JSON shape of a global session row — taskTitle 'Scratchpad', projectName 'Global', global:true, orphaned fields — used to seed the canned backend with real wire shapes)
  </read_first>
  <action>
  Create internal/mcp/global_parity_test.go (package mcp). Use the house pattern: an httptest backend whose canned responses are seeded with the REAL wire shape of global session rows (mirroring the actual /api/sessions handler output for a live global session — labels taskTitle 'Scratchpad' / projectName 'Global', the global and orphaned flags), driving the real bridge{base, token, client} and unwrapping CallToolResult TextContent per the existing test idiom.

  Leak direction (task tools NEVER surface anything global): backend serves the manual-tasks array exactly as the real GET /api/tasks returns it while a global session is live (manual rows only — the API-level exclusion is proven in plan 17-02 Task 1; this leg locks the BRIDGE contract). Call b.listTasks → assert the result contains the manual task identifiers and zero occurrences of the strings 'Scratchpad' and 'Global'.

  Honest direction (session tools describe the global session, never 404 / never garbled — GINT-02):
  - Backend serves a sessions array containing the global session row (global:true, orphaned:false, the honest labels). Call b.listSessions → assert the global session IS surfaced (the orphan-filter passthrough does not drop or mangle it) with taskTitle 'Scratchpad' and projectName 'Global' intact.
  - Backend serves the single global session for GET /api/sessions/{id}. Call b.getSession with the global id → assert a 200-shaped passthrough result carrying the labels — the bridge must surface it, never 404.
  - Include the orphaned variant (orphaned:true, global:true — the post-restart shape from plan 17-01) in the list fixture to prove the filter degrades to passthrough on global rows rather than dropping them.
  </action>
  <verify>
    <automated>go test ./internal/mcp -run 'TestGlobalParity' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
  - internal/mcp/global_parity_test.go exists and go test ./internal/mcp -run 'TestGlobalParity' -count=1 exits 0
  - The tests drive the real bridge struct (not HTTP clients against a fake MCP server) — grep finds the bridge construction with base/token/client fields
  - The leak-direction assertion negative-checks 'Scratchpad' and 'Global' in the list_tasks result text
  - The honest-direction assertions positive-check taskTitle 'Scratchpad' and projectName 'Global' in both list_sessions and get_session results
  - The list fixture includes the orphaned+global row variant and asserts it survives the passthrough
  </acceptance_criteria>
  <done>
  GINT-02's MCP half is closed at the bridge level: task tools are leak-free, session tools honestly label the task-less global session in both live and orphaned shapes, and the read-only contract is untouched (no write-path calls in the test).
  </done>
</task>

</tasks>

## Artifacts this phase produces (plan 02)

- New file: internal/api/global_noleak_test.go — the consolidated TestGlobalNoLeak* family
- New file: internal/api/global_interlock_test.go — the both-direction interlock tests
- New file: internal/mcp/global_parity_test.go — the TestGlobalParity* bridge spot-checks
- New documentation artifact (in the plan SUMMARY): the D-55 audit table — surface → query/guard file:line → assertion — destined for 17-VERIFICATION.md
- No production code changes, no new exports, no new dependencies

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → sandboxed HOME | all namespace/filesystem assertions run under a t.Setenv HOME sandbox; nothing writes to the real ~/.kamacu |
| bridge → canned httptest backend | the MCP tests control the backend wire exactly; assertions are about the bridge's passthrough behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering (user data) | interlock/filesystem tests writing into the real ~/.kamacu/repos namespace | high | mitigate | HOME sandboxed via t.Setenv to a t.TempDir for every test that creates managed clones; all github flows run through the offline Set*ForTest seams (no network, no real gh) |
| T-17-06 | Information Disclosure | leak tests missing a surface would silently re-open the sentinel-leak invariant | medium | mitigate | the D-55 audit table enumerates every surface in one place (SUMMARY → 17-VERIFICATION.md); adding a future surface = one row + one subtest, reviewable in one diff |
| T-17-07 | Spoofing | MCP tests asserting against a fabricated wire shape that drifts from the real handler output | low | mitigate | the canned backend rows are seeded from the verified global-session wire shape (17-PATTERNS cites the exact field spellings); the API-level exclusion proofs live in plan 17-02 Task 1 against the real handlers |
</threat_model>

<verification>
- go test ./internal/api -run 'TestGlobalNoLeak|TestGlobalInterlock' -count=1 -v green
- go test ./internal/mcp -run 'TestGlobalParity' -count=1 -v green
- The audit table in the SUMMARY covers every surface named in ROADMAP success criterion 4 (boards, project/task listings, workspace guard, Activity, MCP task tools) — no surface silently dropped
- No test writes outside its HOME sandbox (no absolute paths outside t.TempDir in the interlock assertions)
</verification>

<success_criteria>
- ROADMAP success criteria 3 (interlock both directions) and 4 (sentinel-leak sweep, every surface enumerated and asserted) are green as permanent regression tests
- GINT-03's exclusion and GINT-02's honest-labels half are closed with bridge-level evidence
- The audit-table pattern exists so future enumeration surfaces have a copyable proof form
</success_criteria>

<output>
Create `.planning/phases/17-hardening-e2e/17-02-SUMMARY.md` when done — the SUMMARY MUST contain the D-55 audit table (surface → query/guard file:line → assertion, ≥8 rows) for the phase verifier to carry into 17-VERIFICATION.md
</output>
