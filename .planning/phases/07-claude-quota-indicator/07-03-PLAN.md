---
phase: 07-claude-quota-indicator
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [QUOTA-01, QUOTA-06, QUOTA-07]

must_haves:
  truths:
    - "The full stack builds and all tests pass with the quota feature integrated"
    - "A live GET /api/usage on this machine returns 200 with a coherent state and no token material in the body"
    - "No sk-ant-oat token material appears in server logs or API payloads (roadmap-mandated grep audit)"
    - "A human confirmed the trigger, popup rows, refresh, and red-zone color on the real UI in both headers"
  artifacts:
    - path: "internal/quota/quota_test.go"
      provides: "Six-state matrix proof (from 07-01) re-run green in integration"
  key_links:
    - from: "browser (BoardPage/TaskPage)"
      to: "GET /api/usage"
      via: "useQuota 60s poll through the single Go binary"
      pattern: "api/usage"
---

<objective>
Integrate and verify the whole quota feature: full-stack build, whole-repo tests, the roadmap-mandated token-leak grep audit, a live smoke of `GET /api/usage`, and a human visual pass over the trigger/popup states on the real UI.

Purpose: closes QUOTA-06 verification (six-state degradation matrix + leak audit) and the visual halves of QUOTA-01/QUOTA-07 that automation can't judge.
Output: verified phase; no new source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-claude-quota-indicator/07-CONTEXT.md
@.planning/phases/07-claude-quota-indicator/07-01-SUMMARY.md
@.planning/phases/07-claude-quota-indicator/07-02-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-stack build, test sweep, token-leak audit, live smoke</name>
  <files></files>
  <read_first>
    - .planning/phases/07-claude-quota-indicator/07-01-SUMMARY.md (what the backend plan actually shipped — field names, any deviations)
    - .planning/phases/07-claude-quota-indicator/07-02-SUMMARY.md (frontend deviations, if any)
    - cmd/kangent/main.go (confirm UsageRoutes wiring landed)
  </read_first>
  <action>
    Run, in order, treating any failure as a gap to fix before the checkpoint:

    1. Whole-repo regression sweep (this phase touches zero session/PTY code — everything must stay green):
       ```bash
       go vet ./... && go test ./... -count=1
       cd web && npm run build && cd ..
       ```

    2. Source-level token audit (roadmap phase note): 
       ```bash
       grep -rn "sk-ant-oat" internal/ cmd/ --include="*.go" | grep -v _test.go   # MUST be empty
       grep -rn "sk-ant-oat" web/src/                                             # MUST be empty
       grep -rEn "WriteFile|os\.Create|OpenFile" internal/quota/quota.go          # MUST be empty (read-only passenger)
       ```

    3. Live smoke — build the real binary (so the embedded SPA and UA probe both run), start it on a scratch port/db, probe, and tear down:
       ```bash
       cd web && npm run build && cd ..
       go build -o /tmp/kangent-verify ./cmd/kangent
       /tmp/kangent-verify --addr 127.0.0.1:7411 --db /tmp/kangent-verify.db > /tmp/kangent-verify.log 2>&1 &
       sleep 2
       curl -s http://127.0.0.1:7411/api/usage | tee /tmp/usage-payload.json
       curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:7411/api/usage   # expect 200
       kill %1
       ```
       Assert on /tmp/usage-payload.json: valid JSON; `state` is one of `ok|no_credentials|auth_expired|error`; when state is `ok`, `windows` contains an entry with `"key":"five_hour"` and `"label":"5h"`. (This machine has live credentials per research, so `ok` is expected — but `auth_expired` is acceptable if the ~8h token happens to be expired at run time; both prove the degradation contract.)

    4. Runtime token-leak audit (roadmap-mandated):
       ```bash
       grep -c "sk-ant-oat" /tmp/usage-payload.json /tmp/kangent-verify.log   # MUST print 0 for both
       ```

    5. Cleanup: `rm -f /tmp/kangent-verify /tmp/kangent-verify.db /tmp/usage-payload.json /tmp/kangent-verify.log`
  </action>
  <verify>
    <automated>go vet ./... && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` and `go test ./... -count=1` exit 0 (six-state matrix from 07-01 included)
    - `cd web && npm run build` exits 0
    - All three source-audit greps return empty
    - Live `GET /api/usage` returned HTTP 200 with a valid state value
    - Both runtime grep counts for "sk-ant-oat" are 0
  </acceptance_criteria>
  <done>Whole repo green, live endpoint smoke passed, and the token-leak audit (source + runtime payload + logs) is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — trigger, popup, refresh, degradation</name>
  <files></files>
  <read_first>
    - .planning/phases/07-claude-quota-indicator/07-CONTEXT.md (D-68..D-76 — the visual contract being checked)
  </read_first>
  <action>
    Present the completed feature to the user for visual verification. All automation is already done (Task 1); this checkpoint exists because trigger placement, popup layout, color thresholds, and hover behavior are visual judgments. Start the server for the user if not running, then walk them through the how-to-verify steps below and wait for approval.
  </action>
  <what-built>
    Compact "Claude 5h" quota trigger (thin threshold-colored bar: zinc &lt;60 / amber 60–84 / red ≥85, never green) in the top-right of both the board header and the task header, with a hover popup listing every quota window (bar, rounded %, "Resets in" countdown) and an "Updated Xm ago" + refresh footer. Backed by GET /api/usage with full six-state degradation (already unit-test-proven in 07-01).
  </what-built>
  <how-to-verify>
    Start the server (`go run ./cmd/kangent` or the built binary) and open http://127.0.0.1:7333 — or run dev mode (`go run ./cmd/kangent --dev-origin localhost:5173` + `cd web && npm run dev`).

    1. Board view: top-right shows "Claude 5h" with a thin bar beside the New task button. Bar color is muted zinc unless a window is ≥60% (amber) or ≥85% (red) — compare against the popup numbers in step 3.
    2. Open any task: the same indicator sits beside the three-dots menu in the task header.
    3. Hover the trigger: popup opens and STAYS open while your pointer moves into it. One row per window (expect 5h, 7d, and Sonnet/Opus rows as your account returns them), each with a colored bar, a rounded percentage, and "Resets in Xh Ym" (or "—" where the API gives no reset).
    4. Footer: "Updated Xm ago" left; click the refresh icon — it should briefly disable and the timestamp should reset to "Updated 0m ago".
    5. Red-zone rule (if any window shows ≥85%): the trigger bar is red even though its WIDTH tracks the 5h window. If no window is ≥85% today, confirm instead that the trigger color matches the HIGHEST percentage row in the popup, not necessarily the 5h row.
    6. Degradation spot-check (optional but valuable): temporarily rename ~/.claude/.credentials.json, wait for the next poll (≤60s) or click refresh ×3 — after 3 failures the trigger becomes a small amber-warning chip; popup explains the state. Restore the file; within a poll or two the indicator recovers on its own.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual checkpoint: human confirms steps 1–6; automated coverage of the same behaviors lives in internal/quota/quota_test.go and internal/api/usage_test.go (already green via Task 1)</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms steps 1–5 (and optionally 6) match the described behavior, or reports concrete deviations for gap closure
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User approved the visual pass, or reported issues that feed /gsd:plan-phase 7 --gaps.</done>
</task>

</tasks>

<verification>
- Whole-repo `go test ./... -count=1` green; web production build green
- Token-leak audit clean across source, runtime payload, and server logs
- Human approval of the six visual checks (QUOTA-01 placement, QUOTA-02 rows, QUOTA-03 footer/refresh, QUOTA-07 red-zone, D-71/72/73 degradation)
</verification>

<success_criteria>
- Phase 7 success criteria 1–5 (ROADMAP) all observable on the running app, with the D-69 color override (zinc, not green, below 60) in effect
- Zero regressions in session/PTY/board functionality (phase touches none of it; the sweep proves it)
</success_criteria>

<output>
After completion, create `.planning/phases/07-claude-quota-indicator/07-03-SUMMARY.md`
</output>
