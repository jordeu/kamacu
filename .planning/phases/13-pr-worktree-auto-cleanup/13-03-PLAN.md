---
phase: 13-pr-worktree-auto-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - web/src/pages/TaskPage.tsx
autonomous: false
requirements: [GHCLN-03, GHCLN-01, GHCLN-02]
must_haves:
  truths:
    - "A PR review view shows a ⋯ menu with a single 'Clean up worktree' item (no 'Delete task')"
    - "Selecting it opens the already-rendered CleanupWorktreeDialog (the same gated flow tasks use)"
    - "When the PR state is not OPEN, an inline banner reads 'This PR was merged/closed.' with a manual Clean up prompt while the worktree still exists"
    - "The banner lives inside the shrink-0 header block (no terminal-height regression) and is never a modal/blocking"
    - "Auto-cleanup of a merged clean worktree is observed end-to-end via the reaper (human-verify)"
    - "A dirty/busy merged PR worktree is left in place with its branch kept (human-verify)"
  artifacts:
    - path: "web/src/pages/TaskPage.tsx"
      provides: "PR-specific ⋯ menu (Clean up worktree only) + merged/closed banner"
      contains: "Clean up worktree"
  key_links:
    - from: "web/src/pages/TaskPage.tsx PR ⋯ menu item"
      to: "setCleanupOpen(true)"
      via: "onSelect opens the existing CleanupWorktreeDialog"
      pattern: "setCleanupOpen\\(true\\)"
    - from: "web/src/pages/TaskPage.tsx banner"
      to: "prDetail.state"
      via: "render when state !== 'OPEN'"
      pattern: "state !== \"OPEN\""
---

<objective>
Close GHCLN-03 (manual cleanup from the review view) and surface the merged/closed state (D-09): re-add a PR-specific `⋯` menu with a SINGLE "Clean up worktree" item (no "Delete task") that opens the already-rendered `CleanupWorktreeDialog` (D-08), and add an inline merged/closed banner in the review header block when `prDetail.state !== "OPEN"`. Then a `checkpoint:human-verify` validates all three phase success criteria end-to-end (auto-cleanup, safety, manual cleanup).

Purpose: the manual escape hatch for skipped (dirty/busy) leftovers and the discoverability path for merged/closed PRs. Pure reuse — the dialog, its `cleanupOpen` state, and `usePullRequestDetail` (now carrying `state` from 13-01) all already exist; only the menu entry and banner are added.
Output: the PR `⋯` menu + banner in TaskPage.tsx; a human-verified phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pr-worktree-auto-cleanup/13-CONTEXT.md
@.planning/phases/13-pr-worktree-auto-cleanup/13-RESEARCH.md
@.planning/phases/13-pr-worktree-auto-cleanup/13-01-SUMMARY.md

<interfaces>
<!-- From the live codebase + 13-01 output. Use directly. -->

TaskPage.tsx (web/src/pages/TaskPage.tsx) confirmed seams:
- isPR = task.source === "github_pr" (line 265).
- prDetail = usePullRequestDetail(...) (lines 88-93); now typed PRDetailWire WITH `state: "OPEN"|"CLOSED"|"MERGED"` (added in 13-01).
- cleanupOpen state + setCleanupOpen (line 111).
- CleanupWorktreeDialog ALREADY RENDERED (lines 595-602): open={cleanupOpen} onOpenChange={setCleanupOpen} taskId={task.id} taskTitle={task.title} projectId={projectId} trigger="menu".
- The `⋯` menu is gated OUT for PRs at line 520: `{!isPR && ( <DropdownMenu>...</DropdownMenu> )}`. The non-PR menu has "Clean up worktree" (line 532-534, onSelect={() => setCleanupOpen(true)}) + "Delete task" (line 538-543).
- Imports already present: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator (lines 22-28 import block); Button, Ellipsis; CleanupWorktreeDialog.
- The PR meta line (the merge line) renders in the `isPR` branch (lines 549-578) INSIDE the `w-full shrink-0` header block (the div closed at line 586). Place the banner here too — NOT below TaskTabs (that would steal flex height; 12-07 fix #6 height chain must stay intact).
- task.worktree_path: nullable; non-null means a worktree exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PR-specific ⋯ menu (Clean up worktree only) + merged/closed banner</name>
  <files>web/src/pages/TaskPage.tsx</files>
  <read_first>
    - web/src/pages/TaskPage.tsx (the header block: title region lines 478-513, QuotaIndicator line 515, the `{!isPR && (` menu gate lines 517-546, the isPR merge-line block lines 549-585, the shrink-0 div close line 586; cleanupOpen line 111; CleanupWorktreeDialog lines 595-602)
    - web/src/components/task/CleanupWorktreeDialog.tsx (confirm it works on any task.id with trigger="menu" — reused as-is, no change)
    - web/src/api/pullRequests.ts (PRDetailWire now has `state` from 13-01 — the banner reads prDetail?.state)
    - 13-RESEARCH.md "Frontend: the entry point that already exists" + D-08/D-09 in 13-CONTEXT.md (single Clean-up item, no Delete; banner inside shrink-0, never modal/blocking)
  </read_first>
  <action>
    All edits are in web/src/pages/TaskPage.tsx, inside the header. Two changes:

    CHANGE A — PR-specific ⋯ menu (D-08). Currently the menu is `{!isPR && ( ... )}` (line 520). Add a SEPARATE PR-only menu (do NOT relax the existing gate — keep the manual-task menu exactly as-is with its Delete-task item). Right after the `{!isPR && ( ... )}` block (after line 546, before `</header>` at 547), add:
    ```tsx
    {isPR && task.worktree_path && (
      <DropdownMenu>
        <DropdownMenuTrigger asChild>
          <Button variant="ghost" size="icon-sm" aria-label="Review actions">
            <Ellipsis className="size-4" />
          </Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent align="end">
          {/* D-08: a PR review gets ONLY "Clean up worktree" — no Delete task.
              Opens the existing gated CleanupWorktreeDialog (confirm/force/
              stop-sessions); manual semantics keep the row, null the worktree. */}
          <DropdownMenuItem onSelect={() => setCleanupOpen(true)}>
            Clean up worktree
          </DropdownMenuItem>
        </DropdownMenuContent>
      </DropdownMenu>
    )}
    ```
    Rationale for the `task.worktree_path` guard: with no worktree there is nothing to clean up (a clean auto-removed PR has its row deleted by the reaper, so it won't reach here; a skipped one still has worktree_path set). This mirrors the non-PR menu, which gates the Clean-up item on `task.worktree_path` (line 530).

    CHANGE B — merged/closed banner (D-09). Inside the existing `isPR ?` branch (the merge-line `<div className="flex min-w-0 flex-wrap ...">` block, lines 558-578), add a sibling banner ABOVE or BELOW the merge line but STILL inside the shrink-0 header block (do not move it past `</div>` at line 586 or after TaskTabs). Wrap the existing merge-line div and the new banner in a fragment if needed. Add:
    ```tsx
    {prDetail && prDetail.state !== "OPEN" && (
      <div
        className="mt-1 flex flex-wrap items-center gap-2 rounded-md border border-amber-500/30 bg-amber-500/10 px-2 py-1 text-xs text-amber-700 dark:text-amber-400"
        role="status"
      >
        <span>
          This PR was {prDetail.state === "MERGED" ? "merged" : "closed"}.
        </span>
        {task.worktree_path && (
          <button
            type="button"
            className="font-medium underline underline-offset-2 hover:no-underline"
            onClick={() => setCleanupOpen(true)}
          >
            Clean up worktree
          </button>
        )}
      </div>
    )}
    ```
    Placement notes:
    - It must render INSIDE the `w-full shrink-0` header container (the div whose close is at line 586) so it never participates in the tabs/terminal flex chain (12-07 fix #6). The simplest spot: just after the merge-line `</div>` (line 578) and before the branch's closing `)` (line 579), as a second element in the `isPR ?` arm — wrap both in a `<>...</>` fragment.
    - Never a modal, never blocking — it is an inline advisory (role="status").
    - Degrade: when prDetail is undefined (hard reload before re-hydration, or gh down) the banner simply does not render — the header keeps its task-field fallbacks (existing behavior). The CLEAN auto-removed case has the row deleted by the reaper, so the open tab degrades to the existing "Task not found" path on next fetch — acceptable (D-09); the banner serves the skipped / not-yet-reaped window.

    Do NOT change CleanupWorktreeDialog.tsx, useOpenReview, or usePullRequestDetail — they already carry everything needed (state arrives via 13-01's PRDetailWire.state).
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npx tsc --noEmit && grep -q 'aria-label="Review actions"' src/pages/TaskPage.tsx && grep -q 'This PR was' src/pages/TaskPage.tsx && grep -q 'state !== "OPEN"' src/pages/TaskPage.tsx && grep -c 'setCleanupOpen(true)' src/pages/TaskPage.tsx</automated>
  </verify>
  <done>The PR review view renders a ⋯ menu with exactly one "Clean up worktree" item (no Delete task) that opens the existing dialog; an inline merged/closed banner shows when prDetail.state !== "OPEN", inside the shrink-0 header block, with a Clean-up prompt while the worktree exists; web typecheck passes; the non-PR menu is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build the frontend bundle so the human-verify run uses the new UI</name>
  <files>web/dist (generated), kangent (binary)</files>
  <read_first>
    - Makefile or web/package.json (the build command — `npm run build` / `vite build` producing web/dist consumed by the Go embed)
    - CLAUDE.md (single-binary embed: //go:embed dist served by the Go server)
  </read_first>
  <action>
    Build the frontend so the embedded SPA the Go binary serves reflects the 13-03 UI for the human-verify task. From the repo root:
    - `cd web && npm run build` (or the project's documented build — check web/package.json scripts; if a top-level `make build` exists that does vite build + go build, prefer that).
    - Confirm web/dist exists and is non-empty after the build.
    Then build the Go binary so the tester can run the real server: `go build ./cmd/kangent` (or `make build`).
    Do NOT start the server here — the human-verify task instructs the tester to run it.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && (test -d web/dist && test "$(ls -A web/dist)") && go build ./cmd/kangent && echo BUILD_OK</automated>
  </verify>
  <done>web/dist is freshly built with the 13-03 UI; the Go binary builds with the embedded SPA; the app is runnable for the human-verify task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the three phase success criteria end-to-end</name>
  <what-built>
    Phase 13 complete: (1) the reaper auto-removes a merged/closed PR's pristine, idle review worktree on its tick and deletes the task row (13-02); (2) the conservative gate leaves a dirty/unpushed/stashed/busy worktree in place with its branch kept (13-02); (3) a PR review view has a ⋯ "Clean up worktree" menu opening the gated dialog, plus a merged/closed banner (13-01 state + 13-03 UI).
  </what-built>
  <how-to-verify>
    Prereqs: `gh` installed + authenticated; a project linked to a GitHub repo where you have a review-requested PR (or can open one). Run the freshly built binary: `./kangent` (or `go run ./cmd/kangent`), open http://127.0.0.1:7333.

    NOTE on triggering the reaper: the tick is 10 minutes. To observe auto-cleanup quickly, EITHER wait one tick, OR (faster) restart the server — `Run` calls `reconcilePRsOnce` immediately on start (reaper.go), so a restart forces a reconcile pass right away.

    1) AUTO-CLEANUP (GHCLN-01) — clean case:
       a. Open a PR review (click a PR card) → a review workspace with a worktree is created. Do NOT make any local changes; close any agent/bash sessions in it (no running sessions).
       b. Merge (or close) that PR on GitHub.
       c. Restart the kangent server (forces an immediate reconcile) — or wait a tick.
       d. EXPECT: the review worktree directory is gone (check ~/.kangent/worktrees or your worktree_base), and the task row is deleted. Re-opening the same PR card re-provisions a fresh worktree (or, if still merged, opens then immediately a banner). The reaper log shows "reaped merged/closed PR worktree".

    2) SAFETY (GHCLN-02) — dirty/busy case + branch kept:
       a. Open another PR review; in its bash tab make an uncommitted change (e.g. `echo x >> README`), OR commit a local-only fixup, OR leave an agent/bash session running.
       b. Merge/close that PR on GitHub.
       c. Restart the server (immediate reconcile) — or wait a tick.
       d. EXPECT: the worktree is STILL there, the task row STILL exists, and the reaper log shows a skip reason ("dirty worktree" / "unpushed commits" / "stash present" / "sessions running"). Confirm the git branch (pr/<n> or the head branch) still exists: `git -C <repo> branch --list 'pr/*'` or the head name — it must NOT have been deleted.

    3) MANUAL CLEANUP (GHCLN-03) + BANNER (D-09):
       a. Open the skipped/merged review from step 2 (or any merged PR review whose worktree still exists).
       b. EXPECT: an inline amber banner in the header reads "This PR was merged." (or "closed.") with a "Clean up worktree" link — NOT a modal, and the terminal/tabs below are full height (no scroll/height regression).
       c. Click the ⋯ menu (top-right of the review header) → it shows ONLY "Clean up worktree" (no "Delete task"). 
       d. Click "Clean up worktree" (from the menu OR the banner link) → the gated CleanupWorktreeDialog opens (fresh dirty/session counts). If dirty/busy, it offers force/stop-sessions; confirm cleanup. EXPECT: the worktree is removed, the task row is KEPT (manual semantics — re-opening the PR re-provisions), and the branch is kept.

    Confirm overall: no console errors; the board is unaffected (PR reviews never appear as cards); existing task flows behave as before.
  </how-to-verify>
  <action>
    This is a human-verification gate, not an autonomous edit. The executor must:
    1. Confirm the build is fresh (Task 2 ran: web/dist + the kangent binary reflect 13-01/13-02/13-03).
    2. Present the how-to-verify steps to the user and PAUSE for the user to run the three scenarios (auto-cleanup, safety, manual cleanup + banner) against a real linked GitHub repo with a review-requested PR.
    3. Do NOT proceed past this gate until the user replies. On "approved", write the SUMMARY and close the phase. On reported issues, capture them verbatim for gap-closure planning (/gsd:plan-phase --gaps).
  </action>
  <verify>
    <automated>go build ./cmd/kangent && (test -d web/dist && test "$(ls -A web/dist)") && echo READY_FOR_HUMAN_VERIFY</automated>
    Manual: the user confirms all three success criteria pass (auto-cleanup of a clean merged worktree; safety skip of a dirty/busy one with the branch kept; manual cleanup + the merged/closed banner).
  </verify>
  <done>The user has typed "approved" after running the three scenarios, OR issues are captured for gap closure. The build is confirmed runnable before the gate.</done>
  <resume-signal>Type "approved" if all three criteria pass, or describe the issue(s) for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` passes; web/dist + the Go binary build.
- TaskPage renders a PR-only ⋯ menu with one "Clean up worktree" item and a merged/closed banner gated on `prDetail.state !== "OPEN"`, both inside the shrink-0 header block.
- Human-verify confirms: auto-cleanup (clean), safety skip (dirty/busy, branch kept), manual cleanup + banner.
</verification>

<success_criteria>
- GHCLN-03: the user can manually clean up a PR review worktree from the review view via the ⋯ menu / banner link, using the same gated dialog tasks already use (manual semantics: row kept, worktree nulled).
- D-09: a merged/closed PR shows an inline, non-blocking banner while its worktree lingers (the discoverability path for skipped leftovers).
- The full phase (GHCLN-01/02/03) is human-verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pr-worktree-auto-cleanup/13-03-SUMMARY.md`
</output>
