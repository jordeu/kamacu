---
phase: 12-open-a-review
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [GHREV-01, GHREV-05]
files_modified:
  - internal/worktree/worktree.go
  - internal/worktree/worktree_test.go
  - internal/api/pullrequests.go
  - internal/github/github.go
  - internal/github/github_test.go
  - internal/settings/settings.go
  - internal/settings/validate.go
  - internal/settings/settings_test.go

must_haves:
  truths:
    - "Opening a PR review checks out the PR's REAL head branch (headRefName) in the new worktree, not a detached HEAD."
    - "When the PR head branch name already exists locally (e.g. a fork PR whose head is 'master'), the open falls back to pr/<n> and NEVER reuses or moves the existing local branch ref — the project's primary checkout HEAD is unchanged."
    - "The open endpoint's PR wire response carries the head branch name and the PR's commit count so the frontend can render a GitHub-style merge line."
    - "A pr_review_seed settings key exists with the canned review prompt default and flows through the existing GET /api/settings and PUT /api/settings/{key} surface."
  artifacts:
    - path: "internal/worktree/worktree.go"
      provides: "CheckoutPR creates a NAMED branch (headRefName, pr/<n> fallback) in the new worktree via worktree add -b"
      contains: "headRefName"
    - path: "internal/github/github.go"
      provides: "PRDetail.Commits int field + commits in the gh pr view --json field set"
      contains: "Commits"
    - path: "internal/api/pullrequests.go"
      provides: "prWire surfaces headRefName + commits; CheckoutPR called with detail.HeadRefName"
      contains: "HeadRefName"
    - path: "internal/settings/settings.go"
      provides: "KeyPRReviewSeed constant + Defaults entry"
      contains: "pr_review_seed"
  key_links:
    - from: "internal/api/pullrequests.go"
      to: "internal/worktree CheckoutPR"
      via: "passes detail.HeadRefName as the branch to check out"
      pattern: "CheckoutPR\\(.*HeadRefName"
    - from: "internal/worktree/worktree.go"
      to: "git worktree add -b <branch>"
      via: "named-branch checkout with collision pre-check (show-ref + worktree list)"
      pattern: "worktree.*add.*-b"
---

<objective>
Close three of the five 12-05 human-verify follow-ups on the BACKEND so the
frontend gap plan (12-07) can render the new behaviors:

1. **Named-branch PR checkout (DECISION OVERRIDE of D-01 "detached").** The PR
   review worktree must check out the PR's REAL head branch (`headRefName`),
   with a `pr/<n>` fallback on a local-name collision — never a detached HEAD,
   never reusing/moving an existing branch ref. This realigns the
   implementation with the literal GHREV-01 wording ("the PR's branch checked
   out") while PRESERVING the GHREV-05 safety guarantee (collision fallback +
   main-HEAD-unchanged).

2. **Commits count + head branch in the open response,** so 12-07 can render
   `<author> wants to merge <N> commits into <base> from <head>`.

3. **A `pr_review_seed` settings key** (default = the canned review prompt with
   `<n>`/`<title>` placeholders) flowing through the EXISTING settings KV REST
   surface — no new endpoint, no migration. Placeholders are interpolated on
   the FRONTEND (12-07), not here.

Purpose: GHREV-01 ("the PR's branch checked out") + GHREV-05 (primary-checkout
safety) were both flagged at the 12-05 checkpoint; the detached HEAD did not
match the user's expectation of seeing the PR's named branch. This plan fixes
the checkout mechanic and wires the data the merge line + configurable seed
need.

Output: a CheckoutPR that creates a named branch with a safe collision
fallback (+ tests), a PRDetail/prWire carrying commits + head branch (+ tests),
and a pr_review_seed settings key (+ test). No new Go/npm deps, no migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-open-a-review/12-CONTEXT.md
@.planning/phases/12-open-a-review/12-RESEARCH.md
@.planning/phases/12-open-a-review/12-05-SUMMARY.md

<interfaces>
<!-- Current signatures the executor MUST work against (read from source, do not re-explore). -->

internal/worktree/worktree.go — CURRENT CheckoutPR (to MODIFY: add headRefName + named-branch logic):
```go
func (s *Service) CheckoutPR(ctx context.Context, repo, path, headOID string, prNumber int) error {
    s.mu.Lock(); defer s.mu.Unlock()
    if _, err := os.Stat(path); err == nil { return fmt.Errorf("worktree path already exists: %s", path) }
    if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil { return err }
    if _, err := gitRun(ctx, repo, "fetch", "origin", fmt.Sprintf("refs/pull/%d/head", prNumber)); err != nil { return err }
    _, err := gitRun(ctx, repo, "worktree", "add", "--detach", path, headOID)
    return err
}
```
- `gitRun(ctx, repo, args...)` exists: `git -C repo args...`, returns (stdout, error); error message is git's stderr with "fatal: " stripped.
- The existing `Create` already demonstrates the branch-exists check pattern:
  `gitRun(ctx, repo, "show-ref", "--verify", "--quiet", "refs/heads/"+branch)` returns err==nil when the branch EXISTS.

internal/github/github.go — CURRENT PRDetail + the gh pr view --json field set (to EXTEND with commits):
```go
type PRDetail struct {
    Number int; Title string; Body string
    AuthorLogin string `json:"-"` // flattened from author.login
    URL string; HeadRefName string; HeadRefOid string
    BaseRefName string; BaseRefOid string; IsCrossRepository bool
}
// ViewPR uses: "number,title,body,author,url,headRefName,headRefOid,baseRefName,baseRefOid,isCrossRepository"
// raw decode struct embeds PRDetail + a nested author{login} object.
```

internal/api/pullrequests.go — CURRENT prWire + the CheckoutPR call site (to EXTEND/UPDATE):
```go
type prWire struct {
    Number int `json:"number"`; Title string `json:"title"`; Body string `json:"body"`
    Author string `json:"author"`; URL string `json:"url"`; BaseRefName string `json:"baseRefName"`
}
// call site (≈ line 199):
wtSvc.CheckoutPR(r.Context(), repoPath, prWtPath, detail.HeadRefOid, int(n))
// writeReview builds prWire from github.PRDetail.
```

internal/settings/settings.go — Defaults map + key constants (to ADD pr_review_seed). internal/settings/validate.go — Validate switch (add a case). The KV store needs NO migration: an absent row reads as the code default.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Named-branch PR checkout with safe collision fallback (GHREV-01 + GHREV-05)</name>
  <files>internal/worktree/worktree.go, internal/worktree/worktree_test.go</files>
  <read_first>
    - internal/worktree/worktree.go (the CURRENT CheckoutPR at ≈ line 190; the Create branch-exists pattern at ≈ line 172 using `show-ref --verify --quiet refs/heads/<branch>`; the package header comment asserting the scoped-fetch exception)
    - internal/worktree/worktree_test.go (the EXISTING CheckoutPR tests to update: TestCheckoutPRDetachedHead, TestCheckoutPRLeavesSourceHeadUnchanged ≈ line 591 = the GHREV-05 proof, TestCheckoutPRCreatesNoBranch, TestCheckoutPRPathCollision; the makePRRemote helper ≈ line 547 and cloneRepo/makeRepoOn helpers)
  </read_first>
  <behavior>
    - Test: CheckoutPR with headRefName="feature-x" (not present locally) → the new worktree's HEAD is ON branch `feature-x` (symbolic-ref --short HEAD == "feature-x"), pinned to the PR head OID.
    - Test (GHREV-05, KEEP + adapt the existing TestCheckoutPRLeavesSourceHeadUnchanged): after CheckoutPR the SOURCE clone's symbolic-ref --short HEAD AND rev-parse HEAD are byte-for-byte unchanged before/after.
    - Test (NEW collision test, GHREV-05 safety): pre-create a local branch named `master` in the clone pointing at some commit C; call CheckoutPR with headRefName="master", prNumber=8 → the new worktree is on branch `pr/8` (NOT `master`), AND the pre-existing local `master` ref still points at C (rev-parse refs/heads/master unchanged — never reused, never moved).
    - Test (double collision): when BOTH `<headRefName>` and `pr/<n>` already exist locally → CheckoutPR uses `pr/<n>-<short headOID>` (or returns a clear error — pick one and assert it; recommend the suffix form for resilience).
    - Test (negative guard): the new worktree is NOT detached — symbolic-ref --short HEAD on the worktree SUCCEEDS (the old TestCheckoutPRDetachedHead must be rewritten to assert a NAMED branch, since --detach is gone).
    - Test: path-collision pre-check still fires BEFORE git runs (keep TestCheckoutPRPathCollision; the leaf os.Stat check is unchanged).
  </behavior>
  <action>
    Change `CheckoutPR`'s signature to add a `headRefName string` parameter:
    `func (s *Service) CheckoutPR(ctx context.Context, repo, path, headOID, headRefName string, prNumber int) error`.

    Keep the existing preamble verbatim: mutex lock, the `os.Stat(path)` "worktree path already exists" pre-check, `MkdirAll(filepath.Dir(path))`, and the `git fetch origin refs/pull/<n>/head` (still fetch FIRST; still pin to `headOID`, never FETCH_HEAD — RESEARCH Pitfall 1).

    REPLACE the final `worktree add --detach` with named-branch selection. Add a private helper, e.g.:
    ```go
    // branchExistsLocally reports whether refs/heads/<name> exists in repo.
    func (s *Service) branchExistsLocally(ctx context.Context, repo, name string) bool {
        _, err := gitRun(ctx, repo, "show-ref", "--verify", "--quiet", "refs/heads/"+name)
        return err == nil
    }
    ```
    Choose the branch name with this EXACT rule order (NEVER reuse/move an existing ref):
    1. If `headRefName != ""` AND NOT branchExistsLocally(headRefName) → chosen = headRefName.
    2. Else fall back: candidate = fmt.Sprintf("pr/%d", prNumber). If NOT branchExistsLocally(candidate) → chosen = candidate.
    3. Else (pr/<n> also taken) → chosen = fmt.Sprintf("pr/%d-%s", prNumber, shortOID) where shortOID = headOID truncated to its first 7 chars (guard: if len(headOID) < 7 use the whole string). This near-always resolves; if even THAT exists, return a clear error: `fmt.Errorf("couldn't pick a branch name for PR #%d", prNumber)`.
    4. Defensive empty guard: if `headRefName == ""` start at step 2 directly (a PR with no head ref name still gets pr/<n>).

    Then create the branch IN THE NEW WORKTREE ONLY:
    `_, err := gitRun(ctx, repo, "worktree", "add", "-b", chosen, path, headOID)` and return err.
    `worktree add -b` creates `chosen` AND pins it to `headOID` in one step, in the new worktree — the project's primary checkout HEAD/branch never move (GHREV-05). Because we only ever pass a name that does NOT already exist locally, we can never reuse or fast-forward an existing branch (the fork same-name `master` trap is exactly what step 2 protects against).

    Do NOT use `git fetch` to create or update any local branch ref; do NOT use `gh pr checkout`; do NOT pass `--detach` anymore.

    Update the doc comment: drop the "DETACHED" framing, describe the named-branch + collision fallback, and KEEP the GHREV-05 note ("never reuse/move an existing branch ref → the primary checkout HEAD is unchanged; the collision fallback protects the fork same-name case"). The package-header scoped-fetch exception note (D-24) stays as-is — the fetch is unchanged.

    Then update internal/worktree/worktree_test.go:
    - Rewrite `TestCheckoutPRDetachedHead` → `TestCheckoutPRNamedBranch`: pass a headRefName (extend makePRRemote OR just pass a literal like "pr-source-branch"), assert `symbolic-ref --short HEAD` on the worktree == that branch name AND `rev-parse HEAD` == headOID.
    - Update ALL CheckoutPR call sites in the test to the new 6-arg signature (add the headRefName arg). For tests that don't care about the name, pass a unique name like `fmt.Sprintf("pr-%d-head", n)`.
    - KEEP `TestCheckoutPRLeavesSourceHeadUnchanged` (the GHREV-05 proof) — only fix the call args; the before/after symbolic-ref + rev-parse assertions on the SOURCE clone stay.
    - Rewrite `TestCheckoutPRCreatesNoBranch` → `TestCheckoutPRBranchInNewWorktreeOnly`: a NEW branch named after the PR head IS expected now, but it must live in the new worktree; assert the worktree is on the named branch and the SOURCE clone's CURRENT branch is unchanged (the named branch may now appear in `git branch` — that is correct; the invariant is that no PRE-EXISTING branch ref moved and the source HEAD is unchanged).
    - ADD `TestCheckoutPRCollisionFallsBackToPRBranch`: in the clone, `git branch master <someCommit>` (a pre-existing local branch), capture its OID, call CheckoutPR with headRefName="master" prNumber=8 → assert the worktree's branch is `pr/8` AND `rev-parse refs/heads/master` in the clone equals the captured OID (untouched).
    - KEEP `TestCheckoutPRPathCollision` (fix the call args only).
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go test ./internal/worktree/ -run TestCheckoutPR -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/worktree/` passes with the new + updated tests.
    - `grep -n "headRefName" internal/worktree/worktree.go` shows the new param + selection logic.
    - `grep -n "worktree.*add.*-b" internal/worktree/worktree.go` shows the named-branch checkout in CheckoutPR.
    - `grep -c -- "--detach" internal/worktree/worktree.go` returns 0 inside CheckoutPR (negative guard — the detached path is gone).
    - The GHREV-05 test (TestCheckoutPRLeavesSourceHeadUnchanged) still exists and passes (`grep -n "LeavesSourceHeadUnchanged" internal/worktree/worktree_test.go`).
    - A collision test exists and passes (`grep -n "Collision\|CollisionFallsBack" internal/worktree/worktree_test.go` shows a test asserting pr/<n> fallback with the existing local branch ref untouched).
  </acceptance_criteria>
  <done>CheckoutPR checks out the PR's named head branch with a pr/<n> safe fallback; the existing-local-branch ref is never reused or moved; the source HEAD is unchanged; tests prove all three.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commits count + head branch in PRDetail and the open endpoint wire response</name>
  <files>internal/github/github.go, internal/github/github_test.go, internal/api/pullrequests.go</files>
  <read_first>
    - internal/github/github.go (PRDetail struct ≈ line 106; ViewPR's `--json` field set ≈ line 129; the lenient raw decode struct embedding PRDetail ≈ line 134)
    - internal/github/github_test.go (ghViewFixture ≈ line 108; TestViewPRDecodeFlattensAuthorAndEmptyBody ≈ line 121; viewPRRaw ≈ line 98)
    - internal/api/pullrequests.go (prWire struct ≈ line 218; writeReview ≈ line 228; the CheckoutPR call site ≈ line 199 — UPDATE it to the new 6-arg signature from Task 1, passing detail.HeadRefName)
  </read_first>
  <behavior>
    - Test (github): decode a gh pr view fixture whose `commits` is a JSON ARRAY of length 3 → PRDetail.Commits == 3 (the count of the array length). HeadRefName decodes as before.
    - Test (github): an empty/absent commits array → Commits == 0 (no panic, tolerant decode).
  </behavior>
  <action>
    In internal/github/github.go:
    - Add `Commits int` to `PRDetail` with `json:"-"` (it is DERIVED from the array length, not a scalar in the JSON, so it must NOT be populated by the direct unmarshal). Keep `HeadRefName` as-is (already present).
    - Add `commits` to ViewPR's `--json` field list:
      `"number,title,body,author,url,headRefName,headRefOid,baseRefName,baseRefOid,isCrossRepository,commits"`.
    - In the raw decode struct, add a sibling field to count the array:
      `Commits []struct{} \`json:"commits"\`` (an array of empty structs — we only need the length, gh returns each commit as an object so an empty-struct element decodes fine and ignores fields).
    - After building `d := raw.PRDetail; d.AuthorLogin = raw.Author.Login`, set `d.Commits = len(raw.Commits)`.

    In internal/github/github_test.go:
    - Extend `ghViewFixture` to include a `"commits": [ {"oid":"a"}, {"oid":"b"}, {"oid":"c"} ]` array (3 elements). Update `viewPRRaw` to add the `Commits []struct{} \`json:"commits"\`` field so the decode test exercises the count.
    - In TestViewPRDecodeFlattensAuthorAndEmptyBody, after computing `d`, set `d.Commits = len(raw.Commits)` and assert `d.Commits == 3`. Also assert `d.HeadRefName == "gh-pr"` (the fixture's headRefName) so the head branch surfacing is covered.
    - (TestViewPRFakeGH echoes the same fixture; if it asserts specific fields, it continues to pass — the added commits array is ignored by the fields it checks.)

    In internal/api/pullrequests.go:
    - UPDATE the CheckoutPR call site to the Task-1 signature, passing the head branch name:
      `wtSvc.CheckoutPR(r.Context(), repoPath, prWtPath, detail.HeadRefOid, detail.HeadRefName, int(n))`.
    - Extend `prWire` with `HeadRefName string \`json:"headRefName"\`` and `Commits int \`json:"commits"\``.
    - In `writeReview`, populate the two new fields from the github.PRDetail: `HeadRefName: d.HeadRefName, Commits: d.Commits`.
    These flow through the existing `{task, pr}` envelope writeReview already returns on BOTH the create and the reattach paths — no handler-flow change.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go test ./internal/github/ ./internal/api/ -run 'ViewPR|PullRequest|Review' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Commits" internal/github/github.go` shows the PRDetail field + the `len(raw.Commits)` assignment.
    - `grep -n "commits" internal/github/github.go` shows `commits` in the --json field list.
    - `grep -n "HeadRefName\|Commits" internal/api/pullrequests.go` shows both new prWire fields populated in writeReview.
    - `grep -n "CheckoutPR(.*HeadRefName" internal/api/pullrequests.go` confirms the open endpoint passes the head branch name into CheckoutPR (key link).
    - `go build ./...` passes; `go test ./internal/github/` passes with the commits-count assertion.
  </acceptance_criteria>
  <done>PRDetail and the open endpoint's pr wire response carry the head branch name + commit count; ViewPR counts the commits array; the endpoint provisions the worktree on the PR's named head branch.</done>
</task>

<task type="auto">
  <name>Task 3: pr_review_seed settings key (default prompt) via the existing KV store</name>
  <files>internal/settings/settings.go, internal/settings/validate.go, internal/settings/settings_test.go, internal/settings/validate_test.go</files>
  <read_first>
    - internal/settings/settings.go (the key constants block ≈ line 15; the Defaults map ≈ line 25; Get/GetAll/Set — the KV surface; NOTE: an absent row reads as the code default, so NO migration is needed)
    - internal/settings/validate.go (the Validate switch ≈ line 35; the KeyAgentExtraParams free-text pass-through case ≈ line 63 — the soft-handling pattern to mirror)
    - internal/settings/settings_test.go (TestDefaultsValues ≈ line 26 — the exhaustive defaults map + the `len(Defaults) != len(want)` count check that MUST be updated when a key is added; TestGetReturnsDefaultOnFreshDB; TestGetAllMergesStoredOverDefaults)
    - internal/api/settings.go (getAll/put already serve EVERY Defaults key generically — adding a key needs ZERO handler change; entryFor returns value+default)
  </read_first>
  <action>
    In internal/settings/settings.go:
    - Add the key constant: `KeyPRReviewSeed = "pr_review_seed"` to the const block.
    - Add the Defaults entry with this EXACT default string (placeholders `<n>` and `<title>` are interpolated on the FRONTEND in 12-07, stored literally here):
      ```go
      // GHREV/12-07: configurable PR-review seed prompt. <n>/<title> are
      // interpolated on the frontend at Start; stored literally. Free-text.
      KeyPRReviewSeed: `Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.`,
      ```
      (Use a backtick raw string literal so the embedded double-quotes around `<title>` need no escaping.)

    In internal/settings/validate.go:
    - Add a `case KeyPRReviewSeed:` to the Validate switch. Apply the same soft free-text handling as KeyAgentExtraParams (accept any string), with a reasonable length cap to avoid pathological values:
      ```go
      case KeyPRReviewSeed:
          // Free-text seed prompt (12-07). Accept any value; cap length so a
          // pathological paste can't bloat the KV row. <n>/<title> placeholders
          // are interpolated on the frontend.
          const maxPRReviewSeed = 2000
          if len(value) > maxPRReviewSeed {
              return errors.New("Prompt is too long.")
          }
          return nil
      ```
      (`errors` is already imported in validate.go.)

    The key automatically flows through the REST surface with NO endpoint change:
    GET /api/settings (getAll iterates Defaults) returns it with value+default;
    PUT /api/settings/pr_review_seed validates via the new case then upserts.
    Empty string is a real value (Pitfall 1) — an empty seed means "no
    injection" downstream; that is intended (12-07 treats blank as undefined).

    In internal/settings/settings_test.go:
    - Add `settings.KeyPRReviewSeed: \`Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.\`` to the `want` map in TestDefaultsValues so the default + the `len(Defaults) == len(want)` count check both stay correct. (TestGetReturnsDefaultOnFreshDB and TestGetAllMergesStoredOverDefaults iterate Defaults, so they cover the new key for free.)
    - Add a focused test, e.g. TestValidatePRReviewSeed in internal/settings/validate_test.go (or settings_test.go — match the file that owns Validate tests): assert `settings.Validate(settings.KeyPRReviewSeed, "anything")` returns nil, `settings.Validate(settings.KeyPRReviewSeed, "")` returns nil (empty is allowed), and a 2001-char string returns the "Prompt is too long." error. Use the existing `checkErrString` helper if writing in validate_test.go.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go test ./internal/settings/ ./internal/api/ -run 'Defaults|PRReviewSeed|Settings' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pr_review_seed" internal/settings/settings.go` shows the const + Defaults entry.
    - `grep -n "KeyPRReviewSeed" internal/settings/validate.go` shows the Validate case.
    - The default string matches EXACTLY (including the `#<n>`, the quoted `"<title>"`, and the trailing sentence): `grep -F 'Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.' internal/settings/settings.go`.
    - `go test ./internal/settings/` passes including the updated TestDefaultsValues count check and the new validate test.
    - No new migration file under internal/store/migrations (the KV store needs none): `git status --porcelain internal/store/migrations` shows no additions.
  </acceptance_criteria>
  <done>A pr_review_seed key with the canonical default exists, validates as free-text (length-capped), and is served by the existing GET getAll / PUT {key} settings endpoints with no new endpoint and no migration.</done>
</task>

</tasks>

<verification>
- `go build ./...` passes.
- `go test ./internal/worktree/ ./internal/github/ ./internal/api/ ./internal/settings/` all pass.
- CheckoutPR creates a named branch with a pr/<n> collision fallback; the GHREV-05 source-HEAD-unchanged test and the new collision test both pass.
- `--detach` is gone from CheckoutPR; `gh pr checkout` is never used.
- The open endpoint passes detail.HeadRefName into CheckoutPR and returns headRefName + commits in the pr wire.
- pr_review_seed is in Defaults with the exact default, validates, and serves through GET/PUT — no new endpoint, no migration.
</verification>

<success_criteria>
- GHREV-01 mechanic: a PR review worktree is on the PR's REAL head branch (or pr/<n> on collision), not detached.
- GHREV-05 safety preserved and proven: an existing local branch with the PR head's name is never reused or moved; source HEAD unchanged.
- Backend data for the merge line (commits + head branch) and the configurable seed (pr_review_seed key) are in place for 12-07 to consume.
- No new Go dependency, no new npm dependency, no new DB migration.
</success_criteria>

<output>
After completion, create `.planning/phases/12-open-a-review/12-06-SUMMARY.md`.
</output>
