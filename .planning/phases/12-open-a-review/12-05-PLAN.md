---
phase: 12-open-a-review
plan: 05
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - web/src/api/types.ts
  - web/src/api/pullRequests.ts
  - web/src/components/board/PRCard.tsx
  - web/src/components/board/ReviewColumn.tsx
  - web/src/pages/TaskPage.tsx
  - web/src/components/task/AgentTab.tsx
  - web/src/components/task/DescriptionTab.tsx
autonomous: false
requirements: [GHREV-01, GHREV-02, GHREV-03, GHREV-04, GHREV-05]
must_haves:
  truths:
    - "Clicking a PR card body opens/reattaches the review and routes to the task-like review view"
    - "The review view shows a read-only PR title, a #num · @author · base · ↗ meta line, no ⋯ menu, and a read-only PR-body Description"
    - "Starting the agent in a review prefills (does not auto-send) the canned seed prompt referencing the PR"
    - "The Diff tab is available immediately and shows the PR-base diff (server-side); the review never appears as a kanban card"
  artifacts:
    - path: "web/src/api/pullRequests.ts"
      provides: "useOpenReview mutation hitting POST .../pull-requests/{n}/review"
      contains: "useOpenReview"
    - path: "web/src/components/board/PRCard.tsx"
      provides: "clickable card body that opens the review (↗ stopPropagation kept)"
      contains: "role=\"button\""
    - path: "web/src/pages/TaskPage.tsx"
      provides: "source === 'github_pr' branches: read-only title, PR meta line, omitted ⋯ menu, read-only Description"
      contains: "github_pr"
  key_links:
    - from: "web/src/components/board/PRCard.tsx body click"
      to: "useOpenReview → navigate(/projects/{id}/tasks/{taskId})"
      via: "openReview.mutate(pr.number, { onSuccess })"
      pattern: "openReview"
    - from: "web/src/components/task/AgentTab.tsx Start"
      to: "pasteApiRef.current.paste(seed)"
      via: "one-shot seed paste after connect for source==='github_pr'"
      pattern: "pasteApiRef.current"
---

<objective>
Wire the frontend deltas (all locked by 12-UI-SPEC): make the PR card body open/reattach a review and route to the task-like view; branch `TaskPage` on `task.source === 'github_pr'` for a read-only title, a PR meta line, an omitted ⋯ menu, and a read-only PR-body Description; and seed (prefill, never auto-send) the agent prompt on Start. Then a human-verify checkpoint runs the end-to-end test on a real PR (main HEAD unchanged + diff matches GitHub + not on board).

This is NOT net-new UI: every value is lifted verbatim from the existing task view + Phase 11 PR card per the UI-SPEC. Zero new tokens, zero new npm deps.

Purpose: the user-facing payoff of Phase 12 — click a PR, get a real review workspace — plus the milestone's two non-negotiable safety checks verified live by a human.
Output: the open affordance, the review-view branches, the seeded Start, and a signed-off end-to-end safety check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-open-a-review/12-RESEARCH.md
@.planning/phases/12-open-a-review/12-CONTEXT.md
@.planning/phases/12-open-a-review/12-UI-SPEC.md

<interfaces>
<!-- Real signatures/handles confirmed in source — use directly, no exploration. -->

web/src/api/types.ts Task (extend): currently has id, project_id, title, description, status, position, created_at, updated_at, branch, worktree_path, worktree_error. ADD `source: "manual" | "github_pr"`, `pr_number: number | null`, `pr_base_ref: string | null` (the backend now returns them — 12-02).

web/src/api/client.ts: `post<T>(path, body?)`. The review endpoint returns `{ task: Task, pr: { number, title, body, author, url, baseRefName } }`.

web/src/api/pullRequests.ts: PRSummary already carries number/title/author/url/checks + head/base fields. Add `useOpenReview(projectId)` mirroring useCreateWorktree's mutation shape (worktrees.ts:33).

web/src/components/board/PRCard.tsx (the file you modify): root `<div className="rounded-md border border-border bg-card px-3 py-2">` is INERT today. The ↗ `<a>` is lines 57-65; the checks dot is lines 38-56. PRCard currently takes only `{ pr }` — ADD `projectId`.

web/src/components/board/ReviewColumn.tsx: ReviewStates maps `prs.map(pr => <PRCard key={pr.number} pr={pr} />)` at line 231/232 — pass `projectId` down (ReviewColumnInner already has projectId; ReviewStates needs it as a prop).

web/src/pages/TaskPage.tsx (the file you modify):
- editable title button: lines 432-457 (the `<button … onClick={() => setTitleDraft(...)}>` branch).
- ⋯ DropdownMenu: lines 461-485.
- `<WorktreeMetaLine task=… onCreate=… creating=… />`: lines 488-492.
- DescriptionTab usage: lines 314-321 (`<DescriptionTab task={task} projectId={projectId} />`).
- AgentTab usage: lines 303-311.
- default tab `useState("agent")` (line 82) — UNCHANGED (D-04).
- diff tab gate `task.worktree_path` (lines 162-170, 327-337) — UNCHANGED (worktree exists by render time).
- App route `/projects/:projectId/tasks/:taskId` already matches (App.tsx:44) — route to it via useNavigate.

web/src/components/task/AgentTab.tsx (the file you modify): `pasteApiRef = useRef<{ paste:(t:string)=>void }|null>(null)` (line 38), wired `onReady={(api)=>{ pasteApiRef.current = api }}` (lines 201-203), used for Insert description `pasteApiRef.current?.paste(task.description)` (line 133). xterm wraps paste in bracketed-paste → un-submitted. AgentTab currently takes `{ task, agentSession, projectId }`.

web/src/components/task/DescriptionTab.tsx (the file you modify): the read-only markdown block is `<div className="prose prose-invert prose-sm max-w-none prose-a:text-blue-500"><ReactMarkdown remarkPlugins={[remarkGfm]}>{...}</ReactMarkdown></div>` (lines 62-66); empty state "No description." (line 53). It currently always shows an Edit button.

UI-SPEC LOCKED visual values (copy verbatim):
- Card hover/click: keep `rounded-md border border-border bg-card px-3 py-2`; ADD `hover:bg-[#27272a] cursor-pointer`, `role="button"`, `tabIndex={0}`, `aria-label="Open review for PR #{number}"`, `focus-visible:ring-2 focus-visible:ring-blue-500`, onClick + onKeyDown(Enter/Space). ↗ keeps `onClick={e=>e.stopPropagation()}`. Checks dot stays non-interactive (no stopPropagation).
- Read-only title box: `min-w-0 flex-1 truncate text-base font-medium px-1 py-0.5 text-left` as a `<span>`/`<h1>` (NO hover:bg-muted/50, NO onClick, NO "Edit title" title; add `title={prTitle}`).
- PR meta line: `flex min-w-0 items-center gap-2 text-xs text-muted-foreground`, `·`-separated: `#{number}` · `@{author}` · `<span className="font-mono">{base}</span>` · ↗ `<a href={pr.url} target="_blank" rel="noreferrer" aria-label="Open PR #{number} on GitHub" className="text-muted-foreground hover:text-foreground"><ExternalLink className="size-3.5"/></a>`.
- Description read-only: the prose block, NO Edit button, NO Textarea; empty body → `No description.` in `text-muted-foreground` with NO Edit button.
- Seed text (D-07): `Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.`
- Loading (first open): after 150ms show `Setting up the review worktree…` (`text-sm text-muted-foreground`, centered) — but since the endpoint is synchronous, this primarily covers the in-flight mutation; show it on the card (disable while pending) or as a routed-to placeholder — planner's discretion per UI-SPEC §F.
- Error: headline `Couldn't open this review.` + `Couldn't set up the worktree: {error}` (`text-red-500` detail) + neutral `outline`/`sm` `Retry`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Task type + useOpenReview hook + PR card open trigger</name>
  <read_first>
    - web/src/api/types.ts (the Task interface to extend)
    - web/src/api/pullRequests.ts (PRSummary + the useQuery/useMutation patterns) and web/src/api/worktrees.ts (useCreateWorktree shape to mirror, lines 33-42) and web/src/api/client.ts (post signature)
    - web/src/components/board/PRCard.tsx (the inert card to make clickable — full file) and web/src/components/board/ReviewColumn.tsx (ReviewStates PRCard mapping at line ~231)
    - .planning/phases/12-open-a-review/12-UI-SPEC.md §A "PR card — body becomes the open trigger"
  </read_first>
  <action>
    1. web/src/api/types.ts — add to the Task interface:
       ```ts
       source: "manual" | "github_pr";
       pr_number: number | null;
       pr_base_ref: string | null;
       ```

    2. web/src/api/pullRequests.ts — add the open-review mutation + its response type:
       ```ts
       import { useMutation } from "@tanstack/react-query"; // already imported
       import { post } from "./client";
       import type { Task } from "./types";

       export interface PRDetailWire {
         number: number;
         title: string;
         body: string;
         author: string;
         url: string;
         baseRefName: string;
       }
       export interface OpenReviewResponse { task: Task; pr: PRDetailWire; }

       export function useOpenReview(projectId: number) {
         return useMutation<OpenReviewResponse, ApiError, number /* prNumber */>({
           mutationFn: (n) =>
             post<OpenReviewResponse>(`/api/projects/${projectId}/pull-requests/${n}/review`),
         });
       }
       ```
       (import ApiError from "./client".)

    3. web/src/components/board/PRCard.tsx — make the card body the open trigger (UI-SPEC §A verbatim):
       - change the signature to `{ pr, projectId }: { pr: PRSummary; projectId: number }`.
       - add `const navigate = useNavigate();` (from "react-router") and `const openReview = useOpenReview(projectId);`.
       - root div: add `hover:bg-[#27272a] cursor-pointer`, `role="button"`, `tabIndex={0}`, `aria-label={\`Open review for PR #${pr.number}\`}`, `focus-visible:ring-2 focus-visible:ring-blue-500`, and an `onClick`/`onKeyDown` (Enter/Space) that calls:
         ```ts
         const open = () => {
           if (openReview.isPending) return;
           openReview.mutate(pr.number, {
             onSuccess: (res) => navigate(`/projects/${projectId}/tasks/${res.task.id}`),
           });
         };
         ```
         Guard double-click via `openReview.isPending` (in-flight guard, RESEARCH Pitfall 5). Optionally dim the card or show "Setting up the review worktree…" while pending (UI-SPEC §F — discretion).
       - ↗ `<a>` (lines 57-65): add `onClick={(e) => e.stopPropagation()}` so it opens GitHub only (D-02).
       - checks dot: leave non-interactive (no stopPropagation — a click falls through to open).
       - if openReview.isError, show a small inline `text-xs text-red-500` "Couldn't open this review." near the card (UI-SPEC §F error).

    4. web/src/components/board/ReviewColumn.tsx — thread projectId to the cards. ReviewStates already receives data/prs; add a `projectId` prop and pass it where ReviewColumnInner renders `<ReviewStates …>` and where it maps `<PRCard key={pr.number} pr={pr} projectId={projectId} />`.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/api/types.ts Task contains `source:`, `pr_number:`, `pr_base_ref:`
    - web/src/api/pullRequests.ts contains `export function useOpenReview` posting to `pull-requests/${n}/review`
    - web/src/components/board/PRCard.tsx root has `role="button"`, `cursor-pointer`, `hover:bg-[#27272a]`, `focus-visible:ring-2`, and calls `openReview.mutate(pr.number`
    - PRCard's ↗ anchor onClick calls `e.stopPropagation()` (NEGATIVE GUARD: the checks dot does NOT call stopPropagation)
    - ReviewColumn passes `projectId` to PRCard
    - `npm run build` (tsc + vite) succeeds
  </acceptance_criteria>
  <done>The PR card body is a keyboard-reachable open trigger that routes to the review; ↗ still opens GitHub only; the Task type and the open mutation exist.</done>
</task>

<task type="auto">
  <name>Task 2: TaskPage source branches + seeded AgentTab + read-only Description</name>
  <read_first>
    - web/src/pages/TaskPage.tsx (the file you modify — title button 432-457, ⋯ menu 461-485, WorktreeMetaLine 488-492, AgentTab 303-311, DescriptionTab 314-321, default tab line 82)
    - web/src/components/task/AgentTab.tsx (pasteApiRef at 38, onReady at 201-203, Insert description paste at 133 — the exact seed mechanism)
    - web/src/components/task/DescriptionTab.tsx (the prose block 62-66 + empty state 53 to render read-only)
    - .planning/phases/12-open-a-review/12-UI-SPEC.md §B (read-only identity + omit ⋯), §C (PR meta line), §D (read-only Description), §E (seeded Start)
  </read_first>
  <action>
    The review view IS TaskPage with `task.source === 'github_pr'` branches. The PR detail (title/body/author/url/base) comes from the open endpoint's `pr` object — cache it. Approach: after useOpenReview succeeds (Task 1) the card has the `pr` detail; the simplest reliable transport is to stash it in the query client keyed by task id at open time, then have TaskPage read it; OR re-fetch on TaskPage mount via the open endpoint (RESEARCH OQ1 recommends re-fetch on reattach to avoid stale). Implement: in useOpenReview onSuccess (Task 1), also `queryClient.setQueryData(["pr-detail", res.task.id], res.pr)`. In TaskPage, read `const prDetail = useQueryClient().getQueryData<PRDetailWire>(["pr-detail", taskId])` (or a small useQuery that calls the open endpoint POST on mount when task.source==='github_pr' and no cached detail — discretion; the load-bearing requirement is the header/body come from the live PR, not task.title/description). Document the choice in the SUMMARY.

    Then branch TaskPage on `const isPR = task.source === "github_pr";`:

    1. Title (UI-SPEC §B): when isPR, replace the editable-title button (lines 432-457) with a static element:
       ```tsx
       <span
         className="min-w-0 flex-1 truncate text-base font-medium px-1 py-0.5 text-left"
         title={prDetail?.title ?? task.title}
       >
         {prDetail?.title ?? task.title}
       </span>
       ```
       (NO hover:bg-muted/50, NO onClick, NO "Edit title", NOT a button.)

    2. ⋯ menu (UI-SPEC §B / D-10): when isPR, render NOTHING in the DropdownMenu slot (omit the entire `<DropdownMenu>…</DropdownMenu>`).

    3. Meta line (UI-SPEC §C / D-09): when isPR, replace `<WorktreeMetaLine …>` with a PR meta line:
       ```tsx
       <div className="flex min-w-0 items-center gap-2 text-xs text-muted-foreground">
         <span>#{task.pr_number}</span>
         <span>·</span>
         <span>@{prDetail?.author}</span>
         <span>·</span>
         <span className="font-mono">{prDetail?.baseRefName ?? task.pr_base_ref}</span>
         <span>·</span>
         <a href={prDetail?.url} target="_blank" rel="noreferrer"
            aria-label={`Open PR #${task.pr_number} on GitHub`}
            className="text-muted-foreground hover:text-foreground">
           <ExternalLink className="size-3.5" />
         </a>
       </div>
       ```
       (import ExternalLink from "lucide-react".) Keeping a subordinate WorktreeMetaLine row beneath is optional (UI-SPEC §C discretion) — if kept, do NOT show Create/Retry for a PR (the worktree always exists). Simplest: show only the PR meta line.

    4. Description (UI-SPEC §D / D-11): when isPR, render the PR body read-only. Parameterize DescriptionTab with an optional read-only markdown source, OR branch in TaskPage and pass the PR body. Add an optional prop to DescriptionTab, e.g. `readOnlySource?: string` — when provided, render the prose block with that markdown and NO Edit/Textarea/Save; empty → "No description." in `text-muted-foreground` with NO Edit button. In TaskPage pass `readOnlySource={prDetail?.body ?? ""}` when isPR.

    5. Seeded Start (UI-SPEC §E / D-06/D-07): in AgentTab, when the task is a PR review, after the agent session connects, do a ONE-SHOT paste of the seed into the prompt (prefill, never submit).
       - pass the seed into AgentTab: TaskPage already passes `task` and `projectId`; add a `seed?: string` prop. In TaskPage compute `const seed = isPR ? \`Review PR #${task.pr_number} "${prDetail?.title ?? task.title}". Summarize the changes, then flag bugs, risky changes, and missing tests.\` : undefined;` and pass it.
       - in AgentTab, add a `oneShotSeededRef = useRef(false)`. In the running TerminalPane's `onConnect` (handleConnect) — or a dedicated effect when the session transitions to running — if `seed` is set and `!oneShotSeededRef.current`, call `pasteApiRef.current?.paste(seed)` and set the ref true. This mirrors the Insert-description paste (bracketed-paste → un-submitted). It fires exactly once per mount. Do NOT auto-submit.

    6. Default tab + Diff tab: UNCHANGED (D-04). Confirm `useState("agent")` (line 82) and the `task.worktree_path` diff gate are left as-is — the PR worktree exists by render time so Diff is enabled automatically.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/TaskPage.tsx defines `task.source === "github_pr"` (or an `isPR` const from it) and branches the title, ⋯ menu, meta line, and Description on it
    - The PR title renders as a non-interactive element with `min-w-0 flex-1 truncate text-base font-medium px-1 py-0.5 text-left` (NEGATIVE GUARD: the PR title element has NO onClick and is NOT a `<button>` and has NO "Edit title")
    - The ⋯ DropdownMenu is OMITTED when isPR
    - The PR meta line contains `#{...pr_number}`, `@{...author}`, a `font-mono` base, and an ExternalLink ↗ to the PR url
    - web/src/components/task/DescriptionTab.tsx renders read-only PR body (no Edit button, no Textarea) when given the read-only source; empty → "No description." with NO Edit button
    - web/src/components/task/AgentTab.tsx pastes the seed via `pasteApiRef.current?.paste(seed)` exactly once after connect (guarded by a ref); NEGATIVE GUARD: it does NOT call any submit/Enter and does NOT auto-send
    - The seed string interpolates the PR number and title and matches `Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The review view shows a read-only PR title + meta line, omits the ⋯ menu, renders the read-only PR body, and prefills (never sends) the canned seed on agent Start — all gated on source==='github_pr', with the manual task view unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end safety checkpoint — open a real PR</name>
  <read_first>
    - .planning/ROADMAP.md § "Phase 12: Open-a-Review" (the 5 success criteria incl. the checkout-safety + diff-matches-GitHub tests)
    - .planning/phases/12-open-a-review/12-RESEARCH.md §2 (the verified main-HEAD-unchanged property) and §4 (the board-leak guarantee)
  </read_first>
  <action>
    No code in this task. Tasks 1-2 (and Plans 01-04) automated the entire flow. This is a BLOCKING human-verify gate: the executor builds and starts the app (`make build`, or `go build ./... && (cd web && npm run build)`, then run the binary) and hands off to the user to run the how-to-verify steps below on a real PR. The executor MUST NOT self-approve — wait for the user's resume signal, then record the outcome in the SUMMARY.
  </action>
  <what-built>
    The full open-a-review flow: PR card body click → backend provisions a detached worktree on the PR head (refs/pull/<n>/head, --detach, pinned to headRefOid) → routes to the task-like review view with a read-only PR title, PR meta line, read-only PR-body Description, immediately-available PR-base Diff tab, and a seeded (prefilled-not-sent) agent Start. PR reviews are filtered off the board (source='manual' on all board/position queries + /move 409). Reopening reattaches to the same workspace.
  </what-built>
  <how-to-verify>
    Build and run the app against a project linked to a real GitHub repo where you have review-requested PRs (or temporarily link to a public repo like cli/cli and use a known PR number). Steps:

    1. `cd /home/jordi/workspace/github/kangent && make build` (or `go build ./... && (cd web && npm run build)`), then run the binary; open the board for the linked project and expand the Review column.
    2. Note the project's PRIMARY checkout HEAD first: `git -C <project-repo> rev-parse HEAD` and `git -C <project-repo> symbolic-ref --short HEAD`. Record both.
    3. Click a PR card BODY (not the ↗). Confirm: it routes to a review view; the title is the PR title (read-only — no edit affordance on hover); the meta line shows `#num · @author · base · ↗`; there is NO ⋯ menu; the Description tab shows the PR body read-only (or "No description.").
    4. **CHECKOUT SAFETY (GHREV-05):** re-run `git -C <project-repo> rev-parse HEAD` and `symbolic-ref --short HEAD`. BOTH MUST be IDENTICAL to step 2 (the primary checkout did NOT move). Bonus: open a FORK PR or a PR whose branch name collides with a local branch and re-confirm HEAD unchanged.
    5. **DIFF MATCHES GITHUB (GHREV-03):** open the Diff tab. Compare the changed-files list against GitHub's "Files changed" tab for that PR (especially a PR targeting a NON-default base). They MUST match.
    6. **NOT ON BOARD (GHREV-04):** go back to the board. The PR review MUST NOT appear as a To Do / In Progress / In Review / Done card. Drag a real manual task around — its positioning behaves normally.
    7. **REATTACH (GHREV-02):** click the same PR card again. It MUST route to the SAME review (no duplicate worktree directory under the worktree base, no second tab/row).
    8. **SEED (D-06/D-07):** in the review's Agent tab, click Start agent. After the session connects, the prompt MUST be PREFILLED with `Review PR #<n> "<title>". Summarize the changes, then flag bugs, risky changes, and missing tests.` and MUST NOT be auto-submitted (you press Enter yourself).
  </how-to-verify>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go build ./... && (cd web && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - Primary checkout HEAD + branch unchanged after opening a PR (and ideally a fork/colliding-name PR) — GHREV-05
    - Kangent's review Diff changed-files match GitHub's Files-changed for the PR (incl. a non-default-base PR) — GHREV-03
    - The PR review never renders as a kanban card; manual task positioning is normal — GHREV-04
    - Reopening the PR reattaches (no duplicate worktree/row) — GHREV-02
    - The agent Start prefills the canned seed without auto-sending — D-06/D-07
  </acceptance_criteria>
  <resume-signal>Type "approved" if all five checks pass, or describe which check failed (with the observed vs expected) so a gap-closure plan can be created.</resume-signal>
  <done>The user has signed off all five end-to-end checks (or filed specific failures for gap closure); the outcome is recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds (tsc + vite, no type errors).
- `go build ./...` succeeds.
- Human checkpoint signed off: main HEAD unchanged + diff matches GitHub + not on board + reattach + seeded Start.
</verification>

<success_criteria>
- GHREV-01..05 verified end-to-end by a human on a real PR: open-as-detached-worktree, reattach, PR-base diff, off-the-board, and primary-checkout safety.
</success_criteria>

<output>
After completion, create `.planning/phases/12-open-a-review/12-05-SUMMARY.md`.
</output>
