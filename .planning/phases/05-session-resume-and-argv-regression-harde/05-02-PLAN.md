# 05-02: opencode resume argv + engine-gated resumable flag + fake-opencode argv guard

<objective>
opencode resume argv + engine-gated resumable flag + fake-opencode argv guard.
</objective>

<tasks>
- [ ] **T02**: opencode resume argv + engine-gated resumable flag + fake-opencode argv guard _(M)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
