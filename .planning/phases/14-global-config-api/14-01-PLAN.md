---
phase: 14-global-config-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/global.go
  - internal/api/global_test.go
  - cmd/kamacu/serve.go
autonomous: true
requirements: [GCONF-01, GCONF-02, GCONF-03, GCONF-04]
must_haves:
  truths:
    - "GET /api/global returns the singleton config (root_path, github_repo as nullable string, agent_id, updated_at) plus derived state (root_exists via os.Stat, live{agent,bash,tmux} counts, agent{id,name,engine} summary) — the resume-id columns NEVER serialize on any response (D-17/D-18/D-19/D-22)"
    - "PUT is a single partial endpoint with pointer fields root_path/repo/agent_id — omitted = untouched; non-empty repo → managed variant, non-empty root_path → folder variant, both supplied → 400, root_path:\"\" → the clear (D-20); PUT returns the GET shape (D-23)"
    - "PUT with a non-empty root_path validates the directory via validateRepoPath (git repo required, D-05) and persists it as the folder root; $HOME (any spelling), /, and anything inside ~/.kamacu are rejected 400 (GCONF-01, D-07/D-26/D-27)"
    - "PUT with a non-empty repo gh-validates and clones into ~/.kamacu/repos/global/<owner>/<name>; an existing dir with a matching origin reattaches without re-cloning; a failed clone returns one inline error leaving the PRIOR config and no directory (GCONF-02, D-01/D-02/D-04, SC2 atomicity)"
    - "PUT with agent_id sets the global task's default agent (unknown id 400 'agent not found'); agent-only PUTs never hit the 409 gate and never clear resume ids (GCONF-03, D-24/D-25)"
    - "Any root-field PUT (change or clear) with a live global tmux session returns 409 {error, reasons:[{kind,target}]} with row and disk untouched; the gate runs BEFORE any gh/clone work (GCONF-04, D-13/D-14/D-15)"
    - "Every successful root change — including the clear and the same-repo reattach re-PUT — clears claude_session_id/opencode_session_id in the SAME single UPDATE (D-16, D-21)"
  artifacts:
    - internal/api/global.go
    - internal/api/global_test.go
    - cmd/kamacu/serve.go (one api.GlobalRoutes registration line in the registry block)
  key_links:
    - "cmd/kamacu/serve.go route registry -> api.GlobalRoutes(mux, db, mgr, tmuxClient) -> GET/PUT /api/global"
    - "global.go folder branch -> validateRepoPath (internal/api/projects.go:78) + deleteBlocker 409 grammar (internal/api/projects.go:686)"
    - "global.go managed branch -> github.ParseRepoRef/ValidateRepo/Clone + reattachManaged (internal/api/projects.go:456) + reposBase const (projects.go:355)"
    - "globalLiveBlockers -> SELECT name FROM tmux_sessions WHERE scope='global' + tmux.Client.HasSession exact-match probe (alive only on (true,nil))"
---

<objective>
Build the complete global config API surface — GET/PUT /api/global in a new internal/api/global.go — covering all four GCONF requirements at the API level: the config+derived-state GET (D-17/D-18/D-19), the validated folder root (GCONF-01), the gh-validated managed clone into the global namespace with reattach and atomic failure (GCONF-02), the settable default agent (GCONF-03), and the forward-wired live-session 409 gate with resume-id clearing (GCONF-04).

Purpose: Phase 15's spawn path and Phase 16's Settings UI both consume this endpoint; it must be curl-complete and test-covered before either exists. This is "the v1.4 createByRepo sequence with UPDATE swapped for INSERT" — every mechanism is an in-repo composition (zero new dependencies, locked).

Output: internal/api/global.go (handler + gate), internal/api/global_test.go (harness + unit/seam tests), one serve.go registration line. Route pair GET /api/global + PUT /api/global live on the real server.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-global-data-foundation-safety-net/13-CONTEXT.md
@.planning/phases/14-global-config-api/14-CONTEXT.md
@.planning/phases/14-global-config-api/14-RESEARCH.md
@.planning/phases/14-global-config-api/14-PATTERNS.md
@internal/api/projects.go
@internal/store/migrations/00017_global_task.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire types, GlobalRoutes constructor, GET handler, serve.go wiring, test harness, GET tests</name>
  <files>internal/api/global.go, internal/api/global_test.go, cmd/kamacu/serve.go</files>
  <read_first>
  - internal/api/global.go (if it exists from a prior attempt — otherwise net-new)
  - internal/api/sessions.go lines 27-56 (SessionRoutes constructor shape — the pattern to copy)
  - internal/api/projects.go lines 23-72 (Project struct nullable-repo convention + projectHandlers dependency struct), lines 633-639 (scanProject ErrNoRows idiom)
  - internal/api/projects.go lines 964-985 (liveTmuxNames — the exact-probe analog the tmux count helper adapts)
  - internal/api/respond.go (writeJSON/writeError — use these, do not re-implement)
  - internal/api/projects_test.go lines 31-57 and 431-498 (newRepoTestServer HOME sandbox, newTestServer skeleton, gitRepo, doJSON)
  - internal/api/global_backfill.go + internal/store/migrations/00017_global_task.sql (singleton storage contract; 00013 seeds the default agent in-migration so a fresh migrated test DB already has the singleton)
  - cmd/kamacu/serve.go lines 272-288 (route registry block — where the new line goes)
  - 14-PATTERNS.md sections "Routes constructor + handler struct", "Wire type", "GET handler"
  </read_first>
  <action>
  Create internal/api/global.go (package api) with:

  1. Wire types (D-17/D-22 — exact JSON keys): globalAgent struct with fields ID (json "id"), Name (json "name"), Engine (json "engine"); globalLive struct with Agent, Bash, Tmux int fields (json "agent", "bash", "tmux"); globalConfig struct with RootPath string (json "root_path"), GithubRepo *string (json "github_repo" — nil means folder root, the 00017 marker contract; NO managed bool), AgentID int64 (json "agent_id"), UpdatedAt string (json "updated_at"), RootExists bool (json "root_exists"), Live globalLive (json "live"), Agent globalAgent (json "agent"). The struct has NO claude_session_id/opencode_session_id fields anywhere — D-18 makes the resume ids Phase-15 spawn-path internals that never serialize.

  2. globalHandlers struct holding db (*sql.DB), mgr (*session.Manager), tmuxClient (tmux.Client). Take mgr NOW even though the manager half of the gate returns zeros — Phase 15's ListGlobal() widens the helper without a signature change (research A2).

  3. GlobalRoutes(mux *http.ServeMux, db *sql.DB, mgr *session.Manager, tmuxClient tmux.Client) constructing a globalHandlers value and registering ONLY "GET /api/global" -> g.get at this stage (Task 2 adds the PUT registration).

  4. liveGlobalTmuxNames(ctx) []string on globalHandlers — the liveTmuxNames analog with the predicate swapped: query SELECT name FROM tmux_sessions WHERE scope = 'global', then for each name call h.tmuxClient.HasSession(ctx, name) verbatim (never hand-roll has-session; the "="+name exact-match prefix is embedded in Client.HasSession); a name is live ONLY on (true, nil) — (false, err) is inconclusive and NEVER read as alive (D-13 probe contract). On QueryContext error return nil; on scan error continue (mirror the liveTmuxNames degrade posture exactly).

  5. get handler: single QueryRow SELECT g.root_path, g.github_repo, g.agent_id, g.updated_at, a.id, a.name, a.engine FROM global_task g JOIN agents a ON a.id = g.agent_id WHERE g.id = 1 (INNER JOIN is safe: agent_id is NOT NULL ON DELETE RESTRICT per 00017 and the Phase-13 delete guard refuses deleting the referenced agent). Scan into the config plus a sql.NullString for github_repo; valid -> GithubRepo pointer. ErrNoRows maps to 500 "global task row missing" (fail-loud on the corrupted invariant — the row is guaranteed by 00017 seed + BackfillGlobalTask; GET must not paper over it); any other error is 500 err.Error(). Derive RootExists as RootPath != "" plus an inline os.Stat IsDir check (the api package has NO dirExists helper — do not import internal/migrate). Derive Live.Tmux as len(liveGlobalTmuxNames(ctx)); Live.Agent and Live.Bash are literal zeros with a comment marking the Phase-15 ListGlobal() seam (D-19 — the fields ship now so the wire contract is stable day one). Respond 200 via writeJSON.

  6. cmd/kamacu/serve.go: add api.GlobalRoutes(mux, db, mgr, tmuxClient) as one line in the registry block alongside api.SessionRoutes/api.WorktreeRoutes (all deps already in scope; tmuxClient built at line ~269). Do NOT touch routes.go — per-resource registration lives in serve.go (established convention).

  7. internal/api/global_test.go — harness: newGlobalTestServer(t) mirroring newRepoTestServer (t.Setenv HOME to t.TempDir() so ExpandHome and the D-27 block resolve into the sandbox; store.Open + store.Migrate on a temp db path; mux := http.NewServeMux(); GlobalRoutes(mux, db, session.NewManager(), tmux.Client{}); httptest.NewServer; t.Cleanup closes server and db) returning (*httptest.Server, *sql.DB). Also newGlobalTestServerWithTmux(t, c tmux.Client) — same skeleton with the explicit client (mirrors newWorktreeServerWithTmux) for Task 2's gate tests and Plan 02. Reuse doJSON and gitRepo from projects_test.go (same package). No backfill calls needed — 00013 seeds the default agent and 00017 seeds the singleton in-migration.

  8. GET tests: TestGetGlobalUnconfigured — fresh server, GET returns 200 with root_path "", github_repo JSON null, root_exists false, live zeros, agent.id matching the is_default agent row (query the DB for it), agent.engine "claude", updated_at non-empty; assert the decoded body map has NO keys named claude_session_id or opencode_session_id (D-18 wire assertion). TestGetGlobalConfiguredRoot — hand-UPDATE global_task SET root_path to a gitRepo(t) fixture path, GET shows root_exists true and github_repo null; then UPDATE root_path to a non-existent path, GET shows root_exists false (D-08 complement — honest stat, no revalidation).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestGetGlobal' -count=1 && grep -q 'func GlobalRoutes' internal/api/global.go && grep -q '"GET /api/global"' internal/api/global.go && grep -q 'api.GlobalRoutes(mux, db, mgr, tmuxClient)' cmd/kamacu/serve.go</automated>
  </verify>
  <done>GET /api/global serves the full D-17 wire shape from a migrated fresh DB; serve.go registers the route; both GET tests pass; no resume-id key can appear on the wire.</done>
  <acceptance_criteria>
  - internal/api/global.go contains "func GlobalRoutes(mux *http.ServeMux, db *sql.DB, mgr *session.Manager, tmuxClient tmux.Client)"
  - internal/api/global.go contains a globalConfig struct whose JSON tags are exactly root_path, github_repo, agent_id, updated_at, root_exists, live, agent
  - internal/api/global.go contains "WHERE scope = 'global'" (the liveGlobalTmuxNames predicate) and "global task row missing" (the ErrNoRows 500 message)
  - cmd/kamacu/serve.go contains "api.GlobalRoutes(mux, db, mgr, tmuxClient)"
  - TestGetGlobalUnconfigured and TestGetGlobalConfiguredRoot exit 0 via go test -run 'TestGetGlobal' -count=1
  - TestGetGlobalUnconfigured asserts the absence of both resume-id keys in the decoded response body
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: globalLiveBlockers 409 gate + PUT dispatch grammar, folder variant, clear, agent field, single final UPDATE</name>
  <files>internal/api/global.go, internal/api/global_test.go</files>
  <read_first>
  - internal/api/global.go (current state after Task 1)
  - internal/api/projects.go lines 495-629 (partial-PATCH pointer idiom, agent-existence 400, SET-builder + unconditional updated_at bump)
  - internal/api/projects.go lines 78-101 (validateRepoPath — returns the cleaned absolute path the footgun checks must run against)
  - internal/api/projects.go lines 681-689 and 848-855 (deleteBlocker struct + the 409 write shape)
  - internal/api/workspaces.go lines 142-152 (the "nothing to update" all-nil 400 precedent)
  - internal/settings/home.go (settings.ExpandHome for the D-27 block anchor)
  - 14-RESEARCH.md Pattern 3 (dispatch trap), Pitfalls 1/2/5/6 (dispatch edges, atomicity, HasPrefix form, equality-on-expanded-abs)
  - 14-PATTERNS.md sections "PUT — pointer decode + dispatch grammar", "Single final UPDATE", "409 gate — grammar + liveness probe"
  </read_first>
  <action>
  Extend internal/api/global.go:

  1. globalLiveBlockers(ctx) []deleteBlocker on globalHandlers — the forward-wired GCONF-04 gate: iterate liveGlobalTmuxNames(ctx) and append deleteBlocker{Kind: "sessions", Target: name} for each (reuse the existing deleteBlocker struct from projects.go verbatim — the structured {kind, target} grammar is the locked reading of D-15 per research Open Question 1; the 13-CONTEXT string sketch was illustrative). The manager half is a documented zero-count seam: add a comment that Phase 15's Info.Global + ListGlobal() widens this helper, and that no global PTY can exist until then (D-19). The gate only COUNTS — it never stops anything.

  2. PUT handler g.put (register "PUT /api/global" in GlobalRoutes now): decode the request struct with RootPath *string (json "root_path") and AgentID *int64 (json "agent_id") — the repo field lands in Task 3, do not add it yet (keeps every commit coherent; D-22 fixes the names).

  3. Branch FIRST, validate AFTER the branch knows what it holds (Pitfall 1 — a naive validate-then-branch sends root_path:"" into validateRepoPath and 400s "path must be absolute" in the wrong branch): all fields nil -> 400 "nothing to update" (workspaces precedent, locks the empty-body discretion); then rootSupplied := req.RootPath != nil.

  4. Gate (GCONF-04): if rootSupplied and len(globalLiveBlockers(r.Context())) > 0 -> writeJSON 409 with map {"error": "the Scratchpad root can't be changed while sessions are running", "reasons": blockers} and return — NOTHING mutated, and this check runs before any validation or disk work (D-13: any live global PTY blocks; D-14: exited sessions and persisted resume ids never block — the probe is live-only by construction; the error copy uses the Phase-13-locked "Scratchpad" label, D-09).

  5. Variants (only when rootSupplied): *req.RootPath == "" is THE CLEAR (D-21) — newRoot "" and github_repo NULL, no path validation; non-empty is the FOLDER variant — abs, err := validateRepoPath(*req.RootPath) with err -> 400 err.Error() (D-05 verbatim semantics, D-26: NO project-overlap guard beyond this), then the D-07 footgun equality set on the POST-expansion absolute value (Pitfall 6): abs == os.UserHomeDir() result or abs == "/" -> 400 (one comparison catches "~", "~/" and the literal home dir), then the D-27 block: base from settings.ExpandHome("~/.kamacu"), reject when abs == base or strings.HasPrefix(abs, base + string(os.PathSeparator)) -> 400 with copy that explains WHY (Kamacu-managed machinery under the data dir gets cleaned up by its owners — a task worktree by gated cleanup, a managed clone by its project — so a global root there is always a trap, never a choice). Folder variant sets newRoot = abs AND github_repo = NULL (Pitfall 8 — never leave a stale managed marker).

  6. Agent field (D-24/D-25): if req.AgentID != nil, SELECT 1 FROM agents WHERE id = ? — ErrNoRows -> 400 "agent not found", other error -> 500. NO session gate on agent changes (a live global session keeps the agent it was spawned with; the change applies at the next Start) and NO resume-id touching (ids are engine-keyed, not agent-keyed).

  7. Single final UPDATE (never split — Pitfall 2 anti-pattern): build sets/args with the projects.go SET-builder idiom; when rootSupplied append root_path = ?, github_repo = ? (clear -> "" and NULL; folder -> abs and NULL) AND unconditionally claude_session_id = NULL, opencode_session_id = NULL (D-16 — ANY successful root change clears the ids, no no-op-re-PUT exception); when agent supplied append agent_id = ?; always append updated_at = strftime('%Y-%m-%dT%H:%M:%fZ','now'); WHERE id = 1. Then re-read the row through the same load path as GET (factor a shared helper so get() and put() build the identical D-23 shape) and respond 200 — not 201, this is an update of a guaranteed row.

  8. Tests in global_test.go (no tmux needed — the gate trivially passes with zero rows; the live-tmux trip test is Plan 02's heavyweight): TestPutGlobalFolder — PUT root_path = gitRepo(t) -> 200, response shows root_path equal to the returned abs, github_repo null, root_exists true, updated_at bumped vs the pre-PUT GET. TestPutGlobalFootguns — table over: the literal "$HOME" path string, os.UserHomeDir() itself, "/", the sandbox "~/.kamacu", "~/.kamacu/repos/foo" (expanded forms), and a plain non-git temp dir — each PUT -> 400 with the singleton row byte-identical before/after (read root_path/agent_id via db both times). TestPutGlobalClear — PUT a folder root first, then PUT root_path "" -> 200 and a follow-up GET shows root_path "", github_repo null, root_exists false. TestPutGlobalAgent — PUT agent_id of a row inserted into agents (INSERT a second agent) -> 200 with agent summary reflecting the new agent; PUT an unknown id (e.g. 99999) -> 400 "agent not found". TestPutGlobalEmptyBody — PUT {} -> 400 "nothing to update". TestPutGlobalRootChangeClearsResumeIDs — hand-UPDATE global_task SET claude_session_id='c1', opencode_session_id='o1', then PUT a folder root -> both ids NULL afterwards (query the DB); then seed ids again and PUT agent_id only -> both ids STILL set (D-16 is the only clearing path; D-25 forbids agent-only clearing).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestPutGlobal(Folder|Footguns|Clear|Agent|EmptyBody|RootChangeClearsResumeIDs)' -count=1 && grep -q '"PUT /api/global"' internal/api/global.go && grep -q 'claude_session_id = NULL' internal/api/global.go && grep -q 'nothing to update' internal/api/global.go</automated>
  </verify>
  <done>The full root_path/agent_id PUT grammar works end-to-end: folder persist, footgun 400s, clear, agent set/400, id-clearing exactly on root changes, gate helper in place — all covered by passing tests.</done>
  <acceptance_criteria>
  - internal/api/global.go contains "func (h *globalHandlers) globalLiveBlockers" returning []deleteBlocker, and contains "claude_session_id = NULL" plus "opencode_session_id = NULL" inside the single UPDATE SET list
  - internal/api/global.go contains the 409 write with "reasons" mapped to the blocker list, and "nothing to update" plus "agent not found" 400 messages
  - The D-27 rejection triggers on both abs == base and the base + separator prefix form; the D-07 check compares the post-expansion absolute path
  - TestPutGlobalFootguns asserts row-untouched after each 400; TestPutGlobalRootChangeClearsResumeIDs asserts ids NULL after a root PUT and STILL SET after an agent-only PUT
  - All six named tests exit 0 via the automated command
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Managed-clone variant — repo dispatch, 8-step atomic ordering, reattach, seam tests</name>
  <files>internal/api/global.go, internal/api/global_test.go</files>
  <read_first>
  - internal/api/global.go (current state after Task 2 — the dispatch gains the repo arms here)
  - internal/api/projects.go lines 357-478 (createByRepo full 8-step ordering + reattachManaged verbatim — the exact semantics D-04 reuses; discretion allows calling the package-level func as-is)
  - internal/api/projects.go lines 482-485 (msgRepoNotFound / msgGHUnavailable degrade copy constants — already package-level, reuse them)
  - internal/github/testhooks.go (SetValidateRunnerForTest / SetCloneRunnerForTest / SetAvailableForTest — each returns a restore func; call via defer restore())
  - internal/github/github.go lines 40+ (ParseRepoRef — syntax + leading-dash rejection) and the ValidateRepo/Clone package functions
  - 14-RESEARCH.md Pattern 4 (the adapted 8 steps with the gate hoisted to step 0), Pitfall 2 (atomicity), the namespace path-math paragraph
  - 14-PATTERNS.md sections "PUT managed variant", "Reattach — verbatim reuse"
  </read_first>
  <action>
  Extend the PUT in internal/api/global.go:

  1. Add Repo *string (json "repo") to the request struct (D-22 — matches the v1.4 create dispatch field exactly; GET keeps github_repo, the intentional asymmetry). Dispatch arms, evaluated with the existing branch-first grammar: repo non-empty (strings.TrimSpace) AND root_path non-empty -> 400 "supply either repo or root_path, not both"; repo non-nil AND TrimSpace-empty -> 400 with guidance "repo must be owner/name; to clear the root send root_path \"\"" (locks research Open Question 2 — explicitness beats silent ignore, and avoids a second clear spelling); non-empty repo routes to the managed flow and makes rootSupplied true (so the Task-2 gate covers it — the gate MUST run before any gh/clone work; a clone takes minutes, never discover a 409 after one).

  2. Managed flow (inline or a helper mirroring createByRepo's shape), in this exact order: ParseRepoRef(repoInput) -> 400 err.Error() (the only hard host-independent reject, and the argv-injection guard); github.ValidateRepo -> err -> 400 err.Error(), not-verified -> 400 with msgRepoNotFound, or msgGHUnavailable when !github.Available() (degrade-don't-break copy, chosen exactly like projects.go:375-382); dest := filepath.Join(base, "global", canonical) where base comes from settings.ExpandHome(reposBase) — the D-02 three-segment namespace ~/.kamacu/repos/global/<owner>/<name> (structurally uncollidable with the two-segment project clones; "global" is a reserved pseudo-owner per 00017's in-line contract); if os.Stat(dest) succeeds -> reattachManaged(r.Context(), dest, canonical) called VERBATIM (same package, package-level func) -> error -> 409 err.Error() (never clobber, D-04); else github.Clone(r.Context(), canonical, dest) -> error -> _ = os.RemoveAll(dest) (belt-and-braces; Clone self-cleans at clone.go:50) + ONE inline 500 err.Error() and return — the singleton KEEPS ITS PRIOR VALUE (that is the SC2 atomicity: no half-configured row, no directory).

  3. Only after exit-0 (or reattach) does the single final UPDATE run: root_path = dest, github_repo = canonical, plus the D-16 id clears and updated_at (Task 2's SET machinery already builds this — the managed variant just supplies newRoot = dest and a NON-NULL github_repo). Agent_id rides the same UPDATE when supplied. Respond 200 + the GET shape (D-23 — curl shows the clone/reattach outcome immediately).

  4. Tests (all via the exported github test seams — unit tests never need live gh; wrap each in defer restore()): TestPutGlobalManagedClone — SetValidateRunnerForTest returning canonical "octo/widgets" verified, SetCloneRunnerForTest succeeding (and capturing dest) -> PUT repo "Octo/Widgets" -> 200; GET shows root_path equal to the captured dest (must live under the HOME sandbox at .kamacu/repos/global/octo/widgets) and github_repo "octo/widgets"; root_exists reflects whether the fake created the dir (have the fake git-init the dest so reattach tests can build on it). TestPutGlobalManagedValidateFail — validate fake not-verified -> 400 msgRepoNotFound; same with SetAvailableForTest(false) -> 400 msgGHUnavailable. TestPutGlobalManagedCloneFailureAtomic — pre-configure a folder root via PUT, then a clone fake returning an error -> 500, follow-up GET shows the OLD folder root byte-identical (root_path, github_repo null) and os.Stat(dest) fails (no residue). TestPutGlobalManagedReattach — after a successful fake clone (dir is a real git repo with origin https://github.com/octo/widgets.git — the fake can git init + git remote add), a second PUT with the same repo and a must-NOT-be-called clone fake (the failCloneNeverCalled pattern from projects_test.go:49-57) -> 200 reattach, same root_path, clone fake never invoked (D-04). TestPutGlobalManagedReattachMismatch — existing dest whose origin points at a different repo -> 409 with reattachManaged's "a different repository is already checked out" message. TestPutGlobalRepoDispatch — table: PUT with both repo "o/w" and root_path "/x" -> 400 "supply either repo or root_path, not both"; PUT with repo "" -> 400 with the guidance copy; PUT with a syntactically invalid ref (no slash, e.g. "notaref") -> 400 from ParseRepoRef.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestPutGlobalManaged|TestPutGlobalRepoDispatch' -count=1 && grep -q 'filepath.Join(base, "global", canonical)' internal/api/global.go && grep -q 'reattachManaged(r.Context(), dest, canonical)' internal/api/global.go</automated>
  </verify>
  <done>The managed variant clones into the global namespace, reattaches on same-repo re-PUT without re-cloning, fails atomically leaving prior config and no directory, and the full repo/root_path dispatch grammar is test-covered.</done>
  <acceptance_criteria>
  - internal/api/global.go contains the dest computation joining "global" with the canonical ref, a call to reattachManaged, and a call to github.Clone followed by an os.RemoveAll on the error path
  - The gate call precedes ParseRepoRef/ValidateRepo/Clone on every managed-path entry (code order: gate -> parse -> validate -> dest -> reattach-or-clone -> UPDATE -> respond)
  - TestPutGlobalManagedCloneFailureAtomic asserts the prior root_path/github_repo survive and the dest path does not exist
  - TestPutGlobalManagedReattach asserts the clone runner was never called on the second PUT
  - All managed + dispatch tests exit 0 via the automated command; the full file suite go test ./internal/api/ -run 'TestGlobal|TestGetGlobal|TestPutGlobal' -count=1 is green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl/browser -> PUT /api/global | Untrusted-ish JSON body crosses into path computation, gh argv, and DB writes (localhost single-user, but the body is still attacker-adjacent input per the package posture) |
| gh/git CLI | Repo refs flow into arg arrays of external binaries |
| ~/.kamacu/repos/global/ | First write to a new on-disk namespace Kamacu owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering/Elevation | PUT folder branch (footgun root) | medium | mitigate | D-07 equality set on the post-expansion abs path ($HOME any spelling, /) + D-27 ~/.kamacu prefix block + git-repo requirement via validateRepoPath (Task 2) |
| T-14-02 | Tampering | repo ref -> gh/git argv | medium | mitigate | github.ParseRepoRef leading-dash/syntax rejection; all shell-outs are arg arrays, never sh -c (Task 3) |
| T-14-03 | Spoofing | reattach to wrong repo via ref confusion | medium | mitigate | reattachManaged verbatim: origin read + case-insensitive canonical compare + refuse on any mismatch, never clobber (Task 3) |
| T-14-04 | Tampering | clone residue / half-configured singleton | medium | mitigate | 8-step ordering: gate-early, validate-before-clone, UPDATE strictly after exit-0, RemoveAll belt-and-braces (Task 3) |
| T-14-05 | Information Disclosure | resume ids on the wire | low | mitigate | D-18: the wire struct has no resume-id fields — enforced by the TestGetGlobalUnconfigured key-absence assertion (Task 1) |
| T-14-06 | Tampering | gate bypass via misread tmux probe | medium | mitigate | Client.HasSession exact-match "="+name embedded; alive counted ONLY on (true, nil); inconclusive probes never block-read as alive-or-dead (Task 2) |

No package installs occur in this phase (Go, zero new dependencies — locked), so the package-legitimacy gate is not applicable.
</threat_model>

<verification>
- go test ./internal/api/ -run 'TestGetGlobal|TestPutGlobal' -count=1 — the full global suite green
- go build ./... and go vet ./internal/api/ ./cmd/kamacu/ clean
- grep -q 'api.GlobalRoutes(mux, db, mgr, tmuxClient)' cmd/kamacu/serve.go — production wiring present
- The dispatch grammar, footgun blocks, atomicity, reattach, id-clearing, and gate are each covered by at least one named test (see acceptance criteria)
</verification>

<success_criteria>
- GET /api/global returns the D-17 wire shape (config + root_exists + live counts + agent summary) with resume ids never serializable (D-18)
- PUT folder/clear/agent/repo all behave per D-20/D-21/D-22/D-24 with the 409 gate (D-13/D-15) guarding every root change and D-16 clearing riding the single UPDATE
- The managed clone lands in ~/.kamacu/repos/global/<owner>/<name> (D-01/D-02), reattaches on same-repo re-PUT (D-04), and fails atomically (SC2)
- Zero new dependencies; routes.go untouched; all existing package tests still green
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by THIS plan (14-01):
- internal/api/global.go (NEW file): GlobalRoutes(mux *http.ServeMux, db *sql.DB, mgr *session.Manager, tmuxClient tmux.Client); type globalHandlers{db, mgr, tmuxClient}; type globalConfig{RootPath, GithubRepo *string, AgentID, UpdatedAt, RootExists, Live, Agent} with JSON keys root_path/github_repo/agent_id/updated_at/root_exists/live/agent; type globalLive{Agent, Bash, Tmux}; type globalAgent{ID, Name, Engine}; handlers get/put (+ managed flow); helper globalLiveBlockers(ctx) []deleteBlocker; helper liveGlobalTmuxNames(ctx) []string; shared load helper behind the GET-shape responses
- internal/api/global_test.go (NEW file): newGlobalTestServer, newGlobalTestServerWithTmux, TestGetGlobalUnconfigured, TestGetGlobalConfiguredRoot, TestPutGlobalFolder, TestPutGlobalFootguns, TestPutGlobalClear, TestPutGlobalAgent, TestPutGlobalEmptyBody, TestPutGlobalRootChangeClearsResumeIDs, TestPutGlobalManagedClone, TestPutGlobalManagedValidateFail, TestPutGlobalManagedCloneFailureAtomic, TestPutGlobalManagedReattach, TestPutGlobalManagedReattachMismatch, TestPutGlobalRepoDispatch
- cmd/kamacu/serve.go: one added registration line api.GlobalRoutes(mux, db, mgr, tmuxClient)
- HTTP routes: GET /api/global, PUT /api/global
- On-disk namespace first written: ~/.kamacu/repos/global/<owner>/<name>/

Created by plan 14-02 (verification only, no new production symbols): additional tests TestPutGlobalRootBlockedByLiveTmux, TestPutGlobalConfigHistory (+ no-reclone and no-no-op-exception assertions) in internal/api/global_test.go.

Reused, NOT new (exclude from drift verification): validateRepoPath, reattachManaged, deleteBlocker, reposBase, msgRepoNotFound/msgGHUnavailable, github.ParseRepoRef/ValidateRepo/Clone/Available, settings.ExpandHome, tmux.Client.HasSession, writeJSON/writeError, BackfillGlobalTask, migrations 00017/00018.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-global-config-api/14-01-SUMMARY.md` when done
</output>
