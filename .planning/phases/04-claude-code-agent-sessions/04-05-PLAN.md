---
phase: 04-claude-code-agent-sessions
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - internal/api/agent_integration_test.go
  - internal/api/testdata/fake-claude
autonomous: false
requirements: [TERM-01, STAT-01, STAT-02]
must_haves:
  truths:
    - "An end-to-end Go test drives spawn → hook POSTs → status transitions → attach-clears-waiting → stop → exited(143, stopRequested) against a fake-claude stub"
    - "The test proves DELETE /api/tasks/{id} stops running sessions and that hook POSTs without the token are rejected"
    - "A human has verified the real claude v2.1.170 flow: TUI fidelity, amber-on-permission-prompt, clean reattach redraw, insert-paste, Start again"
  artifacts:
    - path: "internal/api/agent_integration_test.go"
      provides: "full agent lifecycle integration test"
      contains: "fake-claude"
    - path: "internal/api/testdata/fake-claude"
      provides: "claude stand-in for CI (no auth/API usage)"
      contains: "exit 143"
  key_links:
    - from: "agent_integration_test.go"
      to: "Manager.SetAgentConfig ClaudeBin override"
      via: "stub script path injection"
      pattern: "ClaudeBin"
---

<objective>
Prove the whole phase: a Go integration test covering the agent lifecycle and both security/leak gap fixes with a fake-claude stub, full build gates, and human verification of the four roadmap success criteria against the real claude v2.1.170 binary.

Purpose: phase 4 sign-off — TERM-01, STAT-01, STAT-02 demonstrably true.
Output: green test suite + human-approved live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-claude-code-agent-sessions/04-RESEARCH.md
@.planning/phases/04-claude-code-agent-sessions/04-01-SUMMARY.md
@.planning/phases/04-claude-code-agent-sessions/04-02-SUMMARY.md
@internal/api/integration_test.go

<interfaces>
fake-claude stub (research "Code Examples" — copy verbatim, then extend):
```bash
#!/usr/bin/env bash
# testdata/fake-claude: enough to exercise spawn/status/teardown.
printf '\x1b[?2004h'           # bracketed paste on (like real claude)
echo "fake claude ready"
trap 'exit 143' TERM            # mirrors verified SIGTERM behavior
sleep 300 & wait                # idle until stopped
```
Inject via session.AgentConfig{ClaudeBin: <abs path>} (plan 04-01) — never spawn real claude in tests (auth + API cost).

Phase 3 integration test conventions (internal/api/integration_test.go): outer-t fixture git repo, httptest server with the full route set, real SQLite in t.TempDir().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end agent lifecycle integration test</name>
  <files>internal/api/agent_integration_test.go, internal/api/testdata/fake-claude</files>
  <read_first>internal/api/integration_test.go (full — fixture setup: repo on outer t, route registration, worktree provisioning flow), internal/session/agent.go (AgentConfig), internal/api/hooks.go (receiver contract), 04-RESEARCH.md "Test stub" + Pitfall 3/4</read_first>
  <action>
    Commit `internal/api/testdata/fake-claude` exactly as the research stub (above), executable bit set (`git update-index --chmod=+x` or create with 0o755; the test should also `os.Chmod` defensively since git perms can drop).

    `internal/api/agent_integration_test.go` — one lifecycle test on the Phase 3 fixture pattern (outer-t git repo, full mux: Routes + SessionRoutes + WorktreeRoutes + HookRoutes + AgentRoutes, AgentConfig{BaseURL: test server URL, Token: "test-token", ClaudeBin: abs path to testdata/fake-claude}). Stages, sequential subtests:

    1. **spawn**: create project + task (worktree provisions); `POST /api/sessions {"task_id":N,"kind":"agent"}` → 201; body has label "Agent", kind "agent", agentStatus "working". Assert `tasks.claude_session_id` is a parseable uuid (direct DB query).
    2. **one-per-task**: second agent spawn → 409 body contains "agent session already running".
    3. **hook token (gap #2)**: `POST /api/hooks/sessions/{id}` with no token → 401; wrong token → 401; correct token + `{"hook_event_name":"SessionStart","session_id":"<uuid>"}` → 204.
    4. **waiting via hook**: correct token + `{"hook_event_name":"Notification","notification_type":"permission_prompt"}` → 204; `GET /api/agents/status` shows this task with status "waiting", correct taskId/projectId.
    5. **attach clears waiting (D-45)**: open the WS (`/api/sessions/{id}/ws`, loopback Origin) — after attach, agents status shows "idle" (allow a brief poll loop ≤2s).
    6. **stop → gray semantics (gap #3)**: `POST /api/sessions/{id}/stop` → 202; wait for exit (poll status ≤7s, covering the 5s grace — fake-claude exits in ~0s on TERM); agents status entry: status "exited", exitCode 143, stopRequested true (the client maps this to the muted-gray dot, never red).
    7. **fresh restart**: third spawn now → 201 (exited agent no longer blocks); claude_session_id overwritten with a DIFFERENT uuid; agents status shows the NEW session id with "working" (newest-wins).
    8. **task-delete stops sessions (gap #1)**: spawn a bash session for the task too; `DELETE /api/tasks/{id}` → 204; assert no running sessions remain for the task (`GET /api/sessions?task_id=N` has zero status=="running" entries).

    Use the established marker discipline where output assertions are needed (quote-splitting so PTY echo can't satisfy them). Keep the whole test serial on one fixture task where possible; budget < 30s wall time.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run TestAgentLifecycle -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/api/ -run TestAgentLifecycle -count=1` exits 0
    - `test -x internal/api/testdata/fake-claude` exits 0
    - `grep -n 'exit 143' internal/api/testdata/fake-claude` returns a match
    - `grep -n '401' internal/api/agent_integration_test.go` and `grep -n 'stopRequested' internal/api/agent_integration_test.go` and `grep -n 'DELETE' internal/api/agent_integration_test.go` all return matches (all three research gaps asserted)
    - `grep -rn 'LookPath("claude")\|"claude"' internal/api/agent_integration_test.go | grep -v fake-claude` shows no real-claude invocation
  </acceptance_criteria>
  <done>The full agent lifecycle and all three research gap fixes are locked in by a deterministic test that never touches the real claude binary.</done>
</task>

<task type="auto">
  <name>Task 2: Full build gates</name>
  <files>(none — verification only)</files>
  <read_first>Makefile (build orchestration), web/package.json (script names)</read_first>
  <action>
    Run the complete gate set and fix anything red (fixes belong to the owning files; report what changed):
    - `go vet ./...`
    - `go test ./... -count=1` (entire backend including ws + cmd suites)
    - `go test ./internal/session/ -race -count=1` (the status fields added mutex-guarded state — prove it)
    - `cd web && npm run build` (tsc + vite)
    - `make build` (or the repo's documented single-binary build) — the embedded binary must still assemble
  </action>
  <verify>
    <automated>go vet ./... && go test ./... -count=1 && (cd web && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` exits 0
    - `go test ./... -count=1` exits 0
    - `go test ./internal/session/ -race -count=1` exits 0
    - `cd web && npm run build` exits 0
    - `git diff go.mod web/package.json` shows no new dependencies (phase contract: zero new deps)
  </acceptance_criteria>
  <done>Everything compiles, tests pass race-clean, the single binary builds, and the zero-new-dependencies contract holds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification with real claude v2.1.170</name>
  <files>(none)</files>
  <read_first>.planning/ROADMAP.md Phase 4 success criteria, 04-RESEARCH.md Open Questions 1-2 + Pitfall 7</read_first>
  <action>
    Build and launch the app for the user, then present the verification checklist below and pause: `make build && ./kangent` (or `go run ./cmd/kangent --dev-origin localhost:5173` + `cd web && npm run dev`), app at http://127.0.0.1:7333. Confirm the server started and a project with a worktree-backed task exists (create one if needed) before handing off.
  </action>
  <what-built>Phase 4 complete: Start button spawns real claude in the task worktree with injected status hooks; live dots on cards/tab/sidebar; clean detach/reattach; insert-description paste; stop/start-again lifecycle.</what-built>
  <how-to-verify>
    1. **Start + TUI fidelity (SC-1)**: Open a task that has a worktree → lands on the Agent tab → pre-start shows `No agent session` with the branch in mono → click `Start agent`. Claude's TUI renders; type text; try a slash command (`/help`); enter plan mode (shift+tab twice). Everything behaves as in a native terminal.
    2. **Hooks → amber (SC-2/SC-3, research OQ1)**: Ask claude to run a command that needs permission (e.g. "run ls -la"). When the permission prompt appears, go to the board: within ~5s the card dot is pulsing amber, the card border is amber-tinted, the sidebar shows an amber `1` chip, and the Agent tab dot pulses. Tooltip reads `Waiting for input`. ALSO verify plan-mode's exit-plan approval flips amber (this is the unverifiable-without-API open question — report if it does NOT).
    3. **Attach clears waiting (D-45)**: Open the task's Agent tab → dot returns to gray idle (board within one poll). Answer the prompt; while claude works the dot is green; ~10s after output stops it goes gray idle. Finishing a turn must NOT leave a green or amber dot (Stop hook → idle; no spurious working flash).
    4. **Detach/reattach (SC-4)**: With claude mid-conversation, close the browser tab entirely. Reopen the task: history is readable, the prompt box is intact (brief replay flicker OK, corruption is not). Resize the window while detached, reattach — clean repaint.
    5. **Insert description (D-35)**: On a task WITH a description: `Insert description` appears in the pane header → click → the text appears in claude's prompt box, NOT submitted, editable; click again — it repeats. On a task with an empty description the button is absent.
    6. **Stop / Start again (D-41)**: Click `Stop` → `Stopping…` → banner `Session exited (code 143)` with only `Start again` (no Close). Card dot is MUTED GRAY (not red — stop-initiated). `Start again` → fresh claude session (no prior conversation). Kill claude from inside (`/exit`) → exited(0) muted gray dot.
    7. **Cleanup gate (D-42)**: With the agent running, open Clean up worktree → dialog counts it in "N sessions running"; `Stop sessions and clean up` stops claude too; Agent tab then shows the no-worktree pre-start state.
    8. **Bell fallback sanity (STAT-02)**: nothing to click — confirm the server log shows no hook-pipeline warning during a normal session (SessionStart canary received). Known caveat to accept: a repo NEVER opened in claude before shows the trust dialog in the terminal without an amber dot (Pitfall 7, documented v1 behavior).
  </how-to-verify>
  <verify>Human confirms all 8 checklist items against the live app with real claude v2.1.170</verify>
  <resume-signal>Type "approved" or describe which step failed and what you saw</resume-signal>
  <done>All four roadmap success criteria observed against the real binary; open questions (plan-mode Notification coverage) answered empirically.</done>
</task>

</tasks>

<verification>
- Integration test green and deterministic (run twice: `-count=2`)
- All build gates green
- Human approval recorded for the live checklist
</verification>

<success_criteria>
- TERM-01: Start button → interactive claude in the worktree PTY, full TUI fidelity (human-verified)
- STAT-01: card dots track working/idle/waiting/exited live (test + human-verified)
- STAT-02: hook-driven detection with token security and bell fallback wiring (test-verified), no leaked sessions on task delete
</success_criteria>

<output>
After completion, create `.planning/phases/04-claude-code-agent-sessions/04-05-SUMMARY.md`
</output>
