---
phase: 18-project-icon-data-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/types.ts
  - web/src/api/mutations.ts
autonomous: true
requirements: [ICON-04]
must_haves:
  truths:
    - "D-12: the TS Project interface mirrors the Go wire shape with icon_letters: string and icon_color: string (non-null)"
    - "D-12: useUpdateProjectSettings carries optional icon_letters + icon_color in its PATCH payload, only sent when defined"
  artifacts:
    - path: "web/src/api/types.ts"
      provides: "Project interface with icon_letters + icon_color string fields"
      contains: "icon_letters: string"
    - path: "web/src/api/mutations.ts"
      provides: "useUpdateProjectSettings carrying icon_letters? + icon_color? optional payload"
      contains: "icon_letters"
  key_links:
    - from: "web/src/api/mutations.ts useUpdateProjectSettings"
      to: "PATCH /api/projects/{id}"
      via: "conditional payload build - omitted field never sent (matches backend partial-PATCH)"
      pattern: "icon_letters"
---

<objective>
Extend the frontend wire path so Phase 19's editors have a typed path to read and write the two icon fields: add `icon_letters` + `icon_color` to the TS `Project` interface, and carry them as optional, only-sent-when-defined fields in the `useUpdateProjectSettings` PATCH mutation. NO UI in this phase - just the type + mutation surface.

Purpose: Mirror the backend wire shape (D-12) so Phase 19 can render avatars from `icon_letters`/`icon_color` and save edits through `useUpdateProjectSettings` without re-touching the API layer.
Output: modified `web/src/api/types.ts`, modified `web/src/api/mutations.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-project-icon-data-foundation/18-CONTEXT.md
@.planning/phases/18-project-icon-data-foundation/18-PATTERNS.md

<interfaces>
<!-- Backend wire shape this MUST mirror (D-12, locked - matches Plan 01/02 Go json tags): -->
  icon_letters: string   // two uppercase chars (or 1, or "?"), NOT NULL on the wire (never null)
  icon_color: string     // a lowercase curated-palette hex like "#dc2626", NOT NULL on the wire

<!-- Existing types.ts Project interface (line 12): has github_repo: string | null; managed: boolean; created_at; updated_at -->
<!-- Existing mutations.ts useUpdateProjectSettings (line 32): mutationFn({id, description, github_repo?})
       -> patch<Project>(`/api/projects/${id}`, github_repo === undefined ? {description} : {description, github_repo})
       The github_repo optional-field "undefined means omit" branch is the EXACT pattern to extend. -->
<!-- No frontend test framework: verify via `cd web && npm run build` (tsc -b + vite build) + `npm run lint`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon fields to Project type + useUpdateProjectSettings payload</name>
  <files>web/src/api/types.ts, web/src/api/mutations.ts</files>
  <read_first>
    - web/src/api/types.ts lines 12-24 (the Project interface - the managed/github_repo fields show the field shape + comment style to mirror)
    - web/src/api/mutations.ts lines 32-54 (useUpdateProjectSettings - the github_repo `undefined`-means-omit conditional payload branch to extend)
    - .planning/phases/18-project-icon-data-foundation/18-PATTERNS.md lines 372-413 (touch points for types.ts + mutations.ts with exact before/after excerpts)
  </read_first>
  <action>
    In `web/src/api/types.ts`, add to the `Project` interface (between `managed: boolean;` and `created_at: string;`) two non-null fields: `icon_letters: string;` and `icon_color: string;` with a brief comment noting they are the v1.7 icon identity (two uppercase letters + a curated-palette hex like `#dc2626`), both NOT NULL on the wire (never null) - mirror the `managed` field's comment style. These mirror the Go json tags from Plan 01/02 (D-12).

    In `web/src/api/mutations.ts`, extend `useUpdateProjectSettings` (line 32) to carry two new OPTIONAL fields. Add `icon_letters?: string;` and `icon_color?: string;` to the `mutationFn` argument type. Then change the body-building expression so that each defined field is included and each `undefined` field is omitted - replace the two-branch `github_repo === undefined ? {description} : {description, github_repo}` ternary with a conditionally-assembled payload object (e.g. start from `{ description }` and conditionally add `github_repo`, `icon_letters`, `icon_color` only when each is not `undefined`), so an omitted field is NEVER sent (matching the backend partial-PATCH contract D-09 where omitted = untouched). Keep the existing `onSuccess` invalidation of `["projects"]` unchanged. Do NOT add any UI, swatch picker, or input component - that is Phase 19; this only establishes the typed wire path.
  </action>
  <verify>
    <automated>cd web && grep -q "icon_letters: string" src/api/types.ts && grep -q "icon_color: string" src/api/types.ts && grep -q "icon_letters?: string" src/api/mutations.ts && grep -q "icon_color?: string" src/api/mutations.ts && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/api/types.ts` Project interface contains `icon_letters: string` and `icon_color: string` (non-null, between managed and created_at)
    - `web/src/api/mutations.ts` useUpdateProjectSettings arg type includes `icon_letters?: string` and `icon_color?: string`
    - The PATCH body is assembled so an `undefined` icon_letters/icon_color/github_repo is never sent (no field forced onto the body when undefined)
    - `cd web && npm run build` exits 0 (tsc -b + vite build pass)
    - `cd web && npm run lint` exits 0 (no new lint errors introduced)
  </acceptance_criteria>
  <done>The Project type and useUpdateProjectSettings mutation expose the two icon fields with the correct optional/omit semantics; the web build and lint pass; no UI added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS client -> PATCH body | The mutation assembles the JSON PATCH body sent to the backend (which re-validates server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | mutation PATCH payload | accept | Frontend payload is convenience only; the backend (Plan 02 validateIconLetters/validateIconColor) is the authoritative enforcer - any client-side value is re-validated server-side, so a tampered TS payload cannot persist an off-palette color or empty letters |
| T-18-11 | Information Disclosure | typed icon fields | accept | icon_letters/icon_color are non-sensitive display data (a monogram + a hex from a fixed palette); no PII, local single-user app |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages added (only edits to existing types.ts/mutations.ts); package.json unchanged, slopcheck not triggered |
</threat_model>

<verification>
- `cd web && npm run build` exits 0 (TypeScript compiles with the extended Project type + mutation)
- `cd web && npm run lint` exits 0 (no new lint errors)
- Type mirror check: `icon_letters: string` + `icon_color: string` present in types.ts (matches the Go wire shape from Plans 01/02)
- Payload omit semantics: an undefined icon field is not forced onto the PATCH body (manual read of the assembled object + the backend partial-PATCH contract)
</verification>

<success_criteria>
- The TS `Project` interface carries icon_letters + icon_color as non-null strings matching the backend wire shape (ICON-04 wire path)
- `useUpdateProjectSettings` can send either, both, or neither icon field (omitted = untouched), giving Phase 19's editors a ready PATCH path (ICON-04)
- Frontend build + lint stay green; no UI introduced this phase
</success_criteria>

<output>
Create `.planning/phases/18-project-icon-data-foundation/18-03-SUMMARY.md` when done
</output>
