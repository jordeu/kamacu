---
phase: 13-global-data-foundation-safety-net
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
- internal/store/migrations/00017_global_task.sql
- internal/store/migrations/00018_tmux_scope.sql
- internal/store/global_task_migration_test.go
autonomous: true
requirements:
- GDATA-01
- GDATA-02
must_haves:
  truths:
  - "A DB staged at goose v16 (pre-00017 v1.12 shape) with seeded projects/tasks/tmux_sessions rows (custom labels + exact created_at literals) upgrades through the real Migrate() with every tmux_sessions row byte-for-byte identical — id, task_id, n, name, label, created_at exact-string compared (GDATA-02, ROADMAP SC1)"
  - "After upgrade, global_task holds exactly one row: id=1, agent_id = the agents row with is_default=1 (never a hardcoded id), root_path='', github_repo/claude_session_id/opencode_session_id NULL (GDATA-01)"
  - "INSERT INTO global_task with id=2 is rejected by CHECK(id=1); DELETE of the agent it references is rejected by ON DELETE RESTRICT (GDATA-01)"
  - "tmux_sessions accepts a task-less row (task_id NULL, scope='global') and rejects BOTH ambiguous (task_id set + scope='global') AND neither (task_id NULL + scope='task'); every copied pre-existing row reads scope='task' (GDATA-02, ROADMAP SC3)"
  - "PRAGMA foreign_keys returns 1 on the migrated handle — the NO TRANSACTION rebuild re-armed the single pooled connection"
  - "A second Migrate(db) is a clean no-op (goose version-table idempotence)"
  artifacts:
  - internal/store/migrations/00017_global_task.sql
  - internal/store/migrations/00018_tmux_scope.sql
  - internal/store/global_task_migration_test.go
  key_links:
  - "goose.UpTo(db, 'migrations', 16) staging → seed → Migrate(db) exercises the REAL embedded migrations (migrationsFS in internal/store/migrate.go) — the same goose runner boot uses"
  - "00018's trailing PRAGMA foreign_keys = ON re-arms the single pooled connection opened by store.Open's foreign_keys(1) DSN + SetMaxOpenConns(1) (internal/store/store.go:12-24) — without it, FK enforcement goes nondeterministic"
---

<objective>
Create the two Phase-13 migrations and prove them with the repo's staged-upgrade test pattern: 00017 establishes the `global_task` singleton (DB-enforced id=1, agent FK ON DELETE RESTRICT, resume-id columns, seeded from the default agent), and 00018 rebuilds `tmux_sessions` so task-less global rows can exist (nullable `task_id` + explicit `scope` discriminator + XOR CHECK) while every existing row, label, and FK discipline survives byte-for-byte (GDATA-01, GDATA-02).

Purpose: This is the data foundation every later v1.13 phase builds on. The representation is research-locked (CONTEXT carry-forward): a `global_task` singleton table + task-free "global scope" sessions — never a sentinel project/task row, never `TaskID = 0`. The rebuild technique is locked: CREATE-copy-drop-rename under `PRAGMA foreign_keys=OFF` / `NO TRANSACTION` (the 00012/00013 discipline, one notch heavier).

Output: two new goose migrations + one migration test file (`TestGlobalTaskMigration`, `TestTmuxScopeRebuildMigration`) proving the upgrade path on a seeded v1.12-shape install. No Go runtime code changes in this plan — no API routes, no frontend, no spawn paths (CONTEXT phase boundary).

Planner resolutions of the agent's-discretion items (per CONTEXT "the agent's Discretion"): migration split = **two migrations 00017+00018** (research recommendation — keeps the NO TRANSACTION discipline scoped to the one migration that needs it; this exact split is what the research rehearsal validated); scope discriminator = **`scope TEXT NOT NULL DEFAULT 'task' CHECK (scope IN ('task','global'))`** (self-documenting, verified XOR pairing); resume-id storage = **two separate nullable columns** `claude_session_id`, `opencode_session_id` (the rehearsal-validated spelling, per research assumption A1).
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-global-data-foundation-safety-net/13-CONTEXT.md
@.planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md
@.planning/phases/13-global-data-foundation-safety-net/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations 00017_global_task.sql and 00018_tmux_scope.sql</name>
  <files>internal/store/migrations/00017_global_task.sql, internal/store/migrations/00018_tmux_scope.sql</files>
  <read_first>
  - .planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md (Pattern 1 + Pattern 2 — the exact SQL was probe-verified 15/15 on the pinned modernc v1.52.0 + goose v3.27.1 and rehearsal-verified 11/11 on a copy of the real install; lift it, do not redesign; also read "Anti-Patterns to Avoid" and Pitfalls 2-4)
  - .planning/phases/13-global-data-foundation-safety-net/13-PATTERNS.md (assignments for both migrations — header/comment discipline + the critical deviations from the 00012/00013 analogs)
  - internal/store/migrations/00012_workspaces.sql (the NO TRANSACTION + FK-off/re-arm discipline 00018 copies)
  - internal/store/migrations/00013_agents.sql (the seeded-table + RESTRICT FK shape 00017 mirrors — but see the critical seed deviation below)
  - internal/store/migrations/00005_tmux_sessions.sql (the exact table shape being rebuilt — every column/constraint must survive)
  </read_first>
  <action>
  Create `internal/store/migrations/00017_global_task.sql` — plain `-- +goose Up` transactional migration (fresh CREATE TABLE; the FK-off annotation belongs to 00018 only, do NOT copy 00013's NO TRANSACTION header). WHY-header comment citing GDATA-01, why the seed runs in-migration, and that the managed-root path format is `~/.kamacu/repos/global/<owner>/<name>` (D-02 — Phase 14 writes it into root_path/github_repo; this phase only bakes the storage contract). Table `global_task` with columns exactly: `id INTEGER PRIMARY KEY CHECK (id = 1)`, `root_path TEXT NOT NULL DEFAULT ''` ('' = unconfigured), `github_repo TEXT` (NULL ⇒ folder root, NOT NULL ⇒ managed clone), `agent_id INTEGER NOT NULL REFERENCES agents(id) ON DELETE RESTRICT`, `claude_session_id TEXT`, `opencode_session_id TEXT`, `created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))`, `updated_at` same shape. Seed with `INSERT INTO global_task (id, agent_id) SELECT 1, id FROM agents WHERE is_default = 1` — NEVER hardcode agent id 1 (13-RESEARCH Pitfall 4: the real install has 4 agents and POST /api/agents/{id}/default moves the flag; 00013's hardcoded DEFAULT was safe only because its own seed created id 1 in the same migration). `-- +goose Down` → `DROP TABLE global_task;` (plain transaction — nothing references it).

  Create `internal/store/migrations/00018_tmux_scope.sql` — `-- +goose NO TRANSACTION` with the WHY header copied from 00012's discipline (foreign_keys can only be toggled OUTSIDE a transaction; store.go pins one pooled connection via SetMaxOpenConns(1), so the trailing re-arm is load-bearing). Lift the verified 12-step body verbatim from 13-RESEARCH.md Pattern 2, in the sqlite.org-blessed ordering create-new → copy → drop-old → rename-new (renaming the old table away FIRST is the documented way to corrupt references — never do it): `PRAGMA foreign_keys = OFF;` then `BEGIN;` then `CREATE TABLE tmux_sessions_new` with `id INTEGER PRIMARY KEY`, `task_id INTEGER REFERENCES tasks(id)` (nullable now — global rows have no task), `scope TEXT NOT NULL DEFAULT 'task' CHECK (scope IN ('task','global'))`, `n INTEGER NOT NULL`, `name TEXT NOT NULL UNIQUE`, `label TEXT NOT NULL DEFAULT ''`, `created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))`, `UNIQUE(task_id, n)`, and the XOR CHECK using the exact verified spelling `CHECK ( (task_id IS NULL) = (scope = 'global') )`; then `INSERT INTO tmux_sessions_new (id, task_id, scope, n, name, label, created_at) SELECT id, task_id, 'task', n, name, label, created_at FROM tmux_sessions;` then `DROP TABLE tmux_sessions;` then `ALTER TABLE tmux_sessions_new RENAME TO tmux_sessions;` then `COMMIT;` then `PRAGMA foreign_key_check;` (non-gating parity — goose ignores its rows, keep the 00012/13 comment posture) then `PRAGMA foreign_keys = ON;`. `-- +goose Down` mirrors 00012's reverse discipline: same FK-off/BEGIN/COMMIT/re-arm wrapper, rebuilding the old NOT-NULL-task_id shape and copying back only rows WHERE task_id IS NOT NULL (reverting scope support inherently drops global rows — acceptable for a dev-only Down).

  Spelling warnings (verified failure modes, 13-RESEARCH Pitfall 3): the inverted XOR `(task_id IS NOT NULL) = (scope='global')` rejects every task row at copy time, and an OR-spelling accepts everything silently — use the verified spelling above unchanged. Do not put the foreign_keys toggles inside BEGIN/COMMIT (no-op inside a transaction, probe B2). Do not modify 00005 or any other existing migration file.
  </action>
  <verify>
    <automated>grep -c "CHECK (id = 1)" internal/store/migrations/00017_global_task.sql && grep -c "is_default = 1" internal/store/migrations/00017_global_task.sql && grep -c -- "-- +goose NO TRANSACTION" internal/store/migrations/00018_tmux_scope.sql && grep -c "(task_id IS NULL) = (scope = 'global')" internal/store/migrations/00018_tmux_scope.sql && grep -c "PRAGMA foreign_keys = ON" internal/store/migrations/00018_tmux_scope.sql && grep -c "tmux_sessions_new" internal/store/migrations/00018_tmux_scope.sql && git status --short internal/store/migrations/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
  - internal/store/migrations/00017_global_task.sql exists and contains: `CREATE TABLE global_task`, column names root_path, github_repo, agent_id, claude_session_id, opencode_session_id, `CHECK (id = 1)`, `REFERENCES agents(id) ON DELETE RESTRICT`, and the seed `SELECT 1, id FROM agents WHERE is_default = 1` (source assertion — no literal agent id in the seed)
  - 00017's first goose annotation is exactly `-- +goose Up` (plain transaction; the FK-off annotation appears only in 00018)
  - internal/store/migrations/00018_tmux_scope.sql exists and contains: `-- +goose NO TRANSACTION`, `task_id    INTEGER REFERENCES tasks(id)` (NOT NULL dropped), `scope TEXT NOT NULL DEFAULT 'task' CHECK (scope IN ('task','global'))`, the exact XOR spelling `CHECK ( (task_id IS NULL) = (scope = 'global') )`, `UNIQUE(task_id, n)`, the create-new/copy/drop-old/rename-new sequence, `PRAGMA foreign_keys = OFF;` before BEGIN, and `PRAGMA foreign_keys = ON;` after COMMIT
  - Both files have `-- +goose Down` sections
  - `git status --short internal/store/migrations/` shows exactly 2 new untracked files and zero modifications to existing migrations
  - Both files' copy SELECT preserves id, task_id, n, name, label, created_at (no column omitted or reordered in the new-table INSERT column list)
  </acceptance_criteria>
  <done>Both migrations exist with the exact verified shapes; no existing migration touched; ready to be proven by the Task 2 staged-upgrade test.</done>
</task>

<task type="auto">
  <name>Task 2: Staged-upgrade migration test (TestGlobalTaskMigration + TestTmuxScopeRebuildMigration)</name>
  <files>internal/store/global_task_migration_test.go</files>
  <read_first>
  - internal/store/agents_migration_test.go (THE analog — mirror the staging discipline exactly: Open(t.TempDir()) with real DSN, goose.SetBaseFS(migrationsFS), SetDialect("sqlite3"), UpTo staging, sqlite_master pre-checks, seeded survival data, post-Migrate assertions, FK pragma probe, second-Migrate idempotence)
  - internal/store/migrate.go (migrationsFS embed + Migrate — what the test drives)
  - internal/store/migrations/00017_global_task.sql and 00018_tmux_scope.sql (the migrations being proven — from Task 1)
  - .planning/phases/13-global-data-foundation-safety-net/13-RESEARCH.md (Pattern 3 — the test sketch for this exact phase; "Common Pitfalls" 2/3 for why both XOR rejection directions and the pragma probe are mandatory)
  - internal/store/workspaces_migration_test.go (second instance of the staging shape, for the seed-column details the staged v16 schema requires)
  </read_first>
  <action>
  Create `internal/store/global_task_migration_test.go` (package store) with a small shared staging helper plus two test functions, each with its own staged DB:

  Helper: opens store.Open(filepath.Join(t.TempDir(), "test.db")), sets goose.SetBaseFS(migrationsFS) + SetDialect("sqlite3"), runs goose.UpTo(db, "migrations", 16) to stage the pre-00017 v1.12 schema, and asserts via sqlite_master that global_task does NOT exist yet (the staging sanity pre-check, agents_migration_test.go:39-47 pattern).

  TestGlobalTaskMigration: stage at 16, pre-check, then Migrate(db), then assert — (a) exactly one global_task row; (b) its id == 1 and agent_id == SELECT id FROM agents WHERE is_default = 1 (on the staged fresh DB that is the claude seed, but query the flag — never assert the literal); (c) root_path == '' and github_repo/claude_session_id/opencode_session_id are NULL; (d) INSERT INTO global_task (id, agent_id) VALUES (2, ...) is rejected by CHECK(id=1); (e) DELETE FROM agents WHERE id = (SELECT agent_id FROM global_task) errors (ON DELETE RESTRICT — the delete-guard's DB backstop, agents_migration_test.go:175-179 pattern); (f) a second Migrate(db) is a clean no-op with still exactly one global_task row (goose version-table idempotence).

  TestTmuxScopeRebuildMigration: stage at 16, pre-check, then seed the "existing install" the way agents_migration_test.go seeds — one project via `INSERT INTO projects (name, repo_path, workspace_id) VALUES (?, ?, 1)` (repo_path unique, e.g. /tmp/gd-alpha), two tasks under it, and three tmux_sessions rows carrying CUSTOM labels and exact created_at literals (e.g. ('t1', 1, 'kamacu-1-1', 'Build', '2026-01-02T03:04:05.678Z'), one with label '' to cover the DEFAULT, one under the second task) — the byte-for-byte survival data. Snapshot BEFORE via `SELECT id, task_id, n, name, label, created_at FROM tmux_sessions ORDER BY id`. Migrate(db). Then assert — (a) the AFTER snapshot is exact-string identical row-for-row (byte-for-byte, SC1); (b) every copied row reads scope='task'; (c) INSERT of a global row (task_id NULL, scope='global', n=1, name='kamacu-global-1', label '') succeeds; (d) INSERT of the ambiguous row (task_id set + scope='global') FAILS; (e) INSERT of the neither row (task_id NULL + scope='task') FAILS — both rejection directions are mandatory (Pitfall 3: an inverted or OR spelling is only catchable this way); (f) INSERT with bogus task_id=999 (scope='task') FAILS — the FK is re-armed and bites on this same pooled handle; (g) PRAGMA foreign_keys scans 1 (the load-bearing re-arm canary, agents_migration_test.go:181-188 pattern — keep verbatim); (h) a second Migrate(db) is a clean no-op (row count unchanged).

  Follow the analog's error-handling idiom throughout: t.Fatalf on setup failures, t.Errorf on assertion failures, so one wrong assertion does not hide the rest.
  </action>
  <verify>
    <automated>go test ./internal/store/ -run 'TestGlobalTaskMigration|TestTmuxScopeRebuildMigration' -v && go test ./internal/store/</automated>
  </verify>
  <acceptance_criteria>
  - `go test ./internal/store/ -run 'TestGlobalTaskMigration|TestTmuxScopeRebuildMigration' -v` exits 0 with both tests PASSING (not skipping)
  - TestTmuxScopeRebuildMigration seeds at least 3 tmux rows including at least 2 with non-empty custom labels and exact created_at literals, and compares BEFORE/AFTER snapshots as exact strings (a COUNT-only comparison does not satisfy this)
  - Both XOR rejection directions (ambiguous AND neither) are asserted as failures, plus both acceptance directions (task row via the copy, global row via insert)
  - The PRAGMA foreign_keys == 1 probe runs on the same migrated *sql.DB handle
  - `go test ./internal/store/` (full package) exits 0 — no regression to TestWorkspacesMigration / TestAgentsMigration
  </acceptance_criteria>
  <done>Both migrations proven by the real goose runner against a seeded v1.12-shape DB: singleton invariants (one row, CHECK, RESTRICT, seed-from-default) and rebuild invariants (byte-for-byte survival, scope discriminator, XOR both directions, FK re-arm, idempotence) all hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema ← future writers | Every future Go writer (Phase 14 config API, Phase 15 spawn path) inserts through these constraints; the DB is the enforcement point, not caller discipline |
| Existing install data → migration | The upgrade path transforms a live v1.12 DB in place; pre-existing rows are the asset under protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering/Destruction | migrations 00017+00018 against live-install data | high | mitigate | sqlite.org 12-step rebuild ordering + staged-upgrade test with byte-for-byte snapshot diff (Task 2) + goose version-table idempotence + boot refuses on migration error (serve.go:124-127); the real-install rehearsal follows in Plan 13-02 |
| T-13-02 | Tampering | tmux_sessions shape (future buggy writers) | medium | mitigate | Table-level XOR CHECK + scope CHECK constraint reject ambiguous/neither rows at the DB regardless of caller (verified rc 275); both rejection directions asserted in Task 2 |
| T-13-03 | Tampering | global_task.agent_id reference | medium | mitigate | CHECK(id=1) makes a second singleton row unrepresentable; FK ON DELETE RESTRICT refuses deleting the referenced agent (asserted in Task 2) |
| T-13-04 | Tampering | FK enforcement after NO TRANSACTION rebuild | high | mitigate | Trailing PRAGMA foreign_keys = ON re-arms the pooled single connection (Pitfall 2); Task 2's pragma probe + bogus-FK insert assert the re-arm on the migrated handle |
| T-13-SC | Tampering | package installs | low | accept | Zero installs this phase — locked decision verified against go.mod/web/package.json (13-RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go build ./...` compiles (migrations are embedded via migrationsFS — the embed picks up the new files automatically)
- `go test ./internal/store/ -run 'TestGlobalTaskMigration|TestTmuxScopeRebuildMigration' -v` — both PASS
- `go test ./internal/store/` — full package green (existing migration tests unregressed)
- `git status --short internal/store/migrations/` — exactly two new files, zero existing migrations modified
</verification>

<success_criteria>
- GDATA-01 schema half proven: singleton row id=1 exists post-migration, seeded from the is_default agent; CHECK(id=1) and ON DELETE RESTRICT both bite at the DB
- GDATA-02 proven: task-less global rows representable, ambiguous rows rejected, every pre-existing tmux row (id, task_id, n, name, label, created_at) survives byte-for-byte through the real goose upgrade path
- FK enforcement deterministic post-rebuild (PRAGMA foreign_keys == 1 on the pooled handle)
- Second Migrate is a no-op (upgrade is safely re-runnable)
</success_criteria>

## Artifacts this phase produces (Plan 13-01)

New schema objects (created by migration 00017): table `global_task` with columns `id`, `root_path`, `github_repo`, `agent_id`, `claude_session_id`, `opencode_session_id`, `created_at`, `updated_at`; constraints `CHECK (id = 1)` and `agent_id → agents(id) ON DELETE RESTRICT`.
Schema changes (migration 00018): rebuilt table `tmux_sessions` — `task_id` now nullable, NEW column `scope TEXT NOT NULL DEFAULT 'task'`, new constraints `CHECK (scope IN ('task','global'))` and table-level `CHECK ( (task_id IS NULL) = (scope = 'global') )`; all prior columns/constraints (`n`, `name UNIQUE`, `label`, `created_at`, `UNIQUE(task_id, n)`) preserved.
New file paths: `internal/store/migrations/00017_global_task.sql`, `internal/store/migrations/00018_tmux_scope.sql`, `internal/store/global_task_migration_test.go`.
New Go test functions: `TestGlobalTaskMigration`, `TestTmuxScopeRebuildMigration` (plus their unexported staging helper).
No new API routes, no frontend files, no new dependencies, no runtime Go code.

<output>
Create `.planning/phases/13-global-data-foundation-safety-net/13-01-SUMMARY.md` when done
</output>
