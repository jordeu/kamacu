---
phase: 26-workspace-switcher-management-assignment
plan: 04
type: execute
wave: 3
depends_on: ["26-03"]
files_modified:
  - web/src/components/sidebar/WorkspaceNameDialog.tsx
  - web/src/components/sidebar/ManageWorkspacesDialog.tsx
  - web/src/components/sidebar/WorkspaceSwitcher.tsx
autonomous: true
requirements: [WSNAV-01, WSMGMT-01, WSMGMT-02, WSMGMT-03, WSMGMT-04]

must_haves:
  truths:
    - "A create/rename dialog takes a name, shows the server's duplicate-name error inline, and disables Save while pending or empty"
    - "A Manage-workspaces dialog lists every workspace with rename + delete affordances"
    - "Delete is disabled with a tooltip for the default workspace and for any non-empty workspace"
    - "The switcher trigger opens a dropdown listing workspaces (active marked with a check), a New-workspace entry, and a Manage-workspaces entry; picking a workspace sets it active and navigates to its first project"
  artifacts:
    - path: "web/src/components/sidebar/WorkspaceNameDialog.tsx"
      provides: "create/rename name dialog (D-04)"
      contains: "useCreateWorkspace"
    - path: "web/src/components/sidebar/ManageWorkspacesDialog.tsx"
      provides: "rename/delete hub with client-side count guards (D-03/D-05/D-06/D-07)"
      contains: "is_default"
    - path: "web/src/components/sidebar/WorkspaceSwitcher.tsx"
      provides: "expanded-only switcher trigger + dropdown (D-01/D-02) + switch navigation (D-11)"
      contains: "group-data-[collapsible=icon]:hidden"
  key_links:
    - from: "WorkspaceSwitcher"
      to: "useActiveWorkspace + useNavigate"
      via: "pick workspace → setActiveWorkspaceId + navigate to first project"
      pattern: "setActiveWorkspaceId"
    - from: "ManageWorkspacesDialog"
      to: "useProjects"
      via: "client-side per-workspace project count for the non-empty delete guard"
      pattern: "workspace_id"
---

<objective>
Build the workspace switcher and its management dialogs — the WSNAV-01 switcher trigger + dropdown, the WSMGMT-01/02 create/rename dialog, and the WSMGMT-03/04 Manage-workspaces hub with the disabled-delete guards. These are three self-contained new components consuming the plan-03 data layer; they are mounted into the sidebar in plan 06.

Purpose: the entire user-facing workspace-management surface.
Output: `WorkspaceNameDialog.tsx`, `ManageWorkspacesDialog.tsx`, `WorkspaceSwitcher.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md
@.planning/phases/26-workspace-switcher-management-assignment/26-PATTERNS.md
@.planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md
@web/src/components/sidebar/RenameProjectDialog.tsx
@web/src/components/sidebar/ProjectMenu.tsx
@web/src/components/sidebar/ProjectSidebar.tsx
@web/src/components/ui/dropdown-menu.tsx
@web/src/api/types.ts

<interfaces>
Data-layer hooks (plan 03) this plan consumes:
- `useWorkspaces()` → `Workspace[]` (queries.ts).
- `useCreateWorkspace()`, `useRenameWorkspace()`, `useDeleteWorkspace()` (mutations.ts).
- `useProjects()` → `Project[]` (each `Project` now has `workspace_id`).
- `useActiveWorkspace()` → `{ activeWorkspaceId, setActiveWorkspaceId }` (web/src/lib/useActiveWorkspace.tsx).

UI primitives + analogs (all already vendored):
- Create/rename dialog template — `web/src/components/sidebar/RenameProjectDialog.tsx:1-99` (controlled name, prefill-on-open effect, `ApiError`→`sentenceCase` inline error `text-xs text-destructive`, Save disabled while pending or empty).
- DropdownMenu composition — `web/src/components/sidebar/ProjectMenu.tsx:53-76`.
- `DropdownMenuCheckboxItem`, `DropdownMenuSeparator`, `DropdownMenuItem`, `DropdownMenuTrigger`, `DropdownMenuContent` — `web/src/components/ui/dropdown-menu.tsx` (exports confirmed).
- AlertDialog delete confirm — `web/src/components/sidebar/ProjectMenu.tsx:90-105`.
- Expanded-only idiom `group-data-[collapsible=icon]:hidden` — `web/src/components/sidebar/ProjectSidebar.tsx:53`.
- Client-side per-project derivation over the list (`waitingByProject` Map) — `web/src/components/sidebar/ProjectSidebar.tsx:36-44` (mirror for per-workspace count).
- Icon library: lucide (`Check`, `Plus`, `Settings`, `ChevronsUpDown`, `Pencil`, `Trash2`).
</interfaces>

Copywriting + interaction contract: 26-UI-SPEC.md "Copywriting Contract" + "Interaction & State Contract" tables are authoritative for all copy, the `bg-sidebar-accent` hover/open fill, the `✓` active marker, sentence-case, and the muted (never destructive-red) disabled-delete tooltips.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceNameDialog — create + rename name dialog</name>
  <files>web/src/components/sidebar/WorkspaceNameDialog.tsx</files>
  <read_first>
    - web/src/components/sidebar/RenameProjectDialog.tsx (whole file — the near-verbatim template)
    - web/src/api/types.ts (Workspace)
    - .planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md (Copywriting Contract rows: create title "New workspace"/button "Create"; rename title "Rename workspace"/button "Save"; duplicate error copy)
  </read_first>
  <action>
    Clone `RenameProjectDialog.tsx` into `WorkspaceNameDialog.tsx`. Props: `{ mode: "create" | "rename"; workspace?: Workspace; open: boolean; onOpenChange: (open: boolean) => void }`. Keep the `sentenceCase` helper (RenameProjectDialog.tsx:21-24). Controlled `name` state; prefill-on-open effect (RenameProjectDialog.tsx:36-41): `mode === "rename"` → `workspace!.name`, `mode === "create"` → `""`; also clear `error`.

    Submit handler mirrors RenameProjectDialog.tsx:43-56: `mode === "create"` → `await useCreateWorkspace().mutateAsync({ name: name.trim() })`; `mode === "rename"` → `await useRenameWorkspace().mutateAsync({ id: workspace!.id, name: name.trim() })`; then `onOpenChange(false)`. `catch` → `setError(err instanceof ApiError ? sentenceCase(err.message) : "Couldn't save. Try again.")`. The server's 409 duplicate message renders in the same `<p className="text-xs text-destructive">` line (UI-SPEC: "A workspace with that name already exists.").

    Markup mirrors RenameProjectDialog.tsx:58-97: `Dialog`/`DialogContent aria-describedby={undefined}`/`DialogHeader`/`DialogTitle`, a `form` `flex flex-col gap-4`, a `label` `text-xs font-medium` "Name" + `Input autoFocus` + the inline error, and a `DialogFooter` with a ghost Cancel + a primary submit button. Title + submit-button label switch on `mode` (create → "New workspace"/"Create"; rename → "Rename workspace"/"Save"). Submit disabled while the active mutation `isPending` or `name.trim() === ""`.
  </action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `WorkspaceNameDialog.tsx` renders title "New workspace"+button "Create" for `mode="create"` and title "Rename workspace"+button "Save" for `mode="rename"`.
    - It calls `useCreateWorkspace` in create mode and `useRenameWorkspace` in rename mode, and renders `ApiError.message` sentence-cased in a `text-xs text-destructive` element.
    - Submit is `disabled` while pending or when `name.trim() === ""`.
    - `cd web && npm run lint` exits 0.
  </acceptance_criteria>
  <done>A reusable create/rename workspace dialog with inline server-error handling exists.</done>
</task>

<task type="auto">
  <name>Task 2: ManageWorkspacesDialog — rename/delete hub with guard tooltips</name>
  <files>web/src/components/sidebar/ManageWorkspacesDialog.tsx</files>
  <read_first>
    - web/src/components/sidebar/RenameProjectDialog.tsx:58-97 (Dialog shell)
    - web/src/components/sidebar/ProjectMenu.tsx:90-105 (AlertDialog delete confirm to mirror)
    - web/src/components/sidebar/ProjectSidebar.tsx:36-44 (client-side Map derivation over projects)
    - web/src/components/sidebar/WorkspaceNameDialog.tsx (from Task 1 — opened for create + rename)
    - .planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md (Copywriting Contract: "Manage workspaces" title, delete-confirm copy, guard tooltips; accessibility note on disabled-with-tooltip)
  </read_first>
  <action>
    Build `ManageWorkspacesDialog.tsx` — a `Dialog` (shell from RenameProjectDialog.tsx:58-97) titled "Manage workspaces". Consume `useWorkspaces()` + `useProjects()`. Compute a per-workspace project count client-side: a `Map<number, number>` over `projects` keyed by `workspace_id` (mirror the `waitingByProject` derivation, ProjectSidebar.tsx:36-44) — D-05, no count endpoint.

    Render one row per workspace (name-sorted) with the workspace name, a **Rename** control (lucide `Pencil`) opening `WorkspaceNameDialog mode="rename"` for that workspace, and a **Delete** control (lucide `Trash2`) whose behavior is:
    - `ws.is_default` → delete **disabled** + `Tooltip` "The default workspace can't be deleted." (WSMGMT-04, D-06; keyed off `is_default`, never the name).
    - else count > 0 → delete **disabled** + `Tooltip` `Move or remove its ${n} project${n === 1 ? "" : "s"} first.` (WSMGMT-03, D-05).
    - else (empty, non-default) → delete **enabled** → opens an `AlertDialog` confirm (mirror ProjectMenu.tsx:90-105) titled "Delete workspace?", body per UI-SPEC (e.g. `"{name}" will be removed. Its projects are unaffected — it has none.`), with `AlertDialogCancel` (ghost) + `AlertDialogAction variant="destructive"` "Delete workspace" → `useDeleteWorkspace().mutateAsync(ws.id)`.

    Add an "New workspace" add control in the dialog (lucide `Plus`) opening `WorkspaceNameDialog mode="create"` (the same fast path the switcher offers).

    Accessibility (UI-SPEC): the disabled Delete must stay focusable-with-tooltip — do NOT put a raw `disabled` attribute on a button wrapped by a Tooltip trigger (it swallows hover/focus); wrap the control in a focusable `span`/use `aria-disabled` + a click no-op, or the established tooltip-on-disabled pattern. Guard tooltips use `text-muted-foreground`, never `text-destructive`.
  </action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ManageWorkspacesDialog.tsx` derives a per-workspace count from `useProjects()` by `workspace_id` (no dedicated count query).
    - Delete is disabled with the exact default-workspace tooltip when `ws.is_default`, and disabled with the pluralized "Move or remove its N project(s) first." tooltip when count > 0.
    - An enabled Delete opens an AlertDialog with `AlertDialogAction variant="destructive"` labelled "Delete workspace" calling `useDeleteWorkspace`.
    - The dialog offers a "New workspace" control opening `WorkspaceNameDialog mode="create"` and per-row Rename opening `mode="rename"`.
    - No disabled Delete relies on a raw `disabled` that suppresses its tooltip.
    - `cd web && npm run lint` exits 0.
  </acceptance_criteria>
  <done>Rename + guarded delete for every workspace live in one Manage dialog.</done>
</task>

<task type="auto">
  <name>Task 3: WorkspaceSwitcher — expanded-only trigger + dropdown + switch navigation</name>
  <files>web/src/components/sidebar/WorkspaceSwitcher.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectMenu.tsx:53-76 (DropdownMenu composition)
    - web/src/components/sidebar/ProjectSidebar.tsx:48-58 (expanded-only brand-lockup idiom, min-h-7 px-3 text-sm font-medium)
    - web/src/components/ui/dropdown-menu.tsx (DropdownMenuCheckboxItem, DropdownMenuSeparator, DropdownMenuItem)
    - web/src/components/sidebar/WorkspaceNameDialog.tsx + ManageWorkspacesDialog.tsx (Tasks 1-2 — opened from the dropdown)
    - .planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md (Interaction & State Contract: trigger states, ✓ active marker, expanded-only; Copywriting: "New workspace", "Manage workspaces…")
  </read_first>
  <action>
    Build `WorkspaceSwitcher.tsx`. Consume `useActiveWorkspace()`, `useWorkspaces()`, `useProjects()`, and `useNavigate()` (react-router). State for the two dialogs (`nameDialogOpen` create-mode, `manageOpen`).

    Trigger: a full-width name+chevron button (`min-h-7 px-3 text-sm font-medium`, matching the project-row height / brand-lockup weight) showing the active workspace's name (looked up from `useWorkspaces()` by `activeWorkspaceId`) plus a trailing lucide `ChevronsUpDown`. `aria-label={`Workspace: ${activeName}`}` (chevron decorative). Hover/open fill `bg-sidebar-accent` (UI-SPEC). Wrap the ENTIRE switcher in a container with `group-data-[collapsible=icon]:hidden` so it disappears in the collapsed icon rail (WSNAV-01; brand-lockup idiom ProjectSidebar.tsx:53).

    Dropdown (`DropdownMenuContent side="bottom" align="start"`):
    - one `DropdownMenuCheckboxItem` per workspace (name-sorted), `checked={ws.id === activeWorkspaceId}` (the ✓ active marker, D-02); `onSelect` → **pick active + navigate** (D-11): `setActiveWorkspaceId(ws.id)`, then compute the target's first project `const first = (projects ?? []).filter(p => p.workspace_id === ws.id)[0]` (the projects list is already name-sorted from the backend, so `[0]` is first-by-name) and `navigate(first ? `/projects/${first.id}` : "/")` — navigating to `"/"` when the workspace is empty lets the index route render the workspace-scoped empty state (plan 05).
    - `DropdownMenuSeparator`.
    - `DropdownMenuItem` "New workspace" (lucide `Plus`) → open `WorkspaceNameDialog mode="create"`.
    - `DropdownMenuItem` "Manage workspaces…" (lucide `Settings`) → open `ManageWorkspacesDialog`.
    Render `<WorkspaceNameDialog mode="create" …/>` and `<ManageWorkspacesDialog …/>` alongside the dropdown. Copy exactly per UI-SPEC (ellipsis only on "Manage workspaces…").
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The switcher's outermost element carries `group-data-[collapsible=icon]:hidden` (hidden in the icon rail).
    - The trigger shows the active workspace name + a chevron and sets `aria-label="Workspace: {name}"`.
    - Each workspace renders as a `DropdownMenuCheckboxItem` with `checked` true only for `activeWorkspaceId`; selecting one calls `setActiveWorkspaceId(ws.id)` and `navigate` to the workspace's first project (or `"/"` when empty).
    - The dropdown includes a "New workspace" item and a "Manage workspaces…" item (with ellipsis) that open the two dialogs.
    - `cd web && npm run build && npm run lint` exit 0.
  </acceptance_criteria>
  <done>An expanded-only switcher that picks the active workspace, navigates on switch, and reaches create + manage flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → workspace name / rendered names | Names typed by the user are sent to the API and rendered back in the dropdown/dialogs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Elevation of Privilege | client-side delete-disable guards | accept | The disabled Delete is a UX affordance only; the real block-when-non-empty / block-when-default enforcement is server-side (plan 01) + `ON DELETE RESTRICT`. No security decision depends on the client. |
| T-26-09 | Tampering (XSS) | workspace name rendered in menu/dialogs | mitigate | Names are rendered as React text children (auto-escaped); no `dangerouslySetInnerHTML`. |
| T-26-04 | Spoofing | no auth | accept | Local-only single-user app. |
| T-26-SC | Tampering | dependencies | accept | No new npm packages — every primitive (`dropdown-menu`, `dialog`, `alert-dialog`, `tooltip`, lucide) is already vendored (UI-SPEC Registry Safety). |
</threat_model>

<verification>
- `cd web && npm run build` (tsc -b + vite) exits 0.
- `cd web && npm run lint` exits 0.
- The three components typecheck and consume only the plan-03 hooks + existing UI primitives.
- Full visual/interaction verification is deferred to the plan-06 end-of-phase human-verify gate.
</verification>

<success_criteria>
The complete workspace-management UI (switcher trigger + dropdown, create/rename dialog, Manage hub with guarded delete) exists, builds, and drives off the shared active-workspace context — awaiting mounting in plan 06.
</success_criteria>

<output>
Create `.planning/phases/26-workspace-switcher-management-assignment/26-04-SUMMARY.md` when done.
</output>
