---
phase: 26-workspace-switcher-management-assignment
plan: 06
type: execute
wave: 4
depends_on: ["26-03", "26-04", "26-05"]
files_modified:
  - web/src/components/sidebar/ProjectSidebar.tsx
  - web/src/components/sidebar/ProjectMenu.tsx
autonomous: false
requirements: [WSNAV-01, WSNAV-02, WSPROJ-01, WSBAR-01]

must_haves:
  truths:
    - "The workspace switcher renders at the top of the expanded sidebar and is hidden in the collapsed icon rail"
    - "The sidebar project list (expanded rows AND collapsed rail) shows only the active workspace's projects"
    - "The project ⋯ menu offers a Move-to-workspace submenu; moving the currently-open project makes the view follow it"
    - "The global Active Sessions bar still shows sessions across every workspace"
  artifacts:
    - path: "web/src/components/sidebar/ProjectSidebar.tsx"
      provides: "mounted WorkspaceSwitcher + active-workspace filter over the project list"
      contains: "WorkspaceSwitcher"
    - path: "web/src/components/sidebar/ProjectMenu.tsx"
      provides: "Move-to-workspace radio submenu + follow-the-project (D-08/D-09)"
      contains: "Move to workspace"
  key_links:
    - from: "ProjectSidebar"
      to: "useActiveWorkspace"
      via: "filter (projects ?? []).filter(p => p.workspace_id === activeWorkspaceId)"
      pattern: "workspace_id === activeWorkspaceId"
    - from: "ProjectMenu"
      to: "useMoveProject + useActiveWorkspace"
      via: "move project; if it is the open one, setActiveWorkspaceId(target)"
      pattern: "useMoveProject"
---

<objective>
Wire the workspace layer into the sidebar: mount the switcher at the top of the expanded sidebar, filter the project list (both surfaces) to the active workspace, and add the "Move to workspace" transfer submenu to the project ⋯ menu with follow-the-project behavior. Then run the end-of-phase human-verify gate covering all WSMGMT/WSNAV/WSPROJ requirements and the WSBAR-01 non-regression.

Purpose: WSNAV-01 (mount, expanded-only), WSNAV-02 (per-workspace filtering), WSPROJ-01 (transfer UI), and the WSBAR-01 guardrail verification.
Output: filtered/switcher-mounted `ProjectSidebar.tsx`, transfer-enabled `ProjectMenu.tsx`, and a human-approved end-to-end verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md
@.planning/phases/26-workspace-switcher-management-assignment/26-PATTERNS.md
@.planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md
@web/src/components/sidebar/ProjectSidebar.tsx
@web/src/components/sidebar/ProjectMenu.tsx
@web/src/components/ui/dropdown-menu.tsx
@web/src/api/agents.ts
@web/src/components/layout/ActiveSessionsBar.tsx

<interfaces>
Consumed from earlier plans:
- `<WorkspaceSwitcher />` — `web/src/components/sidebar/WorkspaceSwitcher.tsx` (plan 04; self-hides in the icon rail).
- `useActiveWorkspace()` → `{ activeWorkspaceId, setActiveWorkspaceId }` (plan 03).
- `useWorkspaces()` → `Workspace[]`, `useMoveProject()` (`{ id, workspace_id }`) (plan 03).

Existing structure to modify:
- `ProjectSidebar` — mount point is the top of `SidebarContent` (ProjectSidebar.tsx:67-68); the project `.map` that renders BOTH the expanded rows and the collapsed `size-10` rail is a single map at ProjectSidebar.tsx:69. `waitingByProject`/`useAgentStatuses` (ProjectSidebar.tsx:29-44) must NOT be touched (WSBAR-01).
- `ProjectMenu` — the `DropdownMenuContent` to extend at ProjectMenu.tsx:62-75; it already imports `useNavigate`/`useParams` and knows `projectId` (ProjectMenu.tsx:2,37-38) and mirrors an `isCurrent` follow flow at ProjectMenu.tsx:40-49.
- Transfer submenu primitives (already exported): `DropdownMenuSub`, `DropdownMenuSubTrigger`, `DropdownMenuSubContent`, `DropdownMenuRadioGroup`, `DropdownMenuRadioItem` — `web/src/components/ui/dropdown-menu.tsx`.

WSBAR-01 guardrail (DO NOT TOUCH): `useAgentStatuses()` (`web/src/api/agents.ts:20-26`, queryKey `["agent-statuses"]`) and `ActiveSessionsBar.tsx` stay unfiltered and cross-workspace.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the switcher + filter the project list by active workspace</name>
  <files>web/src/components/sidebar/ProjectSidebar.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectSidebar.tsx (whole file — mount point + the single project .map at :69, and the untouchable waitingByProject/useAgentStatuses block at :29-44)
    - web/src/components/sidebar/WorkspaceSwitcher.tsx (plan 04 — the component to mount)
    - web/src/lib/useActiveWorkspace.tsx (plan 03)
  </read_first>
  <action>
    Import `WorkspaceSwitcher` and `useActiveWorkspace`. Mount `<WorkspaceSwitcher />` at the very top of `SidebarContent` (above the `SidebarMenu`, ProjectSidebar.tsx:67-68); the switcher self-hides in the icon rail via its own `group-data-[collapsible=icon]:hidden` wrapper, so no extra wrapper is needed here (WSNAV-01).

    Read `const { activeWorkspaceId } = useActiveWorkspace()`. Filter the project map (ProjectSidebar.tsx:69): change `(projects ?? []).map((project) => {` to `(projects ?? []).filter((p) => p.workspace_id === activeWorkspaceId).map((project) => {`. Because the expanded rows and the collapsed `size-10` rail avatars are rendered by this SAME map, filtering here filters BOTH surfaces at once (WSNAV-02, D-12). When `activeWorkspaceId == null` (still resolving) the filter yields an empty list, which is fine — it fills in once the hook resolves.

    Do NOT modify `waitingByProject`, `useAgentStatuses`, or anything feeding the Active Sessions bar (WSBAR-01). Leave the `AddProjectDialog` mount in the footer as-is (it self-serves the active workspace via plan 05).
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectSidebar.tsx` renders `<WorkspaceSwitcher />` at the top of `SidebarContent`.
    - The project map is prefixed with `.filter((p) => p.workspace_id === activeWorkspaceId)` (a single filter feeding both the expanded rows and the collapsed rail).
    - `useAgentStatuses`/`waitingByProject` are unchanged (`git diff` shows no edit to ProjectSidebar.tsx:29-44 logic).
    - `cd web && npm run build && npm run lint` exit 0.
  </acceptance_criteria>
  <done>The switcher is mounted expanded-only and both sidebar surfaces show only the active workspace's projects.</done>
</task>

<task type="auto">
  <name>Task 2: "Move to workspace" transfer submenu + follow-the-project</name>
  <files>web/src/components/sidebar/ProjectMenu.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectMenu.tsx (whole file — DropdownMenuContent at :62-75, isCurrent follow flow at :40-49)
    - web/src/components/ui/dropdown-menu.tsx (Sub/RadioGroup/RadioItem exports)
    - web/src/lib/useActiveWorkspace.tsx (plan 03)
    - .planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md (D-08 submenu, D-09 follow)
  </read_first>
  <action>
    Extend the `DropdownMenuContent` (ProjectMenu.tsx:62-75) with a "Move to workspace" submenu using the already-exported `DropdownMenuSub` / `DropdownMenuSubTrigger` / `DropdownMenuSubContent` / `DropdownMenuRadioGroup` / `DropdownMenuRadioItem`. Consume `useWorkspaces()`, `useMoveProject()`, and `useActiveWorkspace()`.

    The submenu: `DropdownMenuSubTrigger` "Move to workspace"; `DropdownMenuRadioGroup value={String(project.workspace_id)}`; one `DropdownMenuRadioItem` per workspace with `value={String(ws.id)}`, `disabled={ws.id === project.workspace_id}` (the project's current workspace is checked + disabled, D-08), and `onSelect` → transfer. Transfer + follow (D-09): compute `const isCurrent = projectId === String(project.id)` (mirror ProjectMenu.tsx:41), call `useMoveProject().mutate({ id: project.id, workspace_id: ws.id })`; when `isCurrent`, also `setActiveWorkspaceId(ws.id)` so the view follows the moved project into its new workspace (the project stays selected + visible; localStorage is updated by the hook setter). A not-currently-open project simply drops out of the filtered sidebar list, active workspace unchanged.
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectMenu.tsx` renders a `DropdownMenuSub` labelled "Move to workspace" with a `DropdownMenuRadioGroup value={String(project.workspace_id)}`.
    - Each workspace is a `DropdownMenuRadioItem`; the current workspace item is `disabled`.
    - Selecting another workspace calls `useMoveProject().mutate({ id: project.id, workspace_id: ws.id })`, and when `projectId === String(project.id)` it also calls `setActiveWorkspaceId(ws.id)`.
    - `cd web && npm run build && npm run lint` exit 0.
  </acceptance_criteria>
  <done>Projects transfer from the ⋯ menu and the open project's view follows it across workspaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human verification (WSMGMT / WSNAV / WSPROJ / WSBAR)</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md (the approved interaction + copy contract to check against)
    - .planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md (the locked decisions D-01..D-15)
  </read_first>
  <action>
    STOP for human verification. All Phase 26 implementation waves are complete; this is the end-of-phase gate. Build the app (`cd web && npm run build`), start the kamacu binary (or the Vite dev server + Go backend), and walk the ten checks in <how-to-verify> against the approved 26-UI-SPEC contract. Present the results to the user and wait for "approved" before marking the phase done; if any check fails, log the specific gap for a `/gsd:plan-phase --gaps` pass rather than silently editing.
  </action>
  <what-built>
    The full workspace feature: an expanded-only sidebar switcher (dropdown with a ✓ active marker, "New workspace", "Manage workspaces…"), workspace create/rename dialogs, a Manage-workspaces hub with guarded delete, per-workspace filtering of both sidebar surfaces, switch/reload navigation, project transfer via the ⋯ menu, create-in-active-workspace, and the untouched cross-workspace Active Sessions bar.
  </what-built>
  <how-to-verify>
    Build and run the app (`cd web && npm run build`, then run the kamacu binary — or use the Vite dev server with the Go backend). Then:
    1. Expanded sidebar shows the workspace switcher at the top. Collapse the rail (Ctrl+B) → the switcher is hidden while the project avatars remain (and are filtered). (WSNAV-01)
    2. Open the switcher → the active workspace has a ✓; the sidebar (expanded rows AND rail) shows ONLY the active workspace's projects. (WSNAV-02, D-02)
    3. "New workspace" → create one; a duplicate name shows the inline "A workspace with that name already exists." error. (WSMGMT-01)
    4. Switch to the new (empty) workspace → the board shows "No projects in {workspace} yet"; switch to a populated one → it navigates to that workspace's first project. (WSNAV-03, D-13/D-11)
    5. Manage workspaces → rename Personal (the default) → succeeds. (WSMGMT-02)
    6. In Manage: the default workspace's Delete is disabled with "The default workspace can't be deleted."; a non-empty workspace's Delete is disabled with "Move or remove its N project(s) first."; an empty non-default workspace deletes via the confirm dialog. (WSMGMT-03/04)
    7. Project ⋯ → "Move to workspace" → move a NON-open project → it leaves the current filtered list; move the CURRENTLY-OPEN project → the view follows it (active workspace flips to the target, project stays selected). (WSPROJ-01, D-08/D-09)
    8. Add a project (from the sidebar or the empty state) → it appears in the ACTIVE workspace. (WSPROJ-02)
    9. Deep-link/reload a `/projects/:id` whose project belongs to a different workspace than the saved active one → the active workspace flips to that project's workspace and the project is visible in the sidebar. (WSNAV-03/D-14)
    10. Confirm the global Active Sessions bar STILL lists sessions from projects across ALL workspaces regardless of the active filter. (WSBAR-01)
  </how-to-verify>
  <verify>
    <human-check>All ten steps in <how-to-verify> pass against the 26-UI-SPEC contract; the user replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - All ten verification steps behave as described and match the 26-UI-SPEC copy/interaction contract.
    - The Active Sessions bar remains cross-workspace (step 10) — WSBAR-01 non-regression confirmed.
    - The user replies "approved" (or the issues are logged for a gap-closure pass).
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe any issues to fix.</resume-signal>
  <done>The full workspace feature is human-verified end to end and the user approved it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active-workspace filter → sidebar vs. sessions bar | The filter must reach the sidebar list but must NOT reach the global sessions bar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | Information Disclosure (regression) | Active Sessions bar accidentally workspace-filtered | mitigate | Do NOT thread any workspace state into `useAgentStatuses()`/`ActiveSessionsBar`; the bar keeps its own `["agent-statuses"]` poll. Verified at the human-verify gate step 10 (WSBAR-01). |
| T-26-08 | Elevation of Privilege | client transfer/delete affordances | accept | Server (plans 01/02) validates transfer targets and enforces delete guards; client controls are UX only. |
| T-26-09 | Tampering (XSS) | workspace/project names in menus | mitigate | Rendered as React text (auto-escaped). |
| T-26-04 | Spoofing | no auth | accept | Local-only single-user app. |
| T-26-SC | Tampering | dependencies | accept | No new npm packages — all submenu primitives already vendored. |
</threat_model>

<verification>
- `cd web && npm run build` (tsc -b + vite) exits 0.
- `cd web && npm run lint` exits 0.
- The end-of-phase human-verify gate (Task 3) is approved, confirming WSMGMT-01..04, WSNAV-01..03, WSPROJ-01/02, and the WSBAR-01 non-regression end to end.
</verification>

<success_criteria>
The switcher is mounted and expanded-only, both sidebar surfaces filter to the active workspace, projects transfer (with follow-the-project) from the ⋯ menu, and the cross-workspace Active Sessions bar is confirmed unaffected — all human-verified.
</success_criteria>

<output>
Create `.planning/phases/26-workspace-switcher-management-assignment/26-06-SUMMARY.md` when done.
</output>
