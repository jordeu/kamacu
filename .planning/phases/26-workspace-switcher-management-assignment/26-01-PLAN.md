---
phase: 26-workspace-switcher-management-assignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/workspaces.go
  - internal/api/routes.go
  - internal/api/workspaces_test.go
autonomous: true
requirements: [WSMGMT-01, WSMGMT-02, WSMGMT-03, WSMGMT-04, WSNAV-01]

must_haves:
  truths:
    - "GET /api/workspaces returns every workspace (id, name, is_default) name-sorted"
    - "POST /api/workspaces creates a workspace by name; a duplicate (case-insensitive) name is rejected"
    - "PATCH /api/workspaces/{id} renames any workspace including the default Personal one"
    - "DELETE /api/workspaces/{id} refuses a non-empty workspace and refuses the is_default workspace; deletes an empty non-default one"
  artifacts:
    - path: "internal/api/workspaces.go"
      provides: "workspaceHandlers CRUD (list/create/update/delete) + Workspace JSON struct + workspaceColumns + scanWorkspace"
      contains: "type workspaceHandlers"
    - path: "internal/api/routes.go"
      provides: "the 4 /api/workspaces route registrations"
      contains: "/api/workspaces"
    - path: "internal/api/workspaces_test.go"
      provides: "handler tests for list/create/rename/guarded-delete"
      contains: "newTestServer"
  key_links:
    - from: "internal/api/routes.go"
      to: "workspaceHandlers"
      via: "mux.HandleFunc VERB /api/workspaces"
      pattern: "workspaceHandlers"
    - from: "workspaceHandlers.delete"
      to: "projects.workspace_id"
      via: "SELECT COUNT(*) FROM projects WHERE workspace_id = ?"
      pattern: "COUNT\\(\\*\\) FROM projects"
---

<objective>
Add the `/api/workspaces` CRUD surface that the Phase 26 switcher UI drives: list, create-by-name, rename (Personal included), and a guarded delete (block-when-non-empty, block-when-default). No schema change — this builds on the shipped migration `00012` (`workspaces` table + `projects.workspace_id NOT NULL … ON DELETE RESTRICT`).

Purpose: WSMGMT-01/02/03/04 need a backend enforcer; WSNAV-01's switcher needs a list endpoint to render.
Output: `workspaceHandlers` in `internal/api/workspaces.go`, 4 routes in `routes.go`, handler tests in `workspaces_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md
@.planning/phases/26-workspace-switcher-management-assignment/26-PATTERNS.md
@internal/api/workspaces.go
@internal/api/projects.go
@internal/api/routes.go
@internal/api/settings.go
@internal/api/respond.go
@internal/store/migrations/00012_workspaces.sql

<interfaces>
Existing symbols the executor reuses directly (no exploration needed):
- `writeError(w, status, msg)` and `writeJSON(w, status, v)` — `internal/api/respond.go:9-18`. Error body is `{"error": msg}`.
- `pathID(w, r) (int64, bool)` — `internal/api/projects.go:833-840`. Parses `{id}`, writes 400 on failure.
- `scanProject` / `projectColumns` / `list` loop / folder-create dedup pre-check — `internal/api/projects.go:97-142, 180-187` (the shape to mirror for workspaces).
- INTEGER→bool scan idiom (`managedInt`) — `internal/api/projects.go:106,116` (mirror for `is_default`).
- `settingsHandlers struct{ db *sql.DB }` + `SettingsRoutes` — `internal/api/settings.go:31-38` (the db-only handler-struct shape to mirror).
- migration `00012`: `workspaces(id, name, is_default, created_at, updated_at)`, `idx_workspaces_name_nocase` (UNIQUE, COLLATE NOCASE), `projects.workspace_id … ON DELETE RESTRICT`.
- `store.go` opens with `foreign_keys(1)` + `SetMaxOpenConns(1)` — RESTRICT bites; use parameterized `?` and collect-then-write.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace struct + list/create/rename handlers + route registration</name>
  <files>internal/api/workspaces.go, internal/api/routes.go</files>
  <read_first>
    - internal/api/workspaces.go (current file — holds only BackfillWorkspaces; add to it, do not remove that hook)
    - internal/api/projects.go:97-221 (projectColumns, scanProject, list, create dedup pre-check, defaultWorkspaceID)
    - internal/api/settings.go:31-38 (handler-struct + route shape)
    - internal/api/respond.go (writeError/writeJSON)
    - internal/api/routes.go (route block to extend)
    - internal/store/migrations/00012_workspaces.sql (column names + UNIQUE index)
  </read_first>
  <action>
    In `internal/api/workspaces.go` add a `Workspace` JSON struct mirroring `Project` (projects.go:26-54): `ID int64` json `id`, `Name string` json `name`, `IsDefault bool` json `is_default`, `CreatedAt string` json `created_at`, `UpdatedAt string` json `updated_at`. Add `const workspaceColumns = "id, name, is_default, created_at, updated_at"`. Add `scanWorkspace(row interface{ Scan(...any) error }) (Workspace, error)` that scans `is_default` into an `int` then maps to bool (the `managedInt != 0` idiom at projects.go:106,116). Add `type workspaceHandlers struct{ db *sql.DB }` (mirror `settingsHandlers`, settings.go:38).

    Add three handlers on `*workspaceHandlers`:
    - `list` (GET): `SELECT `+workspaceColumns+` FROM workspaces ORDER BY name COLLATE NOCASE`, loop `scanWorkspace`, return `[]Workspace{}` (never nil) via `writeJSON(w, 200, …)` — mirror projects.go:121-142.
    - `create` (POST): decode `{ Name string }`; `strings.TrimSpace`; empty → `writeError(400, "name is required")` (projects.go:393-401). Duplicate pre-check `SELECT 1 FROM workspaces WHERE name = ? COLLATE NOCASE` → on a row `writeError(409, "a workspace with that name already exists")` (mirror the folder-create dedup at projects.go:180-187; the UNIQUE index is the ultimate backstop). On `sql.ErrNoRows` proceed: `INSERT INTO workspaces (name) VALUES (?) RETURNING `+workspaceColumns via `scanWorkspace(h.db.QueryRow(...))`, `writeJSON(w, 201, ws)`.
    - `update` (rename, PATCH): `pathID`; decode `{ Name *string }`; nil → `writeError(400, "nothing to update")`; `TrimSpace` empty → `writeError(400, "name is required")`. Duplicate pre-check excluding self `SELECT 1 FROM workspaces WHERE name = ? COLLATE NOCASE AND id != ?` → `writeError(409, "a workspace with that name already exists")`. Then `UPDATE workspaces SET name = ?, updated_at = strftime('%Y-%m-%dT%H:%M:%fZ','now') WHERE id = ? RETURNING `+workspaceColumns via `scanWorkspace`; `errors.Is(err, sql.ErrNoRows)` → `writeError(404, "workspace not found")`. Do NOT gate rename on `is_default` — Personal must be renamable (WSMGMT-02, D-04). All SQL uses `?` placeholders.

    In `internal/api/routes.go` (route block routes.go:19-33), construct `wh := &workspaceHandlers{db: db}` and register alongside the project routes: `GET /api/workspaces` → `wh.list`, `POST /api/workspaces` → `wh.create`, `PATCH /api/workspaces/{id}` → `wh.update`, `DELETE /api/workspaces/{id}` → `wh.delete` (delete handler lands in Task 2). Leave `BackfillWorkspaces` wiring untouched (already called in cmd/kamacu/main.go).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type workspaceHandlers' internal/api/workspaces.go` is true.
    - `grep -q 'workspaceColumns' internal/api/workspaces.go` is true and equals `id, name, is_default, created_at, updated_at`.
    - `internal/api/routes.go` contains all four of: `GET /api/workspaces`, `POST /api/workspaces`, `PATCH /api/workspaces/{id}`, `DELETE /api/workspaces/{id}`.
    - `create` and `update` both run a `COLLATE NOCASE` duplicate pre-check returning HTTP 409; `update` excludes self via `AND id != ?`.
    - `update` contains no reference to `is_default` (rename is never gated on the default flag).
    - `go build ./...` and `go vet ./internal/api/` exit 0.
  </acceptance_criteria>
  <done>list/create/rename handlers exist and compile; the 4 workspace routes are registered; rename works for Personal.</done>
</task>

<task type="auto">
  <name>Task 2: Guarded delete handler (block-when-non-empty, block-when-default)</name>
  <files>internal/api/workspaces.go</files>
  <read_first>
    - internal/api/workspaces.go (after Task 1)
    - internal/api/projects.go:543-580 (load-marker-then-branch delete idiom)
    - internal/store/migrations/00012_workspaces.sql (ON DELETE RESTRICT backstop)
    - .planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md (D-05, D-06 delete guards)
  </read_first>
  <action>
    Add `delete` (DELETE) on `*workspaceHandlers`, mirroring the "load the marker column, then branch" idiom (projects.go:543-576):
    - `pathID`; load the marker `SELECT is_default FROM workspaces WHERE id = ?` into an `int`; `errors.Is(err, sql.ErrNoRows)` → `writeError(404, "workspace not found")`.
    - **Default guard (WSMGMT-04, D-06):** if `is_default == 1` → `writeError(409, "the default workspace can't be deleted")`. Key off the `is_default` flag, NEVER the name "Personal" (rename-proof, Phase 25 D-02).
    - **Non-empty guard (WSMGMT-03, D-05):** `SELECT COUNT(*) FROM projects WHERE workspace_id = ?` into an `int`; if `> 0` → `writeError(409, …)` with a message that embeds the count, e.g. `fmt.Sprintf("move or remove its %d project(s) first", n)`. Do the explicit count first so the refusal carries a clean message instead of a raw FK error (the `ON DELETE RESTRICT` at 00012:31 is the ultimate backstop, not the message source).
    - On all-clear: `res, err := h.db.Exec("DELETE FROM workspaces WHERE id = ?", id)`; `if n, _ := res.RowsAffected(); n == 0` → `writeError(404, "workspace not found")`; else `w.WriteHeader(http.StatusNoContent)`.
    All SQL parameterized with `?`.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - `workspaces.go` `delete` selects `is_default` first and returns 409 with `the default workspace can't be deleted` when it is 1.
    - `delete` runs `SELECT COUNT(*) FROM projects WHERE workspace_id = ?` and returns 409 embedding the count when > 0.
    - Successful delete returns 204 (`w.WriteHeader(http.StatusNoContent)`); a missing id returns 404.
    - No branch compares the workspace name to the literal string `Personal`.
    - `go build ./...` and `go vet ./internal/api/` exit 0.
  </acceptance_criteria>
  <done>The delete handler enforces both guards server-side and 204s an empty non-default workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Handler tests for workspace CRUD + guards</name>
  <files>internal/api/workspaces_test.go</files>
  <behavior>
    - GET /api/workspaces returns exactly one workspace (Personal) with is_default true on a fresh migrated DB.
    - POST {name:"Work"} → 201; a following GET returns 2 workspaces.
    - POST {name:"work"} (case-insensitive dup of "Work") → 409.
    - POST {name:"  "} → 400.
    - PATCH the Personal workspace {name:"Home"} → 200 (rename of the default succeeds).
    - PATCH a workspace to an existing name → 409.
    - DELETE an empty non-default workspace → 204.
    - DELETE a workspace that owns a project → 409.
    - DELETE the default (Personal) workspace → 409.
  </behavior>
  <read_first>
    - internal/api/workspaces_test.go (current file — has TestBackfillWorkspaces; add tests, keep it)
    - internal/api/projects_test.go:420-530 (newTestServer, gitRepo, doJSON, doJSONList, createProject helpers)
  </read_first>
  <action>
    Add HTTP handler tests using the fuller harness from `projects_test.go`: `newTestServer(t)` (projects_test.go:430-452, migrates a temp DB — migration 00012 already seeds Personal), `doJSON(t, method, url, body)` and `doJSONList(t, url)` (projects_test.go:467-516). Assert the nine behaviors above against `srv.URL+"/api/workspaces"` (and `…/{id}` for PATCH/DELETE).

    To exercise the **non-empty delete guard without depending on plan 02's transfer endpoint**: create a project via the existing `createProject(t, srv, gitRepo(t))` helper (projects_test.go:519), create a new workspace via POST, then place the project into that workspace with a direct `db.Exec("UPDATE projects SET workspace_id = ? WHERE id = ?", wsID, projID)` (the test owns the `db` handle from `newTestServer`). Then DELETE that workspace → assert 409.

    Read the created/renamed workspace's `id` from the `doJSON` response map (`body["id"].(float64)`), and `is_default` from `body["is_default"].(bool)`.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'Workspace' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/api/ -run 'Workspace' -count=1` passes (includes the existing TestBackfillWorkspaces plus the new handler tests).
    - The suite asserts each of: 201 create, 409 duplicate (case-insensitive), 400 empty, 200 rename-of-Personal, 409 rename-collision, 204 empty-delete, 409 non-empty-delete, 409 default-delete.
    - The non-empty-delete test places the project via a direct `UPDATE projects SET workspace_id` (no call to POST/PATCH `/api/workspaces` for assignment) so it stays independent of plan 02.
  </acceptance_criteria>
  <done>All workspace handler behaviors are proven green under `go test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (localhost) → `/api/workspaces` | Untrusted JSON (workspace name, `{id}` path) crosses into SQL + DB mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | workspace name → SQL | mitigate | All queries use `?` placeholders (never string-concatenated input); single-writer `SetMaxOpenConns(1)`. |
| T-26-02 | Elevation of Privilege | delete guards | mitigate | Server-side `is_default` guard + `SELECT COUNT(*) FROM projects` non-empty guard, backstopped by `ON DELETE RESTRICT` (00012:31). The client disable is UX only. |
| T-26-03 | Tampering | duplicate / empty name | mitigate | `TrimSpace` + `COLLATE NOCASE` duplicate pre-check (create + rename) with the UNIQUE index as backstop. |
| T-26-04 | Spoofing | no auth on local API | accept | Local-only, single-user, loopback-bound by default (remote bind is opt-in no-auth per quick task 260626-hwd); auth is out of scope project-wide. |
| T-26-SC | Tampering | dependencies | accept | No new packages — stdlib (`database/sql`, `net/http`, `errors`, `fmt`, `strings`) only; no supply-chain surface. |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go vet ./internal/api/` exits 0.
- `go test ./internal/api/ -run 'Workspace' -count=1` passes.
- `GET /api/workspaces` returns Personal with `is_default:true`; the four routes are registered in routes.go.
</verification>

<success_criteria>
The workspace CRUD surface exists and is enforced server-side: create-by-name (dup-rejected), rename (Personal included), and a delete that refuses non-empty and default workspaces while removing empty non-default ones — all covered by green tests.
</success_criteria>

<output>
Create `.planning/phases/26-workspace-switcher-management-assignment/26-01-SUMMARY.md` when done.
</output>
