---
phase: 26-workspace-switcher-management-assignment
plan: 05
type: execute
wave: 3
depends_on: ["26-03"]
files_modified:
  - web/src/App.tsx
  - web/src/components/sidebar/AddProjectDialog.tsx
autonomous: true
requirements: [WSNAV-03, WSPROJ-02]

must_haves:
  truths:
    - "The index route redirects to the first project OF THE ACTIVE WORKSPACE, or a workspace-scoped empty state when it has none (D-13: workspace-scoped \"No projects in {workspace} yet\" empty state + Add project)"
    - "Deep-linking a project whose workspace differs from the saved active workspace flips the active workspace to that project's workspace (D-14: on reload/deep-link the URL's project wins)"
    - "Adding a project from the empty state or the sidebar creates it in the currently active workspace"
  artifacts:
    - path: "web/src/App.tsx"
      provides: "workspace-aware RedirectToFirstProject + empty state + URL-wins deep-link sync"
      contains: "useActiveWorkspace"
    - path: "web/src/components/sidebar/AddProjectDialog.tsx"
      provides: "create body carries the active workspace_id (WSPROJ-02 client side)"
      contains: "workspace_id"
  key_links:
    - from: "App.tsx RedirectToFirstProject"
      to: "useActiveWorkspace + useProjects"
      via: "filter projects to activeWorkspaceId before picking [0]"
      pattern: "workspace_id === activeWorkspaceId"
    - from: "AddProjectDialog"
      to: "useCreateProject"
      via: "include workspace_id: activeWorkspaceId in the create body"
      pattern: "workspace_id"
---

<objective>
Make routing and project creation workspace-aware: the index redirect lands on the active workspace's first project (or a workspace-scoped empty state), a deep-linked project reconciles the active workspace to that project's workspace on reload (URL wins), and new projects are created into the active workspace (WSPROJ-02 client side, sending the `workspace_id` the plan-02 backend accepts).

Purpose: WSNAV-03 (localStorage restore + switch navigation + empty state + URL-wins) and the client half of WSPROJ-02.
Output: workspace-aware `App.tsx` + an active-workspace-carrying `AddProjectDialog`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md
@.planning/phases/26-workspace-switcher-management-assignment/26-PATTERNS.md
@.planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md
@web/src/App.tsx
@web/src/components/sidebar/AddProjectDialog.tsx
@web/src/api/types.ts

<interfaces>
Data-layer hooks (plan 03) this plan consumes:
- `useActiveWorkspace()` → `{ activeWorkspaceId: number | null, setActiveWorkspaceId }` (web/src/lib/useActiveWorkspace.tsx).
- `useWorkspaces()` → `Workspace[]` (for the empty-state workspace name).
- `useProjects()` → `Project[]` (each has `workspace_id`).
- `useCreateProject()` — body type now includes optional `workspace_id?: number` (mutations.ts, plan 03).

Existing structure to modify:
- `RedirectToFirstProject` (index route) — `web/src/App.tsx:12-35` (currently redirects to `projects[0]` globally; empty state at App.tsx:21-33).
- Routes block — `web/src/App.tsx:38-52` (`/projects/:projectId` → BoardPage, `/projects/:projectId/tasks/:taskId` → TaskPage). Routes stay unchanged; workspace stays OUT of the URL.
- `AddProjectDialog` create call — `web/src/components/sidebar/AddProjectDialog.tsx:102-134` (both repo + folder branches at 107-110); it already `navigate`s to the new project.
- `useParams` / `Navigate` from react-router (already imported in App.tsx).
- localStorage/`is_default` fallback for a stale saved id is handled inside `useActiveWorkspace` (plan 03) — this plan does NOT re-implement the fallback.
</interfaces>

Copy for the empty state (UI-SPEC Copywriting Contract): heading "No projects in {workspace} yet" (`text-xl font-medium`), body "Add a project to this workspace to get a board.", primary button "Add project".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace-aware redirect, empty state, and URL-wins deep-link sync</name>
  <files>web/src/App.tsx</files>
  <read_first>
    - web/src/App.tsx (RedirectToFirstProject + Routes, App.tsx:1-53)
    - web/src/api/types.ts (Project.workspace_id, Workspace)
    - .planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md (D-11 first-by-name, D-13 empty state, D-14 URL-wins)
    - .planning/phases/26-workspace-switcher-management-assignment/26-UI-SPEC.md (empty-state copy)
  </read_first>
  <action>
    **RedirectToFirstProject (App.tsx:12-35):** read `useActiveWorkspace()` + `useProjects()` + `useWorkspaces()`. While `isLoading` or `activeWorkspaceId == null` → `return null`. Compute `const wsProjects = (projects ?? []).filter(p => p.workspace_id === activeWorkspaceId)`. If `wsProjects.length > 0` → `<Navigate to={`/projects/${wsProjects[0].id}`} replace />` (first-by-name — the backend list is `ORDER BY name COLLATE NOCASE`, so `[0]` is the first project, D-11). Else render the workspace-scoped empty state: reuse the existing centered layout (App.tsx:21-33) but with heading `No projects in {workspaceName} yet` (workspaceName looked up from `useWorkspaces()` by `activeWorkspaceId`), body "Add a project to this workspace to get a board.", and the "Add project" button opening `AddProjectDialog` (D-13). Do NOT show a global "No projects yet" while other workspaces still hold projects.

    **URL-wins deep-link sync (D-14):** add a small wrapper component (e.g. `BoardWorkspaceSync`) used as the `element` for `/projects/:projectId` (and `/projects/:projectId/tasks/:taskId`): it reads `useParams().projectId`, `useProjects()`, and `useActiveWorkspace()`; in a `useEffect` keyed on the resolved project + `activeWorkspaceId`, when the URL project exists and `project.workspace_id !== activeWorkspaceId`, call `setActiveWorkspaceId(project.workspace_id)` so the viewed project is always visible under its own workspace's sidebar filter (localStorage is updated by the hook setter). The wrapper renders the underlying page (`<BoardPage/>` / `<TaskPage/>`) unchanged. Keep the route paths exactly `/projects/:projectId` and `/projects/:projectId/tasks/:taskId` — workspace never enters the URL.
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `RedirectToFirstProject` filters projects by `p.workspace_id === activeWorkspaceId` before choosing `[0]` and returns `null` while `activeWorkspaceId == null`.
    - The empty state heading reads `No projects in {workspaceName} yet` (workspace name resolved from `useWorkspaces()`), not a global "No projects yet".
    - A `useEffect`-based sync sets the active workspace to a deep-linked project's `workspace_id` when it differs, without changing the route path.
    - Route paths remain `/projects/:projectId` and `/projects/:projectId/tasks/:taskId`.
    - `cd web && npm run build && npm run lint` exit 0.
  </acceptance_criteria>
  <done>Index redirect, empty state, and reload/deep-link are all active-workspace-aware.</done>
</task>

<task type="auto">
  <name>Task 2: AddProjectDialog creates into the active workspace</name>
  <files>web/src/components/sidebar/AddProjectDialog.tsx</files>
  <read_first>
    - web/src/components/sidebar/AddProjectDialog.tsx (handleSubmit, AddProjectDialog.tsx:102-134)
    - web/src/lib/useActiveWorkspace.tsx (from plan 03)
  </read_first>
  <action>
    In `AddProjectDialog.tsx`, read `const { activeWorkspaceId } = useActiveWorkspace()`. In `handleSubmit` (AddProjectDialog.tsx:102-134), include `workspace_id: activeWorkspaceId ?? undefined` in BOTH create bodies (the repo branch and the folder branch at AddProjectDialog.tsx:107-110) so a new project lands in the currently active workspace (WSPROJ-02, D-10). When `activeWorkspaceId` is `null` (not yet resolved), omit the field (pass `undefined`) so the backend falls back to the default (plan 02). `useCreateProject` already accepts `workspace_id` (plan 03) and the dialog already `navigate`s to the created project — because it was created in the active workspace, it stays visible under the sidebar filter with no extra work.
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Both create branches in `handleSubmit` include `workspace_id: activeWorkspaceId ?? undefined`.
    - The dialog imports and calls `useActiveWorkspace()`.
    - `cd web && npm run build && npm run lint` exit 0.
  </acceptance_criteria>
  <done>New projects (repo + folder) are created into the active workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL / localStorage → active-workspace state | The URL projectId and saved workspace id together decide which workspace is active (client-side). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-10 | Tampering | create carries a client-chosen workspace_id | mitigate | The backend (plan 02) validates the workspace exists and falls back to the default; a bad client value cannot create a workspace-less or foreign project. |
| T-26-07 | Tampering | deep-link / saved-id reconciliation | mitigate | URL-wins only sets the active workspace to a project that exists in `useProjects()`; the `is_default` fallback for stale ids lives in `useActiveWorkspace` (plan 03). |
| T-26-09 | Tampering (XSS) | workspace name in empty-state heading | mitigate | Rendered as React text (auto-escaped). |
| T-26-04 | Spoofing | no auth | accept | Local-only single-user app. |
| T-26-SC | Tampering | dependencies | accept | No new npm packages. |
</threat_model>

<verification>
- `cd web && npm run build` (tsc -b + vite) exits 0.
- `cd web && npm run lint` exits 0.
- Index redirect + empty state + deep-link sync + create-in-active all typecheck; full runtime verification is at the plan-06 human-verify gate.
</verification>

<success_criteria>
Routing and creation honor the active workspace: switch/restore lands on the workspace's first project or its own empty state, deep-links reconcile the active workspace, and new projects are created into it.
</success_criteria>

<output>
Create `.planning/phases/26-workspace-switcher-management-assignment/26-05-SUMMARY.md` when done.
</output>
