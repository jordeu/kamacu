---
phase: 26-workspace-switcher-management-assignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/projects.go
  - internal/api/projects_test.go
autonomous: true
requirements: [WSPROJ-01, WSPROJ-02]

must_haves:
  truths:
    - "PATCH /api/projects/{id} with a workspace_id transfers the project to that workspace"
    - "PATCH with a non-existent workspace_id is rejected without mutating the row"
    - "POST /api/projects with a workspace_id creates the project in that workspace"
    - "POST /api/projects without a workspace_id still lands in the default (Personal) workspace"
  artifacts:
    - path: "internal/api/projects.go"
      provides: "optional workspace_id on the update PATCH (transfer) + on both create paths (active-workspace-at-create)"
      contains: "WorkspaceID *int64"
    - path: "internal/api/projects_test.go"
      provides: "transfer + create-in-workspace tests"
      contains: "workspace_id"
  key_links:
    - from: "projectHandlers.update"
      to: "workspaces table"
      via: "SELECT 1 FROM workspaces WHERE id = ? (validate target) then SET workspace_id = ?"
      pattern: "workspace_id = \\?"
    - from: "projectHandlers.create / createByRepo"
      to: "workspaces table"
      via: "resolve requested workspace_id or fall back to defaultWorkspaceID()"
      pattern: "defaultWorkspaceID"
---

<objective>
Extend the existing project endpoints so a project can be transferred between workspaces (WSPROJ-01) and so a newly created project lands in the client's active workspace (WSPROJ-02). No new route — transfer is an optional `workspace_id` on the existing partial-PATCH `update`, and create accepts an optional `workspace_id` that falls back to the default Personal workspace when omitted (refining Phase 25's interim default-to-Personal).

Purpose: WSPROJ-01 (transfer via the ⋯ menu) and WSPROJ-02 (create-in-active) both drive off this backend behavior; the switcher UI never gets its own transfer route (D-17).
Output: `workspace_id` handling in `update`, `create`, and `createByRepo`, plus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-workspace-switcher-management-assignment/26-CONTEXT.md
@.planning/phases/26-workspace-switcher-management-assignment/26-PATTERNS.md
@internal/api/projects.go
@internal/store/migrations/00012_workspaces.sql

<interfaces>
Existing symbols the executor reuses directly:
- `update` partial-PATCH (pointer fields + conditional `sets`/`args` + `RETURNING projectColumns`) — `internal/api/projects.go:369-476`. `projectColumns` already includes `workspace_id`, so `RETURNING` already serializes it.
- The github_repo "validate-then-append, else reject 400 without touching the row" idiom — `internal/api/projects.go:410-436` (the model for validating the transfer target).
- `create` folder path + `createByRepo` repo-first path, and `defaultWorkspaceID()` — `internal/api/projects.go:157-221, 231-323` (both INSERT sites currently call `defaultWorkspaceID()`).
- `pathID`, `writeError`, `scanProject` — projects.go:833, respond.go, projects.go:99.
- migration `00012`: `projects.workspace_id INTEGER NOT NULL … REFERENCES workspaces(id) ON DELETE RESTRICT`, DEFAULT 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transfer via optional workspace_id on PATCH /api/projects/{id}</name>
  <files>internal/api/projects.go</files>
  <read_first>
    - internal/api/projects.go:369-476 (the update handler — pointer req struct, all-nil guard, conditional sets/args, RETURNING)
    - internal/api/projects.go:410-436 (github_repo validate-then-append reject idiom to mirror)
    - internal/store/migrations/00012_workspaces.sql (FK target)
  </read_first>
  <action>
    In `update` (projects.go:369-476): add `WorkspaceID *int64` json `workspace_id` to the `req` struct (projects.go:374-380). Add `req.WorkspaceID == nil` as another conjunct in the all-nil "nothing to update" guard (projects.go:385-388) so the guard still fires only when every field is nil.

    When `req.WorkspaceID != nil`: validate the target workspace exists before appending — `SELECT 1 FROM workspaces WHERE id = ?` with `*req.WorkspaceID`; on `errors.Is(err, sql.ErrNoRows)` → `writeError(w, http.StatusBadRequest, "workspace not found")` and return WITHOUT touching the row (mirror the github_repo reject at projects.go:422-431); on other error → `writeError(500, …)`. On success append `sets = append(sets, "workspace_id = ?")` and `args = append(args, *req.WorkspaceID)`.

    The existing `updated_at` append, `RETURNING projectColumns`, and `sql.ErrNoRows → 404` tail (projects.go:463-475) are unchanged — `projectColumns` already carries `workspace_id`, so the response reflects the transfer. This is the entire WSPROJ-01 backend (D-17); do NOT add a new route.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - The `update` req struct has a `WorkspaceID *int64` field tagged `json:"workspace_id"`.
    - The all-nil guard includes `req.WorkspaceID == nil` (an update with only `workspace_id` is NOT rejected as "nothing to update").
    - A non-existent target `workspace_id` produces HTTP 400 `workspace not found` and appends nothing to `sets` (row untouched).
    - `go build ./...` and `go vet ./internal/api/` exit 0.
  </acceptance_criteria>
  <done>PATCH /api/projects/{id} accepts an optional validated workspace_id and transfers the project, returning the updated row.</done>
</task>

<task type="auto">
  <name>Task 2: Active-workspace-at-create on both create paths</name>
  <files>internal/api/projects.go</files>
  <read_first>
    - internal/api/projects.go:157-221 (create folder path + defaultWorkspaceID helper)
    - internal/api/projects.go:231-323 (createByRepo repo-first path + its INSERT)
  </read_first>
  <action>
    Make both create paths honor an optional client-supplied `workspace_id`, falling back to the default when omitted (D-10 / D-14 baseline: missing → default).

    Add `WorkspaceID *int64` json `workspace_id` to the `create` `req` struct (projects.go:158-162). Add a resolver method on `*projectHandlers`, e.g. `resolveCreateWorkspaceID(req *int64) (int64, int, error)` that returns `(id, httpStatus, err)`: when `req != nil`, `SELECT 1 FROM workspaces WHERE id = ?` — `sql.ErrNoRows` → return `(0, 400, err)` (so the caller can `writeError(400, "workspace not found")`), other error → `(0, 500, err)`, success → `(*req, 0, nil)`; when `req == nil`, delegate to the existing `defaultWorkspaceID()` (projects.go:217-221) and map its error to 500.

    In `create` (projects.go), resolve `wsID` via `resolveCreateWorkspaceID(req.WorkspaceID)` BEFORE branching to `createByRepo`; on error `writeError(status, msg)` and return. Pass the resolved `wsID` into `createByRepo` by extending its signature to `createByRepo(w, r, req.Repo, req.Name, wsID)` and use that `wsID` in its INSERT (projects.go:314-316) INSTEAD of calling `defaultWorkspaceID()` a second time (projects.go:309-313 removed). In the folder path, use the already-resolved `wsID` in its INSERT (projects.go:202-204), removing the folder path's own `defaultWorkspaceID()` call (projects.go:197-201). Keep `defaultWorkspaceID()` itself — it is now reached only through the resolver's nil branch.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/api/</automated>
  </verify>
  <acceptance_criteria>
    - The `create` req struct has a `WorkspaceID *int64` field tagged `json:"workspace_id"`.
    - Both INSERT sites (folder path and `createByRepo`) use a single resolved `wsID`; `defaultWorkspaceID()` is invoked only via the resolver's nil branch (verify: at most one direct `defaultWorkspaceID()` call site remains, inside the resolver).
    - A create with a non-existent `workspace_id` returns HTTP 400 `workspace not found`; a create with no `workspace_id` uses the default (Personal) id.
    - `go build ./...` and `go vet ./internal/api/` exit 0.
  </acceptance_criteria>
  <done>New projects (folder + repo-first) land in the requested workspace, or Personal when unspecified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transfer + create-in-workspace tests</name>
  <files>internal/api/projects_test.go</files>
  <behavior>
    - PATCH /api/projects/{id} {workspace_id: W} → 200 and the returned body's workspace_id == W; a follow-up GET /api/projects shows the project under W.
    - PATCH /api/projects/{id} {workspace_id: 999999} (non-existent) → 400 and the row is unchanged.
    - POST /api/projects {repo_path, workspace_id: W} → 201 with body workspace_id == W.
    - POST /api/projects {repo_path} (no workspace_id) → 201 with body workspace_id == the default Personal id (1).
  </behavior>
  <read_first>
    - internal/api/projects_test.go:420-530 (newTestServer, gitRepo, doJSON, createProject helpers)
  </read_first>
  <action>
    Add tests using `newTestServer(t)` + `doJSON`. Create a target workspace WITHOUT depending on plan 01's POST endpoint — insert it directly with the test's `db` handle: `db.Exec("INSERT INTO workspaces (name) VALUES ('Work')")` then read its id via `SELECT id FROM workspaces WHERE name = 'Work'` (or `res.LastInsertId()`). Create a project with `createProject(t, srv, gitRepo(t))`. Then:
    - PATCH `srv.URL+"/api/projects/"+id` with `map[string]any{"workspace_id": workID}` → assert 200 and `body["workspace_id"].(float64) == workID`.
    - PATCH with `map[string]any{"workspace_id": 999999}` → assert 400.
    - POST `srv.URL+"/api/projects"` with `map[string]any{"repo_path": gitRepo(t), "workspace_id": workID}` → assert 201 and body workspace_id == workID.
    - POST with only `repo_path` → assert 201 and body workspace_id == 1 (Personal, seeded by migration 00012).
    Keep these tests independent of the `/api/workspaces` handlers (this plan is wave-1 parallel with plan 01).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'Project' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/api/ -run 'Project' -count=1` passes, including the new transfer + create-in-workspace assertions.
    - The transfer-target and create-target workspaces are seeded via direct `db.Exec` INSERT (no call to POST/PATCH `/api/workspaces`).
    - The non-existent-workspace transfer assertion checks for HTTP 400.
  </acceptance_criteria>
  <done>Transfer and create-in-workspace behaviors are proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (localhost) → `/api/projects` | Untrusted `workspace_id` (transfer + create) crosses into a project mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | transfer to forged/non-existent workspace_id | mitigate | Validate `SELECT 1 FROM workspaces WHERE id = ?` before append; the `REFERENCES workspaces(id)` FK is the backstop. Reject 400 without touching the row. |
| T-26-06 | Tampering | create with forged/absent workspace_id | mitigate | Resolver validates the id when supplied, else falls back to `defaultWorkspaceID()`; `NOT NULL DEFAULT 1` FK guarantees a project is never workspace-less. |
| T-26-04 | Spoofing | no auth on local API | accept | Local-only single-user loopback app (see plan 01 T-26-04). |
| T-26-SC | Tampering | dependencies | accept | No new packages — edits `internal/api/projects.go` with existing stdlib imports only. |
</threat_model>

<verification>
- `go build ./...` and `go vet ./internal/api/` exit 0.
- `go test ./internal/api/ -run 'Project' -count=1` passes.
- A transfer PATCH returns the moved project; a create with `workspace_id` lands the project there; a create without it lands in Personal.
</verification>

<success_criteria>
Projects can be transferred between workspaces via the existing PATCH, and new projects are created in the requested (active) workspace with a safe fall-back to Personal — validated and green.
</success_criteria>

<output>
Create `.planning/phases/26-workspace-switcher-management-assignment/26-02-SUMMARY.md` when done.
</output>
