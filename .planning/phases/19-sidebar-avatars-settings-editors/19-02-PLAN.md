---
phase: 19-sidebar-avatars-settings-editors
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/src/components/sidebar/ProjectSidebar.tsx
  - web/src/components/layout/AppLayout.tsx
autonomous: false
requirements: [ICON-05, ICON-06, ICON-07, ICON-08, ICON-09, ICON-10]
must_haves:
  truths:
    - "When the sidebar is collapsed the user sees a vertical rail of project monogram avatars instead of an empty off-screen sidebar (ICON-05, D-01)"
    - "Clicking a rail avatar switches to that project without expanding the sidebar (ICON-06)"
    - "The currently-open project's rail avatar shows an active ring (ICON-07, D-06)"
    - "Hovering a rail avatar shows the full project name in a side=right tooltip (ICON-08)"
    - "A project with >=1 waiting agent shows a static amber dot on its collapsed avatar; the expanded count chip is unchanged (ICON-09, D-07/D-08)"
    - "The same monogram avatar appears inline beside the project name when expanded, with the existing name + amber count chip preserved (ICON-10, D-08)"
    - "The floating CollapsedSidebarTrigger is retired and <main> no longer applies pl-9; the rail's header SidebarTrigger is the re-expand entry point (D-02)"
  artifacts:
    - path: "web/src/components/sidebar/ProjectSidebar.tsx"
      provides: "collapsible=icon rail with avatars (active ring, tooltip, waiting dot) + inline avatar in expanded rows; footer hidden in icon mode"
      contains: 'collapsible="icon"'
    - path: "web/src/components/layout/AppLayout.tsx"
      provides: "Floating trigger removed, <main> pl-9 dropped, unused imports pruned; controlled open + localStorage kept"
  key_links:
    - from: "web/src/components/sidebar/ProjectSidebar.tsx"
      to: "ProjectAvatar"
      via: "import from @/components/ui/ProjectAvatar (rail + inline call sites)"
      pattern: "ProjectAvatar"
    - from: "web/src/components/sidebar/ProjectSidebar.tsx"
      to: "waitingByProject map"
      via: "same useAgentStatuses-derived count → rail dot AND expanded chip"
      pattern: "waitingByProject"
    - from: "web/src/components/layout/AppLayout.tsx"
      to: "localStorage kangent.sidebar"
      via: "controlled open state (kept exactly as-is, D-02/D-04)"
      pattern: "kangent.sidebar"
---

<objective>
Turn the off-screen offcanvas sidebar into a collapsible icon RAIL of project
avatars (active ring, name tooltip, amber waiting dot), put the same avatar
inline beside the name when expanded, and retire the floating re-expand trigger
now that the rail occupies layout space. Covers ICON-05, 06, 07, 08, 09, 10.

Purpose: A collapsed sidebar must still let the user see and switch projects —
today it slides fully off-screen (collapsible=offcanvas → w-0), leaving no
project reference. Switching to shadcn's built-in `collapsible="icon"` 3rem rail
and rendering `<ProjectAvatar>` in it delivers the whole collapsed-rail feature.
Output: edited `ProjectSidebar.tsx` (rail + inline avatars, footer hidden in
icon mode) and `AppLayout.tsx` (floating trigger gone, `<main>` pl-9 dropped,
unused imports pruned). Open-state persistence is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-UI-SPEC.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-CONTEXT.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-PATTERNS.md

# Foundation built in Wave 1 — consume, do not redefine:
@.planning/phases/19-sidebar-avatars-settings-editors/19-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1). Use directly; do not re-explore. -->

ProjectAvatar — web/src/components/ui/ProjectAvatar.tsx:
  <ProjectAvatar
    size="rail" | "inline"
    letters={project.icon_letters}
    color={project.icon_color}
    active={boolean}        // rail only — adds ring-2 ring-sidebar-ring
    waiting={boolean}       // rail only — static amber dot, makes root relative
    waitingLabel={string}   // aria-label when waiting
    className={string}
  />

Existing in ProjectSidebar.tsx (reuse, do not rebuild):
  - waitingByProject: Map<number, number>   (count of status==="waiting" agents per projectId; L33-42)
  - isActive boolean:  projectId === String(project.id)   (L65)
  - Tooltip/TooltipTrigger/TooltipContent imported, used side="right" (L18-22, L48-53)
  - existing amber count chip on the expanded row (L74-85) — KEEP UNCHANGED
  - SidebarTrigger already in SidebarHeader inside a side=right tooltip (L48-53)

shadcn icon-mode facts (ui/sidebar.tsx, read-only reference):
  - <Sidebar collapsible="icon"> → 3rem rail (--sidebar-width-icon); default is "offcanvas" (L154)
  - collapsed SidebarMenuButton becomes size-8! p-2! (L469) — exactly the rail avatar box
  - group-data-[collapsible=icon]:hidden — the utility to hide expanded-only chrome (footer)
  - SidebarContent already overflow-auto + no-scrollbar (L373)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch ProjectSidebar to the icon rail + render avatars (rail + inline) + footer hidden in icon mode</name>
  <files>web/src/components/sidebar/ProjectSidebar.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectSidebar.tsx — the file being edited (self-analog): the `<Sidebar>` (L45), the `waitingByProject` map (L33-42), the `isActive` check (L65), the expanded row Link + name span + amber chip (L68-86), the side=right Tooltip on the header trigger (L48-53), and the SidebarFooter Add/Settings block (L95-125).
    - web/src/components/ui/sidebar.tsx — read-only reference: `collapsible` prop + default (L154/L162), the icon-mode `size-8! p-2!` SidebarMenuButton base (L469), `group-data-[collapsible=icon]:hidden` utility (e.g. L575), SidebarContent overflow (L373).
    - web/src/components/ui/ProjectAvatar.tsx — the Wave 1 component being consumed (prop contract in <interfaces>).
    - 19-UI-SPEC.md § "Collapsed rail (ICON-05/06/07/08/09)" and § "Expanded sidebar (ICON-10)" (the binding contract: collapsible="icon", ring-2 ring-sidebar-ring, static size-2 amber dot top-right, side=right tooltip = project.name, inline size-5 avatar before the name, KEEP the existing count chip, footer hidden in icon state).
    - 19-CONTEXT.md D-01, D-03, D-05, D-06, D-07, D-08 and 19-PATTERNS.md § "MODIFY ProjectSidebar.tsx".
  </read_first>
  <action>
    Edit `web/src/components/sidebar/ProjectSidebar.tsx`:
    (1) Change `<Sidebar>` (L45) to `<Sidebar collapsible="icon">` (D-01) — the
    3rem rail + group-data-[collapsible=icon] states are built into ui/sidebar.tsx;
    do NOT hand-roll a collapsed render.
    (2) Import `ProjectAvatar` from `@/components/ui/ProjectAvatar`.
    (3) In each project row, render `<ProjectAvatar size="inline" .../>` BEFORE
    the `min-w-0 flex-1 truncate` name span, passing
    `letters={project.icon_letters}` and `color={project.icon_color}`. Keep the
    name span and the existing amber count chip (L74-85) UNCHANGED — do NOT add
    the dot here (would double-signal, D-08). The inline avatar is `size-5` and
    naturally hidden when the row collapses (the row text truncates away in icon
    mode; only the rail avatar shows).
    (4) Render the RAIL avatar so it fills the icon-mode `size-8` hit target:
    inside the existing `SidebarMenuButton asChild` `<Link to="/projects/{id}">`,
    add a `<ProjectAvatar size="rail" letters color active={isActive}
    waiting={count > 0} waitingLabel={...} />` that is visible in icon mode. Use
    the established approach: the inline avatar + name show when expanded, the
    rail avatar shows when collapsed (gate visibility with
    `group-data-[collapsible=icon]:` / its inverse via `cn` and the sidebar's
    collapse data attributes — mirror how ui/sidebar.tsx hides/shows chrome).
    Pass the active ring via the SAME `isActive = projectId === String(project.id)`
    boolean already computed (D-06, ICON-07). Pass `waiting={count > 0}` where
    `count = waitingByProject.get(project.id) ?? 0` — REUSE the existing map, do
    not add a second query (D-07, ICON-09). For `waitingLabel`, reuse the EXACT
    existing chip copy: `count === 1 ? "1 agent waiting for input" :
    "${count} agents waiting for input"`.
    (5) Tooltip (ICON-08): wrap each RAIL avatar in the existing `Tooltip` /
    `TooltipTrigger asChild` / `TooltipContent side="right"` (already imported),
    content = `{project.name}` (full, untruncated). TooltipProvider delayDuration
    is already set once in AppLayout — do NOT re-wrap a provider.
    (6) Footer (D-03): add `group-data-[collapsible=icon]:hidden` to the
    `SidebarFooter` so Add(+)/Settings(gear) disappear in the collapsed rail
    (ICON-FUT-05 deferred); they remain reachable by expanding. Keep the header
    `SidebarTrigger` (re-expand entry point, D-02) visible — it lives in
    SidebarHeader, which is not hidden in icon mode.
    Keep the rail avatar link's `aria-current="page"` when active (recommended in
    UI-SPEC Accessibility) and set `aria-label` to the project name (plus the
    waiting copy when waiting) so the tooltip is supplementary, not the only name.
    Do not change the AddProjectDialog mount or ProjectMenu usage.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -8 && npx eslint src/components/sidebar/ProjectSidebar.tsx 2>&1 | tail -8 && grep -q 'collapsible="icon"' src/components/sidebar/ProjectSidebar.tsx && grep -q 'ProjectAvatar' src/components/sidebar/ProjectSidebar.tsx && grep -q 'size="rail"' src/components/sidebar/ProjectSidebar.tsx && grep -q 'size="inline"' src/components/sidebar/ProjectSidebar.tsx && grep -q 'group-data-\[collapsible=icon\]:hidden' src/components/sidebar/ProjectSidebar.tsx && grep -q 'waitingByProject' src/components/sidebar/ProjectSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `<Sidebar collapsible="icon">` is present (grep passes) — D-01.
    - `ProjectAvatar` is imported and used at BOTH `size="rail"` and `size="inline"` call sites (all three greps pass) — ICON-05/10.
    - The rail avatar's active ring is driven by `projectId === String(project.id)` and the waiting dot by `count > 0` off the EXISTING `waitingByProject` map (grep `waitingByProject` passes; no second agent-status query is added) — ICON-07/09.
    - Each rail avatar is wrapped in a `Tooltip` with `TooltipContent side="right"` containing `project.name` — ICON-08.
    - The existing expanded amber count chip (L74-85) is preserved unchanged and no amber dot is added to the inline avatar — ICON-10/D-08.
    - `SidebarFooter` carries `group-data-[collapsible=icon]:hidden` (grep passes) — D-03.
    - `cd web && npm run build` exits 0 and `npx eslint src/components/sidebar/ProjectSidebar.tsx` is clean.
  </acceptance_criteria>
  <done>Sidebar uses collapsible="icon"; rail shows per-project avatars with active ring, side=right name tooltip, and a static amber waiting dot; expanded rows show an inline avatar before the unchanged name + count chip; footer hidden in icon mode; build + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the floating CollapsedSidebarTrigger + drop main pl-9 + prune unused imports (AppLayout)</name>
  <files>web/src/components/layout/AppLayout.tsx</files>
  <read_first>
    - web/src/components/layout/AppLayout.tsx — the file being edited (self-analog): the `CollapsedSidebarTrigger` helper (L23-37) and its render inside `<main>` (L67), the `<main>` className branch with collapsed `pl-9` (L60-66), the imports (L1-15), the KEEP-AS-IS controlled `open` + `localStorage["kangent.sidebar"]` (L17, L43-50), `SidebarProvider open/onOpenChange` + `--sidebar-width: 15rem` (L54-58), `TooltipProvider delayDuration={0}` (L53), `<ActiveSessionsBar/>` (L74).
    - 19-UI-SPEC.md § "Collapsed rail" → "Re-expand" bullet (retire CollapsedSidebarTrigger, drop main pl-9 because the rail now occupies layout space).
    - 19-CONTEXT.md D-02 (retire floating trigger, drop pl-9, KEEP controlled open + localStorage exactly) and 19-PATTERNS.md § "MODIFY AppLayout.tsx".
  </read_first>
  <action>
    Edit `web/src/components/layout/AppLayout.tsx`:
    (1) Delete the entire `CollapsedSidebarTrigger` helper (L23-37) and its usage
    inside `<main>` (L67). The 3rem icon rail (from Task 1) now occupies layout
    space and the rail's header `SidebarTrigger` is the re-expand entry point
    (D-02).
    (2) Simplify the `<main>` className (L60-66) to a single
    `"relative flex-1 overflow-hidden"` — REMOVE the collapsed `pl-9` branch
    (the floating trigger that needed the padding is gone; the rail provides the
    offset). `<main>` no longer depends on `open` for padding.
    (3) Prune now-unused imports: after the helper is removed, `useSidebar`,
    `SidebarTrigger`, and the `Tooltip`/`TooltipContent`/`TooltipTrigger` set are
    no longer referenced in this file — remove them from the import statements.
    KEEP `SidebarProvider`, `TooltipProvider`, `useState`, `type CSSProperties`,
    `Outlet`, `ProjectSidebar`, `ActiveSessionsBar` (still used). Lint/build will
    fail on any dangling unused import, so this pruning is mandatory.
    (4) KEEP EXACTLY AS-IS (D-02/D-04): the `SIDEBAR_STORAGE_KEY` const, the
    controlled `open` `useState` initializer reading `localStorage`, the
    `handleOpenChange` writer, the `SidebarProvider open/onOpenChange` wiring, the
    `--sidebar-width: 15rem` style, `TooltipProvider delayDuration={0}`, and the
    `<ActiveSessionsBar/>` mount. Do not change persistence behavior.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -8 && npx eslint src/components/layout/AppLayout.tsx 2>&1 | tail -8 && (grep -q 'CollapsedSidebarTrigger' src/components/layout/AppLayout.tsx && { echo "FAIL: CollapsedSidebarTrigger not removed"; exit 1; } || echo "trigger removed OK") && (grep -q 'pl-9' src/components/layout/AppLayout.tsx && { echo "FAIL: pl-9 still present"; exit 1; } || echo "pl-9 dropped OK") && grep -q 'kangent.sidebar' src/components/layout/AppLayout.tsx && grep -q 'delayDuration={0}' src/components/layout/AppLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CollapsedSidebarTrigger` no longer appears in the file (the grep-negation gate exits 0 / prints "trigger removed OK") — D-02.
    - `pl-9` no longer appears in the file (the grep-negation gate exits 0 / prints "pl-9 dropped OK") — D-02.
    - The controlled `open` + `localStorage["kangent.sidebar"]` persistence and `TooltipProvider delayDuration={0}` are still present (both greps pass) — D-02/D-04 kept exactly.
    - No unused imports remain (`cd web && npm run build` exits 0 and `npx eslint src/components/layout/AppLayout.tsx` is clean — eslint/tsc flags dangling `useSidebar`/`SidebarTrigger`/`Tooltip*`).
  </acceptance_criteria>
  <done>Floating trigger helper + its usage removed; main is a single non-padded className; unused imports pruned; controlled open/localStorage + TooltipProvider kept; build + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual-verify the collapsed rail + inline avatars (ICON-05..10)</name>
  <what-built>
    The collapsed-rail feature: `ProjectSidebar.tsx` now uses
    `collapsible="icon"` and renders per-project `<ProjectAvatar>`s — a 3rem
    rail of avatars when collapsed (active ring + side=right name tooltip +
    static amber waiting dot) and an inline avatar beside the name + the existing
    amber count chip when expanded. `AppLayout.tsx` retired the floating
    `CollapsedSidebarTrigger` and dropped `<main>`'s `pl-9`. Build + lint are
    green; this gate verifies the RUNTIME appearance the headless build cannot.
  </what-built>
  <how-to-verify>
    1. Run the app locally (the standard dev flow: backend running + `cd web &&
       npm run dev`, or `make build` + run the binary) and open localhost in a
       browser with at least 2 projects.
    2. COLLAPSE the sidebar via the header toggle. CONFIRM: a thin vertical rail
       of rounded-square colored monogram avatars appears (NOT an empty/blank
       gutter). Each avatar shows its two white letters on the project's color.
       (ICON-05)
    3. Click a rail avatar. CONFIRM: it switches to that project WITHOUT first
       expanding the sidebar; the URL/board changes. (ICON-06)
    4. CONFIRM: the currently-open project's rail avatar has a visible ring
       around it (active marker); the others do not. (ICON-07)
    5. Hover a rail avatar. CONFIRM: a tooltip on the RIGHT shows the full
       project name. (ICON-08)
    6. If a project has an agent waiting for input (you may need to run a session
       WITHOUT --dangerously-skip-permissions to trigger the waiting state — see
       STATE.md Blockers), CONFIRM: a small STATIC amber dot sits at the
       top-right of that project's collapsed avatar (no number, no pulsing).
       If you cannot induce a waiting state, note that and approve the rest.
       (ICON-09)
    7. EXPAND the sidebar. CONFIRM: each row now shows a small avatar to the LEFT
       of the project name, the name is unchanged, and the existing amber COUNT
       chip (the number) still appears for waiting projects — and there is NO
       second dot on the inline avatar. (ICON-10)
    8. CONFIRM: while collapsed there is no leftover floating trigger button
       overlapping the main area, and the main content is not pushed by a stale
       pl-9 gap; the re-expand toggle at the top of the rail works.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (per ICON-05..10 step).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (project list/GET) | `icon_letters` + `icon_color` rendered in the rail/rows arrive from the local Go API, validated server-side at create/PATCH (Phase 18 D-10/D-11). |
| browser localStorage → client | `kangent.sidebar` open flag (read at mount). Unchanged by this plan. |

Frontend-only; no new network/auth/storage surface. The collapse mechanism is a
CSS/data-attribute change on existing components; the open flag persistence is
kept verbatim.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering / Injection | ProjectAvatar `backgroundColor: project.icon_color` | mitigate | Handled in ProjectAvatar (Plan 01): hex flows ONLY into an inline `backgroundColor` on a constrained element; `icon_color` is palette-membership-validated server-side (Phase 18 D-11). This plan passes the value through unchanged — no new interpolation surface. |
| T-19-02 | Injection (XSS) | project.name in tooltip + aria-label; icon_letters in avatar | mitigate | `project.name` and `icon_letters` render as React text children / string attributes (TooltipContent text, aria-label) — React auto-escapes; no `dangerouslySetInnerHTML`. |
| T-19-03 | Tampering | `localStorage["kangent.sidebar"]` | accept | A local single-user flag controlling only sidebar open/closed; tampering at most toggles the user's own UI. No security impact. Behavior is unchanged from prior phases. |
| T-19-S | Spoofing | — | N/A | Local-only single-user app; no auth/identity surface. |
| T-19-R | Repudiation | — | N/A | No new state-changing action (navigation only); nothing to audit. |
| T-19-I | Information Disclosure | — | N/A | No secrets; renders project identity data already on the client. |
| T-19-D | Denial of Service | — | N/A | No new endpoint; rail renders one bounded avatar per existing project (SidebarContent already scrolls). |
| T-19-E | Elevation of Privilege | — | N/A | No new authz path. |
| T-19-SC | Tampering (supply chain) | npm installs | N/A | No new npm packages (UI-SPEC Registry Safety: no installs). Nothing to gate. |
</threat_model>

<verification>
- `cd web && npm run build` exits 0 (tsc -b + vite build) after both edits.
- `cd web && npm run lint` clean for both changed files (no dangling imports).
- Grep gates: `collapsible="icon"`, `ProjectAvatar`, `size="rail"`, `size="inline"`, `group-data-[collapsible=icon]:hidden`, `waitingByProject` present in ProjectSidebar.tsx; `CollapsedSidebarTrigger` and `pl-9` absent and `kangent.sidebar` + `delayDuration={0}` present in AppLayout.tsx.
- Human visual-verify checkpoint confirms the runtime rail appearance (ICON-05..10) the headless build cannot.
</verification>

<success_criteria>
- Collapsed sidebar shows a clickable avatar rail with active ring, side=right name tooltip, and static amber waiting dot (ICON-05/06/07/08/09).
- Expanded sidebar shows the same avatar inline before the unchanged name + count chip (ICON-10).
- Floating trigger retired, `<main>` pl-9 dropped, unused imports pruned; open-state persistence unchanged (D-02/D-04).
- Build + lint green; human visual-verify approved.
</success_criteria>

<output>
Create `.planning/phases/19-sidebar-avatars-settings-editors/19-02-SUMMARY.md` when done
</output>
