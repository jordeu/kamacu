---
phase: 19-sidebar-avatars-settings-editors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/palette.ts
  - web/src/components/ui/ProjectAvatar.tsx
autonomous: true
requirements: [ICON-07, ICON-09, ICON-10, ICON-12]
must_haves:
  truths:
    - "A single <ProjectAvatar> component renders a rounded-square white-on-color monogram and is the ONLY place a palette hex is applied as a background (D-04)"
    - "ProjectAvatar supports two sizes via a size prop: rail (~32px) and inline (~20px) (D-05)"
    - "ProjectAvatar can show an active ring and an optional static amber waiting dot, both rail-only (D-06/D-07)"
    - "PROJECT_PALETTE lists the 9 curated hexes byte-for-byte in the same order as the Go projectPalette (D-13)"
  artifacts:
    - path: "web/src/lib/palette.ts"
      provides: "PROJECT_PALETTE const — 9 hexes mirroring internal/api/icons.go verbatim (D-13)"
      contains: "PROJECT_PALETTE"
    - path: "web/src/components/ui/ProjectAvatar.tsx"
      provides: "Shared monogram avatar primitive (rail|inline size, active ring, waiting dot)"
      exports: ["ProjectAvatar"]
  key_links:
    - from: "web/src/components/ui/ProjectAvatar.tsx"
      to: "project.icon_color"
      via: "inline style backgroundColor (never a Tailwind class — runtime hex)"
      pattern: "backgroundColor"
    - from: "web/src/lib/palette.ts"
      to: "internal/api/icons.go projectPalette"
      via: "verbatim TS mirror (no endpoint, Phase 18 D-02/D-13)"
      pattern: "#dc2626"
---

<objective>
Build the two foundation artifacts every other Phase 19 surface consumes: the
shared `<ProjectAvatar>` monogram component and the `PROJECT_PALETTE` TS const.
Nothing renders an avatar or a swatch until these exist, so they are the
dependency root (Wave 1) for the sidebar (Plan 02) and the settings editors
(Plan 03).

Purpose: One avatar component reused in three call sites (rail, expanded row,
settings preview) is the ICON-10 integration point; one palette const mirrored
from Go is the single source for swatches and the only legal avatar background.
Output: `web/src/lib/palette.ts` (PROJECT_PALETTE) and
`web/src/components/ui/ProjectAvatar.tsx` (ProjectAvatar). No call sites are
wired in this plan — that is Plans 02/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-UI-SPEC.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-CONTEXT.md
@.planning/phases/19-sidebar-avatars-settings-editors/19-PATTERNS.md

<interfaces>
<!-- Contracts downstream plans (02/03) implement against. Executor builds these; -->
<!-- do not explore the codebase to invent a different shape. -->

ProjectAvatar prop contract (this plan defines it):
- size: "rail" | "inline"          (required — rail = size-8 text-sm, inline = size-5 text-[10px])
- letters: string                   (project.icon_letters — server-guaranteed non-empty)
- color: string                     (project.icon_color — a PROJECT_PALETTE hex; applied as inline backgroundColor)
- active?: boolean                  (rail only — adds ring-2 ring-sidebar-ring ring-offset-2 ring-offset-sidebar)
- waiting?: boolean                 (rail only — overlays a static amber dot; root becomes relative)
- waitingLabel?: string             (aria-label text when waiting, e.g. "2 agents waiting for input")
- className?: string                (cn-merged last, override-safe)

PROJECT_PALETTE (this plan defines it):
- export const PROJECT_PALETTE: readonly string[] (9 lowercase hexes, `as const`)

Go source of truth to mirror VERBATIM — internal/api/icons.go L19-22:
  "#dc2626", "#ea580c", "#d97706", "#16a34a", "#0d9488",
  "#2563eb", "#4f46e5", "#7c3aed", "#db2777"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PROJECT_PALETTE const (palette.ts)</name>
  <files>web/src/lib/palette.ts</files>
  <read_first>
    - internal/api/icons.go L11-22 — the Go `projectPalette` source of truth (the 9 lowercase hexes, locked order: red, orange, amber, green, teal, blue, indigo, violet, pink).
    - web/src/lib/time.ts — the `web/src/lib/*.ts` file convention to copy (top provenance doc comment + a single named export, zero deps). This is the analog from 19-PATTERNS.md.
    - 19-UI-SPEC.md § "Color" → "Curated avatar palette (PROJECT_PALETTE)" table (the 9-hex table + the byte-for-byte-sync rule).
    - 19-CONTEXT.md D-13 (palette mirror, the verbatim hex list) and Phase 18 18-CONTEXT.md D-02 (Go-const source of truth, no endpoint).
  </read_first>
  <action>
    Create `web/src/lib/palette.ts` exporting `PROJECT_PALETTE` as an `as const`
    array of exactly these 9 lowercase hex strings IN THIS ORDER:
    #dc2626, #ea580c, #d97706, #16a34a, #0d9488, #2563eb, #4f46e5, #7c3aed,
    #db2777 (per D-13, mirroring internal/api/icons.go projectPalette verbatim).
    Use `as const` so consumers get a tuple of literal hex strings. Add a top
    doc comment (time.ts convention) stating it mirrors internal/api/icons.go
    projectPalette byte-for-byte, there is no palette endpoint (Phase 18 D-02),
    Go is the source of truth, and white text is fixed. Do NOT add a default-
    export, a helper, or any color other than the 9. This const is the swatch-
    grid source (Plan 03) and the documented-only-legal avatar background; never
    hardcode a palette hex anywhere else in the codebase.
  </action>
  <verify>
    <automated>cd web && npx tsc -b 2>&1 | tail -5; node -e "const re=/#[0-9a-f]{6}/g; const m=(require('fs').readFileSync('src/lib/palette.ts','utf8').match(re)||[]); const want=['#dc2626','#ea580c','#d97706','#16a34a','#0d9488','#2563eb','#4f46e5','#7c3aed','#db2777']; if(JSON.stringify(m)!==JSON.stringify(want)){console.error('PALETTE MISMATCH',m);process.exit(1)} console.log('palette OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/palette.ts` exists and `export const PROJECT_PALETTE` is present (grep `PROJECT_PALETTE`).
    - The file's hex strings, in source order, are EXACTLY the 9 above (the node assertion in <verify> exits 0 / prints "palette OK"). A reorder or typo fails the gate.
    - The array uses `as const`.
    - No other source file gains a hardcoded palette hex as part of this task.
    - `cd web && npm run build` exits 0 (no new tsc errors introduced by this file).
  </acceptance_criteria>
  <done>palette.ts exports PROJECT_PALETTE = the 9 verbatim hexes in Go order, as const, with a provenance doc comment; build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create <ProjectAvatar> shared monogram primitive</name>
  <files>web/src/components/ui/ProjectAvatar.tsx</files>
  <read_first>
    - web/src/components/ui/skeleton.tsx — the canonical shadcn primitive shape to copy (import `cn`, `data-slot`, `cn(base, className)` merge, single named export, `rounded-md`). COPY THE SHAPE, NOT its `animate-pulse` — this avatar and its dot are STATIC.
    - web/src/components/ui/input.tsx — the `cn(longBaseString, className)` + spread-props + `data-slot` convention.
    - web/src/lib/utils.ts — `cn` = clsx + tailwind-merge (for the size/active/waiting conditional classes).
    - 19-UI-SPEC.md § "Component & Interaction Contract" → "<ProjectAvatar>" and the Spacing + Typography tables (size-8/size-5 boxes, text-sm/text-[10px] glyphs, size-2 dot, font-medium, text-white, leading-none, the "letter color is ALWAYS text-white" rule).
    - 19-PATTERNS.md § "NEW web/src/components/ui/ProjectAvatar.tsx" (construction checklist).
    - 19-CONTEXT.md D-04 (rounded-md, white font-medium letters on icon_color), D-05 (two sizes), D-06 (active ring), D-07 (static amber dot, aria-label).
  </read_first>
  <action>
    Create `web/src/components/ui/ProjectAvatar.tsx` exporting a `ProjectAvatar`
    function component with the prop contract in this plan's `<interfaces>`
    block (size "rail"|"inline", letters, color, optional active, waiting,
    waitingLabel, className). Follow the skeleton.tsx/input.tsx primitive shape:
    `data-slot="project-avatar"`, build classes with `cn(...)`, spread remaining
    props. Root element renders a rounded-square monogram:
    `rounded-md flex items-center justify-center text-white font-medium
    leading-none shrink-0` plus the size branch — rail = `size-8 text-sm`,
    inline = `size-5 text-[10px]` (per D-05/UI-SPEC). Apply the background via
    INLINE STYLE `style={{ backgroundColor: color }}` — NEVER a Tailwind class
    (the hex is a runtime palette value; per UI-SPEC the 9 hues are reserved
    exclusively for this background). Render `{letters}` as the only child text.
    Letter color is ALWAYS text-white — do not derive it from `color`.
    Active marker (RAIL ONLY, when `active` is true): add
    `ring-2 ring-sidebar-ring ring-offset-2 ring-offset-sidebar` (D-06).
    Waiting dot (RAIL ONLY, when `waiting` is true): make the root `relative`
    and render a child dot span `absolute -top-0.5 -right-0.5 size-2
    rounded-full bg-amber-400` — STATIC, never `animate-pulse` (D-07; matches
    the existing chip which never pulses). When `waiting` is true, put
    `waitingLabel` on the root as `aria-label` (the caller passes the existing
    "N agents waiting for input" copy). Ignore active/waiting visuals for the
    inline size (they are rail-only). Use `font-medium` for the glyph weight —
    NOT font-bold (UI-SPEC Typography is binding; the CONTEXT D-04 word "bold"
    is superseded by the approved spec's `font-medium`). Do NOT wire any call
    site here — the rail, expanded row, and settings preview are Plans 02/03.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -8 && npx eslint src/components/ui/ProjectAvatar.tsx src/lib/palette.ts 2>&1 | tail -8 && grep -q "export function ProjectAvatar\|export { ProjectAvatar" src/components/ui/ProjectAvatar.tsx && grep -q "backgroundColor" src/components/ui/ProjectAvatar.tsx && (grep -v '^[[:space:]]*//' src/components/ui/ProjectAvatar.tsx | grep -q "animate-pulse" && { echo "FAIL: avatar must be static (no animate-pulse)"; exit 1; } || echo "static OK")</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/ui/ProjectAvatar.tsx` exports `ProjectAvatar` (grep passes).
    - Background is applied via inline `backgroundColor` style, not a Tailwind `bg-*` class (grep `backgroundColor` passes).
    - The component contains NO `animate-pulse` in non-comment lines (the static gate in <verify> exits 0 / prints "static OK").
    - Source contains the exact size tokens `size-8` and `size-5`, the glyph tokens `text-sm` and `text-[10px]`, the dot `size-2 ... bg-amber-400`, the active ring `ring-2 ring-sidebar-ring`, and `text-white font-medium` (verifiable by reading the source against the UI-SPEC Spacing/Typography tables). Glyph weight is `font-medium`, never `font-bold`.
    - `cd web && npm run build` exits 0 and `npx eslint` is clean for the two changed files.
  </acceptance_criteria>
  <done>ProjectAvatar renders a rounded-md white-on-icon_color monogram in rail/inline sizes, optional rail-only active ring + static amber waiting dot with aria-label; build + lint clean; no call sites wired yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (project list/GET) | `icon_letters` + `icon_color` arrive from the local Go API; both were validated server-side at create/PATCH (Phase 18 D-10/D-11). This plan only renders them. |

This phase is frontend-only and adds NO new network/auth/storage surface
(Phase 18 shipped the validated PATCH path; this plan creates two render
artifacts with zero I/O).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering / Injection | ProjectAvatar `backgroundColor: color` | mitigate | `color` is interpolated ONLY into the `backgroundColor` of a constrained `<div>` style object — never into a class name, `dangerouslySetInnerHTML`, URL, or arbitrary CSS. React serializes the style value as a string property; even a malformed value cannot escape the style attribute. `icon_color` is also a palette-membership-validated server value (Phase 18 D-11), not arbitrary user CSS. No style-injection vector. |
| T-19-02 | Injection (XSS) | ProjectAvatar `{letters}` text node | mitigate | `letters` is rendered as a React text child; React auto-escapes text content. No `dangerouslySetInnerHTML`. Value is also server-normalized to ≤2 alphanumerics (Phase 18 D-10). |
| T-19-S | Spoofing | — | N/A | Local-only single-user app, no auth surface; no identity to spoof. |
| T-19-R | Repudiation | — | N/A | No state-changing action in this plan (pure render artifacts); nothing to log. |
| T-19-I | Information Disclosure | — | N/A | No secrets; renders only project identity data already on the client. |
| T-19-D | Denial of Service | — | N/A | No new endpoint or unbounded loop; component renders bounded ≤2-char text + a fixed 9-element const. |
| T-19-E | Elevation of Privilege | — | N/A | No new authz path; no privileged operation. |
| T-19-SC | Tampering (supply chain) | npm installs | N/A | No new npm packages this plan (UI-SPEC Registry Safety: no new installs; `cn`/`lucide` already present). No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `cd web && npm run build` exits 0 (tsc -b + vite build) after both files exist.
- `cd web && npm run lint` is clean for the two new files.
- `web/src/lib/palette.ts` hexes match internal/api/icons.go projectPalette in count, order, and value (the node assertion in Task 1).
- `web/src/components/ui/ProjectAvatar.tsx` exports ProjectAvatar, uses inline `backgroundColor`, contains no `animate-pulse`, and uses `font-medium` (not `font-bold`).
</verification>

<success_criteria>
- PROJECT_PALETTE exists and equals the 9 Go hexes byte-for-byte in order (D-13).
- ProjectAvatar exists with the documented prop contract, two sizes, rail-only active ring + static amber waiting dot, white `font-medium` letters on an inline-style icon_color background (D-04/05/06/07).
- Build + lint pass; no call sites wired (consumers are Plans 02/03).
</success_criteria>

<output>
Create `.planning/phases/19-sidebar-avatars-settings-editors/19-01-SUMMARY.md` when done
</output>
