---
phase: 05-recovery-review
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - internal/api/recovery_integration_test.go
autonomous: false
requirements: [RCVR-01, RCVR-02, REVW-01]
must_haves:
  truths:
    - "An end-to-end Go test proves restart reconciliation by architecture: a fresh Manager over the existing DB yields zero running sessions, zero bash stubs, no auto-spawn, and a DB-derived resumable status entry (RCVR-01, D-58, D-65)"
    - "The test drives resume end-to-end against fake-claude: --resume argv with the persisted uuid, same-id persistence, honest exit-1 failure flow, and the D-67 one-per-task gate on concurrent resume"
    - "A human has verified the full v1 experience end-to-end against real claude v2.1.173: all five phase goals composing, restart recovery, live resume, the Diff tab, and the carried plan-mode amber item"
  artifacts:
    - path: "internal/api/recovery_integration_test.go"
      provides: "restart reconciliation + resume lifecycle + diff endpoint integration test"
      contains: "fake-claude"
  key_links:
    - from: "recovery_integration_test.go"
      to: "session.NewManager (second, fresh instance)"
      via: "simulated restart over the same SQLite file"
      pattern: "NewManager"
    - from: "recovery_integration_test.go"
      to: "FAKE_CLAUDE_ARGS_FILE / FAKE_CLAUDE_RESUME_FAIL"
      via: "testdata/fake-claude env contract from plan 05-01"
      pattern: "FAKE_CLAUDE"
---

<objective>
Prove the whole phase and close out v1: a Go integration test covering restart reconciliation and the resume lifecycle against the fake-claude stub, full build gates, and a blocking human verification that walks the complete v1 experience — all five phase goals composing — against real claude v2.1.173.

Purpose: phase 5 sign-off (RCVR-01, RCVR-02, REVW-01 demonstrably true) AND the v1 milestone gate; this is the LAST phase.
Output: green deterministic test suite + human-approved live verification including the carried plan-mode-amber UAT item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recovery-review/05-RESEARCH.md
@.planning/phases/05-recovery-review/05-01-SUMMARY.md
@.planning/phases/05-recovery-review/05-02-SUMMARY.md
@internal/api/agent_integration_test.go
@internal/api/integration_test.go

<interfaces>
From plan 05-01 (consume, do not modify):
- `session.SpawnOpts{..., ResumeSessionID string}`; POST /api/sessions body `{task_id, kind:"agent", resume:true}`; 409 bodies "no session to resume" / "agent session already running"
- agents/status: `resumable` on every entry; DB-derived post-restart entries `{sessionId:"", status:"exited", exitCode:null, resumable:true}`
- `agentHandlers.globRoot` / `sessionHandlers.globRoot` injectable in-package for transcript fixtures (`<root>/<anydir>/<uuid>.jsonl`)
- testdata/fake-claude env contract: `FAKE_CLAUDE_ARGS_FILE` (argv recorded one-per-line), `FAKE_CLAUDE_RESUME_FAIL` (mirrors verified v2.1.173 missing-transcript behavior: error text + exit 1)

From plan 05-02: GET /api/tasks/{id}/diff → {base, totals, files[]}; 409 "task has no worktree".

Phase 4 integration conventions (internal/api/agent_integration_test.go): outer-t fixture git repo, full mux (Routes + SessionRoutes + WorktreeRoutes + HookRoutes + AgentRoutes — ADD DiffRoutes), AgentConfig{ClaudeBin: testdata/fake-claude abs path}, real SQLite in t.TempDir(), poll loops ≤2s for async transitions, marker discipline for output assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart-reconciliation + resume lifecycle integration test</name>
  <files>internal/api/recovery_integration_test.go</files>
  <read_first>internal/api/agent_integration_test.go (full — fixture + stage pattern to mirror), internal/api/testdata/fake-claude (05-01 extended contract), internal/api/resume.go, 05-RESEARCH.md §Reconciliation Design + Pitfall 4 + Pitfall 6</read_first>
  <action>
    One lifecycle test (e.g. TestRecoveryLifecycle) on the Phase 4 fixture pattern, with `globRoot` pointed at a t.TempDir() and `FAKE_CLAUDE_ARGS_FILE` at a temp path (t.Setenv — agent spawns inherit test env). Sequential subtest stages:

    1. **seed**: create project + task (worktree provisions); spawn agent (fake-claude) + one bash session; read `tasks.claude_session_id` (uuid A) from the DB. Stop the agent? NO — leave both RUNNING to simulate dying with the server.
    2. **restart**: build a SECOND, fresh `session.NewManager()` + fresh mux over the SAME *sql.DB (and same globRoot/AgentConfig). This IS the restart — the old manager is simply abandoned (its processes get stopped in test cleanup). Assert against the new mux:
       - `GET /api/sessions?task_id=N` → `[]` (no ghost sessions, no bash stubs — D-58/RCVR-01 by architecture; the DB has no row that can claim "running": verified across migrations 00001-00003)
       - `GET /api/agents/status` → `[]` while NO transcript fixture exists (never-prompted sessions are not resumable — Pitfall 4)
       - create the transcript fixture `<globRoot>/x/<uuidA>.jsonl` → status now returns exactly one DB-derived entry: sessionId "", status "exited", exitCode null, stopRequested false, resumable true
       - poll status twice more; `GET /api/sessions?task_id=N` still `[]` (NO auto-resume anywhere — D-67/anti-pattern)
    3. **resume**: `POST /api/sessions {"task_id":N,"kind":"agent","resume":true}` → 201; argv file shows line 1 == `--resume`, line 2 == uuidA, `--settings` present; `tasks.claude_session_id` still uuidA (the persist line rewrote the same value — verified same-id reuse).
    4. **one-per-task on resume (D-67)**: second resume POST while the resumed agent runs → 409 body contains "agent session already running".
    5. **honest failure (D-56)**: stop the running agent (wait for exit); delete the transcript fixture; resume POST → 409 "no session to resume" (server-side fresh validation). Then recreate the fixture, t.Setenv FAKE_CLAUDE_RESUME_FAIL=1, resume → 201 but the session exits code 1 (poll sessions ≤5s for status exited, exitCode 1); status entry shows exited/red-path data with resumable still true (transcript exists — a crashed-but-resumable session stays resumable; only deleting the fixture flips it false). Unset the env var after.
    6. **no-session-to-resume**: a SECOND task whose claude_session_id is NULL → resume POST → 409 "no session to resume".
    7. **diff endpoint smoke**: in the first task's worktree, write one modified tracked file and one untracked file (exec git/os in test); `GET /api/tasks/{id}/diff` → 200, totals.files == 2, untracked entry has status "new"; the no-worktree second task (after removing its worktree, or a third task created without provisioning) → 409 "task has no worktree". (Full diff matrix lives in internal/diff unit tests — this proves the wired path only.)

    Budget < 30s wall. Cleanup: stop all sessions from BOTH managers in t.Cleanup so no fake-claude outlives the test.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run TestRecoveryLifecycle -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/api/ -run TestRecoveryLifecycle -count=2 -timeout 240s` exits 0 (deterministic)
    - `grep -n 'NewManager' internal/api/recovery_integration_test.go | wc -l` ≥ 2 (two manager instances — a real simulated restart)
    - `grep -n '"--resume"\|--resume' internal/api/recovery_integration_test.go` returns a match (argv asserted)
    - `grep -n 'no session to resume' internal/api/recovery_integration_test.go` and `grep -n 'agent session already running' internal/api/recovery_integration_test.go` return matches
    - `grep -n 'FAKE_CLAUDE_RESUME_FAIL' internal/api/recovery_integration_test.go` returns a match (honest-failure path driven)
    - `grep -rn 'LookPath("claude")' internal/api/recovery_integration_test.go` returns nothing (real claude never spawned in CI)
  </acceptance_criteria>
  <done>Restart reconciliation, the full resume lifecycle (success, gate, honest failure, unresumable), D-58 bash silence, and the wired diff path are locked in by one deterministic test that never touches real claude.</done>
</task>

<task type="auto">
  <name>Task 2: Full build gates</name>
  <files>(none — verification only; fixes belong to owning files)</files>
  <read_first>Makefile, web/package.json (script names)</read_first>
  <action>
    Run the complete gate set and fix anything red (report what changed):
    - `go vet ./...`
    - `go test ./... -count=1` (entire backend: api, session, worktree, diff, ws, store, cmd)
    - `go test ./internal/session/ -race -count=1` (Spawn changes touched mutex-adjacent code)
    - `cd web && npm run build` (tsc + vite)
    - `make build` — the embedded single binary must still assemble
    - Dependency contract: `git diff go.mod` shows no new Go modules; `git diff web/package.json` shows only @radix-ui/react-collapsible (the sanctioned block)
  </action>
  <verify>
    <automated>go vet ./... && go test ./... -count=1 && (cd web && npm run build) && make build</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` exits 0
    - `go test ./... -count=1` exits 0
    - `go test ./internal/session/ -race -count=1` exits 0
    - `cd web && npm run build` exits 0
    - `make build` exits 0
    - `git diff go.mod` is empty; `git diff web/package.json` contains only the collapsible dependency
  </acceptance_criteria>
  <done>Everything compiles, tests pass race-clean, the single binary builds, and the dependency contract holds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — Phase 5 + the full v1 experience (real claude v2.1.173)</name>
  <files>(none)</files>
  <read_first>.planning/ROADMAP.md (all five phase Goal lines — the v1 walkthrough composes them), 05-RESEARCH.md Open Questions 1-2 + Pitfall 5, .planning/STATE.md Blockers (carried plan-mode amber item)</read_first>
  <action>
    Build and launch for the user: `make build && ./kangent`, app at http://127.0.0.1:7333. Confirm the server is up and at least one project with a worktree-backed task exists (create one if needed), then present the checklist and pause. Note: the `/terminal` dev route stays in v1 (CONTEXT discretion: costs nothing) — nothing to remove.
  </action>
  <what-built>Phase 5 complete — and with it, v1: restart reconciliation (silent, by architecture), Resume session via claude --resume with the persisted UUID in both placements, and the read-only Diff tab vs the base branch's merge-base. This checkpoint verifies Phase 5's three success criteria AND walks the entire v1 promise end-to-end.</what-built>
  <how-to-verify>
    **A. Phase 5: Recovery**
    1. **Restart reconciliation (RCVR-01, D-65)**: With an agent session running in a task (send at least one prompt first) and a bash tab open, Ctrl+C the server and restart `./kangent`. Reload the browser: NO banners or notices anywhere; the bash tab is simply gone (no stub); the task card shows a MUTED GRAY dot with tooltip `Exited` (no code, never red).
    2. **Post-restart resume (RCVR-02, D-54b)**: Open that task — the Agent tab shows the pre-start headline `Agent session ended.` with the body about picking the conversation up, `Reset session` (ghost) + `Resume session` (primary). Click Resume: claude relaunches IN THE WORKTREE with the prior conversation's history available (ask it "what were we just discussing?"). Status detection still works after resume: trigger a permission prompt → amber dot within ~5s (the hook overlay applies on resume — verified, but confirm live).
    3. **Within-run resume (D-54a)**: `/exit` claude → exited banner `Agent session ended.` now shows BOTH `Reset session` (ghost) and `Resume session` (primary). Resume continues the same conversation.
    4. **Reset-only honesty (Pitfall 4)**: On a fresh task, Start agent but type NOTHING, then Stop. The banner offers `Reset session` only — no Resume (a never-prompted session has no transcript; offering Resume would guarantee an error).
    5. **Never two PTYs (D-67)**: With an agent running, the banner/pre-start pair is absent (no Resume offered for a running agent); if you race a second tab, the loser shows the inline `Couldn't start a session. Try again.`
    **B. Phase 5: Diff tab (REVW-01)**
    6. **Diff content (D-59/D-60)**: In a bash tab in the worktree: commit one change, modify a tracked file without committing, and `echo hi > newfile.txt`. Open the Diff tab: totals bar `{N} files changed, +A −B vs {base}` (base in mono), three file sections — the untracked file marked `new` rendered as full additions, green/red line coloring, per-file ± stats. Click the refresh button after another edit — content updates in place, icon spins, no blanking.
    7. **Diff edges (D-62/D-63/D-64)**: `seq 500 > big.txt` → its section starts COLLAPSED (stats only, click to expand). A binary file (`head -c 100 /dev/urandom > bin.dat`) shows `Binary file changed`, not expandable. A fresh task's diff shows `No changes yet. The worktree matches {base}.` A task without a worktree shows the Diff tab DISABLED with tooltip `The diff needs a worktree`. Tab order: Agent, Description, Diff, Bash 1..N.
    **C. Carried UAT item (research OQ1 — REPORT EXPLICITLY)**
    8. **Plan-mode exit-plan approval → amber**: In a claude session, enter plan mode (shift+tab twice), ask for a small plan, and wait for the exit-plan approval prompt. Check the board: does the dot go pulsing AMBER? This was never confirmed in Phase 4 — report YES or NO either way (a NO means widening the Notification matcher in a follow-up; it is not a Phase 5 regression).
    **D. Full v1 walkthrough (all five phase goals composing — final milestone gate)**
    9. **End-to-end narrative**: Create a NEW project pointing at a real repo → create a task with a markdown description (worktree + branch appear automatically) → drag it to In Progress → Start agent → `Insert description` → let claude make a small change (approve the permission prompt; watch amber→green→idle dots from the board) → close the browser tab mid-run and reopen (clean reattach, history intact) → open the Diff tab and review exactly what changed → drag the task to In Review, then Done → accept the worktree-cleanup offer (branch kept) → restart the server one more time → everything (projects, board, task state) is exactly where you left it. That is the v1 promise: one place to see and drive all agent work.
  </how-to-verify>
  <verify>Human confirms all 9 checklist items against the live app with real claude v2.1.173</verify>
  <resume-signal>Type "approved" (optionally with the item-8 amber YES/NO) or describe which step failed and what you saw</resume-signal>
  <done>Phase 5's three success criteria observed live; the carried plan-mode question answered empirically; the complete v1 experience verified end-to-end.</done>
</task>

</tasks>

<verification>
- Integration test green and deterministic (`-count=2`)
- All build gates green; single binary assembles
- Human approval recorded for the live checklist, including an explicit YES/NO on the plan-mode amber item
</verification>

<success_criteria>
- RCVR-01: post-restart, no ghost sessions and no session ever auto-resumed into two PTYs (test + human-verified)
- RCVR-02: Resume session relaunches via claude --resume with the persisted ID in the worktree, in both placements, failing honestly (test + human-verified)
- REVW-01: the read-only diff tab shows the worktree's changes vs the base branch (test + human-verified)
- v1 milestone: all five phase goals demonstrated composing in one continuous walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/05-recovery-review/05-05-SUMMARY.md`
</output>
