---
phase: 05-recovery-review
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/components/ui/collapsible.tsx
  - web/src/api/diffs.ts
  - web/src/components/task/DiffTab.tsx
  - web/src/components/task/DiffFileSection.tsx
  - web/src/components/task/TaskTabs.tsx
  - web/src/pages/TaskPage.tsx
autonomous: true
requirements: [REVW-01]
must_haves:
  truths:
    - "The task view tab order is Agent, Description, Diff, Bash 1..N (D-64); on tasks without a worktree the Diff tab is present but disabled with tooltip 'The diff needs a worktree'"
    - "The Diff tab fetches on activation and on the manual refresh button only — no polling (D-61); current content stays visible while refetching"
    - "Totals bar reads '{N} files changed, +{A} −{B} vs {base}' (singular, zero segments omitted, true minus, base in mono) with a spinning-while-refetching refresh button (D-60)"
    - "Files over 400 changed lines start collapsed; binary files render header-only 'Binary file changed' (D-62); empty state reads 'No changes yet. The worktree matches {base}.' (D-63)"
    - "Diff content is read-only: selectable text, horizontal scroll per file body, no per-line actions (REVW-01)"
  artifacts:
    - path: "web/src/components/ui/collapsible.tsx"
      provides: "shadcn collapsible block (Radix — unmounts closed content, D-62 free)"
      contains: "Collapsible"
    - path: "web/src/api/diffs.ts"
      provides: "DiffResponse types mirroring the server JSON + useTaskDiff"
      contains: "useTaskDiff"
    - path: "web/src/components/task/DiffTab.tsx"
      provides: "totals bar + file list + empty/loading/error states"
      contains: "No changes yet."
    - path: "web/src/components/task/DiffFileSection.tsx"
      provides: "collapsible per-file unified diff renderer"
      contains: "Binary file changed"
  key_links:
    - from: "web/src/components/task/DiffTab.tsx"
      to: "GET /api/tasks/{id}/diff"
      via: "useTaskDiff query on mount (tab activation) + refetch()"
      pattern: "api/tasks/.*/diff"
    - from: "web/src/pages/TaskPage.tsx"
      to: "TaskTabs TabDef disabled"
      via: "Diff tab at index 2, disabled without worktree, Agent fallback when removed"
      pattern: "disabled"
---

<objective>
The read-only Diff tab (REVW-01): collapsible per-file unified diffs with the scoped green/red content palette, totals bar with manual refresh, and the disabled-without-worktree tab pattern.

Purpose: finished agent work gets reviewed without leaving the app — the In Review column finally means something.
Output: DiffTab + DiffFileSection rendering plan 05-02's structured JSON, Diff tab wired into the task view per the approved 05-UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recovery-review/05-UI-SPEC.md
@.planning/phases/05-recovery-review/05-RESEARCH.md
@web/src/components/task/TaskTabs.tsx
@web/src/pages/TaskPage.tsx
@web/src/api/worktrees.ts
@web/src/api/client.ts

<interfaces>
Server JSON from plan 05-02 (GET /api/tasks/{id}/diff — types mirror this exactly):
```ts
interface DiffResponse {
  base: string;
  totals: { files: number; additions: number; deletions: number };
  files: DiffFile[];
}
interface DiffFile {
  path: string;
  oldPath: string | null;
  status: "modified" | "new" | "deleted" | "renamed";
  binary: boolean;
  additions: number | null; // null for binary
  deletions: number | null;
  hunks: DiffHunk[];
}
interface DiffHunk { header: string; lines: DiffLine[] }
interface DiffLine { kind: "context" | "add" | "del"; text: string }
```
Errors: client.ts throws ApiError whose .message is the server's {error} string (the trimmed git stderr) — render it verbatim in the muted error line.

Current TabDef seam (web/src/components/task/TaskTabs.tsx lines 20-32): id/label/content/onClose/muted/keepMounted/leading. Task 3 adds disabled + disabledTooltip.

TaskPage facts (web/src/pages/TaskPage.tsx): tabs array built at lines 254-315 (agent, description, ...bash); tabIds memo at 127-130 drives the Pitfall-7 fallback layout effect (138-149) — a value not in tabIds reactivates the left neighbor, else "agent". The disabled Diff tab stays OUT of tabIds so that effect handles worktree removal for free.

UI-SPEC diff content palette (scoped to the Diff tab ONLY — never bleeds into chrome):
- Added line: bg-green-500/10, marker "+" in green-400 (#4ade80), text zinc-50
- Removed line: bg-red-500/10, marker "−" in red-400 (#f87171), text zinc-50
- Context line: text zinc-400, no background
- Hunk header: zinc-400 text on zinc-900 row
- Stats segments: +A green-400, −B red-400 (12px/500 tabular-nums); the totals SENTENCE stays zinc-50 — only the ± numbers are colored
- Explicitly NOT diff-colored: tab label/indicator (accent blue), refresh button (neutral ghost), section borders/headers (zinc-800/zinc-900), file paths (zinc-50 mono)
- True minus U+2212 (−) in all stats; lucide icons: chevron-right 16px (rotates 90° open), refresh-cw 16px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn collapsible block + diff API layer</name>
  <files>web/package.json, web/package-lock.json, web/src/components/ui/collapsible.tsx, web/src/api/diffs.ts</files>
  <read_first>web/components.json (registry config — official registry only), web/src/api/worktrees.ts (hook conventions), web/src/api/client.ts (get/ApiError), 05-RESEARCH.md Pattern 5 + Pattern 6</read_first>
  <action>
    1. `cd web && npx shadcn@latest add collapsible` — the ONE sanctioned new block this phase (official registry; vendors @radix-ui/react-collapsible). Verify web/src/components/ui/collapsible.tsx exports Collapsible, CollapsibleTrigger, CollapsibleContent. No components.json changes. Radix Collapsible unmounts closed content by default — exactly what D-62's collapsed >400-line files need (zero DOM cost).

    2. New web/src/api/diffs.ts — the types from the interfaces block VERBATIM, plus:
    ```ts
    export function useTaskDiff(taskId: number) {
      return useQuery({
        queryKey: ["diff", taskId],
        queryFn: () => get<DiffResponse>(`/api/tasks/${taskId}/diff`),
        // D-61: fetch on tab activation (DiffTab mounts only while active —
        // default staleTime 0 refetches on every mount) + manual refetch().
        // NO refetchInterval — never poll.
      });
    }
    ```
    TanStack keeps previous data rendered during a same-key refetch (UI-SPEC: no blanking) — no placeholderData needed. Export the ApiError-aware error: callers read `error.message` for the muted git-stderr line.
  </action>
  <verify>
    <automated>cd web && npm run build && test -f src/components/ui/collapsible.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - `test -f web/src/components/ui/collapsible.tsx` exits 0
    - `grep -n '@radix-ui/react-collapsible' web/package.json` returns a match and `git diff web/package.json` shows NO other new dependencies
    - `grep -n 'useTaskDiff' web/src/api/diffs.ts` returns a match
    - `grep -n 'refetchInterval' web/src/api/diffs.ts` returns nothing (D-61: no polling)
  </acceptance_criteria>
  <done>The sanctioned collapsible block is vendored and the typed diff query exists with fetch-on-activation semantics and zero polling.</done>
</task>

<task type="auto">
  <name>Task 2: DiffFileSection + DiffTab renderers</name>
  <files>web/src/components/task/DiffFileSection.tsx, web/src/components/task/DiffTab.tsx</files>
  <read_first>05-UI-SPEC.md Copywriting Contract (Diff tab table — every string verbatim) + Layout Contract (totals bar, file sections, diff lines, hunk headers) + Color (diff content palette), web/src/components/terminal/TerminalPane.tsx lines 84-91 (the 150ms no-flash delay pattern to copy)</read_first>
  <action>
    1. web/src/components/task/DiffFileSection.tsx — `{ file: DiffFile }`:
    - Container: `rounded-lg border border-border overflow-hidden` (1px zinc-800, Phase 1 radius).
    - BINARY: plain (non-collapsible) header row — chevron slot empty, path, then muted 12px `Binary file changed` in place of stats. Not expandable.
    - Otherwise Collapsible with `defaultOpen={(file.additions ?? 0) + (file.deletions ?? 0) <= 400}` (D-62 — changed lines = additions + deletions, computed client-side from stats).
    - CollapsibleTrigger = the ENTIRE header row (click or Enter/Space toggles — Radix native): `flex w-full items-center gap-1 bg-card px-3 py-2 hover:bg-[#27272a] focus-visible:ring-2 focus-visible:ring-blue-500 outline-none`. Contents left→right: `<ChevronRight className="size-4 shrink-0 transition-transform group-data-[state=open]:rotate-90" />` (or className on trigger state attr), path `text-sm font-mono truncate flex-1 text-left`, optional status suffix muted 12px (`new` for status "new", `deleted` for "deleted"; modified gets NO suffix; renamed shows `{oldPath} → {path}` in the path slot), stats right: `+{x}` in text-green-400 and `−{y}` in text-red-400, `text-xs font-medium tabular-nums`, gap xs, ZERO segments omitted, − is U+2212.
    - CollapsibleContent body (zinc-950): one `overflow-x-auto` div; for each hunk: header row `bg-card px-3 py-2 font-mono text-xs text-zinc-400 whitespace-pre` rendering hunk.header; then lines: `font-mono text-xs leading-normal whitespace-pre pl-3` with a fixed-width marker column (e.g. `<span className="inline-block w-4 select-none">`): add → row `bg-green-500/10`, marker `+` text-green-400, text zinc-50; del → row `bg-red-500/10`, marker `−` (U+2212) text-red-400, text zinc-50; context → marker space, text zinc-400, no background. Backgrounds span the full row width (`min-w-max` rows inside the scroll container or `w-fit min-w-full`). No line numbers, no wrapping (whitespace-pre), text selectable, NO per-line actions of any kind (REVW-01 read-only).

    2. web/src/components/task/DiffTab.tsx — `{ taskId: number }`, uses useTaskDiff(taskId):
    - Root: `flex h-full min-h-0 flex-col overflow-y-auto` — single vertical scroll container, lg (p-4) padding, full main-area width (terminal envelope, NOT the 860px island — code needs width).
    - Totals bar STICKY at top of the scroll container: `sticky top-0 z-10 flex items-center border-b border-border bg-background px-3 py-2`. Sentence (14px/400, zinc-50): `{N} files changed` with `1 file changed` singular, then when totals.additions > 0 a `+{A}` segment in text-green-400, when totals.deletions > 0 a `−{B}` segment (U+2212) in text-red-400, then `vs ` + `<span className="font-mono">{base}</span>`. Right side: icon-only ghost refresh button — `<RefreshCw className="size-4" />`, `aria-label="Refresh diff"`, Tooltip `Refresh diff`, `disabled={isFetching}`, icon gets `animate-spin motion-reduce:animate-none` while isFetching; onClick={() => refetch()}.
    - File list below: `flex flex-col gap-2 pt-2` of DiffFileSection, in server order (already path-sorted).
    - Empty state (data && files.length === 0): centered muted body line, `py-8` (2xl): `No changes yet. The worktree matches ` + mono base + `.` — no heading, no CTA.
    - Loading (isPending, no data): centered muted `Loading diff…` rendered only after a 150ms timer (copy the TerminalPane showConnecting pattern — no flash).
    - Error (isError): centered, `py-8`: `Couldn't load the diff.` (14px) + muted 12px line with `error.message` verbatim (the relayed git stderr) + `<Button variant="outline" size="sm" onClick={() => refetch()}>Reload diff</Button>`.
    - While refetching WITH data: current content stays rendered (default TanStack behavior); only the refresh button spins. Expand/collapse state resets on success swap — acceptable per UI-SPEC (the file set may have changed).
    Keep ALL copy in single template literals (grep-able contract phrases).
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - `grep -n 'No changes yet. The worktree matches ' web/src/components/task/DiffTab.tsx` returns a match
    - `grep -n "Couldn't load the diff." web/src/components/task/DiffTab.tsx` and `grep -n 'Reload diff' web/src/components/task/DiffTab.tsx` return matches
    - `grep -n 'Refresh diff' web/src/components/task/DiffTab.tsx` returns a match (aria-label + tooltip)
    - `grep -n 'file changed' web/src/components/task/DiffTab.tsx` returns a match (singular handling present)
    - `grep -n 'Binary file changed' web/src/components/task/DiffFileSection.tsx` returns a match
    - `grep -n '400' web/src/components/task/DiffFileSection.tsx` returns a match (collapse gate)
    - `grep -n 'bg-green-500/10' web/src/components/task/DiffFileSection.tsx` and `grep -n 'bg-red-500/10' web/src/components/task/DiffFileSection.tsx` return matches
    - `grep -n '−' web/src/components/task/DiffTab.tsx web/src/components/task/DiffFileSection.tsx` returns matches (true minus U+2212 in stats and del markers)
    - `grep -n 'overflow-x-auto' web/src/components/task/DiffFileSection.tsx` returns a match (long lines scroll, never wrap)
  </acceptance_criteria>
  <done>The structured diff renders per the approved UI-SPEC: loud colored change lines inside quiet neutral chrome, collapsible sections with the >400 gate, and all four data states (content/empty/loading/error) verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: TabDef disabled support + Diff tab insertion in TaskPage (D-64)</name>
  <files>web/src/components/task/TaskTabs.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>web/src/components/task/TaskTabs.tsx (full), web/src/pages/TaskPage.tsx (tabs array lines 254-315, tabIds memo 127-130, Pitfall-7 layout effect 137-149, trailing + disabled-span tooltip pattern 319-342), 05-UI-SPEC.md Interaction Contract (Diff tab table)</read_first>
  <action>
    1. web/src/components/task/TaskTabs.tsx — TabDef gains:
    ```ts
    /** Renders the trigger disabled (zinc-600 label) with an explanation tooltip. */
    disabled?: boolean;
    disabledTooltip?: string;
    ```
    In the trigger map: when tab.disabled, render the TabsTrigger with `disabled` and the label span className `text-zinc-600`, wrapped in the established disabled-tooltip pattern (Tooltip > TooltipTrigger asChild > `<span className="inline-flex">` around the trigger — disabled elements swallow pointer events, the span keeps the tooltip firing; mirror of the bash `+` pattern). Non-disabled tabs render exactly as today.

    2. web/src/pages/TaskPage.tsx — insert the Diff TabDef at index 2 (after description, before bash sessions — D-64 order Agent, Description, Diff, Bash 1..N):
    ```ts
    {
      id: "diff",
      label: "Diff",
      disabled: !task.worktree_path,
      disabledTooltip: `The diff needs a worktree`,
      // NOT keepMounted: mounting on activation IS the D-61 fetch-on-open;
      // re-activating remounts and refetches.
      content: (
        <div className="flex h-full min-h-[320px] w-full flex-col">
          <DiffTab taskId={task.id} />
        </div>
      ),
    },
    ```
    Never gets onClose (no ×), never gets leading (no dot) — the label and active indicator stay stock (accent blue), explicitly NOT diff-colored.

    3. tabIds memo: include "diff" ONLY when `task.worktree_path` is set:
    ```ts
    () => ["agent", "description", ...(task?.worktree_path ? ["diff"] : []), ...visibleSessions.map((s) => s.id)]
    ```
    Worktree removed while the Diff tab is active (Phase 3 cleanup): the tab disables in place via the tabs array, "diff" drops out of tabIds, and the EXISTING Pitfall-7 layout effect reactivates the Agent tab (the universal fallback) before paint — no new effect needed. Verify the memo deps include task?.worktree_path.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - `grep -n 'disabled?: boolean' web/src/components/task/TaskTabs.tsx` returns a match
    - `grep -n 'The diff needs a worktree' web/src/pages/TaskPage.tsx` returns a match (verbatim tooltip)
    - `grep -n 'id: "diff"' web/src/pages/TaskPage.tsx` returns a match positioned AFTER `id: "description"` and BEFORE the visibleSessions spread (verify by line numbers)
    - `grep -n 'text-zinc-600' web/src/components/task/TaskTabs.tsx` returns a match (disabled label color)
    - `grep -n 'worktree_path ? \["diff"\]' web/src/pages/TaskPage.tsx` (or equivalent) shows diff conditionally in tabIds
  </acceptance_criteria>
  <done>Diff is the permanent third tab — disabled with an explanation when no worktree exists, auto-falling back to Agent when the worktree disappears mid-view, fetch-on-activation by construction.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` passes
- `git diff web/package.json` shows exactly one new dependency: @radix-ui/react-collapsible (the sanctioned collapsible block) and nothing else
- Diff colors appear ONLY in DiffTab.tsx/DiffFileSection.tsx: `grep -rln 'bg-green-500/10\|bg-red-500/10' web/src/ | grep -v Diff` returns nothing (palette scope rule)
- All D-60..D-64 copy strings grep-able verbatim
</verification>

<success_criteria>
- REVW-01: the worktree's changes vs the base branch are reviewable in a read-only tab — totals, per-file collapsible unified diffs, binary/large-file handling, honest error and empty states — without leaving the app
</success_criteria>

<output>
After completion, create `.planning/phases/05-recovery-review/05-04-SUMMARY.md`
</output>
