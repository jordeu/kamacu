# 02-01: Added typed agents API client (Agent type + useAgents query + 4 CRUD/default mutations) mirroring the workspaces shapes; tsc clean.

<objective>
Added typed agents API client (Agent type + useAgents query + 4 CRUD/default mutations) mirroring the workspaces shapes; tsc clean..
</objective>

<tasks>
- [x] **T01**: Added typed agents API client (Agent type + useAgents query + 4 CRUD/default mutations) mirroring the workspaces shapes; tsc clean. _(S)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
