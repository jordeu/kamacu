# 02-02: Added Agent selector (shadcn Select) to Project Settings with conditional-PATCH on change; extended useUpdateProjectSettings to carry agent_id. tsc + vite green.

<objective>
Added Agent selector (shadcn Select) to Project Settings with conditional-PATCH on change; extended useUpdateProjectSettings to carry agent_id. tsc + vite green..
</objective>

<tasks>
- [x] **T02**: Added Agent selector (shadcn Select) to Project Settings with conditional-PATCH on change; extended useUpdateProjectSettings to carry agent_id. tsc + vite green. _(M)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
