---
phase: 03-worktree-isolation-bash-tabs
plan: 06
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - internal/api/integration_test.go
autonomous: false
requirements: [GIT-01, GIT-02, GIT-03, TERM-04]

must_haves:
  truths:
    - "One Go test exercises the entire lifecycle: task -> worktree+branch -> task-scoped session -> gated cleanup -> branch survives"
    - "The full repo builds: go build/vet/test green, production frontend build green"
    - "A human has verified all four phase success criteria in the browser"
  artifacts:
    - path: "internal/api/integration_test.go"
      provides: "end-to-end worktree lifecycle test over the real REST surface"
      min_lines: 100
  key_links:
    - from: "integration test"
      to: "full route stack"
      via: "one mux with Routes + SessionRoutes + WorktreeRoutes, real git repo, real session manager"
      pattern: "WorktreeRoutes"
---

<objective>
Prove Phase 3 end to end: an automated lifecycle test across all the plan seams, full build gates, and human verification of the four roadmap success criteria.

Purpose: phase-goal verification — every plan tested its slice; this proves the slices compose.
Output: integration test + verified phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-03-SUMMARY.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-04-SUMMARY.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-05-SUMMARY.md
@internal/ws/integration_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end worktree lifecycle integration test</name>
  <files>internal/api/integration_test.go</files>
  <read_first>
    - internal/ws/integration_test.go (Phase 2's integration harness conventions: real manager, httptest server, the mar''ker output trick)
    - internal/api/worktrees_test.go + internal/api/tasks_test.go (makeRepo helper, route wiring in tests — reuse, don't fork)
    - cmd/kangent/main.go (the exact production wiring order to mirror: Routes, SessionRoutes, WorktreeRoutes)
  </read_first>
  <behavior>
    One test function (subtests for stages), single mux wired like production with worktree.NewService(t.TempDir()), temp SQLite + migrations, real session.Manager, project pointing at a makeRepo temp repo with a commit on main:
    1. POST project, POST task {"title":"Fix Login"} -> 201; worktree_path set; dir exists; `git -C repo branch --list "task/fix-login-*"` non-empty (GIT-01: collision-safe slug+id, outside the repo tree).
    2. POST /api/sessions {"task_id": id} -> 201 label "Bash 1"; WriteInput pwd-marker proves cwd == worktree_path; GET /api/sessions?task_id=id lists it running (TERM-04).
    3. Write an untracked file into the worktree. GET /api/tasks/{id}/worktree -> dirty_files >= 1, running_sessions == 1.
    4. DELETE {} -> 409 "sessions running"; DELETE {"stop_sessions":true} -> 409 "worktree has uncommitted changes" (gates compose, GIT-03).
    5. DELETE {"stop_sessions":true,"force":true} -> 204; session exited; dir gone; `git branch --list` STILL shows the branch (GIT-02/D-34); GET task -> all three worktree fields null.
    6. POST /api/tasks/{id}/worktree -> 200; worktree re-created REUSING the kept branch (worktree_error null, same branch name).
  </behavior>
  <action>
    Create internal/api/integration_test.go in package api (or api_test) following the behavior stages exactly. Reuse the existing test helpers from this package's other test files; if makeRepo lives in worktrees_test.go it is already shareable within the package. Keep total runtime under ~15s (idle bash dies on SIGTERM instantly; avoid sleeping — poll with deadlines like the ws integration test). Skip with t.Skip if `git` is not on PATH (it always is here, but keep CI honest).
  </action>
  <verify>
    <automated>go test ./internal/api/ -run TestIntegration -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains all six stages (grep for: "Bash 1", "sessions running", "uncommitted changes", "branch --list")
    - Branch-survival assertion present AFTER the 204 stage
    - Re-create-after-cleanup stage asserts worktree_error null (branch reuse path proven over REST)
    - `go test ./internal/api/ -run TestIntegration -count=1` passes in < 60s
  </acceptance_criteria>
  <done>The full GIT-01 -> TERM-04 -> GIT-03 -> GIT-02 lifecycle passes over the real REST surface in one test.</done>
</task>

<task type="auto">
  <name>Task 2: Full build gates</name>
  <files>(none — verification only)</files>
  <read_first>
    - Makefile (Phase 1's single-binary build target)
  </read_first>
  <action>
    Run the complete gate set and fix anything that fails (fixes belong to the owning plan's files; small compile/lint slips are fair game here):
    1. `go build ./... && go vet ./...`
    2. `go test ./... -count=1`
    3. `cd web && npx tsc -b --noEmit && npm run build`
    4. `make build` (embedded single binary builds with the real dist)
  </action>
  <verify>
    <automated>go test ./... -count=1 && (cd web && npm run build) && make build</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0
    - `ls -la kangent 2>/dev/null || ls -la bin/ 2>/dev/null` shows a fresh binary (per Makefile output path)
  </acceptance_criteria>
  <done>Whole-tree build, vet, tests, frontend production build, and the embedded single binary all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the four phase success criteria</name>
  <files>(none — human verification)</files>
  <action>Pause and present the verification script below to the user. Do not proceed until the user responds.</action>
  <what-built>Phase 3 complete: every task gets an isolated worktree + branch on creation (with Retry/lazy-create fallbacks), bash session tabs run inside the worktree with full attach/detach, and marking Done offers gated, confirmed cleanup that always keeps the branch.</what-built>
  <how-to-verify>
    Setup: `make build && ./kangent` (or the Makefile's output path), open http://127.0.0.1:7333, use a project pointing at a real local git repo (create one if needed).

    1. GIT-01 — worktree per task:
       a. Create a task titled "Fix login flow". Open it: the meta line under the title shows `task/fix-login-flow-{id}` with a git-branch icon; hovering shows the worktree path under ~/.kangent/worktrees/.
       b. In a terminal: `git -C <repo> worktree list` shows the new path (outside the repo tree); `git -C <repo> branch --list 'task/*'` shows the branch.
       c. Open a pre-Phase-3 task (or one whose creation you broke deliberately): it shows `No worktree yet.` + `Create worktree`, and clicking it creates one (D-26).
    2. TERM-04 — bash tabs in the worktree:
       a. In the task view, click `+`: a `Bash 1` tab appears and activates; run `pwd` — it prints the worktree path. Spawn `Bash 2`.
       b. Run `sleep 300` in Bash 1, close the browser tab, reopen the task: both tabs are back (reattached, replayed). Click × on Bash 1: tooltip says `Stop and close`; the tab mutes then disappears; `ps aux | grep sleep` shows no orphan.
       c. On a task WITHOUT a worktree, `+` is disabled and its tooltip reads `Bash sessions need a worktree`.
    3. GIT-02 — Done offers cleanup, branch kept:
       a. Drag the task to Done: AFTER the card lands, the `Clean up worktree?` dialog opens with `Keep worktree` as the decline button. Decline — worktree still exists. Drag out and back into Done — it prompts again.
       b. Accept on a clean task: dialog says the branch is kept; after confirm, the meta line shows `No worktree yet.`, and `git branch --list 'task/*'` STILL shows the branch.
    4. GIT-03 — gates:
       a. With a running bash tab, open the menu item `Clean up worktree`: dialog says `1 session running...` with CTA `Stop sessions and clean up`.
       b. In a bash tab, `touch dirty.txt`, then open cleanup: warning row reports changed files and the red CTA is DISABLED until you type the worktree dir name (e.g. `fix-login-flow-3`) exactly. Confirm: sessions stop, worktree removed, branch kept, uncommitted file gone (that was the warned consequence).
  </how-to-verify>
  <resume-signal>Type "approved" if all four criteria pass, or describe what failed</resume-signal>
  <verify>User response recorded; on "approved" the phase is verified, otherwise failures route to /gsd:plan-phase 3 --gaps</verify>
  <acceptance_criteria>
    - User explicitly approved, or every reported failure is captured verbatim in the SUMMARY for gap planning
  </acceptance_criteria>
  <done>Human approval of all four ROADMAP Phase 3 success criteria recorded</done>
</task>

</tasks>

<verification>
- Integration test green, all build gates green, human approval recorded
</verification>

<success_criteria>
- All four ROADMAP Phase 3 success criteria verified by a human against the single built binary
- Requirements GIT-01, GIT-02, GIT-03, TERM-04 demonstrably satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-worktree-isolation-bash-tabs/03-06-SUMMARY.md`
</output>
