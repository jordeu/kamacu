---
phase: 03-worktree-isolation-bash-tabs
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - web/src/api/types.ts
  - web/src/api/client.ts
  - web/src/api/worktrees.ts
  - web/src/api/sessions.ts
  - web/src/components/task/WorktreeMetaLine.tsx
  - web/src/components/task/TaskTabs.tsx
  - web/src/pages/TaskPage.tsx
autonomous: true
requirements: [GIT-01, TERM-04]

must_haves:
  truths:
    - "Task view shows the branch meta line (active), the failed state with Retry, or 'No worktree yet.' with Create worktree (D-25/D-26)"
    - "The + button spawns a bash tab cwd'd in the worktree and activates it; disabled with explanation when no worktree exists (D-27/D-30)"
    - "Reopening a task shows one tab per still-running session, reattached with replay (D-28)"
    - "Closing a tab stops the session; closing the active tab activates the left neighbor else Description (D-29, Pitfall 7)"
  artifacts:
    - path: "web/src/components/task/WorktreeMetaLine.tsx"
      provides: "branch line / failed+Retry / absent+Create states per UI-SPEC copy"
      min_lines: 50
    - path: "web/src/components/task/TaskTabs.tsx"
      provides: "controlled Tabs with closable triggers and trailing slot"
      contains: "onValueChange"
    - path: "web/src/api/worktrees.ts"
      provides: "WorktreeState type, useCreateWorktree, useCleanupWorktree, worktree state fetch"
      exports: ["useCreateWorktree", "useCleanupWorktree"]
    - path: "web/src/api/sessions.ts"
      provides: "task-scoped useSessions/useSpawnSession"
      contains: "task_id"
  key_links:
    - from: "web/src/pages/TaskPage.tsx"
      to: "/api/sessions?task_id="
      via: "useSessions(taskId) polling, server is tab truth (D-28)"
      pattern: "useSessions\\(task"
    - from: "TaskTabs value"
      to: "tab removal"
      via: "neighbor activation computed in the same update that removes the tab"
      pattern: "onValueChange"
    - from: "TerminalPane"
      to: "TabsContent"
      via: "mounted unchanged per D-12 (attach-only; page owns spawning)"
      pattern: "<TerminalPane"
---

<objective>
Frontend half of GIT-01's visibility and all of TERM-04's UX: typed API layer for worktrees and task-scoped sessions, the worktree presence meta line with Retry/Create, and bash session tabs mounted into the TaskTabs seam with the full spawn/mirror/close lifecycle.

Purpose: the task view becomes the working surface — terminals inside the worktree, worktree state always visible.
Output: extended API hooks, WorktreeMetaLine, controlled TaskTabs, rebuilt TaskPage layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-CONTEXT.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-RESEARCH.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-03-SUMMARY.md

<interfaces>
<!-- Backend contract CONSUMED (created by plan 03-03): -->

Task JSON: `branch: string|null`, `worktree_path: string|null`, `worktree_error: string|null`
- worktree_path set -> active; worktree_error set -> failed (Retry); both null -> absent (Create worktree)

POST /api/tasks/{id}/worktree (empty body) -> 200 Task (success OR failure-with-worktree_error)
GET  /api/tasks/{id}/worktree -> 200 {branch, path, dirty_files, running_sessions} | 404
DELETE /api/tasks/{id}/worktree {stop_sessions, force} -> 204 | 409 | 500 {error}
POST /api/sessions {task_id} -> 201 TermSession (taskId, label "Bash N") | 404 | 409
GET  /api/sessions?task_id=N -> TermSession[] (running AND exited)

TermSession JSON gains: `taskId?: number`

<!-- Existing frontend contracts to respect (read the files): -->

TerminalPaneProps (web/src/components/terminal/TerminalPane.tsx — D-12, mount UNCHANGED):
```ts
{ sessionId: string; label: string; status: "running"|"exited"; exitCode?: number;
  onClosed?: () => void; onSessionExit?: () => void; onNewTerminal?: () => void }
```
</interfaces>

@web/src/api/sessions.ts
@web/src/api/client.ts
@web/src/api/types.ts
@web/src/components/task/TaskTabs.tsx
@web/src/pages/TaskPage.tsx
@web/src/pages/TerminalPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API layer — Task worktree fields, worktrees.ts hooks, task-scoped session hooks</name>
  <files>web/src/api/types.ts, web/src/api/client.ts, web/src/api/worktrees.ts, web/src/api/sessions.ts</files>
  <read_first>
    - web/src/api/sessions.ts + web/src/api/mutations.ts (hook shapes, invalidation keys: ["sessions"], ["task", id], ["tasks", projectId])
    - web/src/api/client.ts (del() has no body parameter today — the cleanup DELETE needs one)
    - web/src/pages/TerminalPage.tsx (calls useSessions() / useSpawnSession() with NO args — must keep compiling unchanged)
  </read_first>
  <action>
    1. types.ts: Task gains `branch: string | null; worktree_path: string | null; worktree_error: string | null;`.
    2. client.ts: `del<T = void>(path: string, body?: unknown)` — pass body through like post/patch (DELETE /worktree carries {stop_sessions, force}).
    3. New web/src/api/worktrees.ts:
       - `export interface WorktreeState { branch: string; path: string; dirty_files: number; running_sessions: number }`
       - `export function useWorktreeState(taskId: number, enabled: boolean)` — useQuery({ queryKey: ["worktree", taskId], queryFn: () => get<WorktreeState>(`/api/tasks/${taskId}/worktree`), enabled, gcTime: 0, staleTime: 0 }) — gcTime 0 + enabled-on-open is the Pitfall 8 guard: the cleanup dialog must see reality, never cache.
       - `export function useCreateWorktree(taskId: number, projectId: number)` — useMutation POST `/api/tasks/${taskId}/worktree` returning Task; onSuccess: queryClient.setQueryData(["task", taskId], task) (the response IS truth — may carry a fresh worktree_error) + invalidateQueries(["tasks", projectId]).
       - `export function useCleanupWorktree(taskId: number, projectId: number)` — useMutation((body: {stop_sessions: boolean; force: boolean}) => del(`/api/tasks/${taskId}/worktree`, body)); onSuccess: invalidate ["task", taskId], ["tasks", projectId], ["sessions", taskId].
    4. sessions.ts: TermSession gains `taskId?: number`. `useSessions(taskId?: number)` — queryKey ["sessions", taskId] when taskId given else ["sessions"]; queryFn URL `/api/sessions?task_id=${taskId}` when given; keep refetchInterval 5000. `useSpawnSession(taskId?: number)` — mutationFn POSTs `{ task_id: taskId }` when given, else no body; onSuccess: when taskId given, setQueryData<TermSession[]>(["sessions", taskId], prepend new session) BEFORE invalidating (Phase 2 spawn-select race fix per STATE.md) then invalidate ["sessions"] (prefix-matches scoped keys too). No-arg calls behave exactly as today.
  </action>
  <verify>
    <automated>cd web && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'worktree_error: string | null' web/src/api/types.ts` matches
    - `grep -n 'task_id' web/src/api/sessions.ts` matches; `grep -n 'gcTime: 0' web/src/api/worktrees.ts` matches
    - `grep -n 'setQueryData' web/src/api/sessions.ts` matches (spawn race fix)
    - TerminalPage.tsx untouched and tsc clean (optional params)
  </acceptance_criteria>
  <done>Typed hooks for every Phase 3 endpoint exist; existing pages compile without modification.</done>
</task>

<task type="auto">
  <name>Task 2: WorktreeMetaLine + TaskPage full-width layout</name>
  <files>web/src/components/task/WorktreeMetaLine.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>
    - .planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md (Worktree presence copy table + Layout Contract + Color table — copy strings are VERBATIM contract)
    - web/src/pages/TaskPage.tsx (current 860px-centered container, header row, skeleton states)
    - web/src/components/task/DescriptionTab.tsx (content that must stay at 860px)
  </read_first>
  <action>
    1. New WorktreeMetaLine.tsx, props `{ task: Task; onCreate: () => void; creating: boolean }`, three states derived from task fields (D-25/D-26):
       - Active (worktree_path set): single row, lucide `GitBranch` size-3 zinc-400 + branch in `text-xs font-mono text-muted-foreground`, xs (gap-1) icon gap, `truncate`, text selectable, no click action. Wrap in Tooltip showing task.worktree_path in mono (absolute path).
       - Failed (worktree_error set): `text-xs text-red-500` message EXACTLY `Couldn't create a worktree: {task.worktree_error}` + Button variant="outline" size="sm" label `Retry creation`; while creating: disabled, label `Creating…`. The button is standard outline, NOT red (UI-SPEC: the message carries the destructive color, never the button).
       - Absent (both null): `text-xs text-muted-foreground` EXACTLY `No worktree yet.` + outline sm button `Create worktree` / disabled `Creating…` (neutral — absence is not an error).
    2. TaskPage.tsx:
       - Wire `const createWorktree = useCreateWorktree(taskId, projectId)`; render `<WorktreeMetaLine task={task} onCreate={() => createWorktree.mutate()} creating={createWorktree.isPending} />` directly under the header row, above the tab strip (Layout Contract slot).
       - Layout: container becomes full-width (`w-full p-4`, drop `mx-auto max-w-[860px]` from the OUTER div). Header row and meta line live inside a `max-w-[860px]` wrapper; the tab strip spans full width; Description tab content gets a `max-w-[860px]` wrapper (Layout Contract: prose comfort for description, full width reserved for terminals). Keep the loading/error states' existing 860px containers.
  </action>
  <verify>
    <automated>cd web && npx tsc -b --noEmit && grep -c "Couldn't create a worktree" src/components/task/WorktreeMetaLine.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'No worktree yet.' web/src/components/task/WorktreeMetaLine.tsx` matches exactly (with period)
    - `grep -n 'Retry creation\|Create worktree\|Creating…' web/src/components/task/WorktreeMetaLine.tsx` shows all three labels
    - `grep -n 'GitBranch' web/src/components/task/WorktreeMetaLine.tsx` matches; `grep -n 'font-mono' web/src/components/task/WorktreeMetaLine.tsx` matches
    - `grep -n 'WorktreeMetaLine' web/src/pages/TaskPage.tsx` shows it rendered between header and TaskTabs
    - `grep -c 'max-w-\[860px\]' web/src/pages/TaskPage.tsx` >= 2 (header/meta wrapper + description wrapper), outer container full-width
  </acceptance_criteria>
  <done>All three worktree presence states render with verbatim UI-SPEC copy; Retry/Create round-trips through POST /worktree and reflects fresh success or fresh error; page is full-width with 860px prose islands.</done>
</task>

<task type="auto">
  <name>Task 3: Controlled TaskTabs with closable tabs + bash session lifecycle in TaskPage</name>
  <files>web/src/components/task/TaskTabs.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>
    - .planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md (Tab strip copy table + Interaction Contract "Tab strip & bash sessions" — every row is implemented here)
    - .planning/phases/03-worktree-isolation-bash-tabs/03-RESEARCH.md (§Pattern 7 + Pitfall 7 + Open Question 2 — the exact mechanism decisions)
    - web/src/components/task/TaskTabs.tsx (current TabDef seam)
    - web/src/components/terminal/TerminalPane.tsx (props contract; the hidden-tab fit guard noted in its mount effect)
    - web/src/components/ui/tabs.tsx (shadcn Tabs primitives — check TabsContent mount behavior / forceMount support)
  </read_first>
  <action>
    1. TaskTabs.tsx — make it controlled and extensible, preserving the Phase 1 seam:
       - Props: `{ tabs: TabDef[]; value: string; onValueChange: (v: string) => void; trailing?: ReactNode }`. TabDef gains `onClose?: () => void; muted?: boolean`.
       - Trigger renders label (muted -> `text-muted-foreground`) and, when onClose set, an × affordance: lucide `X` size-3, zinc-400, hover `text-zinc-50 bg-[#27272a] rounded-sm`, NEVER red (D-29 is routine, not destructive), aria-label `Stop {label} and close tab`, Tooltip `Stop and close`, xs gap from label, stopPropagation so clicking × never activates the tab. Render the × as a nested `<span role="button" tabIndex={0}>` NOT a `<button>` (button-in-button is invalid HTML inside TabsTrigger); Enter/Space triggers onClose; visible accent focus-visible ring.
       - `trailing` renders after the last trigger inside the TabsList row with `ml-2` (sm gap).
       - Strip never wraps: `overflow-x-auto` on the list. Keep the `data-active:after:bg-blue-500` indicator.
       - Render bash TabsContent kept-mounted: `forceMount` + `className="data-[state=inactive]:hidden"` so switching tabs never detaches the WS (TerminalPane's fit guard defers fitting until visible — RESEARCH flags this for live verification; if forceMount misbehaves with the fit guard, fall back to default unmount and rely on replay-on-reattach, noting it in the SUMMARY). Description content keeps default behavior.
    2. TaskPage.tsx — full lifecycle wiring (server is tab truth, D-28):
       - `const { data: sessions } = useSessions(taskId)`; `const spawn = useSpawnSession(taskId)`; `const stopSession = useStopSession()`; `const deleteSession = useDeleteSession()`.
       - `const [activeTab, setActiveTab] = useState("description")`; `const [closingIds, setClosingIds] = useState<Set<string>>(new Set())`; `const [keepExitedIds, setKeepExitedIds] = useState<Set<string>>(new Set())` — ids seen RUNNING during this mount (RESEARCH Open Question 2: exited tab stays muted until banner-Close within a visit; later revisits show running sessions only). Track via an effect: every session currently running for this task joins keepExitedIds.
       - Visible bash sessions = sessions where status === "running" OR (status === "exited" AND keepExitedIds.has(id) AND NOT closingIds.has(id)). When a CLOSING session leaves the running list (poll or onSessionExit), remove its tab (drop from keepExited) — user-initiated close removes, self-exit keeps muted (D-29 vs exited-banner row of the Interaction Contract).
       - Tabs array: description first (no onClose), then visible sessions in spawn order — sort by createdAt ASCENDING then label (server List is newest-first; the strip is `Bash 1, Bash 2, ...` left-to-right per UI-SPEC tab-order row). Each bash TabDef: id = session.id, label = session.label, muted = closingIds.has(id) || status === "exited", onClose: running && !closing -> add to closingIds + stopSession.mutate(id); content = `<TerminalPane key={id} sessionId={id} label={label} status={status} exitCode={exitCode} onSessionExit={() => invalidate ["sessions", taskId]} onClosed={() => removeTab(id, {deleteServerSide: true})} onNewTerminal={handleSpawn} />` wrapped in a full-width flex column `min-h-[320px]` filling viewport height below the strip (bash content is exempt from the 860px constraint).
       - `removeTab(id)`: compute next active in the SAME state update — if removing the active tab, activate the left neighbor in the tabs array else "description" (Pitfall 7: a Radix Tabs value pointing at a removed tab renders blank). For banner-Close on exited sessions also fire deleteSession.mutate(id) to free the server ring (mirrors TerminalPage).
       - `handleSpawn`: spawn.mutate(undefined, { onSuccess: (s) => { add s.id to keepExitedIds; setActiveTab(s.id); } }) — the hook's setQueryData makes the tab render immediately. Click-handler only, never effects (StrictMode double-spawn).
       - Trailing `+` button: ghost icon Button size 28px square with lucide `Plus` size-4; disabled when `!task.worktree_path || spawn.isPending`; aria-label `New bash session`; Tooltip: enabled -> `New bash session`, disabled-for-worktree -> `Bash sessions need a worktree` (wrap disabled button in a span so the tooltip fires — D-30). Inline spawn error right of the button: `text-xs text-red-500` EXACTLY `Couldn't start a session. Try again.` cleared on next attempt (spawn.isError && !spawn.isPending).
       - Esc-to-board: existing isTypingTarget covers the xterm helper textarea (it IS a textarea) — verify, don't rebuild.
  </action>
  <verify>
    <automated>cd web && npx tsc -b --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'onValueChange' web/src/components/task/TaskTabs.tsx` matches (controlled); `grep -n 'defaultValue' web/src/components/task/TaskTabs.tsx` returns nothing
    - `grep -n 'trailing' web/src/components/task/TaskTabs.tsx` matches; `grep -n 'onClose' web/src/components/task/TaskTabs.tsx` matches
    - `grep -n 'Bash sessions need a worktree' web/src/pages/TaskPage.tsx` matches exactly
    - `grep -n "Couldn't start a session. Try again." web/src/pages/TaskPage.tsx` matches (Phase 2 copy verbatim)
    - `grep -n 'useSessions(task' web/src/pages/TaskPage.tsx` matches; `grep -n 'min-h-\[320px\]' web/src/pages/TaskPage.tsx` matches
    - `grep -n '<TerminalPane' web/src/pages/TaskPage.tsx` matches with key={...} (clean remount per session)
    - Production build succeeds
  </acceptance_criteria>
  <done>Bash tabs spawn/attach/mirror/close per the full UI-SPEC interaction contract; tab=session is the only model; Description never closable; neighbor activation never leaves a dangling Tabs value.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc -b --noEmit && npm run build` pass
- Dev smoke: `go run ./cmd/kangent --dev-origin localhost:5173 --dev-origin 127.0.0.1:5173` + `cd web && npm run dev` — open a task with a worktree: meta line shows branch; `+` spawns Bash 1 with cwd = worktree (`pwd`); close × stops it; task without worktree shows disabled `+` with tooltip
</verification>

<success_criteria>
- Phase success criterion 2 (bash tabs in worktree cwd) fully working end-to-end in the browser
- GIT-01's user-visible surface (branch line, failed Retry, absent Create) complete with verbatim UI-SPEC copy
</success_criteria>

<output>
After completion, create `.planning/phases/03-worktree-isolation-bash-tabs/03-04-SUMMARY.md`
</output>
