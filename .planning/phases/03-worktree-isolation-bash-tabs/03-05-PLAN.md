---
phase: 03-worktree-isolation-bash-tabs
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - web/src/components/task/CleanupWorktreeDialog.tsx
  - web/src/pages/TaskPage.tsx
  - web/src/components/board/Board.tsx
autonomous: true
requirements: [GIT-02, GIT-03]

must_haves:
  truths:
    - "Moving a task with a worktree into Done opens the cleanup offer AFTER the move persists; declining keeps the worktree (D-31)"
    - "The same dialog is reachable from the task view ellipsis menu (D-31 'Both')"
    - "Running sessions show 'N sessions running' with a 'Stop sessions and clean up' CTA — never silent kills (D-32)"
    - "Uncommitted changes require typing the worktree dir name to enable the CTA; the branch-kept reassurance appears in every variant (D-33/D-34)"
  artifacts:
    - path: "web/src/components/task/CleanupWorktreeDialog.tsx"
      provides: "single AlertDialog, 4 state-derived variants, fresh state fetch on open"
      min_lines: 120
    - path: "web/src/components/board/Board.tsx"
      provides: "done-transition trigger at the moveTask call site"
      contains: "CleanupWorktreeDialog"
  key_links:
    - from: "CleanupWorktreeDialog"
      to: "GET /api/tasks/{id}/worktree"
      via: "useWorktreeState(taskId, open) — fetched on open, never stale cache (Pitfall 8)"
      pattern: "useWorktreeState"
    - from: "Board.tsx handleDragEnd"
      to: "cleanup dialog"
      via: "moveTask.mutate onSuccess callback at the CALL SITE (status done + worktree_path)"
      pattern: "onSuccess"
    - from: "confirm CTA"
      to: "DELETE /api/tasks/{id}/worktree"
      via: "useCleanupWorktree with {stop_sessions, force} derived from fetched state"
      pattern: "useCleanupWorktree"
---

<objective>
The cleanup dialog family and its two triggers — the GIT-02/GIT-03 user surface. One AlertDialog whose body composes from fresh server state into the four UI-SPEC variants, fired on every transition into Done and from the task-view menu.

Purpose: confirmed, never-silent worktree cleanup; the vibe-kanban lost-work bug class designed out.
Output: CleanupWorktreeDialog component, ellipsis menu item, Board done-transition trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-CONTEXT.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-04-SUMMARY.md

<interfaces>
<!-- CONSUMED (plans 03-03/03-04): -->

```ts
// web/src/api/worktrees.ts
interface WorktreeState { branch: string; path: string; dirty_files: number; running_sessions: number }
useWorktreeState(taskId: number, enabled: boolean)   // gcTime 0, fetch-on-open
useCleanupWorktree(taskId: number, projectId: number) // DELETE {stop_sessions, force}; invalidates task/tasks/sessions
```
DELETE responses: 204 | 409 {"error":"sessions running"} | 409 {"error":"worktree has uncommitted changes"} | 500 {"error":"<git error>"}
Task JSON: worktree_path: string|null (trigger predicate)
ApiError (web/src/api/client.ts): .message carries the server error string

<!-- Variant copy (UI-SPEC, VERBATIM — title always `Clean up worktree?`): -->
(a) clean+idle:    `The worktree for "{task title}" will be removed.`
(b) sessions:      `{N} sessions running in this worktree will be stopped.` + `The worktree will be removed.`
(c) dirty:         warning row: `This worktree has {N} changed files with uncommitted work. Removing it permanently deletes those changes.` + type-to-confirm
(d) dirty+sessions: (b) line, then (c) row + type-to-confirm
Every variant ends: `The branch {branch} is kept.` (branch mono)
CTA: sessions>0 ? `Stop sessions and clean up` : `Remove worktree` (destructive); in-flight `Cleaning up…`
Cancel: trigger==="done" ? `Keep worktree` : `Cancel`
Type-to-confirm label: `Type {slug}-{id} to confirm` — target = worktree DIR NAME (basename of path), mono, exact case-sensitive match
Failure inline: `Couldn't remove the worktree: {git error}`
Pluralize: `1 session running` / `3 sessions running`, `1 changed file` / `5 changed files`
</interfaces>

@web/src/components/task/DeleteTaskDialog.tsx
@web/src/components/board/Board.tsx
@web/src/pages/TaskPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CleanupWorktreeDialog — four variants, fresh-state fetch, type-to-confirm gate</name>
  <files>web/src/components/task/CleanupWorktreeDialog.tsx</files>
  <read_first>
    - .planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md (Cleanup dialog family table + Interaction Contract "Cleanup flow" — the auditor checks these rows)
    - web/src/components/task/DeleteTaskDialog.tsx (the established AlertDialog + destructive-confirmation pattern to mirror)
    - web/src/api/worktrees.ts (hooks from 03-04 — consume, don't reimplement)
    - .planning/phases/03-worktree-isolation-bash-tabs/03-RESEARCH.md (Pitfall 8 — stale-state hazard this component exists to prevent)
  </read_first>
  <action>
    Create CleanupWorktreeDialog.tsx, props `{ open: boolean; onOpenChange: (o: boolean) => void; taskId: number; taskTitle: string; projectId: number; trigger: "done" | "menu" }`:
    - State fetch: `useWorktreeState(taskId, open)` — variant derives from the RESPONSE, never from board cache. While loading, render title + a one-line Skeleton body with footer buttons disabled (no variant guessing). If the fetch 404s (worktree vanished between trigger and open), close the dialog silently.
    - Variant derivation: sessions = running_sessions, dirty = dirty_files; (a) both 0, (b) sessions>0 && dirty===0, (c) dirty>0 && sessions===0, (d) both >0. Compose body paragraphs per the interface block VERBATIM, sm (space-y-2) gaps. Dirty warning row: lucide `TriangleAlert` size-4 zinc-400 + zinc-50 body text, md horizontal padding, xs icon gap — NEUTRAL colors, the dialog's red budget belongs to the CTA alone (UI-SPEC Color table). Branch values inline mono (`font-mono`).
    - Branch-kept reassurance LAST in every variant: `The branch {branch} is kept.`
    - Type-to-confirm (dirty>0 only): 12px Label `Type {target} to confirm` with target = `state.path.split("/").pop()` rendered mono; Input mono, empty placeholder, autoFocus, no red border states; CTA disabled until input === target (exact, case-sensitive); Enter in the input activates confirm only when enabled.
    - Footer: Cancel label by trigger prop (`Keep worktree` for "done", `Cancel` for "menu"); confirm = destructive Button, label per sessions count, disabled while gate unmet or mutation pending; pending label `Cleaning up…` (covers stop+remove in one state).
    - Confirm: `useCleanupWorktree(taskId, projectId).mutate({ stop_sessions: sessions > 0, force: dirty > 0 })`. onSuccess: onOpenChange(false) (hook invalidations land the task in the absent state; tabs vanish because their sessions were stopped). onError (ApiError): inline `text-xs text-red-500` above the footer: `Couldn't remove the worktree: {error.message}`; CTA re-enabled; ALSO refetch the worktree state query so a 409 raced-state error re-derives the correct variant (Pitfall 8 server-side re-check showing through). Reset input + error when `open` flips.
    - Declining (cancel or Esc) does nothing beyond closing — no code path in this component stops sessions or removes anything without the explicit confirm (D-32 "never silent").
  </action>
  <verify>
    <automated>cd web && npx tsc -b --noEmit && grep -c "is kept." src/components/task/CleanupWorktreeDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Clean up worktree?' web/src/components/task/CleanupWorktreeDialog.tsx` matches (title)
    - `grep -n 'Stop sessions and clean up' web/src/components/task/CleanupWorktreeDialog.tsx` and `grep -n 'Remove worktree' ...` and `grep -n 'Cleaning up…' ...` all match
    - `grep -n 'Keep worktree' web/src/components/task/CleanupWorktreeDialog.tsx` matches (done-trigger cancel label)
    - `grep -n 'permanently deletes those changes' web/src/components/task/CleanupWorktreeDialog.tsx` matches (variant c warning)
    - `grep -n "Couldn't remove the worktree" web/src/components/task/CleanupWorktreeDialog.tsx` matches
    - `grep -n 'useWorktreeState(taskId, open)' web/src/components/task/CleanupWorktreeDialog.tsx` matches (fetch-on-open)
    - `grep -n 'TriangleAlert' web/src/components/task/CleanupWorktreeDialog.tsx` matches; no amber/yellow classes anywhere in the file
  </acceptance_criteria>
  <done>One dialog renders all four variants from fresh server state with verbatim copy; the type-to-confirm gate and pending/failure states match the UI-SPEC interaction contract row by row.</done>
</task>

<task type="auto">
  <name>Task 2: Triggers — Done transition on the board + task-view menu item</name>
  <files>web/src/components/board/Board.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>
    - web/src/components/board/Board.tsx (handleDragEnd — the ONLY path into Done; the moveTask.mutate call site where the onSuccess callback attaches per RESEARCH §Pattern 6)
    - web/src/api/mutations.ts (useMoveTask — do NOT modify the shared hook; the callback belongs at the call site)
    - web/src/pages/TaskPage.tsx (ellipsis DropdownMenu — current sole item is destructive `Delete task`)
    - .planning/phases/03-worktree-isolation-bash-tabs/03-UI-SPEC.md (Interaction Contract "Cleanup flow" + Worktree presence menu-item row)
  </read_first>
  <action>
    1. Board.tsx (D-31 done-trigger):
       - Add `const [cleanupTask, setCleanupTask] = useState<Task | null>(null)`.
       - In handleDragEnd, attach a call-site callback: `moveTask.mutate({ id: activeId, status, afterId }, { onSuccess: (t) => { if (status === "done" && t.worktree_path) setCleanupTask(t); } })` — fires AFTER the move persists (server response), never blocks or rolls back the move; the response Task carries worktree fields (plan 03-03 taskColumns), so no stale-cache predicate. Declining keeps the worktree; re-entering Done prompts again (no suppression state).
       - Render once after DragOverlay: `{cleanupTask && <CleanupWorktreeDialog open onOpenChange={(o) => { if (!o) setCleanupTask(null); }} taskId={cleanupTask.id} taskTitle={cleanupTask.title} projectId={projectId} trigger="done" />}`.
    2. TaskPage.tsx (D-31 menu path):
       - Add `const [cleanupOpen, setCleanupOpen] = useState(false)`.
       - In the ellipsis DropdownMenuContent: when `task.worktree_path` is set, render neutral item `Clean up worktree` (NOT the destructive variant — it opens a gated dialog; `Delete task` stays the menu's only red item) followed by a DropdownMenuSeparator, ABOVE `Delete task`. Import DropdownMenuSeparator from the existing ui/dropdown-menu module.
       - Render `<CleanupWorktreeDialog open={cleanupOpen} onOpenChange={setCleanupOpen} taskId={task.id} taskTitle={task.title} projectId={projectId} trigger="menu" />`.
       - After successful cleanup the task cache invalidation flips the meta line to `No worktree yet.` and disables `+` — no extra wiring needed (verify visually in dev smoke).
  </action>
  <verify>
    <automated>cd web && npx tsc -b --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'onSuccess' web/src/components/board/Board.tsx` matches inside handleDragEnd's mutate call; `grep -n 'worktree_path' web/src/components/board/Board.tsx` matches
    - `grep -c 'onSuccess' web/src/api/mutations.ts` unchanged from before this task (shared hook untouched — verify with git diff)
    - `grep -n 'Clean up worktree' web/src/pages/TaskPage.tsx` matches as a neutral DropdownMenuItem (no variant="destructive" on it)
    - `grep -n 'DropdownMenuSeparator' web/src/pages/TaskPage.tsx` matches
    - `grep -n 'trigger="done"' web/src/components/board/Board.tsx` and `grep -n 'trigger="menu"' web/src/pages/TaskPage.tsx` match
    - Production build succeeds
  </acceptance_criteria>
  <done>Dragging a worktree-bearing task into Done opens the offer after persist; the menu item opens the same dialog with `Cancel`; declining never touches the worktree; cleanup lands the task view in the absent state.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc -b --noEmit && npm run build` pass
- Dev smoke: drag a task with worktree to Done -> dialog variant (a); open a bash tab and `touch x.txt` -> menu cleanup shows variant (d) with `1 changed file`... wait, with a running session AND dirt it must show the sessions line + warning row + type-to-confirm; type the dir name -> CTA enables -> confirm -> tabs gone, `No worktree yet.` shown, `git branch --list 'task/*'` still has the branch
</verification>

<success_criteria>
- Phase success criteria 3 and 4 fully working in the browser: offer-on-Done keeping the branch, dirty warning with typed confirmation, explicit stop-sessions gate, no silent path
</success_criteria>

<output>
After completion, create `.planning/phases/03-worktree-isolation-bash-tabs/03-05-SUMMARY.md`
</output>
