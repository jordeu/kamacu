---
phase: 03-worktree-isolation-bash-tabs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/manager.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/api/sessions.go
autonomous: true
requirements: [TERM-04]

must_haves:
  truths:
    - "Spawn accepts a working directory and task association; the shell actually starts in that cwd"
    - "Task-scoped sessions get labels 'Bash 1', 'Bash 2'... monotonic PER TASK, never reused; task-less sessions keep 'bash #N'"
    - "Spawn with a nonexistent cwd fails cleanly before any PTY is allocated"
    - "StopAllForTask stops every running session of a task and blocks until all have exited"
  artifacts:
    - path: "internal/session/manager.go"
      provides: "SpawnOpts{Cwd, TaskID}, Spawn(opts), ListByTask, StopAllForTask, per-task label counters"
      exports: ["SpawnOpts"]
      contains: "func (m *Manager) Spawn(opts SpawnOpts)"
    - path: "internal/session/session.go"
      provides: "Info gains TaskID for REST filtering"
      contains: "TaskID"
  key_links:
    - from: "internal/api/sessions.go"
      to: "Manager.Spawn"
      via: "updated call with empty SpawnOpts (dev route behavior preserved)"
      pattern: "Spawn\\(session\\.SpawnOpts\\{\\}\\)"
    - from: "SpawnOpts.Cwd"
      to: "exec.Cmd.Dir"
      via: "validated assignment before pty.StartWithSize"
      pattern: "cmd\\.Dir"
---

<objective>
Extend the Phase 2 session manager so sessions can run in a worktree cwd and belong to a task — the engine half of TERM-04. Pure `internal/session` work plus the minimal caller fix in `internal/api/sessions.go` to keep the tree compiling; REST task-wiring (spawn body, list filter) is plan 03-03.

Purpose: bash tabs need Spawn(cwd, taskID), task-scoped labels, and StopAllForTask (cleanup gate, D-32).
Output: extended manager with tests; existing /terminal dev route behavior unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-CONTEXT.md
@.planning/phases/03-worktree-isolation-bash-tabs/03-RESEARCH.md

<interfaces>
<!-- Contract this plan CREATES. Plan 03-03's REST layer and 03-06's e2e test build against this. -->

```go
package session

// SpawnOpts configures a new session.
type SpawnOpts struct {
    Cwd    string // "" -> user home (preserves Phase 2 /terminal dev behavior)
    TaskID int64  // 0 -> unscoped dev session, label "bash #N" (global counter)
}

func (m *Manager) Spawn(opts SpawnOpts) (*Session, error)

// ListByTask returns Info snapshots for sessions with the given TaskID,
// newest first (same ordering contract as List).
func (m *Manager) ListByTask(taskID int64) []Info

// StopAllForTask stops every RUNNING session of the task concurrently and
// blocks until all have fully exited (each Stop already blocks through the
// 5s SIGTERM grace; concurrent calls are safe and idempotent).
func (m *Manager) StopAllForTask(taskID int64)

// Info gains:
type Info struct {
    // ... existing fields unchanged ...
    TaskID int64 `json:"taskId,omitempty"` // 0 omitted for dev sessions
}
```
</interfaces>

@internal/session/session.go
@internal/session/manager.go
@internal/api/sessions.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpawnOpts with cwd validation, task association, per-task labels, ListByTask</name>
  <files>internal/session/manager.go, internal/session/session.go, internal/session/session_test.go</files>
  <read_first>
    - internal/session/manager.go (current Spawn — the global counter, env construction, pty.StartWithSize flow you are extending)
    - internal/session/session.go (Session struct, Info, the mutex discipline; do NOT touch pump/waitExit/Stop internals)
    - internal/session/session_test.go (existing test style; the mar''ker quote-splitting trick from STATE.md: PTY command echo must never satisfy output assertions)
    - .planning/phases/03-worktree-isolation-bash-tabs/03-RESEARCH.md (§Pattern 5 — the exact recommended shape)
  </read_first>
  <behavior>
    - Spawn(SpawnOpts{}) behaves exactly as Phase 2's Spawn(): cwd = home, label "bash #N" global counter (existing tests must keep passing unmodified except for the call-site signature).
    - Spawn(SpawnOpts{Cwd: dir, TaskID: 7}) -> Info().TaskID == 7, label "Bash 1"; a second spawn for task 7 -> "Bash 2"; a spawn for task 8 -> "Bash 1" (counters are per-task); after stopping task 7's "Bash 1", the next task-7 spawn is "Bash 3" (monotonic, never reused — UI-SPEC).
    - Cwd is real: spawn with Cwd = t.TempDir(), WriteInput("echo mar''ker:$PWD\r"), poll Snapshot() until it contains "marker:"+dir (the quote-split means the echoed COMMAND can never match).
    - Spawn(SpawnOpts{Cwd: "/nonexistent/path-xyz"}) -> error containing the path, returned BEFORE pty start (no session registered: List() length unchanged).
    - ListByTask(7) returns only task-7 sessions, newest first; ListByTask(0) returns only unscoped sessions.
  </behavior>
  <action>
    In internal/session:
    - Add `SpawnOpts` and change signature to `Spawn(opts SpawnOpts)`. Inside: if opts.Cwd != "" -> `fi, err := os.Stat(opts.Cwd)`; on error or !fi.IsDir() return fmt.Errorf("working directory does not exist: %s", opts.Cwd) BEFORE any PTY allocation (RESEARCH §Pattern 5: a deleted worktree must produce the clean "Couldn't start a session" path, not a confusing shell error). cmd.Dir = opts.Cwd when set, else home. Env construction unchanged.
    - Manager gains `taskCounters map[int64]int` (init in NewManager). Label logic: opts.TaskID == 0 -> existing `m.counter` -> "bash #%d" (dev route unchanged); else increment taskCounters[TaskID] -> "Bash %d" (capital B, space, task-scoped ordinal — UI-SPEC copy contract). Counters are never decremented or reset.
    - Session struct gains `taskID int64`; Info gains `TaskID int64 \`json:"taskId,omitempty"\`` populated in Info().
    - Add `ListByTask(taskID int64) []Info`: same snapshot+sort as List(), filtered on taskID (reuse the sort by extracting a helper or filtering the List loop — do not duplicate the comparator).
    RED: extend session_test.go first (compile failures count as RED for the signature change). GREEN: implement. Keep ALL existing Phase 2 tests passing — they are the regression net for TERM-05/TERM-06.
  </action>
  <verify>
    <automated>go test ./internal/session/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'type SpawnOpts struct' internal/session/manager.go` matches with Cwd string and TaskID int64 fields
    - `grep -n 'taskId,omitempty' internal/session/session.go` matches
    - `grep -n '"Bash %d"' internal/session/manager.go` AND `grep -n '"bash #%d"' internal/session/manager.go` both match (both label families preserved)
    - Test asserting "Bash 3" after a stop exists (never-reused counter)
    - Cwd validation test asserts error BEFORE registration (List length unchanged)
  </acceptance_criteria>
  <done>Spawn carries cwd + task identity, labels follow the per-task "Bash N" / global "bash #N" split, ListByTask filters correctly, all Phase 2 session tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StopAllForTask + compile-fix the REST caller</name>
  <files>internal/session/manager.go, internal/session/session_test.go, internal/api/sessions.go</files>
  <read_first>
    - internal/session/session.go (Stop contract: idempotent, blocks until Done; signalSession/sessionPGIDs — do not modify)
    - internal/api/sessions.go (create handler — the one call site to fix this plan; full task wiring happens in 03-03)
  </read_first>
  <behavior>
    - Spawn two sessions for task 5 and one for task 6. StopAllForTask(5) returns only after both task-5 sessions report StatusExited in Info(); the task-6 session is still StatusRunning.
    - StopAllForTask on a task with no sessions returns immediately (no panic, no block).
    - Calling StopAllForTask twice is safe (Stop is idempotent).
  </behavior>
  <action>
    - Add `StopAllForTask(taskID int64)`: under m.mu, collect `*Session` pointers whose taskID matches and Info().Status == StatusRunning; release the lock; launch one goroutine per session calling s.Stop() with a sync.WaitGroup; Wait(). (RESEARCH Open Question 3: concurrent stops collapse the worst case to a single 5s grace window — required because the DELETE /worktree handler in 03-03 calls this inline in the request.)
    - For fast tests, spawn task-scoped sessions and send "exit\r" is NOT needed — Stop's SIGTERM kills idle bash within the grace; to keep the test under ~2s, set a shorter termGrace via the existing test seam if one exists, otherwise accept SIGTERM (idle bash dies on SIGTERM immediately, no grace wait — verify in existing Phase 2 stop tests how they keep runtime low and copy that approach).
    - In internal/api/sessions.go `create`: change `h.mgr.Spawn()` to `h.mgr.Spawn(session.SpawnOpts{})` — nothing else (task_id body parsing is plan 03-03's task; this keeps wave-1 file ownership clean and the tree compiling).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/session/ ./internal/api/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'func (m \*Manager) StopAllForTask' internal/session/manager.go` matches
    - `grep -n 'sync.WaitGroup' internal/session/manager.go` matches (concurrent stop)
    - `grep -n 'Spawn(session.SpawnOpts{})' internal/api/sessions.go` matches
    - `go build ./...` succeeds (whole tree compiles)
    - Test exists asserting non-matching task's session still running after StopAllForTask
  </acceptance_criteria>
  <done>StopAllForTask stops exactly the task's running sessions and blocks until done; whole repo compiles; `go test ./internal/session/ ./internal/api/` green.</done>
</task>

</tasks>

<verification>
- `go build ./... && go vet ./internal/session/ && go test ./internal/session/ ./internal/api/` all pass
- Existing Phase 2 behaviors regression-tested: detach/reattach replay, process-tree Stop, exit notification (existing tests untouched and green)
- No file outside internal/session/ + internal/api/sessions.go modified
</verification>

<success_criteria>
- Plan 03-03 can implement `POST /api/sessions {task_id}` and `DELETE /api/tasks/{id}/worktree {stop_sessions:true}` purely against SpawnOpts / ListByTask / StopAllForTask without touching the manager again.
- /terminal dev route behavior byte-identical (global "bash #N" labels, home cwd).
</success_criteria>

<output>
After completion, create `.planning/phases/03-worktree-isolation-bash-tabs/03-02-SUMMARY.md`
</output>
