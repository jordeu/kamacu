---
phase: 17-hardening-e2e
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - internal/api/e2e_opencode_resume_test.go
  - .planning/phases/17-hardening-e2e/17-UAT.md
autonomous: true
requirements: [GSESS-02]
must_haves:
  truths:
    - "opencode restart-resume through the real binary (D-52 / GSESS-02 opencode half): with the system OpenCode agent's command PATCHed to an argv-recording wrapper that execs the real opencode, a global agent spawned against a folder root captures a real ses_ session id; after SIGTERM + a second process on the same --db, POST /api/sessions {scope:global,kind:agent,resume:true} returns 201 and the wrapper's argv file contains the consecutive pair -s ses_<id> (the engine-branched resume append, sessions.go:571-574)"
    - "The capture half is observable over HTTP: after the out-of-band first turn and stopping the agent, GET /api/agents/status shows the global opencode entry resumable:true (opencode keys resumable off the persisted opencode_session_id alone — no transcript)"
    - "The D-54 opencode refusal edge: after clearing the root (which clears the persisted ids), resume:true returns 409 with body containing 'no global opencode session to resume'"
    - "The leg runs with REAL HOME (opencode resolves real config/auth — NOT HOME-sandboxed) but an isolated TMUX_TMPDIR sandbox and a dedicated temp --db; the wrapper agent's command is restored via PATCH in a t.Cleanup registered immediately after the mutation; the test skips when opencode/tmux/git are absent from PATH"
    - "17-UAT.md exists in the 16-UAT.md shape (D-59): frontmatter status/phase/source/started/updated, numbered flows with one-line expected: and result: fields, a Summary counter block (total/passed/issues/pending/skipped/blocked) and a Gaps section"
    - "The UAT flows cover D-59's locked list: configure (folder root against the user-picked real repo) → agent → bash → reattach → stop; BOTH engines' same-conversation resume (D-53); the interlock sanity (SC3); the sentinel-leak visual sanity (SC4); the D-12 bar-row clarity check with a task row and the global row live simultaneously (D-61 — default KEEP text-only; flip only on a real confusion, recorded as a decision + future work)"
    - "The UAT doc carries the mandatory safety-posture paragraph (D-60): it names the concrete risk — a --dangerously-skip-permissions agent writing directly into the checkout the user picked (no worktree net, no diff tab, no gated cleanup) — and suggests, without requiring, a disposable clone for wary users"
    - "Every browser string the UAT expects is quoted verbatim in backticks (e.g. 'Agent session ended.', 'Resume session', 'Reset session', 'Global · Scratchpad', 'No root configured yet.', 'no global claude session to resume') and every expected line names a state/label/button precise enough for an unambiguous pass/fail judgment"
  prohibitions:
    - statement: "No bar-row visual change ships in Phase 17 — the D-12 settlement is a UAT-recorded decision made with the live bar in front of the user; any flip is recorded as future work, not implemented"
      status: enforced
      verification: "git diff shows zero changes under web/src/components/ and web/src/pages/ (no ActiveSessionsBar/SessionRow edits); the UAT doc's D-12 flow records keep-or-flip as a decision line only"
  artifacts:
    - internal/api/e2e_opencode_resume_test.go
    - .planning/phases/17-hardening-e2e/17-UAT.md
  key_links:
    - "PATCH /api/agents/{id} {command} on the system OpenCode agent (command is editable; engine is locked for system agents, agents_crud.go:161-214) → wrapper script records argv then execs the real opencode at its LookPath-resolved absolute path → the engine-branched resume path (-s <ocsid> append) is exercised verbatim while argv is observable"
    - "capture poller: out-of-band first turn (opencode run in the root dir) → global_task.opencode_session_id persisted → GET /api/agents/status global entry resumable:true (opencode keys off the persisted id alone) → resume 201 carries the id on the argv"
    - "the harness from plan 17-01 (same package, unexported helpers reused): real-binary spawn/SIGTERM/restart, TMUX_TMPDIR sandbox — with the ONE env posture difference: real HOME, per TestGlobalOpencodeCaptureHost's precedent"
    - "17-UAT.md expected lines ⇄ the locked copy table in 17-UI-SPEC (rows 1-16) — the doc quotes, never re-words"
---

<objective>
Finish the E2E story for the second engine and author the human walkthrough: D-52's real-opencode restart-resume leg (wrapper-agent argv proof through a REAL restart — no production code changes), and the 17-UAT.md walkthrough doc (D-59/D-60) including the D-53 both-engines same-conversation flows and the D-61 D-12 settlement procedure.

Purpose: GSESS-02 is engine-branched — the claude half is proven by plan 17-01, but the opencode resume append (-s <ses_ id>) and its refusal edge need their own real-binary proof; and SC5 (folder-mode root against a real repo, end-to-end in a browser) is inherently human — auth, quota, and 'it remembers' judgments never belong in an automated gate.

Output: internal/api/e2e_opencode_resume_test.go (extends the 17-01 harness with the real-HOME posture) and .planning/phases/17-hardening-e2e/17-UAT.md (the walkthrough doc the human executes at verify time).
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hardening-e2e/17-CONTEXT.md
@.planning/phases/17-hardening-e2e/17-RESEARCH.md
@.planning/phases/17-hardening-e2e/17-PATTERNS.md
@.planning/phases/17-hardening-e2e/17-UI-SPEC.md
@.planning/phases/16-global-view-settings-bar-integration/16-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-52 — real-opencode restart-resume leg (wrapper-agent argv proof)</name>
  <files>internal/api/e2e_opencode_resume_test.go</files>
  <read_first>
  - internal/api/e2e_global_restart_test.go (the plan-17-01 harness — reuse its unexported helpers; this file extends, never rebuilds)
  - .planning/phases/17-hardening-e2e/17-RESEARCH.md (Pattern 3 the opencode wrapper agent; Pitfall 3 the two env postures; Pitfall 9 cleanup ordering; the OPENCODE LEG diagram)
  - internal/api/sessions_global_test.go (TestGlobalOpencodeCaptureHost near lines 1131-1193 — the NOT-HOME-sandboxed posture doc comment, the out-of-band first-turn block, the capture poller loop)
  - internal/api/agents_crud.go (system-agent engine lock + editable command, lines 161-214)
  - internal/api/sessions.go (the engine-branched resume -s append at lines 571-574; the 'no global opencode session to resume' 409 at line 512)
  - internal/api/agents.go (opencode resumable keys off the persisted opencode_session_id alone)
  </read_first>
  <action>
  Create internal/api/e2e_opencode_resume_test.go (package api), one test function (e.g. TestE2EGlobalOpencodeRestartResume) in the TestE2EGlobal* family, host-gated on exec.LookPath for opencode, tmux and git (t.Skip when any is absent — D-52's live-argv proof degrades to the API-level + UAT halves on hosts without opencode).

  Env posture — the ONE difference from the 17-01 harness (Pitfall 3): REAL HOME (opencode resolves real config/auth from it), but TMUX_TMPDIR sandboxed to the test temp dir, a DEDICATED temp --db (e.g. <sandbox>/k-oc.db), and an allocated free port. Everything else reuses the 17-01 helpers (build, spawn, healthz poll, SIGTERM+Wait, restart).

  Wrapper agent (zero production changes): resolve opencode's absolute path via exec.LookPath in the test; write a wrapper script into the sandbox that writes its arguments one-per-line to a baked-in absolute argv file and then execs the resolved absolute opencode path with those arguments. GET /api/agents, find the system OpenCode agent (engine 'opencode', is_system true — the 00015 seed 'OpenCode'); PATCH /api/agents/{id} with body {"command": <wrapper absolute path>}; register the restore PATCH (back to 'opencode') in a t.Cleanup IMMEDIATELY after the mutation succeeds (Pitfall 9 — before any server teardown).

  The story:
  1. PUT /api/global {"root_path": <fresh temp git repo>} → 200 (folder root never touches ~/.kamacu paths).
  2. PUT /api/global {"agent_id": <the OpenCode agent id>} → 200 (the D-24 default-agent setter).
  3. PUT /api/settings/shell {"value":"tmux"} → 200.
  4. POST /api/sessions {"scope":"global","kind":"agent"} → 201; the wrapper argv file records the fresh argv (NO -s token on a fresh spawn).
  5. Out-of-band first turn (TestGlobalOpencodeCaptureHost block verbatim): run opencode run with a trivial prompt (e.g. 'Reply with just: ok') with cmd.Dir set to the root and PWD pinned to it, 120s budget, model errors tolerated — the session row is created regardless (probe-verified on opencode 1.18.22).
  6. Stop the agent (POST /api/sessions/{id}/stop → 200). Poll GET /api/agents/status until the global opencode entry shows resumable:true (capture persisted; bounded poll ~60s at 200ms — the id alone keys opencode resumability).
  7. stop() the server (SIGTERM, deadline-bounded Wait); start() a second process with identical args/env (same --db); poll healthz.
  8. POST /api/sessions {"scope":"global","kind":"agent","resume:true"} → 201; the wrapper argv file now contains the consecutive pair -s ses_<id>.
  9. Stop the resumed agent. PUT /api/global {"root_path":""} → 200 (clears the persisted ids). POST /api/sessions {"scope":"global","kind":"agent","resume:true"} → 409 with body containing 'no global opencode session to resume' (the D-54 opencode stale/absent-ocsid edge).
  </action>
  <verify>
    <automated>go test ./internal/api -run 'TestE2EGlobalOpencode' -count=1 -v -timeout 20m</automated>
  </verify>
  <acceptance_criteria>
  - internal/api/e2e_opencode_resume_test.go exists and reuses the 17-01 harness helpers (no duplicated build/spawn/restart logic — grep finds the 17-01 constructor call)
  - The test host-gates on exec.LookPath for opencode, tmux and git with t.Skip
  - The child env uses the REAL HOME (no HOME override in this leg) while TMUX_TMPDIR and --db stay sandboxed/temp
  - The wrapper argv file is asserted to contain the consecutive pair -s ses_ after the post-restart resume 201
  - The restore PATCH is registered in t.Cleanup immediately after the wrapper PATCH (grep ordering: PATCH handler before Cleanup registration statement)
  - The D-54 edge asserts the exact string 'no global opencode session to resume' on a 409 after the root clear
  - go test ./internal/api -run 'TestE2EGlobalOpencode' -count=1 -timeout 20m exits 0 on this host (opencode present)
  </acceptance_criteria>
  <done>
  GSESS-02's opencode half is proven through a real restart with the real binary: capture, survive, resume-with-id, refuse-after-clear — with the system agent's command restored and no production code changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-59/D-60/D-61 — author the 17-UAT.md walkthrough doc</name>
  <files>.planning/phases/17-hardening-e2e/17-UAT.md</files>
  <read_first>
  - .planning/phases/16-global-view-settings-bar-integration/16-UAT.md (the walkthrough-doc shape to mirror: frontmatter, numbered Title (gate-ref) sections with one-line expected/result, Summary counters, Gaps)
  - .planning/phases/17-hardening-e2e/17-UI-SPEC.md (§ UAT Walkthrough Copy Contract — doc shape, quoting rules, the mandatory safety-posture paragraph; § Verification-Surface Contracts — the SC1-SC5 expected-visual table; § D-12 Settlement Contract; § Copywriting Contract rows 1-16)
  - .planning/phases/17-hardening-e2e/17-CONTEXT.md (D-59..D-61 — the locked flows and the D-12 settlement procedure)
  </read_first>
  <action>
  Create .planning/phases/17-hardening-e2e/17-UAT.md mirroring the 16-UAT.md shape exactly: YAML frontmatter (status: pending, phase: 17-hardening-e2e, source: [17-PLAN references], started/updated timestamps), a ## Tests section of numbered '### N. Title (gate-ref)' blocks each carrying a one-line expected: and an empty result: line for the human, then the ## Summary counter block (total/passed/issues/pending/skipped/blocked) and ## Gaps.

  Flows (content locked by D-59; exact numbering/copy at executor discretion):
  1. Folder-mode lifecycle against a real repo (SC5): configure the root (the user's pick at UAT time — e.g. the kamacu checkout itself, dogfooding), Start agent, spawn a bash tab, reattach after navigating away, stop everything. Expected lines quote verbatim: the 'Start agent' CTA, the tab label form 'Bash <n>', the stop menu rendering exactly 'Stop'.
  2. Claude same-conversation resume (D-53): ask the agent something identifiable, stop the server process, restart it, hit 'Resume session' — expected: heading 'Agent session ended.', the 'Reset session' / 'Resume session' pair, and the agent remembering the identifiable thing (the human judgment).
  3. Opencode same-conversation resume (D-53): the same flow with the OpenCode agent set as the global default (Settings → Scratchpad agent Select).
  4. Interlock sanity (SC3): with a managed project clone AND a managed global root of the SAME repo, delete the project — expected: the Settings Scratchpad root row (mono owner/name + managed badge) unchanged and /global still functional.
  5. Sentinel-leak visual sanity (SC4): with a live global session, every enumeration surface shows zero global presence — kanban columns, project sidebar, an empty workspace still deletes, Activity lists/stats; the global appears ONLY on the bar row, /global, Settings Scratchpad, and MCP session-tool labels.
  6. D-12 bar-row settlement (D-61): with a task row AND the global row live simultaneously in the expanded bar, confirm the text-only 'Global · Scratchpad' row is distinguishable from a task's project/task pair — decision recorded in the doc (default KEEP; a flip only on real confusion, recorded as a decision + follow-up, no visual work this phase).

  Mandatory safety-posture paragraph (D-60, its own short section near the top): name the concrete risk — a --dangerously-skip-permissions agent writing directly into the checkout the user picked (no worktree net, no diff tab, no gated cleanup) — and suggest, without requiring, running the UAT against a disposable clone if wary. Not boilerplate.

  Quoting rules: every browser string in backticks verbatim from the 17-UI-SPEC copy table ('Agent session ended.', 'Resume session', 'Reset session', 'Resuming…', 'Global · Scratchpad', 'No root configured yet.', 'no global claude session to resume', 'global agent already running'); paths and repo refs as mono literals; every expected line specific enough for an unambiguous pass/fail (name the state, the label, the button). Include a short preamble noting what is ALREADY verified (16-UAT tests 2-4) so the human does not re-verify Phase-16 UX. No screenshots (text-only results, 16-UAT precedent).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-hardening-e2e/17-UAT.md && grep -c '^### ' .planning/phases/17-hardening-e2e/17-UAT.md</automated>
  </verify>
  <acceptance_criteria>
  - .planning/phases/17-hardening-e2e/17-UAT.md exists with frontmatter keys status, phase (17-hardening-e2e), source, started, updated
  - grep -c '^### ' returns at least 6 (the six D-59 flows)
  - The doc contains verbatim-quoted strings: 'Agent session ended.', 'Resume session', 'Reset session', 'Global · Scratchpad', 'No root configured yet.', 'no global claude session to resume' (grep each)
  - The safety-posture section names --dangerously-skip-permissions and the no-worktree-net concretes (no worktree net, no diff tab, no gated cleanup) and suggests a disposable clone
  - The D-12 flow is present and records a DECISION slot (keep-or-flip) rather than implementing anything
  - A ## Summary counter block and ## Gaps section exist; every flow has an expected: line and an empty result: line
  - Both engines' resume flows are present (claude AND opencode — grep finds both engine names)
  </acceptance_criteria>
  <done>
  The SC5 walkthrough exists in the 16-UAT pattern with locked flows, verbatim expected strings, the honest safety posture, and the D-12 settlement procedure — ready for the human to execute and record results at verify time.
  </done>
</task>

</tasks>

## Artifacts this phase produces (plan 04)

- New file: internal/api/e2e_opencode_resume_test.go — one TestE2EGlobal* family function (e.g. TestE2EGlobalOpencodeRestartResume) plus any small unexported wrapper/argv helpers local to the leg
- New file: .planning/phases/17-hardening-e2e/17-UAT.md — the D-59 walkthrough doc (authored with pending results; executed by the human at verify time)
- No production code changes (the wrapper agent is a test-written script; the PATCH is restored), no new exports, no new dependencies

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real HOME | the opencode leg intentionally runs un-sandboxed HOME (real config/auth); only the temp DB, temp root and TMUX_TMPDIR sandbox confine it |
| PATCH /api/agents/{id} → system agent row | the test mutates the system OpenCode agent's command inside its own temp DB; a missed restore must be contained by DB teardown |
| spawned server → host tmux socket | same shared-socket hazard as plan 17-01 — TMUX_TMPDIR isolation applies to this leg too |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-10 | Denial-of-Service (user data) | the real-HOME leg still shares the default tmux socket directory if TMUX_TMPDIR is forgotten | high | mitigate | TMUX_TMPDIR sandbox is set in BOTH the server child env and harness probes for this leg too (17-RESEARCH Pitfall 3 explicitly keeps it); t.Cleanup KillServer on the sandbox socket |
| T-17-11 | Tampering (persistent state) | a leftover wrapper command poisoning an agents row if the test fatals before restore | medium | mitigate | restore PATCH registered in t.Cleanup immediately after the mutation (Pitfall 9) AND a dedicated temp --db so even a missed restore is contained by teardown |
| T-17-12 | Spoofing | a fake opencode on PATH shadowing the real binary in the wrapper | low | mitigate | the test resolves opencode via exec.LookPath and bakes the ABSOLUTE path into the wrapper's exec line; no PATH games |
| T-17-13 | Repudiation (test evidence) | the wrapper argv file is the sole resume-proof instrument | low | mitigate | the argv file lives in the test sandbox and is asserted with the same consecutive-token-pair idiom as the fake-claude recorder (cross-checked against the status-feed resumable flip) |
</threat_model>

<verification>
- go test ./internal/api -run 'TestE2EGlobal' -count=1 -timeout 20m green (both 17-01's legs and this plan's opencode leg)
- The UAT doc passes its structural gates (frontmatter, ≥6 flows, verbatim strings, safety paragraph, decision slot)
- After the test run: no wrapper command remains in any DB (temp DB deleted), no stray server/tmux processes (pgrep + tmux ls on the real socket unchanged)
</verification>

<success_criteria>
- GSESS-02 is closed for BOTH engines through real-binary restarts (claude: plan 17-01; opencode: this plan)
- SC5's instrument — the walkthrough doc — exists with locked flows, verbatim expectations, and the honest safety posture; D-12 has its settlement procedure with the live bar
- The milestone's last phase leaves no red tests, no phantom fixes, and no undocumented safety posture
</success_criteria>

<output>
Create `.planning/phases/17-hardening-e2e/17-04-SUMMARY.md` when done — note in the SUMMARY that 17-UAT.md is authored-pending and the human walkthrough (including the D-12 settlement) executes at verify time
</output>
