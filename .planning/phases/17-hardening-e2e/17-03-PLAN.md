---
phase: 17-hardening-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/manager.go
  - internal/session/opencode_engine_test.go
  - web/src/api/types.ts
  - web/src/pages/TaskPage.tsx
autonomous: true
requirements: [] # Phase 17 owns no REQ-IDs (ROADMAP); this plan closes the carried-forward debt D-62/D-63 flagged into this phase by 16-01 and the 13/14 deferred-items
must_haves:
  truths:
    - "go test ./... exits 0 on this host BOTH with and without the three KAMACU_ variables exported into the test environment (D-63 suite-green substrate); the ACTUAL red set found by the baseline run is recorded in the SUMMARY (17-RESEARCH Pitfall 4: the TestInput_* family is already green — do not plan a phantom fix)"
    - "The custom/opencode spawn arm in internal/session/manager.go strips KAMACU_SESSION_ID, KAMACU_HOOK_TOKEN and KAMACU_HOOK_BASE from the inherited environment BEFORE the opencode-gated re-injection — the parent's hook token (a secret) never propagates into arbitrary custom-agent children (D-63 production fix)"
    - "TestCustomEngineDoesNotGetHookEnv passes even when the parent shell exports all three KAMACU_ vars, and its body still asserts absence of all three in the custom child env; a comment in the test documents the under-Kamacu context"
    - "TestOpencodeSpawnInjectsHookEnv still passes — the opencode arm still receives its three KAMACU_ injections with the configured values (the D014 contract intact)"
    - "web/src/api/types.ts declares Agent.engine as the union 'claude' | 'custom' | 'opencode' (D-62, matching the Go wire + the 00015 opencode system-agent seed)"
    - "web/src/pages/TaskPage.tsx computes isClaudeAgent as projectEngine === 'claude' — the claude-quota chip is hidden for opencode-engine projects and during agents-loading (the accepted cosmetic flip; no loading machinery added)"
  prohibitions:
    - statement: "No frontend file other than web/src/api/types.ts and web/src/pages/TaskPage.tsx changes (UI-SPEC Continuity Lock: allowed frontend diffs this phase are exactly those two lines); web/src/pages/BoardPage.tsx carries the identical predicate but is OUT OF CONTRACT — record it as a known consistency site in the SUMMARY, do not edit it"
      status: enforced
      verification: "the plan's commits touch exactly the four declared files; web/src/pages/BoardPage.tsx and web/package.json appear in no diff"
    - statement: "QuotaIndicator.tsx and GlobalTaskPage.tsx are untouched — no rendering or rule changes inside the component, and the /global rule stays as shipped"
      status: enforced
      verification: "git diff shows zero changes under web/src/components/ and web/src/pages/GlobalTaskPage.tsx"
  artifacts:
    - internal/session/manager.go (env-strip in the custom spawn arm)
    - internal/session/opencode_engine_test.go (context comment on TestCustomEngineDoesNotGetHookEnv)
    - web/src/api/types.ts (engine union widened)
    - web/src/pages/TaskPage.tsx (predicate flipped)
  key_links:
    - "manager.go custom-arm env construction: strip the three KAMACU_ names from os.Environ() BEFORE the opencode engine gate re-adds them (append-last-value-wins idiom preserved) — the exact edit site is the cmd.Env = append(os.Environ(), envExtra...) line in the AgentEngine != claude branch"
    - "types.ts Agent.engine union ⇄ the Go agents JSON engine field ⇄ the 00015 migration seed (name='OpenCode', command='opencode', engine='opencode', is_system=1)"
    - "TaskPage.tsx isClaudeAgent predicate ⇄ the QuotaIndicator render site on the same page ⇄ the locked /global rule (GlobalTaskPage.tsx config.agent.engine === 'claude')"
---

<objective>
Close the two carried-forward debts parked for this phase: D-63 (the suite must be green — root-cause fix for TestCustomEngineDoesNotGetHookEnv, which fails only under a Kamacu terminal because the custom spawn arm inherits the parent's KAMACU_* env including the hook-token secret) and D-62 (the stale Agent.engine union + the TaskPage QuotaIndicator predicate flagged by 16-01 Pitfall 2).

Purpose: a hardening phase does not ship with known-red tests, and the frontend types must be honest about the opencode engine the wire already carries. The D-63 fix also closes a real secret-propagation hole (HOOK_TOKEN reaching arbitrary custom-agent children).

Output: a green full-suite baseline recorded in the SUMMARY, the manager.go env-strip fix, and the two-line frontend type/predicate correction.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-hardening-e2e/17-CONTEXT.md
@.planning/phases/17-hardening-e2e/17-RESEARCH.md
@.planning/phases/17-hardening-e2e/17-PATTERNS.md
@.planning/phases/17-hardening-e2e/17-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-63 — baseline the suite, root-cause the red test, strip KAMACU_* in the custom spawn arm</name>
  <files>internal/session/manager.go, internal/session/opencode_engine_test.go</files>
  <read_first>
  - .planning/phases/17-hardening-e2e/17-RESEARCH.md (Pitfall 2 — the reproduced root cause; Pitfall 4 — TestInput_* is already green, the baseline run decides the real red set; Open Question 3 — production fix recommended over test-side scrubbing)
  - .planning/phases/17-hardening-e2e/17-PATTERNS.md (manager.go edit-site section with the current env posture excerpt)
  - internal/session/manager.go (the custom/opencode spawn arm — the envExtra construction and the cmd.Env append around lines 187-209)
  - internal/session/opencode_engine_test.go (TestOpencodeSpawnInjectsHookEnv at line 96; TestCustomEngineDoesNotGetHookEnv at line 138; the writeEnvDumpStub/envValue helpers)
  </read_first>
  <behavior>
    - Test 1 (existing, must keep passing): TestOpencodeSpawnInjectsHookEnv — an opencode-engine spawn receives KAMACU_SESSION_ID, KAMACU_HOOK_TOKEN (the configured token value) and KAMACU_HOOK_BASE (the configured base URL) in its child env.
    - Test 2 (existing, must pass everywhere after the fix): TestCustomEngineDoesNotGetHookEnv — with the parent test process EXPORTING all three KAMACU_ names (simulate the under-Kamacu context by setting them via os.Setenv in the test with t.Cleanup restore), a custom-engine spawn's child env contains NONE of the three.
  </behavior>
  <action>
  Step 1 — baseline FIRST (D-63 / research A1): run the full go test ./... -count=1 and record the ACTUAL red set in the SUMMARY (expected from targeted live runs: only TestCustomEngineDoesNotGetHookEnv, and only when run under a Kamacu session). If the full suite surprises with other red tests, absorb them — that is the investigate-and-fix mandate; root-cause fix preferred over expectation-adjustment.

  Step 2 — the production fix in internal/session/manager.go, custom/opencode spawn arm (the AgentEngine != claude branch): BEFORE the opencode-gated envExtra injection, filter the three names KAMACU_SESSION_ID, KAMACU_HOOK_TOKEN and KAMACU_HOOK_BASE out of the inherited os.Environ() slice; the opencode gate then re-adds its own values on top of the filtered base (keep the existing append-last-value-wins idiom and its comment block intact). Extract the filter as a small unexported helper (e.g. stripKamacuEnv) so the intent is named; do not change the claude arm.

  Step 3 — the test companion edit in internal/session/opencode_engine_test.go: extend TestCustomEngineDoesNotGetHookEnv to EXPORT the three KAMACU_ names from within the test (os.Setenv with t.Cleanup restore — t.Setenv cannot express the under-Kamacu parent state on its own since the assertion must hold with them present), add a comment documenting the context (a Kamacu terminal exports all three; the custom arm strips inherited ones so the D014 absence assertion holds in every environment), and keep asserting absence of all three via envValue. TestOpencodeSpawnInjectsHookEnv needs NO change and must stay green.

  Step 4 — full green: go test ./... -count=1 exits 0, and the targeted proof: running go test ./internal/session with all three KAMACU_ vars exported in the environment exits 0.
  </action>
  <verify>
    <automated>KAMACU_SESSION_ID=t KAMACU_HOOK_TOKEN=t KAMACU_HOOK_BASE=t go test ./internal/session -run 'TestCustomEngineDoesNotGetHookEnv|TestOpencodeSpawnInjectsHookEnv' -count=1 -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
  - The baseline run output and the actual red set are recorded in the plan SUMMARY
  - internal/session/manager.go contains the env-filter helper removing all three KAMACU_ names before the opencode gate (grep finds KAMACU_HOOK_TOKEN in the strip/filter logic inside the custom spawn arm)
  - KAMACU_SESSION_ID=t KAMACU_HOOK_TOKEN=t KAMACU_HOOK_BASE=t go test ./internal/session -run 'TestCustomEngineDoesNotGetHookEnv' -count=1 exits 0
  - go test ./internal/session -run 'TestOpencodeSpawnInjectsHookEnv' -count=1 exits 0 (the opencode injection intact)
  - go test ./... -count=1 exits 0 on this host
  - The claude arm's env construction is unchanged (diff shows the edit scoped to the custom/opencode branch)
  </acceptance_criteria>
  <done>
  The suite is green from a clean shell AND from inside a Kamacu session; the parent hook token can no longer leak into custom-agent children; the baseline evidence is in the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-62 — widen the Agent.engine union + flip the TaskPage QuotaIndicator predicate</name>
  <files>web/src/api/types.ts, web/src/pages/TaskPage.tsx</files>
  <read_first>
  - .planning/phases/17-hardening-e2e/17-UI-SPEC.md (§ The One Frontend Change: D-62 LOCKED — the exact two edits, the two accepted user-visible deltas, the non-goals, the Continuity Lock)
  - .planning/phases/17-hardening-e2e/17-RESEARCH.md (Pitfall 7 — the loading-state nuance and the locked resolution)
  - web/src/api/types.ts (line 62 engine union; the Task.source union at line 90 as the in-file widen-in-place precedent with its provenance comment; the extra_params comment citing claude-engine-only)
  - web/src/pages/TaskPage.tsx (the projectAgentId/projectEngine/isClaudeAgent computation around line 62-64; the render site around line 535)
  </read_first>
  <action>
  Two edits, nothing else (17-UI-SPEC Continuity Lock):

  1. web/src/api/types.ts line 62: widen the Agent.engine union from the two-value form to the three-value form adding 'opencode' — matching the Go wire and the 00015 opencode system-agent seed. Follow the file's commenting convention: the wire field carries a short why-comment citing the backend serialization (mirror the Task.source provenance comment style).

  2. web/src/pages/TaskPage.tsx line 64: replace the isClaudeAgent computation with the strict equality form projectEngine === 'claude' (aligned with the locked /global rule in GlobalTaskPage.tsx). Update the trailing comment: the chip now shows ONLY for claude-engine projects and is hidden while agents are unresolved (loading) — the accepted cosmetic flip; no spinner, no placeholder, no loading machinery (UI-SPEC delta b).

  Explicitly DO NOT touch: QuotaIndicator.tsx, GlobalTaskPage.tsx, BoardPage.tsx (identical predicate, out of contract — record it as a known consistency site in the SUMMARY for future scoping), web/package.json.
  </action>
  <verify>
    <automated>cd web && npm run build && npx eslint src/api/types.ts src/pages/TaskPage.tsx</automated>
  </verify>
  <acceptance_criteria>
  - web/src/api/types.ts contains the line: engine: "claude" | "custom" | "opencode";
  - web/src/pages/TaskPage.tsx contains the line: const isClaudeAgent = projectEngine === "claude";
  - cd web && npm run build (tsc -b && vite build) exits 0
  - npx eslint src/api/types.ts src/pages/TaskPage.tsx exits with zero errors (no posture regression on the edited files)
  - The plan's git diff touches NO other web/ file — BoardPage.tsx, QuotaIndicator.tsx, GlobalTaskPage.tsx and package.json absent from the diff
  - The SUMMARY records the BoardPage.tsx identical-predicate consistency site as known future scope
  </acceptance_criteria>
  <done>
  The frontend type is honest about the opencode engine, the claude-quota chip follows the claude-only rule on task pages, and the entire frontend delta is the two locked lines.
  </done>
</task>

</tasks>

## Artifacts this phase produces (plan 03)

- New unexported helper in internal/session/manager.go: the KAMACU_* env-strip function used by the custom/opencode spawn arm (name at executor discretion, e.g. stripKamacuEnv)
- Edited wire type: web/src/api/types.ts Agent.engine union widened to include 'opencode'
- Edited predicate: web/src/pages/TaskPage.tsx isClaudeAgent strict-equality form
- Edited test: TestCustomEngineDoesNotGetHookEnv now self-exports the three KAMACU_ names (with t.Cleanup restore) and documents the under-Kamacu context
- Summary documentation: the full-suite baseline red set + the BoardPage.tsx consistency-site note

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent process env → spawned agent children | everything os.Environ() carries propagates into agent processes unless stripped; under Kamacu this includes the hook-token secret |
| Go wire → TS types | the stale union made TS blind to a runtime value the backend already emits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-08 | Information Disclosure | KAMACU_HOOK_TOKEN (secret) propagating from a nested Kamacu parent into arbitrary custom-agent child processes via os.Environ inheritance in manager.go's custom arm | medium | mitigate | the D-63 production fix strips KAMACU_SESSION_ID/KAMACU_HOOK_TOKEN/KAMACU_HOOK_BASE from the inherited env BEFORE the opencode gate re-adds them — honoring the D014 'custom agents get none of these' contract in ALL contexts, not just clean shells |
| T-17-09 | Tampering (type-level) | the stale Agent.engine union lets opencode-engine projects render claude-only affordances (the QuotaIndicator bug) | low | mitigate | the D-62 union widening exposes the runtime value to the type system and the strict-equality predicate enforces the claude-only rule |
</threat_model>

<verification>
- KAMACU_SESSION_ID=t KAMACU_HOOK_TOKEN=t KAMACU_HOOK_BASE=t go test ./internal/session -count=1 exits 0
- go test ./... -count=1 exits 0 (full suite green — the hardening phase's own bar)
- cd web && npm run build exits 0; eslint clean on the two edited files
- git diff scope: exactly the four declared files
</verification>

<success_criteria>
- The carried-forward debt flagged into Phase 17 (16-01 Pitfall 2 + the 13/14 deferred-items) is closed in-plan, not deferred again
- The suite-green substrate exists for the E2E plans' assertions (a red baseline would poison every other verification)
- The secret-propagation hole is closed in production code, with regression coverage that fails if the strip is removed
</success_criteria>

<output>
Create `.planning/phases/17-hardening-e2e/17-03-SUMMARY.md` when done — the SUMMARY MUST record the full-suite baseline output (actual red set found) and the BoardPage.tsx consistency-site note
</output>
