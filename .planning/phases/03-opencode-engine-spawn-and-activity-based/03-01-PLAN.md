# 03-01: opencode system agent seed (migration + backfill)

<objective>
opencode system agent seed (migration + backfill).
</objective>

<tasks>
- [ ] **T01**: opencode system agent seed (migration + backfill) _(1h)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
