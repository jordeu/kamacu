---
phase: 24-session-bar-board-tab-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/layout/ActiveSessionsBar.tsx
  - web/src/components/board/Column.tsx
  - web/src/components/board/QuickAdd.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03]
must_haves:
  truths:
    - "The collapsed active-sessions bar no longer shows the total-sessions count; the working/waiting/idle colored counts remain"
    - "Clicking outside the expanded active-sessions bar collapses it and persists collapsed"
    - "Clicking the bar itself still toggles (the outside-click listener does not double-fire on an inside click)"
    - "The To Do column no longer shows the inline + New task shortcut"
  artifacts:
    - path: "web/src/components/layout/ActiveSessionsBar.tsx"
      provides: "collapsed bar without total + outside-click auto-collapse"
      contains: "mousedown"
    - path: "web/src/components/board/Column.tsx"
      provides: "To Do column without the QuickAdd slot"
  key_links:
    - from: "ActiveSessionsBar document mousedown listener"
      to: "collapse() helper"
      via: "ref-guarded outside-click → collapse()"
      pattern: "collapse\\(\\)"
---

<objective>
Sharpen the active-sessions bar and board (POLISH-01/02/03): drop the total-sessions count from
the collapsed bar (keeping the per-state colored counts), auto-collapse the bar on an outside
click by reusing the existing `collapse()` helper, and remove the To Do column's inline
`+ New task` quick-add (retiring `QuickAdd.tsx`).

Purpose: These are three independent daily-driver cleanups with no backend and no cross-plan
dependency — grouped so they ship in one Wave-1 pass.

Output: An edited `ActiveSessionsBar.tsx` (total span removed, document pointer listener added);
an edited `Column.tsx` (QuickAdd slot + import removed); a deleted `QuickAdd.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-session-bar-board-tab-polish/24-CONTEXT.md
@.planning/phases/24-session-bar-board-tab-polish/24-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

From web/src/components/layout/ActiveSessionsBar.tsx:
- `const storageKey = "kamacu:sessions-bar-collapsed";` (line 33)
- `const collapse = () => { setCollapsed(true); localStorage.setItem(storageKey, "1"); };` (lines 46-49) — reuse verbatim
- `const total = live.length;` (line 66) — STILL used by `total === 0` empty-state checks (lines 91, 130); keep the variable, remove only the collapsed-bar render span (lines 143-145)
- The outer wrapper `<div className="fixed inset-x-0 bottom-0 z-30 flex flex-col">` (line 86) — attach the ref here
- The collapsed bar `onClick={toggle}` (line 125) — inside clicks must NOT be treated as outside
- `collapsed` state (line 34) — the listener only collapses when NOT already collapsed

From web/src/pages/TaskPage.tsx (document-listener idiom to mirror) lines 215-231:
- `useEffect(() => { function onKeyDown(...) {...} window.addEventListener(...); return () => window.removeEventListener(...); }, [...])`

From web/src/components/board/Column.tsx:
- `import { QuickAdd } from "./QuickAdd";` (line 9) — remove
- `topSlot` doc-comment mentions To Do's quick-add (line 16) — update the comment
- `{status === "todo" && <QuickAdd projectId={projectId} />}` (line 46) — remove

Grep confirms `QuickAdd` has exactly ONE importer (Column.tsx). Task creation survives via
BoardPage.tsx's header "New task" button + the `n` shortcut → NewTaskDialog (D-12 safety net).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the total count + add outside-click auto-collapse (POLISH-01/02)</name>
  <files>web/src/components/layout/ActiveSessionsBar.tsx</files>
  <read_first>
    - web/src/components/layout/ActiveSessionsBar.tsx — read the WHOLE file: the `collapse()` helper (46-49), `toggle` (37-42), the collapsed-bar count block (135-147) incl. the `total` span (143-145), the `total === 0` empty-state guards (91, 130), the outer wrapper (86), and the bar `onClick={toggle}` (125)
    - web/src/pages/TaskPage.tsx lines 215-231 — the add-on-mount / remove-on-cleanup document-listener idiom
    - .planning/phases/24-session-bar-board-tab-polish/24-PATTERNS.md § "ActiveSessionsBar.tsx — drop total (D-10) + outside-click auto-collapse (D-11)"
    - .planning/phases/24-session-bar-board-tab-polish/24-CONTEXT.md D-10 and D-11
  </read_first>
  <action>
    Two edits, one file.

    (D-10) Remove ONLY the total span (lines 143-145: the `<span className="text-xs font-medium
    text-muted-foreground tabular-nums">{total}</span>`) from the collapsed-bar `<>` fragment. Keep
    the three `<CountGroup>` renders (working/waiting/idle). Do NOT delete the `total` variable
    (line 66) — it is still read by the `total === 0` empty-state checks at lines 91 and 130.

    (D-11) Add outside-click auto-collapse. Add a `useRef<HTMLDivElement>(null)` (name it `barRef`)
    and attach it to the outer `fixed inset-x-0 bottom-0` wrapper (line 86). In a `useEffect`,
    register a document `mousedown` listener (mirror the TaskPage document-listener idiom:
    add-on-mount, remove-on-cleanup). The handler collapses ONLY when the bar is currently expanded
    AND the click target is outside `barRef.current` (`barRef.current && !barRef.current.contains(e.target as Node)`),
    calling the existing `collapse()` helper (which sets collapsed + persists "1"). Guard the effect
    on `collapsed` so it is a no-op when already collapsed and re-registers correctly. Clicks inside
    the bar are ignored by the ref-contains check, so the existing `onClick={toggle}` on the
    collapsed bar (line 125) still toggles without the listener double-firing into a re-expand.
    Import `useEffect` and `useRef` from "react" (the file currently imports only `useState`).

    Discretion decisions to make and document in the SUMMARY (D-11 leaves these open): use the
    `mousedown` phase (bubble, not capture) so nested surfaces inside the bar register as "inside";
    do NOT also collapse on Escape (keep the gesture click-only for this pass — note it as a
    deliberate scope choice).
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `ActiveSessionsBar.tsx` no longer contains a `{total}` render inside the collapsed-bar fragment (the three `CountGroup`s remain); `grep -c 'tabular-nums">\s*{total}' web/src/components/layout/ActiveSessionsBar.tsx` returns 0
    - Source: the file imports `useEffect` and `useRef` and contains a document `mousedown` listener that calls `collapse()`
    - Source: the `total` variable declaration (`const total = live.length;`) is retained (still used by `total === 0`)
    - Behavior: clicking outside the expanded bar collapses it and persists `"1"` under `kamacu:sessions-bar-collapsed`; clicking the bar itself still toggles
    - Build: `npm run build` and `npm run lint` both exit 0
  </acceptance_criteria>
  <done>The collapsed bar shows only the three colored counts, an outside click collapses the expanded bar via `collapse()`, and the web build + lint are green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the To Do quick-add + retire QuickAdd.tsx (POLISH-03)</name>
  <files>web/src/components/board/Column.tsx, web/src/components/board/QuickAdd.tsx</files>
  <read_first>
    - web/src/components/board/Column.tsx — the `QuickAdd` import (line 9), the `topSlot` doc-comment (line 16), and the `{status === "todo" && <QuickAdd .../>}` slot (line 46)
    - web/src/components/board/QuickAdd.tsx — the file being deleted
    - web/src/pages/BoardPage.tsx — confirm the surviving task-creation paths (header "New task" button + `n` shortcut → NewTaskDialog) so the deletion is safe (D-12 safety net)
    - .planning/phases/24-session-bar-board-tab-polish/24-PATTERNS.md § "Column.tsx + QuickAdd.tsx — remove To Do quick-add (D-12)"
  </read_first>
  <action>
    In `Column.tsx`: delete the `import { QuickAdd } from "./QuickAdd";` line (9), delete the
    `{status === "todo" && <QuickAdd projectId={projectId} />}` line (46), and update the `topSlot`
    doc-comment (line 16) to drop the "(in addition to To Do's quick-add)" clause since the quick-add
    no longer exists. Then delete the file `web/src/components/board/QuickAdd.tsx` entirely
    (grep confirmed `Column.tsx` is its only importer). Do NOT touch task creation in
    `BoardPage.tsx` — the header "New task" button and the `n` shortcut remain the creation paths.
    Confirm no remaining reference to `QuickAdd` anywhere under `web/src` after the edit.
  </action>
  <verify>
    <automated>test ! -f web/src/components/board/QuickAdd.tsx && ! grep -rq "QuickAdd" web/src && cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/components/board/QuickAdd.tsx` no longer exists
    - Source: `grep -rc "QuickAdd" web/src` returns 0 (no importers, no references)
    - Source: `Column.tsx` no longer contains `status === "todo"` with a `QuickAdd` render
    - Build: `cd web && npm run build` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>The To Do column renders no `+ New task` row, `QuickAdd.tsx` is deleted with zero dangling references, and the web build + lint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure client-side UI: removing a render span, adding a DOM click listener, and deleting a component. No new server input, no new persisted data beyond the existing `kamacu:sessions-bar-collapsed` localStorage key already written by `collapse()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Denial of Service | document mousedown listener | mitigate | The listener is registered/torn down via `useEffect` cleanup (mirrors the vetted TaskPage idiom) so no leaked global listener accumulates across mounts |
| T-24-06 | Tampering | kamacu:sessions-bar-collapsed | accept | Local-only single-user; the collapse flag is the user's own UI preference — no integrity boundary |
| T-24-SC | Tampering | npm installs | mitigate | No new packages installed (edits + one deletion only); slopcheck N/A |
</threat_model>

<verification>
- `cd web && npm run build` exits 0
- `cd web && npm run lint` exits 0
- `grep -rc "QuickAdd" web/src` returns 0
- `test ! -f web/src/components/board/QuickAdd.tsx`
</verification>

<success_criteria>
- Collapsed bar shows working/waiting/idle colored counts, no total
- Outside click collapses the expanded bar (via `collapse()`); inside clicks still toggle
- To Do column has no inline quick-add; `QuickAdd.tsx` deleted with no dangling refs
- Web build + lint green
</success_criteria>

<output>
Create `.planning/phases/24-session-bar-board-tab-polish/24-02-SUMMARY.md` when done. Record the
D-11 discretion decisions (mousedown/bubble phase; Escape-collapse deliberately out of scope for
this pass).
</output>
