---
phase: 12-activity-chart-tooltips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/components/ui/chart.tsx
  - web/src/lib/time.ts
  - web/src/components/activity/StatsStrip.tsx
autonomous: true
requirements:
  - CHART-01
  - STAT-01
  - ENTRY-01
must_haves:
  truths:
    - "shadcn chart primitive (chart.tsx) exists at web/src/components/ui/chart.tsx exporting ChartContainer, ChartConfig, ChartTooltip, ChartTooltipContent"
    - "recharts is the sole new npm dependency added this phase (D-02) — present in web/package.json dependencies"
    - "formatDateTime(iso) is exported from web/src/lib/time.ts and produces 'Jul 29, 14:32' for a UTC noon timestamp (D-11)"
    - "formatAgo remains exported from web/src/lib/time.ts (NOT deleted — QuotaIndicator still calls it, D-11)"
    - "StatsStrip's three TimeStatRow instances (Cycle, In progress, In review) each render a lucide Info icon whose hover/focus opens the existing shadcn Tooltip with the D-08 copy"
    - "The two count rows (taskCount, reviewCount) have NO Info icon / NO tooltip (D-09)"
  artifacts:
    - web/src/components/ui/chart.tsx
    - web/src/lib/time.ts (+formatDateTime)
    - web/src/components/activity/StatsStrip.tsx (modified)
  key_links:
    - "chart.tsx named exports ←→ ActivityChart.tsx imports (Plan 02 consumes ChartContainer/ChartConfig/ChartTooltip/ChartTooltipContent)"
    - "formatDateTime export ←→ ActivityList.tsx + ReviewsList.tsx call sites (Plan 02 consumes)"
    - "StatsStrip TimeStatRow help prop ←→ D-08 tooltip copy strings"
---

<objective>
Lay the foundation for the Activity chart + tooltip phase and ship the independent
stat-strip tooltips. Three prerequisite pieces that every later surface depends on,
plus the StatsStrip tooltips which have zero dependency on the chart:

1. **shadcn chart primitive + recharts** (D-01/D-02) — generate `chart.tsx` and pull
   recharts as the sole new npm dependency.
2. **`formatDateTime` formatter** (D-11) — the hand-rolled absolute-timestamp sibling
   in `lib/time.ts` that both the chart hover labelFormatter and the per-entry swap
   consume.
3. **StatsStrip explanatory tooltips** (D-08/D-09) — Info icon + existing shadcn
   Tooltip on the three time-stat rows; counts untouched.

Purpose: Plan 02 (the chart component + entry-time swap) cannot start until chart.tsx
exists and formatDateTime is exported. This plan produces both, plus the no-dependency
stat tooltips, as the Wave 1 root.

Output: generated `chart.tsx`, extended `time.ts`, modified `StatsStrip.tsx`, one new
entry in `web/package.json`.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-activity-chart-tooltips/12-CONTEXT.md
@.planning/phases/12-activity-chart-tooltips/12-RESEARCH.md
@.planning/phases/12-activity-chart-tooltips/12-PATTERNS.md
@.planning/phases/12-activity-chart-tooltips/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate shadcn chart primitive + install recharts (D-01/D-02)</name>
  <files>web/package.json, web/package-lock.json, web/src/components/ui/chart.tsx</files>
  <read_first>
    - `web/components.json` (the shadcn registry config — confirms `"style": "radix-nova"`, `"aliases.ui": "@/components/ui"`, `"tailwind.css": "src/index.css"`; the CLI reads these to decide where chart.tsx lands and whether to touch index.css)
    - `web/package.json` (current dependencies block at lines 12-32 — confirm NO charting lib present today; recharts will be the sole new entry)
    - `web/src/components/ui/tooltip.tsx` (the closest existing shadcn-generated primitive — every shadcn primitive in this repo follows this skeleton: `import * as React`, `import { cn } from "@/lib/utils"`, `data-slot="..."` on root, named exports at bottom. chart.tsx will follow the SAME shape — read this to spot a botched generation)
    - `12-RESEARCH.md` §Standard Stack + §Code Examples "shadcn chart.tsx — the generated primitives" (the expected export surface: ChartContainer, ChartConfig, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, ChartStyle + useChart context + getPayloadConfigFromPayload helper)
  </read_first>
  <action>
    Run the canonical shadcn add command from the `web/` directory so the CLI reads
    `web/components.json`:

    `cd web && npx shadcn@latest add chart` (accept prompts / use `-y` if the CLI
    asks for confirmation).

    This does two things (per D-01):
    (a) Pulls `recharts` as a NORMAL `package.json` dependency (resolves to `^3.10.1`,
        the React-19-compatible v3 major — verified in 12-RESEARCH §Standard Stack).
        Do NOT install recharts separately via `npm install recharts` — the shadcn
        block owns the add.
    (b) Generates `web/src/components/ui/chart.tsx` (the shadcn ChartContainer /
        ChartConfig / ChartTooltip / ChartTooltipContent primitives wrapping
        recharts' ResponsiveContainer + Tooltip).

    After the CLI finishes, run `cd web && npm install` so recharts materializes
    into `node_modules` and `package-lock.json` is updated.

    **D-02 contract — verify the CLI added ONLY recharts:** diff `web/package.json`
    before/after. The sole new entry under `dependencies` must be `recharts`. If the
    CLI attempted to add any other package (a tooltip lib, a date lib, a second
    charting dep), REMOVE it — recharts is the documented, deliberate sole exception
    to the Phase-11 zero-deps principle. Do NOT hand-edit chart.tsx — it is a vendored
    shadcn primitive; adapt consumers (Plan 02) to its actual exports instead.

    **Do NOT use the saturated default `--chart-1..5` CSS tokens** for anything in
    this task — they already exist in `web/src/index.css` (Pitfall 3); the chart
    segment colors are set in Plan 02's ChartConfig from PROJECT_PALETTE hexes, not
    here. If `shadcn add chart` touched index.css's `--chart-*` values, leave them
    (they are unused by this phase's ChartConfig).

    The `radix-nova` preset (components.json:3) is a style variant of the same
    registry path the RESEARCH source was read from; the export SURFACE is identical
    to the documented one, only Tailwind class names inside chart.tsx may differ.
    Read the actually-generated file and confirm the 7 named exports + ChartConfig
    type exist before declaring done.
  </action>
  <verify>
    <automated>cd web && test -f src/components/ui/chart.tsx && grep -q '"recharts"' package.json && grep -q 'ChartContainer' src/components/ui/chart.tsx && grep -q 'ChartTooltipContent' src/components/ui/chart.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/ui/chart.tsx` exists and exports `ChartContainer`, `ChartConfig`, `ChartTooltip`, `ChartTooltipContent` (grep each name in the file).
    - `web/package.json` `dependencies` contains exactly one new entry: `"recharts"` (the `git diff web/package.json` shows recharts added and nothing else under dependencies).
    - `npm run build` (which runs `tsc -b && vite build`) exits 0 — recharts compiles against `react@^19.2.6`, chart.tsx type-checks.
    - `npm run lint` exits 0 — no new lint errors introduced by the generated primitive.
  </acceptance_criteria>
  <done>
    shadcn chart primitive generated and recharts installed as the sole new dependency;
    `npm run build` + `npm run lint` pass; chart.tsx exports the primitives Plan 02
    imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add formatDateTime formatter to lib/time.ts (D-11)</name>
  <files>web/src/lib/time.ts</files>
  <read_first>
    - `web/src/lib/time.ts` (the file being modified — read `formatAgo` lines 1-11 and `formatDuration` lines 13-33 to replicate the exact formatter idiom: JSDoc header + zero-deps rationale, primitive input, degenerate-input guard FIRST, string return)
    - `12-RESEARCH.md` §Pattern 5 "Per-entry absolute timestamp formatter" + §Code Examples (the MDN-verified reference implementation and the exact toLocaleString options)
    - `12-CONTEXT.md` D-11 (the format contract `Jul 29, 14:32`; formatAgo stays for QuotaIndicator)
    - `web/src/components/quota/QuotaIndicator.tsx` (confirm formatAgo is still imported there — proves formatAgo must NOT be deleted; do a quick grep if the file path is uncertain)
  </read_first>
  <behavior>
    - `formatDateTime("2026-07-29T14:32:00.000Z")` under `TZ=UTC` returns `"Jul 29, 14:32"` (ms-precision task doneAt).
    - `formatDateTime("2026-07-29T14:32:00Z")` under `TZ=UTC` returns `"Jul 29, 14:32"` (second-precision gh closedAt — both precisions parse via Date.parse).
    - `formatDateTime(null)` returns `"—"` (em-dash sentinel).
    - `formatDateTime(undefined)` returns `"—"`.
    - `formatDateTime("not-a-date")` returns `"—"` (NaN guard — defense-in-depth for malformed gh closedAt).
    - Output has no weekday and no year (all entries fall within the 7/30-day window).
    - The function uses NO date library (no date-fns/dayjs/luxon) — `Date.prototype.toLocaleString` only (D-02 zero-deps).
  </behavior>
  <action>
    Add a third sibling formatter `formatDateTime` to `web/src/lib/time.ts`, following
    the EXACT idiom of the existing `formatAgo` (lines 1-11) and `formatDuration`
    (lines 13-33):

    - JSDoc header explaining the contract: absolute `"Jul 29, 14:32"` — compact
      month-abbrev + day + comma + 24h HH:MM, no weekday/year (D-11); all entries
      fall within the 7/30-day window so year is implicit and weekday is recoverable
      from the chart. Note the zero-deps rationale (D-02 — the zero-deps principle
      is relaxed for recharts ONLY; this formatter stays hand-rolled).
    - Signature: `export function formatDateTime(iso: string | null | undefined): string`
      (accept null AND undefined — ReviewDoneSummary.completedAt is gh-sourced and
      could in theory be absent; the null/undefined guard is defense-in-depth).
    - Guard FIRST: `if (!iso) return "—";` then `const ms = Date.parse(iso); if (Number.isNaN(ms)) return "—";`
      (the em-dash sentinel matches `formatDuration`'s guard at line 22 — Pitfall 6
      / RESEARCH A4; never return an empty string or throw).
    - Body: `return new Date(ms).toLocaleString("en-US", { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit", hour12: false });`
      — pass the EXPLICIT `"en-US"` locale for determinism (the app is English-only;
      MDN warns `toLocaleString` with no locale arg varies by host — Pitfall 6).
      `hour12: false` forces 24-hour output per MDN.

    **Do NOT delete or modify `formatAgo`** (D-11) — it is still called by
    `QuotaIndicator` and stays exported from this file. `formatDateTime` is a NEW
    sibling, not a replacement of formatAgo's definition (the replacement happens
    at the CALL SITES in ActivityList/ReviewsList, which is Plan 02's Task 2).

    Place `formatDateTime` AFTER `formatDuration` (file order: formatAgo →
    formatDuration → formatDateTime) so the three formatters read as a sibling group.
  </action>
  <verify>
    <automated>cd web && grep -q 'export function formatDateTime' src/lib/time.ts && grep -q 'export function formatAgo' src/lib/time.ts && grep -q 'hour12: false' src/lib/time.ts && TZ=UTC node -e "const d=new Date(Date.parse('2026-07-29T14:32:00.000Z')); const s=d.toLocaleString('en-US',{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false}); if(s!=='Jul 29, 14:32'){console.error('FAIL expected Jul 29, 14:32 got',JSON.stringify(s));process.exit(1)} console.log('OK',s)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export function formatDateTime' web/src/lib/time.ts` — the new formatter is exported.
    - `grep -q 'export function formatAgo' web/src/lib/time.ts` — formatAgo is RETAINED (not deleted, per D-11).
    - The standalone node assertion `TZ=UTC node -e "...toLocaleString('en-US',{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false})..."` prints `OK Jul 29, 14:32` (proves the toLocaleString options produce the exact D-11 format in a deterministic TZ; the function wrapping is type-checked by build).
    - `grep -q 'hour12: false' web/src/lib/time.ts` — the 24h option is present.
    - `npm run build` exits 0 (formatDateTime type-checks; no unused-import errors).
  </acceptance_criteria>
  <done>
    `formatDateTime` exported from `lib/time.ts` producing `"Jul 29, 14:32"` for a
    UTC timestamp; `formatAgo` retained; build passes; ready for Plan 02's entry-time
    swap and chart hover labelFormatter.
  </done>
</task>

<task type="auto">
  <name>Task 3: StatsStrip explanatory tooltips on the three time-stat rows (D-08/D-09)</name>
  <files>web/src/components/activity/StatsStrip.tsx</files>
  <read_first>
    - `web/src/components/activity/StatsStrip.tsx` (the file being modified — read TimeStatRow lines 18-35 and the three call sites at lines 56-58; the two count rows at lines 41-53 must NOT be touched per D-09)
    - `web/src/components/StatusDot.tsx` (lines 56-70 — the cleanest in-repo example of the `<Tooltip><TooltipTrigger asChild>{trigger}</TooltipTrigger><TooltipContent>{text}</TooltipContent></Tooltip>` idiom; clone this shape verbatim)
    - `web/src/components/ui/tooltip.tsx` (the EXISTING shadcn Tooltip being reused — confirms `delayDuration=0` default, `sideOffset=0`, exports `Tooltip, TooltipContent, TooltipTrigger, TooltipProvider`; NO new tooltip library per D-02)
    - `web/src/components/layout/AppLayout.tsx` (confirms `TooltipProvider` is mounted ONCE at the app root — StatsStrip does NOT need its own provider)
    - `12-CONTEXT.md` D-08 + D-09 (the three verbatim tooltip copy strings and the "counts get NO tooltip" rule)
    - `12-UI-SPEC.md` §"StatsStrip Tooltip Contract (D-08..D-09)" (the trigger a11y contract — Info icon wraps in a real `<button>` for keyboard focus)
  </read_first>
  <action>
    Modify `TimeStatRow` in `web/src/components/activity/StatsStrip.tsx` to add an
    optional `help?: string` prop and mount the Info-icon tooltip beside the label.
    This implements D-08 (the three explanatory tooltips) and D-09 (counts excluded).

    **Imports to add** (mirror StatusDot.tsx:2-6):
    - `import { Info } from "lucide-react";` (the Info icon — lucide-react@^1.17.0 is
      already in package.json; `Info` export verified present in 12-RESEARCH)
    - `import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";`
      (the EXISTING shadcn Tooltip — do NOT import TooltipProvider, it is mounted at
      AppLayout root with `delayDuration={0}`)

    **TimeStatRow signature change** (line 18): add `help?: string` to the props
    destructure: `function TimeStatRow({ label, stat, help }: { label: string; stat: TimeStat; help?: string })`.
    When `help` is present, render the `<Info>` + `<Tooltip>` beside `{label}`
    INSIDE the existing `<span className="min-w-[5.5rem] text-muted-foreground">`
    (the min-w may need a small nudge since the icon adds ~16px — agent's discretion
    per CONTEXT; keep it visually tight with `gap-1` / `items-center`).

    **The trigger must be a real `<button>`** (D-08 a11y / UI-SPEC interaction
    contract) — NOT a bare icon — so Tab reaches it and focus opens the tooltip
    (radix default with `delayDuration=0`). Structure:
    `<Tooltip><TooltipTrigger asChild><button type="button" aria-label="{label} metric explanation" className="text-muted-foreground hover:text-foreground transition-colors"><Info className="size-3.5" /></button></TooltipTrigger><TooltipContent>{help}</TooltipContent></Tooltip>`
    — clone the StatusDot.tsx:56-70 idiom (TooltipTrigger asChild wrapping a single
    element).

    **The `stat.n === 0` em-dash guard (lines 22-29) stays UNCHANGED** — the Info
    icon + tooltip mount regardless of `n` (the metric definition is useful even
    when there's no data). Do not move or remove the guard.

    **The three call sites** (lines 56-58) gain a `help="..."` prop with the D-08
    VERBATIM copy:
    - Cycle row: `help={`Time from In Progress → Done. min · median · max = fastest · typical · slowest.`}`
    - In progress row: `help={`Time spent in the In Progress column. min · median · max = fastest · typical · slowest.`}`
    - In review row: `help={`Time spent in the In Review column. min · median · max = fastest · typical · slowest.`}`

    **The two count rows (lines 41-53) are NOT touched** (D-09 — taskCount and
    reviewCount are self-evident; they get NO Info icon, NO tooltip). Do not add a
    `help` prop to anything outside the three TimeStatRow calls.
  </action>
  <verify>
    <automated>cd web && grep -q 'from "lucide-react"' src/components/activity/StatsStrip.tsx && grep -q 'TooltipContent' src/components/activity/StatsStrip.tsx && [ "$(grep -c 'help=' src/components/activity/StatsStrip.tsx)" = "3" ] && grep -q 'fastest · typical · slowest' src/components/activity/StatsStrip.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'help=' web/src/components/activity/StatsStrip.tsx` equals `3` — exactly three `help=` props (one per time-stat row; D-08/D-09).
    - `grep -q 'from "lucide-react"' web/src/components/activity/StatsStrip.tsx` — the Info icon import is present.
    - `grep -q 'TooltipContent' web/src/components/activity/StatsStrip.tsx` — the existing shadcn Tooltip is wired (not a new tooltip lib).
    - `grep -q 'fastest · typical · slowest' web/src/components/activity/StatsStrip.tsx` — the D-08 min·median·max legend copy is present.
    - `grep -q 'aria-label=' web/src/components/activity/StatsStrip.tsx` — the trigger is a keyboard-focusable button with an aria-label (D-08 a11y).
    - `npm run build` exits 0 (TimeStatRow type-checks with the new optional `help` prop; no unused-import errors since noUnusedLocals is on).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>
    The three time-stat rows each show an Info icon whose hover/focus opens the
    existing shadcn Tooltip with the D-08 copy; counts have no tooltip; build + lint
    pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | `npx shadcn add chart` pulls `recharts` from the public npm registry into the build supply chain (the sole new external dependency this phase) |
| shadcn registry → source tree | `shadcn add chart` writes `web/src/components/ui/chart.tsx` (a vendored primitive) into the repo from the official shadcn registry (`radix-nova` preset) |
| local backend → SPA render | The stat-tooltip copy is hardcoded (D-08 strings); no server/user data flows into the tooltip text |

## STRIDE Threat Register

ASVS level 1, block threshold "high". This plan touches the dependency surface
(shadcn add chart) and a pure formatter + tooltip-copy change — the threat surface is
minimal. No high/critical threats; the highest is medium (mitigated).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-SC | Supply chain (dependency integrity) | recharts npm package (installed by shadcn add chart) | medium | mitigate | recharts vetted in 12-RESEARCH §Package Legitimacy Audit: 11-year history (created 2015-08-07), ~49M weekly downloads, official repo github.com/recharts/recharts, no postinstall script, not deprecated, shadcn's own chart block depends on it. The `SUS` verdict from the package-legitimacy seam was a false positive (keyed on latest-version publish date, not package creation date) — overridden and documented. Pin via package-lock.json (`npm install` after `shadcn add chart`). Not auto-approvable bypass — the signals are cross-checked against npm registry + shadcn docs + GitHub; no `checkpoint:human-verify` needed because the evidence is overwhelming. |
| T-12-01 | Information disclosure (XSS via tooltip content) | StatsStrip TooltipContent rendering D-08 copy | low | accept | The tooltip copy is developer-authored hardcoded strings (D-08 verbatim) — no server or user data flows into TooltipContent. React auto-escapes. No `dangerouslySetInnerHTML` in app code. |
| T-12-02 | Tampering (CSS injection via ChartStyle) | shadcn chart.tsx ChartStyle component | low | accept | ChartStyle uses `dangerouslySetInnerHTML` to inject `--color-*` CSS vars, but the values come from ChartConfig.color (sourced from the hardcoded PROJECT_PALETTE hex array in palette.ts), NOT from server/user data. This is the documented shadcn pattern; safe by construction. A compromised palette.ts would be a separate supply-chain issue. No mitigation beyond sourcing colors from the vetted palette (Plan 02's ChartConfig). |
| T-12-03 | Information disclosure (malformed timestamp crash) | formatDateTime parsing gh-sourced completedAt | low | mitigate | formatDateTime guards `!iso` → `"—"` and `Number.isNaN(Date.parse(iso))` → `"—"` BEFORE formatting (defense-in-depth for a malformed gh closedAt). The entry never renders a blank row or throws. |
</threat_model>

<verification>
- `cd web && npm run build` passes (tsc -b type-checks chart.tsx, formatDateTime, and the modified StatsStrip; vite build succeeds).
- `cd web && npm run lint` passes (no unused imports — noUnusedLocals is on; the StatsStrip changes introduce no lint regressions).
- `web/src/components/ui/chart.tsx` exists and exports ChartContainer / ChartConfig / ChartTooltip / ChartTooltipContent (grep each).
- `recharts` is the sole new entry in `web/package.json` dependencies (D-02).
- `formatDateTime` produces `"Jul 29, 14:32"` under TZ=UTC (node assertion in Task 2 verify).
- `formatAgo` is still exported from `lib/time.ts` (D-11 — not deleted).
- StatsStrip has exactly 3 `help=` props and the D-08 legend copy (grep counts in Task 3 verify).
- Visual UAT of the stat tooltips (hover/focus the Info icon, read the copy, confirm keyboard reachability) is deferred to phase verification (`/gsd-verify-work`) — this plan's automated gates prove structure and type-correctness.
</verification>

<success_criteria>
- shadcn chart primitive + recharts installed; chart.tsx exports the primitives Plan 02 imports.
- formatDateTime exported, produces the D-11 format, formatAgo retained.
- StatsStrip's three time-stat rows have Info-icon tooltips with D-08 copy; counts untouched.
- `npm run build` + `npm run lint` both pass.
- Plan 02 (Wave 2) is unblocked: it can import chart.tsx exports and formatDateTime.
</success_criteria>

<output>
Create `.planning/phases/12-activity-chart-tooltips/12-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01)

**New files:**
- `web/src/components/ui/chart.tsx` — shadcn-generated chart primitive (vendored). Exports: `ChartContainer`, `ChartConfig` (type), `ChartTooltip`, `ChartTooltipContent`, `ChartLegend`, `ChartLegendContent`, `ChartStyle`, plus internal `useChart` context and `getPayloadConfigFromPayload` helper.

**New exports in existing files:**
- `web/src/lib/time.ts` → `formatDateTime(iso: string | null | undefined): string` — absolute `"Jul 29, 14:32"` formatter (sibling of `formatAgo` / `formatDuration`).

**New npm dependency:**
- `recharts` (`^3.10.1`) — added to `web/package.json` dependencies by `shadcn add chart`; sole new dependency this phase (D-02).

**Modified components:**
- `web/src/components/activity/StatsStrip.tsx` `TimeStatRow` — gains optional `help?: string` prop; when present, renders a lucide `Info` icon inside `<Tooltip><TooltipTrigger asChild><button aria-label="…">…</TooltipTrigger><TooltipContent>{help}</TooltipContent></Tooltip>` beside the label.
