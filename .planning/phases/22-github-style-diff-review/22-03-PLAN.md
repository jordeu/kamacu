---
phase: 22-github-style-diff-review
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - web/src/api/diffs.ts
  - web/src/components/ui/checkbox.tsx
  - web/src/components/task/DiffFileSection.tsx
  - web/src/components/task/DiffTab.tsx
autonomous: true
requirements: [DIFF-02, DIFF-03, DIFF-04]
must_haves:
  truths:
    - "The DiffFile type carries hash + viewed; useToggleViewed optimistically flips viewed and invalidates the diff query"
    - "Each file header shows a blue-500 'Viewed' checkbox as a SIBLING of the collapse trigger (no button-in-button)"
    - "Marking Viewed collapses that file and dims its header (path + stats → muted-foreground); unchecking re-expands and restores contrast"
    - "Collapse and Viewed are independent — a file can be collapsed without being marked viewed (controlled Collapsible)"
    - "Binary files also get the Viewed checkbox; collapse-on-view is a harmless no-op there (D-04)"
    - "A changed file (new hash) remounts un-viewed + expanded via key=path:hash (DIFF-04 rendered outcome)"
  artifacts:
    - path: "web/src/components/ui/checkbox.tsx"
      provides: "shadcn checkbox primitive (radix-ui unified import)"
      contains: "CheckboxPrimitive"
    - path: "web/src/api/diffs.ts"
      provides: "hash+viewed on DiffFile; useToggleViewed optimistic mutation"
      contains: "useToggleViewed"
    - path: "web/src/components/task/DiffFileSection.tsx"
      provides: "sticky header, controlled Collapsible, sibling Viewed checkbox, dim-when-viewed, data-diff-path"
      contains: "Checkbox"
  key_links:
    - from: "web/src/components/task/DiffFileSection.tsx checkbox"
      to: "useToggleViewed"
      via: "onCheckedChange → mutate({path,hash,viewed}) + setOpen"
      pattern: "onCheckedChange"
    - from: "web/src/api/diffs.ts useToggleViewed"
      to: "PUT /api/tasks/{id}/diff/viewed"
      via: "put() + optimistic setQueryData(['diff', taskId])"
      pattern: "diff/viewed"
---

<objective>
Build the client Viewed control on top of the Plan 02 backend contract: extend the diff types, add the optimistic toggle mutation, add the shadcn `checkbox` primitive, and restructure `DiffFileSection` so a `blue-500` "Viewed" checkbox sits as a sibling of the collapse trigger, collapses/dims on check, and re-expands on uncheck — with collapse independently toggleable (DIFF-02) and the changed-file remount handled by a `path:hash` key (DIFF-04).

Purpose: The checkbox is the phase's one new accent interaction. It must obey the sibling-not-child rule (no invalid button-in-button) and keep collapse orthogonal to Viewed.
Output: `web/src/api/diffs.ts` (types + `useToggleViewed`), `web/src/components/ui/checkbox.tsx`, restructured `DiffFileSection.tsx`, and a MINIMAL `DiffTab.tsx` edit (pass `taskId` + `key={path:hash}`) so the toggle works end-to-end in the current single-column layout. The two-pane layout + file tree come in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-github-style-diff-review/22-UI-SPEC.md
@.planning/phases/22-github-style-diff-review/22-RESEARCH.md
@.planning/phases/22-github-style-diff-review/22-PATTERNS.md
@web/src/api/diffs.ts
@web/src/api/mutations.ts
@web/src/api/client.ts
@web/src/components/task/DiffFileSection.tsx
@web/src/components/task/DiffTab.tsx
@web/src/components/ui/switch.tsx
@web/src/components/task/TaskTabs.tsx
</context>

<interfaces>
<!-- Backend contract from Plan 22-02 (already shipped). GET /api/tasks/{id}/diff now returns per file: hash, viewed. -->
<!-- PUT /api/tasks/{id}/diff/viewed  body { path, hash, viewed }  → 204. put() returns undefined on 204 (client.ts:30-32). -->

DiffFile (extend, diffs.ts:16-24): add `hash: string;` and `viewed: boolean;`.
Query key (unchanged): ["diff", taskId].
useToggleViewed(taskId): mutationFn put(`/api/tasks/${taskId}/diff/viewed`, {path, hash, viewed}); optimistic setQueryData(["diff", taskId], map files → set viewed on path match); onSettled invalidate ["diff", taskId]. Mirror useMoveTask (mutations.ts:148-168).
</interfaces>

<notes>
- **checkbox primitive:** prefer `cd web && npx shadcn add checkbox` (copies `web/src/components/ui/checkbox.tsx`, imports from the already-installed `radix-ui@1.5.0` — no new npm dependency, matches `switch.tsx`). If the shadcn registry is unreachable in this environment, hand-author `web/src/components/ui/checkbox.tsx` mirroring `switch.tsx`'s unified import exactly: `import { Checkbox as CheckboxPrimitive } from "radix-ui"`, render `CheckboxPrimitive.Root` + `CheckboxPrimitive.Indicator` with a lucide `Check` icon and `cn()` classes. Either path is acceptable; no third-party registry.
- **blue-500 override (UI-SPEC Decision 2):** the default checkbox fill is near-white `primary` in dark; at the call site add `data-[state=checked]:border-blue-500 data-[state=checked]:bg-blue-500 data-[state=checked]:text-white`.
- **DiffTab edit is intentionally minimal here** — only `taskId` prop + `key={path:hash}`. Plan 04 (wave 4, depends on this plan) does the two-pane restructure of the same file.
- **Binary strict-D-01 limitation** carries through: a binary file can be marked Viewed; it will not auto-reset on a byte change (accepted, Plan 01 `<notes>`).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Extend diff types, add useToggleViewed, add checkbox primitive</name>
  <files>web/src/api/diffs.ts, web/src/components/ui/checkbox.tsx</files>
  <read_first>
    - web/src/api/diffs.ts — the `DiffFile` interface (diffs.ts:16-24) and `useTaskDiff` (diffs.ts:45-50, queryKey ["diff", taskId]).
    - web/src/api/mutations.ts — `useMoveTask` optimistic pattern (mutations.ts:148-168): onMutate cancel+snapshot+setQueryData, onError rollback, onSettled invalidate.
    - web/src/api/client.ts — `put` helper (client.ts:55-60); `api()` returns undefined for 204 (client.ts:30-32).
    - web/src/components/ui/switch.tsx — the exact unified `radix-ui` import convention the generated/hand-authored checkbox mirrors.
    - .planning/phases/22-github-style-diff-review/22-RESEARCH.md §Area 5 (toggle mutation body) and §Standard Stack (checkbox add command).
  </read_first>
  <action>
    In `web/src/api/diffs.ts`, add `hash: string;` and `viewed: boolean;` to the `DiffFile` interface. Add `useToggleViewed(taskId: number)` mirroring `useMoveTask`: `mutationFn` = `put(\`/api/tasks/${taskId}/diff/viewed\`, { path, hash, viewed })` typed to the arg `{ path: string; hash: string; viewed: boolean }`; `onMutate` cancels `["diff", taskId]`, snapshots the `DiffResponse`, and `setQueryData` maps `files` setting `viewed` on the matching `path`; `onError` restores the snapshot; `onSettled` invalidates `["diff", taskId]`.

    Add the checkbox primitive at `web/src/components/ui/checkbox.tsx`: run `cd web && npx shadcn add checkbox` if the registry is reachable; otherwise hand-author it to mirror `switch.tsx` (unified `radix-ui` `Checkbox.Root` + `Checkbox.Indicator`, lucide `Check`, `cn()`). Do NOT install any new npm dependency — `radix-ui@1.5.0` already exports `Checkbox`. Do not apply the blue-500 override in the primitive itself (that is a call-site class in Task 2).
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hash: string' web/src/api/diffs.ts` ≥ 1 and `grep -c 'viewed: boolean' web/src/api/diffs.ts` ≥ 1.
    - `grep -c 'useToggleViewed' web/src/api/diffs.ts` == 1; it calls `put(` against `/diff/viewed` and invalidates `["diff", taskId]`.
    - `web/src/components/ui/checkbox.tsx` exists and imports from `"radix-ui"` (`grep -c 'from "radix-ui"' web/src/components/ui/checkbox.tsx` ≥ 1).
    - `web/package.json` dependency list is unchanged except (at most) nothing new (no new registry package added).
    - `cd web && npm run build` (tsc -b + vite build) and `npm run lint` both pass.
  </acceptance_criteria>
  <done>DiffFile carries hash+viewed; useToggleViewed exists as an optimistic mutation on ["diff", taskId]; checkbox primitive present via radix-ui with no new dependency; build+lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure DiffFileSection — sibling Viewed checkbox, controlled collapse, sticky header, dim-when-viewed</name>
  <files>web/src/components/task/DiffFileSection.tsx, web/src/components/task/DiffTab.tsx</files>
  <read_first>
    - web/src/components/task/DiffFileSection.tsx — the current header-is-the-trigger structure (DiffFileSection.tsx:54-71), the uncontrolled `defaultOpen={changedLines <= 400}` (DiffFileSection.tsx:56), the binary branch (DiffFileSection.tsx:33-52), and the ± stats / `−` U+2212 / `tabular-nums` literals.
    - web/src/components/task/TaskTabs.tsx — the sibling-not-child precedent (TaskTabs.tsx:104-125): a focusable control inside another trigger must be a sibling with `e.stopPropagation()`; established `hover:bg-[#27272a]` + `focus-visible:ring-2 focus-visible:ring-blue-500` literals.
    - web/src/components/task/DiffTab.tsx — the section render loop (DiffTab.tsx:125-128) to receive the `taskId` prop + `key` change.
    - .planning/phases/22-github-style-diff-review/22-UI-SPEC.md — "Viewed control" + "Per-file collapse/expand" sections (header order `[chevron] [path flex-1 truncate] [status] [+add −del] [☐ Viewed]`, sticky header offset by totals-bar height, aria-labels, blue-500 checked fill).
    - .planning/phases/22-github-style-diff-review/22-RESEARCH.md §Area 5 (controlled collapse ↔ viewed coupling; blue-500 override) and 22-PATTERNS.md "DiffFileSection.tsx".
  </read_first>
  <action>
    Restructure the `DiffFileSection` header so the whole row is a flex `<div>`: the `CollapsibleTrigger` shrinks to `flex-1` (keeps chevron + path + status + ± stats), and a `<label>` wrapping the `Checkbox` + a `text-xs text-muted-foreground` "Viewed" label is a SIBLING after the stats. The checkbox calls `e.stopPropagation()` on click so toggling does not fire the collapse trigger; `onCheckedChange` calls the toggle and adjusts open state. Add `data-diff-path={file.path}` and `scroll-mt-[41px]` to the `Collapsible` root, and make the header `<div>` sticky (`sticky top-[41px] z-[5] bg-card`) so the checkbox stays reachable on long files (`41` ≈ the totals-bar height; a shared `TOTALS_BAR_PX` constant is finalized in Plan 04 — use 41 here).

    Promote the Collapsible from uncontrolled to CONTROLLED: accept a new required prop `taskId: number`; inside the component call `const toggle = useToggleViewed(taskId)`; `const [open, setOpen] = useState(!file.viewed && changedLines <= 400)`. On check → `toggle.mutate({ path: file.path, hash: file.hash, viewed: true })` and `setOpen(false)`; on uncheck → `toggle.mutate({ ...viewed: false })` and `setOpen(true)`. Pass `open`/`onOpenChange={setOpen}` to the Collapsible so manual collapse stays independent of Viewed. When `file.viewed`, dim the path label + ± stats to `text-muted-foreground`. Apply the blue-500 checked override on the Checkbox: `data-[state=checked]:border-blue-500 data-[state=checked]:bg-blue-500 data-[state=checked]:text-white`. Set the checkbox `aria-label` to `Mark {filename} as viewed` / `Mark {filename} as not viewed` (sentence case; `{filename}` is the path leaf). Apply the SAME sibling checkbox to the binary branch (D-04) — collapse-on-view is a no-op there since it is already non-collapsible; still dim its header when viewed.

    In `DiffTab.tsx` make the MINIMAL change only: pass `taskId={taskId}` to each `<DiffFileSection>` and change the render key from `file.path` to `` `${file.path}:${file.hash}` `` (so a new hash remounts the section un-viewed + expanded — DIFF-04). Do NOT restructure the layout, add the tree, or add scroll-spy — that is Plan 04.
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The Viewed checkbox is a sibling of (not nested inside) the `CollapsibleTrigger`; `grep -c 'stopPropagation' web/src/components/task/DiffFileSection.tsx` ≥ 1.
    - `grep -c 'data-\[state=checked\]:bg-blue-500' web/src/components/task/DiffFileSection.tsx` ≥ 1 (blue-500, not primary).
    - `grep -c 'data-diff-path' web/src/components/task/DiffFileSection.tsx` ≥ 1.
    - `grep -c 'useToggleViewed' web/src/components/task/DiffFileSection.tsx` == 1; the Collapsible uses controlled `open`/`onOpenChange` (no `defaultOpen`).
    - Checkbox aria-label uses exact copy `Mark ` … ` as viewed` / `as not viewed`; the "Viewed" label text is present.
    - `grep -c 'file.path}:${file.hash' web/src/components/task/DiffTab.tsx` ≥ 1 (key=path:hash) and `taskId={taskId}` is passed to DiffFileSection.
    - Deletions still render `−` (U+2212) with `tabular-nums`; the binary branch has the checkbox too.
    - `cd web && npm run build` and `npm run lint` both pass.
  </acceptance_criteria>
  <done>File headers show a blue-500 sibling Viewed checkbox; checking collapses + dims, unchecking re-expands, manual collapse stays independent; sticky headers + data-diff-path + scroll-mt in place; binary files included; DiffTab passes taskId and keys sections on path:hash; build+lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser component → API | Reuses the existing GET diff read and the Plan 02 `PUT .../diff/viewed` write. No NEW server surface is added by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | Invalid HTML: checkbox `<button>` nested in the trigger `<button>` breaks click/hydration semantics | mitigate | Checkbox is a SIBLING of a `flex-1` trigger with `e.stopPropagation()` (TaskTabs.tsx:104-125 precedent), never a child. Task 2 acceptance greps for the sibling structure. |
| T-22-SC | Tampering | `npx shadcn add checkbox` supply chain | accept | Copies a source file only; depends solely on the pre-installed, CLAUDE.md-blessed `radix-ui@1.5.0`. No new registry package is installed (RESEARCH Package Legitimacy Audit). Hand-authored fallback removes even the network fetch. |

No server-side auth/session/access-control surface is introduced. The optimistic client cache write is reconciled by the authoritative `onSettled` invalidate.
</threat_model>

<verification>
- `cd web && npm run build && npm run lint` green.
- With the backend from Plan 02 running: checking Viewed collapses + dims a file and persists (survives a reopen); unchecking re-expands. Collapse works without checking Viewed.
</verification>

<success_criteria>
- DiffFile carries hash+viewed; the optimistic toggle mutation is wired to the Plan 02 endpoint.
- A blue-500 sibling Viewed checkbox drives collapse+dim (check) / re-expand (uncheck), independent of manual collapse; binary files included.
- Sections remount un-viewed+expanded on a hash change (key=path:hash).
</success_criteria>

<output>
Create `.planning/phases/22-github-style-diff-review/22-03-SUMMARY.md` when done.
</output>
