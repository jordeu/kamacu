---
phase: 17-global-active-sessions-bar
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - web/src/components/layout/ActiveSessionsBar.tsx
  - web/src/components/layout/AppLayout.tsx
autonomous: false
requirements: [SBAR-01, SBAR-02, SBAR-03, SBAR-04, SBAR-05, SBAR-06, SBAR-07, SBAR-08, SBAR-09]
must_haves:
  truths:
    - "A status bar is pinned to the bottom of every route (board, task, settings); with zero live sessions it stays present showing 'No active sessions'"
    - "Collapsed, the bar shows per-state colored counts (working/waiting/idle) + a total; the waiting count is amber and pulses only when > 0"
    - "Expanding floats a panel UP over content as an overlay; main content and xterm terminals never reflow or resize"
    - "The expanded list is a flat list across all projects sorted waiting -> working -> idle; each row shows state dot, project name, task/PR title; PR rows get a #<n> badge; the current task's row is highlighted"
    - "Clicking a row navigates to /projects/:projectId/tasks/:taskId, including cross-project"
    - "Collapse/expand persists in localStorage under kangent:sessions-bar-collapsed, default collapsed; the bar updates within ~5s off the existing poll"
    - "Only live sessions (working/waiting/idle) appear; exited entries are filtered out"
  artifacts:
    - path: "web/src/components/layout/ActiveSessionsBar.tsx"
      provides: "The Global Active Sessions Bar: collapsed counts + waiting emphasis, expanded overlay list, navigation, persistence, live-only filter, loading/empty states"
      min_lines: 120
      contains: "kangent:sessions-bar-collapsed"
    - path: "web/src/components/layout/AppLayout.tsx"
      provides: "Mounts <ActiveSessionsBar /> at the bottom of the shell, outside <Outlet/>, on every route"
      contains: "ActiveSessionsBar"
  key_links:
    - from: "web/src/components/layout/ActiveSessionsBar.tsx"
      to: "useAgentStatuses() (web/src/api/agents.ts)"
      via: "the existing 5s poll, filtered to status in {working,waiting,idle}"
      pattern: "useAgentStatuses"
    - from: "web/src/components/layout/ActiveSessionsBar.tsx"
      to: "dotMeta() (web/src/components/StatusDot.tsx)"
      via: "status -> color for collapsed counts and per-row dots"
      pattern: "dotMeta"
    - from: "web/src/components/layout/ActiveSessionsBar.tsx"
      to: "/projects/:projectId/tasks/:taskId"
      via: "useNavigate on row click"
      pattern: "navigate\\(`/projects/"
    - from: "web/src/components/layout/AppLayout.tsx"
      to: "ActiveSessionsBar"
      via: "rendered inside SidebarProvider, outside <Outlet/>"
      pattern: "<ActiveSessionsBar"
---

<objective>
Build the Global Active Sessions Bar — a persistent, collapsible bottom bar showing every LIVE Claude agent session across all projects — and mount it in the app shell so it appears on every route. Collapsed (the default) it shows per-state counts with waiting emphasized; expanded it floats a panel UP over content (overlay, never reflowing the xterm terminals) listing each session attention-first, each row click-through to that task's agent view (cross-project). Covers SBAR-01..SBAR-09. Consumes the `taskTitle`/`projectName` fields added in Plan 01.

Purpose: One always-present, app-wide view of all live agent sessions, with the waiting state pulling the eye without the user expanding.
Output: `web/src/components/layout/ActiveSessionsBar.tsx` (new) + `AppLayout.tsx` mount; typechecks + builds; verified by the user against a live instance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-global-active-sessions-bar/17-CONTEXT.md
@.planning/phases/17-global-active-sessions-bar/17-UI-SPEC.md

<interfaces>
<!-- Contracts the bar consumes. Plan 01 added taskTitle + projectName to AgentStatusEntry. -->

AFTER Plan 01, web/src/api/agents.ts exports:
```ts
export interface AgentStatusEntry {
  taskId: number;
  projectId: number;
  sessionId: string;
  status: "working" | "idle" | "waiting" | "exited";
  exitCode: number | null;
  stopRequested: boolean;
  resumable: boolean;
  prNumber: number | null;
  source: "manual" | "github_pr";
  taskTitle: string;   // NEW (Plan 01)
  projectName: string; // NEW (Plan 01)
}
export function useAgentStatuses(): UseQueryResult<AgentStatusEntry[]>; // 5s refetchInterval
```

web/src/components/StatusDot.tsx exports (REUSE — never hardcode status colors):
```ts
export function dotMeta(
  entry: Pick<AgentStatusEntry, "status" | "exitCode" | "stopRequested">,
): { className: string; tooltip: string };
// working -> "bg-green-500" / "Working"
// waiting -> "bg-amber-400 animate-pulse motion-reduce:animate-none" / "Waiting for input"
// idle    -> "bg-zinc-400" / "Idle"
```

react-router (already used across the app):
```ts
import { useNavigate, useParams } from "react-router";
// route target exists in App.tsx: /projects/:projectId/tasks/:taskId
```

shadcn primitives already installed under web/src/components/ui/: button.tsx (variant="ghost", size="icon-sm"), skeleton.tsx (Skeleton), tooltip.tsx. lucide-react: ChevronUp, ChevronDown.

cn helper: import { cn } from "@/lib/utils";
</interfaces>

<patterns>
<!-- Mirror these EXACT idioms. They are the source of truth for chrome/persistence. -->

ReviewColumn localStorage default-collapsed idiom (web/src/components/board/ReviewColumn.tsx ~L56-65):
```ts
const storageKey = "kangent:sessions-bar-collapsed"; // GLOBAL key for this bar (D-04)
const [collapsed, setCollapsed] = useState<boolean>(
  () => localStorage.getItem(storageKey) !== "0", // absent => collapsed (default)
);
const toggle = () =>
  setCollapsed((c) => {
    const next = !c;
    localStorage.setItem(storageKey, next ? "1" : "0"); // expanded persists as "0"
    return next;
  });
```

ReviewColumn panel chrome + scroll: `bg-[#101013]`, `overflow-y-auto`, padding `p-3`, border `border-border`.
ReviewColumn empty block tone: `py-6 text-center` muted text.
ProjectSidebar amber waiting chip (web/src/components/sidebar/ProjectSidebar.tsx ~L74-85): `bg-amber-400/10 px-1 text-xs font-medium text-amber-400 tabular-nums`, with pluralized aria-label.
ProjectSidebar truncate idiom: `min-w-0 flex-1 truncate` on the label cell.
</patterns>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build ActiveSessionsBar.tsx (data derivation, collapsed counts, expanded overlay list, states)</name>
  <files>web/src/components/layout/ActiveSessionsBar.tsx</files>
  <read_first>
    - web/src/components/board/ReviewColumn.tsx (collapse idiom L56-65, panel chrome `bg-[#101013]`/`overflow-y-auto`/`p-3`, empty block `py-6 text-center`, skeleton loading)
    - web/src/components/StatusDot.tsx (dotMeta() — the ONLY source of status colors/tooltips)
    - web/src/components/sidebar/ProjectSidebar.tsx (amber waiting-chip classes L74-85, `min-w-0 flex-1 truncate` label idiom, pluralized aria-labels, how it derives counts from useAgentStatuses())
    - web/src/api/agents.ts (the AgentStatusEntry type extended by Plan 01 + useAgentStatuses)
    - web/src/components/ui/button.tsx, web/src/components/ui/skeleton.tsx (variant/size props available)
    - .planning/phases/17-global-active-sessions-bar/17-UI-SPEC.md (exact tokens/dimensions/copy — Spacing, Typography, Color, States, Copywriting sections)
  </read_first>
  <action>
    Create `web/src/components/layout/ActiveSessionsBar.tsx` exporting `function ActiveSessionsBar()`. Implement EXACTLY per 17-UI-SPEC.md. Concrete spec:

    DATA (single source — D-12; do NOT add a poll):
    - `const { data } = useAgentStatuses();`
    - LIVE filter (D-09): `const live = (data ?? []).filter((e) => e.status === "working" || e.status === "waiting" || e.status === "idle");` — `exited` (incl. DB-derived) never appears.
    - Counts: `working`, `waiting`, `idle` = `live.filter(...).length` each; `total = live.length`.
    - Sorted list (SBAR-05 / D-05), attention-first, STABLE within a state (keep feed order — do NOT sort by anything else, avoids 5s jitter):
      ```ts
      const rank = { waiting: 0, working: 1, idle: 2 } as const;
      const sorted = [...live].sort((a, b) => rank[a.status as keyof typeof rank] - rank[b.status as keyof typeof rank]);
      ```
      (Array.prototype.sort is stable in modern engines, so equal-rank rows keep feed order.)

    PERSISTENCE (SBAR-07 / D-04) — mirror ReviewColumn exactly, GLOBAL key:
    - `const storageKey = "kangent:sessions-bar-collapsed";`
    - `const [collapsed, setCollapsed] = useState<boolean>(() => localStorage.getItem(storageKey) !== "0");`
    - `toggle` writes `next ? "1" : "0"`.

    CURRENT-TASK HIGHLIGHT (D-07): `const { taskId: openTaskId } = useParams();` — a row is "you are here" when `String(entry.taskId) === openTaskId`.

    NAVIGATION (SBAR-06 / D-08): `const navigate = useNavigate();` row onClick → `navigate(\`/projects/${entry.projectId}/tasks/${entry.taskId}\`)`. Works cross-project.

    LAYOUT — the component returns a SINGLE root that is `fixed` to the viewport bottom so it overlays content and never reflows the xterm terminals (D-03). Use:
    `<div className="fixed inset-x-0 bottom-0 z-30 flex flex-col">` as the outer wrapper; the expanded panel renders ABOVE the collapsed bar inside this column (panel first, bar second) so the panel floats UP over content.

    COLLAPSED BAR (always rendered, SBAR-01/02/03 / D-01/02):
    - A `role="button"` element (whole bar is the toggle — D-04), `h-9` tall, `bg-[#101013]` fill, `border-t border-border`, horizontal padding `px-3`, `flex items-center gap-2`, `aria-expanded={!collapsed}`, and an `aria-label` describing counts e.g. `Active agent sessions: ${working} working, ${waiting} waiting, ${idle} idle — ${collapsed ? "expand" : "collapse"}`.
    - LOADING (data === undefined): render a single quiet `<Skeleton className="h-3 w-24" />` where the counts go (no spinner; bar height unchanged).
    - ZERO (resolved, total === 0): muted `<span className="text-xs text-muted-foreground">No active sessions</span>`.
    - POPULATED (total >= 1): three count groups, each `<span className="inline-flex items-center gap-1 text-xs font-medium tabular-nums">` with a `size-2 rounded-full` dot whose color comes from `dotMeta({ status, exitCode: null, stopRequested: false }).className` (working/idle) — NEVER hardcode the hex. For the WAITING group: when `waiting > 0` use the amber chip treatment from ProjectSidebar (`text-amber-400 tabular-nums`, dot keeps `bg-amber-400 animate-pulse motion-reduce:animate-none` via dotMeta('waiting')); when `waiting === 0` render it with the same muted treatment as the others (NO amber, NO pulse). Then a total like `<span className="text-xs font-medium text-muted-foreground tabular-nums">{total}</span>`.
    - Chevron at the end (`ml-auto`): lucide `ChevronUp` when collapsed (will expand) / `ChevronDown` when expanded (will collapse), `className="size-3.5 text-muted-foreground"`, inside a `Button variant="ghost" size="icon-sm"` whose onClick calls `e.stopPropagation(); toggle();` (mirror ReviewColumn's nested toggle button).

    EXPANDED PANEL (only when `!collapsed`, SBAR-04/05 / D-03/05/10) — rendered ABOVE the bar in the column:
    - `<div className="max-h-80 overflow-y-auto border-t border-border bg-[#101013] p-3">` (height-capped + scroll, D-10).
    - A short height/opacity transition (~150ms ease-out) honoring reduced motion: `transition-[max-height,opacity] duration-150 ease-out motion-reduce:transition-none` (Claude's Discretion, locked D-46).
    - If total === 0: centered muted empty block (mirror ReviewColumn) — heading `No active sessions` + body `Agent sessions appear here while they're running.`, `py-6 text-center` muted.
    - Else map `sorted` to ROWS. Each ROW:
      - `role="button"`, keyboard-focusable (`tabIndex={0}`), `onClick` navigates, `onKeyDown` activates on Enter/Space.
      - `aria-label={\`Open ${entry.taskTitle} in ${entry.projectName}\`}`.
      - Container: `<div className={cn("flex min-h-9 items-center gap-2 rounded-md px-2 py-1.5 text-sm cursor-pointer hover:bg-muted/50", isCurrent && "bg-sidebar-accent text-sidebar-accent-foreground")}>` where `isCurrent = String(entry.taskId) === openTaskId` (D-07 subtle non-chromatic highlight; must NOT use a status color).
      - State dot: a `size-2 rounded-full shrink-0` span colored from `dotMeta(entry).className` (reuse the exact entry — gives waiting its pulse).
      - Project name: `<span className="shrink-0 text-muted-foreground">{entry.projectName}</span>` followed by a separator dot `<span className="text-muted-foreground">·</span>`.
      - Title: `<span className="min-w-0 flex-1 truncate" title={entry.taskTitle}>{entry.taskTitle}</span>` (truncate idiom from ProjectSidebar).
      - PR badge (D-06): when `entry.source === "github_pr" && entry.prNumber != null`, append `<span className="shrink-0 rounded-full bg-card px-1.5 text-xs font-medium text-muted-foreground tabular-nums">#{entry.prNumber}</span>` (identifier, NOT a status color).

    Use `key={entry.taskId}` on rows. Import everything from the interfaces/patterns blocks above. Do NOT introduce new color tokens, do NOT hardcode status hex, do NOT add a second poll, do NOT change anything in xterm/task view.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/layout/ActiveSessionsBar.tsx` exists and `grep -c 'export function ActiveSessionsBar' web/src/components/layout/ActiveSessionsBar.tsx` returns 1.
    - File contains `"kangent:sessions-bar-collapsed"` and the `!== "0"` default-collapsed idiom.
    - File contains `useAgentStatuses` and `dotMeta` (status colors sourced from dotMeta, not hardcoded) — `grep -c 'dotMeta' ActiveSessionsBar.tsx` >= 2 (collapsed counts + per-row).
    - File contains `useNavigate` and the literal substring `navigate(\`/projects/${` (row navigation to the task route).
    - File contains the live filter expressed as `=== "working"` AND `=== "waiting"` AND `=== "idle"` and contains NO `=== "exited"` inclusion in the rendered list (exited filtered out).
    - File contains `fixed inset-x-0 bottom-0` (overlay pinned to viewport bottom, no reflow).
    - File contains `max-h-80 overflow-y-auto` (expanded panel cap + scroll) and `bg-[#101013]` (panel/bar chrome).
    - File contains `h-9` (collapsed bar height) and `size-2` (dots) and `size-3.5` (chevron).
    - File contains the exact strings `No active sessions` and `Agent sessions appear here while they're running.`.
    - File contains `text-amber-400` and `tabular-nums` (waiting emphasis + non-jittering counts) and `bg-sidebar-accent` (current-row highlight).
    - File contains `#{entry.prNumber}` (PR badge) and `bg-card` (badge chrome).
    - File contains `ChevronUp` and `ChevronDown` imported from `lucide-react`.
    - `cd web && npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>ActiveSessionsBar renders a fixed bottom overlay: collapsed per-state counts with waiting amber-pulse-when->0 + total, expanded attention-first flat list with project name · title (+ #n PR badge), current-row highlight, cross-project navigation, localStorage default-collapsed persistence, live-only filter, quiet loading/empty states — all colors from dotMeta, all tokens per UI-SPEC; typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount ActiveSessionsBar in AppLayout (bottom, outside Outlet, every route)</name>
  <files>web/src/components/layout/AppLayout.tsx</files>
  <read_first>
    - web/src/components/layout/AppLayout.tsx (the whole file — SidebarProvider wraps ProjectSidebar + the `<main>` that holds `<Outlet/>`; the bar must live alongside, not inside, the scrolling main so it pins to the viewport bottom on every route)
    - web/src/components/layout/ActiveSessionsBar.tsx (the component from Task 1 — it is already `fixed inset-x-0 bottom-0`, so mounting it once anywhere inside the provider makes it global)
  </read_first>
  <action>
    Edit `web/src/components/layout/AppLayout.tsx`:
    1. Add the import near the existing component imports: `import { ActiveSessionsBar } from "@/components/layout/ActiveSessionsBar";`
    2. Render `<ActiveSessionsBar />` inside `<SidebarProvider>` but OUTSIDE the `<main>`/`<Outlet/>` (D-13: bottom of the shell, present on every route). Place it as the last child of the `SidebarProvider` (after the `<main>...</main>` block), e.g.:
       ```tsx
       <ProjectSidebar />
       <main className={...}>
         <CollapsedSidebarTrigger />
         <Outlet />
       </main>
       <ActiveSessionsBar />
       ```
    Because ActiveSessionsBar is `fixed inset-x-0 bottom-0` (Task 1), a single mount renders it over every route without changing `<main>`'s layout or height — the xterm terminals never reflow (D-03). Do NOT alter the `<main>` className, the sidebar wiring, the localStorage sidebar logic, or `<Outlet/>`. Do NOT add bottom padding to `<main>` (the bar overlays; reserving space was the rejected "push content up" approach, D-03).
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/layout/AppLayout.tsx` contains `import { ActiveSessionsBar } from "@/components/layout/ActiveSessionsBar";`.
    - `web/src/components/layout/AppLayout.tsx` contains `<ActiveSessionsBar />`.
    - The `<ActiveSessionsBar />` line appears AFTER the closing `</main>` tag (outside Outlet) — verify with: `grep -n '</main>\|<ActiveSessionsBar' web/src/components/layout/AppLayout.tsx` shows ActiveSessionsBar's line number greater than the `</main>` line number.
    - `<main>` still uses its original `overflow-hidden` className (unchanged): `grep -c 'flex-1 overflow-hidden' web/src/components/layout/AppLayout.tsx` >= 1.
    - `cd web && npx tsc -b` exits 0 AND `npx vite build` exits 0 (the embedded SPA `web/dist` is rebuilt).
  </acceptance_criteria>
  <done>ActiveSessionsBar is mounted once in AppLayout outside the Outlet so it appears on board, task, and settings routes; `<main>` layout is untouched; tsc + vite build succeed and web/dist is rebuilt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — bar behavior, waiting emphasis, cross-project nav, no terminal reflow</name>
  <files>web/src/components/layout/ActiveSessionsBar.tsx, web/src/components/layout/AppLayout.tsx</files>
  <action>
    Claude has already automated the full implementation (Tasks 1-2) and confirmed `npx tsc -b && npx vite build` succeed. This task is the human-in-the-loop visual/functional verification that cannot be automated: confirming the overlay does not reflow the xterm terminal, the waiting amber pulse, and cross-project navigation against a live instance. Pause and present the how-to-verify steps below; do not proceed past this gate until the user responds.
  </action>
  <what-built>
    A persistent bottom Active Sessions Bar (web/src/components/layout/ActiveSessionsBar.tsx) mounted globally in AppLayout. Collapsed (default) it shows per-state counts (working/waiting/idle) + total with the waiting count amber + pulsing when >0; expanded it floats a panel UP over content listing live sessions attention-first (waiting -> working -> idle), each row = state dot · project name · task/PR title (PR rows get #n), clickable to that task's agent view (cross-project), with the current task's row highlighted. Persists collapse state in localStorage; updates within ~5s off the existing poll; exited sessions never appear.
  </what-built>
  <how-to-verify>
    Build and run, then check each item:
    1. Build + run the single binary: `cd /home/jordi/workspace/github/kangent && make build && ./bin/kangent` (or run the Vite dev server `cd web && npm run dev` against `make dev-backend`). Open http://localhost (or the dev URL).
    2. SBAR-01/09: With NO agent sessions running, confirm a thin bar is pinned to the bottom on the board, on a task page, AND on /settings, showing muted "No active sessions".
    3. SBAR-02/03: Start one or more agent sessions across one or more projects. Collapsed bar shows colored counts per state + a total. While a session is WAITING for input, its waiting count renders amber and the waiting dot pulses (note: with --dangerously-skip-permissions on by default the waiting state rarely fires — to exercise it, run a session WITHOUT the skip flag, or trigger a plan-mode/permission prompt; see STATE.md blockers).
    4. SBAR-04/05/07: Click the bar to expand. Confirm the panel floats UP over the content (does NOT push content up), lists every live session sorted waiting -> working -> idle, each row showing project name + task/PR title. PR-review sessions show a #<number> badge. Reload the page — the bar stays expanded (persistence); clear the choice and confirm it defaults to collapsed.
    5. SBAR-06: With project A open, click a row whose session belongs to project B — confirm you land on project B's task agent view. Confirm the currently-open task's row is subtly highlighted.
    6. D-03 (critical): Open a task with a live xterm terminal. Expand and collapse the bar repeatedly — the terminal must NOT resize/reflow and its content must not jump (the bar overlays; it does not change the terminal's height).
    7. SBAR-08: Stop a session and confirm it drops off the bar within ~5s; start one and confirm it appears within ~5s. Confirm exited/resumable sessions never appear in the bar.
  </how-to-verify>
  <verify>Manual: user confirms all 7 verification items pass against a live instance.</verify>
  <done>User has confirmed the bar appears on every route incl. zero state, waiting amber emphasis fires, expand floats up without reflowing the xterm terminal, cross-project navigation lands on the right task, and ~5s freshness with live-only filtering — or has reported issues for revision.</done>
  <resume-signal>Type "approved" if all checks pass, or describe what's wrong (e.g. "terminal reflows when bar expands", "waiting count not amber", "cross-project nav goes to wrong project").</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/jordi/workspace/github/kangent/web && npx tsc -b && npx vite build` exit 0 (typecheck + embedded SPA rebuilt).
- `grep -c 'dotMeta' web/src/components/layout/ActiveSessionsBar.tsx` >= 2 (no hardcoded status colors).
- ActiveSessionsBar is `fixed inset-x-0 bottom-0` and mounted outside `<Outlet/>` in AppLayout.
- Human checkpoint (Task 3) approved: bottom-on-every-route, waiting emphasis, expand-overlay-no-reflow, cross-project nav, ~5s freshness, live-only.
</verification>

<success_criteria>
- SBAR-01: bar present on every route incl. an always-present zero state.
- SBAR-02/03: collapsed per-state counts + total; waiting amber-pulse emphasis only when waiting > 0.
- SBAR-04/05: expand overlay lists live sessions attention-first; each row state dot · project name · task/PR title.
- SBAR-06: row click navigates to the task agent view, cross-project.
- SBAR-07: collapse choice persists, default collapsed.
- SBAR-08: ~5s freshness off the existing poll; no new hook.
- SBAR-09: zero state quiet, never blocks the view; xterm terminals never reflow (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/17-global-active-sessions-bar/17-02-SUMMARY.md`
</output>
