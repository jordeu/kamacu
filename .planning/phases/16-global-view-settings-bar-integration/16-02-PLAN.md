---
phase: 16-global-view-settings-bar-integration
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - web/src/components/task/AgentTab.tsx
  - web/src/pages/TaskPage.tsx
  - web/src/pages/GlobalTaskPage.tsx
  - web/src/App.tsx
autonomous: true
requirements: [GVIEW-01, GVIEW-04, GINT-01]
must_haves:
  truths:
    - 'App.tsx registers <Route path="/global" element={<GlobalTaskPage />} /> inside the AppLayout route group — a plain sibling of /settings and /activity, NOT wrapped in BoardWorkspaceSync, no URL params, plain default import (no lazy loading)'
    - 'The configured view renders the full TaskPage-derived shell with EXACTLY the Agent tab plus bash tabs (TabDef list = agent + visibleSessions) — no description tab, no diff tab, no kanban presence (GVIEW-01; no-new-scope carry-forward)'
    - 'State 3 (root_path === "") renders the unconfigured full-page hero pointing at Settings; state 4 (root_path set, !root_exists, all live counts 0) renders the DISTINCT vanished-root hero naming root_path in a mono span — never the generic unconfigured state (D-39/D-40)'
    - 'State 5 (!root_exists but live > 0) renders the full shell plus the advisory D-42 warning banner (role="status"); attached terminals keep streaming — nothing killed or detached; state 6 (folder root, root_exists) shows the persistent D-38 isolation banner; state 7 (managed clone) shows NO banner — exactly one of the two banners ever renders in states 5/6, never both, never neither'
    - 'The AgentTab global variant: Start enabled whenever the shell renders with body copy naming the root in a mono span (D-41); the running ⋯ menu renders exactly one item — Stop, destructive variant, no Insert items, no separator (D-36); the resumable flag comes from the server status entry found by source === "global" — never inferred client-side'
    - 'QuotaIndicator renders in the header iff the global default agent engine === "claude" (resolved from the ["global"] query — NOT TaskPage pattern-based gating; D-37 outcome locked, research Pitfall 2)'
    - 'The h1 is the static label Scratchpad — no back-arrow (D-35), no page-level menu, no Escape-to-board handler'
    - 'The task call site in TaskPage renders AgentTab with the task scope and byte-for-byte identical behavior — build green proves the loosening broke nothing'
  artifacts:
    - 'web/src/pages/GlobalTaskPage.tsx — NEW: the trimmed TaskPage shell + 7-state view matrix + degraded heroes'
  key_links:
    - 'agentEntry lookup: useAgentStatuses().data.find(e => e.source === "global") — the Agent tab StatusDot + resumable driver'
    - 'agentSession: the kind === "agent" entry of useGlobalSessions() data'
    - 'tmux reattach effect with the reattachedRef one-shot guard (TaskPage.tsx:148-162 mirrored) firing useReattachGlobalTmux for orphaned tmux ghosts'
    - 'Spawn/Start/Resume fire ONLY from click handlers (StrictMode rule); the trailing + keeps the 409-verbatim inline error span (TaskPage.tsx:465-473 idiom)'
  prohibitions:
    - statement: 'Do NOT extract or refactor a shared shell out of TaskPage — copy-then-trim into GlobalTaskPage (research Anti-Patterns; TaskPage is the hottest page in the app)'
      status: resolved
      verification: 'git diff shows TaskPage.tsx touched ONLY at the AgentTab call site'
    - statement: 'No BoardWorkspaceSync wrap, no :projectId/:taskId params on /global (sentinel-leak posture)'
      status: resolved
      verification: 'grep -c BoardWorkspaceSync web/src/pages/GlobalTaskPage.tsx returns 0; the route line carries no params'
    - statement: 'No Escape handler on /global (no parent board exists — research Anti-Patterns)'
      status: resolved
      verification: 'grep -ci "keydown" web/src/pages/GlobalTaskPage.tsx returns 0'
    - statement: 'Do NOT touch TaskPage QuotaIndicator gating or the types.ts Agent.engine union this phase (stale-union fix is out of scope; research Pitfall 2)'
      status: resolved
      verification: 'git diff on TaskPage.tsx shows only the AgentTab call-site change'
---

<objective>
The /global Scratchpad view (GVIEW-01 + GVIEW-04): a sibling GlobalTaskPage — copy-then-trim from TaskPage, never a refactor of it — rendering the 7-state view matrix off the shared ["global"] query, with AgentTab loosened behind a task/global scope discriminator (not forked), the route registered in App.tsx, and the click-through destination the 16-01 bar edits navigate to.

Purpose: The user-facing global scratchpad shell — Agent tab + bash tabs only, task-parity Start/Stop/Resume, honest degraded states driven entirely by server truth (D-17 one round-trip; D-28..D-30 honesty posture: never a silent $HOME/cwd fallback).
Output: web/src/pages/GlobalTaskPage.tsx (new), loosened AgentTab (single file), TaskPage call-site update, one App.tsx route line.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-global-view-settings-bar-integration/16-CONTEXT.md
@.planning/phases/16-global-view-settings-bar-integration/16-RESEARCH.md
@.planning/phases/16-global-view-settings-bar-integration/16-PATTERNS.md
@.planning/phases/16-global-view-settings-bar-integration/16-UI-SPEC.md
@.planning/phases/16-global-view-settings-bar-integration/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loosen AgentTab behind a task/global scope discriminator</name>
  <files>web/src/components/task/AgentTab.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>
  - web/src/components/task/AgentTab.tsx (whole file — hooks at :48-50, resumable lookup :54-59, pre-start CTA gating + copy :95-133, ⋯ menu :147-184, handleConnect cache write :191-199, running TerminalPane :213-251, onSessionExit :242-244)
  - web/src/pages/TaskPage.tsx:348-475 (the AgentTab call site you must keep behavior-identical)
  - web/src/api/sessions.ts (the 16-01 global hooks: useSpawnGlobalAgent, useResumeGlobalAgent, useRenameGlobalSession)
  - web/src/api/agents.ts (widened source union)
  - 16-RESEARCH.md Pattern 4 (the recommended discriminator shape) + Pitfall 4 (under-scoping the coupling audit)
  </read_first>
  <action>
  Loosen — do NOT fork. Introduce an exported scope discriminator type AgentTabScope as the union { kind: "task"; taskId: number } | { kind: "global" }, replacing the whole-Task and dead project props on AgentTab (the never-destructured project prop is deleted outright — it is dead code; keep description and seed as optional props the task call site continues to pass).
  Internal branches resolve off scope.kind (single file, thin call sites — research Pattern 4):
  - Hooks: task branch keeps useSpawnAgent(taskId)/useResumeAgent(taskId); global branch uses the 16-01 hooks useSpawnGlobalAgent()/useResumeGlobalAgent(). Stop stays the shared scope-blind useStopSession.
  - Resumable lookup: task branch keeps find by taskId; global branch reads find(e => e.source === "global")?.resumable — the server flag stays the single driver, never inferred (standing rule).
  - Pre-start state (D-41): task branch keeps its worktree gating + branch copy byte-for-byte; global branch enables the Start CTA unconditionally (the shell only renders it in states 5/6/7) and the body copy names the root: heading No agent session, body The agent runs directly in {root}. with the root in a mono span — obtain root_path from the shared useGlobal() query (cached instance, no new fetch; both queries already app-wide cached per D-37). CTA label Start agent, pending Starting… (locked task-parity strings).
  - ⋯ menu (D-36): the global branch forces both Insert affordances false and suppresses the separator so the running menu renders exactly one item — Stop (destructive variant). No Settings shortcut joins it.
  - Cache writes: the handleConnect optimistic write (waiting→idle) looks the status entry up by source === "global" in the global branch; onSessionExit invalidation rides the ["sessions"] prefix as today (16-01 hooks wrote ["sessions","global"] under it).
  - Reset/Resume pair, exited banner, TerminalPane wiring (headerMenu, dimWhenExited, showExitedClose={false}) are shared verbatim — the four AgentTab states map 1:1 onto the global scope.
  TaskPage.tsx call site: pass the task scope plus the same description/seed values it passes today — behavior byte-for-byte identical. No other TaskPage edits.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'AgentTabScope' web/src/components/task/AgentTab.tsx && grep -n 'kind: "global"' web/src/components/task/AgentTab.tsx && test "$(grep -c 'projectId' web/src/components/task/AgentTab.tsx)" = "0" && cd web && npx eslint src/components/task/AgentTab.tsx</automated>
    <!-- planner-discipline-allow: projectId -->
  </verify>
  <acceptance_criteria>
  - AgentTab.tsx exports AgentTabScope; the global branch exists (kind: "global" present); the dead never-read project prop is gone (grep count 0 in the file)
  - The global running ⋯ menu renders Stop only — both Insert flags false and no separator in that branch
  - Global resumable lookup is find by source === "global" on the shared status feed
  - TaskPage.tsx diff touches ONLY the AgentTab call site (imports + one JSX block) — build green proves task behavior compiles identical
  - npx eslint on AgentTab.tsx clean (no new set-state-in-effect debt)
  </acceptance_criteria>
  <done>AgentTab serves both scopes from one file; the task path is provably unchanged; the global path satisfies D-36/D-41.</done>
</task>

<task type="auto">
  <name>Task 2: Create GlobalTaskPage (7-state matrix + shell) and register the /global route</name>
  <files>web/src/pages/GlobalTaskPage.tsx, web/src/App.tsx</files>
  <read_first>
  - web/src/pages/TaskPage.tsx (whole file — the copy-then-trim source: state block :113-221 incl. the tmux reattach effect with reattachedRef guard :148-162 and the Pitfall-7 neighbor-reactivation useLayoutEffect :208-221, visibleSessions memo :168-188, removeTab/handleCloseTab/handleSpawn :307-346, tab strip :348-475, pending skeleton :246-256, spawn error span :465-473, page container idiom :481)
  - web/src/App.tsx:118-143 (the /activity route precedent + the AppLayout group) and :41-54 (the centered-hero idiom)
  - web/src/pages/SettingsPage.tsx:128-138 (the error/retry idiom for state 2)
  - web/src/components/task/TaskTabs.tsx (TabDef — already covers the global shape; untouched)
  - web/src/api/global.ts + web/src/api/sessions.ts (16-01: useGlobal, useGlobalSessions, useSpawnGlobalSession, useReattachGlobalTmux, useRenameGlobalSession)
  - web/src/components/quota/QuotaIndicator.tsx (self-gating on ["usage"]; you only gate its rendering)
  - 16-UI-SPEC.md — The View State Matrix (LOCKED) + Surface 1/2 + Copywriting rows 1-17
  </read_first>
  <action>
  NEW web/src/pages/GlobalTaskPage.tsx — copy-then-trim from TaskPage (research Anti-Patterns: never extract a shared shell; duplicate the ~80-line bash-tab lifecycle block wholesale, then delete the task-only branches).
  View-state matrix — one useGlobal() response drives everything, evaluated top-down (UI-SPEC LOCKED):
  - State 1 (query pending): TaskPage pending skeleton idiom (max-w-[860px] space-y-4 p-4 + Skeleton rows).
  - State 2 (query error): centered Couldn't load the scratchpad. + outline button Retry loading (SettingsPage error idiom).
  - State 3 (root_path === ""): D-39 unconfigured full-page hero — heading Scratchpad isn't configured yet, body The root folder and default agent live in Settings., primary button Open Settings navigating to /settings (centered-hero idiom from App.tsx:41-54).
  - State 4 (root_path set, !root_exists, live.agent + live.bash + live.tmux all 0): D-40 vanished-root hero — heading The Scratchpad root no longer exists, body The configured root no longer exists on disk: {path}. Point Scratchpad at a new root in Settings. with {path} = root_path in a mono span, same Open Settings CTA. DISTINCT from state 3 — never the generic unconfigured state.
  - States 5/6/7: the full shell (below). Banner slot renders exactly one: state 5 (!root_exists && any live > 0) the D-42 advisory banner — The configured root no longer exists on disk: {path}. Running sessions are unaffected. (path mono, role="status", amber idiom, non-blocking; terminals keep streaming, nothing killed/detached — warn-don't-disturb); state 6 (folder root: github_repo === null && root_exists) the persistent D-38 banner — Agent runs directly in {root} — no worktree isolation. (root mono); state 7 (github_repo !== null && root_exists) NO banner. Amber banner classes per the standing idiom (TaskPage.tsx:642): border-amber-500/30 bg-amber-500/10 text-amber-400, rounded-md px-2 py-1 text-xs, role="status" — never alert.
  Shell structure (GVIEW-01, D-35/D-37):
  - Page container: the TaskPage working-surface idiom; header row with static h1 label Scratchpad (text-base font-medium) — no back-arrow, no title button, no page menu — and QuotaIndicator right-aligned rendering iff the global agent engine === "claude" (from useGlobal data; the D-37 locked outcome — do not copy TaskPage pattern-based engine gating, research Pitfall 2). No Escape handler anywhere.
  - Tab strip via TaskTabs: TabDef list is exactly [agent, ...visibleSessions-derived bash tabs]. Agent tab: label Agent, leading StatusDot from agentEntry (useAgentStatuses data found by source === "global"), keepMounted true, no onClose. Bash tabs: server labels, onClose/onRename while running, keepMounted, TerminalPane content min-h-[320px] with onSessionExit invalidation. NO description tab, NO diff tab.
  - Bash-tab lifecycle copied wholesale from TaskPage: activeTab/closingIds/keepExitedIds state (always lands on Agent), keepExited poll effect, the tmux reattach effect WITH the reattachedRef one-shot guard firing useReattachGlobalTmux for orphaned ghosts, the visibleSessions filter+sort memo (kind !== "agent", !orphaned, running-or-kept-exited), the neighbor-reactivation useLayoutEffect with "agent" as the universal fallback, removeTab/handleCloseTab/handleSpawn — all spawns/reattaches fire from click handlers ONLY (StrictMode rule). Cache keys become ["sessions","global"]; rename uses useRenameGlobalSession.
  - Trailing + (task parity): ghost icon-sm button, aria-label and tooltip New bash session, Plus icon; enabled whenever the shell renders (states 5/6/7 — including state 5, warn-don't-block; a click that 409s surfaces the message verbatim); keep the inline 409-verbatim error span (ApiError && status 409 ? message : Couldn't start a session. Try again., text-xs text-red-500).
  - Agent tab content: AgentTab with the global scope + agentSession (the kind === "agent" entry from useGlobalSessions data) — Task 1 delivers the internals.
  - Dropped entirely from the TaskPage copy: title editing, worktree meta line, create/cleanup/delete dialogs, PR branches, the board Escape handler, and every taskId-derived lookup.
  Route: App.tsx — plain default import of GlobalTaskPage and <Route path="/global" element={<GlobalTaskPage />} /> inside the AppLayout route group as a sibling of /settings and /activity (the /activity precedent), NOT wrapped in BoardWorkspaceSync, with a one-line comment marking the Phase-16 top-level Scratchpad route.
  All wire-supplied strings (root paths, labels) render as JSX text/span children — React auto-escaping is the mitigation; never render them through a raw-HTML injection API.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'path="/global"' web/src/App.tsx && grep -n 'Scratchpad' web/src/pages/GlobalTaskPage.tsx && grep -n 'source === "global"' web/src/pages/GlobalTaskPage.tsx && test "$(grep -c 'Description' web/src/pages/GlobalTaskPage.tsx)" = "0" && test "$(grep -ci 'keydown' web/src/pages/GlobalTaskPage.tsx)" = "0" && test "$(grep -c 'dangerouslySetInnerHTML' web/src/pages/GlobalTaskPage.tsx)" = "0" && cd web && npx eslint src/pages/GlobalTaskPage.tsx</automated>
  </verify>
  <acceptance_criteria>
  - App.tsx route path /global inside AppLayout (grep hit), plain import, NOT inside BoardWorkspaceSync (grep BoardWorkspaceSync in GlobalTaskPage.tsx returns 0)
  - GlobalTaskPage.tsx contains: the h1 label Scratchpad; the D-39 copy (Scratchpad isn't configured yet); the D-40 copy (The configured root no longer exists); both D-38/D-42 banner strings; the state-2 copy Couldn't load the scratchpad. + Retry loading
  - agentEntry lookup by source === "global" present; the tmux reattach guard (a ref-based one-shot Set) present
  - Zero occurrences of: a Description tab label, any keydown handler, the raw-HTML injection API literal (grep counts all 0)
  - QuotaIndicator gated on engine === "claude" off the ["global"] query (grep engine === "claude" hits)
  - npx eslint on the new file clean; build green
  </acceptance_criteria>
  <done>/global renders the trimmed task-parity shell across all 7 states with honest degraded heroes (GVIEW-01 + GVIEW-04), and the 16-01 bar click-through now lands on a real page (GINT-01 destination half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localhost API | Existing boundary; this plan adds zero endpoints — it renders GET /api/global + the scoped session/status feeds already shipped |
| wire → DOM | Server-supplied strings (root_path, labels, tmux names) rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | Wire strings (root_path, path in D-40/D-42 heroes, tab labels) rendered into DOM | medium | mitigate | JSX text nodes + mono spans only (React auto-escaping); grep gate proves the raw-HTML injection API appears 0 times in the new file |
| T-16-02 | Elevation of Privilege | Un-isolated agent running in the user's real checkout (research P3 — no worktree safety net) | medium | mitigate | D-38 persistent isolation banner on every folder-root render (state 6) + the shipped server-side footgun blocks (D-07/D-27, Phase 14); the view never fabricates or falls back to a root (D-28..D-30) |
| T-16-03 | Denial of Service | State-5 spawn attempts against a vanished root | low | accept | Locked warn-don't-block posture (D-42): the server 409s and the message surfaces verbatim inline — never client-pre-blocked |
| T-16-SC | Tampering | package installs | n/a | accept | Zero-install phase (16-RESEARCH audit: none) |
</threat_model>

<verification>
- npm --prefix web run build green; npx eslint clean on GlobalTaskPage.tsx + AgentTab.tsx in isolation (the 29 pre-existing repo lint errors are out of scope — new files must not add to them)
- All grep gates above pass (route, copy strings, source lookup, zero Description tab, zero Escape handler, zero raw-HTML injection)
- End-of-phase manual UAT (drives GVIEW-01/GVIEW-04): unconfigured → hero with Open Settings; configure a folder root via curl PUT /api/global → shell + D-38 banner; Start agent → terminal + bar row; rename the root dir on disk → state 5 warn banner with terminals still streaming; stop all + vanish → state 4 hero naming the path; repo root → state 7 no banner; opencode default agent → no quota chip (D-37)
</verification>

<success_criteria>
- GVIEW-01: /global renders Agent + bash tabs only with Start/⋯-Stop/Resume task parity (D-36/D-41)
- GVIEW-04: honest unconfigured + vanished-root states, server-truth-driven, distinct (D-39/D-40)
- GINT-01 destination: bar click-through and highlight resolve to a real page
- TaskPage untouched except the AgentTab call site; zero new packages
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / File | Kind |
|---------------|------|
| web/src/pages/GlobalTaskPage.tsx | NEW file — default export GlobalTaskPage |
| AgentTabScope (type) | export, AgentTab.tsx — { kind: "task"; taskId: number } \| { kind: "global" } |
| AgentTab scope/description/seed props | loosened prop surface replacing the Task-object prop (AgentTab.tsx) |
| TaskPage AgentTab call site | only TaskPage.tsx edit (scope={{kind:"task",…}}) |
| /global Route | App.tsx route registration |

<output>
Create `.planning/phases/16-global-view-settings-bar-integration/16-02-SUMMARY.md` when done
</output>
