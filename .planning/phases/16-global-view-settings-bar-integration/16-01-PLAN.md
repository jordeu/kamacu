---
phase: 16-global-view-settings-bar-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/client.ts
  - web/src/api/agents.ts
  - web/src/api/sessions.ts
  - web/src/api/global.ts
  - web/src/components/layout/ActiveSessionsBar.tsx
autonomous: true
requirements: [GINT-01, GVIEW-01]
must_haves:
  truths:
    - 'AgentStatusEntry.source is the union "manual" | "github_pr" | "global" (agents.ts:13) and `tsc -b` passes — the type catches up to the server value emitting "global" since Phase 15 (agents.go:192)'
    - 'ApiError carries an optional reasons field of shape {kind: string; target: string}[] populated in client.ts whenever the error body carries reasons (the 409 PUT /api/global body); every existing single-message caller behaves identically'
    - 'TermSession declares global?: boolean alongside orphaned?/tmuxName? (mirrors session.Info.Global, session.go:73)'
    - 'useGlobal() polls GET /api/global under queryKey ["global"] with 5000ms refetchInterval; useSaveGlobal() PUTs a partial body {root_path?|repo?|agent_id?} (omit untouched keys) and writes the PUT response into the ["global"] cache — the PUT response IS the GET shape (D-23)'
    - 'useGlobalSessions() lists GET /api/sessions?scope=global under queryKey ["sessions", "global"] with 5000ms refetch; useSpawnGlobalSession posts {scope:"global"}, useSpawnGlobalAgent posts {scope:"global", kind:"agent"} and also invalidates ["agent-statuses"], useResumeGlobalAgent posts {scope:"global", kind:"agent", resume:true}, useReattachGlobalTmux(name) posts {scope:"global", reattach_tmux_name:name}, useRenameGlobalSession mirrors useRenameSession keyed ["sessions", "global"] — every mutation does setQueryData(["sessions","global"], …) FIRST, then invalidateQueries({queryKey: ["sessions"]})'
    - 'ActiveSessionsBar rows are keyed entry.sessionId (was entry.taskId); a global entry opens navigate("/global") + collapse(); a global entry highlights when location.pathname === "/global"; task rows keep String(entry.taskId) === openTaskId unchanged; SessionRow itself is untouched'
  artifacts:
    - 'web/src/api/global.ts — NEW: GlobalConfig interface, useGlobal(), useSaveGlobal()'
  key_links:
    - '["global"] cache shared by useGlobal/useSaveGlobal and (Phase plans 16-02/16-03) the /global view + Settings section — D-17 one round-trip'
    - '["sessions","global"] scoped cache sits under the ["sessions"] prefix so existing prefix invalidation keeps working'
    - 'Bar onOpen global branch navigates to /global — the route itself lands in 16-02'
  prohibitions:
    - statement: 'No new runtime `source ===` branch sites anywhere outside ActiveSessionsBar.tsx (grep-verified audit: the only pre-existing site is the PR badge at :268 === "github_pr")'
      status: resolved
      verification: 'grep -rn "source ===" web/src --include=*.tsx --include=*.ts shows only the bar (2 new sites) + the 16-02 agentEntry lookup'
    - statement: 'No client-side inference of liveness or resumability — the wire live counts and resumable flag are the only drivers'
      status: resolved
      verification: 'global.ts contains no os.Stat-like probing; hooks pass wire data through unchanged'
---

<objective>
The wire layer and bar integration for the global Scratchpad surface (GINT-01 + the GVIEW-01 session-surface foundations): TS wire-type widenings the shipped Phase 14–15 backends already emit, the new global config client, scope-aware session hooks, and the Active Sessions bar's three surgical changes (P6 re-keying, /global navigation branch, pathname highlight).

Purpose: Every backend surface Phase 16 drives is shipped and contract-frozen; this plan makes the frontend types and data layer catch up so plans 16-02 (the /global view) and 16-03 (Settings section) are pure composition. Zero new packages (carry-forward locked).
Output: web/src/api/global.ts (new), additive widenings in client.ts/agents.ts/sessions.ts, the bar's GINT-01 edits.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-global-view-settings-bar-integration/16-CONTEXT.md
@.planning/phases/16-global-view-settings-bar-integration/16-RESEARCH.md
@.planning/phases/16-global-view-settings-bar-integration/16-PATTERNS.md
@.planning/phases/16-global-view-settings-bar-integration/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the TS wire types + surface 409 reasons on ApiError</name>
  <files>web/src/api/client.ts, web/src/api/agents.ts, web/src/api/sessions.ts</files>
  <read_first>
  - web/src/api/client.ts (whole file — the ApiError class and the !res.ok extraction at :19-28 you are extending)
  - web/src/api/agents.ts (whole file — AgentStatusEntry.source at :13)
  - web/src/api/sessions.ts (whole file — TermSession at :5-22; you edit only the interface in this task)
  - internal/api/global.go:338-346 (the 409 body you are surfacing: error string + reasons array of {kind, target})
  - internal/api/agents.go:182-196 (the shipped global status entries the source union must admit)
  - internal/session/session.go:70-75 (session.Info.Global — the flag TermSession.global mirrors)
  </read_first>
  <action>
  Three additive, behavior-preserving edits (research Pitfall 1/3/7):
  1. client.ts — extend ApiError with an optional reasons field of type array of {kind: string; target: string}. In the !res.ok branch, widen the decoded body type to include an optional reasons array of the same shape and populate the field on the thrown ApiError when the body carries it (the 409 gate at global.go:341-344 emits it). Existing single-message callers (nothing reads reasons yet) are untouched — no behavior change outside the new field.
  2. agents.ts — widen the AgentStatusEntry.source union from "manual" | "github_pr"" to also admit "global" (the server emits it since Phase 15). No other edits to this file; the only runtime branch on source in the codebase is the PR badge in ActiveSessionsBar.tsx (=== "github_pr"), which a global value flows through harmlessly — do not add any new branch in this file.
  3. sessions.ts — add global?: boolean to the TermSession interface alongside orphaned?/tmuxName?, commented as mirroring session.Info.Global (session.go:73) — orphaned global tmux ghosts are synthesized with it (sessions.go:159-166). Type-honesty only; not load-bearing for rendering.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'reasons' web/src/api/client.ts && grep -n '"github_pr" | "global"\|"global" | "github_pr"\|"manual" | "github_pr" | "global"' web/src/api/agents.ts && grep -n 'global?: boolean' web/src/api/sessions.ts</automated>
  </verify>
  <acceptance_criteria>
  - web/src/api/agents.ts contains source: "manual" | "github_pr" | "global" (any union order) — tsc -b passes (npm run build green)
  - web/src/api/client.ts declares the reasons field on ApiError AND populates it in the error path when the decoded body carries it (2+ grep hits for reasons)
  - web/src/api/sessions.ts TermSession contains global?: boolean
  - Zero runtime behavior change: no existing call site of ApiError or AgentStatusEntry is modified
  </acceptance_criteria>
  <done>Wire types admit the shipped global values; ApiError can carry the structured 409 reasons; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Create web/src/api/global.ts + the scope-aware session hooks</name>
  <files>web/src/api/global.ts, web/src/api/sessions.ts</files>
  <read_first>
  - web/src/api/settings.ts (whole file — the useSettings/useSaveSetting query+mutation shape you are mirroring)
  - web/src/api/agents.ts:18-26 (the 5s refetchInterval idiom every feed uses)
  - web/src/api/sessions.ts (whole file — the task-scoped hooks whose onSuccess contracts the global variants copy exactly)
  - internal/api/global.go:44-60 (globalConfig — the frozen GET wire: root_path, github_repo nullable pointer, agent_id, updated_at, root_exists, live {agent,bash,tmux}, agent {id,name,engine})
  - internal/api/global.go:292-320 (PUT partial-PATCH pointer grammar: root_path/repo/agent_id pointers, omit untouched keys, root_path "" is THE clear)
  - internal/api/sessions.go:68-80 (GET ?scope=global branch) and :330-360 (POST body grammar: scope, kind, resume, reattach_tmux_name — scope mutually exclusive with task_id)
  </read_first>
  <action>
  A. NEW web/src/api/global.ts (research Pattern 2 is the locked spelling):
  - Export interface GlobalConfig mirroring globalConfig exactly: root_path string; github_repo string | null (null ⇒ folder root or unconfigured — the pointer IS the managed marker, no managed boolean); agent_id number; updated_at string; root_exists boolean; live {agent: number; bash: number; tmux: number}; agent {id: number; name: number→ string; engine: string}. Type agent.engine as plain string mirroring the Go wire — do NOT reuse the stale Agent.engine union in types.ts (research Pitfall 2: the opencode system seed emits "opencode" the union lacks).
  - Export useGlobal(): useQuery, queryKey ["global"], queryFn get&lt;GlobalConfig&gt;("/api/global"), refetchInterval 5000. This ONE query is shared by the Settings section (16-03) and the /global view (16-02) — D-17 one round-trip.
  - Export useSaveGlobal(): useMutation with mutationFn taking the partial body {root_path?: string; repo?: string; agent_id?: number} calling put&lt;GlobalConfig&gt;("/api/global", body) — omit untouched keys (pointer semantics); onSuccess writes the response into the cache via setQueryData(["global"], response) — the PUT response IS the GET shape (D-23); no extra refetch.
  B. web/src/api/sessions.ts — add the scope-aware hooks (research Pattern 3; the CONTRACT is keys/bodies/5s parity — these are new hooks, the existing task-scoped ones stay untouched):
  - useGlobalSessions(): useQuery, queryKey ["sessions", "global"], queryFn get&lt;TermSession[]&gt;("/api/sessions?scope=global"), refetchInterval 5000.
  - useSpawnGlobalSession(): posts {scope: "global"}.
  - useSpawnGlobalAgent(): posts {scope: "global", kind: "agent"}; ALSO invalidates ["agent-statuses"] (useSpawnAgent precedent at sessions.ts:99).
  - useResumeGlobalAgent(): posts {scope: "global", kind: "agent", resume: true}.
  - useReattachGlobalTmux(): mutate arg is the tmux name string; posts {scope: "global", reattach_tmux_name: name} (attach-or-create, TMUX-05/D-88 semantics).
  - useRenameGlobalSession(): PATCH /api/sessions/{id} {label} mirroring useRenameSession but with cache writes keyed ["sessions", "global"].
  - EVERY mutation onSuccess: first the spawn-select race fix — setQueryData(["sessions","global"], …) writing/preplacing the returned TermSession exactly like the task hooks do for ["sessions", taskId] — then invalidateQueries({queryKey: ["sessions"]}) (prefix-matches the scoped key). Agent variants also invalidate ["agent-statuses"].
  - Leave useStopSession / useDeleteSession untouched (scope-blind via the ["sessions"] prefix invalidation).
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -c 'queryKey: \["global"\]' web/src/api/global.ts && grep -c '\["sessions", "global"\]' web/src/api/sessions.ts && grep -c 'scope=global' web/src/api/sessions.ts && grep -n 'refetchInterval: 5000' web/src/api/global.ts web/src/api/sessions.ts</automated>
  </verify>
  <acceptance_criteria>
  - web/src/api/global.ts exists, exports GlobalConfig + useGlobal + useSaveGlobal; queryKey ["global"] with 5000ms refetch; useSaveGlobal onSuccess does setQueryData(["global"], response)
  - web/src/api/sessions.ts contains all six global hooks; grep counts: ["sessions", "global"] appears ≥ 7 times (1 query + 6 mutation cache writes), scope=global exactly 1
  - All agent-variant mutations invalidate ["agent-statuses"]; every mutation does setQueryData BEFORE invalidateQueries
  - Agent.engine consumed as plain string — no import of the types.ts Agent union in global.ts
  - Build green
  </acceptance_criteria>
  <done>The shared ["global"] config query and the full scope-aware session hook set exist with exact keys/bodies matching the shipped backend grammar.</done>
</task>

<task type="auto">
  <name>Task 3: ActiveSessionsBar — the three surgical GINT-01 edits (P6)</name>
  <files>web/src/components/layout/ActiveSessionsBar.tsx</files>
  <read_first>
  - web/src/components/layout/ActiveSessionsBar.tsx (whole file — the useParams/openTaskId wiring at :74, the LIVE filter at :79-85, the sorted.map SessionRow block at :127-139, SessionRow at :244-274)
  - web/src/api/agents.ts (the widened source union from Task 1)
  - 16-RESEARCH.md Pitfall 5 (sessionId "" occurs only on exited rows, removed by the LIVE filter) and Pitfall 6 (useLocation not useParams)
  </read_first>
  <action>
  Exactly three changes to the SessionRow render site — nothing else in the file (P6 research-locked; UI-SPEC Surface 4):
  1. Re-key rows: key={entry.sessionId} replacing key={entry.taskId} (:129), with a comment citing that empty sessionIds occur only on DB-derived exited rows which the LIVE filter (:79-85) removes before render — live rows always carry unique sessionIds (Pitfall 5).
  2. Navigation branch: inside onOpen, when entry.source === "global" call navigate("/global") instead of the task route, then collapse() as before. This replaces the Phase-15 interim dead-route artifact (/projects/0/tasks/0) with the real destination (route lands in 16-02).
  3. Highlight branch: add useLocation() alongside useParams(); a global entry's isCurrent is location.pathname === "/global"; task rows keep String(entry.taskId) === openTaskId unchanged (Pitfall 6 — no taskId param exists on /global).
  SessionRow, the LIVE filter, sorting, aria templates, and the collapsed counts are UNTOUCHED — the wire-synthesized Global · Scratchpad labels flow through the standard slots (GINT-01 non-nullable wire contract), and NO badge/tint/icon joins them (D-12 defers visual treatment to Phase 17 UAT). Do not add any other source === site.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -n 'key={entry.sessionId}' web/src/components/layout/ActiveSessionsBar.tsx && test "$(grep -c 'source === "global"' web/src/components/layout/ActiveSessionsBar.tsx)" = "2" && grep -n 'useLocation' web/src/components/layout/ActiveSessionsBar.tsx</automated>
  </verify>
  <acceptance_criteria>
  - ActiveSessionsBar.tsx renders key={entry.sessionId} (no key={entry.taskId} remains)
  - Exactly 2 occurrences of source === "global" (the onOpen branch + the isCurrent branch) — the navigation and highlight cases
  - useLocation imported and used for the global-row current-page highlight; task-row highlight expression unchanged
  - SessionRow component body unchanged (no props added, no badge/globe/tint)
  - Build green
  </acceptance_criteria>
  <done>A live global agent session renders as a standard Global · Scratchpad bar row that navigates to /global and highlights when /global is the current page (GINT-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localhost API | Existing single-user boundary; this plan only reads/extends client typing and the bar render — no new endpoints, no new transports |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | Wire strings (paths, labels, reasons[].target) rendered into DOM | medium | mitigate | All later rendering (16-02/16-03) uses JSX text nodes + mono spans (React auto-escaping); this plan passes wire data through unchanged — no string composition into markup |
| T-16-02 | Information Disclosure | ApiError.reasons exposing internal tmux names | low | accept | Single-user local app; tmux names are user-created artifacts; rendering them verbatim is the D-45 contract |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero-install phase (16-RESEARCH Package Legitimacy Audit: no installs; carry-forward "Zero new packages") |
</threat_model>

<verification>
- npm --prefix web run build green (tsc -b + vite build)
- All grep gates above pass (union widened, reasons plumbed, six hooks with exact keys, bar re-keyed with exactly 2 source === "global" sites)
- Manual smoke (deferred to end-of-phase UAT): a live global agent session (spawned via curl POST /api/sessions {scope:"global", kind:"agent"} against a running server) renders in the bar and clicking it targets /global
</verification>

<success_criteria>
- GINT-01 wire half complete: source union widened, bar re-keyed by sessionId, /global navigation + pathname highlight shipped
- GVIEW-01 data half complete: ["sessions","global"] hook set + shared ["global"] config query ready for 16-02/16-03 consumption
- Zero new packages; zero backend edits; zero TaskPage/TaskTabs/TerminalPane edits
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / File | Kind |
|---------------|------|
| web/src/api/global.ts | NEW file |
| GlobalConfig (interface) | export, global.ts |
| useGlobal() | export hook, global.ts — queryKey ["global"] |
| useSaveGlobal() | export hook, global.ts — PUT + setQueryData |
| useGlobalSessions() | export hook, sessions.ts — queryKey ["sessions","global"] |
| useSpawnGlobalSession() / useSpawnGlobalAgent() / useResumeGlobalAgent() / useReattachGlobalTmux() / useRenameGlobalSession() | export hooks, sessions.ts |
| TermSession.global?: boolean | field, sessions.ts |
| AgentStatusEntry.source += "global" | union widening, agents.ts |
| ApiError.reasons?: {kind: string; target: string}[] | field, client.ts |

<output>
Create `.planning/phases/16-global-view-settings-bar-integration/16-01-SUMMARY.md` when done
</output>
