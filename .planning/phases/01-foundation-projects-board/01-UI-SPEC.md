---
phase: 1
slug: foundation-projects-board
status: draft
shadcn_initialized: false
preset: zinc-dark (init during Phase 1 execution — no frontend scaffold exists yet)
created: 2026-06-10
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Sources: CONTEXT.md decisions D-01..D-11 (locked), RESEARCH.md Standard Stack + Pattern 8 (frontend shell), Claude's discretion areas resolved below. No user questions were needed — upstream artifacts answered every contract category.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (CLI latest) — init during execution: `npx shadcn@latest init` with base color **zinc**, CSS variables **yes** (source: RESEARCH.md install steps; project is greenfield, no scaffold exists to init against today) |
| Preset | zinc base, dark-only: `class="dark"` hardcoded on `<html>` in index.html, no theme toggle (D-01) |
| Component library | Radix (via shadcn/ui) |
| Icon library | lucide-react (shadcn default) — 16px icons in dense contexts, 20px in empty states |
| Font | UI: system-ui sans stack (`ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif`). Mono: `ui-monospace, "SF Mono", "Cascadia Mono", Menlo, Consolas, monospace` for repo paths (and Phase 2+ terminals). No webfont download — local-only single binary stays offline-clean |
| Radius | `--radius: 0.375rem` (RESEARCH.md dense-aesthetic recommendation) |
| Tailwind | v4.3.0 CSS-first — tokens below live in `@theme` / CSS variables in `src/index.css`. NO tailwind.config.js |

shadcn components for this phase (from RESEARCH.md, locked): `button, dialog, alert-dialog, input, textarea, tabs, sidebar, dropdown-menu, tooltip, separator, skeleton`. TaskCard, Column, QuickAdd are custom components styled with the tokens below.

---

## Spacing Scale

Declared values (all multiples of 4). Dense developer-tool density (D-02): default to the small end; 24px+ is for page chrome only, never inside cards.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, badge padding, gap between card title and meta |
| sm | 8px | Card internal padding (vertical), gap between cards in a column, inline control gaps |
| md | 12px | Card internal padding (horizontal), column internal padding, sidebar item padding |
| lg | 16px | Gap between board columns, task view content padding, dialog internal padding |
| xl | 24px | Page header padding, gap between task view tab strip and content |
| 2xl | 32px | Empty state vertical padding |
| 3xl | 48px | Centered empty state container offset (no-projects state) |

Exceptions: `md: 12px` sits off the 8-point progression and is declared as a justified exception — the locked dense developer-tool aesthetic (D-02) needs an intermediate step between 8px and 16px for card horizontal padding and sidebar/column internals; jumping straight to 16px reads airy, 8px reads cramped. It is still a multiple of 4 and the only off-progression token. (No touch-target exception — desktop-first localhost tool; minimum interactive hit area is 28px height for dense rows, achieved via padding within the scale.)

---

## Typography

Exactly 4 sizes, 2 weights. Body is 14px (`text-sm` on `<body>` per RESEARCH.md Pattern 8 dense nudge).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 500 | 1.4 — column headers (uppercase, tracking-wide, muted), card meta, sidebar section labels, badge text |
| Heading | 16px | 500 | 1.2 — task view title, dialog titles, card titles use 14px/500 (Body size, Label weight) |
| Display | 20px | 500 | 1.2 — empty state headings only; no marketing-scale type in this product |

Rules:
- Weights are 400 and 500 ONLY. No bold (600/700) anywhere in app chrome; rendered markdown `<strong>` may use the prose plugin's defaults.
- Markdown prose: `prose prose-invert prose-sm max-w-none` (@tailwindcss/typography via `@plugin` in CSS) — D-10 preview mode.
- Repo paths render in the mono stack at `text-sm font-mono` (14px, preferred — clipped with `truncate`) or `text-xs font-mono` (12px in dense meta contexts). No other mono sizes — both map onto the declared 4-size scale.

---

## Color

Dark-only (D-01), neutral-dominant (D-03). Values are Tailwind zinc ramp (shadcn zinc base maps to these); executor uses the shadcn CSS variables, listed hex are the contract targets.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#09090b` (zinc-950) | App background, board canvas, task view background |
| Secondary (30%) | `#18181b` (zinc-900) surfaces; `#27272a` (zinc-800) borders/separators; columns are `#101013`-range (zinc-925-ish via `--muted` tuned darker than cards) | Sidebar, cards, dialogs, tab strip, column wells. Columns are NOT individually colored (D-03) — all four identical |
| Accent (10%) | `#3b82f6` (blue-500) | See reserved list below — nothing else |
| Destructive | `#ef4444` (red-500) | Delete confirmations and destructive menu items only |

Text: primary `#fafafa` (zinc-50), secondary/muted `#a1a1aa` (zinc-400), disabled `#52525b` (zinc-600).

Accent reserved for:
1. Focus-visible rings (keyboard navigation)
2. Active tab indicator in the task view tab strip (D-06)
3. Drag-over column highlight (border/ring on the drop-target column during drag)
4. Links inside rendered markdown (prose `a`)

Explicitly NOT accent: primary buttons, selected sidebar item, hover states. Primary CTA buttons use shadcn dark-primary inversion: zinc-50 background, zinc-900 text — high-contrast neutral, keeping color budget free for Phase 4 status badges (D-03). Selected sidebar item = `#27272a` background + zinc-50 text. Hover states = one step lighter surface (`#27272a` on cards), never color.

Status-badge palette is deliberately UNSPECIFIED — arrives in Phase 4's UI-SPEC; this phase must not consume colors that compete with it.

---

## Copywriting Contract

Tone: terse, lowercase-calm developer-tool voice. Sentence case everywhere (never Title Case in body copy), no exclamation marks, no "please".

| Element | Copy |
|---------|------|
| Primary CTA (board) | Button: `New task` — opens full dialog (D-08); dialog submit: `Create task`, cancel: `Cancel` |
| Quick-add (To Do column) | Trigger row: `+ New task`; placeholder: `Task title`; Enter commits, Esc cancels (D-08) |
| Sidebar CTA | Button: `Add project`; dialog title: `Add project`; fields: `Repository path` (mono input, placeholder `/home/you/code/my-repo`), `Name` (placeholder `Defaults to folder name`); submit: `Add project` |
| Empty state — no projects | Heading: `No projects yet`. Body: `Point Kangent at a local git repository to get a board.` CTA: `Add project` (centered, Display size heading) |
| Empty state — empty column | No text. Empty columns render the bare column well (it is a live drop target); To Do keeps its quick-add row. Unobtrusive per CONTEXT discretion note |
| Empty state — no description | Muted body text: `No description.` next to the `Edit` button in the Description tab |
| Error — invalid repo path | Inline under the path field, destructive-colored 12px text: `Not a git repository: {path}` / `Path must be absolute` / `Not a directory: {path}` (mirror server messages verbatim). Field keeps its value for correction |
| Error — duplicate project | `This repository is already added.` |
| Error — board load failure | Centered in board area: `Couldn't load tasks.` + `Retry loading` button (refetch query) |
| Error — save failure (task edit) | Inline near the `Save description` button: `Couldn't save. Try again.` — draft text is never cleared |
| Destructive — delete task (D-11) | AlertDialog title: `Delete task?` Body: `"{title}" will be permanently deleted. This can't be undone.` Buttons: `Cancel` / `Delete task` (destructive variant) |
| Destructive — delete project | AlertDialog title: `Delete project?` Body: `"{name}" and all its tasks will be removed from Kangent. The repository on disk is untouched.` Buttons: `Cancel` / `Delete project` (destructive variant) |
| Markdown editor toggle (D-10) | View mode: `Edit` button. Edit mode: `Save description` (primary) / `Cancel` (ghost) |
| Back affordance (D-07) | Icon button (arrow-left) + tooltip `Back to board (Esc)` at top of task view |

Column headers (fixed, TASK-02): `To Do` / `In Progress` / `In Review` / `Done` — rendered as 12px/500 uppercase muted labels with a muted task count (e.g. `TO DO 3`).

---

## Interaction Contract

States the executor must implement; the auditor will check these.

| Interaction | Contract |
|-------------|----------|
| Card click vs drag | Click navigates to `/projects/:pid/tasks/:tid` (D-05); drag requires 5px pointer movement (`activationConstraint: { distance: 5 }` — RESEARCH.md Pitfall 4) |
| Drag feedback | Dragged card renders in `DragOverlay` with subtle scale (1.02) + shadow; origin slot shows the card at 40% opacity; drop-target column gets the accent ring (Color §3) |
| Drop persistence | Optimistic reorder, rollback on error (RESEARCH.md Pattern 7); no spinners during drag |
| New task placement | Quick-add and dialog tasks land at top of To Do (D-09) |
| Sidebar collapse (D-04) | shadcn Sidebar collapsible `icon` or `offcanvas` mode; toggle button in header with tooltip `Toggle sidebar (Ctrl+B)` and `aria-label="Toggle sidebar"`; state persists in localStorage |
| Tab strip (D-06) | shadcn Tabs rendered from a typed array — Phase 1 array is `[{id:"description", label:"Description"}]`; the single tab still renders as a visible tab strip (the seam must be visible, not hidden) |
| Esc navigation (D-07) | Esc on task page navigates to `/projects/:pid` — suppressed when focus is in input/textarea/contenteditable or a dialog is open (RESEARCH.md Pitfall 9) |
| Markdown editing (D-10) | Explicit Edit → textarea (mono NOT required; plain sans textarea, min-height 240px) → Save description/Cancel; no autosave, no side-by-side preview |
| Hover | Cards and sidebar items: surface lightens one step (`#27272a`); cursor-grab only while pointer is over the card drag area |
| Focus | All interactive elements get the accent focus-visible ring; keyboard drag via dnd-kit KeyboardSensor is wired |
| Loading | Board initial load: 3 skeleton cards per column (shadcn skeleton); no full-page spinners |

### Keyboard Shortcuts (discretion — resolved)

| Key | Action | Scope |
|-----|--------|-------|
| `Esc` | Back to board (D-07, locked) | Task view, outside inputs/dialogs |
| `Ctrl/Cmd+B` | Toggle sidebar | Global (ships free with shadcn Sidebar) |
| `n` | Open New Task dialog | Board page, outside inputs/dialogs |

No further shortcuts in Phase 1 — keep cheap and obvious per CONTEXT.

---

## Layout Contract

| Region | Spec |
|--------|------|
| App shell | Sidebar (left, collapsible) + main area; sidebar visible on task route too (D-05) |
| Sidebar | Expanded width 240px; project list rows 28px min-height, md horizontal padding; app name/logo top, `Add project` action at list bottom or header `+` |
| Board | Horizontal row of 4 equal columns, lg (16px) gaps, columns scroll vertically independently; board area scrolls horizontally if viewport is narrow; column min-width 260px |
| Task card | Secondary surface, sm/md padding, radius token, 1px zinc-800 border; title 14px/500 up to 2 lines (`line-clamp-2`); no description preview on cards (dense) |
| Task view | Header row (back button + title + actions incl. delete in a dropdown-menu) → tab strip → tab content, max-width 860px for prose comfort, lg padding |
| Dialogs | shadcn Dialog defaults, max-width 480px (New Task: 560px for the description textarea) |

Focal point: on the board screen the `New task` button is the only high-contrast inverted element (zinc-50 on zinc-900) and is the visual anchor of the page header — everything else on the page is neutral surfaces and muted text, so the eye lands there first.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, dialog, alert-dialog, input, textarea, tabs, sidebar, dropdown-menu, tooltip, separator, skeleton | not required |

Third-party registries: **none declared**. Vetting gate: not applicable (verified — no third-party blocks in this contract, 2026-06-10).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
