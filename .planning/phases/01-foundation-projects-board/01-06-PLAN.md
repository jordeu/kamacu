---
phase: 01-foundation-projects-board
plan: 06
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - web/src/pages/TaskPage.tsx
  - web/src/components/task/TaskTabs.tsx
  - web/src/components/task/DescriptionTab.tsx
  - web/src/components/task/DeleteTaskDialog.tsx
autonomous: true
requirements: [TASK-04, TASK-05]
must_haves:
  truths:
    - "Clicking a task opens a full-page route /projects/:pid/tasks/:tid with the sidebar still visible; the URL is deep-linkable (direct browser hit works without board cache) (TASK-05, D-05)"
    - "The task view shows a visible tab strip rendered from a typed array containing only the Description tab (D-06 — the architectural seam)"
    - "Description renders as markdown (GFM) in view mode; an explicit Edit button swaps to a textarea with Save description / Cancel; no autosave (D-10)"
    - "Task title can be edited and the task deleted behind a confirmation dialog; delete navigates back to the board (TASK-04, D-11)"
    - "Esc returns to the board except while typing in inputs/textarea or with a dialog open; back button and browser back also work (D-07)"
  artifacts:
    - path: "web/src/pages/TaskPage.tsx"
      provides: "full-page task view: header (back, title, actions), tab strip, Esc handling"
      min_lines: 60
    - path: "web/src/components/task/TaskTabs.tsx"
      provides: "tab strip driven by a typed TabDef[] array — Phase 2+ appends to it"
      contains: "description"
    - path: "web/src/components/task/DescriptionTab.tsx"
      provides: "markdown view/edit toggle with react-markdown + remark-gfm"
      contains: "remarkGfm"
    - path: "web/src/components/task/DeleteTaskDialog.tsx"
      provides: "AlertDialog with exact destructive copy"
      contains: "Delete task?"
  key_links:
    - from: "web/src/pages/TaskPage.tsx"
      to: "useTask(taskId) query"
      via: "fetch by id so deep links work without board cache"
      pattern: "useTask\\("
    - from: "web/src/components/task/DescriptionTab.tsx"
      to: "useUpdateTask mutation"
      via: "Save description → PATCH {description}"
      pattern: "useUpdateTask"
    - from: "web/src/pages/TaskPage.tsx"
      to: "/projects/:projectId board route"
      via: "back button + guarded Esc navigate (not navigate(-1))"
      pattern: "navigate\\("
---

<objective>
Build the expanded task view: full-page route with header (back, editable title, delete), the extensible tab strip with the Description tab, and the markdown edit/preview toggle.

Purpose: Implements TASK-04 (edit/delete) and TASK-05 (expanded view). The tab strip is the architectural seam every later phase extends (Agent, Bash, Diff tabs) — it must be a real container driven by data, and visibly rendered even with one tab.
Output: Deep-linkable task page with working markdown description editing and confirmed deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-projects-board/01-CONTEXT.md
@.planning/phases/01-foundation-projects-board/01-RESEARCH.md
@.planning/phases/01-foundation-projects-board/01-UI-SPEC.md
@web/src/api/types.ts
@web/src/api/queries.ts
@web/src/api/mutations.ts

<interfaces>
From plan 01-03 (use directly):
```ts
// queries.ts
useTask(taskId: number): UseQueryResult<Task>   // key ["task", taskId] — GET /api/tasks/{id}, works without board cache
// mutations.ts
useUpdateTask(projectId)   // PATCH /api/tasks/{id} {title?, description?} → invalidates ["tasks", projectId] and ["task", id]
useDeleteTask(projectId)   // DELETE /api/tasks/{id} → invalidates ["tasks", projectId]
```
Markdown stack (installed in 01-03): `react-markdown` v10 + `remark-gfm`. Do NOT add rehype-raw (raw HTML stays unrendered — that is the XSS posture). Typography plugin loaded via CSS; prose classes: `prose prose-invert prose-sm max-w-none`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskPage — header, tab strip container, Esc navigation</name>
  <files>web/src/pages/TaskPage.tsx, web/src/components/task/TaskTabs.tsx</files>
  <read_first>
    - web/src/pages/TaskPage.tsx (stub from plan 01-03 — replace body, keep param reading)
    - web/src/components/ui/tabs.tsx (generated shadcn Tabs API: Tabs, TabsList, TabsTrigger, TabsContent)
    - .planning/phases/01-foundation-projects-board/01-UI-SPEC.md (Layout Contract task view row; Interaction Contract tab strip + Esc rows; Copywriting back affordance)
    - .planning/phases/01-foundation-projects-board/01-RESEARCH.md (Pattern 8: Esc guard details, Pitfall 9)
  </read_first>
  <action>
    `TaskPage.tsx`:
    - Read `projectId` and `taskId` params; `useTask(taskId)` (NOT the board list — deep links must work cold, TASK-05). Loading: skeleton lines. Not found / error: muted `Task not found.` + a `Back to board` link.
    - Header row: back icon button (lucide `ArrowLeft`, 16px) with shadcn Tooltip `Back to board (Esc)`, navigating to `/projects/${projectId}` — explicit route, NOT `navigate(-1)` (deep-linked tabs have no history, D-07). Title: 16px/500 (`text-base font-medium`), click-to-edit: clicking the title (or an edit pencil) swaps to an Input; Enter or blur saves via `useUpdateTask(projectId).mutate({ id, title })`; trimmed-empty reverts without saving (TASK-04 title edit). Right side: dropdown-menu (ellipsis icon) with destructive item `Delete task` opening DeleteTaskDialog (Task 2 — import; create a stub file first if needed to keep build green).
    - Content: `max-w-[860px]` with 16px padding, 24px gap between tab strip and content (UI-SPEC).
    - Esc handler (D-07 + Pitfall 9): page-scoped keydown; if `document.activeElement` (or e.target) is input/textarea/contenteditable, or any Radix dialog is open (check `document.querySelector('[role="dialog"]')` or rely on Radix Esc stopPropagation + a `[data-state="open"]` check), do nothing; otherwise navigate to `/projects/${projectId}`. Browser back keeps working (plain routing).

    `TaskTabs.tsx` (D-06 — the seam):
    ```tsx
    export interface TabDef { id: string; label: string; content: React.ReactNode }
    export function TaskTabs({ tabs }: { tabs: TabDef[] })
    ```
    — shadcn Tabs with `defaultValue={tabs[0].id}`, TabsList mapping over the array, TabsContent per tab. The strip RENDERS VISIBLY even with a single tab (the seam must be visible, UI-SPEC). Active tab indicator uses the accent color (one of the four sanctioned accent uses). TaskPage calls it with `[{ id: "description", label: "Description", content: <DescriptionTab .../> }]`.
  </action>
  <verify>
    <automated>cd web && npm run build && grep -q 'TabDef' src/components/task/TaskTabs.tsx && grep -q 'Back to board (Esc)' src/pages/TaskPage.tsx && grep -q 'useTask(' src/pages/TaskPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `TaskTabs.tsx` exports a `TabDef` interface and maps `tabs` to TabsTrigger/TabsContent (no hardcoded single TabsTrigger)
    - `TaskPage.tsx` contains `useTask(`, the tooltip string `Back to board (Esc)`, `max-w-[860px]`, and a keydown handler checking for INPUT/TEXTAREA/contenteditable before navigating
    - `TaskPage.tsx` contains `useUpdateTask` (title editing) and no `navigate(-1)`
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>Task view is a deep-linkable full page with editable title, a data-driven visible tab strip, and guarded Esc/back navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Description tab (markdown edit/preview) + delete confirmation</name>
  <files>web/src/components/task/DescriptionTab.tsx, web/src/components/task/DeleteTaskDialog.tsx</files>
  <read_first>
    - web/src/pages/TaskPage.tsx (Task 1 integration: tab content slot + dropdown delete item)
    - web/src/api/mutations.ts (useUpdateTask / useDeleteTask signatures)
    - .planning/phases/01-foundation-projects-board/01-UI-SPEC.md (Copywriting: markdown editor toggle, no-description empty state, save failure, Destructive — delete task; Interaction Contract markdown row)
  </read_first>
  <action>
    `DescriptionTab.tsx` (D-10 — explicit toggle, no autosave, no side-by-side):
    - View mode: if description non-empty, render
      ```tsx
      <div className="prose prose-invert prose-sm max-w-none">
        <ReactMarkdown remarkPlugins={[remarkGfm]}>{task.description}</ReactMarkdown>
      </div>
      ```
      plus an `Edit` button (ghost). If empty: muted text `No description.` next to the `Edit` button.
    - Edit mode: plain shadcn Textarea (sans, NOT mono), `min-h-[240px]`, initialized with current description; buttons `Save description` (primary) and `Cancel` (ghost). Save → `useUpdateTask(projectId).mutateAsync({ id, description: draft })`; on success return to view mode. On error: inline text near the Save button, exact copy `Couldn't save. Try again.` — the draft is NEVER cleared (UI-SPEC). Cancel discards the draft and returns to view mode.
    - Do NOT add rehype-raw or any HTML sanitizer config — react-markdown's defaults are the security posture (raw HTML omitted, javascript: URLs neutralized).
    - Esc inside the textarea must not navigate (the TaskPage guard covers it; ensure the textarea doesn't call stopPropagation in a way that breaks Radix dialogs).

    `DeleteTaskDialog.tsx` (D-11 — hard delete behind confirm):
    - shadcn AlertDialog. Title: `Delete task?`. Body: `"{title}" will be permanently deleted. This can't be undone.` Buttons: `Cancel` / `Delete task` (destructive variant).
    - Confirm → `useDeleteTask(projectId).mutateAsync(taskId)` then `navigate('/projects/' + projectId)`.
    - Wire into TaskPage's dropdown menu (Task 1 slot).
  </action>
  <verify>
    <automated>cd web && npm run build && grep -q 'remarkGfm' src/components/task/DescriptionTab.tsx && grep -q 'prose prose-invert prose-sm' src/components/task/DescriptionTab.tsx && grep -q 'Save description' src/components/task/DescriptionTab.tsx && grep -q "permanently deleted" src/components/task/DeleteTaskDialog.tsx && ! grep -rq 'rehype-raw' src/</automated>
  </verify>
  <acceptance_criteria>
    - `DescriptionTab.tsx` contains `ReactMarkdown`, `remarkGfm`, `prose prose-invert prose-sm`, exact strings `Edit`, `Save description`, `Cancel`, `No description.`, and `Couldn't save. Try again.`
    - `DescriptionTab.tsx` contains `min-h-[240px]` and no autosave (no debounce/interval/onChange-triggered mutate)
    - `DeleteTaskDialog.tsx` contains exact strings `Delete task?` and `will be permanently deleted. This can't be undone.` and uses AlertDialog with `useDeleteTask`
    - `rehype-raw` appears nowhere under web/src and is not in web/package.json
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>Markdown descriptions render safely with GFM, edit via explicit toggle with draft-preserving error handling, and tasks hard-delete behind the spec'd confirmation.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` green
- With backend running: open a task by clicking a card AND by pasting its URL into a fresh tab (deep link); edit title inline; edit description with GFM (table, task list) and see it rendered; Esc from the page returns to board, Esc inside the textarea does not; delete task → confirm dialog → lands on board with the card gone
</verification>

<success_criteria>
- TASK-05: expanded, URL-addressable task view with visible single-tab strip (the D-06 seam)
- TASK-04: title/description editing and confirmed hard deletion
- D-05/D-07/D-10/D-11 honored with UI-SPEC copy verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-projects-board/01-06-SUMMARY.md`
</output>
