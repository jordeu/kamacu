---
phase: 01-foundation-projects-board
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - web/src/components/layout/AppLayout.tsx
  - web/src/components/sidebar/ProjectSidebar.tsx
  - web/src/components/sidebar/AddProjectDialog.tsx
  - web/src/components/sidebar/ProjectMenu.tsx
  - web/src/components/sidebar/RenameProjectDialog.tsx
  - web/src/App.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03]
must_haves:
  truths:
    - "All projects appear in the left sidebar; clicking one navigates to its board route (PROJ-02)"
    - "User can add a project via a dialog with a mono repository-path input; server validation errors render inline under the field with the field value preserved (PROJ-01)"
    - "User can rename a project and delete it behind a confirmation dialog whose copy states the repo on disk is untouched (PROJ-03)"
    - "Sidebar collapses via trigger and Ctrl/Cmd+B, and the collapsed state persists across reloads (D-04)"
    - "With zero projects, a centered empty state shows 'No projects yet' with an Add project CTA"
  artifacts:
    - path: "web/src/components/sidebar/ProjectSidebar.tsx"
      provides: "Project list, selection state, add-project entry point, per-project menu"
      min_lines: 60
    - path: "web/src/components/sidebar/AddProjectDialog.tsx"
      provides: "Repository path + Name fields, inline server-error display"
      contains: "Repository path"
    - path: "web/src/components/sidebar/ProjectMenu.tsx"
      provides: "dropdown with Rename / Delete project, AlertDialog confirm"
      contains: "Delete project?"
  key_links:
    - from: "web/src/components/sidebar/ProjectSidebar.tsx"
      to: "/projects/:projectId route"
      via: "Link/NavLink per project row"
      pattern: "to=\\{?`?/projects/"
    - from: "web/src/components/sidebar/AddProjectDialog.tsx"
      to: "useCreateProject mutation"
      via: "onSubmit → mutateAsync, ApiError.message rendered inline"
      pattern: "useCreateProject"
    - from: "web/src/components/sidebar/ProjectMenu.tsx"
      to: "useDeleteProject mutation"
      via: "AlertDialog action → delete → navigate away if current project deleted"
      pattern: "useDeleteProject"
---

<objective>
Build the projects sidebar: collapsible shadcn Sidebar with the project list, add-project dialog (path validation UX), rename, delete-with-confirm, and the no-projects empty state — wired to the hooks from plan 01-03 and the API from plan 01-02.

Purpose: Implements PROJ-01/02/03 user-facing flows and D-04 (collapsible sidebar), following UI-SPEC copy verbatim.
Output: Working project management UI in the persistent app shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-projects-board/01-CONTEXT.md
@.planning/phases/01-foundation-projects-board/01-UI-SPEC.md
@web/src/api/types.ts
@web/src/api/queries.ts
@web/src/api/mutations.ts

<interfaces>
From plan 01-03 (already in the tree — use directly, do not re-create):
```ts
// web/src/api/types.ts
export interface Project { id: number; name: string; repo_path: string; created_at: string; updated_at: string; }
// web/src/api/client.ts
export class ApiError extends Error { status: number }
// web/src/api/queries.ts
useProjects(): UseQueryResult<Project[]>            // key ["projects"]
// web/src/api/mutations.ts
useCreateProject()  // POST {name?, repo_path}; throws ApiError with server message on 400/409
useRenameProject()  // PATCH {name}
useDeleteProject()  // DELETE
```
Server error messages to mirror verbatim (plan 01-02): `path must be absolute`, `not a directory: {path}`, `not a git repository: {path}`, `this repository is already added` (UI-SPEC maps the last one to "This repository is already added." — render ApiError.message with sentence-case first letter).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectSidebar — list, selection, collapse, empty state</name>
  <files>web/src/components/sidebar/ProjectSidebar.tsx, web/src/components/layout/AppLayout.tsx, web/src/App.tsx</files>
  <read_first>
    - web/src/components/layout/AppLayout.tsx (placeholder sidebar from plan 01-03 — replace its content with ProjectSidebar, keep SidebarProvider/Outlet structure)
    - web/src/components/ui/sidebar.tsx (generated shadcn API: SidebarProvider, Sidebar, SidebarHeader, SidebarContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarTrigger, useSidebar — compose these, don't hand-roll)
    - web/src/App.tsx (RedirectToFirstProject — extend its empty-projects branch)
    - .planning/phases/01-foundation-projects-board/01-UI-SPEC.md (Layout Contract: sidebar 240px, rows 28px min-height; Copywriting: empty state + sidebar CTA copy)
  </read_first>
  <action>
    Create `web/src/components/sidebar/ProjectSidebar.tsx`:
    - `useProjects()` for the list. Header: app name `kangent` (14px/500) + `SidebarTrigger` with tooltip `Toggle sidebar (Ctrl+B)` and `aria-label="Toggle sidebar"`.
    - One `SidebarMenuItem` per project: row min-height 28px (`min-h-7`), 12px horizontal padding (`px-3`), project name truncated. Click navigates via react-router `Link` to `/projects/{id}` (PROJ-02). Active project (match `useParams().projectId`) gets `bg-[#27272a] text-zinc-50` via the menu button's active state — selection is NEUTRAL, never accent (UI-SPEC color rules).
    - Hover: one step lighter surface, no color.
    - Footer (or list bottom): `Add project` button (ghost/secondary variant, NOT accent) opening AddProjectDialog (Task 2 — render the dialog here, import it; if executing this task first, stub the dialog component file with a no-op export so the build stays green).
    - Each row's trailing area: ProjectMenu trigger (Task 2) shown on hover/focus.
    - Collapse persistence (D-04): shadcn SidebarProvider already persists via cookie and binds Ctrl/Cmd+B; verify that works — if the generated component doesn't persist, add `open`/`onOpenChange` controlled state backed by `localStorage` key `kangent.sidebar`.

    Update `web/src/components/layout/AppLayout.tsx`: render `<ProjectSidebar />` inside the existing SidebarProvider; main area unchanged (`<Outlet />`).

    Update `RedirectToFirstProject` in `web/src/App.tsx` (empty-projects branch): render a centered empty state — Display heading (20px/500) `No projects yet`, body muted `Point Kangent at a local git repository to get a board.`, CTA `Add project` (the page-level CTA may be the one inverted/primary button) opening the same AddProjectDialog. Vertical padding 32px, centered container (UI-SPEC 2xl/3xl tokens).
  </action>
  <verify>
    <automated>cd web && npm run build && grep -q 'No projects yet' src/App.tsx && grep -q 'Toggle sidebar (Ctrl+B)' src/components/sidebar/ProjectSidebar.tsx && grep -q 'useProjects' src/components/sidebar/ProjectSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectSidebar.tsx` contains `useProjects`, a `Link`/navigate to `/projects/` per row, `aria-label="Toggle sidebar"`, and the string `Add project`
    - `App.tsx` contains the exact strings `No projects yet` and `Point Kangent at a local git repository to get a board.`
    - `AppLayout.tsx` contains `<ProjectSidebar` and still contains `<Outlet`
    - No accent/blue classes on project rows or the Add project button (grep: `ProjectSidebar.tsx` contains no `blue-500` and no `bg-primary` on row elements)
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>Sidebar lists projects, switches boards on click, collapses with persisted state, and shows the spec'd empty state when no projects exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add project dialog, rename, delete-with-confirm</name>
  <files>web/src/components/sidebar/AddProjectDialog.tsx, web/src/components/sidebar/ProjectMenu.tsx, web/src/components/sidebar/RenameProjectDialog.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectSidebar.tsx (integration points created in Task 1)
    - web/src/api/mutations.ts and web/src/api/client.ts (mutation signatures + ApiError shape)
    - .planning/phases/01-foundation-projects-board/01-UI-SPEC.md (Copywriting Contract rows: Sidebar CTA, Error — invalid repo path, Error — duplicate project, Destructive — delete project)
  </read_first>
  <action>
    `AddProjectDialog.tsx` (shadcn Dialog, max-width 480px):
    - Title: `Add project`. Fields per UI-SPEC: `Repository path` — Input with `font-mono` class, placeholder `/home/you/code/my-repo`; `Name` — Input, placeholder `Defaults to folder name`. Submit button: `Add project` (primary inverted variant), cancel: `Cancel`.
    - Submit → `useCreateProject().mutateAsync({ repo_path, name: name || undefined })`. On success: close dialog, reset fields, `navigate('/projects/' + project.id)`. On ApiError: render `error.message` (sentence-cased: uppercase first letter) as 12px destructive text inline UNDER the path field; KEEP the field values for correction (UI-SPEC). Duplicate (409) message renders as `This repository is already added.`
    - Disable submit while pending and when path is empty.

    `ProjectMenu.tsx` (shadcn DropdownMenu on each sidebar row, trigger: ellipsis icon button, 16px lucide icon):
    - Items: `Rename` (opens RenameProjectDialog) and `Delete project` (destructive-styled item, opens AlertDialog).
    - Delete AlertDialog copy EXACTLY per UI-SPEC: title `Delete project?`, body `"{name}" and all its tasks will be removed from Kangent. The repository on disk is untouched.`, buttons `Cancel` / `Delete project` (destructive variant). Confirm → `useDeleteProject().mutate(id)`; if the deleted project is the currently routed one, `navigate('/')` after success.

    `RenameProjectDialog.tsx`: small Dialog with a single `Name` input prefilled with the current name; submit `Save` → `useRenameProject().mutateAsync({ id, name })`; empty name disables submit; ApiError message inline.

    Wire both dialogs into ProjectSidebar rows / footer created in Task 1. Sentence case everywhere, no exclamation marks (UI-SPEC tone).
  </action>
  <verify>
    <automated>cd web && npm run build && grep -q 'Defaults to folder name' src/components/sidebar/AddProjectDialog.tsx && grep -q '/home/you/code/my-repo' src/components/sidebar/AddProjectDialog.tsx && grep -q 'Delete project?' src/components/sidebar/ProjectMenu.tsx && grep -q 'The repository on disk is untouched' src/components/sidebar/ProjectMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AddProjectDialog.tsx` contains `font-mono`, placeholder strings `/home/you/code/my-repo` and `Defaults to folder name`, `useCreateProject`, and an inline error element rendering the ApiError message (contains `error.message` or equivalent state variable)
    - `ProjectMenu.tsx` contains exact strings `Delete project?` and `The repository on disk is untouched.` and uses `AlertDialog`
    - `RenameProjectDialog.tsx` contains `useRenameProject`
    - Field state is not cleared on error (no `setPath("")` in the catch/error path)
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>Projects can be added with inline validation feedback, renamed, and deleted behind a confirmation that promises (and the backend guarantees) the repo on disk is untouched.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` green
- With backend running (plans 01-01/01-02): add a real repo path → project appears in sidebar and board route loads; add an invalid path → inline error, value preserved; rename reflects immediately; delete prompts with exact copy and removes the project; sidebar Ctrl+B collapse persists across reload
</verification>

<success_criteria>
- PROJ-01: create project via dialog with server-validated path and mirrored inline errors
- PROJ-02: sidebar lists all projects; clicking switches the board route
- PROJ-03: rename + confirmed hard delete; copy and behavior assert the repo is untouched
- D-04: collapsible sidebar with persisted state and Ctrl+B
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-projects-board/01-04-SUMMARY.md`
</output>
