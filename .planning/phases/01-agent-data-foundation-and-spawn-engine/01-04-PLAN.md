# 01-04: Threaded agent_id through the projects API (columns/scan/create-default/both INSERT paths/PATCH validation) mirroring the workspace_id pattern; 4 tests green, no project/workspace regression.

<objective>
Threaded agent_id through the projects API (columns/scan/create-default/both INSERT paths/PATCH validation) mirroring the workspace_id pattern; 4 tests green, no project/workspace regression..
</objective>

<tasks>
- [x] **T04**: Threaded agent_id through the projects API (columns/scan/create-default/both INSERT paths/PATCH validation) mirroring the workspace_id pattern; 4 tests green, no project/workspace regression. _(S)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
