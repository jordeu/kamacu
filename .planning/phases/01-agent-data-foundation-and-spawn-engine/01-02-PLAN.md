# 01-02: Added BackfillAgents startup hook (mirrors BackfillWorkspaces) + main.go wiring + idempotency test; verified the re-create path restores the Claude seed with engine/is_system markers.

<objective>
Added BackfillAgents startup hook (mirrors BackfillWorkspaces) + main.go wiring + idempotency test; verified the re-create path restores the Claude seed with engine/is_system markers..
</objective>

<tasks>
- [x] **T02**: Added BackfillAgents startup hook (mirrors BackfillWorkspaces) + main.go wiring + idempotency test; verified the re-create path restores the Claude seed with engine/is_system markers. _(S)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
