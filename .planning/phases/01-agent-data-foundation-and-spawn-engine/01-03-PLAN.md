# 01-03: Added /api/agents CRUD (list/create/update/delete/set-default) with is_system + in-use guards, mirroring the workspaces handler shape; 11 handler tests green.

<objective>
Added /api/agents CRUD (list/create/update/delete/set-default) with is_system + in-use guards, mirroring the workspaces handler shape; 11 handler tests green..
</objective>

<tasks>
- [x] **T03**: Added /api/agents CRUD (list/create/update/delete/set-default) with is_system + in-use guards, mirroring the workspaces handler shape; 11 handler tests green. _(M)_
</tasks>

<verification>
All tasks complete and tests pass.
</verification>
