---
phase: 25-workspace-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - internal/api/projects.go
  - internal/api/projects_test.go
autonomous: true
requirements: [WSDATA-01]

must_haves:
  truths:
    - "GET /api/projects returns a `workspace_id` integer field on every project — honors D-10 (WorkspaceID threaded through the Project struct, projectColumns const, and scanProject Scan at one matching slot)"
    - "POST /api/projects (folder path) resolves the default workspace and the created project carries workspace_id=1 — honors D-09 (both create INSERTs resolve WHERE is_default=1 and set workspace_id; the only write-path change in Phase 25)"
    - "POST create-by-repo path resolves the default workspace and the created project carries workspace_id=1"
    - "The NOT NULL workspace_id FK invariant continues to hold for newly-created projects (never workspace-less)"
  artifacts:
    - path: "internal/api/projects.go"
      provides: "WorkspaceID threaded through Project struct + projectColumns + scanProject, and default-workspace resolve+set in both create INSERTs"
      contains: "WorkspaceID"
    - path: "internal/api/projects_test.go"
      provides: "tests asserting workspace_id on the read wire and default-assignment on create"
      contains: "workspace_id"
  key_links:
    - from: "internal/api/projects.go create()/createByRepo()"
      to: "workspaces row where is_default = 1"
      via: "SELECT id FROM workspaces WHERE is_default = 1 before each INSERT"
      pattern: "is_default = 1"
    - from: "projectColumns const"
      to: "scanProject Scan(...) call"
      via: "workspace_id occupies the SAME slot (after icon_color, before created_at) in both"
      pattern: "icon_color, workspace_id, created_at"
---

<objective>
Thread `workspace_id` onto the projects **read** wire and make both project-create paths assign the default (Personal) workspace, so `GET/POST/PATCH /api/projects` all carry `workspace_id` for the Phase 26 switcher and every newly-created project satisfies the NOT NULL FK.

Purpose: Success criterion 4 of the phase — the projects API carries `workspace_id` on the wire, ready for Phase 26 to filter on (D-10) — plus the only Phase-25 write-path change: creation resolves and sets the default workspace (D-09) so the NOT NULL FK invariant holds without relying solely on the column DEFAULT.
Output: Four coordinated edits in `internal/api/projects.go` (struct, const, scanProject, two INSERTs) + tests in `internal/api/projects_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-workspace-data-foundation/25-CONTEXT.md
@.planning/phases/25-workspace-data-foundation/25-RESEARCH.md
@.planning/phases/25-workspace-data-foundation/25-PATTERNS.md

<interfaces>
<!-- Copy-ready code: RESEARCH.md §2 (wire) and §2b (create-path). PATTERNS.md §projects.go has the 4 exact edit sites with current line numbers. -->
<!-- CURRENT state to modify (from repo, verified): -->
<!--   Project struct tail (projects.go ~44): IconLetters, IconColor, CreatedAt, UpdatedAt -->
<!--     => insert `WorkspaceID int64 `json:"workspace_id"`` after IconColor, before CreatedAt -->
<!--   projectColumns (projects.go:90): -->
<!--     `id, name, repo_path, description, github_repo, managed, icon_letters, icon_color, created_at, updated_at` -->
<!--     => becomes `... icon_letters, icon_color, workspace_id, created_at, updated_at` -->
<!--   scanProject Scan (projects.go ~103): Scan(&p.ID, &p.Name, &p.RepoPath, &p.Description, &repo, &managedInt, &p.IconLetters, &p.IconColor, &p.CreatedAt, &p.UpdatedAt) -->
<!--     => insert `&p.WorkspaceID` after &p.IconColor, before &p.CreatedAt (SAME slot as the const) -->
<!--   create() folder INSERT (projects.go ~184): INSERT INTO projects (name, repo_path, icon_letters, icon_color) VALUES (?, ?, ?, ?) RETURNING `+projectColumns -->
<!--   createByRepo() INSERT (projects.go ~277): INSERT INTO projects (name, repo_path, github_repo, managed, description, icon_letters, icon_color) VALUES (?, ?, ?, 1, ?, ?, ?) RETURNING `+projectColumns (note the literal 1 for managed) -->
<!-- The struct doc-comment (~lines 38-42) states the column-ORDER contract "between managed and the timestamps" — extend it to mention workspace_id. -->
<!-- Test harness: projects_test.go ~431 helper does store.Open + store.Migrate then builds projectHandlers; model wire test on TestProjectList (~923), create tests on TestProjectCreate*/TestCreateRepoSuccess. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread workspace_id onto the projects read wire</name>
  <files>internal/api/projects.go, internal/api/projects_test.go</files>
  <behavior>
    - GET /api/projects (list handler) returns each project with a `workspace_id` field; for a project migrated/created under 00012 it equals 1 (Personal).
    - `scanProject` maps the `workspace_id` column into `Project.WorkspaceID` (int64) — build must have exactly matching column/Scan-arg counts (Pitfall 4).
    - The JSON tag is exactly `json:"workspace_id"`.
  </behavior>
  <read_first>
    - internal/api/projects.go (the Project struct ~26-47, projectColumns const line 90, scanProject ~92-109 — the four-site column-order contract)
    - .planning/phases/25-workspace-data-foundation/25-RESEARCH.md §2 (copy-ready struct/const/scan edit; note workspace_id is INTEGER NOT NULL → scan straight into int64, never sql.NullInt64)
    - .planning/phases/25-workspace-data-foundation/25-PATTERNS.md §projects.go (the exact current excerpts + Pitfall 4 warning)
    - internal/api/projects_test.go (TestProjectList ~923 and the migrated-DB handler harness ~431-438 to model the wire test)
  </read_first>
  <action>
    In `internal/api/projects.go`: (1) add field `WorkspaceID int64 \`json:"workspace_id"\`` to the `Project` struct, positioned AFTER `IconColor` and BEFORE `CreatedAt`; update the struct doc-comment that documents the "between managed and the timestamps" column-order contract to also name `workspace_id`. (2) In the `projectColumns` const (line 90) insert `workspace_id` between `icon_color` and `created_at` so it reads `... icon_letters, icon_color, workspace_id, created_at, updated_at`. (3) In `scanProject` (~line 103) insert `&p.WorkspaceID` into the `Scan(...)` call between `&p.IconColor` and `&p.CreatedAt` — the SAME slot as the const (Pitfall 4: a mismatch is a `go build` scan-arg error or a mis-mapped column). Do NOT touch the two INSERT statements in this task (Task 2 owns them) — the `RETURNING `+projectColumns already carries the new column back automatically. In `internal/api/projects_test.go`: add a test (model on TestProjectList + the migrated-DB harness) that lists projects through the handler and asserts the response JSON for a seeded project includes `"workspace_id"` equal to 1.
  </action>
  <acceptance_criteria>
    - `internal/api/projects.go` `projectColumns` string contains the exact substring `icon_color, workspace_id, created_at`.
    - `scanProject`'s `Scan(...)` call contains `&p.WorkspaceID` positioned between `&p.IconColor` and `&p.CreatedAt`.
    - The `Project` struct contains `WorkspaceID int64 \`json:"workspace_id"\``.
    - `go build ./...` exits 0 (proves column/Scan-arg counts line up).
    - The new list-wire test passes: `GET /api/projects` JSON for a seeded project includes `workspace_id` == 1.
    - `go test ./internal/api/ -run 'TestProjectList|TestProjectsWorkspaceWire' -v` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestProjectList|TestProjectsWorkspaceWire' -v</automated>
  </verify>
  <done>`workspace_id` is threaded through struct/const/scanProject in matching slots; GET /api/projects serializes `workspace_id` (==1 for migrated projects); build and the wire test pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default-to-Personal in both create INSERTs</name>
  <files>internal/api/projects.go, internal/api/projects_test.go</files>
  <behavior>
    - Creating a project via the folder path (POST /api/projects) returns a project whose `workspace_id` equals the default workspace (1).
    - Creating a project via the repo-first path (createByRepo) returns a project whose `workspace_id` equals the default workspace (1).
    - If the default-workspace resolve query fails, the handler responds 500 (does not silently create a workspace-less project).
  </behavior>
  <read_first>
    - internal/api/projects.go (create() folder INSERT ~184, createByRepo() INSERT ~277 — note the literal `1` for `managed` in createByRepo so placeholder counts stay correct; the writeError 500 idiom ~186-189)
    - .planning/phases/25-workspace-data-foundation/25-RESEARCH.md §2b (copy-ready: resolve `SELECT id FROM workspaces WHERE is_default = 1` into wsID before each INSERT, add the workspace_id column + `?` placeholder + wsID arg)
    - .planning/phases/25-workspace-data-foundation/25-PATTERNS.md §projects.go 4a/4b (the two INSERT edit sites + the errors.Is/writeError reject shape)
    - internal/api/projects_test.go (TestCreateRepoSuccess ~123 and TestProjectCreateValidRepo ~532 to model create tests against the migrated-DB harness)
  </read_first>
  <action>
    In `internal/api/projects.go`, in BOTH `create()` (folder path, ~line 184) and `createByRepo()` (~line 277): before the INSERT, resolve the default workspace with `h.db.QueryRow(\`SELECT id FROM workspaces WHERE is_default = 1\`).Scan(&wsID)` into a local `var wsID int64`; on error, `writeError(w, http.StatusInternalServerError, err.Error())` then `return` (the file's standard 500 reject shape). Then add `workspace_id` to the INSERT column list and a matching `?` placeholder, and append `wsID` as the last arg — for `create()` the columns become `(name, repo_path, icon_letters, icon_color, workspace_id)` with 5 placeholders and args `name, abs, deriveLetters(name), pickColor(), wsID`; for `createByRepo()` the columns become `(name, repo_path, github_repo, managed, description, icon_letters, icon_color, workspace_id)` keeping the literal `1` for `managed` in the VALUES list and adding a `?` for workspace_id, args `name, dest, canonical, desc, deriveLetters(name), pickColor(), wsID` (count placeholders carefully around the hardcoded `1`). Per D-09 this explicit resolve+set is required even though the column has `DEFAULT 1` — it is the documented interim behavior that Phase 26's WSPROJ-02 refines to the *active* workspace. In `internal/api/projects_test.go`: add tests asserting a project created via each path returns `workspace_id` == 1.
  </action>
  <acceptance_criteria>
    - `create()` and `createByRepo()` each contain a `SELECT id FROM workspaces WHERE is_default = 1` resolve before their INSERT.
    - Both INSERT statements list `workspace_id` as a column with a matching `?` placeholder and pass `wsID` as an arg.
    - No SQL string in these paths concatenates a name/id (parameterized `?` only).
    - `go build ./...` exits 0.
    - New create tests pass: a project created via the folder path AND via createByRepo each return `workspace_id` == 1.
    - `go test ./internal/api/ -run 'TestProjectCreate|TestCreateRepo|TestProjectsCreateAssignsDefaultWorkspace' -v` exits 0.
    - Full `go test ./internal/api/...` exits 0 (no regression; note the flaky TestSessionTmuxReattach may need a re-run in isolation per MEMORY).
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestProjectCreate|TestCreateRepo|TestProjectsCreateAssignsDefaultWorkspace' -v</automated>
  </verify>
  <done>Both create paths resolve `is_default = 1` and set `workspace_id`; created projects (folder + repo-first) carry workspace_id=1; resolve failure returns 500; build + create tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/projects handlers | project name/description are untrusted request input reaching SQL in the create paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering (injection) | `create()` / `createByRepo()` INSERTs + `is_default` resolve | mitigate | All values passed as `?` placeholders (existing projects.go norm); the `SELECT id FROM workspaces WHERE is_default = 1` resolve has no interpolated input. |
| T-25-04 | Tampering (invariant break) | new-project creation under the NOT NULL FK | mitigate | Explicit resolve+set of `workspace_id` (D-09) plus the column `DEFAULT 1` backstop guarantees a created project is never workspace-less; tests assert workspace_id==1 on both paths. |
| T-25-SC | Tampering (supply chain) | package installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `go build ./...` exits 0.
- `go vet ./internal/api/...` exits 0.
- Wire test: `GET /api/projects` JSON includes `workspace_id` (==1 for migrated projects).
- Create tests: folder-path and repo-first created projects return `workspace_id` == 1.
- `go test ./internal/api/...` stays green (re-run TestSessionTmuxReattach in isolation if it flakes under full load — MEMORY note, unrelated to this phase).
</verification>

<success_criteria>
- `workspace_id` ships on the projects read wire (GET/POST/PATCH via `RETURNING `+projectColumns) — success criterion 4.
- Both create paths resolve the default workspace (`is_default = 1`) and set `workspace_id`, keeping the NOT NULL FK invariant (D-09).
- Covers WSDATA-01 (every project belongs to exactly one workspace; the read wire surfaces it).
- No new workspace endpoints added (D-11 honored — all workspace CRUD is Phase 26).
</success_criteria>

<output>
Create `.planning/phases/25-workspace-data-foundation/25-02-SUMMARY.md` when done.
</output>
