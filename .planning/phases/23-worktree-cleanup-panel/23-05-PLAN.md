---
phase: 23-worktree-cleanup-panel
plan: 05
type: execute
wave: 4
depends_on: ["23-03"]
files_modified: []
autonomous: false
requirements: [WTREE-01, WTREE-02, WTREE-03, WTREE-04]
user_setup: []

must_haves:
  truths:
    - "The full build (go build + web build) is green and the panel renders in Settings against the live install"
    - "Every worktree across all projects is listed grouped by project with classification + flags (WTREE-01)"
    - "Orphaned worktrees are listed with an Orphan chip and are removable (WTREE-04)"
    - "A force-remove of a normal dirty/unpushed worktree works behind the confirm and keeps the branch (WTREE-02)"
    - "A force-remove of a foreign-uid-blocked worktree (the sched ./.db case) degrades to the blocked banner with a copyable sudo hint, not a 500 (D-01)"
    - "Bulk Clean eligible previews the exact safe set and removes it, never forcing (WTREE-03)"
  artifacts: []
  key_links: []
---

<objective>
End-of-phase verification gate: prove WTREE-01..04 and the load-bearing D-01
blocked outcome against a real Kamacu install with real worktrees (including the
`sched` project's foreign-uid `./.db` stragglers Phase 21 deliberately left for
this panel). This is the human-verify checkpoint — Claude runs the full build and
guides the user through the runtime checks; the user confirms the visual +
functional behavior no automated test can assert (there is no frontend test
framework, per STATE.md).

Purpose: The panel performs privileged-adjacent filesystem removal driven from a
localhost endpoint. The D-01 blocked path in particular can only be truly
verified against the real uid-70 `./.db` case; the milestone's core safety promise
is that it degrades to a copyable manual instruction, never a silent partial
failure or a raw 500.

Output: a verified, shippable Worktree Cleanup Panel; the checkpoint outcome
recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-worktree-cleanup-panel/23-CONTEXT.md
@.planning/phases/23-worktree-cleanup-panel/23-UI-SPEC.md
@.planning/phases/23-worktree-cleanup-panel/23-01-SUMMARY.md
@.planning/phases/23-worktree-cleanup-panel/23-02-SUMMARY.md
@.planning/phases/23-worktree-cleanup-panel/23-03-SUMMARY.md
@.planning/phases/23-worktree-cleanup-panel/23-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-phase build + vet + test gate</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/23-worktree-cleanup-panel/23-01-SUMMARY.md, 23-02-SUMMARY.md, 23-03-SUMMARY.md, 23-04-SUMMARY.md (what each plan delivered — confirm the full surface is in place)
    - .planning/STATE.md § "Verification model" and § "Pending Todos" (the ~18-20 pre-existing react-hooks lint advisories that are ALLOWED to remain — a new error is a failure, a pre-existing one is not)
  </read_first>
  <action>
    Run the complete backend + frontend gate and confirm all green (only pre-existing lint advisories allowed):
    - `go build ./...` → exit 0
    - `go vet ./...` → exit 0
    - `go test ./...` → exit 0 (all new + existing tests, incl. the D-04 regression DELETE tests)
    - `cd web && npm run build` (tsc -b + vite build) → exit 0
    - `cd web && npm run lint` → no NEW errors beyond the STATE-noted pre-existing react-hooks advisories

    If anything fails, STOP and report the exact failure — do not advance to the human-verify checkpoint on a red build. Note the `falcon-may-kill-kangent` memory: if `./bin/kamacu` gets silently SIGKILLed while starting the live instance for the next task, that is CrowdStrike Falcon, not a code bug — surface it and continue the verification against a manually-restarted binary.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./... && cd web && npm run build</automated>
  </verify>
  <done>`go build ./...`, `go vet ./...`, `go test ./...` all exit 0; `cd web && npm run build` exits 0; `npm run lint` shows no new errors. The full phase surface (parser, extended core, endpoints, hooks, badge, section, dialogs) is present and compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete Worktree Cleanup Panel: a new "Worktree cleanup" section in Settings that lists every git worktree across all projects (grouped by project) with classification (Referenced / Orphan / Stale pointer / Blocked) and dirty/unpushed/stash flags; a per-row force-remove behind a confirm; a D-01 blocked-row banner with a copyable `sudo rm -rf` hint; and a bulk "Clean eligible" preview→confirm. Backend: a `git worktree list --porcelain -z` parser, the extended shared removal core (orphan mode + blocked outcome), and the `GET /api/worktrees` + `POST /api/worktrees/{remove,clean-eligible,clear-pointer}` endpoints.
  </what-built>
  <how-to-verify>
    Start the app against your real install and open Settings:
    1. Build + run: `make build` then `./bin/kamacu` (if the process dies instantly, that's Falcon — restart it; see the memory note). Open the localhost URL, go to Settings, scroll to the "Worktree cleanup" section.
    2. WTREE-01 — LISTING: Confirm every worktree you expect appears, grouped by project, each with a classification badge, its task/PR association (`Task: …` or `PR #…`), and dirty/unpushed/stash chips where applicable. Confirm the main repo checkout of each project is NOT listed. Confirm the header count reads e.g. `N worktrees · K orphaned`.
    3. WTREE-04 — ORPHANS: Confirm the `sched` project's stale/orphaned worktree dirs (the ones Phase 21 left behind) appear as `Orphan` (or `Stale pointer` if the dir is gone), and that they have a `Remove` (orphan) or `Clear pointer` (stale) control.
    4. Refresh (D-10): Click the Refresh icon — it should spin while fetching and the list should update; confirm there is NO auto-refresh when you sit idle (no polling).
    5. WTREE-02 — NORMAL FORCE-REMOVE: Pick a disposable dirty or unpushed worktree, click `Remove`, confirm the dialog NAMES what will be destroyed (uncommitted/unpushed/stash counts, running sessions) and shows `The branch <name> is kept.`; if dirty, confirm the type-the-dirname gate is required; complete it and confirm the worktree is removed, the branch still exists (`git branch --list` in the repo), and the list refreshes.
    6. D-01 — BLOCKED CASE (the load-bearing check): Point at the `sched` project's worktree that contains the uid-70 mode-700 `./.db` Postgres dir (or reproduce one). Click `Remove` and confirm. Expect: the row degrades to a `Blocked — needs manual removal` banner naming the offending path with a copyable `sudo rm -rf <path>` hint — NOT a raw error toast, NOT a 500, NOT a partially-deregistered "vanished then reappeared" row. Click the Copy button and confirm it copies the exact command (the app must NOT run it). Optionally run the copied command yourself, then Refresh and confirm the row is gone.
    7. WTREE-03 — BULK CLEAN: Click `Clean eligible`. Confirm the preview lists the EXACT set (orphans + done/merged-pristine worktrees) with a why-eligible label each, and the reassurance line about nothing being forced. Confirm; confirm only those are removed, anything dirty/blocked is left behind, and the list refreshes.

    Reply `approved` if all six behaviors hold, or describe what is off (which requirement, which row, exact symptom).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live localhost endpoint → filesystem | This is the first real exercise of the panel deleting real worktrees; the human-verify step confirms the D-01 blocked path does NOT silently partially-remove or 500. |
| rendered sudo hint → user shell | The user (not the app) runs the copied `sudo rm -rf`; the checkpoint confirms the app only rendered it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-16 | Denial of Service (undeletable shell) | live D-01 blocked path | mitigate | The human-verify step explicitly checks the blocked worktree degrades to the banner with the git registration + row intact (retryable after manual `sudo rm`), never a partial deregister or 500. This is the one behavior no automated test can fully assert against the real uid-70 case. |
| T-23-17 | Elevation | live sudo hint | mitigate | The checkpoint confirms the Copy button copies text and the app never executes it (D-01, Security V5). |
| T-23-18 | Tampering | live force-remove | mitigate | The checkpoint confirms force-remove keeps the branch (`git branch --list`) and names destruction before committing (D-02/D-34, WTREE-02). |
| T-23-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./...` exit 0.
- `cd web && npm run build` exits 0; `npm run lint` has no new errors.
- The human-verify checkpoint returns `approved` covering all six behaviors (WTREE-01/02/03/04 + D-01 blocked + no-poll Refresh).
</verification>

<success_criteria>
- The panel is proven against a real install: listing (WTREE-01), orphans (WTREE-04), normal force-remove keeping the branch (WTREE-02), the D-01 blocked banner + copyable hint, and bulk clean-eligible (WTREE-03) all behave per the UI-SPEC contract.
- The full build/test surface is green.
</success_criteria>

<output>
Create `.planning/phases/23-worktree-cleanup-panel/23-05-SUMMARY.md` when done, recording the checkpoint outcome (approved / issues) and any follow-up gaps.
</output>
