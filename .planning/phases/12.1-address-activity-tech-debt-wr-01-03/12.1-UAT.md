---
status: complete
phase: 12.1-address-activity-tech-debt-wr-01-03
source: [12.1-VERIFICATION.md]
started: 2026-08-02T08:05:00Z
updated: 2026-08-25T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. WR-01 first-open behavior (TD-ACT-01)

**Setup:** In browser devtools Application/Storage, run `localStorage.removeItem("kamacu.activity.scope")`, then reload `/activity` fresh (backend running).

expected: No "Global" trigger-label flash on first paint; the Network tab shows exactly ONE `GET /api/activity?scope=workspace:<id>...` call (the promoted default) — NOT a preceding `?scope=global` call that gets discarded.
result: pass

### 2. WR-02 stale-scope self-heal + D-06 non-persistence (TD-ACT-02)

**Setup:** Run `localStorage.setItem("kamacu.activity.scope", "workspace:999999")` (a dead id), reload. Repeat with `"project:999999"`.

expected: ScopeSelector trigger label reads "Global"; the "All activity" dropdown item is checked; Network shows `?scope=global` (NOT `?scope=workspace:999999`); data is global. **D-06 check:** re-open devtools Storage — `kamacu.activity.scope` is STILL `workspace:999999` (the demotion is ephemeral, NOT persisted).
result: pass

### 3. WR-03 screen-reader / axe announcement (TD-ACT-03)

**Setup:** Navigate to `/activity` with reviews present. Inspect a reviews entry's `<a>` in devtools Elements; run axe DevTools on the page.

expected: aria-label reads `Open PR #<number> <title> on GitHub` (title present); axe reports no "link has a non-empty accessible name" failure on reviews entries. Optional: VoiceOver/NVDA spot-check announces both the number and the title.
result: pass

## Regression surface (re-confirm during UAT — must NOT break)

- Returning user with a valid saved `workspace:N`/`project:N` scope → label = that name, that dropdown item checked, query fetches that scope (demotion does not fire for live ids).
- First-time user lands on `workspace:<activeWorkspaceId>` scope (promotion effect still fires; WR-01 only defers the fetch until `scopeResolved` trips in lockstep).
- An explicit ScopeSelector pick still writes `kamacu.activity.scope` via the persisting `setScope` wrapper (demotion never touches this path).

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
