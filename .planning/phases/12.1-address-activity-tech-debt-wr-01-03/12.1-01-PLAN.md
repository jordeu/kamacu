---
phase: 12.1-address-activity-tech-debt-wr-01-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - web/src/lib/useActivityView.ts
  - web/src/pages/ActivityPage.tsx
  - web/src/components/activity/ReviewsList.tsx
autonomous: true
requirements:
  - TD-ACT-01
  - TD-ACT-02
  - TD-ACT-03

must_haves:
  truths:
    - "First-time users (no kamacu.activity.scope in localStorage) see no Global flash and fire no discarded ?scope=global fetch when opening /activity — the gate trips in lockstep with the promotion effect"
    - "A persisted workspace:N or project:N whose id no longer matches a live row resolves to effective global — the ScopeSelector trigger label reads Global, the All activity dropdown item is checked, and the query fetches ?scope=global (label, query, and selection agree)"
    - "The stale-scope demotion is ephemeral: it is useMemo-derived and NEVER writes localStorage; the saved kamacu.activity.scope value is left untouched (D-06)"
    - "Screen readers hear the PR title in each ReviewsList entry's accessible name — the aria-label contains both review.number and review.title"
    - "Returning users with a valid saved scope are unaffected — scope resolves to their workspace/project, the promotion effect does not fire, the persisting setScope wrapper still writes on an explicit pick"
    - "The enabled: boolean = true default on useActivity stays (the query remains reusable in isolation); ActivityPage is now the sole caller that overrides it"
  artifacts:
    - "web/src/lib/useActivityView.ts exports scopeResolved (boolean) and returns the useMemo-derived effective scope; internal state renamed savedScope/setSavedScope mirroring useActiveWorkspace's savedId"
    - "web/src/pages/ActivityPage.tsx destructures scopeResolved from useActivityView() and forwards it as the 3rd arg to useActivity(scope, window, scopeResolved)"
    - "web/src/components/activity/ReviewsList.tsx aria-label template is Open PR #{number} {title} on GitHub"
    - ".planning/REQUIREMENTS.md defines TD-ACT-01, TD-ACT-02, TD-ACT-03 under a Phase 12.1 tech-debt closure sub-section with 3 new rows in the Traceability table"
  key_links:
    - "useActivityView.scopeResolved (useMemo over [activeWorkspaceId]) → ActivityPage forwards → useActivity 3rd arg (the gate is live, IN-01 folded by construction)"
    - "useActivityView.scope (useMemo-derived effective scope over [savedScope, workspaces, projects]) → consumed transparently by ActivityPage's useActivity AND ScopeSelector's trigger label + dropdown-check (label/query/selection agree)"
    - "useActivityView.setScope (persisting wrapper, unchanged) ← ScopeSelector onScopeChange on an explicit user pick (the ONLY path that writes localStorage)"
---

<objective>
Close the three non-blocking quality gaps the v1.12 milestone audit surfaced in Phase 11's Activity page before milestone archive. Three surgical frontend edits, no backend, no SQL, no migration, no new dependency.

1. **WR-01 (TD-ACT-01, hardens ACT-03)** — wire the `useActivity` `enabled` gate so first-time users don't fire a wasted `global` fetch that is immediately discarded when the default-promotion effect flips scope to `workspace:N`, and don't see a brief wrong-scope flash on first open. The `enabled` parameter already exists (queries.ts:95-108); the sole caller `ActivityPage.tsx:24` never passes it. This phase makes it live.
2. **WR-02 (TD-ACT-02, hardens ACT-02)** — resolve a stale/deleted persisted `workspace:N`/`project:N` to effective `global` (label + query + dropdown-check all agree), mirroring the established `useActiveWorkspace` resolve-against-live-rows pattern WITHOUT persisting the demotion.
3. **WR-03 (TD-ACT-03, hardens REVIEWS-02)** — include the PR title in the `ReviewsList` external-link `aria-label` so screen readers hear the title that distinguishes entries, not just the PR number.

Purpose: Retire the v1.12 audit's WR-01..03 tech-debt bucket so the milestone archives with the Activity page's documented design contract honored (the `enabled` gate was specified in 4 Phase 11 planning artifacts and shipped dead) and with the three user-facing quality warts closed.

Output: Three edited frontend files (useActivityView.ts, ActivityPage.tsx, ReviewsList.tsx), REQUIREMENTS.md traceability updated with 3 new debt-closure IDs, the phase SUMMARY.md.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-CONTEXT.md
@.planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-RESEARCH.md
@.planning/phases/11-activity-page-controls/11-REVIEW.md

@web/src/lib/useActivityView.ts
@web/src/lib/useActiveWorkspace.tsx
@web/src/pages/ActivityPage.tsx
@web/src/components/activity/ReviewsList.tsx
@web/src/api/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define three Phase 12.1 debt-closure requirement IDs in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current file — the v1.12 Requirements + Traceability sections; the 18 existing IDs stay marked Complete)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-CONTEXT.md (## phase_requirements note: requirements are TBD; the three candidate IDs map 1:1 to the three WRs; this task picks the scheme)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-RESEARCH.md (## phase_requirements table — gate-wired/stale-scope-resolved/reviews-a11y-name → hardens ACT-03/ACT-02/REVIEWS-02; Open Question 1 recommends a TD- prefix OR descriptive names)
  </read_first>
  <action>
    Pick the requirement-ID scheme `TD-ACT-01`, `TD-ACT-02`, `TD-ACT-03` (TD prefix marks these as tech-debt closure, distinguishing them from the capability requirements ACT-01..04 that stay Complete; the `-ACT-` infix ties them to the Activity page they harden; `TD-ACT-03` hardens a Reviews requirement but lives under the Activity page's debt closure, which is consistent with where the fix ships).

    Add a new sub-section `### Phase 12.1 tech-debt closure (TD-ACT)` immediately AFTER the existing `### Activity Chart & Stat Tooltips (CHART/STAT/ENTRY)` section in the v1.12 Requirements body. Define three unchecked items:
    - `TD-ACT-01` — the `useActivity` `enabled` gate is wired (scopeResolved forwarded from useActivityView to useActivity) so first-time users get no wasted `global` fetch and no wrong-scope flash on first `/activity` open (closes 11-REVIEW WR-01/IN-01; hardens ACT-03)
    - `TD-ACT-02` — a stale/deleted persisted `workspace:N`/`project:N` scope self-heals to effective `global` so the ScopeSelector trigger label, the All-activity dropdown check, and the `?scope=` query all agree (closes 11-REVIEW WR-02; hardens ACT-02)
    - `TD-ACT-03` — each ReviewsList entry's accessible name includes the PR title (aria-label carries both number and title) so screen readers hear the title that distinguishes entries (closes 11-REVIEW WR-03; hardens REVIEWS-02)

    Then add three rows to the SECOND Traceability table (the Phase 12 one) — or, if cleaner, add a small new Traceability sub-table titled `Phase 12.1` directly beneath it. Columns: Requirement | Phase | Status. Rows: `TD-ACT-01 | Phase 12.1 | Pending`, `TD-ACT-02 | Phase 12.1 | Pending`, `TD-ACT-03 | Phase 12.1 | Pending`.

    Update the `**Coverage:**` block beneath the traceability tables: bump the v1.12 requirements total from 18 to 21, mapped from 18 to 21, unmapped stays 0. Update the `**Phase distribution:**` line to append `Phase 12.1 (Address Activity tech debt): 3 — TD-ACT-01..03 (debt closure hardening ACT-02/ACT-03/REVIEWS-02)`.

    Update the `*Last updated:*` footer line to reference Phase 12.1 planning. Do NOT alter the 18 existing Complete rows or mark any existing capability requirement as anything other than Complete. Do NOT touch the Future Requirements or Out of Scope sections.
  </action>
  <verify>
    <automated>grep -c 'TD-ACT-0[123]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md has a new `### Phase 12.1 tech-debt closure (TD-ACT)` sub-section defining TD-ACT-01, TD-ACT-02, TD-ACT-03
    - The Traceability section has 3 new rows mapping each TD-ACT-ID to Phase 12.1 with status Pending
    - Coverage block shows 21 total / 21 mapped / 0 unmapped
    - The 18 existing v1.12 capability requirements remain marked Complete
  </done>
  <acceptance_criteria>
    - `grep -c 'TD-ACT-01' .planning/REQUIREMENTS.md` returns at least 2 (definition + traceability row)
    - `grep -c 'TD-ACT-02' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'TD-ACT-03' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'Phase 12.1 tech-debt closure' .planning/REQUIREMENTS.md` returns 1
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` returns at least 3 (the three new traceability rows)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-01 gate + WR-02 stale-scope demotion in useActivityView.ts and ActivityPage.tsx forwarding</name>
  <files>web/src/lib/useActivityView.ts, web/src/pages/ActivityPage.tsx</files>
  <read_first>
    - web/src/lib/useActivityView.ts (the file being modified — current 92-line shape; the promotion effect at lines 72-79; the in-memory setter setScopeState at line 64 vs the persisting setScope wrapper at lines 81-84)
    - web/src/lib/useActiveWorkspace.tsx (lines 53-71 — THE structural template WR-02 mirrors: useMemo over (rows, savedId) → resolve-or-fallback-to-is_default WITHOUT persisting the fallback; persist only via setActiveWorkspaceId)
    - web/src/pages/ActivityPage.tsx (the sole caller — lines 24-25, the gap to close)
    - web/src/api/queries.ts (lines 85-108 — useActivity's enabled parameter already exists with default true; this file is NOT modified, the default stays per D-01/D-03)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-CONTEXT.md (decisions D-01..D-07 — the gate strategy and the stale-scope handling; D-02 scopeResolved semantics; D-06 do NOT persist the demotion; D-07 grammar+liveness layering)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-RESEARCH.md (## Per-WR Implementation Approach §WR-01 and §WR-02 — the recommended useMemo idiom for both, the concrete shape, and the pitfalls; ## Code Examples — the widened useActivityView recommended shape)
    - .planning/phases/11-activity-page-controls/11-REVIEW.md (WR-01 entry lines 42-70 with the fix snippet; WR-02 entry lines 72-89)
  </read_first>
  <action>
    In `web/src/lib/useActivityView.ts`:
    - Add `useWorkspaces` and `useProjects` to the import from `@/api/queries` (alongside the existing `useActiveWorkspace` import); add `useMemo` to the React import.
    - Inside `useActivityView()`, add `const { data: workspaces } = useWorkspaces();` and `const { data: projects } = useProjects();` next to the existing `useActiveWorkspace()` destructure.
    - Rename the internal state cell `scope`/`setScopeState` to `savedScope`/`setSavedScope` (mirrors useActiveWorkspace's `savedId` naming; clarifies that the returned `scope` is now derived). Update the `useState` initializer to call `readScope("global")` unchanged.
    - Update the existing promotion effect (the `useEffect` that adopts `workspace:<activeWorkspaceId>` on first open) to call the renamed `setSavedScope` instead of `setScopeState`. Do NOT alter its guard logic (`activeWorkspaceId !== null && localStorage.getItem(ACTIVITY_SCOPE_KEY) === null`) — that logic is correct and is part of the separate deferred lint-debt bucket; this phase does not fix it. Do NOT remove or rewrite the effect.
    - Add a `scopeResolved` `useMemo` returning `localStorage.getItem(ACTIVITY_SCOPE_KEY) !== null || activeWorkspaceId !== null` with dependency array `[activeWorkspaceId]` (D-02). This is the WR-01 gate signal.
    - Add an effective `scope` `useMemo` typed `ActivityScope` with dependency array `[savedScope, workspaces, products]`... `[savedScope, workspaces, projects]`. The body implements D-04/D-07: if `savedScope === "global"` return `"global"`; else if `savedScope` starts with the workspace prefix, parse the trailing id and — only when `workspaces` is defined (loaded) — return `"global"` if no row matches, otherwise return `savedScope`; else (project prefix) parse the trailing id and — only when `projects` is defined — return `"global"` if no row matches, otherwise return `savedScope`. The `workspaces &&`/`projects &&` loading guards are mandatory (Pitfall 2 — no premature demotion while rows are undefined).
    - Keep the existing `setScope` persisting wrapper but rename its internal setter call from `setScopeState` to `setSavedScope`. The wrapper continues to call `localStorage.setItem(ACTIVITY_SCOPE_KEY, s)` — this is the ONLY path that writes localStorage. CRITICAL (D-06): the demotion must NEVER route through this wrapper or through any setter — it is purely the `useMemo` derivation above. Do NOT call either setter from the demotion path.
    - `setWindow` is unchanged.
    - Widen the return to `{ scope, setScope, window, setWindow, scopeResolved }` (scopeResolved is the new field).
    - Update the JSDoc above the hook (currently lines 52-61) to mention: (a) `scopeResolved` is returned and forwards to useActivity's `enabled` (closes the Phase 11 Pitfall 6 dead-gate); (b) the returned `scope` is now the useMemo-derived effective scope, not the raw saved value; (c) a stale saved `workspace:N`/`project:N` demotes to `global` once rows load without persisting (mirrors useActiveWorkspace.tsx:57-66). Keep the existing comment density — do NOT add a wall of new inline comments on the useMemo blocks (the useActiveWorkspace pattern is self-documenting). Do NOT add any new inline `// ...` comments beyond what is needed to keep the existing doc-comments accurate.

    In `web/src/pages/ActivityPage.tsx`:
    - Line 24: widen the destructure to `const { scope, setScope, window, setWindow, scopeResolved } = useActivityView();`.
    - Line 25: change `useActivity(scope, window)` to `useActivity(scope, window, scopeResolved)` — forwarding the gate signal as the 3rd arg.
    - No other changes to ActivityPage. The `enabled: boolean = true` default in queries.ts stays (D-01/D-03).

    Do NOT add comments to either file beyond updating the existing useActivityView JSDoc to remain accurate. Do NOT touch queries.ts. Do NOT touch ScopeSelector.tsx (its useDisplayName Global-fallbacks stay as defense-in-depth for the loading window — Pitfall 3).
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <done>
    - `cd web && npm run build` (tsc -b && vite build) exits 0
    - useActivityView.ts exports scopeResolved and returns the useMemo-derived effective scope
    - The internal state cell is renamed savedScope/setSavedScope (mirrors useActiveWorkspace's savedId)
    - The promotion effect still writes the renamed setter; its guard logic is unchanged
    - The demotion is purely useMemo-derived — no setScope, no setSavedScope, no localStorage.setItem on the demotion path (D-06)
    - ActivityPage.tsx destructures scopeResolved and forwards it as the 3rd arg to useActivity
    - The enabled: boolean = true default in queries.ts is unchanged
  </done>
  <acceptance_criteria>
    - `grep -c 'scopeResolved' web/src/lib/useActivityView.ts` returns at least 3 (the useMemo declaration + the return field + the JSDoc mention)
    - `grep -c 'scopeResolved' web/src/pages/ActivityPage.tsx` returns at least 2 (the destructure + the useActivity 3rd arg)
    - `grep -c 'savedScope' web/src/lib/useActivityView.ts` returns at least 4 (the useState declaration + the promotion effect setter call + the effective-scope useMemo dependency + body)
    - `grep -c 'useWorkspaces\|useProjects' web/src/lib/useActivityView.ts` returns at least 4 (the import + the two hook calls + at least one reference inside the effective-scope useMemo)
    - `grep -c 'useMemo' web/src/lib/useActivityView.ts` returns at least 2 (scopeResolved + effective scope)
    - The 3rd arg to useActivity in ActivityPage.tsx is `scopeResolved` (not a boolean literal)
    - A read of the effective-scope useMemo body confirms the demotion returns the string `global` directly — it does NOT call any setter and does NOT call localStorage.setItem
    - `cd web && npm run build` exits 0 (the widened return type typechecks; the forwarding typechecks)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WR-03 a11y — include the PR title in the ReviewsList external-link aria-label</name>
  <files>web/src/components/activity/ReviewsList.tsx</files>
  <read_first>
    - web/src/components/activity/ReviewsList.tsx (the file being modified — the `<a>` at lines 82-89, aria-label at line 87; the visible text at lines 90-96 stays as-is)
    - web/src/api/types.ts (ReviewDoneSummary shape — confirm review.number and review.title are the field names; review.url and review.completedAt are unchanged)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-CONTEXT.md (decisions D-08 include the title in aria-label; D-09 exclude the timestamp from the accessible name; D-10 do NOT validate the href scheme — IN-02 is deferred)
    - .planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-RESEARCH.md (## Per-WR Implementation Approach §WR-03 — exact wording, field-name confirmation, D-09 timestamp-exclusion rationale)
    - .planning/phases/11-activity-page-controls/11-REVIEW.md (WR-03 entry lines 91-107 — root cause: aria-label replaces visible text as the accessible name per WAI-ARIA; the fix snippet)
  </read_first>
  <action>
    In `web/src/components/activity/ReviewsList.tsx`, change ONLY the `aria-label` attribute on the reviews `<a>` element (currently line 87). The current template is `` `Open PR #${review.number} on GitHub` ``. The new template is `` `Open PR #${review.number} ${review.title} on GitHub` `` — interpolate `review.title` between the number and the literal ` on GitHub`.

    This is a single-attribute edit. Do NOT change the visible text spans (the `#{review.number} {review.title}` truncating span and the `formatDateTime(review.completedAt)` timestamp span stay verbatim — D-09: the timestamp is excluded from the accessible name but stays as visible text). Do NOT change `href`, `target`, `rel`, `key`, or `className`. Do NOT validate the href scheme (D-10 — IN-02 is explicitly deferred). Do NOT add any comment. Do NOT touch the HARD_DEGRADE set, the byCompletedDesc comparator, the groupByProject import, or the null-safe `prs` normalization.

    The template carries the PR number AND the title AND the literal ` on GitHub` external-link cue — that satisfies the contract (number + title + external/GitHub signal) per D-08.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <done>
    - `cd web && npm run build` exits 0
    - The ReviewsList `<a>` aria-label interpolates review.title between the number and the literal ` on GitHub`
    - The visible text spans (number + title + timestamp) are unchanged
    - No other attribute on the `<a>` is changed
  </done>
  <acceptance_criteria>
    - `grep -c 'review.title on GitHub' web/src/components/activity/ReviewsList.tsx` returns 1 (the new aria-label template)
    - `grep -c 'Open PR #' web/src/components/activity/ReviewsList.tsx` returns 1 (still exactly one aria-label — no duplication)
    - The aria-label template reads `Open PR #${review.number} ${review.title} on GitHub` — number AND title AND external cue all present
    - The visible `formatDateTime(review.completedAt)` span is still present (timestamp stays as visible text per D-09)
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase is frontend-only and edits a query `enabled` gate (defers an existing read), a client-side scope-id demotion to `global`, and an `aria-label` string interpolation. No new inputs cross a trust boundary. |

## STRIDE Threat Register

This phase introduces **no new attack surface**. The three edits are a11y / query-gate / scope-demotion hardening against a local-only single-user app. The reviews `href` URL is constructed server-side from gh data (`internal/github/prlist.go`, `https://github.com/…` form) — scheme validation for that URL is a separate security item (IN-02, explicitly deferred per D-10). The stale-scope demotion can only demote to the literal string `global` (no arbitrary scope injection — `readScope` grammar-validates on read and the useMemo only returns `global` or the grammatically-valid saved value).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.1-01 | Information Disclosure | useActivityView effective-scope useMemo | low | accept | A stale saved scope for a deleted workspace/project could briefly fetch `?scope=workspace:<dead-id>` (returning empty data) before rows load and demote. This is one loopback GET of empty data on a localhost single-user app — matches useActiveWorkspace's resolve cadence and is explicitly accepted in CONTEXT.md the agent's Discretion. No mitigation warranted. |
| T-12.1-02 | Elevation of Privilege | useActivityView demotion path | low | accept | The demotion can only fall back to the literal `global` scope — it cannot escalate to a different workspace/project id (the useMemo body returns either `global` or the grammatically-validated saved value; no arbitrary id construction). No mitigation warranted. |
| T-12.1-SC | Tampering | (none — no packages installed) | low | accept | This phase installs zero npm/pip/cargo packages; the Package Legitimacy Gate is trivially satisfied (see 12.1-RESEARCH.md §Package Legitimacy Audit). No slopcheck or blocking checkpoint needed. |
</threat_model>

<verification>

**Automated gate (D-11, the project norm):**

```bash
cd web && npm run build
```

This is `tsc -b && vite build` (package.json `build` script). `tsc -b` catches any type breakage from the widened `useActivityView` return type and the `scopeResolved` forwarding; `vite build` catches the production bundle. No new test runner is installed or added (D-11/D-12 — adding vitest/jest would violate the no-new-dependency boundary; no runner exists in web/ today).

**Behavioral UAT (D-11 — operable in a browser devtools session against a running backend):**

The localStorage key is `kamacu.activity.scope` (useActivityView.ts:19).

- **WR-01 (TD-ACT-01) — first-time user, no wasted global fetch:** In devtools Application/Storage, run `localStorage.removeItem("kamacu.activity.scope")`, then reload `/activity` fresh. **Expect:** no "Global" trigger-label flash; the Network tab shows exactly ONE `GET /api/activity?scope=workspace:<id>...` call (the promoted default), NOT a preceding `?scope=global` call that gets discarded. Regression check: with a saved scope present, reload → one fetch for the saved scope, no flash.
- **WR-02 (TD-ACT-02) — stale saved workspace scope self-heals:** Run `localStorage.setItem("kamacu.activity.scope", "workspace:999999")` (a dead id), reload. **Expect:** the ScopeSelector trigger label reads "Global"; the "All activity" dropdown item is checked; Network shows `?scope=global` (NOT `?scope=workspace:999999`); the data is global. **D-06 check:** re-open devtools Storage — `kamacu.activity.scope` is STILL `workspace:999999` (the demotion is ephemeral, NOT persisted). Repeat with `localStorage.setItem("kamacu.activity.scope", "project:999999")` — same self-heal to global. Regression check: set a *live* `workspace:<id>` (any id from the workspaces list) → label is that workspace's name, that item is checked, the query fetches that scope.
- **WR-03 (TD-ACT-03) — screen readers hear the title:** Navigate to `/activity` with reviews present. Inspect a reviews entry's `<a>` in devtools Elements → the `aria-label` now reads `Open PR #<number> <title> on GitHub` (title present). Run axe DevTools on the page → no "link has a non-empty accessible name" failure on reviews entries (the link's accessible name now includes the title). Optional: VoiceOver/NVDA spot-check announces both the number and the title.

**Regression surface (must NOT break — re-verify after the phase):**
1. Returning user with a valid saved workspace/project scope → label = that workspace/project name, that dropdown item checked, query fetches that scope (the useMemo returns savedScope unchanged when the id is live; the promotion effect does NOT fire because the `localStorage.getItem(...) === null` guard blocks it).
2. First-time user lands on `workspace:<activeWorkspaceId>` scope (not global) — the promotion effect still fires; WR-01 only *defers the fetch* until scopeResolved trips in lockstep.
3. Degrade-don't-break: reviews HARD_DEGRADE state still renders ReviewsList as null (unchanged).
4. An explicit ScopeSelector pick still writes `kamacu.activity.scope` via the persisting `setScope` wrapper (the demotion never touches this path).
</verification>

<success_criteria>
- [ ] `cd web && npm run build` exits 0
- [ ] First-time Activity open fires ONE `?scope=workspace:<id>` fetch (no preceding `?scope=global`)
- [ ] A stale `workspace:<dead-id>` or `project:<dead-id>` saved scope self-heals to effective global — trigger label, dropdown check, and query all agree
- [ ] The stale-scope demotion is ephemeral — `kamacu.activity.scope` retains the dead value in localStorage after the demotion (D-06)
- [ ] Screen readers (or axe) hear/confirm the PR title in each reviews entry's accessible name
- [ ] Returning users with a valid saved scope are unaffected (regression-free)
- [ ] REQUIREMENTS.md defines TD-ACT-01/02/03 with traceability rows
</success_criteria>

<output>
Create `.planning/phases/12.1-address-activity-tech-debt-wr-01-03/12.1-01-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

**New / changed symbols:**

| Symbol | File | Change | Why (decision) |
|--------|------|--------|----------------|
| `scopeResolved` | `web/src/lib/useActivityView.ts` (return), `web/src/pages/ActivityPage.tsx` (consume + forward) | **NEW** return field (`boolean`); forwarded as `useActivity`'s 3rd arg | WR-01 (D-02/D-03). Computed via `useMemo` over `[activeWorkspaceId]`. IN-01 folded by construction — the gate parameter is no longer dead. |
| `scope` (effective) | `web/src/lib/useActivityView.ts` | **CHANGED** semantics: now the `useMemo`-derived effective scope (was the raw `useState` value). Demotes a stale `workspace:N`/`project:N` to `global` once rows load. | WR-02 (D-04/D-07). Callers (`ActivityPage`, `ScopeSelector`) consume transparently. |
| `savedScope` / `setSavedScope` | `web/src/lib/useActivityView.ts` | **RENAMED** from `scope` / `setScopeState` (internal state cell) | Mirrors `useActiveWorkspace`'s `savedId` naming; clarifies that the returned `scope` is now derived, not the raw saved value. |
| `useWorkspaces()` / `useProjects()` reads | `web/src/lib/useActivityView.ts` | **NEW** hook calls alongside the existing `useActiveWorkspace()` | WR-02 (D-05/D-07) — the liveness-check inputs. |
| effective-scope `useMemo` | `web/src/lib/useActivityView.ts` | **NEW** — derives the effective scope from `(savedScope, workspaces, projects)` with loading guards | WR-02 (D-04/D-06/D-07). Mirrors `useActiveWorkspace.tsx:57-66`. NEVER writes localStorage. |
| `aria-label` template | `web/src/components/activity/ReviewsList.tsx:87` | **CHANGED** — `Open PR #{number} on GitHub` → `Open PR #{number} {title} on GitHub` | WR-03 (D-08). |
| `TD-ACT-01`, `TD-ACT-02`, `TD-ACT-03` | `.planning/REQUIREMENTS.md` | **NEW** requirement IDs + 3 traceability rows | Phase 12.1 traceability. Each maps 1:1 to a WR and hardens ACT-03/ACT-02/REVIEWS-02 respectively. |

**Files modified (4):**
- `.planning/REQUIREMENTS.md` — 3 new debt-closure IDs + traceability rows + coverage block bump
- `web/src/lib/useActivityView.ts` — renamed internal state, two new useMemo blocks (scopeResolved + effective scope), widened return, JSDoc update
- `web/src/pages/ActivityPage.tsx` — destructure + 3rd-arg forwarding (2-line edit)
- `web/src/components/activity/ReviewsList.tsx` — single-attribute aria-label edit

**Files read but unchanged (3):**
- `web/src/api/queries.ts` — the `enabled` param already exists (lines 95-108); the default `true` stays (D-01/D-03)
- `web/src/components/activity/ScopeSelector.tsx` — no functional change; `useDisplayName` "Global" fallbacks stay as defense-in-depth for the loading window (Pitfall 3)
- `web/src/lib/useActiveWorkspace.tsx` — the structural template WR-02 mirrors (reference only)

**Optional artifact NOT produced (per D-12 + 12.1-RESEARCH):**
- A pure `resolveEffectiveScope(scope, workspaces, projects)` helper export + vitest unit test — explicitly NOT recommended (no test runner installed in `web/`; extraction purely for testability would either violate the no-new-dependency boundary or ship an untested helper). The inline `useMemo` form matches `useActiveWorkspace` (itself untested) and is verified by `tsc -b` + UAT.
