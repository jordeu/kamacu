---
phase: 12.1-address-activity-tech-debt-wr-01-03
verified: 2026-08-02T08:05:00Z
status: passed
score: 3/6 must-haves verified
behavior_unverified: 3
overrides_applied: 0
re_verification:
  previous_status: none
  notes: "Initial verification (no prior VERIFICATION.md)."
behavior_unverified_items:

  - truth: "First-time users see no Global flash and fire no discarded ?scope=global fetch on first /activity open"
    test: "In browser devtools, localStorage.removeItem('kamacu.activity.scope') then reload /activity fresh"
    expected: "No 'Global' trigger-label flash; Network tab shows exactly ONE GET /api/activity?scope=workspace:<id>... (the promoted default), NOT a preceding ?scope=global that gets discarded"
    why_human: "The wiring is verified statically (scopeResolved useMemo + ActivityPage forwarding) but the first-open UX behavior (flash absence + exact fetch count in the Network tab) is observable only in a running browser against a backend"

  - truth: "A persisted workspace:N/project:N whose id no longer matches resolves to effective global — ScopeSelector label reads Global, All-activity item is checked, query fetches ?scope=global"
    test: "localStorage.setItem('kamacu.activity.scope', 'workspace:999999') then reload; repeat with 'project:999999'"
    expected: "Trigger label reads 'Global'; the 'All activity' dropdown item is checked; Network shows ?scope=global (NOT ?scope=workspace:999999); data is global"
    why_human: "Label/query/selection agreement is a runtime property across the React render tree (useActivityView → ScopeSelector/useActivity) that grep cannot prove end-to-end; requires browser Elements + Network tabs"

  - truth: "Screen readers hear the PR title in each ReviewsList entry's accessible name"
    test: "Navigate to /activity with reviews present; inspect the <a> aria-label in devtools Elements; run axe DevTools"
    expected: "aria-label reads 'Open PR #<number> <title> on GitHub'; axe reports no 'link has a non-empty accessible name' failure on reviews entries"
    why_human: "The accessible-name announcement is a runtime a11y behavior (axe / VoiceOver / NVDA) — the source edit is verified verbatim but the screen-reader behavior needs an a11y tool"
---

# Phase 12.1: Address Activity tech debt (WR-01..03) — Verification Report

**Phase Goal:** Close the three non-blocking quality gaps surfaced by the v1.12 milestone audit — wire the `useActivity` `enabled` gate (WR-01/04), resolve stale/deleted persisted scopes to their real effective value instead of "Global" (WR-02), and include the PR title in the `ReviewsList` aria-label (WR-03).
**Verified:** 2026-08-02T08:05:00Z
**Status:** human_needed — all source-level assertions verified; 3 truths assert runtime behavior (UX / a11y) that requires browser UAT against a running backend
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | First-time users (no `kamacu.activity.scope` in localStorage) see no Global flash and fire no discarded `?scope=global` fetch on first `/activity` open | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `useActivityView.ts:97-102` `scopeResolved = useMemo(() => localStorage.getItem(ACTIVITY_SCOPE_KEY) !== null \|\| activeWorkspaceId !== null, [activeWorkspaceId])`; `ActivityPage.tsx:24-25` destructures `scopeResolved` and forwards it as 3rd arg to `useActivity(scope, window, scopeResolved)`; `queries.ts:98` `enabled: boolean = true` default unchanged. Wiring is live (D-02/D-03 honored) but first-open flash/fetch behavior needs browser devtools. |
| 2 | A persisted `workspace:N`/`project:N` whose id no longer matches a live row resolves to effective global — trigger label reads Global, All-activity dropdown item is checked, query fetches `?scope=global` | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `useActivityView.ts:104-114` effective scope is `useMemo` over `[savedScope, workspaces, projects]` returning literal `"global"` when `workspaces && !workspaces.some((w) => w.id === id)` (or projects equivalent); loading guards present (lines 108, 112). Demotion returns the string `"global"` directly — no setter, no `localStorage.setItem` on the demotion path. Label/query/selection agreement is runtime and needs browser UAT. |
| 3 | The stale-scope demotion is ephemeral: useMemo-derived and NEVER writes localStorage; the saved `kamacu.activity.scope` value is left untouched (D-06) | ✓ VERIFIED | `useActivityView.ts:104-114` demotion returns the literal `"global"` string from the useMemo body — zero setter calls (`setSavedScope`/`setScope`/`setWindow`) and zero `localStorage.setItem` calls in that block (verified by `awk 104-114 | grep -E 'set...|setItem'` → NONE). File total: exactly 2 `localStorage.setItem` calls (line 118 inside `setScope` wrapper, line 123 inside `setWindow` wrapper) — both on the explicit-pick path, none on the demotion path. Internal state renamed `scope`/`setScopeState` → `savedScope`/`setSavedScope` (line 80, mirrors `useActiveWorkspace.tsx:55`). D-06 honored structurally. |
| 4 | Screen readers hear the PR title in each ReviewsList entry's accessible name — aria-label contains both `review.number` and `review.title` | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `ReviewsList.tsx:87` aria-label is `` `Open PR #${review.number} ${review.title} on GitHub` `` — verbatim match against canonical RESEARCH.md:431 and D-08. Visible text spans (lines 90-96) unchanged; `formatDateTime(review.completedAt)` stays as visible text and is excluded from aria-label (D-09). Single-attribute edit (commit `9ae8833`: 1 insertion / 1 deletion). The screen-reader announcement is a runtime a11y property that needs axe/AT. |
| 5 | Returning users with a valid saved scope are unaffected — scope resolves to their workspace/project, the promotion effect does not fire, the persisting setScope wrapper still writes on an explicit pick | ✓ VERIFIED | Static analysis proves non-regression: (a) effective-scope useMemo returns `savedScope` unchanged when the id matches a live row (`useActivityView.ts:109` and `:113` — `return savedScope;`); (b) promotion effect guard `localStorage.getItem(ACTIVITY_SCOPE_KEY) === null` is unchanged (lines 90-91), so a saved scope blocks the effect; (c) persisting `setScope` wrapper (lines 116-119) unchanged and is the only path writing `ACTIVITY_SCOPE_KEY`; (d) `scopeResolved` is `true` on first render when a saved scope exists (line 99). No code path that affects a returning user with a live id was mutated. |
| 6 | The `enabled: boolean = true` default on `useActivity` stays (the query remains reusable in isolation); ActivityPage is now the sole caller that overrides it | ✓ VERIFIED | `queries.ts:95-108` `useActivity(scope, window, enabled: boolean = true)` signature unchanged (commit-touch audit: `queries.ts` NOT in the 4 modified files; only `useActivityView.ts`, `ActivityPage.tsx`, `ReviewsList.tsx`, `REQUIREMENTS.md` were touched). D-01/D-03 honored. |

**Score:** 3/6 truths verified (3 present, behavior-unverified — see `behavior_unverified_items`)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `web/src/lib/useActivityView.ts` | exports `scopeResolved` (boolean) and returns the useMemo-derived effective scope; internal state renamed `savedScope`/`setSavedScope`; promotion effect guard unchanged; `useWorkspaces`/`useProjects` reads added | ✓ VERIFIED | Lines 1-2 imports (`useEffect, useMemo, useState` / `useActiveWorkspace`) + lines 3-4 split `useProjects`/`useWorkspaces` imports. Lines 78-79 hook calls. Line 80 renamed `savedScope/setSavedScope`. Lines 88-95 promotion effect (guard unchanged, calls `setSavedScope`). Lines 97-102 `scopeResolved` useMemo. Lines 104-114 effective scope useMemo. Lines 116-119 persisting `setScope` wrapper. Lines 121-124 `setWindow` unchanged. Line 126 widened return. JSDoc (lines 6-24, 50-75) updated to cover scopeResolved + effective scope semantics (existing doc-comments preserved/updated — no new inline `//` prose). 127 lines total. |
| `web/src/pages/ActivityPage.tsx` | destructures `scopeResolved` from `useActivityView()` and forwards it as the 3rd arg to `useActivity(scope, window, scopeResolved)` | ✓ VERIFIED | Line 24 destructure includes `scopeResolved`; line 25 forwards as 3rd arg. 2-line edit; no other change (commit `152b452`: 2 insertions / 2 deletions). |
| `web/src/components/activity/ReviewsList.tsx` aria-label template | `Open PR #{number} {title} on GitHub` (interpolates `review.title` between the number and ` on GitHub`); visible text spans unchanged | ✓ VERIFIED | Line 87 verbatim match against RESEARCH.md:431 canonical form. Visible spans (lines 90-96) unchanged. Single-attribute edit. HARD_DEGRADE / byCompletedDesc / groupByProject import / null-safe prs normalization untouched. |
| `.planning/REQUIREMENTS.md` | defines TD-ACT-01, TD-ACT-02, TD-ACT-03 under a `### Phase 12.1 tech-debt closure (TD-ACT)` sub-section; 3 new traceability rows; coverage bumped 18→21 | ✓ VERIFIED | Lines 45-51 new sub-section with the three unchecked items (now flipped Complete). Lines 107-111 new traceability sub-table titled `Phase 12.1` (3 rows). Coverage block (lines 115-117) reads `21 total / 21 mapped / 0 unmapped`. Phase distribution (line 119) includes Phase 12.1 entry. The 18 existing v1.12 capability requirements remain marked Complete. Each TD-ACT ID references the WR it closes and the requirement it hardens (ACT-03/ACT-02/REVIEWS-02). |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `useActivityView.scopeResolved` (useMemo over `[activeWorkspaceId]`) | `useActivity`'s `enabled` arg | ActivityPage destructures + forwards as 3rd positional arg | ✓ WIRED | `useActivityView.ts:97-102` → `ActivityPage.tsx:24` destructure → `ActivityPage.tsx:25` `useActivity(scope, window, scopeResolved)` → `queries.ts:95-108` `enabled` param. IN-01 folded by construction — the gate parameter is no longer dead. |
| `useActivityView.scope` (useMemo-derived effective scope over `[savedScope, workspaces, projects]`) | consumed by `ActivityPage.useActivity` AND `ScopeSelector` (label + dropdown-check) | `ActivityPage.tsx:55` `<ScopeSelector scope={scope} onScopeChange={setScope} />`; `ActivityPage.tsx:25` `useActivity(scope, window, scopeResolved)` | ✓ WIRED | Effective scope flows transparently to both consumers (same field name); label, query, and selection will all see the demoted `"global"` value once rows load. |
| `useActivityView.setScope` (persisting wrapper, unchanged) | `ScopeSelector.onScopeChange` on an explicit user pick | `ActivityPage.tsx:55` `onScopeChange={setScope}` → `useActivityView.ts:116-119` wrapper writes localStorage | ✓ WIRED | The ONLY path that writes `ACTIVITY_SCOPE_KEY` (verified: 2 setItem calls total in file, the demotion path has none). D-06 invariant honored. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `useActivityView.scope` (effective) | `savedScope`, `workspaces`, `projects` rows | `useState(readScope)` + `useWorkspaces()`/`useProjects()` TanStack queries | Yes — `useWorkspaces`/`useProjects` are real GET `/api/workspaces`/`/api/projects` queries (queries.ts:11-23); demotion logic gates on row-loaded guards (`workspaces &&`/`projects &&`) | ✓ FLOWING |
| `useActivityView.scopeResolved` | `activeWorkspaceId`, hasSavedScope | `useActiveWorkspace()` context + `localStorage.getItem` | Yes — `useActiveWorkspace` resolves against real `/api/workspaces` rows; localStorage read is direct | ✓ FLOWING |
| `ReviewsList` aria-label | `review.number`, `review.title` | `data.reviews.prs` from `useActivity` (`GET /api/activity`) | Yes — same `ActivityResponse` already powering the visible text | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Build gate (D-11) | `cd web && npm run build` | `tsc -b && vite build` — `✓ built in 1.24s`, exit 0, 2800 modules transformed | ✓ PASS |
| `scopeResolved` exported | `grep -c scopeResolved web/src/lib/useActivityView.ts` | 3 (useMemo decl + return field + JSDoc) — meets ≥3 criterion | ✓ PASS |
| `scopeResolved` forwarded | `grep -c scopeResolved web/src/pages/ActivityPage.tsx` | 2 (destructure + 3rd arg) — meets ≥2 criterion | ✓ PASS |
| `savedScope` rename present | `grep -c savedScope web/src/lib/useActivityView.ts` | 8 — exceeds ≥4 criterion (useState + setter rename + promotion effect setter call + useMemo dep + body) | ✓ PASS |
| `useWorkspaces`/`useProjects` imported + used | `grep -cE "useWorkspaces\|useProjects" web/src/lib/useActivityView.ts` | 4 (split import lines × 2 + hook calls × 2) — meets ≥4 criterion | ✓ PASS |
| `useMemo` blocks | `grep -c useMemo web/src/lib/useActivityView.ts` | 4 — exceeds ≥2 criterion (2 inline uses + 2 in import line) | ✓ PASS |
| Single aria-label (no duplication) | `grep -c "Open PR #" web/src/components/activity/ReviewsList.tsx` | 1 — meets ==1 criterion | ✓ PASS |
| aria-label template verbatim | `grep -F 'aria-label={`Open PR #${review.number} ${review.title} on GitHub`}' ReviewsList.tsx` | VERBATIM MATCH against RESEARCH.md:431 / D-08 | ✓ PASS |
| D-06 non-persistence invariant | `awk 'NR>=104 && NR<=114' useActivityView.ts \| grep -E "set...\|setItem"` | NONE — demotion path has zero setter/setItem calls; exactly 2 setItem calls total (lines 118, 123), both in persisting wrappers | ✓ PASS |
| No `enabled` default mutated | `git show --stat 152b452` | `queries.ts` NOT in modified files; `queries.ts:98` `enabled: boolean = true` confirmed by read | ✓ PASS |
| Commits exist | `git show --stat fee7a95 152b452 9ae8833` | All three exist with correct scope (docs/fix/fix) touching only the expected 4 files (REQUIREMENTS.md + 3 web files) | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| (none) | No `scripts/*/tests/probe-*.sh` declared in PLAN or conventional scripts dir for this frontend-only phase | — | — |

Step 7c: SKIPPED — Phase declares no probes (frontend-only, build gate is the single automated check, per D-11).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| TD-ACT-01 | 12.1-01-PLAN Task 2 | The `useActivity` `enabled` gate is wired (`scopeResolved` forwarded from `useActivityView` to `useActivity`); hardens ACT-03; closes WR-01/IN-01 | ✓ SATISFIED (source) / ⚠ behavior UAT | REQUIREMENTS.md:49 (defined) + :109 (traceability row Complete). Source: `useActivityView.ts:97-102` + `ActivityPage.tsx:24-25`. First-open UX is human UAT. |
| TD-ACT-02 | 12.1-01-PLAN Task 2 | Stale/deleted `workspace:N`/`project:N` self-heals to effective `global` (label + query + check agree); demotion ephemeral (D-06); hardens ACT-02; closes WR-02 | ✓ SATISFIED (source + D-06 structurally) / ⚠ label/query/selection agreement UAT | REQUIREMENTS.md:50 (defined) + :110 (traceability row Complete). Source: `useActivityView.ts:104-114` useMemo with loading guards; demotion path has zero setters/setItem. Browser UAT for label/query/selection agreement. |
| TD-ACT-03 | 12.1-01-PLAN Task 3 | `ReviewsList` aria-label carries PR title (number + title + external cue); hardens REVIEWS-02; closes WR-03 | ✓ SATISFIED (source) / ⚠ a11y announcement UAT | REQUIREMENTS.md:51 (defined) + :111 (traceability row Complete). Source: `ReviewsList.tsx:87` verbatim match against RESEARCH.md:431. Screen-reader announcement is human UAT (axe). |

**Traceability cross-check:** All three Phase 12.1 requirement IDs (TD-ACT-01/02/03) are defined in REQUIREMENTS.md AND have a traceability row AND are mapped to the hardened requirement (ACT-03/ACT-02/REVIEWS-02 respectively). Coverage block correctly bumped 18→21. No orphaned requirements (every Phase-12.1-mapped requirement appears in a plan's `requirements:` field). REQUIREMENTS.md `Phase distribution` line correctly appends Phase 12.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | — | — | No TBD/FIXME/XXX debt markers, no TODO/HACK/PLACEHOLDER, no `return null`/`return []`/`=> {}` empty impls, no hardcoded-empty props, no console.log-only impls in any of the 3 modified files. |

### Scope Guardrails (D-10/D-11/D-12 / CLAUDE.md / OUT-OF-SCOPE)

| Guardrail | Status | Evidence |
| --- | --- | --- |
| No backend / SQL / migration / new API | ✓ HONORED | `git diff --name-only fee7a95^..9ae8833` returns ONLY `.planning/REQUIREMENTS.md`, `web/src/lib/useActivityView.ts`, `web/src/pages/ActivityPage.tsx`, `web/src/components/activity/ReviewsList.tsx` — no Go files, no migrations, no new routes. |
| No new dependency / no test runner added (D-11/D-12) | ✓ HONORED | `grep -E '"(vitest\|jest\|mocha\|playwright\|testing-library)' web/package.json` → NONE. The four modified files include no new imports beyond React's `useMemo` (already in dependency tree) and the existing `useProjects`/`useWorkspaces` hooks. |
| No Phase 12 lint debt pulled in (`react-hooks/set-state-in-effect`) | ✓ HONORED | `grep -E "react-hooks/set-state-in-effect"` on the 3 modified files → NONE. The agent chose the `useMemo` form (RESEARCH §WR-01 recommendation) precisely to avoid introducing this lint-debt class. |
| No IN-02 href scheme validation (D-10 — explicitly deferred) | ✓ HONORED | `grep -E "javascript:\|data:\|validateUrl\|safeUrl\|href.*startsWith"` on `ReviewsList.tsx` → NONE. The `<a href={review.url}>` is unchanged except for the aria-label attribute. |
| ReviewsList unchanged beyond the aria-label | ✓ HONORED | `git show 9ae8833 -- ReviewsList.tsx` is exactly 1 insertion / 1 deletion on line 87. HARD_DEGRADE set, byCompletedDesc, groupByProject import, null-safe `prs` normalization, visible text spans, and other attributes (`href`/`target`/`rel`/`key`/`className`) all untouched. |
| No new prose `//` comments (CLAUDE.md rule) | ✓ HONORED | `git show 152b452 -- useActivityView.ts \| grep -E "^\+.*//"` → NO NEW `//` COMMENTS. The existing JSDoc above `useActivityView` (lines 6-24, 50-75) was updated to keep accurate (mention `scopeResolved` + effective scope semantics) — this is preserving/updating existing doc-comments, not adding new inline prose. |
| `queries.ts` `enabled` default unchanged (D-01/D-03) | ✓ HONORED | `queries.ts` is not in the modified-files list; line 98 still reads `enabled: boolean = true`. |
| Promotion effect guard logic unchanged | ✓ HONORED | `useActivityView.ts:88-95` — guard `activeWorkspaceId !== null && localStorage.getItem(ACTIVITY_SCOPE_KEY) === null` is byte-identical to the pre-phase version; only the renamed setter call (`setScopeState` → `setSavedScope`) changed. |
| Loading guards on demotion (Pitfall 2 — no premature demotion while rows undefined) | ✓ HONORED | `useActivityView.ts:108` `if (workspaces && !workspaces.some(...))` and `:112` `if (projects && !projects.some(...))` — both gated on row-loaded. |

### Human Verification Required

Three truths are present and wired in source but assert runtime behavior that grep/build cannot prove — they require a browser devtools / a11y-tool UAT session against a running backend. These route to `/gsd-verify-work`:

### 1. WR-01 first-open behavior (TD-ACT-01)

**Test:** In browser devtools Application/Storage, run `localStorage.removeItem("kamacu.activity.scope")`, then reload `/activity` fresh (with the backend running).
**Expected:** No "Global" trigger-label flash on first paint; the Network tab shows exactly ONE `GET /api/activity?scope=workspace:<id>...` call (the promoted default) — NOT a preceding `?scope=global` call that gets discarded.
**Why human:** The wiring is structurally proven (scopeResolved useMemo + ActivityPage forwarding + enabled gate), but the user-visible flash and the exact fetch count in the Network tab are runtime browser properties against a live backend.

### 2. WR-02 stale-scope self-heal + D-06 non-persistence (TD-ACT-02)

**Test:** Run `localStorage.setItem("kamacu.activity.scope", "workspace:999999")` (a dead id), reload. Repeat with `"project:999999"`.
**Expected:** ScopeSelector trigger label reads "Global"; the "All activity" dropdown item is checked; Network shows `?scope=global` (NOT `?scope=workspace:999999`); data is global. **D-06 check:** re-open devtools Storage — `kamacu.activity.scope` is STILL `workspace:999999` (the demotion is ephemeral, NOT persisted).
**Why human:** Label/query/selection agreement is a cross-component runtime property (useActivityView → ScopeSelector render → useActivity fetch key) that needs browser Elements + Network tabs to confirm. The D-06 non-persistence invariant IS verified statically (zero setters/setItem on the demotion path) but the post-self-heal localStorage value is a runtime fact worth a one-glance confirm.

### 3. WR-03 screen-reader / axe announcement (TD-ACT-03)

**Test:** Navigate to `/activity` with reviews present. Inspect a reviews entry's `<a>` in devtools Elements → the aria-label reads `Open PR #<number> <title> on GitHub`. Run axe DevTools on the page.
**Expected:** aria-label carries the title; axe reports no "link has a non-empty accessible name" failure on reviews entries. Optional: VoiceOver/NVDA spot-check announces both the number and the title.
**Why human:** The accessible-name announcement is a runtime a11y behavior — the source edit is verified verbatim against the canonical RESEARCH form, but the AT announcement is what closes the gap for users.

### Regression surface (must NOT break — re-verify during the UAT)

1. Returning user with a valid saved workspace/project scope → label = that workspace/project name, that dropdown item checked, query fetches that scope (the useMemo returns savedScope unchanged when the id is live; the promotion effect does NOT fire because the `localStorage.getItem(...) === null` guard blocks it).
2. First-time user lands on `workspace:<activeWorkspaceId>` scope (not global) — the promotion effect still fires; WR-01 only *defers the fetch* until scopeResolved trips in lockstep.
3. Degrade-don't-break: reviews HARD_DEGRADE state still renders ReviewsList as null (unchanged).
4. An explicit ScopeSelector pick still writes `kamacu.activity.scope` via the persisting `setScope` wrapper (the demotion never touches this path — verified: exactly 2 setItem calls, both in wrappers).

### Gaps Summary

No gaps. All six must-have truths are present and wired. Three of the six (truths #1, #2, #4) assert runtime UX/a11y behavior that no static check or test runner in this repo can exercise — the build gate (`tsc -b && vite build`) passes, the source assertions match the canonical RESEARCH/CONTEXT forms verbatim, and D-06's non-persistence invariant is structurally enforced (zero setters/setItem on the demotion path). The remaining three truths (#3 D-06 invariant, #5 regression-non-regression, #6 enabled default preserved) are statically verified. The phase therefore routes to `human_needed` for the three behavioral UAT items above; once those pass in a browser session the phase can be marked complete and the v1.12 milestone can archive.

---

_Verified: 2026-08-02T08:05:00Z_
_Verifier: the agent (gsd-verifier)_
