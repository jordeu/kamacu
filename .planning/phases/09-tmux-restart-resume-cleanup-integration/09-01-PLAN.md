---
phase: 09-tmux-restart-resume-cleanup-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/tmux/tmux.go
  - internal/tmux/tmux_test.go
  - internal/store/migrations/00006_task_status_timestamps.sql
autonomous: true
requirements: [TMUX-08, REAP-01]

must_haves:
  truths:
    - "The orphan sweep and cleanup paths can enumerate every live kangent-* tmux session by name"
    - "Every task row carries a per-status timestamp column the reaper keys on (done_at) plus three banked stats columns"
    - "Existing Done tasks are immediately reapable after migration (done_at backfilled from updated_at)"
  artifacts:
    - path: "internal/tmux/tmux.go"
      provides: "ListSessions verb on the kangent socket"
      contains: "func (c Client) ListSessions("
    - path: "internal/store/migrations/00006_task_status_timestamps.sql"
      provides: "todo_at/in_progress_at/in_review_at/done_at columns with backfill"
      contains: "done_at"
  key_links:
    - from: "internal/tmux/tmux.go ListSessions"
      to: "tmux list-sessions -F '#{session_name}'"
      via: "exec.CommandContext on the -L kangent socket"
      pattern: "list-sessions.*session_name"
---

<objective>
Lay the two foundation pieces the rest of Phase 9 builds on:
1. Add `ListSessions` to the `internal/tmux` leaf package (D-94) so the orphan sweep (09-04) can diff live tmux sessions against `tmux_sessions` rows.
2. Add migration `00006` (D-90) introducing four per-status timestamp columns on `tasks`, with backfill of existing rows so current Done tasks are immediately reapable by the reaper (09-05).

Purpose: Both are pure leaf additions (one CLI verb, one SQL migration) with zero behavioral coupling — they unblock the Wave 2 plans (orphan sweep needs ListSessions; reaper needs done_at) without touching any live code path.
Output: One new tmux method + tests; one new migration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tmux-restart-resume-cleanup-integration/09-CONTEXT.md

<interfaces>
<!-- The existing internal/tmux Client surface (verbs ListSessions joins). Match this exact shape. -->

From internal/tmux/tmux.go:
```go
const DefaultSocket = "kangent"

type Client struct {
    Socket   string // -L socket name
    ConfPath string // -f config path
}

func (c Client) BaseArgs() []string  // returns []string{"-L", c.Socket, "-f", c.ConfPath}
func (c Client) run(ctx context.Context, args ...string) error  // exec.CommandContext, 5s execTimeout, arg-array only
func (c Client) HasSession(ctx context.Context, name string) (bool, error)  // exit 0 alive / exit 1 dead / err = binary broken
func (c Client) KillSession(ctx context.Context, name string) error  // exit 1 = idempotent success
func (c Client) KillServer(ctx context.Context) error
```

Existing migration naming: `00001_init.sql` ... `00005_tmux_sessions.sql`. The `tasks` table (from 00001):
```sql
CREATE TABLE tasks (
  id INTEGER PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'todo' CHECK (status IN ('todo','in_progress','in_review','done')),
  position REAL NOT NULL,
  created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
  updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
);
```
Migrations are goose (`-- +goose Up` / `-- +goose Down`), dialect `sqlite3`, embedded via `//go:embed migrations/*.sql`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ListSessions to internal/tmux (D-94)</name>
  <files>internal/tmux/tmux.go, internal/tmux/tmux_test.go</files>
  <read_first>
    - internal/tmux/tmux.go (the full Client surface — HasSession/KillSession show the exact run/exit-code/error-handling pattern to mirror)
    - internal/tmux/tmux_test.go (newTestClient + newDetachedSession helpers — per-test -L socket, KillServer cleanup, tmux-skip; Empirical Finding 1 re: -d in tests)
    - .planning/phases/08-tmux-shells-spawn-detach-lifecycle/08-RESEARCH.md (Empirical Finding 14: list-sessions -F '#{session_name}' emits clean one-per-line names; Finding 4/5: exit 1 + "no server running" when the server is down)
  </read_first>
  <behavior>
    - Test: ListSessions on a socket with sessions "alpha" and "beta" created via newDetachedSession returns a slice containing exactly "alpha" and "beta" (order-insensitive).
    - Test: ListSessions on a socket with NO server running (nothing created) returns an empty/nil slice and a nil error — "no server running" (exit 1) is NOT an error, it means zero sessions.
    - Test: ListSessions trims whitespace and drops empty lines — the result never contains "".
  </behavior>
  <action>
    Add a `ListSessions(ctx context.Context) ([]string, error)` method to the `Client` type in `internal/tmux/tmux.go`, immediately after `KillServer`.

    Implementation (do NOT use the `run` helper — that returns only an error; you need stdout):
    - Build args: `append(c.BaseArgs(), "list-sessions", "-F", "#{session_name}")`.
    - Run under a bounded context exactly like `run`: `ctx, cancel := context.WithTimeout(ctx, execTimeout)` then `defer cancel()`.
    - Use `exec.CommandContext(ctx, "tmux", args...)` and capture stdout via `.Output()`.
    - On a clean run: split stdout on "\n", `strings.TrimSpace` each line, skip empty strings, return the collected names and nil.
    - On an `*exec.ExitError` with `ExitCode() == 1`: return `nil, nil` — exit 1 means "no server running" / no sessions on this socket (Empirical Finding 5), which is the normal empty case, NEVER an error. Mirror the `errors.As(err, &exitErr)` pattern already used in HasSession/KillSession.
    - On any other error (binary missing, timeout, other exit codes): return `nil, err`.
    - Add `"strings"` to the import block.

    Doc comment must state: "ListSessions enumerates live session names on this socket (Phase 9 orphan sweep input, D-94). Exit 1 (no server running) is the empty case — nil, nil — never an error."
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go test ./internal/tmux/ -run ListSessions -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func (c Client) ListSessions(ctx context.Context) (\[\]string, error)" internal/tmux/tmux.go` returns 1
    - `grep -c "list-sessions" internal/tmux/tmux.go` returns 1
    - `grep -c "#{session_name}" internal/tmux/tmux.go` returns 1
    - `grep "ExitCode() == 1" internal/tmux/tmux.go` matches inside ListSessions (exit-1 → nil,nil empty case)
    - `cd /home/jordi/workspace/github/kangent && go test ./internal/tmux/` exits 0
    - `cd /home/jordi/workspace/github/kangent && go vet ./internal/tmux/` exits 0
  </acceptance_criteria>
  <done>ListSessions exists, returns clean trimmed names, treats exit-1 as the empty case, and is covered by tests that pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 00006 — per-status timestamps with backfill (D-90)</name>
  <files>internal/store/migrations/00006_task_status_timestamps.sql</files>
  <read_first>
    - internal/store/migrations/00005_tmux_sessions.sql (the most recent migration — copy its -- +goose Up / -- +goose Down structure and comment style exactly)
    - internal/store/migrations/00001_init.sql (the tasks table: status enum todo/in_progress/in_review/done, created_at, updated_at — NO status_at columns today)
    - internal/store/migrate.go (confirms goose dialect sqlite3, embedded glob migrations/*.sql)
  </read_first>
  <action>
    Create `internal/store/migrations/00006_task_status_timestamps.sql` following the goose pattern.

    `-- +goose Up` section:
    1. Add four nullable TEXT columns to `tasks` (one ALTER TABLE ... ADD COLUMN per line — SQLite requires separate statements):
       - `ALTER TABLE tasks ADD COLUMN todo_at TEXT;`
       - `ALTER TABLE tasks ADD COLUMN in_progress_at TEXT;`
       - `ALTER TABLE tasks ADD COLUMN in_review_at TEXT;`
       - `ALTER TABLE tasks ADD COLUMN done_at TEXT;`
       All nullable (no DEFAULT) — a NULL means "never entered that status". The reaper (09-05) keys ONLY on done_at gated by status='done'.
    2. Backfill existing rows so current Done tasks are immediately reapable (Discretion: approximate from updated_at). Single UPDATE:
       - `UPDATE tasks SET done_at = updated_at WHERE status = 'done';`
       This is the ONLY load-bearing backfill (it makes existing Done tasks reapable). Leave todo_at/in_progress_at/in_review_at NULL for existing rows — they are banked for future stats (D-90) and have no current consumer, so approximating them is unnecessary noise.

    `-- +goose Down` section: SQLite cannot DROP COLUMN portably on this goose/driver combo without table rebuild; instead drop the columns via the standard rebuild-free path goose+modernc supports — use `ALTER TABLE tasks DROP COLUMN done_at;` etc. (modernc.org/sqlite tracks SQLite 3.53 which supports DROP COLUMN). One DROP per line for done_at, in_review_at, in_progress_at, todo_at.

    Add a comment block at the top of the Up section: "Per-status entry timestamps (D-90). done_at drives the REAP-01 reaper (time-in-Done); the other three are banked for future cycle-time/dwell-time stats and have no current consumer. set on each status transition in tasks.go move (last-entry-wins). Existing Done rows backfilled from updated_at so they are immediately reapable."
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go test ./internal/store/ ./internal/api/ -run 'Migrat|Task' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/store/migrations/00006_task_status_timestamps.sql` exists
    - `grep -c "ADD COLUMN todo_at TEXT" internal/store/migrations/00006_task_status_timestamps.sql` returns 1
    - `grep -c "ADD COLUMN in_progress_at TEXT" internal/store/migrations/00006_task_status_timestamps.sql` returns 1
    - `grep -c "ADD COLUMN in_review_at TEXT" internal/store/migrations/00006_task_status_timestamps.sql` returns 1
    - `grep -c "ADD COLUMN done_at TEXT" internal/store/migrations/00006_task_status_timestamps.sql` returns 1
    - `grep -c "UPDATE tasks SET done_at = updated_at WHERE status = 'done'" internal/store/migrations/00006_task_status_timestamps.sql` returns 1
    - File contains both `-- +goose Up` and `-- +goose Down`
    - `cd /home/jordi/workspace/github/kangent && go build ./...` exits 0 and `go test ./internal/store/` exits 0 (migration applies cleanly against a fresh DB)
  </acceptance_criteria>
  <done>Migration 00006 adds the four nullable per-status columns, backfills done_at from updated_at for existing Done rows, applies cleanly up and down, and existing store/api tests stay green.</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds.
- `go test ./internal/tmux/ ./internal/store/` exits 0.
- `internal/tmux/tmux.go` exposes `ListSessions` returning trimmed names, exit-1 as empty.
- A fresh DB migrated to head has `todo_at`, `in_progress_at`, `in_review_at`, `done_at` columns on `tasks`; a pre-existing Done task has `done_at` populated.
</verification>

<success_criteria>
- ListSessions is callable from internal/api (Wave 2 orphan sweep dependency satisfied).
- The reaper (Wave 2) can read done_at and existing Done tasks are reapable.
- Zero changes to any live spawn/stop/HTTP path — both additions are inert until Wave 2 wires them.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tmux-restart-resume-cleanup-integration/09-01-SUMMARY.md`
</output>
