---
phase: 06-settings-polish
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/migrations/00004_settings.sql
  - internal/settings/settings.go
  - internal/settings/home.go
  - internal/settings/validate.go
  - internal/settings/template.go
  - internal/settings/tokenize.go
  - internal/settings/settings_test.go
  - internal/settings/validate_test.go
  - internal/settings/template_test.go
  - internal/settings/tokenize_test.go
  - internal/api/settings.go
  - internal/api/settings_test.go
  - internal/api/routes.go
  - cmd/kangent/main.go
autonomous: true
requirements: [SET-02, SET-04, AGENT-02, BRANCH-02]

must_haves:
  truths:
    - "GET /api/settings returns all four keys, each with value + default; the shell entry additionally carries options [\"bash\"]"
    - "PUT /api/settings/branch_template with an invalid template returns 400 with the canonical message and stores nothing (subsequent GET shows the prior value)"
    - "PUT /api/settings/agent_extra_params with an empty string stores \"\" — Get returns \"\" afterward, NOT the default (Pitfall 1)"
    - "Settings rows live in a SQLite table created by migration 00004 and survive process restart"
    - "An absent settings row reads as the code default — no seeded rows in the migration"
  artifacts:
    - path: "internal/store/migrations/00004_settings.sql"
      provides: "settings KV table"
      contains: "CREATE TABLE settings"
    - path: "internal/settings/settings.go"
      provides: "keys, Defaults map, Get/GetAll/Set"
      exports: ["KeyAgentExtraParams", "KeyWorktreeBase", "KeyShell", "KeyBranchTemplate", "Defaults", "Get", "GetAll", "Set"]
    - path: "internal/settings/validate.go"
      provides: "per-key validation with canonical error strings + CheckRefFormat"
      exports: ["Validate", "CheckRefFormat", "AllowedShells"]
    - path: "internal/settings/template.go"
      provides: "branch-template token scan + ExpandTemplate"
      exports: ["ExpandTemplate"]
    - path: "internal/settings/tokenize.go"
      provides: "quote-aware extra-params tokenizer"
      exports: ["Tokenize"]
    - path: "internal/api/settings.go"
      provides: "GET /api/settings + PUT /api/settings/{key}"
  key_links:
    - from: "internal/api/settings.go"
      to: "internal/settings"
      via: "settings.GetAll / settings.Set in handlers"
      pattern: "settings\\.(GetAll|Set)"
    - from: "internal/settings/settings.go Set"
      to: "internal/settings/validate.go"
      via: "validate BEFORE upsert — invalid values are never stored"
      pattern: "Validate\\("
    - from: "internal/api/routes.go"
      to: "internal/api/settings.go"
      via: "SettingsRoutes registered inside Routes"
      pattern: "SettingsRoutes"
---

<objective>
Create the settings foundation: migration 00004 (KV table), the `internal/settings` package (keys, code defaults, Get/Set, per-key validation with the UI-SPEC canonical error copy, branch-template expansion, extra-params tokenizer, relocated ExpandHome), and the REST surface (`GET /api/settings`, `PUT /api/settings/{key}`).

Purpose: SET-02 (SQLite persistence over the API), SET-04 (defaults in code + validation that isolates per key), AGENT-02 (the `--dangerously-skip-permissions` default value lives here — intentional D-51 reversal), BRANCH-02 (save-time template validation). Wave-2 plans wire these values into the existing spawn/creation call sites and the settings page.

Output: a fully tested settings package + API; no behavior change anywhere else yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-polish/06-RESEARCH.md
@.planning/phases/06-settings-polish/06-UI-SPEC.md

<interfaces>
Existing contracts this plan builds against (verified in source 2026-06-11):

From internal/api/respond.go:
```go
func writeJSON(w http.ResponseWriter, status int, v any)
func writeError(w http.ResponseWriter, status int, msg string) // {"error": msg}
```

From internal/api/routes.go:
```go
func Routes(mux *http.ServeMux, db *sql.DB, wt *worktree.Service, mgr *session.Manager)
// registers handlers via mux.HandleFunc("GET /api/...", ...) — Go 1.22 method+pattern mux
```

From cmd/kangent/main.go:183 (to be relocated):
```go
// expandHome resolves a leading "~" or "~/" to the current user's home directory.
func expandHome(p string) (string, error)
```

Migrations: goose, embed.FS, `internal/store/migrations/` — highest existing number is 00003_agent_sessions.sql, so the new file is 00004.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 00004 + settings store core (keys, Defaults, Get/GetAll/Set) + ExpandHome relocation</name>
  <files>internal/store/migrations/00004_settings.sql, internal/settings/settings.go, internal/settings/home.go, internal/settings/settings_test.go, cmd/kangent/main.go</files>
  <read_first>internal/store/migrations/00003_agent_sessions.sql (goose annotation style), internal/store/migrate.go (embed + Up pattern), cmd/kangent/main.go lines 40–45 and 180–195 (expandHome + its dbPath call site), 06-RESEARCH.md Pattern 1</read_first>
  <behavior>
    - Get on a fresh DB (no row) returns Defaults[key] for every key, nil error
    - Set then Get round-trips a value; Set again overwrites (upsert)
    - Set stores empty string for agent_extra_params; Get returns "" — NOT the default (Pitfall 1: only sql.ErrNoRows defaults)
    - GetAll returns a map with all four keys populated (stored value or default)
    - Set rejects unknown keys with an error
  </behavior>
  <action>
    Create `internal/store/migrations/00004_settings.sql` exactly:

    ```sql
    -- +goose Up
    CREATE TABLE settings (
        key   TEXT PRIMARY KEY,
        value TEXT NOT NULL,
        updated_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
    );
    -- +goose Down
    DROP TABLE settings;
    ```

    NO seeded rows (anti-pattern: absent row = code default, so new settings/default changes never need a migration).

    Create `internal/settings/settings.go`:
    - Key constants: `KeyAgentExtraParams = "agent_extra_params"`, `KeyWorktreeBase = "worktree_base"`, `KeyShell = "shell"`, `KeyBranchTemplate = "branch_template"`.
    - `var Defaults = map[string]string{ KeyAgentExtraParams: "--dangerously-skip-permissions", KeyWorktreeBase: "~/.kangent/worktrees/", KeyShell: "bash", KeyBranchTemplate: "task/{slug}-{id}" }` — the extra-params default is AGENT-02's intentional D-51 reversal; add that as a code comment.
    - `func Get(db *sql.DB, key string) (string, error)` — SELECT value; on `sql.ErrNoRows` return `Defaults[key], nil`. Do NOT treat empty string as absent (Pitfall 1).
    - `func GetAll(db *sql.DB) (map[string]string, error)` — start from a copy of Defaults, overlay stored rows.
    - `func Set(db *sql.DB, key, value string) error` — reject keys not in Defaults; call `Validate(key, value)` (Task 2; stub as no-op returning nil if Task 2 not yet written — but tasks run in order, so just call it); then upsert: `INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value, updated_at=strftime('%Y-%m-%dT%H:%M:%fZ','now')`. Validation failure → value never stored (BRANCH-02).

    Create `internal/settings/home.go`: move `expandHome` from cmd/kangent/main.go verbatim, exported as `func ExpandHome(p string) (string, error)`. Update cmd/kangent/main.go: delete the local func, import `kangent/internal/settings`, replace both call sites (`expandHome(*dbFlag)` line ~42 and `expandHome("~/.kangent/worktrees")` line ~83) with `settings.ExpandHome(...)`.

    Tests (`settings_test.go`): table-driven against an in-memory/temp SQLite DB with migrations applied (reuse the existing store test bootstrap pattern — see internal/store tests or internal/api test harnesses for how a migrated test DB is opened).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/settings/... ./internal/store/...</automated>
  </verify>
  <acceptance_criteria>
    - `ls internal/store/migrations/00004_settings.sql` exists; `grep -c "goose Up" internal/store/migrations/00004_settings.sql` = 1; `grep -c "INSERT" internal/store/migrations/00004_settings.sql` = 0 (no seeds)
    - `grep -n '"--dangerously-skip-permissions"' internal/settings/settings.go` matches (AGENT-02 default)
    - `grep -n '"~/.kangent/worktrees/"' internal/settings/settings.go` matches (WT-01 default, raw with tilde + trailing slash)
    - `grep -n '"task/{slug}-{id}"' internal/settings/settings.go` matches (BRANCH-01 default)
    - `grep -n "ErrNoRows" internal/settings/settings.go` matches (absent-row-only defaulting)
    - `grep -cn "func expandHome" cmd/kangent/main.go` = 0 and `grep -n "settings.ExpandHome" cmd/kangent/main.go` shows 2 call sites
    - `go test ./internal/settings/...` passes including an explicit empty-string round-trip test for agent_extra_params
  </acceptance_criteria>
  <done>Migration 00004 creates the KV table; settings.Get/GetAll/Set work with code defaults and upsert semantics; empty-string extra-params is preserved; main.go uses the relocated ExpandHome; all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation, branch-template expansion, extra-params tokenizer (canonical error copy)</name>
  <files>internal/settings/validate.go, internal/settings/template.go, internal/settings/tokenize.go, internal/settings/validate_test.go, internal/settings/template_test.go, internal/settings/tokenize_test.go</files>
  <read_first>internal/worktree/worktree.go lines 45–75 (Slug — the constructive charset to reuse), 06-RESEARCH.md §git check-ref-format verification matrix + Pattern 4 + Pattern 5 + Pattern 6, 06-UI-SPEC.md Copywriting Contract (canonical error strings)</read_first>
  <behavior>
    Validate(KeyBranchTemplate, v) — ordered checks, exact error strings:
    - "" or whitespace → error "Template can't be empty."
    - "task/{slugg}-{id}" → error "Unknown token {slugg}. Use {slug}, {id}, or {title}." (scan regexp `\{[^}]*\}`; braces are GIT-LEGAL so this MUST be an explicit app check — Pitfall 2)
    - "task/{slug}" → error "Template must include {id} so branch names never collide."
    - "task/{slug}-{id}." (trailing dot) → error "Not a valid git branch name."
    - "-{id}" → error "Not a valid git branch name." (leading dash passes git but poisons argv — Pitfall 3)
    - "a..b/{id}", "{id}.lock", "task//{id}" → "Not a valid git branch name."
    - "task/{slug}-{id}", "{id}", "wip/{title}-{id}" → nil
    Validate(KeyWorktreeBase, v):
    - "relative/path" → error "Path must be absolute or start with ~/"
    - an existing FILE path → error "Not a directory: {path}" (raw entered value in the message)
    - nonexistent absolute dir, "~", "~/x", "/abs" → nil (MkdirAll happens at use)
    Validate(KeyShell, v): "bash" → nil; anything else → error "Unknown shell."
    Validate(KeyAgentExtraParams, v): always nil (pass-through field, REQUIREMENTS Out of Scope)
    ExpandTemplate(tpl, slug, id, title):
    - ("task/{slug}-{id}", "fix-login", 42, ...) → "task/fix-login-42"
    - {title} → same sanitizer algorithm as worktree.Slug but capped at 100 chars (research OQ3 resolved: 100); empty result → "task"
    - plain single-pass replacement, no recursion
    Tokenize(s):
    - "--dangerously-skip-permissions" → ["--dangerously-skip-permissions"]
    - `--append-system-prompt "be terse"` → ["--append-system-prompt", "be terse"]
    - `a"b c"d` → ["ab cd"] (mid-token quotes, POSIX-ish — comment this quirk)
    - `"unclosed span` → ["unclosed span"] (LENIENT: rest-of-string is one token — research Pattern 4, no UI-SPEC error copy exists for this field)
    - "" and "   " → nil
  </behavior>
  <action>
    Create `internal/settings/validate.go`:
    - `var AllowedShells = []string{"bash"}` — the SAME slice serves validation here and the GET `options` array in Task 3 (SHELL-02 seam: future shells are data).
    - `func Validate(key, value string) error` — switch on key, rules per the behavior block. Branch-template order is MANDATORY: (1) TrimSpace empty → "Template can't be empty."; (2) token scan with `regexp.MustCompile(`\{[^}]*\}`)`, any group whose inner text is not slug/id/title → fmt.Errorf("Unknown token {%s}. Use {slug}, {id}, or {title}.", tok); (3) `!strings.Contains(tpl, "{id}")` → "Template must include {id} so branch names never collide."; (4) expand with samples (slug "fix-login", id 42, title "fix-login") and run CheckRefFormat on the result → "Not a valid git branch name.". Sample-expansion is sound by the invariance argument (tokens expand to non-empty `[a-z0-9-]`) — cite 06-RESEARCH.md Pattern 5 in a comment.
    - `func CheckRefFormat(ctx context.Context, name string) error` — reject `strings.HasPrefix(name, "-")` first (argv hazard), then `exec.CommandContext(ctx, "git", "check-ref-format", "refs/heads/"+name).Run()`; nonzero → errors.New("Not a valid git branch name."). NEVER use `--branch` (shorthand-expansion semantics — verified anti-pattern). Arg array, never sh -c.
    - Worktree base: trim; require `v == "~"` || `strings.HasPrefix(v, "~/")` || `strings.HasPrefix(v, "/")` else "Path must be absolute or start with ~/"; ExpandHome then os.Stat — exists-but-not-dir → fmt.Errorf("Not a directory: %s", v). Store/compare the RAW value; expansion only here and at use (Pitfall 6).

    Create `internal/settings/template.go`: `func ExpandTemplate(tpl, slug string, id int64, title string) string` — replaces `{slug}`, `{id}` (decimal), `{title}` (sanitizeTitle). Private `sanitizeTitle` reimplements worktree.Slug's loop (lowercase, collapse non-[a-z0-9] runs to '-', trim '-', fallback "task") with a 100-char cap — do NOT import internal/worktree from internal/settings (keep settings dependency-free; the slug ARGUMENT is produced by callers via worktree.Slug).

    Create `internal/settings/tokenize.go`: the quote-aware tokenizer verbatim from 06-RESEARCH.md Pattern 4 (single/double quote spans, no escapes/expansion/globbing, lenient unclosed quote, flush on unicode.IsSpace). Comment the mid-token-quote quirk and the known `--settings`-override sharp edge (user-supplied `--settings` would defeat the hook overlay; SessionStart canary covers it; policing is out of scope).

    All tests table-driven. Validation tests assert EXACT error strings — the frontend mirrors them verbatim (UI-SPEC contract).
  </action>
  <verify>
    <automated>go test ./internal/settings/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fn "Template can't be empty." internal/settings/validate.go` matches
    - `grep -Fn "Use {slug}, {id}, or {title}." internal/settings/validate.go` matches
    - `grep -Fn "Template must include {id} so branch names never collide." internal/settings/validate.go` matches
    - `grep -Fn "Not a valid git branch name." internal/settings/validate.go` matches
    - `grep -Fn "Path must be absolute or start with ~/" internal/settings/validate.go` matches
    - `grep -Fn "Not a directory: %s" internal/settings/validate.go` matches
    - `grep -Fn "Unknown shell." internal/settings/validate.go` matches
    - `grep -n "check-ref-format" internal/settings/validate.go` matches AND `grep -n -- "--branch" internal/settings/validate.go` does NOT match
    - `grep -n "HasPrefix(name, \"-\")" internal/settings/validate.go` matches (leading-dash reject)
    - `grep -rn "internal/worktree" internal/settings/` returns nothing (no import cycle risk)
    - `go test ./internal/settings/...` passes with cases covering every behavior-block row
  </acceptance_criteria>
  <done>Validate/CheckRefFormat/ExpandTemplate/Tokenize implemented with the exact canonical copy and validation order; full table-driven coverage green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settings REST API — GET /api/settings, PUT /api/settings/{key}</name>
  <files>internal/api/settings.go, internal/api/settings_test.go, internal/api/routes.go</files>
  <read_first>internal/api/routes.go (registration pattern), internal/api/respond.go (writeJSON/writeError), internal/api/projects.go (handler-struct + table-driven httptest style), internal/api/projects_test.go (test harness shape), 06-RESEARCH.md Pattern 2</read_first>
  <behavior>
    - GET /api/settings → 200 with all four keys: each `{"value": ..., "default": ...}`; the shell entry additionally `"options": ["bash"]` (from settings.AllowedShells — same slice as validation)
    - PUT /api/settings/branch_template {"value":"task/{slug}-{id}"} → 200 {"value":..., "default":"task/{slug}-{id}"}
    - PUT /api/settings/branch_template {"value":"task/{slugg}"} → 400 {"error":"Unknown token {slugg}. Use {slug}, {id}, or {title}."} and a subsequent GET still shows the previous value (nothing stored)
    - PUT /api/settings/agent_extra_params {"value":""} → 200; GET then shows value "" with default "--dangerously-skip-permissions" (empty ≠ absent)
    - PUT /api/settings/shell {"value":"zsh"} → 400 {"error":"Unknown shell."}
    - PUT /api/settings/nope → 404
    - PUT with invalid JSON body → 400
    - Persistence: after PUT, a direct `SELECT value FROM settings WHERE key=?` on the test DB returns the stored value (SET-02 — survives restart by construction)
  </behavior>
  <action>
    Create `internal/api/settings.go`:
    - `type settingEntry struct { Value string `json:"value"`; Default string `json:"default"`; Options []string `json:"options,omitempty"` }`
    - `func SettingsRoutes(mux *http.ServeMux, db *sql.DB)` registering `GET /api/settings` and `PUT /api/settings/{key}` (Go 1.22 pattern mux, `r.PathValue("key")`).
    - GET: `settings.GetAll(db)`; build `map[string]settingEntry` over the four keys; shell gets `Options: settings.AllowedShells`. DB error → 500 via writeError.
    - PUT: decode `{Value string}`; unknown key (not in settings.Defaults) → 404 writeError "unknown setting"; `settings.Set(db, key, body.Value)` — validation error → 400 with `err.Error()` VERBATIM (the canonical copy rides the existing `{"error": msg}` pipe that web client.ts already extracts — Pitfall 8); success → 200 with the entry (value, default, options for shell). The PUT response MUST include options for shell so the frontend cache replace keeps the dropdown populated.
    - Distinguish validation errors (400) from DB errors (500): have settings.Set return validation errors as-is and wrap storage errors, or perform Validate in the handler before Set — pick one, keep Set's validate-before-write guarantee either way.
    Register in routes.go: add `SettingsRoutes(mux, db)` inside `Routes`.
    Tests: table-driven httptest following projects_test.go conventions; cover every behavior row including the nothing-stored-on-400 assertion.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'Settings' -v && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SettingsRoutes" internal/api/routes.go` matches (registered)
    - `grep -n 'PUT /api/settings/{key}' internal/api/settings.go` and `grep -n 'GET /api/settings' internal/api/settings.go` match
    - `grep -n "AllowedShells" internal/api/settings.go` matches (options served from the validation slice — one source of truth)
    - `go test ./internal/api/ -run Settings` passes; full `go test ./...` stays green (no regression)
    - settings_test.go contains a 400-then-GET case proving invalid templates are never stored
  </acceptance_criteria>
  <done>Settings API live with the Pattern-2 shape; canonical validation copy surfaces through the {"error"} pipe; unknown key 404; full suite green.</done>
</task>

</tasks>

<verification>
- `go build ./... && go test ./...` green.
- Migration count: `ls internal/store/migrations/ | wc -l` = 4.
- All seven canonical error strings grep-able in internal/settings/validate.go exactly as written in 06-UI-SPEC.md.
- No new Go modules: `git diff go.mod` empty (locked stack).
</verification>

<success_criteria>
- Settings persist in SQLite over the API (SET-02): GET/PUT round-trip proven by tests against a migrated DB.
- Defaults live in code, absent row = default, every key restorable by PUTting its default (SET-04 server half).
- The extra-params default is exactly `--dangerously-skip-permissions` (AGENT-02 / D-51 reversal).
- Invalid branch templates are rejected with the canonical copy and never stored (BRANCH-02 save-time half).
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-polish/06-01-SUMMARY.md`
</output>
