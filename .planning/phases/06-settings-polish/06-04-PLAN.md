---
phase: 06-settings-polish
plan: 4
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04, AGENT-01, AGENT-02, WT-01, WT-02, SHELL-01, SHELL-02, BRANCH-01, BRANCH-02, UI-01]

must_haves:
  truths:
    - "make build produces the single binary with the settings page embedded; go test ./... is green"
    - "All seven Phase 6 roadmap success criteria verified live by the user against the built binary"
    - "The amber-under-bypass implication and the carried plan-mode-amber UAT item are explicitly surfaced to the user during verification"
  artifacts:
    - path: "kangent"
      provides: "release binary (vite dist embedded) — built, not committed"
  key_links:
    - from: "web/dist"
      to: "kangent binary"
      via: "make build (vite build → go:embed)"
      pattern: "make build"
---

<objective>
Final phase gate: build the release binary, run the full automated suite plus a verbatim-copy audit, then walk the user through live verification of all seven Phase 6 success criteria — including the documented status-dot implication of the default-on `--dangerously-skip-permissions` and the carried plan-mode-amber UAT item from v1.0.

Purpose: prove the phase goal end-to-end on the real binary before transition.
Output: verified phase; notes for the transition step (D-51 reversal must be logged in PROJECT.md Key Decisions at `/gsd:transition`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-polish/06-RESEARCH.md
@.planning/phases/06-settings-polish/06-UI-SPEC.md
@.planning/phases/06-settings-polish/06-02-SUMMARY.md
@.planning/phases/06-settings-polish/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + test suite + verbatim copy audit</name>
  <files></files>
  <read_first>Makefile (build target), 06-UI-SPEC.md Copywriting Contract (audit checklist), 06-02-SUMMARY.md and 06-03-SUMMARY.md (deviations to spot-check)</read_first>
  <action>
    1. `go test ./...` — full backend suite must pass.
    2. `make build` — vite build → web/dist → go build with embed; must produce the binary cleanly.
    3. Copy audit (Pitfall 4 — the canonical strings must exist verbatim, in Go for server-mirrored errors and in TSX for page chrome):
       ```bash
       grep -Fq "Template can't be empty." internal/settings/validate.go
       grep -Fq "Use {slug}, {id}, or {title}." internal/settings/validate.go
       grep -Fq "Template must include {id} so branch names never collide." internal/settings/validate.go
       grep -Fq "Not a valid git branch name." internal/settings/validate.go
       grep -Fq "Path must be absolute or start with ~/" internal/settings/validate.go
       grep -Fq "Changes apply to the next session or worktree — running sessions are unaffected." web/src/pages/SettingsPage.tsx
       grep -Fq "Couldn't save. Try again." web/src/components/settings/SettingsField.tsx
       grep -Fq "Couldn't load settings." web/src/pages/SettingsPage.tsx
       ```
       Every grep must succeed.
    4. Stack audit: `git diff v1.0-era go.mod` — no new Go modules; `git status web/package.json` — no new npm deps beyond the shadcn-block primitives.
    5. Persistence smoke (SET-02 across a real restart): start the built binary against a scratch DB (`./kangent --db /tmp/kangent-verify.db --addr 127.0.0.1:7333 &`), `curl -s -X PUT 127.0.0.1:7333/api/settings/branch_template -d '{"value":"wip/{slug}-{id}"}'` → expect 200; kill the process; start it again; `curl -s 127.0.0.1:7333/api/settings` → branch_template value is still `wip/{slug}-{id}`. Kill and remove the scratch DB. (Adjust flag names to the actual main.go flags — read them first.)
  </action>
  <verify>
    <automated>go test ./... && make build</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0
    - `make build` exits 0 and the binary exists
    - All eight copy greps exit 0
    - Restart smoke shows the PUT value surviving a process restart (curl output captured in the execution log)
  </acceptance_criteria>
  <done>Binary builds, suite green, canonical copy in place, settings proven to survive a real restart.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — all seven Phase 6 success criteria</name>
  <action>
    Present the verification walkthrough below to the user, with the binary from Task 1 built and ready to run. Do not proceed past this task without the user's explicit response; record the plan-mode-amber observation verbatim in the SUMMARY.
  </action>
  <what-built>
    v1.1 Settings & Polish on the built binary: SQLite-backed global settings (KV table, migration 00004) served over GET/PUT /api/settings; a /settings page (sidebar gear, per-field save, reset-to-default, inline canonical errors); settings wired read-at-use into agent spawn (extra params, default `--dangerously-skip-permissions` — intentional D-51 reversal), bash-tab shell, worktree base, and branch template (validated, both create + Retry paths); and the full-width task-view header (UI-01).
  </what-built>
  <how-to-verify>
    Run the built binary against your real DB (`./kangent`) and open the app. Walk the seven roadmap criteria:

    1. **Settings page + field isolation (SET-01, SET-04):** Click the gear at the bottom of the projects sidebar → full-page Settings route opens, gear shows the active treatment. Type an invalid branch template (e.g. `task/{slug}`) and blur → inline red `Template must include {id} so branch names never collide.` appears under that field only; meanwhile edit the worktree field without committing — the error never disturbs it. Fix the template → `Saved` flash (muted, 2s). Change any field away from its default → `Reset to default` appears on its label row; click it → default restored + flash + button gone. Esc while editing reverts the draft and does NOT navigate.

    2. **Persistence (SET-02):** Change the branch template to `wip/{slug}-{id}`, stop the server, start it again, reload /settings → the value survived.

    3. **Agent params (AGENT-01, AGENT-02, SET-03):** With the DEFAULT extra-params (`--dangerously-skip-permissions`), Start an agent on a task → claude opens straight to the prompt with "⏵⏵ bypass permissions on" in the TUI footer, no permission prompts during tool use. Leave it running. Now CLEAR the extra-params field (empty is valid), and Start an agent on a DIFFERENT task → that one prompts interactively for permissions. The first (running) session is unaffected — no restart happened anywhere. Restore the default afterward via Reset to default.

    4. **Worktree base (WT-01, WT-02):** Note an existing task's worktree path (meta line). Change the base directory to a new location (e.g. `~/kangent-wt-test/`), create a new task → its worktree meta line shows the new base (repo-named subfolder kept). The pre-existing task's path is unchanged and its bash tab still opens in its old worktree. Reset to default when done.

    5. **Branch template (BRANCH-01, BRANCH-02):** With template `wip/{slug}-{id}`, create a task → meta line shows a `wip/...` branch. Then try saving `task/{slugg}` → rejected inline with `Unknown token {slugg}. Use {slug}, {id}, or {title}.` and never used (next task still gets the last valid template). Reset to default.

    6. **Shell (SHELL-01, SHELL-02):** The Bash tabs group shows a Select with `bash` (only option). Open a new bash tab on a task → it runs bash (`echo $SHELL` shows a bash path) — the value came from settings, not a hardcode. Note: previously tabs ran your login $SHELL; bash-always is the specified v1.1 behavior.

    7. **Header width (UI-01):** Open any task → the title row spans the full page width; the three-dots trigger sits flush at the right padding edge, exactly where the tab strip ends. The Description tab's prose column is still comfortably narrow (~860px).

    **Status-dot note (expected, not a bug):** with the default `--dangerously-skip-permissions` active, the amber *waiting* dot rarely fires — permission prompts (its dominant source) no longer happen. Dots will read green (working) / gray (idle/exited) in normal operation. The loud amber board behavior returns when you remove the flag. This is the documented implication of AGENT-02, not a regression.

    **Carried UAT item (v1.0, research OQ1):** while an agent runs WITHOUT the bypass flag (step 3's second session), enter plan mode and reach the exit-plan approval prompt — confirm whether the card/tab dot turns amber. Report what you see either way; no code in this phase depends on the answer, but it closes the carried question.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven criteria pass (include your plan-mode amber observation), or describe any issues found.</resume-signal>
  <done>User confirms all seven success criteria pass live, acknowledges the amber-under-bypass implication, and reports the plan-mode-amber observation.</done>
</task>

</tasks>

<verification>
- All seven roadmap success criteria confirmed live by the user on the built binary.
- The amber-under-bypass implication acknowledged; plan-mode-amber observation recorded for the UAT log.
</verification>

<success_criteria>
- Phase 6 goal verified end-to-end: one settings page drives agent params, worktree placement, branch naming, and shell; changes apply at next spawn/creation with no restart; header is full-width.
- Reminder recorded for `/gsd:transition`: log the D-51 reversal (default-on `--dangerously-skip-permissions`) in PROJECT.md Key Decisions, and record the plan-mode-amber observation against the carried UAT item.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-polish/06-04-SUMMARY.md` (include the user's plan-mode-amber observation and the D-51 transition reminder).
</output>
