---
phase: 06-settings-polish
plan: 3
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - web/src/components/ui/select.tsx
  - web/src/components/ui/label.tsx
  - web/src/api/client.ts
  - web/src/api/settings.ts
  - web/src/components/settings/SettingsField.tsx
  - web/src/pages/SettingsPage.tsx
  - web/src/App.tsx
  - web/src/components/sidebar/ProjectSidebar.tsx
  - web/src/pages/TaskPage.tsx
autonomous: true
requirements: [SET-01, SET-04, AGENT-01, AGENT-02, WT-01, SHELL-01, BRANCH-01, UI-01]

must_haves:
  truths:
    - "A gear control at the bottom of the projects sidebar navigates to a dedicated full-page /settings route (sidebar stays visible) with an active state while on it"
    - "All four settings are editable with per-field commit (blur/Enter for text, change for select), Esc revert, a 2s muted 'Saved' flash, and a 'Reset to default' affordance shown only when value differs from default"
    - "A validation failure shows the server's canonical message inline under that field while every other field's state is untouched (SET-04)"
    - "The shell dropdown options come from the API payload, not a frontend constant (SHELL-02 seam)"
    - "The task-view header block spans the full page width — the ellipsis trigger sits flush at the right padding edge where the tab strip ends (UI-01)"
  artifacts:
    - path: "web/src/api/settings.ts"
      provides: "useSettings + useSaveSetting hooks, SettingEntry type"
      exports: ["useSettings", "useSaveSetting"]
    - path: "web/src/components/settings/SettingsField.tsx"
      provides: "per-field commit state machine (draft, Saved flash, Reset, inline error)"
      min_lines: 60
    - path: "web/src/pages/SettingsPage.tsx"
      provides: "Settings page — heading, sub-line, 4 groups, skeleton/failure states"
      min_lines: 80
    - path: "web/src/components/ui/select.tsx"
      provides: "shadcn select block"
    - path: "web/src/components/ui/label.tsx"
      provides: "shadcn label block"
  key_links:
    - from: "web/src/components/settings/SettingsField.tsx"
      to: "/api/settings/{key}"
      via: "useSaveSetting mutation (client.ts put helper)"
      pattern: "useSaveSetting"
    - from: "web/src/api/settings.ts"
      to: "web/src/api/client.ts"
      via: "new put helper — keeps the ApiError {\"error\"} extraction pipe (Pitfall 8)"
      pattern: "put<"
    - from: "web/src/components/sidebar/ProjectSidebar.tsx"
      to: "/settings"
      via: "Link-wrapped 28px ghost icon button in SidebarFooter"
      pattern: "to=\"/settings\""
    - from: "web/src/App.tsx"
      to: "web/src/pages/SettingsPage.tsx"
      via: "Route path=/settings inside the AppLayout route"
      pattern: "path=\"/settings\""
---

<objective>
Build the settings frontend per the approved 06-UI-SPEC contract: the two sanctioned shadcn blocks (`select`, `label`), a `put` client helper, typed settings hooks, the per-field-commit SettingsField, the SettingsPage (4 groups), the `/settings` route, the sidebar gear — plus the UI-01 task-header width fix.

Purpose: SET-01 (gear → full-page route), SET-04 (per-field isolation, restore-to-default, inline errors), the four field surfaces (AGENT-01/02, WT-01, SHELL-01, BRANCH-01), UI-01.
Output: complete settings UI compiling clean; backend wiring lands in parallel plan 06-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-polish/06-UI-SPEC.md
@.planning/phases/06-settings-polish/06-RESEARCH.md
@.planning/phases/06-settings-polish/06-01-SUMMARY.md

<interfaces>
From web/src/api/client.ts (verified):
```ts
export class ApiError extends Error { /* message = body.error */ }
export function get<T>(path: string): Promise<T>
export function post<T>(path: string, body?: unknown): Promise<T>
export function patch<T>(path: string, body?: unknown): Promise<T>
export function del<T = void>(path: string, body?: unknown): Promise<T>
```

API contract (from 06-01):
```
GET /api/settings → Record<key, {value: string, default: string, options?: string[]}>
  keys: agent_extra_params | worktree_base | shell | branch_template
PUT /api/settings/{key} body {value: string} → 200 same entry shape (shell includes options)
  400 {"error": "<canonical message>"} — nothing stored
```

From web/src/App.tsx (verified): routes nest inside `<Route element={<AppLayout />}>`; existing siblings: index redirect, /projects/:projectId, /projects/:projectId/tasks/:taskId, /terminal.

From web/src/components/sidebar/ProjectSidebar.tsx (verified, lines 93–103): SidebarFooter currently holds only the Add-project ghost Button (variant="ghost" size="sm" className="justify-start").

From web/src/pages/TaskPage.tsx:381 (verified): `<div className="max-w-[860px] shrink-0 space-y-2">` wraps the header row + WorktreeMetaLine. Line 287 Description prose `max-w-[860px]` and the loading (≈186) / error (≈198) states are OUT of UI-01 scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn select + label blocks, client put helper, settings hooks</name>
  <files>web/src/components/ui/select.tsx, web/src/components/ui/label.tsx, web/src/api/client.ts, web/src/api/settings.ts</files>
  <read_first>web/src/api/client.ts (patch helper — put mirrors it exactly), web/src/api/agents.ts + web/src/api/worktrees.ts (hook conventions), web/components.json, 06-RESEARCH.md Pattern 7</read_first>
  <action>
    Run `npx shadcn@latest add select label` in web/ (the ONLY sanctioned new frontend assets — radix-nova preset, components.json verified; no other components, no config changes). If the CLI rewrites unrelated files, revert those hunks.

    Add to web/src/api/client.ts, exactly mirroring patch (Pitfall 8 — never an ad-hoc fetch, or canonical server copy loses the ApiError pipe):
    ```ts
    export function put<T>(path: string, body?: unknown): Promise<T> {
      return api<T>(path, {
        method: "PUT",
        body: body === undefined ? undefined : JSON.stringify(body),
      });
    }
    ```

    Create web/src/api/settings.ts per 06-RESEARCH.md Pattern 7:
    ```ts
    export interface SettingEntry { value: string; default: string; options?: string[] }
    export type Settings = Record<string, SettingEntry>;
    export function useSettings()                 // queryKey ["settings"], get<Settings>("/api/settings")
    export function useSaveSetting(key: string)   // useMutation<SettingEntry, ApiError, string>
    ```
    useSaveSetting: mutationFn `(value) => put<SettingEntry>(`/api/settings/${key}`, { value })`; onSuccess `qc.setQueryData<Settings>(["settings"], (old) => old && { ...old, [key]: entry })`. NO optimistic update (per-field commit holds the draft locally; a 400 leaves the cache untouched).
  </action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls web/src/components/ui/select.tsx web/src/components/ui/label.tsx` both exist
    - `grep -n "export function put<" web/src/api/client.ts` matches; `grep -n 'method: "PUT"' web/src/api/client.ts` matches
    - `grep -n "useSettings\|useSaveSetting" web/src/api/settings.ts` shows both exports; `grep -n "setQueryData" web/src/api/settings.ts` matches
    - `git diff --name-only web/package.json` shows no NEW dependencies beyond what the two blocks require (blocks are copied in; radix select/label primitives are acceptable additions if the CLI adds them — nothing else)
    - `npm --prefix web run build` succeeds
  </acceptance_criteria>
  <done>Blocks present, put helper rides the existing ApiError pipe, typed hooks compile.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsField + SettingsPage + /settings route (verbatim UI-SPEC copy)</name>
  <files>web/src/components/settings/SettingsField.tsx, web/src/pages/SettingsPage.tsx, web/src/App.tsx</files>
  <read_first>06-UI-SPEC.md (Copywriting Contract + Interaction Contract + Layout Contract — the auditor checks copy verbatim), web/src/pages/BoardPage.tsx (load-failure + skeleton patterns), web/src/components/ui/input.tsx, web/src/components/ui/select.tsx (as added in Task 1)</read_first>
  <action>
    Create `web/src/components/settings/SettingsField.tsx` — owns the per-field state machine (06-UI-SPEC Interaction Contract, verbatim):
    - Props: `label`, `entry: SettingEntry`, `help: ReactNode`, `settingKey`, `mono?: boolean`, `control?: "input" | "select"`.
    - Draft state seeded from entry.value; text inputs commit on blur AND Enter; if draft === saved value → no request, no feedback. Esc reverts the draft to the last saved value and keeps focus (field-level only — no global handler exists on /settings, verified). Select commits on change immediately.
    - While in flight: input stays enabled, no spinner. On success: `Saved` flash — 12px muted zinc text right of the label for exactly 2s (setTimeout, cleared on unmount/resave); the response value becomes the new revert target (cache replace from Task 1's onSuccess; sync draft to it).
    - `Reset to default` — 12px muted text button, right-aligned on the label row, rendered ONLY when `entry.value !== entry.default`; click → PUT the default immediately → input shows default + Saved flash; disabled while that field's save is in flight; never red, no confirmation.
    - Errors inline under the control, replacing the help line while present, destructive red 12px: if the failure is an ApiError with a 4xx status → render `error.message` VERBATIM (the canonical server copy); network/5xx → exactly `Couldn't save. Try again.`. Draft is never cleared; the error clears when the draft is edited or on the field's next successful commit. Other fields are never touched (SET-04).
    - Layout per UI-SPEC Field block: label row (14px/500 label + Saved flash + Reset right-aligned) → 4px gap → control → 4px gap → help (12px muted) or error. `mono` puts `font-mono` on the input.
    Keep all contract strings in single template literals so they stay grep-able (Phase 3 convention).

    Create `web/src/pages/SettingsPage.tsx` — Layout Contract: full-page under AppLayout, content column `max-w-[640px]` left-aligned in `p-4`; heading `Settings` (16px/500) + sub-line directly under it (12px muted, xs gap): `Changes apply to the next session or worktree — running sessions are unaffected.`; 24px gap to the first group; groups separated by 24px, no separators/cards. Four groups in order, group labels 12px uppercase tracking-wide muted: `Agent` / `Worktrees` / `Bash tabs` / `Branches`. Fields via SettingsField with EXACT copy:
    - Agent → label `Extra claude parameters`; mono input, full column width, no placeholder (empty is valid = no extra parameters); help: `Appended to every claude spawn, starting with the next Start agent. Remove --dangerously-skip-permissions to restore interactive permission prompts.` (the flag span in mono). NO warning chrome — renders like every other field (D-51 reversal is specified behavior).
    - Worktrees → label `Worktree base directory`; mono input; help: `New task worktrees are created under this directory. Existing worktrees stay where they are.`
    - Bash tabs → label `Shell`; shadcn Select, fixed `w-[240px]`, options mapped from `entry.options` (NEVER a frontend constant — SHELL-02 seam); help: `Used when opening a new bash tab.`
    - Branches → label `Branch name template`; mono input; help: `Tokens: {slug}, {id}, {title}. Applied when a task is created — e.g. task/fix-login-42.` (tokens + example in mono).
    Loading: 4 group-shaped skeleton blocks (label bar + input bar each), no spinners. Load failure: centered `Couldn't load settings.` + `Retry loading` button (refetch) — mirrors the board-failure pattern.

    web/src/App.tsx: add `<Route path="/settings" element={<SettingsPage />} />` as a sibling inside the AppLayout route.
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -Fq "Changes apply to the next session or worktree — running sessions are unaffected." web/src/pages/SettingsPage.tsx && grep -Fq "Couldn't save. Try again." web/src/components/settings/SettingsField.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fn "Extra claude parameters" web/src/pages/SettingsPage.tsx`; `grep -Fn "Worktree base directory" web/src/pages/SettingsPage.tsx`; `grep -Fn "Branch name template" web/src/pages/SettingsPage.tsx`; `grep -Fn "Used when opening a new bash tab." web/src/pages/SettingsPage.tsx` — all match
    - `grep -Fn "Remove --dangerously-skip-permissions to restore interactive permission prompts." web/src/pages/SettingsPage.tsx` matches (AGENT-02 help copy)
    - `grep -Fn "Existing worktrees stay where they are." web/src/pages/SettingsPage.tsx` matches
    - `grep -Fn "Reset to default" web/src/components/settings/SettingsField.tsx` and `grep -Fn ">Saved<" web/src/components/settings/SettingsField.tsx` (or equivalent literal `Saved`) match
    - `grep -Fn "Couldn't load settings." web/src/pages/SettingsPage.tsx` and `grep -Fn "Retry loading" web/src/pages/SettingsPage.tsx` match
    - `grep -n 'path="/settings"' web/src/App.tsx` matches inside the AppLayout route block
    - `grep -n "entry.options\|options?.map\|options.map" web/src/pages/SettingsPage.tsx` matches (options from API data); `grep -n '"bash"' web/src/pages/SettingsPage.tsx` returns nothing (no hardcoded option)
    - No green/amber classes in either new file: `grep -nE "green|amber" web/src/pages/SettingsPage.tsx web/src/components/settings/SettingsField.tsx` returns nothing (Saved is muted zinc; no warning chrome)
    - `npm --prefix web run build` succeeds
  </acceptance_criteria>
  <done>/settings renders the four groups with verbatim contract copy, per-field commit/revert/flash/reset/error isolation, API-driven shell options, skeleton + failure states.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar gear (SET-01) + UI-01 task-header width fix</name>
  <files>web/src/components/sidebar/ProjectSidebar.tsx, web/src/pages/TaskPage.tsx</files>
  <read_first>web/src/components/sidebar/ProjectSidebar.tsx lines 85–110 (current SidebarFooter), web/src/pages/TaskPage.tsx lines 375–395 (header wrapper at line 381) and line 287 (Description prose island — must NOT change), 06-UI-SPEC.md Sidebar gear + UI-01 contracts</read_first>
  <action>
    ProjectSidebar.tsx — SidebarFooter becomes ONE row, 8px gap (`flex items-center gap-2`):
    - Existing `Add project` ghost button: add `flex-1` (keeps `justify-start`, copy and behavior unchanged).
    - New gear flush right: react-router `Link to="/settings"` wrapping a ghost icon Button (`size="icon-sm"`, 28px square — use `asChild` so the Button renders the Link), lucide `<Settings className="size-4" />` (16px), `aria-label="Settings"`, Tooltip `Settings` side right (matching the sidebar-toggle tooltip convention).
    - Active state: when `useLocation().pathname === "/settings"`, apply the selected-sidebar-item treatment (bg-zinc-800/`#27272a` surface + zinc-50 icon) — reuse the existing selected-item classes from the project rows.
    - Collapsed sidebar: no special affordance (offcanvas hides the whole footer — spec'd).

    TaskPage.tsx — UI-01, one targeted change at line 381: `className="max-w-[860px] shrink-0 space-y-2"` → `className="w-full shrink-0 space-y-2"`. Update the adjacent comment (line 379–380) to say header/meta is now full-width per UI-01 while Description prose keeps its 860px island. Do NOT touch: line 287 Description `max-w-[860px]`, the loading skeleton (~186) or error state (~198) max-widths, the header row internals (back button / flex-1 title / `align="end"` ellipsis are already correct for full width).
  </action>
  <verify>
    <automated>npm --prefix web run build && grep -q 'to="/settings"' web/src/components/sidebar/ProjectSidebar.tsx && grep -q 'w-full shrink-0 space-y-2' web/src/pages/TaskPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'aria-label="Settings"' web/src/components/sidebar/ProjectSidebar.tsx` matches; `grep -n "useLocation" web/src/components/sidebar/ProjectSidebar.tsx` matches (active state)
    - `grep -n "flex-1" web/src/components/sidebar/ProjectSidebar.tsx` shows the Add-project button gained flex-1 in the footer row
    - `grep -c "max-w-\[860px\]" web/src/pages/TaskPage.tsx` is exactly ONE LESS than before the change (header wrapper converted; prose island + loading/error states untouched — expect 3 remaining if 4 before)
    - `grep -n 'w-full shrink-0 space-y-2' web/src/pages/TaskPage.tsx` matches
    - `npm --prefix web run build` succeeds
  </acceptance_criteria>
  <done>Gear navigates to /settings with tooltip, aria-label, and active state; task-view header block is full width with the prose island preserved.</done>
</task>

</tasks>

<verification>
- `npm --prefix web run build` clean (tsc + vite).
- Every user-facing settings string is findable verbatim in 06-UI-SPEC.md (Pitfall 4): page sub-line, four field labels, four help lines, `Saved`, `Reset to default`, `Couldn't save. Try again.`, `Couldn't load settings.`, `Retry loading`, gear tooltip `Settings`.
- No new npm dependencies beyond what the two shadcn blocks themselves require.
</verification>

<success_criteria>
- Gear at the sidebar bottom opens the dedicated full-page /settings route; sidebar stays visible; active state shows (SET-01).
- Every setting editable with per-field save, restore-to-default, and inline validation errors that never disturb other fields' in-progress edits (SET-04).
- Extra-params, worktree-base, shell-dropdown, and branch-template fields render per contract with API-driven select options (AGENT-01/02, WT-01, SHELL-01, BRANCH-01 frontend surfaces).
- Task-view header spans full width, menu trigger flush right (UI-01).
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-polish/06-03-SUMMARY.md`
</output>
