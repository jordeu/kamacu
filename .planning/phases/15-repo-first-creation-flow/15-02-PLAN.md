---
phase: 15-repo-first-creation-flow
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - web/src/components/sidebar/AddProjectDialog.tsx
  - web/dist
autonomous: true
requirements: [RPROJ-01, RPROJ-02, RPROJ-03, RPROJ-04, CKOUT-04]
user_setup: []

must_haves:
  truths:
    - "Integration ON → the Add-project dialog shows a 'GitHub repo' | 'Local folder' segmented toggle, defaulting to GitHub repo (repo-first)"
    - "Integration OFF → the dialog is byte-for-byte the current folder-only form (no toggle, no repo field)"
    - "Repo mode prefills the Name from the name segment of the typed owner/name, editable, never clobbering a user edit"
    - "Repo-mode submit POSTs { repo } and shows a blocking 'Cloning <owner/name>…' spinner with inputs disabled, dialog open"
    - "A clone/validate failure surfaces inline in the destructive-alert box, dialog stays open, field values + active mode preserved, no half-created-project copy"
    - "Folder mode is behaviorally unchanged: POSTs { repo_path }, same validateRepoPath path, same error handling"
    - "On success the dialog closes and navigates to /projects/{id}"
    - "web/dist is rebuilt so the Go binary embeds the new dialog"
  artifacts:
    - path: "web/src/components/sidebar/AddProjectDialog.tsx"
      provides: "Repo-first Add-project dialog: integration-gated toggle, repo/folder modes, name prefill, clone spinner, inline destructive-alert error"
      contains: "Cloning"
      min_lines: 150
  key_links:
    - from: "AddProjectDialog.tsx"
      to: "useSettings().github_integration"
      via: "integration-on render gate (=== \"on\")"
      pattern: "github_integration\\?\\.value === \"on\""
    - from: "AddProjectDialog.tsx"
      to: "useCreateProject {repo}"
      via: "repo-mode submit posts the widened body"
      pattern: "repo: "
    - from: "AddProjectDialog.tsx"
      to: "ui/tabs.tsx"
      via: "segmented toggle import"
      pattern: "from \"@/components/ui/tabs\""
---

<objective>
Evolve `web/src/components/sidebar/AddProjectDialog.tsx` from folder-only into the repo-first creation flow (RPROJ-01/02/03/04, CKOUT-04) exactly per the APPROVED 15-UI-SPEC. When `github_integration` is `"on"`: a top segmented toggle "GitHub repo" | "Local folder" (reusing the existing `ui/tabs.tsx`) defaults to GitHub repo; the repo `owner/name` input prefills the editable Name; submit POSTs `{ repo }`, shows a blocking "Cloning <owner/name>…" spinner, and surfaces failures inline in the `ProjectSettingsDialog`-style destructive-alert box with the dialog kept open and values preserved. When integration is `"off"`: the dialog is byte-for-byte the current folder-only form. Then rebuild the embedded SPA (`web/dist`) so the Go binary ships the change.

Purpose: This is the user-facing surface of the milestone — it drives Phase 14's proven atomic `createByRepo` from the UI.
Output: The rewritten `AddProjectDialog.tsx` + a rebuilt `web/dist`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-repo-first-creation-flow/15-CONTEXT.md
@.planning/phases/15-repo-first-creation-flow/15-UI-SPEC.md

<interfaces>
<!-- Extracted from the codebase — use these directly. -->

web/src/components/sidebar/AddProjectDialog.tsx — CURRENT (folder-only) form to evolve. Keep its working parts:
- `sentenceCase(message)` helper (keep verbatim — DO NOT switch the dialog width).
- `DialogContent className="sm:max-w-[480px]"` (KEEP — do not change to sm:max-w-lg).
- folder submit: `createProject.mutateAsync({ repo_path: repoPath.trim(), name: name.trim() || undefined })`.
- error catch: `ApiError` → `sentenceCase`; `if (err.status === 409 && !message.endsWith(".")) message += "."`; else fallback `"Couldn't add the project. Try again."`.
- success: `onOpenChange(false); navigate(`/projects/${project.id}`)`.
- form `className="flex flex-col gap-4"`; field block `className="flex flex-col gap-1.5"`; label `className="text-xs font-medium"`.

web/src/components/sidebar/ProjectSettingsDialog.tsx — the patterns to MIRROR:
- Integration gate: `const { data: settings } = useSettings(); const integrationOn = settings?.github_integration?.value === "on";`
- Destructive-alert box (verbatim classes): `<p className="rounded-md border border-destructive/50 bg-destructive/10 px-3 py-2 text-xs text-destructive">{error}</p>`
- `repoEdited` "don't clobber a user edit" guard (here it's `nameEdited`).
- "Adjust state on prop/open change" reset via `prevOpen` — NO setState-in-effect (the codebase is lint-strict on this; see prevOpen/prevSuggestion block).
- Repo input: `<Input className="font-mono" placeholder="owner/name" .../>`.
- Exactly one of {error | help} renders under the repo field.

web/src/api/mutations.ts — useCreateProject body (widened in Plan 01) is `{ name?; repo_path?; repo? }`.

web/src/api/settings.ts — `useSettings()` → `Settings` (Record<string, { value; default; options? }>).

web/src/components/ui/tabs.tsx — the segmented toggle primitive (default variant = bg-muted pill rail):
```tsx
export { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants }
// <Tabs value={mode} onValueChange={setMode}> as a CONTROLLED segmented control.
// <TabsList aria-label="Project source"> with two flex-1 <TabsTrigger value="repo|folder">.
// Render the input blocks OUTSIDE TabsContent (drive local `mode` state) — contract-compliant.
```

web/src/components/task/DiffTab.tsx — the in-flight spinner idiom (lucide, no spinner lib):
```tsx
import { RefreshCw } from "lucide-react";
<RefreshCw className="size-4 animate-spin motion-reduce:animate-none" />
// Loader2 is the UI-SPEC's named glyph; either Loader2 or RefreshCw at size-4 is contract-compliant.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AddProjectDialog with toggle, repo mode, name prefill, clone spinner, inline error</name>
  <read_first>
    - web/src/components/sidebar/AddProjectDialog.tsx (the dialog to evolve — keep sentenceCase, the 480px width, the folder submit/error/success behavior)
    - web/src/components/sidebar/ProjectSettingsDialog.tsx (MIRROR: integration gate, destructive-alert box classes, nameEdited/repoEdited guard, prevOpen adjust-state-on-change reset with NO setState-in-effect, owner/name mono Input, exactly-one-of {error|help})
    - web/src/components/ui/tabs.tsx (the segmented toggle — Tabs/TabsList/TabsTrigger, default variant, flex-1 triggers)
    - web/src/components/task/DiffTab.tsx (the animate-spin spinner idiom)
    - web/src/api/mutations.ts (the widened useCreateProject body { name?; repo_path?; repo? } from Plan 01)
    - web/src/api/settings.ts (useSettings shape)
    - .planning/phases/15-repo-first-creation-flow/15-UI-SPEC.md (the APPROVED contract — copy strings/tokens/states verbatim from "Copywriting Contract", "Component Contract", "Interaction Contract")
    - .planning/phases/15-repo-first-creation-flow/15-CONTEXT.md (D-01..D-09 LOCKED)
  </read_first>
  <action>
    Rewrite `AddProjectDialog.tsx`. Keep `sentenceCase`, the `DialogContent className="sm:max-w-[480px]"`, the form `flex flex-col gap-4`, the `flex flex-col gap-1.5` field blocks, and the `text-xs font-medium` labels. Add:

    INTEGRATION GATE (D-02, RPROJ-04): `const { data: settings } = useSettings(); const integrationOn = settings?.github_integration?.value === "on";` (mirror ProjectSettingsDialog exactly). When `!integrationOn`, render NO toggle and NO repo field — byte-for-byte the current folder-only form (path Input + Name Input + the destructive-alert error box). The repo-first branch must not mount.

    STATE: `mode` ("repo" default | "folder"), `ownerName` (repo input), `repoPath` (folder input), `name`, `nameEdited` (don't-clobber guard), `error`. Use `useCreateProject` (per D-05, RPROJ-02 — Plan 01 widened it) and `useNavigate`.

    SEGMENTED TOGGLE (D-01) — integration ON only: render at the TOP of the form a controlled `<Tabs value={mode} onValueChange={(v) => { setMode(v as "repo"|"folder"); setError(null); }}>` with `<TabsList aria-label="Project source" className="w-full">` containing two `flex-1` triggers — `<TabsTrigger value="repo">GitHub repo</TabsTrigger>` (DEFAULT, leftmost) and `<TabsTrigger value="folder">Local folder</TabsTrigger>`. Render the input blocks OUTSIDE TabsContent, gated on `mode`. Switching clears the error.

    REPO MODE INPUT (D-01, D-04): field label `Repository`; `<Input className="font-mono" placeholder="owner/name" value={ownerName} .../>`. Help line (muted, under field, only when no error): `Enter a GitHub repo to clone — owner/name.` (owner/name visually mono is fine to leave as plain text in the help). Exactly one of {error | help} shows.

    FOLDER MODE INPUT (D-03 — unchanged): field label `Repository path`; `<Input className="font-mono" placeholder="/home/you/code/my-repo" value={repoPath} .../>`.

    NAME FIELD (shared, both modes): label `Name`; placeholder `Defaults to repo name` in repo mode, `Defaults to folder name` in folder mode; `onChange` sets `nameEdited` true then `setName(...)`.

    NAME PREFILL (D-04, RPROJ-02): in repo mode, when the user types a valid-looking `owner/name` (a local string parse — split on "/", take the segment after the last "/"; NO gh/network call, D-09) and `nameEdited` is false, derive `name` from the name segment. Use the project's adjust-state-on-change idiom (a `prevOwnerName` tracker, like ProjectSettingsDialog's `prevSuggestion`) — NO setState-in-effect. Never clobber once `nameEdited` is true.

    SUBMIT (D-06, D-08, CKOUT-04): on submit, `setError(null)`; then in repo mode POST `{ repo: ownerName.trim(), name: name.trim() || undefined }`; in folder mode POST `{ repo_path: repoPath.trim(), name: name.trim() || undefined }` (unchanged shape). On success: `onOpenChange(false)`, clear fields, `navigate(`/projects/${project.id}`)`.

    CLONE-IN-PROGRESS (D-06): while `createProject.isPending`, disable ALL inputs and the toggle; in repo mode show the blocking affordance — a `Loader2` (or `RefreshCw`) glyph `className="size-4 animate-spin motion-reduce:animate-none"` in `text-muted-foreground` beside the copy `Cloning {ownerName.trim()}…` (use the U+2026 ellipsis character `…`, sentence case, the `{owner/name}` portion in `font-mono`). Keep the dialog OPEN. No cancel button. Folder mode keeps the plain disable-while-pending (no "Cloning" copy).

    FAILURE (D-07, D-09): catch `ApiError` → `sentenceCase`; `if (err.status === 409 && !message.endsWith(".")) message += "."`; else fallback `"Couldn't add the project. Try again."`. Render in the destructive-alert box (verbatim classes from ProjectSettingsDialog: `rounded-md border border-destructive/50 bg-destructive/10 px-3 py-2 text-xs text-destructive`) — this REPLACES the current plain `text-xs text-destructive` line in BOTH modes. Keep dialog OPEN, preserve fields + active mode, re-enable inputs. The copy NEVER implies a "half-created" project (Phase-14 atomicity — truthful-atomicity rule). NO red input border (the box is the signal).

    PRIMARY CTA: `Add project` disabled when `createProject.isPending` OR the active mode's required input is empty (repo mode: `ownerName.trim() === ""`; folder mode: `repoPath.trim() === ""`).

    RESET ON OPEN (lint-clean idiom): mirror ProjectSettingsDialog's `prevOpen` adjust-state-on-change block — on open default `mode` to "repo" (when integrationOn; else folder is the only mode), clear inputs, clear `nameEdited`, clear error. NO `useEffect` setState.

    Copy every string verbatim from 15-UI-SPEC "Copywriting Contract". Do NOT invent new strings or tokens. Reuse `ui/tabs.tsx` — DO NOT add a new dependency or new `ui/` file.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npx tsc --noEmit && npx eslint src/components/sidebar/AddProjectDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'github_integration?.value === "on"' web/src/components/sidebar/AddProjectDialog.tsx` matches (RPROJ-04 gate)
    - `grep -n 'from "@/components/ui/tabs"' web/src/components/sidebar/AddProjectDialog.tsx` matches (toggle reuse, no new ui/ file)
    - `grep -n '>GitHub repo<' web/src/components/sidebar/AddProjectDialog.tsx` and `grep -n '>Local folder<' web/src/components/sidebar/AddProjectDialog.tsx` both match (toggle labels verbatim)
    - `grep -n 'aria-label="Project source"' web/src/components/sidebar/AddProjectDialog.tsx` matches
    - `grep -n 'placeholder="owner/name"' web/src/components/sidebar/AddProjectDialog.tsx` matches AND the repo Input has `className="font-mono"`
    - `grep -n "Cloning" web/src/components/sidebar/AddProjectDialog.tsx` matches and the spinner copy ends with the … ellipsis character
    - `grep -n "animate-spin motion-reduce:animate-none" web/src/components/sidebar/AddProjectDialog.tsx` matches AND `grep -nE "Loader2|RefreshCw" web/src/components/sidebar/AddProjectDialog.tsx` matches
    - `grep -n "border border-destructive/50 bg-destructive/10 px-3 py-2 text-xs text-destructive" web/src/components/sidebar/AddProjectDialog.tsx` matches (mirrored alert box)
    - `grep -n "repo: ownerName.trim()" web/src/components/sidebar/AddProjectDialog.tsx` matches (repo-mode body) AND `grep -n "repo_path: repoPath.trim()" web/src/components/sidebar/AddProjectDialog.tsx` matches (folder-mode body unchanged)
    - `grep -n 'sm:max-w-\[480px\]' web/src/components/sidebar/AddProjectDialog.tsx` matches (width unchanged)
    - `grep -n "nameEdited" web/src/components/sidebar/AddProjectDialog.tsx` matches (don't-clobber guard) AND `grep -n "prevOpen" web/src/components/sidebar/AddProjectDialog.tsx` matches (no-setState-in-effect reset)
    - `grep -c "useEffect" web/src/components/sidebar/AddProjectDialog.tsx` returns 0 (lint-clean reset idiom)
    - `cd web && npx tsc --noEmit` exits 0 and `npx eslint src/components/sidebar/AddProjectDialog.tsx` exits 0 (no new lint advisories)
  </acceptance_criteria>
  <done>The dialog renders the toggle + repo mode when integration is on (folder-only when off), prefills + protects the Name, shows the blocking Cloning spinner on repo submit, surfaces failures inline in the boxed destructive alert with the dialog kept open, and keeps the folder path byte-for-byte; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the embedded SPA so the Go binary ships the dialog</name>
  <read_first>
    - web/package.json (the build script — `npm run build` → vite build → web/dist)
    - CLAUDE.md (the embedded-SPA single-binary pattern: `//go:embed dist`; go build must ship the change — Phases 10/13 rebuilt dist as an explicit task)
  </read_first>
  <action>
    Rebuild the embedded SPA so the Go binary embeds the new dialog: `cd web && npm run build`. This regenerates `web/dist` (the `//go:embed dist` output the Go server serves). Then confirm the whole binary still builds with the embedded asset: `go build ./...`. Stage the regenerated `web/dist` so the change is committed with the source (Phase 10/13 precedent). Do not hand-edit anything under `web/dist` — it is generated.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent/web && npm run build && cd /home/jordi/workspace/github/kangent && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0
    - `web/dist/index.html` exists (`test -f web/dist/index.html`)
    - a freshly built JS bundle under `web/dist/assets/` contains the new dialog string: `grep -rl "Cloning" web/dist/assets/ | head -1` matches at least one file
    - `go build ./...` exits 0 (the embedded `dist` compiles into the binary)
  </acceptance_criteria>
  <done>web/dist is regenerated from the new source, contains the new dialog ("Cloning" present in the bundle), and `go build ./...` embeds it cleanly.</done>
</task>

</tasks>

<verification>
- `cd web && npx tsc --noEmit` and `npx eslint src/components/sidebar/AddProjectDialog.tsx` exit 0
- `cd web && npm run build` exits 0 and `web/dist` is regenerated
- `go build ./...` exits 0 with the embedded dist
- The dialog honors every D-01..D-09 decision and copies the 15-UI-SPEC strings verbatim
</verification>

<success_criteria>
- RPROJ-01: integration ON → toggle defaults to "GitHub repo" (repo-first default)
- RPROJ-02: repo mode prefills the editable Name from owner/name; description auto-capture rides Plan 01's backend (no dialog field)
- RPROJ-03: "Local folder" is the optional alternative under the toggle when integration is on
- RPROJ-04: integration OFF → folder-only, byte-for-byte the current dialog, no repo-first UI
- CKOUT-04: clone/validate failure surfaces inline (boxed alert), dialog stays open, values preserved, no half-created-project copy
- The embedded SPA is rebuilt so the single Go binary ships the change
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-first-creation-flow/15-02-SUMMARY.md`
</output>
