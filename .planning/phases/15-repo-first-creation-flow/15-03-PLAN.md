---
phase: 15-repo-first-creation-flow
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: []
autonomous: false
requirements: [RPROJ-01, RPROJ-02, RPROJ-03, RPROJ-04, CKOUT-04]
user_setup: []

must_haves:
  truths:
    - "A human confirms the repo-first Add-project flow works end-to-end in the running app"
    - "With integration on, the toggle defaults to GitHub repo, repo create clones + navigates, name prefills, folder is the optional alternative"
    - "With integration off, the dialog is the folder-only form (no toggle, no repo field)"
    - "A bad repo surfaces inline with no half-created project"
  artifacts: []
  key_links: []
---

<objective>
Final human-verify gate for the repo-first creation flow (RPROJ-01/02/03/04, CKOUT-04). This is a user-facing UI phase (Phases 11/12/13 ended with a running-app human-verify gate); after the automated work in Plans 01–02, a human runs the real app and confirms each observable behavior before the phase is marked complete.

Purpose: Code verification (grep/test) proves structure; only a human in the running app can confirm the toggle reads right, the clone spinner feels right, and the failure copy is truthful.
Output: Human sign-off (or a list of issues to feed `--gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-repo-first-creation-flow/15-UI-SPEC.md
@.planning/phases/15-repo-first-creation-flow/15-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify the repo-first Add-project flow in the running app</name>
  <action>
    This is a blocking human-verify checkpoint — no code is written. Build and run the single binary (the embedded dist is what ships), then walk the human through the nine CONFIRM steps in `<how-to-verify>` below and wait for the resume signal. Pause execution until the human types "approved" or files issues.
  </action>
  <verify>
    <automated>cd /home/jordi/workspace/github/kangent && go build ./...</automated>
  </verify>
  <done>The human has exercised every CONFIRM step in the running app and typed "approved" (or filed issues for `--gaps`).</done>
  <what-built>
    The Add-project dialog now (Plans 01–02): with GitHub integration ON, shows a "GitHub repo" | "Local folder" segmented toggle defaulting to GitHub repo; the repo `owner/name` input prefills the editable Name; submit clones via the Phase-14 atomic create path showing a blocking "Cloning <owner/name>…" spinner; failures surface inline in a destructive-alert box with the dialog kept open and values preserved; success navigates to the new project. With integration OFF, the dialog is the unchanged folder-only form. The backend auto-captures the GitHub description into the project (visible later in Project settings). The embedded SPA is rebuilt.
  </what-built>
  <how-to-verify>
    Build + run the single binary (the embedded dist is what ships):
    1. From the repo root: `go build -o /tmp/kangent . && /tmp/kangent` (or the project's usual run command — `make build` then run the binary). Open http://localhost (the app's local port).

    With GitHub integration ON (the default — confirm in Settings; `gh` must be authenticated):
    2. Click "Add project". CONFIRM: a segmented toggle "GitHub repo | Local folder" sits at the top, with "GitHub repo" selected by default (RPROJ-01). The repo input shows the `owner/name` placeholder in mono.
    3. Type a real repo you can access, e.g. `owner/name`. CONFIRM: the Name field prefills with the repo's name segment (e.g. `nf-aggregate`), editable (RPROJ-02). Edit the Name, then keep typing in the repo field — CONFIRM your edited Name is NOT clobbered.
    4. Submit. CONFIRM: inputs disable, a muted spinner + "Cloning <owner/name>…" shows, the dialog stays open during the clone (D-06), then on success the dialog closes and the app navigates to the new project (RPROJ-02/D-08).
    5. Open that project's Settings. CONFIRM: the Description field is prefilled with the GitHub repo's description (auto-captured, RPROJ-02/D-05) — or empty if the repo has none (still fine).
    6. Click "Add project" again, switch the toggle to "Local folder". CONFIRM: the input swaps to the folder-path field; adding a local git folder still works exactly as before (RPROJ-03).
    7. Click "Add project", in repo mode enter a bad/inaccessible repo (e.g. `octocat/this-does-not-exist-xyz`) and submit. CONFIRM: an inline destructive-alert box shows the server reason, the dialog STAYS OPEN, your typed values are preserved, and the copy does NOT claim a "half-created" project to clean up (CKOUT-04/D-07). Verify no orphan project appears in the sidebar and no stray dir under `~/.kangent/repos/`.
    8. Re-add a repo you already added. CONFIRM: a "…already added." message (409, trailing period) shows inline.

    With GitHub integration OFF (toggle it off in Settings):
    9. Click "Add project". CONFIRM: NO toggle and NO repo field appear — it is the folder-only form (Repository path + Name), byte-for-byte as before v1.4 (RPROJ-04/D-02). Adding a folder still works.
  </how-to-verify>
  <resume-signal>Type "approved" if every CONFIRM passes, or describe the specific issues (which step, what was wrong) so they can be closed via `/gsd:plan-phase 15 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
- Human confirms all nine CONFIRM steps in the running, freshly-built binary
- Integration on/off both behave per the UI-SPEC
- A failed clone leaves no orphan project row and no partial directory
</verification>

<success_criteria>
- A human has exercised the repo-first create, name prefill, description auto-capture, folder fallback, integration-off folder-only, and inline-failure paths in the running app and approved them (or filed issues for `--gaps`)
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-first-creation-flow/15-03-SUMMARY.md`
</output>
