---
phase: 20-kamacu-rebrand-brand
plan: 03
type: execute
wave: 2
depends_on: [20-02]
files_modified:
  - web/src/components/sidebar/ProjectSidebar.tsx
  - web/src/App.tsx
  - web/src/components/sidebar/ProjectMenu.tsx
  - web/src/pages/SettingsPage.tsx
  - web/src/api/types.ts
  - web/src/api/sessions.ts
autonomous: true
requirements: [REBRAND-01, REBRAND-03, BRAND-01]
must_haves:
  truths:
    - "The expanded sidebar header shows the KamacuMark ember spark plus the 'Kamacu' wordmark"
    - "The collapsed icon rail shows the KamacuMark at the top of the rail"
    - "No user-facing 'Kangent' copy remains in the app (dialogs, empty states, settings help)"
    - "The kangent.* / kangent:* localStorage keys and the ~/.kangent path comment token are left unchanged (Phase 21)"
  artifacts:
    - path: "web/src/components/sidebar/ProjectSidebar.tsx"
      provides: "Sidebar brand lockup (mark + Kamacu wordmark, expanded + rail)"
      contains: "KamacuMark"
  key_links:
    - from: "web/src/components/sidebar/ProjectSidebar.tsx"
      to: "web/src/components/brand/KamacuMark.tsx"
      via: "import + render in SidebarHeader"
      pattern: "import.*KamacuMark.*from \"@/components/brand/KamacuMark\""
---

<objective>
Render the Kamacu brand in the app and finish the user-facing copy rename: rework the sidebar `SidebarHeader` to show the `KamacuMark` ember spark (in both the expanded header and the collapsed icon rail) plus the "Kamacu" wordmark, and rename the remaining visible "Kangent" strings in dialogs, empty states, and settings help. This delivers the in-app slice of BRAND-01 (mark rendered in the UI), the sidebar-brand + copy slice of REBRAND-01, and the frontend half of REBRAND-03.

Purpose: This is where the user actually sees the rebrand. It consumes the `KamacuMark` contract created in Plan 02.
Output: A sidebar header lockup (mark + "Kamacu", expanded and collapsed) and zero remaining user-facing "Kangent" copy — while the `kangent.*`/`kangent:*` localStorage keys and the `~/.kangent` path comment token stay untouched (Phase 21).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md
@.planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md

@web/src/components/sidebar/ProjectSidebar.tsx
@web/src/components/brand/KamacuMark.tsx

<interfaces>
<!-- KamacuMark contract from Plan 02 (20-02). Consume directly — do not re-derive. -->
From web/src/components/brand/KamacuMark.tsx:
```tsx
function KamacuMark({ className, ...props }: React.ComponentProps<"svg">): JSX.Element
export { KamacuMark };
```
Import as: `import { KamacuMark } from "@/components/brand/KamacuMark";`

<!-- Collapsible dual-render idiom already used in this file (ProjectSidebar.tsx). -->
- Expanded-only element: add class `group-data-[collapsible=icon]:hidden` (see line 48, line 116).
- Collapsed-rail-only element: `className="hidden group-data-[collapsible=icon]:flex"` (see line 95).
- Rail avatar sizing precedent: ProjectAvatar rail = `size-6`, inline = `size-5`.

<!-- Current header to replace (ProjectSidebar.tsx:47-50): -->
```tsx
<SidebarHeader className="flex-row items-center justify-between group-data-[collapsible=icon]:justify-center">
  <span className="px-1 text-sm font-medium group-data-[collapsible=icon]:hidden">
    kangent
  </span>
  {/* SidebarTrigger + Tooltip at lines 51-56 — KEEP */}
```
</interfaces>

<constraints>
KEEP-OUT (D-14 — these are Phase 21 runtime keys, do NOT touch even though they are in files this plan edits or nearby):
- `web/src/components/layout/AppLayout.tsx:8` — `SIDEBAR_STORAGE_KEY = "kangent.sidebar"` (NOT edited by this plan; do not touch)
- `web/src/components/layout/ActiveSessionsBar.tsx:33` — `"kangent:sessions-bar-collapsed"` (not edited by this plan)
- `web/src/components/board/ReviewColumn.tsx:56` — `` `kangent:review-collapsed:${projectId}` `` (not edited by this plan)
- `web/src/api/types.ts:19` — the path token `~/.kangent/repos/` inside the comment: rename ONLY the word "Kangent" on line 18; leave the `~/.kangent/repos/` path token on line 19 verbatim.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the sidebar header into the Kamacu brand lockup</name>
  <files>web/src/components/sidebar/ProjectSidebar.tsx</files>
  <read_first>
    - web/src/components/sidebar/ProjectSidebar.tsx (lines 46-57: current `SidebarHeader`; lines 95 & 116: the collapsible dual-render idiom to mirror)
    - web/src/components/brand/KamacuMark.tsx (the component to import and render — from Plan 02)
    - .planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md § "Sidebar brand lockup (D-07, D-08, D-09)"
  </read_first>
  <action>
    Import `KamacuMark` from `@/components/brand/KamacuMark` and rework the `SidebarHeader` (ProjectSidebar.tsx:47-57). Replace the lowercase `kangent` `<span>` (lines 48-50) with the brand lockup: render `<KamacuMark />` sized for the rail (mirror ProjectAvatar's `size-5`/`size-6` precedent — e.g. `className="size-5"`) shown in BOTH states (D-08: the mark sits at the top of the ~3rem collapsed rail, above the project avatars), plus a `<span>` wordmark reading `Kamacu` (title case, D-09) that is expanded-only via `group-data-[collapsible=icon]:hidden` (D-07). KEEP the existing `SidebarTrigger` + `Tooltip` block (lines 51-56) intact. Preserve the header's flex/justify classes so the layout still centers the mark when collapsed. Do NOT touch `AppLayout.tsx` or its `kangent.sidebar` storage key.
  </action>
  <verify>
    <automated>cd web && grep -q 'import { KamacuMark } from "@/components/brand/KamacuMark"' src/components/sidebar/ProjectSidebar.tsx && grep -q '<KamacuMark' src/components/sidebar/ProjectSidebar.tsx && grep -q 'Kamacu' src/components/sidebar/ProjectSidebar.tsx && ! grep -qi '>[[:space:]]*kangent[[:space:]]*<' src/components/sidebar/ProjectSidebar.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/sidebar/ProjectSidebar.tsx` imports `KamacuMark` from `@/components/brand/KamacuMark` and renders `<KamacuMark`
    - The header contains the wordmark text `Kamacu` (title case) inside an expanded-only element carrying `group-data-[collapsible=icon]:hidden`
    - The old lowercase `kangent` brand `<span>` text is gone: `grep -qi '>[[:space:]]*kangent[[:space:]]*<' src/components/sidebar/ProjectSidebar.tsx` returns nothing
    - The `SidebarTrigger` + toggle `Tooltip` block is still present
    - `npm run build` and `npm run lint` both exit 0
    - Human-verify (deferred to 20-04): expanded header shows mark + "Kamacu"; collapsed rail shows the mark at the top above the avatars.
  </acceptance_criteria>
  <done>The sidebar header renders the KamacuMark in both states plus the "Kamacu" wordmark expanded-only; the old lowercase brand text is removed; the toggle is preserved; build + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Rename remaining user-facing "Kangent" copy</name>
  <files>web/src/App.tsx, web/src/components/sidebar/ProjectMenu.tsx, web/src/pages/SettingsPage.tsx, web/src/api/types.ts, web/src/api/sessions.ts</files>
  <read_first>
    - web/src/App.tsx (line 27: empty-board copy)
    - web/src/components/sidebar/ProjectMenu.tsx (line 95: delete-confirmation copy)
    - web/src/pages/SettingsPage.tsx (line 43: GITHUB_INTEGRATION_HELP)
    - web/src/api/types.ts (lines 18-19: comment word vs KEEP-OUT path token) and web/src/api/sessions.ts (lines 14, 16: comments)
    - .planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md § "User-facing copy renames" and § "Keep-Out Boundary"
  </read_first>
  <action>
    Rename the product name "Kangent" → "Kamacu" at these exact verified sites: `web/src/App.tsx:27` ("Point Kangent at a local git repository to get a board." → "Point Kamacu at …"); `web/src/components/sidebar/ProjectMenu.tsx:95` ("…will be removed from Kangent. …" → "…removed from Kamacu. …"); `web/src/pages/SettingsPage.tsx:43` (`GITHUB_INTEGRATION_HELP` "…across Kangent. …" → "…across Kamacu. …"); `web/src/api/types.ts:18` (comment "…true when Kangent cloned and owns the checkout under" → "…when Kamacu cloned…" — rename ONLY the word on line 18; leave the `~/.kangent/repos/` path token on line 19 verbatim, it is a Phase 21 keep-out); `web/src/api/sessions.ts:14` ("Kangent-initiated stop" → "Kamacu-initiated stop") and `:16` ("outlived a Kangent restart" → "outlived a Kamacu restart"). Do NOT touch any `kangent.*`/`kangent:*` localStorage key or any `~/.kangent` path token.
  </action>
  <verify>
    <automated>cd web && ! grep -rn 'Kangent' src/App.tsx src/components/sidebar/ProjectMenu.tsx src/pages/SettingsPage.tsx src/api/sessions.ts && grep -c 'Kamacu' src/App.tsx src/pages/SettingsPage.tsx && grep -q '~/.kangent/repos/' src/api/types.ts && ! grep -q 'when Kangent cloned' src/api/types.ts && grep -q 'kangent.sidebar' src/components/layout/AppLayout.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Kangent' web/src/App.tsx web/src/components/sidebar/ProjectMenu.tsx web/src/pages/SettingsPage.tsx web/src/api/sessions.ts` finds ZERO matches
    - `web/src/api/types.ts` line 18 says "Kamacu cloned" (no "Kangent") AND line 19 still contains the `~/.kangent/repos/` path token verbatim (`grep -q '~/.kangent/repos/' web/src/api/types.ts`)
    - Keep-out preserved: `grep -q 'kangent.sidebar' web/src/components/layout/AppLayout.tsx`; `grep -q 'kangent:sessions-bar-collapsed' web/src/components/layout/ActiveSessionsBar.tsx`; `grep -q 'kangent:review-collapsed' web/src/components/board/ReviewColumn.tsx`
    - `npm run build` and `npm run lint` both exit 0
  </acceptance_criteria>
  <done>Every user-facing "Kangent" string is now "Kamacu"; the `types.ts` path token and all localStorage keys are untouched; build + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localStorage | The sidebar/collapse/panel state persists under `kangent.*`/`kangent:*` keys; a cosmetic copy rename must not touch these runtime keys or existing installs lose their persisted UI state (Phase 21 owns that migration). |
| DB path comment ↔ runtime path | `types.ts` documents the real `~/.kangent/repos/` path; renaming the path token (vs the product word) would misdescribe live behavior — the token is keep-out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | `kangent.*` / `kangent:*` localStorage keys (D-14) | mitigate | This plan does not edit `AppLayout.tsx`, `ActiveSessionsBar.tsx`, or `ReviewColumn.tsx`; copy renames are word-only at the 6 enumerated sites; acceptance criteria assert the three storage keys are still present after the change. |
| T-20-06 | Tampering | `~/.kangent/repos/` path token in `types.ts` comment | mitigate | Rename only the product word "Kangent" on line 18; acceptance criterion asserts the `~/.kangent/repos/` token on line 19 is still present verbatim. |
| T-20-SC | Tampering | package installs (npm/pip/cargo) | accept | No new dependencies added — this is an in-app copy + brand-lockup rendering change over existing components. No install tasks, so no supply-chain surface. |
</threat_model>

<verification>
- `ProjectSidebar.tsx` imports and renders `KamacuMark` in both states plus the "Kamacu" wordmark; old lowercase brand text removed.
- No user-facing "Kangent" copy remains in the four edited copy files.
- `types.ts` renames the word but keeps the `~/.kangent/repos/` path token; all three localStorage keep-out keys intact.
- `cd web && npm run build && npm run lint` both green.
- Visual confirmation of the lockup (expanded + collapsed rail) is deferred to the 20-04 human-verify checkpoint.
</verification>

<success_criteria>
The sidebar header renders the Kamacu ember mark and wordmark in both expanded and collapsed states, no user-facing "Kangent" copy remains, and every localStorage key and `~/.kangent` path token is left unchanged; the frontend builds and lints cleanly.
</success_criteria>

<output>
Create `.planning/phases/20-kamacu-rebrand-brand/20-03-SUMMARY.md` when done.
</output>
