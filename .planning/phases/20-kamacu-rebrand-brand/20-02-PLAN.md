---
phase: 20-kamacu-rebrand-brand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/brand/KamacuMark.tsx
  - web/public/favicon.svg
  - web/index.html
autonomous: true
requirements: [REBRAND-01, BRAND-01, BRAND-02]
must_haves:
  truths:
    - "A reusable KamacuMark ember-spark SVG React component exists and is importable via @/components/brand/KamacuMark"
    - "The ember mark is a multi-tone glow/spark shape, visually distinct from the plain amber `bg-amber-400` waiting dot"
    - "The browser tab shows the Kamacu favicon (a spark-derived SVG served at /favicon.svg)"
    - "The browser tab title reads Kamacu"
  artifacts:
    - path: "web/src/components/brand/KamacuMark.tsx"
      provides: "Ember-spark brand mark as an inline-SVG React component (named export KamacuMark)"
      contains: "export { KamacuMark }"
      min_lines: 20
    - path: "web/public/favicon.svg"
      provides: "Spark-derived favicon served at site root"
      contains: "<svg"
    - path: "web/index.html"
      provides: "Kamacu tab title + favicon link"
      contains: "<title>Kamacu</title>"
  key_links:
    - from: "web/index.html"
      to: "web/public/favicon.svg"
      via: "<link rel=\"icon\">"
      pattern: "rel=\"icon\".*href=\"/favicon.svg\""
---

<objective>
Create the Kamacu brand assets and wire the browser-tab identity: a reusable ember-spark SVG React component (`KamacuMark`), a spark-derived `favicon.svg`, and the `web/index.html` edits that set the tab title to "Kamacu" and link the favicon. This delivers BRAND-01 (new logo SVG), BRAND-02 (favicon), and the tab-title slice of REBRAND-01.

Purpose: These are the net-new visual assets the whole rebrand hangs on. `KamacuMark` is consumed by the sidebar lockup in the next plan, so it must exist and export a clean contract first.
Output: `KamacuMark.tsx` (importable inline-SVG component), `favicon.svg` (served at `/favicon.svg`, embedded via the existing `web/embed.go` `//go:embed all:dist`), and an `index.html` that titles the tab "Kamacu" and references the favicon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md
@.planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md

@web/src/components/ui/ProjectAvatar.tsx
@web/src/components/StatusDot.tsx
@web/index.html
@web/embed.go

<interfaces>
<!-- The component idiom KamacuMark must follow (from ProjectAvatar.tsx, StatusDot.tsx). -->
<!-- Executor should mirror these conventions — no codebase exploration needed. -->

Conventions pinned by existing code:
- Named export (not default): `export { KamacuMark }`.
- `import { cn } from "@/lib/utils"` for class merge; spread `...props`.
- Sizing via Tailwind `size-*` classes on the element (ProjectAvatar uses `size-6` rail / `size-5` inline).
- Accessibility: `role="img"` + `aria-label="Kamacu"` (matches StatusDot.tsx:59-61, ProjectAvatar.tsx:54).
- No `.svg` file imports exist anywhere in web/src — all icons are inline-SVG React components (lucide idiom). KamacuMark MUST be an inline-SVG component, NOT an imported .svg.
- Import alias `@/*` → `./src/*` (tsconfig.app.json + vite.config.ts).

KamacuMark contract (consumed by 20-03 sidebar lockup):
```tsx
interface KamacuMarkProps extends React.ComponentProps<"svg"> {
  className?: string;
}
function KamacuMark({ className, ...props }: KamacuMarkProps): JSX.Element
export { KamacuMark };
```

D-04 DIFFERENTIATION REFERENCE (the mark MUST look clearly different from these):
- StatusDot.tsx:20-21 waiting dot: `bg-amber-400 animate-pulse motion-reduce:animate-none` (a flat 8px amber circle).
- ProjectAvatar.tsx:68 rail waiting overlay: `size-2 rounded-full bg-amber-400`.
A flat single-hue amber circle IS the waiting dot. The mark must be a multi-tone glow/spark
(e.g. gradient orange-500 → amber-300, radial glow, spark/wisp silhouette) — never a plain amber dot.
There is no `--ember` theme token; the ember hue lives inside the SVG (gradient stops/fills) or as
`text-orange-*`/`text-amber-*` utilities.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the KamacuMark ember-spark SVG component</name>
  <files>web/src/components/brand/KamacuMark.tsx</files>
  <read_first>
    - web/src/components/ui/ProjectAvatar.tsx (the component idiom to copy: named export, `cn()`, `...props`, `size-*`, `role="img"` + `aria-label`)
    - web/src/components/StatusDot.tsx (lines 20-21: the exact `bg-amber-400 animate-pulse` waiting dot the mark must NOT resemble — D-04)
    - .planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md § "Logo / brand mark (D-01…D-05)" and § "Claude's Discretion"
  </read_first>
  <action>
    Create `web/src/components/brand/KamacuMark.tsx` as an inline-SVG React component named `KamacuMark` (named export). Follow the ProjectAvatar idiom: `import * as React from "react"`, `import { cn } from "@/lib/utils"`, props `interface KamacuMarkProps extends React.ComponentProps<"svg">`, destructure `{ className, ...props }`, render an `<svg viewBox="0 0 24 24" role="img" aria-label="Kamacu" className={cn(<default size + ember classes>, className)} {...props}>`. The geometry is an ABSTRACT ember spark / wisp / comet — NOT a wordmark, NOT a 'K' monogram, NOT a literal animal or a face (no eyes) — per D-01. Render it as a multi-tone warm-ember glow (D-03): use an SVG `<radialGradient>` or `<linearGradient>` (e.g. orange-500 → amber-300 stops) and/or a soft glow so it reads as a fuller glowing shape, explicitly distinct from the flat `bg-amber-400` waiting dot (D-04 — do a side-by-side mental check against StatusDot.tsx:20). It must read cleanly at ~20px (it doubles into the favicon basis and sits in the ~3rem rail — D-05/D-08). Add a JSDoc block explaining the mark, its sizes, the kamaq animating-life-force story (D-02) as the rationale, and the D-04 constraint. Ship a STATIC mark — no animation (animated variants are BRAND-FUT-01, deferred). Give it a sensible default size class (e.g. `size-5`) that callers can override via `className`.
  </action>
  <verify>
    <automated>cd web && test -f src/components/brand/KamacuMark.tsx && grep -q 'export { KamacuMark }' src/components/brand/KamacuMark.tsx && grep -q 'aria-label="Kamacu"' src/components/brand/KamacuMark.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/brand/KamacuMark.tsx` exists and contains `export { KamacuMark }`
    - Source contains `role="img"` and `aria-label="Kamacu"`
    - Source contains an SVG gradient (`grep -Eq '<(radial|linear)Gradient' web/src/components/brand/KamacuMark.tsx`) OR multiple fill/stop colors — i.e. it is NOT a single flat `bg-amber-400` circle (D-04)
    - Source contains NO animation directive matching the waiting dot: `grep -q 'animate-pulse' web/src/components/brand/KamacuMark.tsx` returns nothing
    - `npm run build` (tsc -b + vite build) and `npm run lint` both exit 0
    - Human-verify (deferred to 20-04 checkpoint): the mark is visually distinct from the amber waiting dot in a side-by-side.
  </acceptance_criteria>
  <done>`KamacuMark` is a lint-clean, type-checking inline-SVG component exporting the contract consumed by the sidebar lockup, rendering a multi-tone ember spark distinct from the amber waiting dot.</done>
</task>

<task type="auto">
  <name>Task 2: Create the favicon and set the browser-tab identity</name>
  <files>web/public/favicon.svg, web/index.html</files>
  <read_first>
    - web/index.html (12 lines: `<title>Kangent</title>`, no favicon link, keep `<html lang="en" class="dark">`)
    - web/embed.go (`//go:embed all:dist` — the `all:` prefix already includes public assets copied into dist)
    - .planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md § "Favicon (D-06)" and § "Claude's Discretion" (one shared SVG source vs two variants)
    - web/src/components/brand/KamacuMark.tsx (from Task 1 — the spark geometry the favicon derives from)
  </read_first>
  <action>
    Create `web/public/favicon.svg` — a spark-derived favicon (same ember spark as `KamacuMark`, simplified for 16–32px legibility) per D-06. It is a real standalone `.svg` file (the favicon is the documented exception to the "no .svg files" rule — Vite serves `web/public/` at the site root). Ensure it stays legible on both light and dark browser-tab chrome (avoid relying on a transparent-only mark that vanishes on dark chrome). Then edit `web/index.html`: change `<title>Kangent</title>` → `<title>Kamacu</title>`, and add inside `<head>` (after the title) a favicon link `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` (root-relative `/favicon.svg`, NOT `@/` or a relative path — Vite public assets are root-served and embedded into `dist/` by the existing `web/embed.go`). Keep `<html lang="en" class="dark">` unchanged. Do NOT edit `web/embed.go` — the `all:dist` embed already covers the copied favicon.
  </action>
  <verify>
    <automated>cd web && test -f public/favicon.svg && grep -q '<svg' public/favicon.svg && grep -q '<title>Kamacu</title>' index.html && grep -Eq 'rel="icon"[^>]*href="/favicon.svg"' index.html && ! grep -q '<title>Kangent</title>' index.html && npm run build && test -f dist/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - `web/public/favicon.svg` exists and is a valid SVG (`grep -q '<svg' web/public/favicon.svg`)
    - `web/index.html` contains `<title>Kamacu</title>` and no longer contains `<title>Kangent</title>`
    - `web/index.html` contains `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` (or equivalent with `rel="icon"` + `href="/favicon.svg"`)
    - `web/index.html` still contains `<html lang="en" class="dark">`
    - After `npm run build`, `web/dist/favicon.svg` exists (proving Vite copied the public asset into the embedded build)
    - `web/embed.go` is unchanged
  </acceptance_criteria>
  <done>The favicon is a spark-derived SVG served at `/favicon.svg` and copied into `dist/`; the tab title reads "Kamacu"; the favicon link is present; the dark-theme html attribute is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → shipped binary | The favicon and mark are static assets bundled into the single `kamacu` binary via Vite `dist/` embed; they must be authored in-repo, not fetched from a third party at build/run time. |
| browser → served static files | `web/public/` is served at the site root; adding `favicon.svg` must not widen what the embed exposes beyond the intended asset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | logo/favicon asset provenance | mitigate | Author the ember spark SVG in-repo by hand (inline component + hand-written favicon.svg); do NOT pull a remote/third-party image URL or CDN reference. No external `<img src>` or network fetch is introduced. |
| T-20-04 | Information Disclosure | static-serving / embed surface | accept | Favicon lands in the existing `web/public/` → `dist/` path already covered by `//go:embed all:dist`; `web/embed.go` and the SPA static-serving handler are unchanged, so no new files are exposed and no new route is added. |
| T-20-SC | Tampering | package installs (npm/pip/cargo) | accept | No new npm dependencies are added — the mark uses only React + the existing `cn` util; the favicon is a static file. No install tasks, so no supply-chain surface. |
</threat_model>

<verification>
- `KamacuMark.tsx` exists, exports `KamacuMark`, is a multi-tone SVG (not a flat amber dot), lint + build clean.
- `web/public/favicon.svg` exists and is copied into `web/dist/` by the build.
- `web/index.html` titles the tab "Kamacu" and links `/favicon.svg`; dark-theme attr preserved.
- `cd web && npm run build && npm run lint` both green.
- Visual confirmation (mark renders, favicon appears in the tab, distinct from the waiting dot) is deferred to the 20-04 human-verify checkpoint.
</verification>

<success_criteria>
An importable ember-spark `KamacuMark` component and a spark-derived `favicon.svg` exist; the browser tab shows the Kamacu title and favicon; the frontend builds and lints cleanly; no external image source or embed-path change was introduced.
</success_criteria>

<output>
Create `.planning/phases/20-kamacu-rebrand-brand/20-02-SUMMARY.md` when done.
</output>
