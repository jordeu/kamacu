---
phase: 20-kamacu-rebrand-brand
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-03]
files_modified:
  - README.md
  - docs/kamacu-board.png
autonomous: false
requirements: [BRAND-03]
must_haves:
  truths:
    - "D-10: The repo root has a shareable open-source README.md describing what Kamacu is, its prerequisites (Claude Code CLI, git, Go 1.26, Node 20.19+/22.12+, tmux), and its make build / run steps"
    - "D-11: The README is concise and high-signal, embedding one screenshot of the board + a task's agent terminal and a short project → task → agent → review workflow walkthrough"
    - "The README build instructions match reality: `make build` produces a single bin/kamacu binary"
    - "The README screenshot was captured AFTER the rebrand landed (shows Kamacu branding, not kangent)"
    - "A human has confirmed the Kamacu branding renders correctly and is visually distinct from the amber waiting dot"
  artifacts:
    - path: "README.md"
      provides: "Shareable open-source README for Kamacu"
      contains: "Kamacu"
      min_lines: 30
    - path: "docs/kamacu-board.png"
      provides: "Screenshot of the branded board + agent terminal"
  key_links:
    - from: "README.md"
      to: "docs/kamacu-board.png"
      via: "markdown image embed"
      pattern: "!\\[.*\\]\\(docs/kamacu-board.png\\)"
---

<objective>
Write the repo-root `README.md` (BRAND-03) and run the end-of-phase human-verify: confirm the full Kamacu rebrand renders in a running instance and capture the branded screenshot the README embeds. This is the last plan in the phase because the README documents the `bin/kamacu` binary (Plan 01) and its screenshot must show the landed Kamacu branding — sidebar lockup, favicon, title (Plans 02–03), per D-12.

Purpose: Close the phase with a shareable README and a human confirmation that the rebrand is correct and the ember mark is distinct from the amber waiting dot.
Output: `README.md` at the repo root (what/why + prerequisites + build/run + workflow walkthrough + screenshot) and `docs/kamacu-board.png` captured from the branded UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md
@.planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md

<constraints>
- Source content from `./CLAUDE.md` (project intro + the recommended stack table) and CONTEXT.md refs. Do NOT copy `web/README.md` — it is a stale default Vite/React template, unrelated boilerplate.
- Prerequisites to list (D-10): Claude Code CLI, git, Go 1.26, Node 20.19+/22.12+, tmux.
- Build/run (D-10/D-11): `make build` → single `bin/kamacu` binary. Keep it concise + high-signal (D-11), NOT a full architecture doc.
- The screenshot MUST be captured AFTER the rebrand lands (D-12) so it shows Kamacu branding, not the old kangent UI.
- Deferred: animated/light-dark logo variants are BRAND-FUT-01 — do NOT add them here.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the repo-root README.md</name>
  <files>README.md</files>
  <read_first>
    - ./CLAUDE.md (Project intro + Recommended Stack table — the content source)
    - .planning/PROJECT.md (core value + workflow narrative)
    - .planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md § "README (D-10, D-11)" and § "Specific Ideas" (the kamaq animating-life-force story)
    - Makefile (confirm `make build` → `bin/kamacu` after Plan 01)
  </read_first>
  <action>
    Create `README.md` at the repo root, written for a stranger discovering the project (shareable open-source framing, D-10). Include, in a concise high-signal structure (D-11): (1) a title "Kamacu" and a 1–2 line what-it-is/why — a local-only web app to organize Claude Code agent sessions around projects and tasks, weaving in the kamaq / kamacu "the one who animates / gives life-force" naming story (D-02) as the rationale for the ember-spark mark; (2) Prerequisites — Claude Code CLI, git, Go 1.26, Node 20.19+/22.12+, tmux; (3) Build & run — `make build` produces a single `bin/kamacu` binary that serves the API + embedded frontend at localhost; note the Vite dev-proxy path for development; (4) a short project → task → agent → review workflow walkthrough (a project points at a local git repo; each task gets an isolated worktree + a persistent Claude Code session you can open/leave/reattach in the browser; review a task's diff/PR); (5) a screenshot placeholder embedding `docs/kamacu-board.png` with markdown `![Kamacu board with a task's agent terminal](docs/kamacu-board.png)` — the actual image is captured in Task 2. Do NOT copy `web/README.md`. Do NOT document runtime data paths as `~/.kamacu` (they are still `~/.kangent` until Phase 21) — keep the README about building and using the app, not internal paths.
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'Kamacu' README.md && grep -q 'make build' README.md && grep -qi 'Claude Code' README.md && grep -qi 'tmux' README.md && grep -q 'Node 20.19' README.md && grep -Eq '!\[.*\]\(docs/kamacu-board.png\)' README.md && [ "$(wc -l < README.md)" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the repo root, titled Kamacu, and is at least 30 lines
    - Contains the prerequisites list: Claude Code CLI, git, Go 1.26, Node 20.19+/22.12+, tmux (`grep` for "Claude Code", "tmux", "Node 20.19", "Go 1.26")
    - Contains `make build` and describes the single `bin/kamacu` binary
    - Contains a project → task → agent → review workflow description
    - Embeds the screenshot via `![...](docs/kamacu-board.png)`
    - Does NOT reference `~/.kamacu` runtime paths (those are Phase 21): `grep -q '~/.kamacu' README.md` returns nothing
    - Is not a copy of the stale `web/README.md` Vite template (`grep -q 'React + TypeScript + Vite' README.md` returns nothing)
  </acceptance_criteria>
  <done>A concise shareable README describes Kamacu, its prerequisites, `make build` → `bin/kamacu`, and the core workflow, with a screenshot embed pointing at `docs/kamacu-board.png`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Kamacu branding and capture the README screenshot</name>
  <files>docs/kamacu-board.png, README.md</files>
  <action>
    STOP for the human. Claude has already automated everything it can (Plans 01–03 landed the rename/mark/favicon/lockup; Task 1 wrote the README with the screenshot placeholder). This task is the human confirmation + screenshot capture that Claude cannot perform: build and run the app, visually verify the Kamacu branding renders correctly and is distinct from the amber waiting dot, capture a branded screenshot, and save it as `docs/kamacu-board.png` so the README image renders. Present the how-to-verify steps below and wait for the resume signal.
  </action>
  <what-built>
    Plans 01–03 renamed the Go module/binary to `kamacu`, added the `KamacuMark` ember-spark component + favicon, set the tab title to "Kamacu", reworked the sidebar header into the mark + "Kamacu" lockup (expanded + collapsed rail), and renamed all user-facing copy. Task 1 wrote the README with a screenshot placeholder at `docs/kamacu-board.png`.
  </what-built>
  <read_first>
    - README.md (the screenshot embed path `docs/kamacu-board.png`)
    - .planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md § "Logo / brand mark (D-04)" and § "README (D-12)"
  </read_first>
  <how-to-verify>
    1. Build and run the app: `make build && ./bin/kamacu` (an existing `~/.kangent` install must still open normally — runtime paths are unchanged this phase).
    2. Open the app in a browser at localhost. Confirm: (a) the browser tab title reads "Kamacu" and shows the ember-spark favicon; (b) the expanded sidebar header shows the ember mark + the "Kamacu" wordmark; (c) collapse the sidebar (Ctrl+B) and confirm the mark sits at the top of the icon rail above the project avatars; (d) no user-facing "Kangent" text remains (empty-board copy, delete-project dialog, Settings GitHub help).
    3. D-04 check: place the ember mark next to a project's amber "agent waiting" dot (or picture it) — confirm the mark is a fuller multi-tone glow/spark, clearly NOT confusable with the plain amber waiting dot.
    4. Capture a screenshot showing the board + a task's agent terminal (Kamacu-branded), save it as `docs/kamacu-board.png` in the repo, and confirm the README image renders (e.g. preview the README).
  </how-to-verify>
  <acceptance_criteria>
    - `docs/kamacu-board.png` exists in the repo and shows the post-rebrand Kamacu UI (not the old kangent branding)
    - The README image reference resolves to the committed file
    - Human confirms: tab title + favicon = Kamacu; expanded lockup = mark + "Kamacu"; collapsed rail shows the mark; no user-facing "Kangent" remains; the ember mark is visually distinct from the amber waiting dot
    - The existing install still opens (runtime `~/.kangent` paths unchanged)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the branding is confirmed and `docs/kamacu-board.png` is captured and rendering in the README, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → README asset | The screenshot embedded in the README is a local capture committed into the repo; it must not be a hotlinked remote/third-party image. |
| running binary → existing install | The human-verify runs against a real `~/.kangent` install to confirm the rebrand did not break runtime path resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | README image provenance | mitigate | The screenshot is captured locally by the human and committed as `docs/kamacu-board.png`; the README references a repo-relative path, not a remote URL — no untrusted external asset. |
| T-20-08 | Denial of Service | existing `~/.kangent` install | mitigate | The checkpoint explicitly verifies an existing install still opens after the rebrand, confirming the keep-out boundary held end-to-end (runtime paths unchanged). |
| T-20-SC | Tampering | package installs (npm/pip/cargo) | accept | No new dependencies added — this plan writes docs and captures a screenshot. No install tasks, so no supply-chain surface. |
</threat_model>

<verification>
- `README.md` exists at repo root with the required sections and a `docs/kamacu-board.png` embed.
- `docs/kamacu-board.png` captured from the branded UI and rendering in the README.
- Human-verify confirms tab title/favicon, sidebar lockup (expanded + rail), no "Kangent" copy, ember-vs-amber-dot distinction, and that an existing install still opens.
</verification>

<success_criteria>
The repo has a concise, shareable Kamacu README with a post-rebrand screenshot, and a human has confirmed the full Kamacu branding renders correctly, is distinct from the amber waiting dot, and that an existing `~/.kangent` install keeps working.
</success_criteria>

<output>
Create `.planning/phases/20-kamacu-rebrand-brand/20-04-SUMMARY.md` when done.
</output>
