---
phase: 20-kamacu-rebrand-brand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - Makefile
  - cmd/kamacu/main.go
  - cmd/kamacu/main_test.go
  - internal/**/*.go
autonomous: true
requirements: [REBRAND-02, REBRAND-03]
must_haves:
  truths:
    - "The Go module is named kamacu and every internal import resolves under kamacu/"
    - "make build produces a bin/kamacu binary"
    - "go build ./..., go vet ./..., and go test ./... are all green"
    - "D-13: the full cosmetic sweep leaves no cosmetic 'kangent' prose anywhere it is safe to change (comments, log lines, non-runtime identifiers) - only keep-out runtime literals and test sentinels remain"
    - "Every runtime data path, tmux socket, session prefix, and localStorage key still reads kangent at runtime (Phase 21 owns the flip)"
  artifacts:
    - path: "go.mod"
      provides: "module kamacu declaration"
      contains: "module kamacu"
    - path: "Makefile"
      provides: "kamacu build target"
      contains: "bin/kamacu"
    - path: "cmd/kamacu/main.go"
      provides: "renamed entrypoint package directory"
  key_links:
    - from: "cmd/kamacu/main.go"
      to: "kamacu/internal/*"
      via: "import block"
      pattern: "\"kamacu/internal/"
    - from: "internal/tmux/tmux.go"
      to: "runtime tmux socket"
      via: "unchanged DefaultSocket constant"
      pattern: "DefaultSocket = \"kangent\""
---

<objective>
Rename the Go code identity from `kangent` to `kamacu`: the module path, all internal import paths, the `cmd/kangent/` → `cmd/kamacu/` entrypoint directory, the `Makefile` build target, and cosmetic product-name prose in comments and log lines. This delivers REBRAND-02 (the app builds as a `kamacu` binary, module renamed, imports updated, `go build`/`vet`/`test ./...` green) and the backend half of REBRAND-03 (code identifiers and log strings say `kamacu`).

Purpose: The binary and Go module are the deepest layer of the product's identity. Every other rename layer (UI, docs) sits on top of a `kamacu`-named build.
Output: A `kamacu` Go module that builds to `bin/kamacu`, with all internal imports rewritten and cosmetic prose swept — while every runtime literal (paths, socket, session prefix, config filename, auth header) stays `kangent` so an existing install keeps working (Phase 21 owns the runtime flip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-kamacu-rebrand-brand/20-CONTEXT.md
@.planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md

<constraints>
The "Keep-Out Boundary" section of `20-PATTERNS.md` is a HARD constraint, not advisory.
The following literals are (or construct) real runtime paths / socket / session prefix / data-dir
filenames / auth keys and MUST be left byte-for-byte unchanged — renaming any of them breaks
`go test ./...` (which REBRAND-02 requires green) or silently breaks runtime path resolution.
They are Phase 21 territory:

- `internal/tmux/tmux.go:22` — `const DefaultSocket = "kangent"`  (the `-L kangent` socket)
- `internal/tmux/tmux.go:29` — the `# kangent-managed tmux config …` Config string body
- `internal/settings/settings.go:31` — `KeyWorktreeBase: "~/.kangent/worktrees/"`
- `internal/api/projects.go:195` — `const reposBase = "~/.kangent/repos/"`
- `internal/api/sessions.go:302` — `fmt.Sprintf("kangent-%d-%d", …)` session-name prefix
- `cmd/kamacu/main.go:35` — `"~/.kangent/kangent.db"` default SQLite path
- `cmd/kamacu/main.go:96` — `"kangent-tmux.conf"` generated config filename
- `cmd/kamacu/main.go:124` — `"~/.kangent/worktrees"` worktree root
- `cmd/kamacu/main.go:270` — `strings.HasPrefix(name, "kangent-")` orphan-sweep guard
- All `*_test.go` assertions on real `.kangent` paths / `kangent-*` session names / `kangent-tmux.conf`
  (e.g. `internal/api/projects_test.go`, `internal/settings/settings_test.go`,
  `internal/tmux/tmux_test.go`, `internal/session/tmux_lifecycle_test.go`,
  `internal/api/sessions_test.go`, `internal/api/worktrees_test.go`).

DECISION — `X-Kangent-Token` auth header: KEEP unchanged (do NOT rename). It is a live auth header
on the agent→server hook wire (sender `internal/session/agent.go:23,63`; receiver
`internal/api/hooks.go:39`; tests `internal/session/agent_test.go:94`, `internal/api/hooks_test.go:87,103`).
A `claude` session spawned by the old binary would 401 against a renamed header after a restart —
the same cross-restart hazard the D-14 keep-out boundary exists to avoid. Per 20-PATTERNS "AMBIGUOUS /
RISKY" and phase guidance, defer it to Phase 21 alongside the other runtime-key flips.

DO-NOT-BOTHER (low-value, leave to minimize churn): the `echo kangent-marker` shell sentinels in
`internal/session/session_test.go`, the `{"single", "kangent", "KA"}` fixture in
`internal/api/icons_test.go:15` and its comment in `internal/api/icons.go:66`, and the bogus repo slug
`octocat/this-repo-does-not-exist-kangent-test` in `internal/api/projects_test.go:765`. These are
self-consistent test sentinels with no user-facing brand value.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Go module, imports, cmd directory, and Makefile target</name>
  <files>go.mod, Makefile, cmd/kamacu/main.go, cmd/kamacu/main_test.go, and all *.go files importing "kangent/ (41 files across internal/**)</files>
  <read_first>
    - go.mod (line 1: `module kangent`)
    - Makefile (backend + dev-backend targets reference `bin/kangent` and `./cmd/kangent`)
    - cmd/kangent/main.go (import block, verify no keep-out literal is on an import line)
    - .planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md § "Go module + import rename" and § "Keep-Out Boundary"
  </read_first>
  <action>
    Rewrite the module identity from `kangent` to `kamacu`. (1) Change `go.mod` line 1 from `module kangent` to `module kamacu`. (2) Rewrite every internal import path prefix `"kangent/` → `"kamacu/` across all `*.go` files (18 non-test + 23 test = 41 files). Use a precise replacement anchored on the opening double-quote plus slash — the pattern `"kangent/` matches ONLY import strings and provably cannot match any keep-out literal, because every keep-out literal is `"~/.kangent…` (tilde-slash-dot before the token), `"kangent"` (quote-close), `"kangent-…` (hyphen), or `kangent-tmux.conf` — none of which contain the quote+`kangent/` sequence. (3) Move the entrypoint directory with `git mv cmd/kangent cmd/kamacu` (both files stay `package main`; the Go package name is unaffected by the directory name). (4) In `Makefile`, change `go build -o bin/kangent ./cmd/kangent` → `-o bin/kamacu ./cmd/kamacu` and `go run ./cmd/kangent` → `./cmd/kamacu`. Run `gofmt -w` on any file whose import block ordering shifted. Do NOT touch any literal listed in the Keep-Out Boundary; do NOT rename `X-Kangent-Token`.
  </action>
  <verify>
    <automated>head -1 go.mod | grep -qx 'module kamacu' && ! grep -rq '"kangent/' --include='*.go' . && test -d cmd/kamacu && ! test -d cmd/kangent && go build ./... && go vet ./... && go test ./... && make build && test -x bin/kamacu</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 go.mod` is exactly `module kamacu`
    - `grep -rq '"kangent/' --include='*.go' .` finds ZERO matches (all imports rewritten)
    - Directory `cmd/kamacu/` exists and `cmd/kangent/` does not
    - `Makefile` contains `bin/kamacu` and `./cmd/kamacu`; contains no `bin/kangent` or `./cmd/kangent`
    - `go build ./...`, `go vet ./...`, and `go test ./...` all exit 0
    - `make build` produces an executable at `bin/kamacu`
    - Keep-out preserved: `grep -c 'DefaultSocket = "kangent"' internal/tmux/tmux.go` == 1; `grep -c 'reposBase = "~/.kangent/repos/"' internal/api/projects.go` == 1; `grep -c 'X-Kangent-Token' internal/api/hooks.go` == 1
  </acceptance_criteria>
  <done>Module and all imports are `kamacu`; `cmd/kamacu/` is the entrypoint; `make build` yields `bin/kamacu`; the full backend test suite is green; all keep-out literals are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep cosmetic product-name prose and non-runtime identifiers</name>
  <files>cmd/kamacu/main.go, internal/quota/quota.go, internal/session/session.go, internal/session/agent.go, internal/session/agent_test.go, internal/session/manager.go, internal/session/session_test.go, internal/api/hooks.go, internal/api/sessions.go, internal/api/tasks.go, internal/api/projects.go, internal/api/projects_test.go, internal/settings/tokenize.go, internal/tmux/tmux.go, internal/ws/integration_test.go, internal/api/agent_integration_test.go, internal/api/integration_test.go</files>
  <read_first>
    - .planning/phases/20-kamacu-rebrand-brand/20-PATTERNS.md § "Go cosmetic prose rename (D-13)" (the D-13 file:line list — treat as a non-exhaustive STARTING POINT; PATTERNS itself is known-incomplete, so the completeness grep in the acceptance criteria is authoritative) and § "Keep-Out Boundary"
    - cmd/kamacu/main.go (log/error prose AND keep-out literals at :35, :96, :124, :270 — edit only the prose)
    - internal/session/agent.go (lines 59, 64: the `kangentSessionID` identifier) and internal/session/agent_test.go (:191, :193: the `kangent session id` comment/error text)
    - internal/tmux/tmux.go (the `// DefaultSocket is … for all Kangent` comment prose at :20 is a D-13 must-rename; the `DefaultSocket = "kangent"` value at :22 and the Config string body are keep-out)
  </read_first>
  <action>
    Rename the product-name WORD "kangent"/"Kangent" → "kamacu"/"Kamacu" ONLY where it is prose (a comment or a log/error string describing the product), never where it is or builds a runtime literal. Do this as a COMPLETENESS-DRIVEN sweep, not a fixed line list: the 20-PATTERNS D-13 enumeration is known-incomplete, so after Task 1's import rewrite, walk EVERY remaining `[Kk]angent` occurrence in the `*.go` tree and rename each one that is product-name prose, leaving ONLY the keep-out literals and test sentinels. The prose class includes: the `cmd/kamacu/main.go` log/error strings (the `SECURITY: … is listening` warning, the `slog.Info` listening line, the loopback `serves a shell and must stay local` error, and the product-name comments elsewhere in that file); product-name comments across `internal/quota/quota.go`, `internal/session/session.go`, `internal/session/manager.go`, `internal/api/hooks.go`, `internal/settings/tokenize.go`, `internal/api/sessions.go`, `internal/api/tasks.go`, `internal/api/projects.go`, and `internal/tmux/tmux.go` (the PROSE comments only — including the `// DefaultSocket is the dedicated tmux socket name (-L) for all Kangent` comment on tmux.go:20; but leave the `DefaultSocket` value on tmux.go:22, the Config string body, and `reposBase`); and product-name comments / test-message strings in `*_test.go` files that are NOT sentinels (e.g. the `kangent session id` comment and `t.Errorf` text in `internal/session/agent_test.go`, the `exits Kangent didn't ask for` comment in `internal/session/session_test.go`, the `never Kangent-owned` comment in `internal/api/projects_test.go`). Also rename the non-runtime identifier `kangentSessionID` → `kamacuSessionID` at `internal/session/agent.go` and every reference (it holds a UUID, not the literal word). Update stale path-doc comments that spell `cmd/kangent/…` → `cmd/kamacu/…` in `internal/ws/integration_test.go`, `internal/api/agent_integration_test.go`, `internal/api/integration_test.go`, and the `go run ./cmd/kangent …` comment in `cmd/kamacu/main.go`. Do NOT touch any Keep-Out Boundary literal, the `X-Kangent-Token` auth header (Phase 21), or the DO-NOT-BOTHER test sentinels (`echo kangent-marker`, the `{"single", "kangent", "KA"}` icons fixture and its `icons.go` comment example, the `octocat/this-repo-does-not-exist-kangent-test` slug). Completeness is proven by the exclusion-filtered grep in the acceptance criteria: after the sweep, zero product-name prose may remain once the keep-out literals and sentinels are excluded.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./... && ! grep -rq 'kangentSessionID' --include='*.go' . && test "$(grep -c 'DefaultSocket = "kangent"' internal/tmux/tmux.go)" -eq 1 && test "$(grep -rc 'X-Kangent-Token' --include='*.go' . | awk -F: '{s+=$2} END{print s}')" -eq 6 && test "$(grep -rniE 'kangent' --include='*.go' . | grep -vE '(\.kangent|kangent-|kangent\.db|X-Kangent-Token|"kangent"|"kangent/)' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...`, `go vet ./...`, `go test ./...` all exit 0 (proves no keep-out literal was disturbed)
    - `grep -rq 'kangentSessionID' --include='*.go' .` finds ZERO matches; `kamacuSessionID` is present in `internal/session/agent.go`
    - `cmd/kamacu/main.go` log lines read `kamacu` (e.g. `grep -c 'kamacu listening' cmd/kamacu/main.go` >= 1) while `grep -c '~/.kangent/kangent.db' cmd/kamacu/main.go` == 1 and `grep -c 'kangent-tmux.conf' cmd/kamacu/main.go` == 1 and `grep -c 'HasPrefix(name, "kangent-")' cmd/kamacu/main.go` == 1 (keep-out intact)
    - `internal/tmux/tmux.go` still contains `DefaultSocket = "kangent"` (exactly one match)
    - `X-Kangent-Token` still present at all 6 sites (3 source: `agent.go:23,63`, `hooks.go:39` + 3 test: `agent_test.go:94`, `hooks_test.go:87,103`): `grep -rc 'X-Kangent-Token' --include='*.go' . | awk -F: '{s+=$2} END{print s}'` == 6
    - Cosmetic prose sweep is COMPLETE (D-13 — no cosmetic `kangent` remains anywhere it is safe to change): `grep -rniE 'kangent' --include='*.go' . | grep -vE '(\.kangent|kangent-|kangent\.db|X-Kangent-Token|"kangent"|"kangent/)' | wc -l` == 0. The exclusion set covers exactly the keep-out literals + sentinels — `~/.kangent`/`.kangent` paths, every `kangent-` hyphenated token (the tmux Config body, `kangent-tmux.conf`, `kangent-<task>` session names, `echo kangent-marker`, the `-kangent-test` slug), `kangent.db`, the `X-Kangent-Token` header, the `"kangent"` literals (the `DefaultSocket = "kangent"` runtime socket value at `internal/tmux/tmux.go:22` and the icons fixture/comment), and `"kangent/` import paths already flipped in Task 1 — so anything else the grep would surface is safe product-name prose that MUST be renamed. Note the `// DefaultSocket is … for all Kangent` comment prose at `internal/tmux/tmux.go:20` is deliberately NOT excluded (there is no `DefaultSocket` exclusion token): it is product-name prose D-13 requires renaming, and once its word is flipped to Kamacu the line contains no `kangent` and drops out of the outer grep, while the tmux.go:22 runtime value stays excluded via `"kangent"`. This gate is robust to the incomplete PATTERNS.md list rather than copying it verbatim.
  </acceptance_criteria>
  <done>Product-name prose and the `kangentSessionID` identifier are renamed to Kamacu (verified by the completeness grep returning zero); all runtime literals, the `X-Kangent-Token` auth header, and test sentinels are untouched; the full backend suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (claude CLI) → server hook | The spawned `claude` session POSTs status to `/api/hooks/...` with the `X-Kangent-Token` auth header; a renamed binary must keep authenticating pre-existing sessions across a restart. |
| binary → local filesystem / tmux | The `kamacu` binary resolves `~/.kangent` data paths and the `-L kangent` tmux socket at runtime; these must stay identical so an existing install keeps working (Phase 21 owns the flip). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | Keep-out runtime literals (paths, socket, session prefix, config filename) | mitigate | Import rewrite is anchored on `"kangent/` (quote+slash), which provably cannot match any keep-out literal; prose sweep edits only comments/logs and is gated by an exclusion-filtered completeness grep; `go test ./...` green is the acceptance gate proving no path/socket literal changed. |
| T-20-02 | Spoofing | `X-Kangent-Token` agent→server auth header | accept | Explicit decision to KEEP the header unchanged and defer to Phase 21; renaming it would 401 in-flight sessions after a restart. Verified present at all 6 sites post-change. |
| T-20-SC | Tampering | package installs (npm/pip/cargo) | accept | No new dependencies are added in this phase — it is a pure identifier/prose rename over existing code. No install tasks, so no supply-chain surface. |
</threat_model>

<verification>
- `head -1 go.mod` == `module kamacu`; no `*.go` file imports `"kangent/`.
- `cmd/kamacu/` exists; `cmd/kangent/` removed.
- `make build` → `bin/kamacu` executable.
- `go build ./...` + `go vet ./...` + `go test ./...` all green — the primary proof the keep-out boundary held.
- Completeness grep (kangent minus keep-out/sentinel exclusions) returns zero — no cosmetic `kangent` prose remains.
- All keep-out literals and the `X-Kangent-Token` header (6 sites) verified still present.
</verification>

<success_criteria>
The Go module, imports, entrypoint directory, and Makefile target are `kamacu`; cosmetic prose and the `kangentSessionID` identifier say Kamacu; `make build` produces `bin/kamacu`; the full backend suite is green; every runtime path/socket/key literal and the auth header are byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/20-kamacu-rebrand-brand/20-01-SUMMARY.md` when done.
</output>
</output>
</output>
