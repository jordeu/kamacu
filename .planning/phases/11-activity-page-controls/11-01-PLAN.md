---
phase: 11-activity-page-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/types.ts
  - web/src/api/queries.ts
  - web/src/lib/time.ts
  - web/src/lib/useActivityView.ts
autonomous: true
requirements: [ACT-02, ACT-03]

must_haves:
  truths:
    # ACT-02 / ACT-03 — the scope/window state layer that drives the controls (Plan 02 renders them)
    - useActivity(scope, window, enabled) exported from queries.ts issues GET /api/activity?scope=…&window=… with queryKey ["activity", scope, window] (per D-04/D-07 param grammar, Phase 10 D-01)
    - Activity wire types in types.ts match the Phase 10 Go structs field-for-field, including all 6 ActivityReviewState values (per D-12 + Pitfall 2)
    - formatDuration(seconds) in time.ts formats adaptively to the largest 1-2 non-zero d/h/m/s units (per D-10), returning em-dash for non-finite/negative input
    - useActivityView() persists scope under kamacu.activity.scope and window under kamacu.activity.window (per D-06), validates the saved scope against the exact parseScope grammar on read
    - useActivityView() adopts the active workspace as default scope once useActiveWorkspace().activeWorkspaceId resolves and no scope is persisted yet (per D-05, Pitfall 6)
  artifacts:
    - web/src/lib/useActivityView.ts
  key_links:
    - useActivityView().scope → useActivity(scope, ...) query key
    - useActivityView().window → useActivity(..., window) query key
---

<objective>
Phase 11 data & state foundation: the wire types, TanStack query hook, adaptive duration
formatter, and localStorage-backed scope/window hook that the Activity page (Plan 02) renders.
Every symbol here is a prerequisite for the page — nothing user-facing ships until Plan 02.

Purpose: Decouple the contract layer (types + data fetch + view state) from the UI layer so
Plan 02 can focus purely on rendering against verified primitives.
Output: Modified types.ts/queries.ts/time.ts + new useActivityView.ts.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-activity-page-controls/11-CONTEXT.md
@.planning/phases/11-activity-page-controls/11-RESEARCH.md
@.planning/phases/11-activity-page-controls/11-PATTERNS.md
@.planning/phases/11-activity-page-controls/11-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activity wire types, useActivity query, and formatDuration util</name>
  <files>web/src/api/types.ts, web/src/api/queries.ts, web/src/lib/time.ts</files>
  <read_first>
    - web/src/api/types.ts (current type declarations — add activity types in the same style)
    - web/src/api/queries.ts (useProjects/useTasks param-keyed patterns — mirror for useActivity)
    - web/src/api/client.ts (get/ApiError primitives — get is the fetch function)
    - web/src/lib/time.ts (formatAgo — the zero-dep adaptive-duration style template for formatDuration)
    - internal/api/activity_helpers.go (activityTask/timeStat/statsBlock structs + parseScope/parseWindow grammar + computeTimeStat seconds truncation)
    - internal/api/activity.go (activityResponse/aggregateReviewsResult wire shape — always HTTP 200)
    - internal/github/prlist.go lines 324-332 (ReviewDoneSummary struct — number/title/completedAt/url/projectName/projectId/repo)
    - .planning/phases/11-activity-page-controls/11-RESEARCH.md (§Code Examples — the verified wire contract + formatDuration reference implementation + Anti-Patterns)
    - .planning/phases/11-activity-page-controls/11-PATTERNS.md (§queries.ts + types.ts + time.ts pattern assignments)
  </read_first>
  <behavior>
    - formatDuration(0) returns "0s"
    - formatDuration(45) returns "45s"
    - formatDuration(2700) returns "45m"
    - formatDuration(15680) returns "4h 20m"
    - formatDuration(97200) returns "1d 3h"
    - formatDuration(86400) returns "1d"
    - formatDuration(-1) returns "—"
    - formatDuration(NaN) returns "—"
    - formatDuration(Infinity) returns "—"
  </behavior>
  <action>
    **formatDuration (per D-10):** Add `formatDuration(totalSeconds: number): string` to web/src/lib/time.ts as a sibling export of `formatAgo` (which is already at lines 4-11). The input is integer SECONDS — the timeStat wire contract (int64 seconds truncated server-side, activity_helpers.go:100-102). Logic: guard `!Number.isFinite(totalSeconds) || totalSeconds < 0` → return "—" (the em-dash string); floor to integer seconds; if seconds < 60 return `${s}s`; compute minutes `m = Math.floor(s/60)`, if m < 60 return `${m}m`; compute hours `h = Math.floor(m/60)` and `remM = m % 60`, if h < 24 return `remM > 0 ? `${h}h ${remM}m` : `${h}h``; compute days `d = Math.floor(h/24)` and `remH = h % 24`, return `remH > 0 ? `${d}d ${remH}h` : `${d}d``. Use unit labels s/m/h/d to match formatAgo's existing style (time.ts:7-10 uses `12s`/`7m`/`1h 5m`). Add a JSDoc comment noting input is seconds per the wire contract and that the n===0 em-dash guard is the CALLER's responsibility (StatsStrip in Plan 02).

    **Wire types (matching Phase 10 Go structs field-for-field):** Add the following TypeScript interfaces/types to web/src/api/types.ts, mirroring the declaration style of the existing `Project`/`Workspace`/`Task` types already in that file:
    - `ActivityReviewState` — a union of exactly 6 string literals: `"ok" | "partial" | "error" | "auth_required" | "no_gh" | "disabled"` (per rollupReviewState in activity_helpers.go:129; `auth_required` MUST be included — Pitfall 2).
    - `ActivityTask` — `{ id: number; title: string; doneAt: string; projectId: number; projectName: string }`. The `doneAt` field is ms-ISO (reaper format). Do NOT include `inProgressAt` or `inReviewAt` — they are `json:"-"` server-side (RESEARCH Anti-Pattern; activity_helpers.go:15-16).
    - `ReviewDoneSummary` — `{ number: number; title: string; completedAt: string; url: string; projectName: string; projectId: number; repo: string }`. `completedAt` is second-precision ISO (gh closedAt). `url` is the https GitHub PR URL (the D-14 click target).
    - `TimeStat` — `{ n: number; min: number; max: number; median: number }`. ALL fields are int64 SECONDS (truncated), NOT milliseconds (Pitfall 3 — activity_helpers.go:100-102). `n` is the count of rows the stat was computed over (may be less than taskCount if timestamps are missing).
    - `StatsBlock` — `{ taskCount: number; reviewCount: number; cycle: TimeStat; dwellInProgress: TimeStat; dwellInReview: TimeStat }`.
    - `ActivityResponse` — `{ tasks: ActivityTask[]; reviews: { state: ActivityReviewState; stale: boolean; fetchedAt: string | null; prs: ReviewDoneSummary[] }; stats: StatsBlock }`.

    **useActivity query (Phase 10 D-01 — one endpoint, one query, one loading state):** Add `useActivity` to web/src/api/queries.ts, mirroring the param-keyed `useTasks` pattern. Signature: `useActivity(scope: ActivityScope, window: ActivityWindow, enabled: boolean = true)` where `ActivityScope` and `ActivityWindow` are imported from the new `useActivityView.ts` (Task 2 creates that file — if the import would create a circular dependency concern, define inline types matching `"global" | \`workspace:${number}\` | \`project:${number}\`` for scope and `"week" | "month"` for window instead). queryKey: `["activity", scope, window]`. queryFn: `() => get<ActivityResponse>(`/api/activity?scope=${scope}&window=${window}`)`. Include the `enabled` parameter so the page can defer the fetch until the persisted scope resolves (Pitfall 6 — don't fire with a null-derived scope). Do NOT set `refetchInterval` — Activity is an occasional view, not a live dashboard (contrast pullRequests.ts which polls every 60s). Import `get` from `"./client"` and `ActivityResponse` from `"./types"`.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20 && node -e "const m = require('./src/lib/time.ts'); " 2>/dev/null; node --input-type=module -e "const s=2700; const m=Math.floor(s/60); console.log(m<60?`${m}m`:'x')" && echo "formatDuration sanity OK"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/api/types.ts exports: ActivityReviewState (union with all 6 states including auth_required), ActivityTask (5 fields, NO inProgressAt/inReviewAt), ReviewDoneSummary (7 fields), TimeStat (4 int-seconds fields), StatsBlock (5 fields), ActivityResponse (tasks/reviews/stats)
    - web/src/api/queries.ts exports useActivity with queryKey ["activity", scope, window], no refetchInterval, enabled parameter
    - web/src/lib/time.ts exports formatDuration alongside existing formatAgo
    - formatDuration(2700) === "45m" (D-10 example)
    - formatDuration(97200) === "1d 3h" (D-10 example)
    - formatDuration(86400) === "1d"
    - formatDuration(-1) === "—"
    - formatDuration(NaN) === "—"
    - `cd web && npx tsc --noEmit` exits 0
    - ActivityReviewState union includes "auth_required" (grep `auth_required` in types.ts returns a match)
  </acceptance_criteria>
  <done>Wire types, query hook, and duration formatter compile cleanly under tsc; formatDuration produces the D-10 adaptive d/h/m/s outputs for the behavior-table inputs</done>
</task>

<task type="auto">
  <name>Task 2: useActivityView localStorage-backed scope+window hook</name>
  <files>web/src/lib/useActivityView.ts</files>
  <read_first>
    - web/src/lib/useActiveWorkspace.tsx (the localStorage hook template — kamacu.* prefix convention, validate-on-read stale-fallback at lines 60-65, resolve-against-live-data pattern at lines 57-66)
    - web/src/lib/migrateStorage.ts (the kamacu.* prefix convention — dot style kamacu.workspace)
    - internal/api/activity_helpers.go lines 51-80 (parseScope/parseWindow grammar — the EXACT param string shapes this hook must produce)
    - web/src/lib/useActivityView.ts (new file — does not exist yet; create it)
    - .planning/phases/11-activity-page-controls/11-PATTERNS.md (§useActivityView.ts pattern assignment — full reference implementation at lines 228-291)
    - .planning/phases/11-activity-page-controls/11-RESEARCH.md (§Pitfall 6 — default scope resolution timing against useActiveWorkspace; §Pattern 3 localStorage-backed view-state hook)
    - .planning/phases/11-activity-page-controls/11-CONTEXT.md (D-05 default scope = active workspace; D-06 localStorage persistence under kamacu.*)
  </read_first>
  <action>
    Per D-05 and D-06, create web/src/lib/useActivityView.ts as a PLAIN React hook (NOT a context/provider — scope and window have ONE consumer: ActivityPage. Contrast useActiveWorkspace.tsx which IS a context because workspace state is read across sidebar + board + redirect. RESEARCH Assumption A4 confirms this distinction).

    Export the following constants and types:
    - `ACTIVITY_SCOPE_KEY = "kamacu.activity.scope"` (dot style, matching `kamacu.workspace`)
    - `ACTIVITY_WINDOW_KEY = "kamacu.activity.window"`
    - `ActivityScope` type = `"global" | \`workspace:${number}\` | \`project:${number}\``
    - `ActivityWindow` type = `"week" | "month"`

    Implement two private read helpers:
    - `readScope(fallback: ActivityScope): ActivityScope` — reads `localStorage.getItem(ACTIVITY_SCOPE_KEY)`. Validates raw against the EXACT parseScope grammar (activity_helpers.go:51): return raw if `raw === "global"` OR `/^workspace:\d+$/.test(raw)` OR `/^project:\d+$/.test(raw)`. Otherwise (stale, missing, or tampered value) return fallback. This mirrors useActiveWorkspace.tsx:60-65 stale-fallback.
    - `readWindow(): ActivityWindow` — reads `localStorage.getItem(ACTIVITY_WINDOW_KEY)`. Returns `"month"` if raw === `"month"`, else `"week"` (default week, D-06).

    Implement `useActivityView()`:
    - Call `useActiveWorkspace()` to get `{ activeWorkspaceId }`.
    - Initialize `scope` via `useState<ActivityScope>(() => readScope("global"))` — initial fallback is `"global"` (D-05 fallback while workspace unresolved).
    - Initialize `window` via `useState<ActivityWindow>(readWindow)`.
    - Add a `useEffect` with dependency `[activeWorkspaceId]`: when `activeWorkspaceId !== null` AND `localStorage.getItem(ACTIVITY_SCOPE_KEY) === null` (the user has no persisted scope yet — first open), call `setScopeState(\`workspace:${activeWorkspaceId}\`)`. This adopts the active workspace as the default scope (D-05) WITHOUT overwriting a user's saved scope (Pitfall 6 — check the key is null, not just that activeWorkspaceId resolved).
    - `setScope(s: ActivityScope)`: writes both React state AND `localStorage.setItem(ACTIVITY_SCOPE_KEY, s)`.
    - `setWindow(w: ActivityWindow)`: writes both React state AND `localStorage.setItem(ACTIVITY_WINDOW_KEY, w)`.
    - Return `{ scope, setScope, window, setWindow }`.

    Import `useActiveWorkspace` from `"@/lib/useActiveWorkspace"` (or `"./useActiveWorkspace"` — match the existing import path convention in the codebase). Import `useState`, `useEffect` from `"react"`.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/lib/useActivityView.ts exports: ACTIVITY_SCOPE_KEY, ACTIVITY_WINDOW_KEY, ActivityScope, ActivityWindow, useActivityView
    - ACTIVITY_SCOPE_KEY === "kamacu.activity.scope" (D-06 dot-prefix convention)
    - ACTIVITY_WINDOW_KEY === "kamacu.activity.window"
    - readScope rejects invalid values (e.g. "foo", "workspace:abc", "") and returns the fallback — the regex `/^workspace:\d+$/` and `/^project:\d+$/` guards are present
    - useActivityView() returns an object with keys {scope, setScope, window, setWindow}
    - The useEffect on [activeWorkspaceId] checks `localStorage.getItem(ACTIVITY_SCOPE_KEY) === null` before adopting the workspace default (Pitfall 6 — does not overwrite a saved scope)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>useActivityView hook persists scope/window in localStorage under kamacu.activity.*, resolves default scope from the active workspace on first open (without overwriting a saved scope), and validates the saved scope against the parseScope grammar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → query param | A user-tampered `kamacu.activity.scope` value crosses into the `scope` query parameter of GET /api/activity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | kamacu.activity.scope localStorage | low | accept | Single-user localhost — no privilege boundary exists. `readScope` validates against the parseScope grammar (global / workspace:N / project:N); invalid values fall back to default. The backend's `parseScope` also rejects malformed values with HTTP 400. A tampered key only changes WHICH of the user's own data they see. |
| T-11-SC | Tampering | npm installs | high → N/A | mitigate | This plan installs ZERO new packages (all deps already in package.json per RESEARCH §Package Legitimacy Audit). The gate is trivially satisfied; no `checkpoint:human-verify` needed. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` exits 0 across all modified files
- `cd web && npx vite build` succeeds (the wire types + query + util are used by nothing yet, but must compile)
- formatDuration behavior verified against the D-10 example outputs
- useActivityView grammar validation prevents malformed scope strings from reaching the query
</verification>

<success_criteria>
- useActivity, ActivityResponse and related wire types, formatDuration, and useActivityView all compile and are importable
- Plan 02 can import every symbol it needs from these files without modification
</success_criteria>

<output>
Create `.planning/phases/11-activity-page-controls/11-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 01)

**New exports:**
- `formatDuration(totalSeconds: number): string` — web/src/lib/time.ts (sibling of formatAgo)
- `useActivity(scope: ActivityScope, window: ActivityWindow, enabled?: boolean)` — web/src/api/queries.ts (TanStack query)
- `ACTIVITY_SCOPE_KEY`, `ACTIVITY_WINDOW_KEY`, `ActivityScope`, `ActivityWindow`, `useActivityView()` — web/src/lib/useActivityView.ts (new file)
- `ActivityReviewState`, `ActivityTask`, `ReviewDoneSummary`, `TimeStat`, `StatsBlock`, `ActivityResponse` — web/src/api/types.ts (new type exports)

**New file paths:**
- `web/src/lib/useActivityView.ts`
