---
phase: 11-activity-page-controls
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - web/src/App.tsx
  - web/src/pages/ActivityPage.tsx
  - web/src/components/activity/ScopeSelector.tsx
  - web/src/components/activity/WindowToggle.tsx
  - web/src/components/activity/StatsStrip.tsx
  - web/src/components/activity/ActivityList.tsx
  - web/src/components/activity/ReviewsList.tsx
  - web/src/components/sidebar/ProjectSidebar.tsx
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, TASKS-02, TASKS-03, REVIEWS-02, REVIEWS-03]

must_haves:
  truths:
    # ACT-01
    - The /activity route renders ActivityPage under AppLayout as a bare sibling of /settings (no :projectId in the path)
    - The Activity sidebar entry is visible in BOTH the expanded sidebar footer AND the collapsed icon rail (per D-02 — must NOT be swallowed by the footer's group-data-[collapsible=icon]:hidden)
    - The Activity sidebar entry's active state is derived from location.pathname === "/activity" (the Settings-gear idiom, per D-01)
    # ACT-02
    - The scope dropdown lists Global + every workspace + every project with the active scope item checked (per D-04)
    - Selecting a scope writes kamacu.activity.scope and updates the useActivity query key (one refetch, one loading state)
    # ACT-03
    - The Week/Month segmented control toggles the window between week and month (per D-07)
    # ACT-04 / D-12
    - The reviews section renders NOTHING (no heading, no hint, no body) when reviews.state is in {disabled, no_gh, auth_required, error} with empty prs — includes auth_required per Pitfall 2
    # TASKS-02 / TASKS-03 / D-13 / D-15
    - Tasks-done entries are grouped by project under a project-name subheading, each showing title + completion time
    - Clicking a tasks-done entry navigates to /projects/:projectId/tasks/:taskId
    # REVIEWS-02 / REVIEWS-03 / D-14
    - Reviews-done entries are grouped by project under a project-name subheading, each showing #number + title + merge/close date
    - Clicking a reviews-done entry opens the PR URL (ReviewDoneSummary.url) in a new tab via a plain <a> element (never window.open, never a router Link)
    # STATS rendering (D-08/D-09/D-11)
    - The stats strip shows taskCount and reviewCount as lead numbers plus cycle/dwellInProgress/dwellInReview as min · median · max rows
    - When timeStat.n === 0, the stat row shows an em-dash (not 0m/0s); counts still render literal 0
    # Ordering
    - Reviews are sorted by completedAt DESC client-side before grouping (cross-repo merge is unsorted server-side, per Pitfall 5)
    - Tasks render in server order (already doneAt DESC from SQL — do NOT re-sort)
  artifacts:
    - web/src/pages/ActivityPage.tsx
    - web/src/components/activity/ScopeSelector.tsx
    - web/src/components/activity/WindowToggle.tsx
    - web/src/components/activity/StatsStrip.tsx
    - web/src/components/activity/ActivityList.tsx
    - web/src/components/activity/ReviewsList.tsx
  key_links:
    - ActivityPage composes ScopeSelector + WindowToggle + StatsStrip + ActivityList + ReviewsList
    - ScopeSelector/WindowToggle write to useActivityView().setScope/setWindow
    - useActivityView().scope/window feed useActivity(scope, window, enabled) query key (Plan 01)
    - ActivityList entry click → navigate(/projects/:projectId/tasks/:taskId)
    - ReviewsList entry click → external <a href={url} target="_blank" rel="noreferrer">
---

<objective>
Phase 11 UI layer: the dedicated Activity page, its scope/window controls, the stats strip,
the grouped tasks-done and reviews-done lists, and the sidebar entry that makes Activity
reachable app-wide. All user-facing requirements (ACT-01..04, TASKS-02/03, REVIEWS-02/03)
ship here. Built on Plan 01's data primitives (types, query, formatDuration, useActivityView).

Purpose: Render the Phase 10 GET /api/activity contract as a usable page with controls.
Output: New ActivityPage + 5 activity components + route + sidebar entry.
</objective>

<execution_context>
@/home/jordi/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/jordi/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-activity-page-controls/11-CONTEXT.md
@.planning/phases/11-activity-page-controls/11-RESEARCH.md
@.planning/phases/11-activity-page-controls/11-PATTERNS.md
@.planning/phases/11-activity-page-controls/11-UI-SPEC.md
@.planning/phases/11-activity-page-controls/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /activity route, ActivityPage shell, ScopeSelector, WindowToggle, StatsStrip</name>
  <files>web/src/App.tsx, web/src/pages/ActivityPage.tsx, web/src/components/activity/ScopeSelector.tsx, web/src/components/activity/WindowToggle.tsx, web/src/components/activity/StatsStrip.tsx</files>
  <read_first>
    - web/src/App.tsx (route registration — lines 114-141, the /settings sibling pattern; DO NOT wrap ActivityPage in BoardWorkspaceSync since /activity has no :projectId)
    - web/src/pages/SettingsPage.tsx (the page-shell template — h-full overflow-y-auto p-4, max-w-[640px], loading Skeletons at lines 148-156, error+retry at lines 128-138, section heading style at line 79)
    - web/src/components/sidebar/WorkspaceSwitcher.tsx (the scope-dropdown template — DropdownMenu + DropdownMenuCheckboxItem + DropdownMenuLabel + ChevronsUpDown trigger)
    - web/src/components/ui/dropdown-menu.tsx (the exported primitives — confirm DropdownMenu/Trigger/Content/Label/CheckboxItem/Separator are available)
    - web/src/components/ui/tabs.tsx (the segmented-control surface — default-variant Tabs IS visually a segmented control with data-active:bg-background)
    - web/src/api/queries.ts (useActivity query from Plan 01 — the query hook this page consumes)
    - web/src/api/types.ts (ActivityResponse/StatsBlock/TimeStat wire types from Plan 01)
    - web/src/lib/useActivityView.ts (the scope/window hook from Plan 01 — {scope, setScope, window, setWindow})
    - web/src/lib/time.ts (formatDuration from Plan 01 — the adaptive d/h/m/s formatter)
    - web/src/api/queries.ts (useProjects/useWorkspaces — already carry workspace_id/is_default, feed the scope dropdown)
    - .planning/phases/11-activity-page-controls/11-PATTERNS.md (§App.tsx + §ActivityPage.tsx + §ScopeSelector + §WindowToggle + §StatsStrip pattern assignments with full reference implementations)
    - .planning/phases/11-activity-page-controls/11-UI-SPEC.md (§Copywriting Contract — section headings, stat row format, scope trigger aria-label; §Spacing — max-w-[640px], gap-3 section-internal, gap-6 section-to-section)
    - .planning/phases/11-activity-page-controls/11-CONTEXT.md (D-04 single dropdown, D-05 default scope, D-07 segmented control, D-08 stats strip on top, D-09 min·median·max rows, D-11 em-dash on n===0)
  </read_first>
  <action>
    **Route (App.tsx):** Add `import ActivityPage from "@/pages/ActivityPage"` alongside the existing SettingsPage import. Add a bare route `<Route path="/activity" element={<ActivityPage />} />` as a sibling of the existing `<Route path="/settings" element={<SettingsPage />} />` (App.tsx:135). Do NOT wrap ActivityPage in `<BoardWorkspaceSync>` (App.tsx:78-112) — that helper reconciles a URL :projectId against the active workspace, and /activity has NO :projectId (D-06 — scope/window live in localStorage, never the URL).

    **ActivityPage.tsx (page shell + control bar + stats strip + placeholder list sections):**
    Mirror SettingsPage.tsx structure verbatim for the shell. The page:
    - Calls `const { scope, setScope, window, setWindow } = useActivityView()`.
    - Calls `const { data, isLoading, isError, refetch } = useActivity(scope, window)` (pass enabled=true since scope always resolves to at least "global").
    - Error + retry (mirror SettingsPage.tsx:128-138): if isError, render a centered column with `<p className="text-muted-foreground">{`Couldn't load activity.`}</p>` and a `<Button variant="outline" onClick={() => refetch()}>{`Retry loading`}</Button>`.
    - Shell (mirror SettingsPage.tsx:140-148): `<div className="h-full overflow-y-auto p-4"><div className="max-w-[640px]">` with a page heading `<h1 className="text-base font-medium">{`Activity`}</h1>`.
    - Loading (mirror SettingsPage.tsx:148-156): if `isLoading || !data`, render 4 Skeleton rows (each `<div className="flex flex-col gap-2"><Skeleton className="h-3 w-24" /><Skeleton className="h-8 w-full" /></div>`) inside `<div className="mt-6 flex flex-col gap-6">`.
    - Content (when data is present): render in order inside `<div className="mt-6 flex flex-col gap-6">`:
      1. Control bar: `<div className="flex items-center gap-2">` containing `<ScopeSelector scope={scope} onScopeChange={setScope} />` and `<WindowToggle window={window} onWindowChange={setWindow} />`.
      2. Stats strip: `<StatsStrip stats={data.stats} />`.
      3. Tasks-done section: leave a placeholder comment `{/* Tasks-done section — wired in Task 2 */}` (ActivityList is created in Task 2).
      4. Reviews-done section: leave a placeholder comment `{/* Reviews-done section — wired in Task 2 */}` (ReviewsList is created in Task 2).
    - Export ActivityPage as default.

    **ScopeSelector.tsx (per D-04):** Mirror WorkspaceSwitcher.tsx's dropdown structure. Props: `{ scope: ActivityScope; onScopeChange: (s: ActivityScope) => void }`. Use `DropdownMenu` + `DropdownMenuTrigger` (a Button variant="outline" with the resolved scope's display name + a `ChevronsUpDown` icon from lucide-react) + `DropdownMenuContent`. Inside the content, render three sections separated by `DropdownMenuSeparator`:
    - `DropdownMenuLabel` "Global" + a `DropdownMenuCheckboxItem` with text "All activity", `checked={scope === "global"}`, `onSelect={() => onScopeChange("global")}`.
    - `DropdownMenuLabel` "Workspaces" + for each workspace (from `useWorkspaces()`, sorted by name), a `DropdownMenuCheckboxItem` with the workspace name, `checked={scope === \`workspace:${ws.id}\`}`, `onSelect={() => onScopeChange(\`workspace:${ws.id}\`)}`.
    - `DropdownMenuLabel` "Projects" + for each project (from `useProjects()`), a `DropdownMenuCheckboxItem` with the project name, `checked={scope === \`project:${p.id}\`}`, `onSelect={() => onScopeChange(\`project:${p.id}\`)}`.
    Import `useProjects`, `useWorkspaces` from `"@/api/queries"`. Import `ChevronsUpDown` from `"lucide-react"`. Import the DropdownMenu primitives from `"@/components/ui/dropdown-menu"`. Import `ActivityScope` from `"@/lib/useActivityView"`. Add `aria-label={`Scope: ${displayName}`}` on the trigger (UI-SPEC Interaction Contract).

    **WindowToggle.tsx (per D-07):** Use default-variant `Tabs` (visually a segmented control — tabs.tsx renders bg-muted list with data-active:bg-background pill). Props: `{ window: ActivityWindow; onWindowChange: (w: ActivityWindow) => void }`. Render `<Tabs value={window} onValueChange={(v) => onWindowChange(v as ActivityWindow)}><TabsList><TabsTrigger value="week">Week</TabsTrigger><TabsTrigger value="month">Month</TabsTrigger></TabsList></Tabs>`. Import Tabs/TabsList/TabsTrigger from `"@/components/ui/tabs"`. Import `ActivityWindow` from `"@/lib/useActivityView"`.

    **StatsStrip.tsx (per D-08, D-09, D-11):** Props: `{ stats: StatsBlock }`. Render a section with heading `<h2 className="text-xs font-medium tracking-wide text-muted-foreground uppercase">{`Statistics`}</h2>` (the locked section-heading style). Then:
    - Count row: two readouts side by side — `<span className="text-2xl font-semibold tabular-nums">{stats.taskCount}</span>` + `<span className="text-xs text-muted-foreground">tasks</span>`, and similarly `reviewCount` + "reviews".
    - Three TimeStat rows (cycle, dwellInProgress, dwellInReview). Each row: label (`Cycle` / `In progress` / `In review` in `text-sm text-muted-foreground`), then if `stat.n === 0` render `<span className="font-mono text-muted-foreground">—</span>` (em-dash, D-11), else render `<span className="font-mono">{formatDuration(stat.min)} · {formatDuration(stat.median)} · {formatDuration(stat.max)}</span>`, then a caption `<span className="text-xs text-muted-foreground">over {stat.n} tasks</span>`. The em-dash guard MUST come before any formatDuration call (Pitfall 4 — never print 0m/0s).
    Import `StatsBlock`, `TimeStat` from `"@/api/types"`. Import `formatDuration` from `"@/lib/time"`. No cards grid, no charts (D-09 / Out of Scope ACTFUT-02).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - web/src/App.tsx contains `<Route path="/activity" element={<ActivityPage />} />` as a sibling of the /settings route (grep `/activity` in App.tsx returns a match)
    - ActivityPage is NOT wrapped in BoardWorkspaceSync (grep `BoardWorkspaceSync.*Activity` in App.tsx returns no match)
    - web/src/pages/ActivityPage.tsx exports default ActivityPage
    - web/src/pages/ActivityPage.tsx calls useActivityView() and useActivity(scope, window)
    - web/src/pages/ActivityPage.tsx renders the error+retry shell (contains "Couldn't load activity" and "Retry loading")
    - web/src/pages/ActivityPage.tsx renders loading Skeletons (contains Skeleton import)
    - web/src/components/activity/ScopeSelector.tsx renders Global + Workspaces + Projects sections with DropdownMenuCheckboxItem
    - web/src/components/activity/WindowToggle.tsx renders Tabs with Week/Month triggers
    - web/src/components/activity/StatsStrip.tsx renders counts + 3 TimeStat rows with formatDuration
    - StatsStrip guards `stat.n === 0` with em-dash BEFORE calling formatDuration (grep for `n === 0` returns a match in StatsStrip)
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/activity route renders the Activity page shell with the scope dropdown, window toggle, and stats strip; loading/error states mirror SettingsPage; the list sections have placeholders for Task 2</done>
</task>

<task type="auto">
  <name>Task 2: ActivityList, ReviewsList, wire lists into ActivityPage, sidebar entry</name>
  <files>web/src/components/activity/ActivityList.tsx, web/src/components/activity/ReviewsList.tsx, web/src/pages/ActivityPage.tsx, web/src/components/sidebar/ProjectSidebar.tsx</files>
  <read_first>
    - web/src/pages/ActivityPage.tsx (the page created in Task 1 — find the placeholder comments for tasks-done and reviews-done sections)
    - web/src/components/board/PRCard.tsx lines 166-176 (the external-link idiom — <a href target="_blank" rel="noreferrer">)
    - web/src/components/sidebar/ProjectSidebar.tsx (the SidebarFooter at lines ~162-192 where Settings gear lives; the project-row dual-surface pattern at lines 117-142; the footer's group-data-[collapsible=icon]:hidden at line 162 — D-02 wrinkle)
    - web/src/components/ui/tooltip.tsx (Tooltip/TooltipTrigger/TooltipContent — the sidebar entry wrapper)
    - web/src/components/ui/button.tsx (Button asChild variant="ghost" size="icon-sm" — the sidebar entry styling)
    - web/src/lib/time.ts (formatAgo — reuse for relative completion time in tasks-done entries)
    - web/src/api/types.ts (ActivityTask, ReviewDoneSummary, ActivityResponse wire types from Plan 01)
    - .planning/phases/11-activity-page-controls/11-PATTERNS.md (§ActivityList.tsx + §ReviewsList.tsx + §ProjectSidebar.tsx pattern assignments with full reference implementations; §Shared Patterns — External Link, Degrade Branch, Collapsed-Rail Dual-Surface)
    - .planning/phases/11-activity-page-controls/11-RESEARCH.md (§Pitfall 1 footer hide, §Pitfall 2 auth_required, §Pitfall 5 reviews sorting, §Pitfall 7 window.open vs <a>; §Code Examples — groupByProject, byCompletedDesc, ReviewsSection degrade branch)
    - .planning/phases/11-activity-page-controls/11-CONTEXT.md (D-01 sidebar footer entry, D-02 collapsed-rail reachable, D-03 lucide Activity icon, D-12 quietly empty reviews, D-13 grouped by project, D-14 universal PR URL, D-15 tasks click navigate)
    - .planning/phases/11-activity-page-controls/11-UI-SPEC.md (§Copywriting Contract — tasks-done entry, reviews-done entry, project group subheading, empty states; §Component Inventory — sidebar entry dual-surface)
  </read_first>
  <action>
    **ActivityList.tsx (tasks-done, per D-13, D-15, TASKS-02/03):**
    Props: `{ tasks: ActivityTask[] }`. If tasks.length === 0, render `<p className="text-sm text-muted-foreground">{`No tasks completed in this window.`}</p>` inside a section with heading "Tasks completed" (the locked section-heading style). Otherwise, bucket tasks by projectId using a `groupByProject` helper (a local function or module-level util):
    - `groupByProject<T extends { projectId: number; projectName: string }>(rows: T[])` — iterate rows, build a `Map<number, { projectId, projectName, entries: T[] }>`, return `[...map.values()]`. Tasks arrive already sorted `doneAt DESC` from the server SQL (verified in activity_test.go:180-257) — do NOT re-sort.
    - Render each group with a project-name subheading `<div className="text-xs font-medium text-muted-foreground">{group.projectName}</div>` (NOT uppercase — the uppercase-tracking-wide treatment is reserved for top-level section headings per UI-SPEC).
    - Render each task entry as a `<Link to={`/projects/${task.projectId}/tasks/${task.id}`}>` (D-15 — internal navigation). Inside the link: `<span className="flex-1 truncate text-sm">{task.title}</span>` and `<span className="text-xs text-muted-foreground">{formatAgo(task.doneAt, Date.now())}</span>`. Import `Link` from `"react-router"` and `formatAgo` from `"@/lib/time"`. Import `ActivityTask` from `"@/api/types"`.

    **ReviewsList.tsx (reviews-done, per D-12, D-13, D-14, ACT-04, REVIEWS-02/03):**
    Props: `{ reviews: ActivityResponse["reviews"] }`. Implement the degrade branch FIRST (D-12 + Pitfall 2):
    - Define `const HARD_DEGRADE: ReadonlySet<ActivityReviewState> = new Set(["disabled", "no_gh", "auth_required", "error"])`. The set MUST include `"auth_required"` — CONTEXT D-12 omits it but the backend emits it (Pitfall 2).
    - If `HARD_DEGRADE.has(reviews.state) && reviews.prs.length === 0`, return `null` — the ENTIRE reviews section is suppressed: no heading, no hint, no body. This is the literal "quietly empty" of D-12/ACT-04. Never render a toast (Phase 26 convention).
    - Otherwise (ok / partial / error-with-cached-prs): sort the prs by `completedAt DESC` client-side before grouping (Pitfall 5 — the server concatenates per-repo lists unsorted). `completedAt` is uniform second-precision ISO so lexical descending sort is chronological: `[...reviews.prs].sort((a, b) => b.completedAt.localeCompare(a.completedAt))`.
    - Group by projectId using the same `groupByProject` helper (can be duplicated locally or extracted — agent's call). If groups.length === 0, return `null` (quiet empty even on ok+empty, per RESEARCH Open Q1).
    - Render a section with heading "Reviews completed". For each group: project-name subheading (same style as ActivityList), then each entry as a plain `<a href={review.url} target="_blank" rel="noreferrer" aria-label={`Open PR #${review.number} on GitHub`}>` containing `<span className="font-mono">#{review.number}</span> {review.title}` (D-14 — universal PR-URL fallback, NOT an in-app workspace lookup) and a relative time `<span className="text-xs text-muted-foreground">{formatAgo(review.completedAt, Date.now())}</span>`. NEVER use `window.open` (Pitfall 7 — popup-blocked). NEVER use a router `<Link>` (it is an external URL). Import `ActivityReviewState`, `ReviewDoneSummary`, `ActivityResponse` from `"@/api/types"`. Import `formatAgo` from `"@/lib/time"`.

    **Wire lists into ActivityPage.tsx (modify Task 1's file):**
    Replace the two placeholder comments (`{/* Tasks-done section — wired in Task 2 */}` and `{/* Reviews-done section — wired in Task 2 */}`) with:
    - `<ActivityList tasks={data.tasks} />`
    - `<ReviewsList reviews={data.reviews} />`
    Add imports for ActivityList and ReviewsList from `"@/components/activity/ActivityList"` and `"@/components/activity/ReviewsList"`.

    **ProjectSidebar.tsx sidebar entry (per D-01, D-02, D-03, ACT-01):**
    Add `Activity` to the existing lucide-react import (line 3 — currently imports `Plus, Settings`; add `Activity`). Add the Activity entry in the SidebarFooter (next to the Settings gear). The CRITICAL wrinkle (D-02 / Pitfall 1): the SidebarFooter at line 162 carries `group-data-[collapsible=icon]:hidden` which hides the WHOLE footer when the sidebar is collapsed. Activity must NOT be swallowed by this hide — it must stay reachable in the collapsed rail. The cleanest fix: remove `group-data-[collapsible=icon]:hidden` from the SidebarFooter's className (line 162) and instead apply it per-child — keep it on the "Add project" button and the Settings gear (their current behavior — they hide in the rail), and leave the Activity button WITHOUT it so it survives the collapse. Alternatively, render the Activity entry as a dual-surface button applying the project-row's rail/expanded pair classes (ProjectSidebar.tsx:117,140): rail icon `hidden group-data-[collapsible=icon]:flex` + expanded label `group-data-[collapsible=icon]:hidden`. Either structure satisfies D-02 — the locked behavior is "always reachable in both states."
    The entry itself: a `<Tooltip>` wrapping `<TooltipTrigger asChild>` wrapping `<Button asChild variant="ghost" size="icon-sm" className={cn(location.pathname === "/activity" && "bg-sidebar-accent text-sidebar-accent-foreground")}><Link to="/activity" aria-label="Activity"><Activity className="size-4" /></Link></Button></TooltipTrigger>` with `<TooltipContent side="right">Activity</TooltipContent>`. The active-state class derivation uses `location.pathname === "/activity"` — the Settings-gear idiom (ProjectSidebar.tsx:180-183). Import `cn` from `"@/lib/utils"` if not already imported. Ensure `useLocation` is already imported (it is — the Settings gear uses it).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit 2>&1 | head -20 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/activity/ActivityList.tsx exports ActivityList; groups tasks by projectId; each entry is a Link to /projects/:projectId/tasks/:taskId (D-15)
    - web/src/components/activity/ReviewsList.tsx exports ReviewsList; contains a HARD_DEGRADE set including "auth_required" (grep `auth_required` in ReviewsList.tsx returns a match — Pitfall 2)
    - ReviewsList returns null when HARD_DEGRADE.has(state) && prs.length === 0 (D-12 quietly empty)
    - ReviewsList sorts prs by completedAt DESC via localeCompare (grep `completedAt` returns a sort match — Pitfall 5)
    - ReviewsList uses plain <a href={url} target="_blank" rel="noreferrer"> for entries (grep `rel="noreferrer"` returns a match; grep `window.open` returns 0 matches — Pitfall 7)
    - web/src/pages/ActivityPage.tsx imports and renders ActivityList and ReviewsList (the placeholder comments are gone)
    - web/src/components/sidebar/ProjectSidebar.tsx imports Activity from lucide-react (grep `Activity` in the lucide import line returns a match)
    - ProjectSidebar.tsx renders a Link to "/activity" with aria-label="Activity" (D-01/D-03)
    - The Activity entry's active state uses location.pathname === "/activity" (grep `/activity` in ProjectSidebar.tsx returns a match)
    - The Activity entry is NOT hidden by group-data-[collapsible=icon]:hidden (either the footer class was removed and re-applied per-child, or the entry uses dual-surface classes — verify by grep: the Activity button element does NOT have an ancestor-level hide that swallows it)
    - `cd web && npx tsc --noEmit` exits 0
    - `cd web && npx vite build` exits 0 (full build passes)
  </acceptance_criteria>
  <done>Tasks-done and reviews-done lists render grouped by project with correct click-through (internal navigate for tasks, external PR link for reviews); reviews degrade quietly on hard-degrade states; the Activity sidebar entry is reachable in both expanded and collapsed sidebar states</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → DOM | Activity data from GET /api/activity (task titles, PR titles, project names, PR URLs) is rendered in the browser |
| external link click | ReviewDoneSummary.url opens in a new browser tab via an <a> element |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | ReviewDoneSummary.url rendered as href | medium | mitigate | The server (Phase 10) sources url from `gh pr list --json url` (always https GitHub). Add a defensive `url.startsWith("https://")` guard before rendering as href — cheap belt-and-suspenders against a hypothetical server compromise. Never render a `javascript:` URL. (RESEARCH §Security V5) |
| T-11-02 | Tampering | task.title / review.title / projectName rendered in JSX | low | mitigate | React auto-escapes all interpolated content. Never use `dangerouslySetInnerHTML`. The titles originate from the local Kamacu DB / gh — rendered as text nodes, not HTML. |
| T-11-03 | Information Disclosure | external link to GitHub PR | low | mitigate | `rel="noreferrer"` on all external `<a>` elements prevents leaking the referrer (and thus the localhost origin) to GitHub. Established PRCard.tsx:166 idiom. |
| T-11-04 | Tampering | kamacu.activity.scope localStorage | low | accept | Single-user localhost — no privilege boundary. Plan 01's readScope validates the grammar; backend parseScope rejects malformed values with HTTP 400. |
| T-11-SC | Tampering | npm installs | high → N/A | mitigate | This plan installs ZERO new packages (all deps already in package.json per RESEARCH §Package Legitimacy Audit). The gate is trivially satisfied. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` exits 0 — all new and modified files type-check
- `cd web && npx vite build` exits 0 — the full production build compiles (the embed path depends on dist/)
- Manual (at UAT): open /activity in the browser — the sidebar entry is visible in both expanded and collapsed (Ctrl+B) states; the scope dropdown lists Global + workspaces + projects; the Week/Month toggle switches the window; stats render with em-dash on empty; tasks-done entries navigate to the task view; reviews-done entries open the PR in a new tab; with GitHub integration off, the reviews section is absent while tasks and stats display normally
</verification>

<success_criteria>
- All 8 Phase 11 requirements (ACT-01..04, TASKS-02/03, REVIEWS-02/03) are functionally delivered
- The Activity page is reachable from the sidebar in both expanded and collapsed states
- Scope/window controls drive the single useActivity query with one loading state
- Reviews degrade silently when GitHub integration is off (no toast, no hint, no heading)
- The production build (`vite build`) passes — the Go binary's embed.FS can consume the dist/ output
</success_criteria>

<output>
Create `.planning/phases/11-activity-page-controls/11-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 02)

**New components:**
- `ActivityPage` — web/src/pages/ActivityPage.tsx (default export; page shell + control bar + section composition)
- `ScopeSelector` — web/src/components/activity/ScopeSelector.tsx (Global/Workspace/Project dropdown)
- `WindowToggle` — web/src/components/activity/WindowToggle.tsx (Week/Month segmented control via Tabs)
- `StatsStrip` — web/src/components/activity/StatsStrip.tsx (counts + 3 TimeStat rows with em-dash guard)
- `ActivityList` — web/src/components/activity/ActivityList.tsx (tasks-done grouped by project, click→navigate)
- `ReviewsList` — web/src/components/activity/ReviewsList.tsx (reviews-done grouped by project, degrade-aware, click→external PR)

**New helpers (module-level in the components that use them):**
- `groupByProject<T>(rows)` — bucketing util in ActivityList.tsx (and/or ReviewsList.tsx)
- `byCompletedDesc(a, b)` — sort comparator in ReviewsList.tsx
- `HARD_DEGRADE: ReadonlySet<ActivityReviewState>` — degrade-state set in ReviewsList.tsx

**Modified exports:**
- `/activity` route — web/src/App.tsx (new Route element)
- Activity sidebar entry — web/src/components/sidebar/ProjectSidebar.tsx (new Link + lucide Activity icon import)

**New file paths:**
- `web/src/pages/ActivityPage.tsx`
- `web/src/components/activity/ScopeSelector.tsx`
- `web/src/components/activity/WindowToggle.tsx`
- `web/src/components/activity/StatsStrip.tsx`
- `web/src/components/activity/ActivityList.tsx`
- `web/src/components/activity/ReviewsList.tsx`
