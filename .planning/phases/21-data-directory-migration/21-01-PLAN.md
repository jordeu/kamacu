---
phase: 21-data-directory-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/migrateStorage.ts
  - web/src/main.tsx
  - web/src/components/layout/AppLayout.tsx
  - web/src/components/layout/ActiveSessionsBar.tsx
  - web/src/components/board/ReviewColumn.tsx
autonomous: true
requirements: [MIGRATE-04]

must_haves:
  truths:
    - "On app boot, every kangent-prefixed localStorage value is copied to the kamacu-prefixed key when that key is absent"
    - "The sidebar, sessions-bar, and review-collapsed components read/write kamacu-prefixed keys"
    - "The copy runs once and is a cheap no-op on subsequent boots"
  artifacts:
    - path: "web/src/lib/migrateStorage.ts"
      provides: "One-shot prefix-scan localStorage migration (implements D-14)"
      exports: ["migrateStorage"]
    - path: "web/src/main.tsx"
      provides: "migrateStorage() invoked before createRoot"
      contains: "migrateStorage"
  key_links:
    - from: "web/src/main.tsx"
      to: "migrateStorage"
      via: "call before createRoot"
      pattern: "migrateStorage\\(\\)"
    - from: "web/src/components/layout/AppLayout.tsx"
      to: "localStorage"
      via: "kamacu.sidebar key"
      pattern: "kamacu\\.sidebar"
---

<objective>
Migrate browser `localStorage` from the `kangent` prefix to `kamacu` (MIGRATE-04) and flip
the three code literals that read/write those keys, so sidebar state, the sessions-bar
collapse preference, and per-project review-collapse state carry over the rebrand.

Purpose: Phase 20 deliberately left these runtime-compat strings as `kangent`. Without this
plan, the copy would be pointless — the app would keep reading `kangent.*` while the migration
populated `kamacu.*` that nothing consumes (RESEARCH assumption A4, resolved IN SCOPE).
Output: `web/src/lib/migrateStorage.ts` (new) + a call site in `main.tsx` + three key-literal flips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-data-directory-migration/21-RESEARCH.md
@.planning/phases/21-data-directory-migration/21-PATTERNS.md

<interfaces>
<!-- The three live localStorage keys (grep-verified). They are NOT uniformly `kangent.*`:
     two separators (dot AND colon) plus a dynamic per-project suffix — a prefix scan over
     ALL keys is mandatory (RESEARCH Pitfall 3). -->
- web/src/components/layout/AppLayout.tsx:8   `const SIDEBAR_STORAGE_KEY = "kangent.sidebar";`
- web/src/components/layout/ActiveSessionsBar.tsx:33  `const storageKey = "kangent:sessions-bar-collapsed";`
- web/src/components/board/ReviewColumn.tsx:56  `const storageKey = \`kangent:review-collapsed:${projectId}\`;`

Existing getItem/setItem idiom to mirror (AppLayout.tsx:8,14):
  localStorage.getItem(SIDEBAR_STORAGE_KEY) !== "false"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-shot prefix-scan localStorage migration + call site</name>
  <files>web/src/lib/migrateStorage.ts, web/src/main.tsx</files>
  <read_first>
    - web/src/main.tsx (current entry — createRoot at line 17; the migration call goes immediately above it, before any component renders and reads a key)
    - .planning/phases/21-data-directory-migration/21-RESEARCH.md §"localStorage prefix-scan migration (D-14 / Pitfall 3)" (the verified algorithm) and §"Pitfall 3" (why a hard-coded key list misses 2 of 3 keys)
    - .planning/phases/21-data-directory-migration/21-PATTERNS.md §"web/src/lib/migrateStorage.ts" (the shape, idempotency guard, and call-site placement)
    - web/src/components/layout/AppLayout.tsx (the existing localStorage read idiom the migration must be compatible with)
  </read_first>
  <action>
    Create `web/src/lib/migrateStorage.ts` exporting `migrateStorage(): void`. Behavior per D-14:
    guard on a one-shot flag `kamacu.storage-migrated === "1"` (return early if set). Iterate ALL
    localStorage keys by index via `localStorage.length` + `localStorage.key(i)`. For any key whose
    name starts with the separator-agnostic prefix `"kangent"` (matches `kangent.sidebar`,
    `kangent:sessions-bar-collapsed`, AND the dynamic `kangent:review-collapsed:<projectId>`), compute
    `newKey = "kamacu" + key.slice("kangent".length)` and, only when `localStorage.getItem(newKey)` is
    `null`, copy the value across with `setItem`. After the loop, set `kamacu.storage-migrated` to `"1"`.
    Do NOT delete the old keys (harmless residue; keeping them makes the copy re-runnable if the guard
    flag is ever cleared). In `web/src/main.tsx`, import and call `migrateStorage()` on the line
    immediately ABOVE `createRoot(...)` so the new keys exist before the first React render reads them.
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/migrateStorage.ts` exports `migrateStorage` and contains `localStorage.key(` (index scan, not a hard-coded list) and `.startsWith("kangent")`
    - `web/src/main.tsx` imports and calls `migrateStorage()` textually before the `createRoot(` call
    - The guard key literal `kamacu.storage-migrated` is present
    - `cd web && npm run build` exits 0 (tsc -b + vite build); `npm run lint` exits 0
  </acceptance_criteria>
  <done>migrateStorage exists, is prefix-scan (not a fixed list), is idempotently guarded, and is called once before createRoot; build + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the three localStorage key literals to kamacu</name>
  <files>web/src/components/layout/AppLayout.tsx, web/src/components/layout/ActiveSessionsBar.tsx, web/src/components/board/ReviewColumn.tsx</files>
  <read_first>
    - web/src/components/layout/AppLayout.tsx (line 8: SIDEBAR_STORAGE_KEY)
    - web/src/components/layout/ActiveSessionsBar.tsx (line 33: storageKey)
    - web/src/components/board/ReviewColumn.tsx (line 56: storageKey template literal)
    - .planning/phases/21-data-directory-migration/21-PATTERNS.md §"AppLayout.tsx / ActiveSessionsBar.tsx / ReviewColumn.tsx (key-literal flips)"
  </read_first>
  <action>
    Change only the string literals (self-analog — nothing else in these files moves):
    `"kangent.sidebar"` → `"kamacu.sidebar"` (AppLayout.tsx:8);
    `"kangent:sessions-bar-collapsed"` → `"kamacu:sessions-bar-collapsed"` (ActiveSessionsBar.tsx:33);
    the template literal `` `kangent:review-collapsed:${projectId}` `` → `` `kamacu:review-collapsed:${projectId}` `` (ReviewColumn.tsx:56).
    Preserve the exact separator shape of each key (dot for sidebar, colon for the other two) so the
    Task-1 copy (`newKey = "kamacu" + key.slice(7)`) lands on precisely these names. Do NOT touch the
    comment at web/src/api/types.ts:19 (it references `~/.kangent/repos/` as prose about managed repos,
    not a localStorage key).
  </action>
  <verify>
    <automated>cd web && npm run build && npm run lint && ! grep -rnE 'kangent[.:]' web/src/components/ web/src/lib/</automated>
  </verify>
  <acceptance_criteria>
    - No `kangent.`/`kangent:` KEY literal (e.g. `kangent.sidebar`, `kangent:sessions-bar-collapsed`, `kangent:review-collapsed:`) remains under `web/src/components/` or `web/src/lib/`. The grep uses `grep -rnE 'kangent[.:]'`, which targets ONLY the flipped key literals; the separator-agnostic scan literals in `migrateStorage.ts` (bare `kangent` with NO `.`/`:` suffix — `.startsWith("kangent")` and `key.slice("kangent".length)`) are legitimately retained and are correctly excluded by the `[.:]` pattern
    - The three files contain `kamacu.sidebar`, `kamacu:sessions-bar-collapsed`, and `kamacu:review-collapsed:` respectively
    - `cd web && npm run build` exits 0; `npm run lint` exits 0
  </acceptance_criteria>
  <done>All three components read/write the kamacu-prefixed keys; build + lint green; no `kangent.`/`kangent:` key literal left in components or lib (the bare-word `kangent` scan literals in migrateStorage.ts are intentional and excluded by the `[.:]` guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage (per-origin) | Client-only state for `localhost:7333`; never crosses the network; read/written only by this SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Tampering | migrateStorage copy loop | accept | Values are the app's own prior state, copied verbatim within the same origin and read back by the same components; no parsing/eval, no injection surface. Low risk on a single-user local tool. |
| T-21-04-02 | DoS (self) | re-running the copy | mitigate | `kamacu.storage-migrated` guard flag + `getItem(newKey) === null` check make the copy idempotent and a no-op after the first boot. |
| T-21-04-SC | Tampering | npm installs | mitigate | No packages added this phase (pure source edits); nothing to audit. |
</threat_model>

<verification>
- `cd web && npm run build` (tsc -b + vite build) exits 0
- `cd web && npm run lint` exits 0
- `grep -rnE 'kangent[.:]' web/src/components/ web/src/lib/` returns nothing. The `[.:]` pattern matches only the flipped key literals (`kangent.sidebar`, `kangent:sessions-bar-collapsed`, `kangent:review-collapsed:`); the bare-word `kangent` scan literals in `migrateStorage.ts` are intentional and are excluded by the pattern, so this guard does not contradict Task 1.
- Runtime carry-over (kangent.* value → kamacu.* key populated on first load) is verified end-to-end in Plan 21-05's browser checkpoint against a real install.
</verification>

<success_criteria>
- MIGRATE-04: browser `localStorage` keys under `kangent.*`/`kangent:*` are migrated to the `kamacu`
  prefix on app boot, and the components now read the new keys — sidebar state, sessions-bar collapse,
  and per-project review-collapse carry over.
- The migration is one-shot and idempotent (guard flag).
</success_criteria>

<output>
Create `.planning/phases/21-data-directory-migration/21-01-SUMMARY.md` when done.
</output>
