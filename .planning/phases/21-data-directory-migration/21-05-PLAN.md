---
phase: 21-data-directory-migration
plan: 05
type: execute
wave: 4
depends_on: [21-01, 21-04]
files_modified: []
autonomous: false
requirements: [MIGRATE-01, MIGRATE-02, MIGRATE-03, MIGRATE-04, MIGRATE-05]

must_haves:
  truths:
    - "Against a copy of the real ~/.kangent install, first boot migrates the dir to ~/.kamacu and the app serves against it"
    - "After migration the board loads, a task view opens, a dead agent resumes, and a diff renders"
    - "A migrated worktree is a clean git status (worktree links repaired, DB paths rewritten)"
    - "A second boot is a clean no-op — the dir stays ~/.kamacu and no re-migration is logged"
    - "Browser localStorage kamacu.* keys are populated from the old kangent.* values"
  artifacts: []
  key_links: []
---

<objective>
Run the milestone's highest-risk verification: a live end-to-end migration against a COPY of the real
`~/.kangent` install (9 managed repos, 29 task worktrees, tmux rows, a hot 2.3 MB WAL), then a human
walkthrough confirming the board, a task view, an agent resume, a diff, a clean worktree `git status`,
a fresh `kamacu-*` shell, and a no-op second boot — covering all of MIGRATE-01..05 as the phase gate.

Purpose: STATE.md flags Phase 21 as the highest-risk phase and demands a real restart/reattach
verification, not just unit tests. RESEARCH OQ3 resolves the safety concern: because the D-13 gate keys
on `ExpandHome("~")`, we exercise the REAL default-path gate and REAL data by pointing HOME at a copied
throwaway home, so the actual 5.5 GB `~/.kangent` install is never at risk.
Output: a verified migration + a human-verify sign-off (no source files changed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-data-directory-migration/21-RESEARCH.md
@.planning/phases/21-data-directory-migration/21-CONTEXT.md

<interfaces>
RESEARCH Open Question 3 (the safe-UAT recipe, on-host verified):
  cp -a ~/.kangent  <TMPHOME>/.kangent
  HOME=<TMPHOME>  ./bin/kamacu        # exercises the real default-path gate against real data
Gate keys on ExpandHome("~") -> HOME override is the whole isolation mechanism.
Success log line (D-10): slog.Info "migrated ~/.kangent -> ~/.kamacu" appears exactly once (first boot only).
Live install shape (RESEARCH Runtime State Inventory): 9 projects (all managed=1), 29 task
worktrees, 8 kangent-* tmux rows, hot WAL. Post-migration expectations: repo_path/worktree_path rows
now under <TMPHOME>/.kamacu; git worktree list clean per repo; zero kangent-* tmux rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated migration smoke against a copied HOME</name>
  <files></files>
  <read_first>
    - .planning/phases/21-data-directory-migration/21-RESEARCH.md §"Open Questions" OQ3 (the copied-HOME recipe) and §"Runtime State Inventory" (the exact expected row shape to assert)
    - .planning/phases/21-data-directory-migration/21-CONTEXT.md D-10 (silent success line), D-11 (refuse-to-boot on anomaly)
    - cmd/kamacu/main.go (the startup sequence: migration runs before ListenAndServe, so a brief run + kill is enough to trigger + observe it)
    - Makefile (the `make build` target that produces ./bin/kamacu)
  </read_first>
  <action>
    Build the binary (make build -> ./bin/kamacu). Create a throwaway home under the scratchpad
    (TMPHOME) and copy the real install into it with `cp -a "$HOME/.kangent" "$TMPHOME/.kangent"` (never
    mutate the real ~/.kangent). BOOT 1: launch `HOME="$TMPHOME" ./bin/kamacu` in the background, wait for
    either the "migrated ~/.kangent -> ~/.kamacu" slog line or a successful GET /api/healthz, then stop the
    process. Assert: `$TMPHOME/.kamacu` exists and `$TMPHOME/.kangent` is gone; `$TMPHOME/.kamacu/kamacu.db`
    exists and `kangent.db` does not; query the copied DB (sqlite3 or a tiny go/sql check) to assert every
    managed projects.repo_path and tasks.worktree_path now begins with `$TMPHOME/.kamacu` and NONE begins
    with `$TMPHOME/.kangent`, and `SELECT COUNT(*) FROM tmux_sessions WHERE name LIKE 'kangent-%'` is 0;
    for one managed repo run `git -C "$TMPHOME/.kamacu/repos/<owner>/<name>" worktree list` and assert it
    is clean (no "prunable"/broken entries) and `git -C <a migrated worktree> status` succeeds. BOOT 2:
    relaunch `HOME="$TMPHOME" ./bin/kamacu`, wait for healthz, stop it, and assert the "migrated" line does
    NOT appear again and the dir is still `$TMPHOME/.kamacu` (idempotent no-op — MIGRATE-05). Keep the
    whole script in the scratchpad; it modifies nothing under the repo or the real HOME.
  </action>
  <verify>
    <automated>make build && test -x ./bin/kamacu && echo "binary built; run the copied-HOME smoke per action"</automated>
  </verify>
  <acceptance_criteria>
    - Boot 1 logs "migrated ~/.kangent -> ~/.kamacu" exactly once; $TMPHOME/.kamacu exists and $TMPHOME/.kangent is gone
    - The copied kamacu.db has every managed repo_path and worktree_path under $TMPHOME/.kamacu and zero rows LIKE 'kangent-%' in tmux_sessions
    - `git worktree list` for a migrated managed repo is clean and `git status` in a migrated worktree succeeds
    - Boot 2 does NOT log the migration line and leaves the dir at $TMPHOME/.kamacu (clean no-op)
    - The real ~/.kangent is byte-for-byte untouched throughout (the script only reads it via cp -a)
  </acceptance_criteria>
  <done>A copied-HOME first boot migrates the real data to ~/.kamacu with repaired worktrees and rewritten DB paths; a second boot is a verified no-op; the real install is never touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the live migration end-to-end (copied HOME)</name>
  <files></files>
  <action>
    STOP for the human. Task 1 already automated everything Claude can (build, copy the real install into
    a throwaway HOME, boot, assert dir move / DB path rewrite / worktree repair / tmux-row cleanup / no-op
    second boot). This task is the human confirmation Claude cannot perform: drive the migrated instance in
    a browser and confirm the board, agent resume, fresh kamacu-* shell, diff, clean worktree git status,
    and carried-over localStorage keys. Present the how-to-verify steps below and wait for the resume signal.
  </action>
  <read_first>
    - .planning/phases/21-data-directory-migration/21-CONTEXT.md D-06 (claude --resume on reopen), D-07 (shells respawn fresh), D-11 (refuse-to-boot anomaly)
    - .planning/phases/21-data-directory-migration/21-RESEARCH.md §"Open Questions" OQ3 (the safe copied-HOME UAT the human drives)
  </read_first>
  <what-built>
    The full ~/.kangent -> ~/.kamacu migration (dir move, worktree repair, DB path rewrite, tmux
    socket/prefix switch, localStorage carry-over) plus the automated copied-HOME smoke from Task 1.
  </what-built>
  <how-to-verify>
    Against the migrated copied-HOME instance from Task 1 (relaunch `HOME="$TMPHOME" ./bin/kamacu` and
    open http://127.0.0.1:7333 in the browser):
    1. The board loads and shows the existing projects and their tasks (MIGRATE-01/02).
    2. Open a task that had an agent session — the agent view opens and, on interaction, the agent
       resumes via `claude --resume` (no orphaned/dead agent; MIGRATE-03/D-06). The transcript is intact.
    3. Open a bash tab on a task — a FRESH shell spawns (scrollback reset is expected, D-07) and, via
       `tmux -L kamacu list-sessions` in another terminal, its name is `kamacu-<task>-<n>` (MIGRATE-03).
    4. Open a task's Diff tab — the diff renders (worktree paths resolve; MIGRATE-02).
    5. In a terminal, `git -C "$TMPHOME/.kamacu/worktrees/<...>" status` on an opened task's worktree is
       clean/normal (links repaired; MIGRATE-02).
    6. In the browser devtools Application -> Local Storage, confirm `kamacu.sidebar`,
       `kamacu:sessions-bar-collapsed`, and any `kamacu:review-collapsed:<id>` keys are present and
       carried the old values (MIGRATE-04).
    7. Confirm `tmux -L kangent list-sessions` reports no server / no kangent-* sessions (old socket
       retired; MIGRATE-03).
    8. Optionally confirm the both-dirs-present anomaly: create an empty `$TMPHOME/.kangent` alongside
       `$TMPHOME/.kamacu`, boot once, and confirm it refuses to start with a clear terminal error naming
       both dirs (MIGRATE-05/D-11) — then remove the empty dir.
    When done, clean up: remove the scratchpad TMPHOME. The real ~/.kangent must be untouched.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the board loads with the migrated projects/tasks
    - An agent resumes on reopen with its transcript intact; a fresh bash tab spawns as a kamacu-<task>-<n> session on -L kamacu
    - A diff renders and a migrated worktree's `git status` is clean/normal
    - kamacu.sidebar / kamacu:sessions-bar-collapsed / kamacu:review-collapsed:<id> localStorage keys are present with carried values
    - `tmux -L kangent list-sessions` shows the old socket retired; (optional) the both-dirs anomaly refuses to boot with a clear error
  </acceptance_criteria>
  <resume-signal>Type "approved" once the board, agent resume, fresh kamacu-* shell, diff, clean worktree, and migrated localStorage keys all check out — or describe what failed (feeds a --gaps replan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real ~/.kangent install (5.5 GB) | Must never be mutated by verification; only read via `cp -a` into a throwaway HOME |
| copied HOME (TMPHOME) | The migration acts on this copy; the D-13 gate keys on HOME so isolation is complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-07 | Tampering (real data) | verification touching the live install | mitigate | All boots run with HOME overridden to a copied throwaway home (RESEARCH OQ3); the real ~/.kangent is only ever read by `cp -a`; acceptance criteria assert it stays untouched |
| T-21-05-08 | DoS (false pass) | clean-close hides the hot-WAL bug | mitigate | Verify against a COPY of the live install which carries the real hot WAL (RESEARCH: a test that only checks a clean close would miss Pitfall 1); assert row integrity post-migration |
| T-21-05-09 | DoS (anomaly not caught) | both-dirs-present at runtime | mitigate | Optional step 8 exercises the refuse-to-boot anomaly path end-to-end (D-11) |
| T-21-05-SC | Tampering | go module installs | mitigate | No new dependencies — verification only runs the built binary + git/sqlite/tmux CLIs |
</threat_model>

<verification>
- Task 1 automated smoke passes (migrate on boot 1, no-op on boot 2, DB paths + worktrees + tmux rows correct)
- Human checkpoint approved: board loads, agent resumes, fresh kamacu-* shell, diff renders, worktree git status clean, kamacu.* localStorage populated, old -L kangent server retired
- The real ~/.kangent install is confirmed untouched
</verification>

<success_criteria>
- All five phase success criteria are demonstrated live against real data: the dir moves and the app
  runs against ~/.kamacu (MIGRATE-01); worktrees stay valid and diffs/task views open (MIGRATE-02); no
  agent is orphaned and new sessions are kamacu-* on -L kamacu (MIGRATE-03); localStorage carries over
  (MIGRATE-04); a second boot is a clean no-op and the anomaly refuses to boot (MIGRATE-05).
</success_criteria>

<output>
Create `.planning/phases/21-data-directory-migration/21-05-SUMMARY.md` when done.
</output>
