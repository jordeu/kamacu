// Stub — replaced with the AlertDialog confirmation in Task 2 of plan 01-06.
interface DeleteTaskDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  taskId: number;
  taskTitle: string;
  projectId: number;
}

export function DeleteTaskDialog(_props: DeleteTaskDialogProps) {
  return null;
}
