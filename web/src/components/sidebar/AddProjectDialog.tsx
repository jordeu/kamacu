export interface AddProjectDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

// Stub — implemented in Task 2 (add-project dialog with path validation UX).
export function AddProjectDialog(_props: AddProjectDialogProps) {
  return null;
}
