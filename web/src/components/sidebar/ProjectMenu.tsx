import type { Project } from "@/api/types";

export interface ProjectMenuProps {
  project: Project;
}

// Stub — implemented in Task 2 (rename / delete-with-confirm dropdown).
export function ProjectMenu(_props: ProjectMenuProps) {
  return null;
}
